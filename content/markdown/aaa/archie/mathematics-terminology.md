# Mathematical Terminology (Canonical Dialect)

This table lists the symbols and mathematical terms used across the Geometrical Model of Nature and explains their meaning in this project’s context.

Treat this as the legend for the equations. When a chapter uses a symbol such as $T$, $\mathbf X$, $g_{\mu\nu}^{\mathrm{eff}}$, $\xi$, or $\chi_{\text{sea}}$, this page records the intended layer and prevents nearby standard notation from taking over by habit.

| Symbol / Term | Meaning in this project | Extended explanation |
| --- | --- | --- |
| $\mathcal{M} = \mathbb{R} \times \mathbb{R}^3$ | Absolute timespace: formal product of absolute time and Euclidean 3-space; no non-degenerate 4D metric. | Treat as a product manifold with a global time coordinate and a flat 3D Riemannian metric; differential operators split into absolute-time derivatives $d/dT$ and spatial operators on slices $\Sigma_T$. Worldlines are ODEs in $T$, and fields are distributions on $\Sigma_T$ parametrized by $T$. Use `absolute timespace` when the formal product structure matters; otherwise name the factors directly as `absolute time` and `Euclidean void`. |
| $T$ | Absolute time (dimensionless after choosing $T_0$); increases globally. | Single global independent variable for native dynamics. After selecting $T_0$, $T$ is dimensionless; derivatives with respect to $T$ define native velocity $\mathbf V$ and native acceleration $\mathbf A$. Monotonicity enforces causal order and precludes closed time loops. |
| $P$, $P_b$, $P_q$, $P_0$ | Cycle period, branch periods, and reference period. | Use $P$ for the duration of one declared cycle and retain the role subscript when needed: $P_b$ for branch $b$, $P_q$ for clock branch $q$, and $P_0$ for its declared reference period. State the clock or time coordinate in which the duration is measured. A period is not the absolute-time coordinate $T$, an epoch $T_0$, or an arbitrary normalization scale. Use $P_{\mathrm{cyc}}$ when another meaning of $P$ coexists; preserve independently defined probability, pressure, momentum, and map notation. |
| $P_{\mathrm{ret}}$, $P_{\mathbf k}$, $P_N$, $P_{N0}$ | Complete-history return, winding-indexed period, and local/reference Noether sea cadence periods. | These are role-qualified members of the period family. Specify what returns: one projected signal, a reduced shape, or the complete delayed history. Equal dimensions do not make those return conditions equivalent. The reference cadence obeys $P_{N0}=2\pi/\Omega_{N0}$ when $\Omega_{N0}$ is the corresponding angular frequency. |
| $\mathbb{U}_{\text{now}} \equiv S(T)$ | Complete ontic universe state on simultaneity slice $\Sigma_T$ at global time $T$. | Canonical symbol for the "state of the universe now": $S(T)=\{(\mathbf X_i(T),\mathbf V_i(T),q_i,\ldots)\}$, including required path-history/provenance bookkeeping and self-hit history for deterministic evolution. This is simulation ground truth and is not an observer. |
| $\Sigma_T$ | Simultaneity slice at time $T$: $\{T\} \times \mathbb{R}^3$. | A 3D hypersurface carrying $h_{ij}$ and volume element $dV$; gradients, divergences, and Laplacians act within $\Sigma_T$. Absolute-timespace integrals use $dT\,dV$. |
| $h_{ij} = \delta_{ij}$ | Euclidean spatial metric on $\Sigma_T$; implements dot products and norms. | Flat metric used for inner products and norms; in spherical coordinates it becomes $\mathrm{diag}(1,r^2,r^2\sin^2\theta)$. Raising/lowering is trivial with $h^{ij}=\delta^{ij}$. |
| $\mathbf X,\ \mathbf X_i(T)$ | Native spatial position vector in the Euclidean void. | Spatial vectors valued in $\mathbb{R}^3$ on the absolute slice $\Sigma_T$. The worldline $\mathbf X_i(T)$ is assumed absolutely continuous so $\mathbf V_i=d\mathbf X_i/dT$ exists a.e.; do not assemble native positions into substrate 4-vectors. |
| $\mathbf V=d\mathbf X/dT,\ \mathbf A=d\mathbf V/dT$ | Native velocity and acceleration vectors. | Kinematic derivatives with respect to absolute time $T$. Under impulsive acceleration contributions $\mathbf V$ has bounded variation; with mollification ($\eta>0$) both $\mathbf V$ and $\mathbf A$ are continuous and classical ODE theory applies. |
| $t_{\mathrm{eff}},\ x_{\mathrm{eff}}^i$ | Effective observer-coordinate time and spatial chart coordinates. | Use these only after the derivation has moved into an observer-level or GR-comparison chart. They may have the same physical dimensions as $T$ and $X^i$, but they are not the same variables. Bare $t$ and $x^i$ may appear only in a labeled standard comparison form for recognition or source matching. They must not become working symbols in student-facing prose; translate them immediately into $t_{\mathrm{eff}},x_{\mathrm{eff}}^i$ or $T,\mathbf X$ before interpretation, diagramming, or term-by-term mapping. |
| $dt_{\mathrm{eff}},\ dx_{\mathrm{eff}}^i,\ \partial_{t_{\mathrm{eff}}}$ | Effective-coordinate differentials and time derivative. | Use these in effective metric, ADM, PPN, Shapiro, lensing, redshift, and observer-clock equations. Do not substitute them for $dT$, $dX^i$, or $\partial_T$ without an explicit coordinate-layer map. |
| $\tau,\ d\tau$ | Derived physical-clock time and its differential. | Proper time is a standard relativity bridge term for a clock readout extracted from physical clocks. In native prose, define it as `derived clock time`, `clock map`, or `extracted clock observable`, not as substrate time. Write $d\tau/dt_{\mathrm{eff}}$ for observer-coordinate clock-rate comparisons and $d\tau/dT$ only when a native clock-map derivation has been declared. |
| $T_W$ | Record window: the finite interval over which a measurement record is formed, evaluated, and required to persist for a declared channel. | The observation-interval symbol in the record packet $(\mathcal{K}_A,\mathcal{Q},W,T_W,\{R_k\},\mu_{*,T_W})$; it is the horizon of the finite-window basin measure $\mu_{*,T_W}$ and of the record residuals such as $\Delta_{\mathrm{Born}}(T_W)$, $\Delta E_{\mathrm{unrec}}(T_W;\theta)$, and $\mathcal{D}_{\mathrm{grav}}(T_W;\theta)$. Keep it distinct from absolute time $T$ (the global substrate clock) and from the persistence time $T_{\mathrm{rec}}$ (how long a formed record must survive). Do not write a bare $T$ for the window; use $T_W$. |
| $T_{\mathrm{rec}}$ | Record persistence time: the minimum duration a formed record must remain stable to count as durable. | Sets the persistence window $[\tau_{\mathrm{meas}},\,\tau_{\mathrm{meas}}+T_{\mathrm{rec}}]$ used by the record-autonomy and restartability residuals. It is a duration measured inside a record window $T_W$, not the window itself and not absolute time $T$. |
| $\chi_{\mathrm{eff}}:(T,\mathbf X,\mathcal N_{\mathrm{sea}},\text{observer record})\mapsto(t_{\mathrm{eff}},x_{\mathrm{eff}}^i)$ | Coordinate-layer map from native absolute variables to effective observer-chart variables. | This map is a closure target unless the local document derives it. It prevents hidden substitution of $T,\mathbf X$ for $t_{\mathrm{eff}},x_{\mathrm{eff}}^i$ in equations that can otherwise pass dimensional checks while failing a layer check. |
| $\|\cdot\|$ | Euclidean norm (length) induced by $h_{ij}$. | The norm induced by $h$; used for separations $r$, speeds, and lengths; coordinate-independent. |
| hat (ˆ) | Unit vector; e.g., $\hat{\mathbf{r}} = \mathbf{r}/\|\mathbf{r}\|$. | Normalization to unit length. $\hat{\mathbf{r}}$ fixes the line of action. At $r=0$ the direction is undefined, and the coincident root is excluded by $H(0)=0$ rather than assigned a symmetry-cancellation contribution. |
| Group velocity; group speed | Whole-assembly translation velocity; its magnitude. | Use one declared center and reference frame. Preserve constant-motion and averaging assumptions explicitly. Distinguish internal constituent motion, Noether sea flow, and conventional wave-packet group velocity. See [Site and Group Velocity Usage](terminology-usage.md#site-and-group-velocity-usage). |
| $v$ | Assembly group speed in a declared channel comparison. | Use $v$ only as a group speed, normally through a channel-qualified ratio such as $\beta_f=v/c_f$. It is not the causal-wake propagation speed. |
| $\kappa > 0$ | Universal coupling constant (absorbs geometric normalizations). | A scale factor absorbing geometric constants (e.g., $4\pi$ and $1/c_f$ Jacobians). Varying $\kappa$ rescales all per-hit accelerations and shifts emergent scales such as $d_0$ and $P_0$. |
| $\epsilon > 0$ | Potential polarity-unit magnitude; identified with $\lvert e\rvert/6$. | Fundamental polarity magnitude for architrinos in observer-level charge bookkeeping. Identifying $\epsilon$ with the magnitude of $e$ divided by 6 makes observed quark charges become integers in units of $\epsilon$, simplifying conservation. |
| $\epsilon_+\equiv+\epsilon,\ \epsilon_-\equiv-\epsilon$ | Signed polarity-unit labels for positive and negative architrino polarity in inventory counts. | Use these labels for axial inventories, weak-coupling-triad inventories, and neutral Noether braid content, e.g. $5\epsilon_+ + 1\epsilon_-$. The full entity names `electrino` and `positrino` remain valid when naming architrinos; compact inventory notation must use the signed $\epsilon_\pm$ labels. |
| $o_{\mathrm{PA}}\in\{+1,-1\}$ | Retained pro/anti Noether braid orientation sign. | Tracks the orientation of the indexed three-dimensional branch frame when that orientation is deformation-stable. Derive it from the indexed path or angular-momentum-frame record, not from a high/middle/low radius order. It is $C$-even and $P$-odd. Do not use it as the matter/antimatter sign. |
| $c_{\mathrm{pol}}\in\{+1,-1\}$ | Polarity-assignment sign on a chart that also carries $o_{\mathrm{PA}}$ and $\chi_{\mathrm{pol}}$. | Defined by $c_{\mathrm{pol}}\equiv\chi_{\mathrm{pol}}o_{\mathrm{PA}}$. It is $C$-odd and $P$-even. At a planar limit where $o_{\mathrm{PA}}$ is undefined, use the relational notation $\mathfrak B$ and $C(\mathfrak B)$ rather than assigning this scalar. |
| $\chi_{\mathrm{pol}}\equiv\operatorname{sign}(\mathbf p\cdot\mathbf S)=o_{\mathrm{PA}}c_{\mathrm{pol}}$ | Polarity-weighted handedness invariant when $\mathbf p\ne0$. | It is reversed by $C$ and by $P$, and preserved by $CP$. Keep it distinct from the pro/anti orientation sign itself. |
| $q_t,\,q_r$ | Intrinsic polarities of the transmitter and receiver (each equals $\pm\epsilon$). | Intrinsic, fixed signs $\pm\epsilon$. They enter accelerations through $|q_tq_r|$ and $\sigma_{tr}=\operatorname{sign}(q_tq_r)$. |
| $\sigma_{tr}=\operatorname{sign}(q_tq_r)$ | Interaction sign: $+1$ like-on-like (repulsion), $-1$ unlike (attraction). | It selects repulsion versus attraction by multiplying the radial unit vector in the EOM. |
| $\mathbf r_t,\ r,\ \hat{\mathbf r}_t$ | Separation vector, distance, and direction from the transmitter's emission site to the receiver's reception site. | $\mathbf r_t=\mathbf X_r(T_r)-\mathbf X_t(T_t)$, $r=\|\mathbf r_t\|$, and $\hat{\mathbf r}_t=\mathbf r_t/r$. The transmitter's position at $T_r$ is not part of the arriving-hit geometry. |
| $D_t(T_r;T_t)$ | Transmitter-side wake-spacing and emission-time Jacobian factor. | $D_t=c_f-\hat{\mathbf r}_t\cdot\mathbf V_t(T_t)$. It controls root transversality and how transmitter motion compresses or dilates successive emitted wake surfaces. |
| $D_r(T_r;T_t)$ | Receiver-side wake-crossing and root-playback factor. | $D_r=c_f-\hat{\mathbf r}_t\cdot\mathbf V_r(T_r)$. It controls how the receiver crosses the emitted wake sequence and enters the exact root-playback derivative. |
| $W_{r\leftarrow t}^{\mathrm{acc}}(T_r;T_t)=c_f/\lvert D_t\rvert$ | Unsigned transmitter-side acceleration weight in the canonical EOM. | It is fixed by transmitter-side causal-surface density and is distinct from the signed root-playback derivative $D_r/D_t$. |
| $J_{r\leftarrow t}^{t}(T_r;T_t)=D_t/c_f$ | Dimensionless transmitter-side causal Jacobian. | It is useful for root ledgers, transversality floors, and transmitter-side diagnostics. It is not by itself the full current acceleration weight. |
| $T_t,\ T_r,\ \Delta_{r\leftarrow t}=T_r-T_t$ | Transmitter emission time, receiver reception time, and causal travel delay. | $T_t$ labels the past event that emitted the arriving wake. $T_r$ labels the receiver event at which acceleration is evaluated. Reserve bare $T$ for absolute time when no transmitter-receiver event pair is being distinguished. |
| $\mathcal C_{r\leftarrow t}(T_r)$ | Causal set of transmitter emission times that reach receiver $r$ at $T_r$. | The set of $T_t<T_r$ solving $\|\mathbf X_r(T_r)-\mathbf X_t(T_t)\|-c_f(T_r-T_t)=0$. For $\|\mathbf V_t\|<c_f$ and transverse crossings, a unique smooth branch follows from the implicit function theorem; multiple roots can occur otherwise. |
| Root ledger / causal-root ledger | Integer bookkeeping of active causal-root branches on a fixed branch chart. | The ledger records which causal roots are active, not a new acceleration law. In the circular binary reduction, $(N_s(s),M_p(s))$ counts active self-hit branches and active partner-hit branches. Use transmitter identity (`self` vs. `partner`) separately from root count (`single-root` vs. `multi-root`). |
| candidate braid; certified braid | Certificate-status terms for Noether braid branches. | A candidate braid is a proposed branch or branch family before all required entries close. A certified braid is a retained branch record with the family-declared inventory: six worldlines for the base, orthogonal-axis three-binary, and coincident-axis three-binary cases, or twelve worldlines for two-component circular configurations. It returns to itself under the delayed dynamics up to declared neutral symmetries, carries positive non-symmetry stability margins, and closes the observer-export entries required of a physical branch. Its retained record includes only path-history, causal-root, wake-tail, energy/action, momentum, angular-momentum, phase, orientation, response-center, group-velocity, Noether sea, and observer-export data that can still affect the declared delayed update or certification. A certified braid must reproduce Lorentz clock/ruler deformation in the applicable homogeneous moving-branch regime, including $\xi\to1/\gamma_{\mathrm{eff}}$, $d\tau/dt_{\mathrm{eff}}\to1/\gamma_{\mathrm{eff}}$, and bounded preferred-frame leakage after an effective observer chart is declared. |
| eigen-braid spectrum | Theorem-target set of dynamically admissible discrete Noether braid modes. | Reserve this term for the spectrum construction in Braid Mathematics: candidate mode solutions indexed by cadence, scale, root topology, and winding data. Membership in the eigen-braid spectrum does not certify a braid. Use candidate braid or certified braid for certificate status; do not use eigen-braid as a certificate-status modifier or label. |
| Wake / Causal Wake | The causal-isochron residue left by architrino motion. | Preferred substrate-level term for the causal structures emitted by moving architrinos. A wake is the architrino-level description of what appears as a field at the effective level. Concretely, it is the union of causal isochrons emitted along a transmitter path. |
| Wake equation; wake map; wake pattern; wake spectrum | Construction rule and its evaluated representations. | The wake equation superposes the causal wakes emitted by constituent architrinos from one declared source-history record. At fixed source record and analysis protocol, the evaluated scalar is the wake map; its spatial or temporal structure is the wake pattern; and its frequency or angular-mode decomposition is the wake spectrum. The Master Equation remains the received-hit acceleration law, while `wave equation` remains correct for a qualified continuum comparison or recovery equation. |
| $\delta(r - c_f\Delta),\ \delta_{S_r}$ | Surface delta on expanding causal isochron; surface measure on surface $S_r$. | $\delta(r-c_f\Delta)$ represents a razor-thin causal isochron in radial coordinates, while $\delta_{S_r}$ is the surface delta on $S_r$. Normalizations ensure total emission integrates to $q$. |
| $\delta_\eta(\cdot)$ | Mollified (Gaussian) causal surface with width $\eta > 0$ for smooth dynamics. | A Gaussian mollifier of width $\eta$ that preserves total amplitude and makes acceleration contributions continuous in time; limits are taken in the weak sense as $\eta\to 0$. |
| $H(\Delta)$ | Heaviside step; convention $H(0)=0$ (no instantaneous self-kick). | Enforces causality (no advanced effects). With $H(0)=0$ it removes coincident-time self-kicks; in distributions it gates support to positive causal delay $\Delta>0$. |
| $\Phi,\ \Phi_\eta$ | Potential and mollified potential reconstructed from superposed causal wakes. | Bookkeeping potentials reconstructed from the superposed causal-wake contributions; $\Phi_\eta$ smooths the distribution so $\nabla\Phi_\eta$ exists pointwise and can be used to verify energy identities over resolved windows. |
| $c_f$ | Primitive causal-wake propagation speed. | Use $c_f$ in every substrate constraint, numerical instantiation, fixture, test, tolerance, and operator-facing numerical example. Canonical nondimensional units set $c_f=1$. Keep it distinct from the assembly group speed $v$ and from dressed observer-channel speeds. |
| $c_{\text{eff}}(\mathbf X,T)$ | Noether sea dressed assembly-channel propagation speed. | Effective propagation speed assigned to coherent assembly-level causal closure, clock/ruler synchronization, and observer-sector metric comparisons after primitive wakes at $c_f$ are retimed by local Noether sea state. It is derived through the constitutive dressing map, with $\chi_{\text{sea}}=c_f/c_{\text{eff}}$ and $c_{\text{eff}}\to c_f$ in weak homogeneous conditions. It is not automatically the photon-channel speed; $c_\gamma(\mathbf X,T)=c_{\text{eff}}(\mathbf X,T)$ is a photon Gate A closure target, not a definition. |
| $c_0$ | Asymptotic observer-sector speed calibration used in weak-field and PPN comparisons. | Use $c_0$ in observer-level benchmark formulas after the effective chart and asymptotic calibration have been declared. It is not a new substrate propagation law and must not be silently identified with $c_f$, $c_{\mathrm{eff}}$, or $c_\gamma$. |
| $c_\gamma(\mathbf X,T)$ | Photon-channel transport speed. | Observer-facing photon propagation channel extracted from a retained photon and Noether sea record. Equality $c_\gamma=c_{\mathrm{eff}}$ is a photon Gate A closure target, not a definition. |
| $c_{\mathrm{GW}}(\mathbf X,T)$ | Gravitational-wave effective transport speed. | Observer-sector tensor/disturbance channel used for gravitational-wave recovery tests. Its relation to $c_\gamma$, $c_{\mathrm{eff}}$, and $c_f$ must be derived and bounded rather than imposed by notation. |
| $c_\star$ | Locally declared comparison-channel speed. | Generic placeholder only when a local derivation explicitly names the channel $X=\star$. The paired quantities are $\beta_\star=v/c_\star$ and $\gamma_\star=(1-\beta_\star^2)^{-1/2}$. |
| $\rho_{\text{NS}}(\mathbf X,T)$ | Physical Noether braid density field. | Mass/number density of the Noether sea in physical units over the native Euclidean slice. The `NS` subscript identifies the established Noether-braid density variable. When projected into an effective observer chart, declare that projection explicitly. |
| $n(\mathbf X,T)\equiv \rho_{\text{NS}}(\mathbf X,T)/\rho_{\text{NS},0}$ | Canonical normalized Noether braid density. | Dimensionless density used in constitutive maps and effective metric handoff. Recover physical density by multiplying by the reference value $\rho_{\text{NS},0}$. Do not change this symbol row opportunistically when replacing prose terminology. |
| $\chi_{\text{sea}}(\mathbf X,T)\equiv c_f/c_{\text{eff}}(\mathbf X,T)$ | Noether sea delay factor. | Theory-native replacement for refractive-index notation in Noether sea propagation maps. It measures effective causal slowing through the Noether sea relative to primitive wake speed. Do not use $n$ for this quantity; reserve $n$ for normalized Noether braid density. |
| $\chi_\gamma(\mathbf X,T)\equiv c_0/c_\gamma(\mathbf X,T)$ | Photon-channel delay factor. | Observer-normalized photon-transport variant used when the comparison is to the asymptotic light-channel speed $c_0$ rather than the primitive wake speed $c_f$. Use only when the photon channel is the explicit subject. |
| $\Phi_N$ | Newtonian benchmark potential used for weak-field matching. | External/source potential used for 1PN/PPN comparison formulas (e.g., Shapiro delay, redshift, precession benchmarks). |
| $\Phi_{\text{eff}}$ | Constitutive effective potential inferred from clock and metric channels. | In the exponential clock-law subclass, $\Phi_{\text{eff}}=c_0^2\ln(\Omega\xi)$ after the geometry-to-clock map is fixed. It governs weak-field geodesic acceleration in the observer sector. The use of $c_0$ marks the observer-sector calibration; in the weak homogeneous branch $c_f$ and $c_0$ differ only by $O(\epsilon_{\mathrm{LV}}c_0)$. |
| $U\equiv-\Phi_N,\quad U_{\Phi}\equiv-\Phi_{\text{eff}}$ | Positive potential conventions for expansions. | Use $U$ for PPN benchmark expansions and $U_{\Phi}$ when expanding directly in constitutive potential. In weak-field closure, $U_{\Phi}=U+O(U^2/c_0^2)$. |
| $\beta_f\equiv v/c_f,\ \beta_{\text{eff}}\equiv v/c_{\text{eff}},\ \beta_\star\equiv v/c_\star,\ \gamma_X\equiv(1-\beta_X^2)^{-1/2}$ | Channel-subscripted group speed parameters and Lorentz factors. | Every speed-ratio $\beta$ carries the subscript of its denominator channel ($\beta_f$ primitive wake, $\beta_{\text{eff}}$ dressed, $\beta_\star$ declared channel, $\beta_\gamma$ photon channel if needed), and the paired Lorentz factor inherits the same subscript. Bare $\beta$ and $\gamma$ are reserved for quoted standard-physics benchmark formulas, where they carry the quoted framework's observer-level $v/c$ meaning. $v$ denotes assembly group speed; in core nondimensional dynamics one often sets $c_f=1$, so $\beta_f=v$. |
| $\xi\equiv R_{\parallel}/R_{\perp},\ \lambda\equiv R_{\perp}/R_{\perp,0}$ | Noether braid envelope shape and scale ratios. | $\xi$ is the geometry-first oblate-envelope axis ratio of the Noether braid, with $R_{\parallel}$ measured along the contraction axis and $R_{\perp}$ measured transversely. $\lambda$ is the transverse scale ratio. In homogeneous Lorentz closure one tries to prove $\xi\to1/\gamma$, but that is a derived closure result, not the definition of $\xi$. Use `oblate spheroidal envelope` or `oblate spheroidal exclusion envelope` for the Noether braid geometry; use `ellipsoid` only for generic geometry, comparison classes, or genuinely triaxial cases. Do not use `Lorentz spheroid` or `Lorentz-spheroid` as a canonical term. The clock-rate factor remains the separate extracted observable $\omega_{\text{clk}}/\omega_0=d\tau/dt_{\mathrm{eff}}$ after an effective observer chart is declared. |
| $\Omega(n,\lambda)$ | Conformal constitutive factor in the effective metric map. | Encodes density/scale response of the Noether braid medium and multiplies the geometry-to-clock and ruler channels in $g_{\mu\nu}^{\mathrm{eff}}$. It must not be used to erase the distinction between the geometry ratios $(\xi,\lambda)$ and the extracted clock-rate factor $\omega_{\text{clk}}/\omega_0$. |
| $\hat{u}^\mu$ | Local medium 4-velocity label used in effective-metric constitutive formulas. | Bookkeeping vector for observer-sector metric construction; it is not a fundamental substrate 4-vector ontology. |
| $g_{\mu\nu}^{\mathrm{eff}},\ g_{\mathrm{eff}}^{\mu\nu}$ | Effective observer metric and its inverse. | Derived constitutive object mapping Noether sea state to operational clocks/rulers and null propagation; not a fundamental metric of the Euclidean void. Its coordinates are $t_{\mathrm{eff}},x_{\mathrm{eff}}^i$ unless the local comparison explicitly declares another chart. |
| $\gamma_{ij}^{\mathrm{eff}}$ | Effective spatial metric or ADM spatial-compliance tensor. | Observer-sector spatial compliance in effective metric or ADM-style equations. Its inverse is $(\gamma_{\mathrm{eff}}^{-1})^{ij}$ and its determinant must have a locally declared scalar name rather than bare $\gamma_{\mathrm{eff}}$. Do not confuse it with the native Euclidean spatial metric $h_{ij}=\delta_{ij}$ on $\Sigma_T$. |
| $\Gamma^\lambda_{\mu\nu}(g^{\mathrm{eff}})$ | Effective affine connection built from $g_{\mu\nu}^{\mathrm{eff}}$. | Governs observer-sector geodesic equations and weak-field acceleration extraction in emergent-gravity chapters. |
| $U_{\text{eff}}$ | Effective cycle-averaged potential on an attractor branch. | In kinematics closure it is the potential whose Hessian yields $K_{\parallel},K_{\perp}$; distinct from weak-field PPN variables $U,U_{\Phi}$ and from local dynamics energy $U_{\text{pot}}$. |
| $K_{\parallel},K_{\perp}$ | Longitudinal/transverse stiffness channels of translating attractors. | Cycle-averaged Hessian projections of the causal-wake potential; their ratio determines anisotropic shape response. |
| $(k_2,\ell_2,k_4,\ell_4)$ | Quadratic/quartic closure coefficients in the $\beta_f$ expansion of stiffness channels. | Matched set used for $O(\beta_f^4)$ Lorentz-kinematics closure; deviations map directly to preferred-frame leakage diagnostics. |
| $\epsilon_{\text{LV}},\ \Delta_{\text{tw}}(\beta_f)$ | Preferred-frame leakage scale and two-way anisotropy mismatch diagnostic. | $\epsilon_{\text{LV}}$ bounds non-Lorentz residual terms; $\Delta_{\text{tw}}$ is the measurable round-trip anisotropy proxy. |
| $\gamma_{\mathrm{PPN}},\ \beta_{\mathrm{PPN}},\ (\alpha_1,\alpha_2,\alpha_3)$ | Standard weak-field PPN parameters used for observational closure. | $\gamma_{\mathrm{PPN}}$ controls refraction/space-curvature response, $\beta_{\mathrm{PPN}}$ the nonlinear clock channel, and $\alpha_i$ preferred-frame leakage. Never use scalar $\gamma_{\mathrm{eff}}$ or $\beta_{\mathrm{eff}}$ for these PPN parameters: those symbols belong to the dressed-channel group-speed family. |
| $(\Xi_1,\Xi_2,\Xi_3,\Xi_4)$ | Constitutive preferred-frame leakage coefficients in weak-field metric expansion. | Map to $(\alpha_1,\alpha_2,\alpha_3)$ in [spacetime/ppn-parameters.md](../spacetime/ppn-parameters.md); all must vanish for zero-leakage closure. |
| axial architrino | One of the six architrinos outside the neutral Noether braid scaffold and bound into a fermion’s six-site organization. | Canonical system-level term for one of the six outward electroweak carriers. Axial architrinos occupy polar sites and contribute to the assembly’s visible bookkeeping without being identified primarily as standalone `charges`. |
| axial layer | The six-site organization formed by the six axial architrinos. | Canonical term for the system-level fermion axial structure. Use `axial` for the organization as a whole and reserve `polar` for the specific site geometry. |
| axial pattern | Arrangement of the six axial architrinos across the three Noether braid axes. | Preferred term for constituent layout, sign arrangement, and ledger patterning. |
| axial inventory | Count/composition bookkeeping for the six axial architrinos in an assembly. | Preferred ledger/catalog term for the assembly's axial composition. |
| axial frame | Coarse-grained orientation defined by the six-site axial arrangement. | Preferred term for the orientation defined by the axial layer. Weak-mixing and misalignment discussions should speak of the relation between the axial frame and the fixed Noether braid frame. |
| Accessory Configuration | Six-architrino configuration associated with a Noether braid but outside the braid member's defining architrino-worldline inventory. | Declare the polarity and position or path record of all six sites. The sites may lie inside, across, or outside the braid envelope. Do not use the term to assume an axial layer, surrounding shell, equal polarity, or a retained configuration. |
| $a\in\{1,2,3\}$; $(R_a,f_a,\phi_a)$ | Persistent binary index and its taxonomy coordinates. | The index is a record identity, not a sorting by radius, frequency, speed, or dynamical role. Equal radii are permitted, coordinate values may cross without relabeling, and indexed frequency ratios such as $f_1:f_2:f_3=4:2:1$ do not imply a radius order. |
| braid symmetry-breaking point | Braid-level terminal-alignment threshold of an orthogonal-axis three-binary branch. | Canonical term for the whole-assembly transition in which the three binary axes converge to the orthogonal-axis three-binary translation direction and precession ceases. Any field-speed, same-transmitter-root, or exposure roles remain attached to persistent indices and must be derived from the retained branch record; the taxonomy does not assign them. Equal threshold speed does not imply equal radius, frequency, or energy because $s_a=\omega_a\rho_a$. |
| planar rank-three Noether braid reduced chart | Reduced planar chart for a Noether braid branch. | A proof and simulation bridge between a full three-dimensional rank-three branch and the photon channel. It freezes the branch into a coplanar or near-coplanar sector with three indexed support-row phase ledgers, frequency pattern $f_1:f_2:f_3$, effective lever arms $\rho_a$, circulation signs, and closure rows. It may nominate frequency and phase families, but retained promotion requires one row set, event/domain support, wake, angular-momentum, energy-routing, and stability closure. |
| $h\in\{1,2,3\}$ | Candidate field-speed or caustic-grazing carrier index in a branch analysis. | Use only after the analysis declares the speed statistic, window, tolerance, and same-record causal-root rows. The value of $h$ is derived or compared; it is not a taxonomy coordinate and does not relabel the binary. |
| polar site | One of the six local dwelling/attachment sites for the axial architrinos. | Use `polar` for the specific site geometry within the axial layer. |
| polar dyad | The two polar sites associated with a single Noether braid axis. | Preferred local geometric term for the site pair on one axis. `Dyad` is preferred to `doublet` to avoid collision with Standard Model terminology. |
| $\mathcal{L}_{\text{eff}}$ | Effective coarse-grained Lagrangian density after averaging fast modes. | Used in dynamics/gauge chapters to encode observer-scale field behavior induced by causal-wake microdynamics. |
| $a(t_{\mathrm{eff}}),\ H(t_{\mathrm{eff}})=\dot a/a,\ \Omega_m,\ \Omega_\Lambda$ | Effective cosmology summary variables (observer side). | In this framework these are coarse-grained descriptors of medium evolution in an observer-level chart, not fundamental geometry variables of the Euclidean container. |
| $D_\mu,\ g,\ g',\ \theta_W,\ Y$ | Effective electroweak gauge-sector symbols in assembly-level closure mappings. | $D_\mu$ is the covariant derivative; $(g,g',\theta_W,Y)$ follow Standard-Model notation when mapping architrino assemblies to observer-level gauge phenomenology. |
| Heat / heating channel | Event-ledger channel where non-coherent transferred energy becomes thermalized ensemble energy. | Heat is not a substance and not a hidden loss term. Use a heat or heating-channel claim only after coherent photon output, recoil, bound remnant, reaction, and boundary-exchange terms have been named or ruled out. In radiation and surface-routing contexts this channel is often written $B_{\mathrm{heat}}$: captured action thermalizes through electron-envelope, bonding/lattice, and Noether sea ensemble updates while the event ledger still closes. |
| $U_{\text{pot}} = q'\,\Phi_\eta$ | Local potential-energy bookkeeping variable in the dynamics sector (mollified; weak limit as $\eta\to 0$). | Assembly/effective bookkeeping reconstructed from a mollified causal-wake potential. When a bulk conversion scale has been declared, $\mathbf F=-\nabla U_{\text{pot}}$ and $\Delta E_k=-\Delta U_{\text{pot}}$ may be used over intervals that resolve the mollifier; neither relation replaces the substrate acceleration law. |
| $\mathbf{F} = -\nabla U_{\text{pot}}$ | Optional assembly-level force bookkeeping from the potential. | This is not an architrino-level force law. It may be compared with the integrated per-hit acceleration account only after the bulk conversion and scope are declared; for $\eta\to0$ it is interpreted in the distributional/integrated sense. |
| $\mu_{\mathrm{arch}}$ | Universal bulk bookkeeping conversion between acceleration and force-like assembly units. | Optional conversion $\mathbf F=\mu_{\mathrm{arch}}\mathbf A$ used only at assembly/effective grade. It is not physical architrino mass and does not license $F=ma$ as a substrate premise. |
| $\mathbf A_{r\leftarrow t}(T_r;T_t)$ | Per-hit acceleration response from transmitter $t$ emitted at $T_t$ (line of action $\hat{\mathbf r}_t$, inverse-square geometry, transmitter-side acceleration weight). | The contribution from one causal root: directed along $\hat{\mathbf r}_t$ and scaling as $W^{\mathrm{acc}}/r^2$ with $W^{\mathrm{acc}}=c_f/\lvert D_t\rvert$. The total acceleration response sums over all transmitters and all $T_t\in\mathcal C_{r\leftarrow t}(T_r)$. |
| $V_r,\, V_\perp$ | Radial and transverse components of native receiver velocity relative to $\hat{\mathbf{r}}$. | Decompose $\mathbf V$ by orthogonal projection onto $\hat{\mathbf{r}}$. Only $V_r$ changes instantaneously at a hit; instantaneous specific power is $\mathbf A_{r\leftarrow t}\cdot\mathbf V_r=\sigma_{tr}\|\mathbf A_{r\leftarrow t}\|V_r$. The polarity sign is required. |
| $d_0,\, P_0$ (candidate emergent scales) | Minimal accepted bound-branch radius and its characteristic period, when supplied by certified dynamics. | The circular simple-root ledger contains algebraic balance candidates, but self-hit supplies only the outward barrier and no candidate is yet a retained stable attractor. Until finite-event continuation, retained-history closure, and return-map stability are certified, $d_0$ and $P_0$ remain conditional branch outputs rather than fundamental units. |
| $\mathrm{SS}_a(T)$ | “Wake locus” of worldline $a$ at absolute observation time $T$ (union of all causal surfaces). | The family of all causal surfaces emitted along a worldline, evaluated at absolute time $T$; a geometric picture of which causal structures can currently act at each point. |
| $\nabla^2$ | Euclidean Laplacian on $\Sigma_T$ ($\nabla^2 = \partial_{X^1}^2 + \partial_{X^2}^2 + \partial_{X^3}^2$). | The standard Euclidean Laplacian acting on functions on $\Sigma_T$. Reserve $\Delta_{r\leftarrow t}$ and other explicitly subscripted $\Delta$ symbols for delays or residuals. |
| $\nabla$ | Nabla operator (gradient/divergence/curl). | Vector differential operator used to define $\nabla f$, $\nabla\cdot \mathbf{F}$, and $\nabla\times \mathbf{F}$. We avoid $\mathbf V\times\mathbf B$ constructs in the canonical law. |
| $\nabla f$ | Gradient of scalar $f$. | Vector field of partial derivatives; points in direction of steepest increase; used for spatial derivatives on $\Sigma_T$. |
| $\nabla\cdot \mathbf{F}$ | Divergence of vector field $\mathbf{F}$. | Scalar measuring net outflow; Gauss-like flux form; identity: $\nabla\cdot(\nabla\times \mathbf{F}) = 0$. |
| $\nabla\times \mathbf{F}$ | Curl of vector field $\mathbf{F}$. | Vector measuring local rotation; identity: $\nabla\times(\nabla f) = \mathbf{0}$. Canonical law uses no right-hand-rule cross-product forces. |
| $\nabla^2 f$ | Laplacian of $f$. | Equal to $\partial_{X^1}^2 f + \partial_{X^2}^2 f + \partial_{X^3}^2 f$ in native Cartesian frames. |
| $E(3)$ | Euclidean group of spatial symmetries (translations $\rtimes$ rotations). | The spatial isometry group of $h_{ij}$. Statements are frame-invariant under translations and rotations, even though finite field speed singles out a preferred rest frame for dynamics. |
| $c_{\max} \equiv c_f$ | Maximal primitive signal speed in the substrate law. | Upper bound on primitive causal-wake propagation; defines the causal surface radius $r=c_f(T-T_t)$. |
| EOM | Master Equation of Motion: delayed, line-of-action, receiver-side inverse-square per-hit acceleration law. | Shorthand for the canonical delayed equation of motion. It is event-driven at $\eta=0$ and becomes a smooth ODE system under mollification. |
| Basin / Basin of attraction | Region of state or path-history space whose admissible trajectories resolve to the same stable or metastable branch, channel, or record outcome. | In deterministic delayed dynamics, a basin $B_i\subseteq\Gamma$ (or $B_k\subseteq\mathcal{S}_L$ in lower-level charts) is the pullback of an outcome, assembly branch, or release channel under the flow or return map over a declared window. Basin boundaries are separatrices, thresholds, or critical surfaces. Basin measures such as $\mu_*(B_i)$ or $\mu_c(B_k\cap K_c)$ supply outcome weights; they are not external probabilities assigned after the dynamics. |
| Mollify / Mollification | Replace distributions by smooth approximations, e.g., $\delta\!\to\!\delta_\eta$ (Gaussian of width $\eta$), to obtain $C^1$ trajectories and well-defined $\nabla\Phi_\eta$. | Replace distributions by smooth kernels (typically Gaussian) to obtain $C^1$ trajectories and well-defined spatial gradients; $\eta$ sets the smoothing scale relative to local geometry. |
| Regularize / Regularization | The process of introducing $\eta>0$ (or similar) to control singularities; interpret $\eta\to 0$ in the weak/integrated sense. | Introduce a small parameter (e.g., $\eta$) to control divergences so acceleration contributions and energy accounts remain finite; results are interpreted via limits of integrals as the regulator vanishes. |
| Normalize / Normalization | Choose constants (e.g., $1/(4\pi r^2)$) and Jacobians so total emission integrates to the source strength $q$; ensures conserved totals. | Pick prefactors so integrating the causal wake surface over space at fixed time returns the source strength $q$; includes the $1/(4\pi r^2)$ surface density and Jacobians from variable changes. |
| Change of variables (delta) | Identity $\delta(r - c_f\Delta) = \dfrac{1}{c_f}\,\delta\!\big(\Delta - r/c_f\big)$; the $1/c_f$ factor is a Jacobian from variable substitution. | Use the identity $\delta(g)=\sum \delta(x-x_i)/\lvert g'(x_i)\rvert$ with $g(\Delta)=r-c_f\Delta$ to convert between causal delay $\Delta$ and $r$; the derivative $g'=-c_f$ yields the $1/c_f$ factor in time integrals. |
| Non-dimensionalize | Choose $L_0,T_0$ so $c_f=L_0/T_0=1$; all speeds become dimensionless ratios to $c_f$. | Pick $L_0,T_0$ with $c_f=L_0/T_0=1$ so speeds are pure numbers; reduces parameter count and clarifies asymptotic regimes. |
| Degenerate / Non-degenerate | Degenerate: determinant zero (e.g., metric not invertible); non-degenerate: invertible. We use no non-degenerate 4D metric; spatial metric $h_{ij}$ is non-degenerate. | “Degenerate” refers to non-invertible bilinear forms. The model avoids a non-degenerate 4D metric and instead uses a positive-definite spatial metric $h$ that is invertible. |
| Dot product | Euclidean scalar product: $\mathbf U\cdot\mathbf V = h_{ij}\,U^i V^j$. | The Euclidean inner product; used to compute projections, angles, and kinetic quantities on $\Sigma_T$. |
| Norm | Length from $h_{ij}$: $\|\mathbf V\| = \sqrt{h_{ij} V^i V^j}$. | Induced by the dot product; used for lengths, speeds, and distances, e.g., $r=\|\mathbf X-\mathbf Y\|$. |
| Coupling | Overall interaction strength set by $\kappa>0$ (absorbs geometric normalizations). | Synonym for the interaction scale $\kappa$; changing it rescales accelerations and modifies emergent scales. |
| Intrinsic | Property of an object independent of motion or environment (e.g., intrinsic polarity $q=\pm\epsilon$). | Attributes fixed to an object independent of kinematics or environment (e.g., polarity sign and magnitude), contrasted with emergent or apparent properties of assemblies. |
| Causal | Respecting propagation delay; emission time $T_t$ must satisfy $r=c_f(T_r-T_t)$ to influence the reception event. | Qualifies constructions that respect finite-speed propagation; only emission times satisfying the declared $c_f$ constraint contribute to present acceleration. |
| Gaussian | The mollifier kernel $\delta_\eta$ used to replace $\delta$, producing smooth wake surfaces of width $\eta>0$. | A normalized bell-shaped kernel used as an approximate identity; parameter $\eta$ controls width and convergence as $\eta\to 0$. |
| Heaviside | Step function $H(\Delta)$; convention $H(0)=0$ (no instantaneous self-kick). | Step function used to restrict distributions to positive causal delay; with $H(0)=0$ it removes coincident-time contributions in self-interactions. |
| Hit | A causal intersection event: some $T_t\in\mathcal C_{r\leftarrow t}(T_r)$ where a causal wake surface reaches receiver $r$. | A solution $T_t\in\mathcal C_{r\leftarrow t}(T_r)$. In the impulsive picture it produces a velocity jump; under mollification it appears as a brief, finite acceleration contribution. |
| Field (substrate-level $\mathbb{A}\mathbb{A}\mathbb{A}$ prose) | Avoid by default; use `wake` or `causal wake` instead. | `Field` carries heavy continuum/QFT baggage. In $\mathbb{A}\mathbb{A}\mathbb{A}$ ontology the emitted structure is a wake generated by an architrino and later intercepted by receivers. Keep `field` only for effective-level translation or explicit comparison with standard modern frameworks. |
| Surrogate location | Stationary, hypothetical transmitter position chosen along the receiver's current unoriented line of action that, together with an adjusted emission time, reproduces the same instantaneous hit. | An inference/visualization device: at an instant, any hit can be recast to a stationary surrogate on line $L$. The source-side per-wavefront amplitude is unchanged; the arriving acceleration contribution still carries $W^{\mathrm{acc}}=c_f/\lvert D_t\rvert$. |
| Superposition | Linear addition of wake contributions / accelerations from all sources at a point and time. | Linearity at the level of distributions: contributions from all sources add. Near-field terms dominate due to $1/r^2$ and phase cancellations at large $r$. |
| Transverse | Component orthogonal to $\hat{\mathbf{r}}$; e.g., $\mathbf V_\perp$ with $\mathbf V_\perp\cdot \hat{\mathbf{r}}=0$. | Component orthogonal to $\hat{\mathbf{r}}$; unchanged instantaneously by a single hit but can change over time as $\hat{\mathbf{r}}$ rotates with the geometry. |
| Radial | Along the line of action $\hat{\mathbf{r}}$ (e.g., $V_r = \mathbf V\cdot\hat{\mathbf{r}}$). | Along the line $\hat{\mathbf{r}}$; every per-hit acceleration is collinear with $\hat{\mathbf{r}}$ and thus updates the radial component of motion. |
| Worldline | Time-parameterized path $\mathbf X(T)$ of an architrino or assembly in the Euclidean void; history up to $T$ is its provenance. | A map $T\mapsto \mathbf X(T)$ with absolute continuity; under impulses $\mathbf V$ has bounded variation. Spatial arclength equals the absolute-time integral of the speed over an interval. |
| Laplacian | Spatial second-derivative operator on $\Sigma_T$; in Cartesian frames $\nabla^2 = \partial_{X^1}^2 + \partial_{X^2}^2 + \partial_{X^3}^2$. | Same operator as $\nabla^2$; included for terminological clarity when not using the symbol; used in spatial PDE analogies and energy checks. |
| Singularity | Idealized divergence (e.g., $1/r^2$ at $r=0$); handled by mollification/weak limits. | Non-integrable behavior in pointwise expressions (e.g., $1/r^2$ at $r=0$); handled by excluding measure-zero cases and by regularization with $\delta_\eta$. |
| Jacobian | Factor from change of variables; e.g., $\delta(r - c_f\Delta) = \dfrac{1}{c_f}\,\delta\!\big(\Delta - r/c_f\big)$ has Jacobian $1/c_f$. | Multiplicative factor from variable changes ensuring measure preservation in integrals (e.g., the $1/c_f$ from $\Delta\leftrightarrow r$ substitutions). |

Notes:
- Bold symbols denote vectors; hats denote unit vectors; $\|\cdot\|$ denotes norms.
- All speeds are nondimensional after choosing $L_0,T_0$ so that $c_f=L_0/T_0=1$.
- Assemblies are dynamical geometries of architrino transceivers (not point particles). Their effective field-like / wave-like behavior is the net superposition of their constituent architrinos evolving under the equation of motion; any “emission/absorption” attributed to an assembly is shorthand for this superposed dynamics.
- Vocabulary: Use “expanding causal isochrons” as the canonical term for emitted structures; “wavefront” may be included once as a parenthetical synonym at first use. Avoid generic “spheres” or “ridges” in technical statements (reserve “ridges” for analogies).
- Vocabulary: Prefer `wake` or `causal wake` to `field` in substrate-level discussion. Use `field` mainly for effective-level observer descriptions or when translating into standard modern terminology.
- Translation rule: `field` -> `wake` when the prose means emitted architrino influence; `wake field` should usually be split into either `causal wake` or `effective field`, depending on level.
- Vocabulary: Keep the container/content distinction explicit. `Euclidean void` names the fixed spatial container; `Noether sea` names the ambient substrate contents; `spacetime medium` is acceptable only as a bridge term when translating toward effective spacetime language.
- Vocabulary: Write `Noether sea` in ordinary prose and compound modifiers such as `Noether sea density`; reserve `Noether Sea` for title contexts, and do not use the hyphenated form `Noether-Sea`.
- Vocabulary: Use `absolute timespace` as the formal name for $\mathbb{R}\times\mathbb{R}^3$, not as a loose catch-all synonym for substrate, contents, and emergent geometry all at once. Plain `spacetime` should normally be reserved for the emergent effective geometry.
- Vocabulary: In fermion axial-structure discussion, use `axial` for the six-site system-level organization and `polar` for the specific site geometry. Prefer `axial architrino`, `axial layer`, `axial pattern`, `axial inventory`, `axial frame`, `polar site`, and `polar dyad`.
- This glossary canonicalizes cross-chapter symbols. One glyph has one meaning within a document. A symbol used in three or more chapters requires a canonical row here; specialized chapter-local symbols must be defined at first use and must not collide with a reserved glyph.
- Subscripts and indices are part of symbol identity. Use $\mathrm{...}$ for stable identifier subscripts and $\text{...}$ for ordinary words or phrases. Do not mix spellings for the same identifier. Rename a local collision rather than adding a prose disclaimer.
- Scalar $\gamma_{\mathrm{eff}}$ and $\beta_{\mathrm{eff}}$ belong exclusively to the dressed-channel Lorentz family; scalar PPN parameters are $\gamma_{\mathrm{PPN}}$ and $\beta_{\mathrm{PPN}}$; effective spatial metric objects must carry their tensor indices.

---
