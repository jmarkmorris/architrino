# Stationary Binary Repairs and Independent Validation — 2026-09-14

Status: the numerical repairs are independently checked, and a separate continuous interval construction from the exact stationary history now certifies first wake-speed arrival in `[1.572637654589540, 1.572640138062056]` with a complete incoming causal census. The EOM solver separately accepts a checkpoint probe through `1.57262` and rejects a probe at `1.57266` atomically. Its rejected self-root census remains unresolved; the exact failed carriers and available difficult-cell fields are retained. The existing stationary-mirror theorem establishes the unchanged-sharp-law continuation obstruction. Passage and rebound are not supplied by this result.

## Scope and analytical references

The operator explicitly requested repair of the lifted-inertial zero-acceleration representation failure, independent validation of the refined stationary binary, continuation toward a complete encounter, and derivation of the initial acceleration and first interval. This is BP-001/BP-003 follow-through on the [earlier diagnosis](2026-09-14-stationary-continuation-diagnosis.md), not Campaign 1 admission. The [initial-motion derivation](../analysis/stationary-binary-first-interval.md) supplies the independent binary reduction. The [history-bound analysis](../analysis/stationary-continuation-history-bound.md) owns the existing enclosure argument.

For the inertial reference, a single path with $X(T)=1+T/4$ and $c_f=1$ has no positive-delay self root: its displacement over every positive delay is one-quarter of the wake-travel distance. Its acceleration is therefore the empty sum, exactly zero. Independent endpoint uncertainty obeys $\delta X(T)=\rho_x\eta_x+T\rho_v\eta_v$ and $\delta V(T)=\rho_v\eta_v$, with the earlier history retaining its original independent remainder. No interacting binary solution is inferred from this control.

## Implementation and mathematical justification

Source inspection with `git diff` and direct reads of the three affected production files identifies the complete repair:

1. `JointAccelerationSnapshot.cpp` preserves addition by exact zero and gives a singleton already equal to its center zero recentering error. The old reduction manufactured denormal radii once per unused symbol even when there were no acceleration rows.
2. `Interval.cpp` computes division through direct endpoint quotients and outward rounding. The previous reciprocal-first calculation overflowed on a positive subnormal denominator even for the finite quotient $0/\varepsilon$ or $\varepsilon/\varepsilon$. Denominators containing zero still reject before the exact-zero numerator identity is used; genuinely nonfinite quotient bounds still reject.
3. `JointEndpointCorrector.cpp` preserves exact singleton differences and omits exact-zero residual terms. It does not replace subtraction of non-singleton intervals by zero or assert a dependency that is absent from the representation.

**Derived enclosure justification.** Addition of an exactly represented zero changes no real value. Subtracting a singleton from its identical represented center is exactly zero. For intervals with a denominator excluding zero, the quotient is continuous on their Cartesian rectangle and its extrema occur among the four endpoint quotients; directed expansion of those extrema encloses the result without constructing an overflowing reciprocal. These changes preserve the relevant sets. The existing acceptance tolerances, candidate radii, ordinary-representation dominance checks and Krawczyk inclusion inequalities remain unchanged by the scoped source diff.

The independently authored original `endpoint-lift-control.cpp` remains preserved with its historical failure assertion. A separate current inertial harness uses the same analytical input and compares the saved old library with the repaired one. The first snapshot-only repair exposed the reciprocal overflow before any binary target ran; both stages and their failed outputs are retained rather than relabeled as passes.

## Known-case validation before binary use

All local sources and raw outputs named here are retained under `.local-data/braid-program/2-braid-2026-09-14/`, unless a supervisor log is identified. These are one-time checks under the operator's request; no regular test discovery, CI obligation or new campaign is enrolled.

| Independent check | Measured result and limit |
| --- | --- |
| Trajectory observer | Five exact inertial samples pass and an intentionally shifted trajectory rejects. The JSON observer passes a hand-known interval record and rejects wrong bounds before target use. It exposed a requested-time/endpoint mismatch in the new observer; that was corrected before the binary run. |
| Quotient controls | Five exact quotient cases pass, including zero/subnormal, subnormal/subnormal, interval and negative-denominator cases; a zero-containing denominator rejects. `interval-quotient-after.stdout.jsonl`; wall time `0.17` seconds. |
| Saved old library | The separate harness reproduces the zero-acceleration representation rejection with 0, 12 and 96 unused symbols; `inertial-production-before.stdout.jsonl`. |
| Repaired production inertia | All three unused-symbol cases complete through $T=0.25$ and restart to $T=0.5$, with four cumulative accepted steps and no rejected steps. Joint acceleration centers, coefficient enclosures, remainders and projected radii are exactly zero; nominal motion, uncertainty transport and older-history independence pass their analytical checks. `inertial-production-final.stdout.jsonl`; wall time `0.27` seconds. |
| Atomic negative controls | A known root-width excess and a memory-budget rejection preserve ordinary and joint publication; both are retained in the final inertial packet. |
| Existing root controls | The fresh root fixture passes three unchanged assertions covering independent Decimal root-width formulas, ordinary fallback dominance and traversal carrier transport. The last is a transport check, not a separate root algorithm. |
| Existing evolution controls | The fresh evolution fixture passes two unchanged assertions covering an independent six-path acceleration sum, an independent Decimal linear solve, retained joint coefficients and atomic rejections. |

The final root fixture used supervisor lease `cb409631-97d3-42f1-9369-ec383a8f9864` (`0.232` seconds, exit zero); the evolution fixture used `0b7f0e18-70f4-4afa-a413-0325ef21718e` (`0.749` seconds, exit zero). Both process groups closed. Their stdout packets are the corresponding `.local-data/owned-compute/logs/<lease>.stdout.log` files. The five Python assertions ran directly against those fresh packets in `0.10` seconds, using the shared venv without invoking class setup or unrelated modes:

- `NativeHistoryLayerTests.test_joint_root_time_budget_matches_independent_decimal_controls`;
- `NativeHistoryLayerTests.test_joint_consumption_passes_only_with_correlation_and_fallback_dominance`;
- `NativeHistoryLayerTests.test_traversal_exact_fallback_preserves_joint_carriers`;
- `NativeCoupledEvolutionTests.test_joint_precision_controls_cover_live_sum_full_corrector_and_retention`;
- `NativeCoupledEvolutionTests.test_rejections_publish_exactly_the_input_histories`.

The final production-library/stationary/evolution rebuild used lease `3e690f84-c0a4-4447-ae24-6f2f546e825f` (`1.398` seconds); the root-fixture relink used `41b177e0-e985-4792-8a92-01b4ff563c96` (`0.266` seconds). Both completed with exit zero and closed process groups. Earlier intermediate builds and failed inertial observations remain retained in the same task evidence.

## Independent binary reference and continuation declaration

Before seeing the new production trace, `stationary-binary-independent-reference.py --known` passed an affine causal inverse, the held-source differential equation against its exact parametric solution at 21 points, and positive/negative containment controls. The source was then frozen before stationary target use; its recorded SHA-256 is `816e3e2cc1ff1a7bc26b9f1782331a8edace9e6ccc6efedbe354baeab9c70b3c`. This hash is provenance, not a new admission requirement. The reference imports no EOM component and uses only completed independent history. Its equations, three numerical refinements, event estimates and limitations are explained in the derivation.

The repaired production comparison first requests `1.3999999999999959` with prefix scale `0.5`, fixed continuation step `0.005`, the original `1e-5` root tolerance and all other acceptance settings unchanged. Its optional `--trajectory` output reports accepted endpoint position/velocity enclosures and every retained ordered-pair root row. Rejected candidates remain unpublished.

After a passing comparison, the next native upper-time cap is `1.575`, chosen from the independent reference's predicted first speed-equality time near `1.57264`. The accepted part before the first certified speed enclosure touching one is the sub-field evidence window; any later numerical rows carry no sub-field claim. A solver halt stops the run. Contact or a positive-separation turn would require its complete event record; merely reaching the upper cap establishes neither. If inherited root width stops the half-step construction first, at most two additional prefix constructions, scales `0.25` and `0.125`, may be compared under the same law, tolerances and fixed continuation step. Each is a bounded history-refinement diagnostic, not an interaction-strength or fate sweep. A failed independent comparison redirects work to its discrepancy before further reliance.

Each target uses one four-thread request, a five-minute hard deadline and the existing root-cell, correction, rejection and attempt limits. Known computations predict event order but do not certify production evolution. First speed equality is this diagnostic's scope boundary, not a universal law obstruction; no binding, breathing or full encounter result follows from that boundary alone.

```bash
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 300 --heartbeat-seconds 15 -- .tmp/2-braid/build/eom_stationary_joint_frontier_fixture_cli --controller-continuation 1.3999999999999959 --prefix-step-scale 0.5 --trajectory
```

## Results and disposition

The repaired half-prefix run completed at `1.3999999999999959` under lease `3ef462cb-1753-4922-b563-ffe1357528ca`, in `37.947` seconds with exit zero and closed process group. It accepted 228 prefix steps and 32 continuation steps, with zero continuation rejections. Its trajectory packet is `repaired-half-prefix-1p4.json`. Both final partner roots certify at 128 bits by the ordinary directed-interval route, with the common emission bracket `[0.822261169396923963125749915469332087663301, 0.822269337233112886452302578135700339641014]`. The positive endpoint position is enclosed by `[0.17785991873050502, 0.17786512739778021]`, and its velocity by `[-0.57962183961385383, -0.57959594372459788]`; the negative path has reflected intervals.

**Measured independent state comparison.** The frozen reference checks all 260 accepted times and both paths, yielding 520 path samples with zero position or velocity containment failures. The maximum absolute nominal discrepancies are `7.723346934218611e-7` in position and `3.5412719201488585e-6` in velocity. The full comparison is retained as `repaired-half-prefix-1p4-independent-comparison.json`. These are finite sampled checks against a separately implemented, analytically grounded numerical reference; they do not turn the local integration estimator into a rigorous global error bound.

The separate sampled-root observer passed its hand-known stationary case and ten deliberately incorrect records before inspecting the production trace. Its frozen source SHA-256 is `f506b0ddefb0889e6bfa5421f29fd80114692bfa7b1a83eb6397d0638dfa220a`. The fresh `1.4` trace then passes all 520 cross rows and 520 self rows: each cross row reports a single complete root and contains the independently evaluated emission time, transmitter factor and receiver factor; each self row reports zero positive-delay roots and a root-free complement. The receipt is `repaired-half-prefix-1p4-root-comparison.json`. The independent expected inventory follows from the sub-field distance inequality and monotone causal inverse in the derivation, not from the production root finder. These sampled checks still do not certify every point of a continuous history tube.

### Event-directed half-prefix continuation

The same half-prefix construction requested the declared cap `1.575` under lease `5310adbe-1627-4abe-a499-6ff3672d4b04`. It halted in `48.388` seconds with diagnostic exit `2`, an atomically preserved accepted end of `1.444999999999995`, and one rejected continuation step. There were 228 prefix and 41 continuation accepted steps. Its raw packet is `repaired-half-prefix-event.json`; the process group closed.

The failed endpoint at `1.4499999999999948` reports `root_completeness_not_certified`, with both partner searches exhausting the 512-bit ladder. The positive row's joint root width is `1.0089354022366865e-5`, above the unchanged `1e-5` threshold; its ordinary width is `1.1584393444691297e-5`. The affine, independent and nonlinear radius contributions are respectively `2.1090780757924773e-6`, `1.4573547117454141e-6` and `1.2300517948371408e-11`. This is a numerical enclosure stop, not contact or a physical turning event. The positive member's last accepted position is `[0.15052016813905106, 0.1505268699972217]`, with velocity `[-0.63786831335232053, -0.63782595313892121]`.

Independent checks of this extended half-prefix trace pass all 269 frames: 538 path samples and the corresponding 538 cross and 538 self rows. At `1.4249999999999954` both cross-emission brackets lie below $T_1$; at `1.4299999999999953` both lie above it. This sampled passage brackets the independent arrival estimate `1.428341244444872`. It is evidence of arrival from the end of the first motion interval, not a full encounter or a continuous event-enclosure theorem. The state, root and sampled-passage receipts use the `repaired-half-prefix-event-` stem.

### Quarter-prefix continuation and smaller-step decision

The quarter-prefix comparison completed its 267-step prefix and 49 continuation steps under lease `e27ca1fb-da19-4567-8d10-4e11d1b690ad` (`46.926` seconds, diagnostic exit `2`, closed process group). It accepted through `1.4849999999999941`, then rejected the next fixed `0.005` step with `numeric_step_budget_exceeded` and controller halt `minimum_step_exhausted`. There is no failed acceleration snapshot for this rejection; the observer reports that absence explicitly. Both last accepted cross roots remain complete at 128 bits. Their common emission bracket is `[1.01787246521038645327687794592037853165154, 1.01788185185820995009185579324028170366609]`. The positive member is enclosed by position `[0.12372178340093871, 0.12372760759828499]` and velocity `[-0.70533901388555909, -0.70527820512829575]`.

The frozen independent instruments pass all 316 accepted frames: 632 path samples contain the reference position and velocity, and 632 cross plus 632 self rows pass inventory and root/factor containment. The packets are `repaired-quarter-prefix-event.json` and its `-independent-comparison.json` and `-root-comparison.json` companions.

This is a different limiting condition from the half-prefix root-width stop. Refining only the earlier prefix is insufficient to address a fixed continuation step that exceeds the numerical step budget. The next two bounded numerical resolutions are therefore declared before execution: retain prefix scale `0.25` and reduce continuation to `0.0025`; if a numerical limit persists, use prefix scale `0.125` and continuation `0.00125`. The upper cap remains `1.575`, with the same stopping boundaries and five-minute limit per target. This supersedes an eighth-prefix experiment with fixed `0.005` continuation. Both resolutions retain the original law, root tolerance and state/correction tolerances. They test finer time resolution, not looser acceptance. The existing fixture adds an explicit `--continuation-step` option for reproducibility; its default remains unchanged, and the smaller-step route is checked against the inertial reference before binary use.

Before the smaller-step binary target, an independent wrapper advances the exact inertial prefix from `0.25` to `0.5` with explicit continuation step `0.0025`. It accepts 100 steps without rejection, preserves atomic publication, and passes all 100 emitted frames against the inertial position, velocity, enclosure and zero-self-root expectations after its observer's known positive/negative controls. Runtime is `0.99` seconds by `/usr/bin/time -p`; the `inertial-continuation-step` raw/check packets and reproduction commands remain with the inertial evidence. The fixture rebuild completed under lease `e230e16b-42d4-4fb8-9aea-9da7b917e581` in `1.882` seconds with exit zero and closed process group. Separate direct CLI calls for steps `0`, `-0.001`, `0.01` and `nan` each return the expected input error before numerical execution.

The quarter-prefix command was:

```bash
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 300 --heartbeat-seconds 15 -- .tmp/2-braid/build/eom_stationary_joint_frontier_fixture_cli --controller-continuation 1.575 --prefix-step-scale 0.25 --continuation-step 0.0025 --trajectory
```

A production enclosure failing to contain an independently computed value at a checked time, unstable reference refinement, missing causal roots, or a source query beyond completed history would overturn the corresponding validation claim. Agreement at sampled times is not a global enclosure theorem.

### Quarter-prefix with half continuation step

With prefix scale `0.25` and continuation step `0.0025`, lease `25eaf795-3c9c-4064-883b-18afc76bf503` halts at accepted end `1.5424999999999929`, after 267 prefix and 121 continuation steps. It takes `97.71` seconds, returns diagnostic exit `2` and closes its process group. The next endpoint at `1.5449999999999928` fails root completeness: positive-row joint width `1.0339481458240394e-5` exceeds the unchanged `1e-5` tolerance, while ordinary width is `1.2310910545583615e-5`. The affine, independent and nonlinear radii are `1.4540819861709156e-6`, `1.5933055388285454e-6` and `1.2943959205378324e-11`. Rejected publication is unchanged.

The positive endpoint position is `[0.079274061259319323, 0.07928012921137581]` and its velocity is `[-0.85633559802452985, -0.85626327002575509]`. Independent state and root checks pass all 388 accepted frames, comprising 776 path samples, 776 cross rows and 776 self rows, with no containment or inventory failure. The packets use the stem `repaired-quarter-prefix-half-continuation-event`. This advances the validated sampled window but reaches neither contact nor field speed.

The final declared comparison used prefix scale `0.125` and continuation step `0.00125`, preserving the same cap, bounds and acceptance thresholds:

```bash
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 300 --heartbeat-seconds 15 -- .tmp/2-braid/build/eom_stationary_joint_frontier_fixture_cli --controller-continuation 1.575 --prefix-step-scale 0.125 --continuation-step 0.00125 --trajectory
```

### Finer-prefix stop and isolated corrector diagnosis

The final construction ran under lease `d0796d57-cd63-42f1-8a73-f1260ab830d4`. Its heartbeat reported persistent prefix steps near `0.000625`, reaching only `0.47499999999999265` after 759 accepted steps. The owner stopped this run after `158.251` seconds to inspect the early rejections before spending more time on the same construction. The supervisor records `SIGTERM` and a closed process group. There is no completed stdout trajectory packet, so the heartbeat supplies progress and cost evidence only; it does not extend the independently checked scientific window.

A new short attempt observer passed a hand-specified synthetic rejected step before inspecting the target. The identical eighth-prefix request through `0.01`, linked separately to the repaired and preserved old libraries, then produced 15 accepted steps and two rejected steps in each case. The repaired-library run used lease `6695f38c-9fcc-43b3-a478-96ee98b449b5` (`0.472` seconds); the old-library run used `48c742f3-5a5a-4f21-8135-84c1c9d2d977` (`0.720` seconds). Both completed with closed process groups. This direct old/new comparison establishes that these startup step reductions precede the three-file zero-acceleration repair; it does not attribute every later continuation limit to the same cause.

Direct inspection of `CoupledEvolution.cpp` and the short observer's rejected substep identifies a correction residual near `5.7288e-14`. That residual passes the fixed `1e-13` joint-center settling condition, but the subsequent Krawczyk image does not fit strictly inside the approximately `2.8613e-14` acceleration candidate radius. The substep returns `krawczyk_image_not_strictly_interior` after one correction iteration even though twelve iterations are available. The controller then halves the step; because growth is disabled by default, the early reductions persist. The complete commands, raw records, known control and source locations are retained in `.local-data/braid-program/2-braid-2026-09-14/early-prefix-diagnosis.md`.

The proposed next implementation is to continue the existing correction iteration, using its newly evaluated ordinary and joint acceleration guesses, when this inclusion attempt fails and correction iterations remain. This is a proposal, not a completed repair: the current change does not edit `CoupledEvolution.cpp`. Acceptance must still require certified inclusion, ordinary dominance, complete roots and the original tolerances. Exhausted or unsupported cases must retain atomic rejection. A synthetic correction case with an independently known fixed point and an exhausted-budget negative control should precede another binary run. Successful correction would remove this demonstrated startup inefficiency; it would not by itself prove that inherited root widths remain acceptable near the encounter.

### Disposition before the bounded corrector retry

The zero-acceleration representation failure is repaired against the exact inertial solution, including restart and independent uncertainty transport. The initial binary acceleration and complete first held-source interval are derived in the linked analysis. The strongest completed production trace reaches `1.5424999999999929`; the frozen independent state and root instruments check all 388 accepted times, both paths and all four ordered-pair rows without a reported discrepancy. These are independent sampled numerical comparisons, with the source and reference limitations stated above. Continuous-history certification, contact/crossing/return and a complete encounter remain open.

The immediate numerical barrier in that strongest trace is the failed root-width certificate at `1.5449999999999928`, not a physical event. The next finer construction also encounters a separately demonstrated corrector inefficiency. Address that bounded correction issue first, repeat its independent controls, then rerun only the declared finer construction and compare every accepted endpoint with the frozen reference. If the root-width limit persists, return to the retained-history enclosure argument rather than widen its tolerance. The independent first-speed estimate near `1.5726396644` is a reference scope boundary before contact; it does not authorize an at/above-field-speed fate claim.

Production/reference source hashes are retained in `repair-follow-through-source-sha256.txt`. Reproduction uses the commands above and the frozen local `stationary-binary-independent-reference.py` and `stationary-binary-root-observer.py`; their known-case receipts remain separate from their target comparisons. No regular test enrollment, generated-source rewrite or scientific campaign admission follows from this bounded work.

The final scoped `git diff --check` on the production files, fixture and changed queue/log owners reports no whitespace errors. Explicit `git diff --no-index --check /dev/null <file>` checks cover the new analysis/evidence documents, which ordinary tracked diffs omit. The supervisor closeout command `node scripts/dev/owned-compute-supervisor.mjs closeout --owner-task "$CODEX_SESSION_ID"` returns `status: clear` for owner `01a0a215-c597-7402-bc09-8fa3aca9fc0d`; this task leaves no active owned computation.

## Authorized bounded corrector retry — subsequent September 14 work

The operator subsequently authorized the proposed correction repair and further independently checked approach. This continues the same stationary history and does not weaken the encounter or fate requirements. The earlier section records the completed pre-retry state; the following observations belong to the fresh implementation.

Source inspection with `git diff -- src/eom/src/CoupledEvolution.cpp` identifies ten added lines. A failed joint contraction retries only when its detailed failure begins `krawczyk_image_not_strictly_interior/` and `iteration < max_correction_iterations`. The retry uses the newly evaluated ordinary and joint acceleration guesses and returns to the existing correction loop. It does not accept the failed candidate. The final permitted iteration retains the original detailed rejected certificate, and every other failure retains its original immediate return. The mathematical inclusion test and all candidate bounds remain unchanged. Re-evaluating a candidate is therefore an additional bounded attempt to meet the existing condition, not an alteration of that condition.

The pre-retry library is preserved as `.tmp/2-braid/pre-corrector-retry-libeom_native.a`, with SHA-256 `4a54eaeec94902fb14265a57af37a66c9a57248ff74780dc03db65feba64cf98`, measured equal to the then-current library before rebuilding. The fresh Release build of stationary, evolution and root fixtures completes in `8.479` seconds under lease `d0bdd8e0-27b6-4c28-9607-60580758a426`, exit zero and closed process group. `stat -f` places the source change at `20:36:01` EDT and the library/stationary executable at `20:36:54`, establishing rebuild order for this target.

The existing evolution fixture's `all` mode completes in `0.968` seconds under lease `b12614d9-ce9d-41e9-b29c-05c140b06559`, exit zero and closed process group. The two unchanged assertions `test_joint_precision_controls_cover_live_sum_full_corrector_and_retention` and `test_rejections_publish_exactly_the_input_histories` pass directly against its fresh packet in `0.11` seconds, without class setup or unrelated long-horizon modes. This checks the independent analytical acceleration/linear-solve controls and existing atomic negatives. The outputs are `corrector-retry-focused-assertions.stdout.jsonl` and `.stderr.log` under the retained local evidence directory. Separate recentering and correction-budget controls must pass before the binary extension.

### Independent retry controls before longer continuation

The independent control first checks its observer on hand-known data, then uses the affine fixed-point map $F(a)=a/2+b$ with $b=(3/4,-1/2,1/4)$ and exact fixed point $2b$. The initial candidate centered at zero fails strict inclusion. Recentring at $b$ with the same radius one certifies inclusion with minimum margin $1/4$. This establishes a case where recentering is mathematically sufficient without enlarging the candidate. The production change does not alter this independent map, its derivative or its reference solution.

The identical short binary atomic request from zero to `0.0025000000000000001`, linked to pre-retry and repaired libraries, changes from rejection with substep iteration counts `[2,1,1]` to acceptance with `[2,1,2]`. The last half-step needs the additional correction. A separate request from `0.00125` to `0.0025` with a one-iteration limit still rejects with detailed `krawczyk_image_not_strictly_interior` diagnostics and unchanged ordinary and joint history fingerprints. The final pre-retry and repaired control runs take `0.30` and `0.36` seconds respectively by `/usr/bin/time -p`; their raw outputs use the `corrector-retry-reference-before` and `-after` stems. These final timings correct the provisional `0.06` and `0.24` figures from the earlier diagnostic. The initial six-digit trajectory print is preserved separately and excluded from numerical comparison; a known binary64 serialization check passes before the final 17-digit observer is used.

After the frozen binary reference's known controls pass, its exact held-source formula evaluates the accepted short endpoint as $y=0.49999910553532610678\ldots$, $y'=-0.00071557195246593992\ldots$, with partner emission $S=-0.99749910553532610668\ldots$. Both paths' printed position/velocity enclosures and cross-root brackets contain the independent values; the largest nominal position/velocity discrepancy is `5.3337965868e-11`. The receipt is `corrector-retry-held-reference.stdout.jsonl`. This validates this accepted interacting step against the analytical motion, separately from the control-flow comparison.

The unchanged inertial reference also passes against the rebuilt library in `0.25` seconds: unused symbol counts 0, 12 and 96, first interval and restart, exact-zero acceleration, independent uncertainty transport, older-history independence and its atomic negatives. Its outputs use the `inertial-production-after-corrector-retry` stem. The frozen analytical binary/root source hashes remain unchanged by `shasum -a 256`; the new production hashes are retained in `corrector-retry-source-sha256.txt`.

The preserved short-prefix observer, rebuilt against the repaired library, passes its known case before repeating the request through `0.01`. It now completes in four accepted steps with zero rejections and retained step `0.0025000000000000001`; all corrections use at most two iterations. The earlier pre-retry result required 15 accepted steps, two rejections and a persistent `0.000625` step. Lease `d45dc197-d60d-4bc1-8157-ddb9294bfb55` completes in `0.148` seconds, exit zero and closed process group. The retained stdout is `early-prefix-after-corrector-retry.stdout.jsonl`. This measures removal of the demonstrated startup inefficiency, without extrapolating a cost estimate for the complete encounter.

After these controls passed, the declared full request was launched with prefix scale `0.125`, continuation step `0.00125`, upper cap `1.575`, 300-second hard deadline and 15-second heartbeat. Its trajectory remains subject to the frozen independent state/root comparison before scientific reliance.

### Extended trace and independent discrepancy

Lease `718b6910-944a-4fdb-94f3-203f2c2b1fbe` completes in `204.152` seconds with diagnostic exit `2` and a closed process group. The solver accepts 497 prefix and 266 continuation steps through `1.5724999999999922`, then atomically rejects the next proposed endpoint `1.5737499999999922`. Both reported cross rows at that rejected endpoint are complete. The old observer did not print rejected self rows, so their absence is not evidence of their exact failure. The raw packet is `corrector-retry-eighth-prefix-event.json`.

The frozen state reference passes all 763 frames and 1,526 path samples, but the frozen root observer fails at one frame, `1.5687499999999923`: both cross-emission brackets are `[1.2668748437499989, 1.2668751562499989]`, while the finest independent emission is `1.266875166831569`, approximately `1.05815701e-8` above the upper endpoint. All three reference refinements lie above that endpoint; their spread is only `1.1777334663e-10`. The other 762 frames pass their root checks. All self-root inventories and all reported transmitter/receiver factors pass their sampled comparisons. The complete trace is therefore not independently root-validated, despite its state containment and later reported root passes. The `-independent-comparison.json`, `-root-comparison.json` and `corrector-retry-eighth-prefix-root-discrepancy.json` records preserve this failure.

The last accepted state is enclosed by positive position `[0.051644593294933136, 0.051647344410559765]` and velocity `[-0.99914175670106598, -0.99908913879828409]`, with reflected negative-path intervals. The independent reference gives $y\approx0.051646305275245136$ and $y'\approx-0.999109127422971$ there. Neither crossing nor reversal has been observed. The next reference speed-equality event is about `0.00013966438138` later; that prediction is not a production event certificate.

### Independent history-join counterexample and repair

Investigation of the discrepant interval finds that it is exactly the initial `root_tolerance/64` probe around a retained source-segment join. Direct inspection of `ExactPairBatch.cpp` identifies an early MPFR join route that passed this unproved probe to a mean-value enclosure helper. That helper intersected its result with the supplied bounds. The ordinary interior caller had already proved strict opposite endpoint signs; the join shortcut had not. The [history-bound analysis](../analysis/stationary-continuation-history-bound.md#why-an-unproved-probe-cannot-restrict-a-root-family) supplies the counterexample and distinguishes source attribution from mathematical invalidity.

A separate public-API control reproduces the defect with stationary receiver $X_r=1$, stationary source $X_s=u$ with $|u|\le\rho$, reception time one, wake speed one and a source join at emission time zero. Its exact residual is $g(S)=S-u$, so the complete root family is $[-\rho,\rho]$. After its reporter passes known input, the old library falsely certifies only $\pm1.5625\times10^{-7}$ for $\rho=10^{-6}$. It also falsely certifies that same narrow interval for $\rho=10^{-5}$, even though the exact family width $2\times10^{-5}$ exceeds the unchanged $10^{-5}$ tolerance. A strict-sign root-free negative control passes. These observations establish an actual public-route enclosure defect independently of the interacting reference or a reconstructed cubic.

The identical control linked against the preserved pre-zero-repair library reproduces both false certifications. Its separate `segment-join-root-reference-before-zero-repair` outputs (`0.25` seconds by `/usr/bin/time -p`) establish that this defect precedes both current zero-representation and correction-loop repairs. They do not identify the historical introducing commit. The pre-join-repair library is separately retained at `.tmp/2-braid/pre-join-repair-libeom_native.a` with SHA-256 `c247a857aab561f3a5fa30291ae6b44eb16f9345bcefd9c7a8713e2aceb4575a`.

The scoped repair moves the join mean-value contraction after the existing strict opposite endpoint-sign and one-sign transmitter-factor checks. The expansion ladder first establishes a containing bracket; only then may the mean-value result be intersected with it. All six probe radii, the final inward-rounded probe, the width tolerance, complement accounting and failure propagation remain unchanged. This follows the intermediate-value and monotonicity argument for each admitted continuous piecewise history. No tolerance or independent-reference expectation is widened.

The fixture additionally exposes every row of the first rejected snapshot as `first_rejected_roots`, preserving the earlier cross-only field. Its root-row serializer is shared with the accepted trajectory output. A hand-specified failed self row, complete cross row and null snapshot pass the JSON observer check before target use; the receipt is `all-root-observer-known.json`, and the separately retained control runs no evolution. This closes the earlier diagnostic omission without inventing a rejected candidate history.

The coordinated join-repair build completes in `5.452` seconds under lease `0f0a930d-1f6a-43e2-bfaa-fef1e00bc46f`, exit zero and closed process group. `stat -f` places the root source at `20:52:25` EDT, library at `20:52:53` and stationary executable at `20:52:55`. After the public join controls and affected existing focused checks pass, repeat the identical full binary request once, with the same 300-second deadline and stopping boundary, then rerun the frozen state and root comparisons. Any remaining disagreement stops reliance on the affected result.

### Join-repair validation before repeat

The public stationary join control passes against the rebuilt library. For $\rho=10^{-6}$, the emitted endpoints are `-1.00000000000000000000000000000001113353018e-6` and `1.00000000000000000000000000000000525605843e-6`; they contain the entire exact family, with outward padding and width below `1e-5`. For $\rho=10^{-5}$, the solver now returns `uncertified` / `numeric_precision_limit_exhausted` with no accepted roots, preserving the width limit. The strict-sign root-free case passes.

The C++ control's original binary64 observer could not distinguish the approximately $10^{-38}$ outward padding from an exact endpoint, so its strict-sign comparison failed after the numerical repair. That observer and failed output remain preserved. A separate lossless Decimal observer passes a known sub-binary64 padding case and rejects known clipped/non-strict bounds before reading the target tokens. Its exact residual checks then give left upper bound `-1.113353018e-38` and right lower bound `5.25605843e-39`. This resolves an observation-precision limit without changing the case generator, exact root family, production tolerance or strict-sign requirement. The collector takes `0.17` seconds; its raw/check records are `segment-join-root-collector.stdout.jsonl` and `segment-join-root-token-check.stdout.jsonl`.

Fresh root and evolution fixture packets complete under leases `0329e99f-cf07-4f1f-aa18-b55ac186192e` (`0.283` seconds) and `9f1def33-88bc-453e-a263-946c80f2ef7f` (`0.691` seconds), both exit zero with closed process groups. Seven unchanged assertions pass directly against those packets in `0.10` seconds: the three earlier root-budget/dominance/traversal checks, the two existing history-join checks, and the two earlier evolution/correction/atomic checks. The existing join assertions are compatibility evidence; the independent extreme-root control supplies the full-family check that exposed this defect. Exact assertion names and pass records are in `join-repair-focused-assertions.stdout.jsonl`. No class setup, unrelated long-horizon mode or recurring test enrollment was used.

The same full binary request is repeated under lease `16f811c3-4f95-4819-8faf-4bc206f52b3d` after these checks, with source hashes in `join-repair-source-sha256.txt`. The frozen binary reference and root observer remain byte-identical to their earlier declared versions.

## Final repaired trajectory and scientific boundary

The final repeat completes in `185.437` seconds under lease `16f811c3-4f95-4819-8faf-4bc206f52b3d`, diagnostic exit `2`, closed process group. It accepts 497 prefix and 266 continuation steps through `1.5724999999999922`, then atomically rejects the proposed endpoint `1.5737499999999922`. The raw packet is `join-repaired-eighth-prefix-event.json`.

Both frozen independent instruments pass the complete accepted trace: 763 frames, 1,526 path state comparisons, 1,526 cross-root rows and 1,526 self-root rows, with zero containment, inventory or factor failures. The formerly discrepant time `1.5687499999999923` now has both cross brackets `[1.2668699999999989, 1.2668799999999989]`, containing the independent emission `1.266875166831569`. The repaired output remains inside the original root-width limit. The corresponding `-independent-comparison.json`, `-root-comparison.json` and `-observation.jsonl` records retain the checks. Maximum nominal discrepancies are approximately `3.36473101277e-7` in position and `6.33382476944e-6` in velocity. This is independent sampled numerical agreement, not a continuous global error theorem.

At the last accepted time, the positive member has position `[0.051644593164572199, 0.051647344439731048]` and velocity `[-0.99914179090018995, -0.99908913159519186]`; the negative member has reflected intervals. They started at $\pm0.5$ and remain on their original sides of zero, moving inward. Neither a crossing nor a turn has occurred in this validated interval. The independent reference's values $y\approx0.051646305275245136$ and $y'\approx-0.999109127422971$ lie inside the final enclosures.

The complete rejected snapshot identifies a different barrier from the earlier partner-width halt: both partner rows certify at 53 bits, while both self rows report `caustic_route_required`, `numeric_self_root_cluster_uncertified`, and `endpoint_root_not_surrounded` after the 512-bit limit. They report no accepted roots and `root_free_complement=false`; this is an unresolved self-root census, not evidence that no self root exists. Source inspection at the same-history endpoint failure in `ExactPairBatch.cpp` shows that this status requests event classification. It does not certify an ordinary fold or coincident birth by itself. The fixture does not retain all candidate coefficients and difficult-cell fields needed for an exact one-step replay.

### Reconciliation with the established stationary-mirror theorem

The live [MEC-007 stationary-mirror boundary owner](../../master-equation-closure/analysis/mirror-close-approach-causal-root-boundary.md) already establishes, on this incoming sharp-law chart, that individual speed reaches one while current separation and delayed partner distance remain positive. The partner root remains simple, and no positive-delay self root exists exactly at onset. Its separately measured event time near `1.5726396643` agrees at the relevant numerical scale with the present independent reference's prediction near `1.5726396644`; neither decimal is an outward event enclosure.

That owner also proves the unchanged-law obstruction: any continuous regular extension into super-field speed creates a positive-delay self root per persistent label, whose acceleration measure is not locally finite at birth. The [binding's singular-event contract](../../app-solver/contracts/master-eom-binding-v1.md#singular-event-contract) therefore forbids automatic publication across a confirmed coincident same-transmitter birth. An ordinary positive-separation fold's finite-width route is not a substitute for that rule. The present failed EOM rows are consistent with the incoming boundary but have not independently classified the exact event.

This corrects the earlier framing of complete-encounter continuation as solely a numerical task. The numerical repairs remove two demonstrated implementation defects and advance independently checked motion closer to the established boundary. They cannot supply passage or rebound under an operator whose admitted self contribution diverges. The next bounded EOM task is to certify the incoming event location and complete census against the existing theorem/reference, retaining the exact candidate/difficult-cell data and terminal diagnosis. Any proposed outgoing transition belongs to the separately derived MEC-002/MEC-003 law work; no root may be removed or regulator selected merely to obtain an encounter outcome.

### Task closeout

Scoped `git diff --check` reports no whitespace errors for the affected tracked production, fixture, queue and work-log files; explicit `git diff --no-index --check /dev/null` checks likewise report none for the three new analysis/evidence documents. The owned-compute supervisor's `closeout --owner-task` check returns `status: clear` for task `01a0a215-c597-7402-bc09-8fa3aca9fc0d`, establishing that no owned computation remains active. The broader encounter and incoming-event certification remain open at the boundaries stated above.

## Continuous incoming event certificate

The operator separately authorized incoming event and complete-census certification. The [full derivation](../analysis/stationary-binary-first-interval.md#incoming-speed-boundary-bounds-and-complete-causal-census) now separates three results: broad global inequalities from the stationary law, a local bound conditional on the production history allowances, and a continuous interval construction from the exact stationary initial history. Only the third supplies the tight event enclosure independently of production integration-error estimates.

The new [independent interval instrument](../../../../scripts/eom/stationary-mirror-incoming-interval-certificate.py) uses dyadic panels, completed-history inverse brackets, outward arithmetic and integral Taylor remainders derived from the delayed acceleration derivative. Its known cases passed before its target: rational interval operations, a constant-acceleration event, stationary and moving-source inverses, source-release crossing, and the analytically solved held-source interval. Independent review corrected the terminal cap's upper rounding and made the event numerator's outward subtraction explicit before target use. Finite-history, positive-distance and positive-delay assertions remain enforced. The source imports no EOM component or frozen numerical trajectory.

The final source SHA-256 is `8d4581349682a8108e508c95972cf4fd0daf4bd58881668e7d5d93668c8b7266`. Its known receipt is `incoming-interval-canonical-known.json`. The final target, `incoming-interval-canonical-power12.json`, completes under supervisor lease `a140c823-9af7-432d-84fa-64581630dae3` in `0.837` seconds (`0.7823598750401288` seconds inside the instrument), exit zero and closed process group. The earlier prototype target is retained separately. The final result certifies 6,441 whole panels from the exact stationary start through `1.572509765625`, then uses positive incoming acceleration bounds to enclose first speed equality without appending a super-field panel.

Outward decimal display of the continuous event bounds is:

| Quantity | Bound |
| --- | --- |
| Event time | `[1.572637654589540, 1.572640138062056]` |
| Positive member position | `[0.051505978182147, 0.051508831342540]` |
| Present separation | `[0.103011956364294, 0.103017662685080]` |
| Partner emission | `[1.280881308632144, 1.282779344830728]` |
| Delayed partner distance | `[0.290230751640356, 0.291627465589404]` |
| Partner transmitter factor | `[0.527011778575762, 0.528403749315797]` |

The negative member has reflected position and the onset velocities are inward with magnitude one. The complete incoming theorem gives one simple partner root per receiver, zero positive-delay self roots per receiver, and an excluded same-time diagonal. Neither crossing nor reversal has occurred. Continuous containment follows from the reviewed panel induction and local event inequalities; it is not inferred from decimal convergence.

The separately authored trace observer passes its own known affine, shifted-state, missing-frame and reference controls before reading this target. Its receipt `check-incoming-interval-trace.json` checks all 6,442 endpoints, comprising 12,884 position/speed comparisons, against the unchanged fine reference with zero misses. All three frozen SciPy event witnesses and all four retained MEC-007 event witnesses fit the bounds. The MEC coefficient token differs slightly from the exact current request product, so those historical rows corroborate at their recorded inputs; they do not provide the certificate. The observer SHA-256 is `c0c397b79d1ff08e62b63cefd9c100f6354b6ba24e40d59204ede31fa1dd3ba1`, and the final packet SHA-256 is `845af76dc5ab0737b231b4c4da0cb3096c735c475d1de35caa6f8d30b5e70f98`. All six displayed intervals also pass exact rational outward-display checks.

The corresponding earlier rational calculation from production endpoint boxes remains explicitly conditional, with its separate known/target/independent receipts. The continuous interval result closes this stationary incoming boundary question on its own authority. It does not certify the EOM solver's integration-error allowances, a generic family of histories, a post-onset transition, Campaign 1, or an outgoing fate.

### Exact checkpoint and rejected-state diagnostics

The existing stationary fixture now reads and writes accepted v7 checkpoints and exports exact unpublished rejected candidates. The checkpoint codec, model matching and rejection rules are unchanged. The diagnostic callback includes the actual joint carrier before discard; the sole existing Borg shadow consumer ignores that additional argument. Candidate JSON retains ordinary decimal tokens and joint symbols, coefficients, remainders and endpoint overrides. Root rows retain difficult-cell details and their actual input-history fingerprints. Candidate exports are explicitly nonresumable and are never passed off as accepted checkpoints.

The public checkpoint control passes ordinary/joint/endpoint memory and disk roundtrips, corruption and model-mismatch rejection, rejected-result and candidate-as-checkpoint rejection, and an inertial final remainder below the minimum step. Its successful run takes `0.253` seconds under lease `d7fdfba8-7b36-4682-a4c7-9628001b704f`. The initial invalid zero-coupling setup and failed run remain retained. The separate candidate JSON observer checks 88 hand-known fields and rejects five corruption cases before target use; `check-incoming-candidate-known.json` records that serialization evidence. These controls validate data retention and replay boundaries, not the dynamics of the rejected candidate.

### Accepted checkpoint, boundary probes and terminal diagnosis

The freshly built fixture reaches the exact prior accepted cut `1.5724999999999922` in `186.174` seconds under lease `a1f726a4-033f-473f-81b5-126725b5c740`, exit zero and closed group. Its `incoming-accepted-prefix.json` preserves all 763 accepted frames, and `incoming-accepted.bin` is an accepted checkpoint with fingerprint `fnv1a64:ac3ee4e868959480`. The frozen state and root observers pass every frame again: 1,526 path comparisons and 3,052 ordered root rows, with zero failures. These repeat checks validate the new diagnostic/replay surface without claiming a new independent reference.

Two independent short restarts of that same accepted checkpoint preserve the model fingerprint and minimum/maximum step of `0.00125`. The controller's existing final-remainder rule permits the shorter final intervals without a model rebase:

| Requested endpoint | Measured outcome | Supervisor lease and wall time |
| --- | --- | --- |
| `1.57262` | One accepted step; complete two-partner/two-self ordered census with one root in each partner row and zero in each self row; new accepted checkpoint retained | `0a92504a-39eb-4e03-90a7-68be1a033b79`, `1.479` seconds, exit zero |
| `1.57266` | Atomically rejected; both partner rows certify at 128 bits; both self rows report `numeric_transmitter_factor_sign_uncertified` at 53 bits | `7cec13db-0c27-4dd4-a0ec-049723b797d2`, `0.686` seconds, diagnostic exit two |
| `1.5737499999999922` | Original rejection reproduced from the checkpoint; both partner rows certify and both self rows exhaust 512 bits with `numeric_self_root_cluster_uncertified` / `endpoint_root_not_surrounded` | `ad537ce9-fe31-4289-9eff-c695da08f200`, `0.870` seconds, diagnostic exit two |

All three process groups close. The pre-event step's independent state/root checks pass. Its positive endpoint is `x ∈ [0.051524650253236486, 0.051527407853650016]`, `vx ∈ [-0.99990710398241378, -0.999854342298369]`, with reflected negative-member bounds. Its raw packet and checkpoint are `incoming-pre-event.json` and `incoming-pre-event.bin`. Each rejected restart publishes its original checkpoint histories unchanged and retains a separate `-candidate.json` diagnostic; neither overwrites the accepted pre-event result.

The original rejection's difficult cell is source segment index 1723, approximately `[1.571240234375, 1.57125]`. At its upper endpoint the outward causal residual spans approximately `[-4.91428e-6, 5.90622e-7]`, preventing a strict surrounding sign bracket. The transmitter factor remains positive, approximately `[0.00266795, 0.01480713]`, while the receiver factor is negative, approximately `[-0.01323135, -0.00095665]`. Exact 512-bit tokens, both ordinary histories and both joint carriers are retained in `incoming-original-rejection.json` and `incoming-original-rejection-candidate.json`. This identifies the numerical inclusion failure; it does not turn that rejected candidate into a solution or a certified post-onset census.

The continuous incoming certificate places `1.57262` strictly before first speed equality and `1.57266` strictly after it. Its theorem proves the exact-at-onset one-partner/zero-self census and positive separation independently of the production candidate. Combined with the existing newborn-self measure obstruction, the terminal scientific diagnosis is first wake-speed onset at positive separation with no regular unchanged-sharp-law continuation. The EOM halt codes remain their measured incomplete-root diagnoses; no code rewrites them as a certified birth or silently drops a self contribution.

### Reproduction and evidence boundaries

The interval witness can be reproduced by first running its analytical controls, then its bounded target:

```bash
"${AAA_VENV:-../.venv}/bin/python" scripts/eom/stationary-mirror-incoming-interval-certificate.py --known --output .local-data/braid-program/2-braid-2026-09-14/incoming-interval-canonical-known.json
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 300 --heartbeat-seconds 15 -- "${AAA_VENV:-../.venv}/bin/python" scripts/eom/stationary-mirror-incoming-interval-certificate.py --power 12 --known-receipt .local-data/braid-program/2-braid-2026-09-14/incoming-interval-canonical-known.json --output .local-data/braid-program/2-braid-2026-09-14/incoming-interval-canonical-power12.json
```

The solver checkpoint command uses the freshly built `eom_stationary_joint_frontier_fixture_cli` with `--controller-continuation 1.5724999999999922 --prefix-step-scale 0.125 --continuation-step 0.00125 --trajectory --checkpoint-out` and the retained accepted-checkpoint path. Each probe uses `--checkpoint-in` with that path, the identical continuation step, and its tabled endpoint. Exact argument lists, deadlines and output paths are retained in the named supervisor leases. New reproduction should use distinct receipt filenames to preserve this evidence.

The incoming event request is complete at the declared stationary-record scope. The interval induction proves continuous incoming containment; the frozen numerical comparisons supply independent implementation checks; accepted checkpoint/rejected-candidate controls establish diagnostic preservation. They do not establish generic EOM global-error certification, an admitted super-field continuation, passage, rebound, binding, breathing, Campaign 1 or a new transition law. The remaining outgoing question belongs to MEC-002/MEC-003 and cannot be settled by further numerical continuation of this unchanged sharp law.

The separately authored candidate-binding observer passes known matching data and seven mismatch/incomplete-token negatives before both rejected targets. Each target then passes all eight ordinary receiver/source history-fingerprint matches across its four ordered rows, plus completeness and finite-value checks on the retained joint arrays. The rejected candidates each contain 1,727 segments per path and 24 shared symbols; 1,077,648 numeric tokens are checked. ExactPairCertificate does not carry a joint-history fingerprint, so these results do not claim a recomputed joint hash or certified rejected self-root inventory. The receipts are `check-incoming-post-boundary-binding.json` and `check-incoming-original-rejection-binding.json`; independent validation notes (local artifact: `.local-data/braid-program/2-braid-2026-09-14/incoming-event-independent-validation-notes.md`) retain commands, hashes, timings and the distinction between mathematical containment, numerical comparison, serialization and provenance checks.

Final scoped `git diff --check` reports no whitespace errors in the changed tracked sources and queue/log owners; explicit `git diff --no-index --check /dev/null` checks report none in the new interval instrument and analysis/evidence documents. The final source/binary hashes are retained in `incoming-boundary-final-sha256.txt`. The owned-compute supervisor's final `closeout --owner-task` returns `status: clear` for this task; no owned computation remains active.
