# Evidence

Write-once results. Named `YYYY-MM-DD-<campaign>-<short-descriptor>.md`. Each file carries: configuration (chart coordinates), instrument, claim grade, predeclared tolerances, result, named falsifier, reproduction command. Files here are never edited after their run closes; corrections are new dated files that cite what they correct. Other documents cite these files by pointer and never restate more than one graded line.
