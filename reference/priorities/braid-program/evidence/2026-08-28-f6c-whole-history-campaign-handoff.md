# F6c whole-history acceleration-mismatch campaign

Closure goal: complete one independently checked whole-history conditional acceleration-mismatch evaluation, demonstrate the declared three-setting accuracy, measure bounded operating costs, and retain a reusable invocation.

## Fixed campaign window and scope

The operator-authorized campaign starts at `2026-08-28T00:54:15Z` (`2026-08-27 20:54:15 -0400`) and ends at `2026-08-28T12:54:15Z` (`2026-08-28 08:54:15 -0400`). The start is the existing goal's creation timestamp, verified at startup; the first wall-clock observation is `2026-08-27 20:54:29 -0400`. This is one twelve-hour window, not a renewable allowance. Independently verified scientific closure, operator stop, safety limits, or indispensable missing authorization may end work earlier. Passing implementation tests or an intermediate result does not close the campaign.

Plainly: the work has a fixed end time. A partial calculation will be handed off honestly, without calling it a complete result or restarting the clock.

The restart authority is [Braid Search Launch Readiness](2026-08-27-braid-search-launch-readiness.md), especially its operator-pause section. Approved geometry, normalized wake speed `c_f=1`, mathematical references, original resource limits and exact historical bytes remain unchanged. Rejected attempts remain separate. No commit, push, unrelated regeneration or evidence deletion is authorized. Shared status owners will not be edited during a frozen run; consumed versions must be preserved before integration.

## Current checkpoint — completed controls, queued audit, private K candidate

The campaign is active under the operator's resume and existing unattended approval. The original stop remains `2026-08-28T12:54:15Z` (08:54:15 EDT); no pause or implementation milestone reset it. This integration preserves the prior handoff's exact `37a883f843076681cd93f720b794ba6780c2f84c7b0fd47523a9522bff33f210` bytes (95931 bytes) and original current identity in `continuation-source-archives/handoff-before-0350-preservation.json`. All task-local paths below are beneath `.local-data/braid-analysis/f6c-whole-history-20260828/`. Older restart sections below are chronology, not current launch instructions.

Scientific coverage remains the independently accepted original intervals zero, one and two; 157 initial intervals, required further adaptive work and both finer settings remain pending. The accepted full conditional causal cover, lossless evidence package and historical byte identities are preserved. Whole-history average/worst mismatch, three-setting accuracy and whole-history measured cost are unavailable. This is not successful scientific closure.

Plainly: the completed operational work improves the reusable path; it does not enlarge the accepted scientific domain.

### Newly completed, independently closed controls

| Exact control scope | Measured final elapsed | Final-summary sampled RSS | Independent closure |
| --- | ---: | ---: | --- |
| Synthetic monitor `eea849ea`, 22 author cases | 0.302426083 s | 67,846,144 B | `continuation/inclusive-control-monitor-author-v1-independent-closure.json`, `f6607bde3049f8510fa519a797d705c6d52406fb59246bb6116e0c83156cfe63` |
| Actual metadata monitor `5ee2ab2c`, 55 author cases | 0.401933542 s | 67,911,680 B | `continuation/retrospective-monitor-author-controls-v3-independent-closure.json`, `5f0f020fff0604aa854de734c5aaa10156d8bae2c545ed4d45d4e1ac737065a1` |

Plainly: these are small synthetic controls, not new interval calculations. Their measured costs cannot be extrapolated to the full history.

All 22 and 55 named cases passed. Maximum sample gaps were 178.153291 and 269.255584 milliseconds. Each published receipt contains five raw samples; the final tool summary adds a sixth sample, whose larger maximum is attributed only to that final summary. Independent post-publication observations at 07:28:57.214Z and 07:40:28.373Z respectively confirmed each run's two recorded PIDs and two groups absent by fresh process table and signal-zero, 13 and 25 original source bindings/identities unchanged, and the shared lock absent. Short-lived excluded probes and unsampled births remain explicitly unrecorded. Both F6c windows were released; no F6c compute reservation follows from either result. Exact tool objects and retained fixture census are linked by the closure records. Earlier 45 monitor and 64 reader controls remain separately accepted under their recorded generations and `c73f237d` closure.

The reusable synthetic form is `node continuation/control-batch-monitor.mjs NEW_RECEIPT EXACT_CONFIGURATION` from the repository root with full task-local paths. The occupied receipt/configuration stems are `inclusive-control-monitor-author-v1` and `retrospective-monitor-author-controls-v3`; never rerun into those outputs. Source admission and actual fresh host checks remain required for a different configuration.

### Immediate queued numerical check

The complete retained-data reader audit of the accepted three intervals is ready for a separate bounded window after the currently queued F5 compiler-only work, subject fresh peer acknowledgment, source/host admission and independent final closure. It has **not run at this checkpoint**. Exact monitor configuration `packaging-review/accepted-three-segment-retrospective.v6.monitor-configuration.json` is `192392b55097bf75f237f0e8a1fd4bee5824cface4bbad7033689002c987e907` (267220 bytes). Independent source/data admission is `packaging-review/accepted-three-retrospective-v6-independent-configuration-review.md`, `8b7a44f04328012ea9721914e37e0b96bd75e1dc5163e0a6aa7d81cbf2b3aaf9`. It binds 478 physical files including itself, 70,744,835 bytes, and preserves all nine historical logical identities. Reader `cd9cb012`, numerical audit `da0898f7`, reader configuration `31e3ac82` and audit inputs `bb1d3fc8` remain unchanged. Current monitor `5ee2ab2c9151408b96fdef0a2d77e4128f10269110b8c7cb06cabed7a32f0157` and author controls `a3fa3c0d` now have the completed 55-case check above.

```sh
/opt/homebrew/Cellar/node/26.3.0/bin/node .local-data/braid-analysis/f6c-whole-history-20260828/packaging-review/actual-prefix-replay-monitor.mjs --configuration /Users/markmorris/vibe/architrino/.local-data/braid-analysis/f6c-whole-history-20260828/packaging-review/accepted-three-segment-retrospective.v6.monitor-configuration.json --configuration-sha256 192392b55097bf75f237f0e8a1fd4bee5824cface4bbad7033689002c987e907 --self-sha256 5ee2ab2c9151408b96fdef0a2d77e4128f10269110b8c7cb06cabed7a32f0157
```

Plainly: this command checks retained evidence through the actual complete audit entrypoint. It launches no new acceleration provider or emission refinement and does not restart the old science clock.

The audit allowance is 120 seconds inclusive, with ordinary work ending at 105 seconds, strict aggregate RSS below 2 GiB, 250-ms polling and at most 1-second gaps, unchanged launch/running host gates, 64-MiB science and 16-MiB log caps. Its complete process/group/lock and result audit remains required. Source-only capacity and reserved headroom are not final measured storage.

### Canonical operational transition and remaining work

The direct scientific caller still has an operational safety blocker: shared K can retire ambiguous live groups, use fallback actions and fresh cleanup waits outside injected inspection, while the caller releases its lock unconditionally on failure. The independent direct-path assessment is `packaging-review/direct-streamed-caller-route-assessment-v1.md` (`fdaea3c8`). A caller-only guard is insufficient. Canonical K `5aa154b1`, H `a327d1ed`, C `5428e4b8`, shared readiness `04a88934` and all mathematical references remain unchanged.

Both peers now support review of a new canonical K generation in principle. A private **nonexecuted source candidate** is being authored under `operational-candidate/`, not used as a second scientific path. Frozen proposal `75387e5c`, fixed addendum `01b64fe5`, and independent addendum acceptance `dfbb2930` reside under `packaging-review/`. The shared write proposal is only the two K operational function bodies, preserving imports, initialization, CLI guard, gate/bootstrap and actually consumed immutable exports. Candidate source acceptance, exact old-byte/identity preservation, both safe-owner acknowledgments and explicit canonical promotion remain pending. Existing old-K windows must close first. The benchmark corrected its earlier lineage concern: its original 194-source declaration contains neither K nor H, so no behavior amendment to its admission helper is required; that earlier reported obstruction must not be treated as current fact.

Plainly: there is a coordinated repair route, but no new scientific launch is admitted yet. Private source review is not permission to execute the candidate or inherit historical acceptance.

The parent-two adapter/caller archive extension remains source-admitted but its new controls are unrun: adapter `d3a6ff0f` / controls `9cf5aae2`, caller `d4bf0171` / controls `bab073b4`. Fresh from-zero descriptors `ef624fc6` are independently checked data only. The two-phase issuer composer `e9600bfb` / controls `4047310d` is independently source-admitted in `parent-batch-issuer-composer-independent-source-review-v4.md` (`fbb063f0`), but its controls and actual watched publication remain pending. Real-issuer process controls `01539ad6` remain disabled and unrun. No pre-reboot batch or old physical identity may be reused as current.

The independent coverage assessment `packaging-review/whole-history-software-coverage-assessment-v1.md` (`0eae53ae`) identifies the reusable route and its real limits: all 160 parents need independently accepted emission refinement for the current audit; 160 advances only reach the first global width decision; consumed parent evidence cannot change inside a saved prefix; and literal fixed-history/frame contracts do not yet support the finer two settings. A bound containing zero is not balance, and unresolved accuracy is not a successful bounded negative. No whole-history cost inference follows from the accepted prefix.

Closure goal: execute and independently accept the queued retained-data audit, finish the coordinated canonical safety transition and complete-domain numerical path, or leave a safe honest checkpoint at the original 08:54:15 EDT stop.

## Operator resume and live-storage boundary — 02:17 EDT

The operator explicitly directed “resume.” The campaign resumed on the original clock, with no compute reservation or surviving process handle inherited from the pause. The fixed stop remains `2026-08-28T12:54:15Z` (08:54:15 EDT). The paused handoff's exact bytes, SHA-256 `212f8e2f9f610b2fb67e86c4d252f52d38f719ad657498604e5777d3d202cd40`, are preserved under the same-hash directory in `.local-data/braid-analysis/f6c-whole-history-20260828/continuation-source-archives/` before this integration. The pause section below is historical chronology, not the current instruction to remain paused.

Measured live revalidation found all 42 pause-census files byte/hash unchanged and their inode and nanosecond timestamps unchanged, but their filesystem device changed from `16777233` to `16777234`. A fresh boot observation places the host restart at 02:11:01 EDT. No historical identity was relabeled as current and no old monotonic deadline was reused. The exact observation is `operator-resume-observation-v1.json`, SHA-256 `532decaf27b687031223c8cb7818d38a87081b1b643d5274244e72b859162e41`, in the task-local lane; the finite diagnosis is `packaging-review/post-reboot-historical-evidence-reuse-diagnosis-v1.md`, SHA-256 `c996c0924932c118cc6cf78d51b7712522cff9f9c1665becff8a5a658657396e`.

Plainly: the evidence survived unchanged, but its present storage identity differs. A new invocation must record current storage honestly while preserving what the old records actually observed.

The existing retrospective reader accepts that separation without a source change. New audit inputs `numerical-review/accepted-three-segment-audit-inputs.v4.json` (`bb1d3fc869349f9b56da7fe4018c380486aa90bfdcabcc7e6b843458ee76a571`) and reader configuration `accepted-three-segment-reader.v4.configuration.json` (`31e3ac824213fc9585dfd99cff59408b3ccd9d4b602b9888b1395e7ad4eac5f9`) preserve all nine historical logical archive identities and refresh only actual physical captures, the new input binding and fresh output path. Independent source-only observation `582604` verified all 464 configured physical files, 69,943,567 bytes and the finite transformation. These configurations are not numerical or process acceptance. The corresponding monitor configuration `packaging-review/accepted-three-segment-retrospective.v4.monitor-configuration.json` (`da4b9cd25ed19318f293fdbfd99641ded3db778f051783171b20b48a170f7974`) was source-reviewed but remains unrun and is superseded for execution by the pending monitor safety repair below; retain it unchanged.

Source inspection found that genuine `continuation:null` starts the unchanged mathematical protocol and avoids the old 319-source physical-identity equality condition. This is preferred over adding a cross-reboot identity exception; it may repeat the three accepted responses, with actual repeated cost measured separately. It is not yet an admitted end-to-end invocation: the old parent-two wrapper tuples require exact nonexecuting archive routes in the existing adapter, and future parent acceptance still needs its independently closed current-generation issuer. No mathematical reference, history, setting or acceptance threshold changes.

The benchmark task identified, and root source inspection confirmed, a process-accounting edge in shared helper `currentOwnedGroup`: an ambiguous live group can be silently retired. A narrow fail-closed guard is being independently reviewed in F6c-owned monitors only; shared C/H/K and EOM files are unchanged. Exact pre-repair bytes and their current identities are preserved in `continuation-source-archives/ambiguity-guard-original-identities-v1.json`. No observed process escape is claimed. F6c tests, builds and numerical launches are held pending the guard's independent review and fresh coordination. F5's separately requested one-stage compiler window is not interfered with; no later F6c reservation is implied.

Plainly: the resumed work is repairing two concrete reuse and accounting boundaries. The scientific result remains only intervals zero, one and two; 157 initial intervals, required refinement, both finer settings and whole-history accuracy/cost remain open.

## Operator pause and exact restart point — 02:06 EDT

The operator requested “pause at the next convenient point.” The already running caller regression finished, all three subagents stopped at complete source/review boundaries, and no further test, build or calculation was launched. Independent final closure at `2026-08-28T06:04:42.498Z` verified all 208 recorded PIDs and 73 groups absent by fresh process census and signal-zero checks, all 25 consumed source bindings and original five-field identities unchanged, and the shared lock absent. Receipt `continuation/issuer-authority-window-independent-closure-v1.json` has SHA-256 `02e47d810eadcd988c37b2ee556e9e09a5a656a24f3200d40c2da6dec5e5e21e`; exact launch, interim and final tool objects are in `issuer-authority-window-tool-observations-v1.json`. These paths are beneath `.local-data/braid-analysis/f6c-whole-history-20260828/`. The F5 and benchmark tasks received explicit closure/release and likewise reported operator pauses. All queued F6c compute requests are withdrawn. No ownership handoff or automatic restart is implied.

**Status: paused, goal not achieved.** The accepted numerical result still covers only original intervals `[0,1,2]`; 157 initial intervals, subsequent required adaptive work and both finer settings remain unevaluated. The accepted lossless package and genuine restart are preserved. Whole-history average/worst mismatch, three-setting accuracy and whole-history measured cost are unavailable. The original campaign deadline remains `2026-08-28 08:54:15 -0400`; operator pause overrides persistence but does not silently create another twelve-hour campaign. Resume only on operator direction, recheck the clock and live bindings first, and do not run beyond that deadline without an explicit new instruction.

Plainly: the machine is released and the work is safely saved. The completed checks are useful progress, not a complete scientific result.

### Last completed checks

| Check | Result | Final monitor duration | Maximum sampled aggregate RSS | Receipt SHA-256 |
| --- | --- | --- | --- | --- |
| Issuer `53355a73`, controls `7a0178b4` | 62/62 passed, including complete main and archived-metadata positive | 7.154407625 s | 162,578,432 B | `c12ef201c2f0204676aec76b197293edddb70f00e584baabc2e8a74b85beba45` |
| Adapter authority installation `f60c09a4`, controls `7401104f` | 122/122 passed | 6.973483459 s | 193,085,440 B | `ebb7b66153ac3df44f82c509f63799a1e9355d29f12710e907d63ae7dc53bcb2` |
| Streamed caller `4b76fd1f`, controls `33ea154f` | 63/63 passed | 53.645318125 s | 571,867,136 B | `8adec78fd1a80d4208279e01eb32876db272e1546064969f6dadf4840dafa16e` |

Plainly: these are synthetic/analytic implementation and lifecycle checks. They ran no actual new parent history, and their costs are not estimates of whole-history cost.

Maximum sample gaps were respectively 404.515417, 448.978458 and 410.416709 milliseconds. Actual launch free-memory observations were 59%, 66% and 67%; the caller's running observations were 65%. Final tools were `47abef` (session `75175`), `bd33d8` (session `16368`) and `ed8a16` (session `96610`), all exit zero. The exact reusable test form is `node .local-data/braid-analysis/f6c-whole-history-20260828/continuation/control-batch-monitor.mjs RECEIPT CONFIGURATION`; the three occupied receipt/configuration stems are `parent-batch-issuer-controls-v7`, `adapter-authority-controls-v1` and `caller-authority-controls-v1` in that continuation directory. Never rerun into those occupied paths. Earlier analytic audit controls also passed all 45 cases (`6f8baa76`); that is not the missing complete `audit_segment` entry-point test.

Independent source admissions are `packaging-review/fresh-closure-authority-independent-source-review-v1.md` (`77b8436c708a739d2d30d11e6e1b338acf2823ea9e11c48f77baf07e3c29a246`), `numerical-review/issuer-owner-filename-independent-review.md` (`ce250920aded876d4485f5ec36d5bd4aa40bdd538617a693d93eabcddb0cf2bc`), and `packaging-review/segment-audit-reader-independent-source-recheck-v2.md` (`a6bd85da536466d5dc5362074d2f44a86b04d74f071b832fd81b0c3604600c38`). The last is source-only: reader `cd9cb012` / controls `24e05768` have not run. Exact `.archive`-suffix failures remain retained separately; the successful issuer writes `consumed-owner.md` with identical owner bytes, preserving the frozen package exclusion rule.

### Resume order and unrun commands

1. Revalidate this checkpoint, all original historical routes and the 42-file pause census `operator-pause-source-census-v1.json`, SHA-256 `61d105e23a92ba2b6a3d7ac87355b3a27732734f24f05c5fbb8db7e95fb7d56c`. It records current hashes, bytes and five-field identities observed at `06:06:00.075Z`; it is not a substitute for fresh checks. Shared readiness remains `04a889345ee7c4cd43b0f41bebb70cb2f176ac4d13af035204347f3c6904feaf`. Coordinate fresh exclusive windows with F5 task `01a046ac-0303-7ac2-b7a1-2bcf75910320` and benchmark task `01a045de-6a34-77f3-bac4-90f2362f6ccd`; no reservation survives this pause.
2. Independently review the newly frozen existing-monitor generation `3d50015c6d455870a779e7c4e42698520ba73912e611f0d60f321ec003f3e995` and author controls `55bad5f29247cf83be50b6d7202fbd802791597824f34beccb5b9ca191d7d104`. Its 43 author groups are unrun; freeze note `packaging-review/actual-monitor-segment-source-freeze-v1.md` is `3ebdbc30e3270882202b2a593a719d17121daea9542032100beee92dad9b090f`. It adds the finite retrospective reader branch and the exact issuer filename/pins. Old successful Node monitoring belongs to archived `ab788d79`, not this generation. The new issuer hard pin prevents the old literal synthetic Python fixture from running; resolve that test arrangement independently without a hash bypass before actual issuer use.
3. Run the reviewed reader controls and the actual complete-audit positive on accepted three-interval data through a newly admitted existing-monitor configuration. The explicit reader v3 configuration is `numerical-review/accepted-three-segment-reader.v3.configuration.json`, SHA-256 `c21edc36169f5d3d75e4f062f417c1aa3f15a9accc801cab8c4f10a512e36658`; its audit input is `6f69a3976440c7888d649dd143f26cc348cab5d19e12773bab42006b0ea6b7c6`. These are unrun configuration inputs, not an executable admission. Fresh segment review remains fail-closed; its original complete-budget baseline must be supplied truthfully, without invented producer identities or a new allowance.
4. Implement and independently check the minimal two-phase data-only issuer configuration composer specified in `numerical-review/parent-batch-issuer-composer-expectations.md` (`d9e97c49448f6a79868c482d3037749c197c8f2baf43748bca4eed0f36cc4156`) and its analytic expectations (`b569c486fbdf67ef7a8026277d4f45101d566764787f6851d16167839ddc2371`). No composer implementation exists. Resolve measured, original-deadline-bound capture/publication ownership before actual use; the pure interface itself supplies no watcher. The genuine final C operation, exact tool/session/chunk, original deadline and final identities cannot be manufactured in advance.
5. Only after the whole post-run acceptance path is ready, freshly admit the already prepared `[3,4,5]` batch. Its exact proposed command is below. Complete all selected stages, independently issue/check the entire batch, package accepted evidence if required, and continue the exact accepted chronological state. Do not consume the original 1,800-second deadline while still improvising a missing acceptance path.

Plainly: the next action is review and completion of the saved execution boundary, not an immediate scientific launch. A ready launch plan cannot compensate for an unready checker or expired clock.

```sh
/opt/homebrew/Cellar/node/26.3.0/bin/node /Users/markmorris/vibe/architrino/scripts/eom/f6c-bounded-operation.mjs --plan /Users/markmorris/vibe/architrino/.local-data/braid-analysis/f6c-whole-history-20260828/continuation/prepared-batch-3-4-5-v1/batch.plan.json --plan-sha256 42589bc128c93e9a9cd2da5f0176afe25075d6c015cbe23bf7dc1961f4b7206f --self-sha256 5428e4b89736730cdae1671f39b3fd5b0067be781fbfb8cda774347a9890b885
```

Plainly: this exact invocation has been prepared and independently checked as data, but has never run. It is not a complete-history evaluator and must not be launched while paused.

### Scoped paths and remaining limitations

Campaign-owned canonical implementation/control paths are explicitly listed in the pause census: bounded coordinator, lossless package codec, parent inventory/snapshot, pure continuation, single-leaf/session wrappers, variable-cell adapter, streamed caller, invocation/batch preparation and parent-refinement composition, their listed controls, and the preparation-only attractor harness. The task-specific `numerical-review/`, `packaging-review/`, `continuation/`, `packaging/` and source-archive lanes retain declarations, exact prior generations, independent reviews and all successful/rejected evidence. This handoff is the sole shared-facing document edited by this checkpoint. No shared `src/eom` source or historical binary was edited by this campaign; no commit, push, unrelated regeneration or evidence deletion occurred.

Required finer history remains unbuilt and unevaluated; the source-reviewed preparation-only harness and helper controls remain unrun. Original numerical/settings/geometry limits are unchanged. Prefix bounds contain zero and do not prove balance. Full-domain accuracy remains unresolved, which is not successful closure or a validated physical negative. The paused source/tool boundary is falsified by any changed recorded binding, missing exact historical artifact, live recorded owned process, unexpected lock, or an attempted promotion beyond these stated checks.

Closure goal: paused on operator instruction with complete observed process closure and an exact restart checkpoint; scientific closure remains open.

## Earlier checkpoint chronology

Startup verified the existing goal, repository instructions and long-running-job procedure. Independent subagents are checking numerical admission and lossless packaging; neither owns shared status documents or frozen mathematical code. The first admitted three-interval launch was rejected at its host resource check before any numerical stage. The current readiness owner is SHA-256 `04a889345ee7c4cd43b0f41bebb70cb2f176ac4d13af035204347f3c6904feaf`, 359,719 bytes. The F5 evolution and parallel-benchmark tasks acknowledged the shared-owner hold and the F6c single-worker limit. F5 owns its separate CLI observability work; this campaign owns no shared EOM source edit or build. F5 subsequently reported its separate bounded build and five synthetic tests closed; no F5 numerical evolution is admitted here.

The fresh `v3` bounded run evaluated original intervals zero, one and two using independently accepted emission refinement. Independently authored arithmetic and operational closure passed. Lossless packaging and genuine adapter equivalence also passed, reducing the adapter's active physical input count by 24. The next 157 initial intervals and subsequent adaptive work remain pending. Subsequent complete-domain work must measure the entire declared history; prefix cost projections and bounds containing zero do not establish scientific closure.

At `2026-08-28 00:36:24 -0400`, the repaired v4 caller's 61 synthetic controls had passed and independent process closure was verified. The repair changes only a missing empty metadata field in the test adapter and stale control-generation pins; the numerical wrappers, adapter and mathematical references remain unchanged. Independent source review is `packaging-review/caller-v4-fixture-repair-source-review.md`, SHA-256 `8b2abbeeabad4a75b77dfffbd38c1462160d0182e131090bebb621700a7b1d0d`, under `.local-data/braid-analysis/f6c-whole-history-20260828/`. The old failed receipt remains failed and preserved.

The exact monitored command was `node .local-data/braid-analysis/f6c-whole-history-20260828/continuation/control-batch-monitor.mjs .local-data/braid-analysis/f6c-whole-history-20260828/continuation/caller-v4-controls-v2.json .local-data/braid-analysis/f6c-whole-history-20260828/continuation/caller-v4-controls-v2.config.json`. Configuration `ba156f7b0ea006101d679469d6373b44b8292c73ecf0d03d25eccb43f53a4d48` selected repaired caller `5d590a14d7f21ff13a347cb5acb5a96c0c3c43cff3915fb4703c65ab3aeda1cc` and controls `ec686f7bf2008be04daab3c3b1321e69501c5c9f32699cf618cf5b6e84e10bb4`. The complete receipt is `continuation/caller-v4-controls-v2.json`, SHA-256 `4b73a1d7511115d34a0ad509c8357bf04764b1cdb5bbc6b807be99cf6d960830`. Session `47752` ended with tool chunk `196203`, exit zero; the control body took 50.981932417 seconds and final monitor completion was 51.206072084 seconds. Maximum sampled aggregate RSS was 517,619,712 bytes over 212 samples; largest gap was 352.451875 milliseconds. The monitor's launch observation was 45% free memory, followed by 44%, 43% and 42%, distinct from the earlier independent 44% probe.

Plainly: the corrected caller now reaches and passes its intended synthetic checks. This does not evaluate a fourth interval or establish whole-history accuracy.

Independent closure `continuation/caller-v4-controls-v2-independent-closure.json`, SHA-256 `8c58ed239e6a03786abbaf06e52c07bbf74a7beff6c3ce9314d57a5122c182f3`, rechecked fourteen source bindings and original five-field identities, all 182 observed PIDs and 70 groups as absent, and the shared lock as absent before and after publication. Exact tool observations, including two retained read-only closure-check syntax/report-parser failures before the successful check, are in `continuation/caller-v4-controls-v2-tool-observations.json`, SHA-256 `9a0c84df0e42184b57d09e8ba2f3fd1e153c66eb9fad1bc959f3ae909e8668b4`. The exclusive control window was then released to peer coordination; no F6c numerical, build or test process remains at that boundary.

The independently authored pure parent-batch closure checker also has a scoped acceptance record at `numerical-review/parent-batch-pure-closure-independent-acceptance.md`, SHA-256 `aad0e68de374993f30c856569617e5bca2cc97fa30f23c859f693835dab9f99c`: 58 independent synthetic cases passed under the previously closed monitor. Actual issuer/monitor admission and the first new parent batch remain pending. No nonempty fresh-parent authority is inferred merely from passing those pure controls.

## Result and exact invocation

Whole-history scientific result pending. This file is the campaign-owned handoff, not an acceptance owner consumed by existing numerical code.

The successful three-interval invocation was:

```sh
node scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs --spec /Users/markmorris/vibe/architrino/.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/invocation-three-refined-parents-v3.json --spec-sha256 716e1f4fb4e035c5e3376c843f10ba6a7089b2a2bb78c7a8a4f6965e6be90d9f --self-sha256 70e25388ce5874e7e069f1203caf80b40b2357e19acc51b5ad8a01e804d7ec66
```

Plainly: this is the exact completed prefix command, not yet a complete-history evaluator. Its occupied output directory must not be reused.

The invocation fixes 17 direct bindings, 162 runtime bindings, three advances and no evidence package. Independent metadata admission `three-parent-v3-independent-metadata.json` in the streamed-diagnostic lane (SHA-256 `6a77fe67e71170b497609fd42ce9803618c9d262010148bdb42d305c37c960f7`) passed 6,723 checks in 21.451491708 seconds. It preadmitted and finally rechecked the complete 319-file physical union, 62,528,525 bytes, including the 308-file adapter pool. It verified eight original histories, 81 frames, all 160 parents and exactly refined parents zero through two without numerical work. The final caller/control generation passed 61 controls in 48.092946583 seconds (`62735/fc4923`, exit zero), including independently reviewed publication replacement, combined-source-limit and competing-process rejection repairs.

The caller completed in session `32609`, final chunk `fe039a`, exit zero. The output is `pilot-three-refined-parents-v3/leaf-evidence.ndjson`, SHA-256 `5d1ba9394793e781f875298c1622a07dab0063e4c9c7d9b974cc2219e30442c7`, 2,594,278 bytes. Its operation is `pilot-three-refined-parents-v3-outer/operation.json`, SHA-256 `c8ad82d68180599a5e05f4ad50b2cc41153aabb72f2bc7ba6812a5b2d1aa55b4`, 635,041 bytes. Both paths are relative to `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/`. The exact final tool wire is preserved in `three-parent-v3-final-caller-tool-record.json` there, SHA-256 `1bebb25cad530bd069980a4f1bfb0eba19e34cf6dd99188fcfbece7add65ccd4`, recovered from the original tool record rather than reconstructed from current files.

Plainly: the stored stream contains the actual three response/transition pairs and a complete footer. Its independent arithmetic and process receipts—not that footer alone—establish the accepted scope.

Independent arithmetic checked 271,021 conditions in 6.175210 seconds, including 768 rows, 1,344 coverage records, 2,304 acceleration-component checks, 960 member intervals, 24 integrals and 72 witnesses. The third interval's squared-residual integral is enclosed by `[0, 9.675216816958217e-7]`; the three-leaf partial sum is `[0, 2.893826384416237e-6]`, with partial peak magnitude `[0, 0.012410144149620187]`. These are partial bounds, not whole-history RMS/peak, not achieved `1e-6` magnitude accuracy and not balance. The independent arithmetic body remained unchanged; the audit's transport-only adaptation admits the new explicit `evidencePackage:null` field.

Measured launch memory was 46% free and subsequent readings were 45%. The maximum sampled aggregate RSS was 576,978,944 bytes over 275 samples, maximum gap 287.7935 milliseconds; all four logs totaled 272,214 bytes. Python reported 66.555206 user and 0.284152 system CPU seconds, with individual maximum RSS 325,091,328 bytes. The caller's elapsed stamp is 67.750050708 seconds, taken before final source/stdio cleanup; it must not be relabeled exact final-exit duration. The separate 95.504-second external observation includes tool approval, transport and polling delay. Final cleanup remains constrained by the original deadline and final-sample coverage. Independent census found all nine observed PIDs, three process groups and the lock absent; seven artifact paths refer to six distinct inodes, 3,501,533 unique logical bytes and 3,780,608 allocated bytes. The complete independent receipts are linked below.

Plainly: the prefix ran below the unchanged resource caps and left no observed owned process. Its measured costs describe this run only; no whole-history cost is inferred from it.

The independent three-interval closeout is `three-parent-v3-independent-acceptance.json`, SHA-256 `ebbcde95d830c503bd06a9ce9e54fe9942262a31a3272e5e14259f036123b40a`, in the same streamed-diagnostic lane. It links numerical receipt `57b948f705510e401c54a2e516af4dfe32236e5e98d4c64567e002d2181de457`, strict operational receipt `553394ea3b1bb150d20d2b487deedf814e60e098314c8a3a2e4510b74eea8532`, and 666 final-wire checks. The reusable detached initial-segment declaration is `three-parent-v3-independent-segment-acceptance-v2.json`, SHA-256 `64aade8a958f487eab39a6d55b0a7fb36b9bcbf75116b0e075e749ce1dbcba8c`, with an explicit null predecessor. The earlier declaration and pending caller observation remain unchanged historical records. The detailed reviewer handoff is `three-parent-v3-independent-handoff.md`, SHA-256 `4c35ff2f9b1b9c0593e2997843c79c6f20eda692e3ef379caf569e412b749657`.

## Accepted lossless package and active input capacity

The exact executed package command was:

```sh
node scripts/eom/f6c-bounded-operation.mjs --plan /Users/markmorris/vibe/architrino/.local-data/braid-analysis/f6c-whole-history-20260828/packaging/accepted-parents-1-2.v1.plan.json --plan-sha256 e5e0c4e79f3f46235c36b65ce6e7c0bbba44c60818ab936d67b520f6b218c144 --self-sha256 5428e4b89736730cdae1671f39b3fd5b0067be781fbfb8cda774347a9890b885
```

Plainly: this bounded operation ran the writer and independently authored decoder in sequence. It performed no acceleration calculation and did not remove any original evidence.

The independently admitted plan is 89,998 bytes and binds 161 physical sources including itself, 12,203,652 bytes. Final supplied controls passed 24 tests, followed by six tiny real-process success/failure/replacement/deadline controls and a complete Python runtime inventory of 114 files including 45 cached files. The actual invocation returned session `16249`, final chunk `bdea38`, exit zero, after launch and running host observations both reported 45% free memory. Its exact tool records are preserved at `packaging/accepted-parents-1-2.v1-caller-observation.json`, SHA-256 `f1857afbbd58f1edd340840fdb9a86a9a71888cf280b3de5d1990cf0efeb28b8`, beneath the whole-history local lane.

The package is `packaging/accepted-parents-1-2.v1/evidence.f6cp`, SHA-256 `d0ca10de0e09dbd891313b08f788a17df4934a49f505390601d1237bedeb5f32`, 8,093,747 bytes. The independently decoded 28 original members total 8,083,912 bytes; format overhead is 9,835 bytes. The original source paths, hashes and exact payload bytes remain in the inventory. Rejected parent-two attempt one is excluded and retained separately. The retained private publication path shares the package's inode; it is not counted twice. The operation is `packaging/accepted-parents-1-2.v1-outer/operation.json`, SHA-256 `d9d4212ab1547a2d88a151c472b8d84ec130b04f287f63bab370bd01cc0ed84c`, 435,943 bytes. Independent decoder rerun `82a442` and source/cost audit `bda9be` accepted the actual package.

Measured operation accounting reports a 1.276816375-second pre-final-cleanup stamp, maximum sampled aggregate RSS 244,563,968 bytes, 17 samples, maximum gap 221.437084 milliseconds, eight distinct output inodes, 8,529,690 scientific bytes and 32,388 log bytes. As with the earlier caller, the stamp is not relabeled exact final-exit duration. All observed package processes and groups closed and the shared lock is absent; the final independent receipt supplies the complete timing and storage qualifications.

Plainly: packaging added a small header and preserved every byte. Its benefit is fewer simultaneously consumed files, not reduced on-disk bytes while the originals are retained.

The genuine adapter comparison used two fresh metadata-only contexts and verified identical original histories, frames, context, all 160 parents, and refinement selection `[0,1,2]`, with zero numerical and geometry counters. The active adapter pool changed from 308 files / 62,145,145 bytes to 284 files / 62,237,665 bytes: net 24 fewer files and 92,520 additional bytes because the package, reader, reader controls and inventory replace 28 loose inputs. This is measured active-input relief, not permission to exceed the unchanged 512-file/1-GiB complete-source cap.

The accepted paired metadata run is session `52629`, final chunk `b6b23a`, exit zero, measured monitored duration 45.893338 seconds, maximum aggregate sampled RSS 675,414,016 bytes across 183 samples. An earlier conservatively 45-second-limited review attempt stopped at 45.017 seconds because whole-process profiling made source validation too slow; its files are retained separately and it produced no accepted snapshot. The successful retry used a declared 120-second review limit, below the unchanged 1,800-second maximum, with numerical-boundary tracing after metadata construction. Both accepted exact original-parent snapshots have SHA-256 `72d023dbdd35a71d59ac2d9c90afe2aa02af33dda0d00cbf1cd31123f0a6e511`, 20,355,043 bytes, at `packaging-review/actual-adapter-metadata-v2/{loose,packaged}-original-parents.json` beneath the whole-history local lane. All observed metadata processes/groups closed.

Plainly: the real consumer, not just a container test, read the package and recovered exactly the same inputs. The remaining work is bounded chronological continuation and the complete scientific evaluation.

The combined actual acceptance is `packaging-review/actual-package-adapter-acceptance.md`, SHA-256 `7ef76d5b278dda9e27aeb1bb9f9ddeda28c6f8fa4d44c4028d880c98fe89d6f1`. The separately bound original-parent snapshot declaration is `packaging-review/actual-adapter-metadata-v2/original-parents-acceptance.json`, SHA-256 `2dcfce20a08186a779dbc20327363bbe626f550f614d8f0270cd2849f6c508fe`. Both are beneath the whole-history local lane. The latter authenticates original parent identities only; its numerical and physical acceptance flags remain false.

## Continuation and reusable refinement checkpoint

The independent continuation expectations were frozen before implementation at `packaging-review/continuation-expectations.md`, SHA-256 `37ff8496aee7211e4a04f2c420add4fc38f1a5edbcc1e2fc7bb3c71e69e820bb`. The pure continuation module `scripts/eom/f6c_leaf_continuation.py` (`b98cce1a5dc1a1e3903146de39e8ff25d4bae996bd9bcd99ca4205fcb2ee6b0a`) and diagnostic/stream generations `88b8fece4d8facc4943b01ebb5eb1cac2fe1061fecc86cae157a5d3ba4658a1e` / `12503e98995464abdca6b1c54616c27797f7ba8849d12217e8677fab7825e46a` pass independently authored exact constant-response, 160-leaf terminal and 164-response adaptive multi-segment checks, with 51 independent hostile-input rejections and zero provider calls. Acceptance is `packaging-review/continuation-independent-acceptance.md`, SHA-256 `f42945515418b8043cc048b6724146ee5f2aa89c64aa8d0ccd75defbbafd7945`.

Plainly: the restart code reconstructs the original sequence through the unchanged numerical protocol. It retains spent evaluations, split counts and pending children; it does not start a fresh work allowance or treat saved output as an independent numerical oracle.

The two independent synthetic batches closed in `eb1eb1` and `6396b6`, respectively. Their monitored durations were 57.833702958 and 4.714607333 seconds, with maximum sampled aggregate RSS 138,985,472 and 95,895,552 bytes. The full 3,200-response exhaustion stress case was not run; the separately derived unchanged-budget invariant is recorded in `packaging-review/continuation-budget-invariant.md`, SHA-256 `9ebe265d27aa2818e0c9b90600178fec9e2519cd62fc308e435dfee9b4d27b63`. No whole-history scientific cost is inferred from these synthetic controls.

The source-bound caller generation `6fee612b5f177117914ca6308d404eaa164dc32444b2962db56540973543e369` separately passed an independently authored valid source-admission case and 13 contradictory receipt, coverage, process, mathematical-source and parent-snapshot cases (`2f4904`, exit zero). Real-data preparation then authenticated the accepted three-parent prefix and produced `continuation/source-validation-v1.spec.json`, SHA-256 `286c40dd4c987a8c99747b5f307e810f8dc156488e682b7c54ed03d4163b14b1`, 123,547 bytes, beneath the whole-history local lane. Preparation closed in `02b5d6`, exit zero, at 0.627032166 monitored seconds with 238,485,504 maximum sampled aggregate RSS bytes; observed processes were absent in `0a97f4`. Its exact tool record is `continuation/source-validation-v1.tool-record.json`. This specification is metadata-validation evidence, not authorization to evaluate the still-unrefined parent three.

Plainly: the real saved receipts and preserved source versions now pass the new caller's input checks. Genuine adapter-to-prefix restoration, final caller-generation controls, and accepted refinement of the next parent remain separate requirements before numerical continuation.

**Subsequent independently accepted restoration:** `packaging-review/actual-prefix-replay-acceptance.md`, SHA-256 `090beee4edb72e9e1daa296c0ad78799963f93c466ef1e3ec4908d76d2fde73c`, closes the genuine packaged-adapter restoration. Its receipt `packaging-review/actual-prefix-replay-v1/actual-prefix-replay.json` is `8957c3f00f27c95de4bdbfeeebf605389e4e7384ebb4be172e746929ddcebcba` (112,107 bytes); the monitor is `0144bbd39bbccb77d0d0719108b2c47bae481e6120223abd3a455910660f6811`. The reviewed operation checked 325 physical inputs totaling 107,590,091 bytes before and after, restored three exact protocol evaluations with zero new provider/geometry calls, and adopted the state with all 80 split counts equal to one and global next generation 160. The next request remains generation 3, frame 1, path `[1]`, reception `[0.0030000000000000001,0.0040000000000000001]`. Monitored completion was 33.10106 seconds, including 8.507067417 seconds of pure restoration; maximum sampled aggregate RSS was 475,381,760 bytes over 132 samples, maximum gap 269.824 milliseconds. The live host probe started at 46%, reached a minimum of 40%, and ended at 41%. Tool `a848b1` returned exit zero; observed processes closed and original source identities were rechecked.

Plainly: the real accepted prefix is restartable without new acceleration work. This result establishes the restart seam, not the pending parent-three refinement or whole-history accuracy.

Caller/control generation `6fee612b`/`aed08abe` subsequently passed all 61 supplied controls under a separate watched run (`a65703`, exit zero): 47.675948791 seconds, 483,835,904 maximum sampled aggregate RSS bytes, 190 samples and maximum gap 267.752667 milliseconds. The receipt is `continuation/caller-controls-v2.json`; every observed owned process was absent and both source hashes remained unchanged. A preceding inline-monitor attempt was rejected by the unchanged competitor guard because the monitor's own command line contained a scientific runner filename. Its rejection record is retained; the successful retry moved monitoring code to a file without weakening the guard.

The subsequent exact archive-routing repair preserves old adapter/control bindings repeated inside accepted metadata as physical archive routes, rather than conflicting with newer canonical generations. The original logical bindings and frozen mathematical-source restrictions remain unchanged; Python reads use the same exact-tuple routes. Caller `f1dd28d4699742677be9d6d0df5740e34cc51feb4dffd084bfe020c837c1df5a` passes 19 independent source-admission outcomes, including archived old adapter and operation inputs with changed current canonical files. Review is `numerical-review/continuation-archive-routing-review.md`, SHA-256 `6c2bce10cc0342647fe3e6c38986b7656a644802b2cc3a6cb8fe47717f650a14`. This later generation still needs its final integrated regression run before a numerical launch; the earlier 61-test result is not relabeled as that run.

Reusable serial batch-refinement semantics are frozen at `numerical-review/batch-refinement-and-generic-inventory-expectations.md`, SHA-256 `b422bf9497571a27db7abb9ee5dde683c8d9a49db8e93135d8fc5a62efcae431`; the exact generic inventory and externally admitted closure-snapshot schema is frozen at `numerical-review/generic-inventory-v2-closed-schema-expectations.md`, SHA-256 `856c05077241bf9c28d75c21fcb50beac0afd23546c4bbbad9be7abd5d0f6710`. These precede the scoped I/O and parser implementation. The pure refinement proposer, independent mathematical comparison, package byte writer and generic resource coordinator remain unchanged. Each operation predeclares its exact parent list; a failed or incomplete batch cannot silently promote a completed prefix.

Plainly: this work makes the existing checked refinement reusable across parents while keeping one shared deadline, one resource budget and complete evidence for every declared parent. No batch launch or new refined parent is accepted yet.

The generic inventory parser `scripts/eom/f6c_parent_evidence_inventory.py`, generation `d69db22ad20881a94a950102e70d438792493fa52efde666575bc53100bd784b`, is independently accepted at pure transport scope: 63 cases and 363 checks, including fresh and historical closure inputs and 61 rejection cases. The review is `numerical-review/generic-inventory-independent-review.md`, SHA-256 `7b6138c8f85eb6acad56d4e8beb261e23f12b87ce211a5c5aabdb26af2aa966c`. The independent known-answer fixture has 25 unique objects, 796 payload bytes, 7,176 index bytes and 8,025 complete package bytes; these are synthetic format counts, not measured whole-history costs. Batch I/O, generic package transport and fresh-batch adapter acceptance remain under separate implementation/review.

## Reusable batch integration checkpoint at 23:00 EDT

**Independently accepted generic packaging transport:** `packaging-review/generic-driver-independent-acceptance.md`, SHA-256 `d891caff660c66d9b18ca5f2133852983cc933918016ca8e5fe287995af71cc0`, accepts driver `5e78cd52c3592ec404e636748eb0e71e54f086d84b79e4d1778d27dfde674b4a`, controls `0cf7f32bda979d0feac596abf41c0e6e38945e65e6de70e673be6729dbedeeb9`, and independent generic reader `693da598db446dbe6045d07ab4bbd13175c54fc2cd5da3b4b110e2bd94ea763f`. The reader uses the unchanged independent byte decoder, not the production inventory parser. Three real-process synthetic controls include exact independent package bytes, false-closure rejection before the writer, and incorrect-total rejection before launch. The complete suite closed in 2.075433708 seconds; the successful registered operation measured 1.154598833 seconds, maximum sampled aggregate RSS 227,852,288 bytes, 17 samples and maximum gap 203.317167 milliseconds. Tool `7ecd17` exited zero; all seven recorded process/group identities and the shared lock were absent in `8f2328`. The test explicitly substituted only a synthetic 80% host-memory observation; actual process, RSS, byte, identity and closure checks remained active. This is transport acceptance, not actual generic evidence packaging or candidate science.

Plainly: the reusable package path now works through the actual process coordinator, including rejection paths. A real batch still needs real host admission and independently accepted scientific evidence.

**Batch review and bounded controls:** independent review identified output-path versus inode accounting, unenforced stage/closure byte allocations, and original publication-descriptor retention gaps. The producer's final callback also allowed a byte-identical replacement after its last identity check; the independent test demonstrated the defect and passed after the narrow repair. Frozen independent expectations and results are under `numerical-review/parent-batch-source-review-and-independent-controls.md` (`d198e40b19afc7c8ae405616a06466c57cfed2d3668cc6afb5a2df36d1293ef2`), with 34 plan cases and 28 publication cases passing against their recorded source generations. The repaired publication result is `parent-publication-independent-izyxhovh/result.json`, SHA-256 `e3b8b3d9430bfe6f0db18e9c8150262496d2dc2d2a20b47c7ba67ada6f3c1dfd`, tool `0165c6`, exit zero. A later cross-module review found a reserved completion-log field in the batch hook response that the unchanged coordinator would reject; that integration repair remains pending at this checkpoint. No actual parent-three-or-later refinement has run.

The watched author batch suite retained one stale field-count test failure without promoting its result: 40 preparation tests passed, 30 of 31 verifier tests passed, and the subsequent JavaScript suite was not started. The full observed run was 73.544568625 seconds, maximum sampled aggregate RSS 325,795,840 bytes, 292 samples and maximum gap 262.693459 milliseconds, with host free memory 45%. After the assertion was updated, the targeted publication/interface and 11 JavaScript controls passed in a separate 0.586235292-second watched run (`5b8609`, exit zero), maximum aggregate RSS 203,259,904 bytes. Its owned processes and lock were absent in `61c2ee` and `c015a1`. Earlier sandbox-denied monitoring attempts and the 33.026-second control run that exceeded its requested 30-second coordination window are retained as distinct observations, not relabeled as within-window successes.

Plainly: these are concrete repairs and measured tests of the reusable batch machinery. They neither refine another scientific interval nor establish whole-history operating cost.

**Adapter work in progress:** original adapter `dff515459655b5795a5ffd60966fdf2a2a270b2e0602b12eabb318b0c17824c6` and controls `f561c331187b959219c12251cba07bf230758a4bb45c90cba7b838165242c4a3` were copied byte-for-byte before edits; original five-field identities are preserved in `continuation-source-archives/adapter-original-identities.v1.json`. The common original-token, row/piece and strict-narrowing block is now shared by the old and proposed fresh acceptance routes. The explicit fresh route has a separate independent validator interface; it remains fail-closed until that validator's reviewed source/control pins exist. Storage expectations were independently frozen at `packaging-review/generic-adapter-storage-expectations.md`, SHA-256 `22162c4032ca3c417d54cc11b1b7921db298bb0ed986bb717da37282654c635c`; closure-validator expectations are `numerical-review/fresh-parent-batch-closure-validator-expectations.md`, SHA-256 `7132bcf6db99bef0b2255418f656e3fb5900eb23fac9d1400d294d5ba8fd2eed`.

Adapter/control generation `59c7cc328311ddfa75d5545468f59b5a5826afde052b0a88979db6ec03b9a8ac` / `01918b2ead71cdaf4ea6060a6ab0f89dbfa901443ab4d1bc76b6ac79822bc6b4` passed all 116 author controls in `31f403`, exit zero, with original source hashes unchanged. The complete watched duration was 5.45198225 seconds, maximum sampled aggregate RSS 158,482,432 bytes, 22 samples and maximum gap 262.853333 milliseconds. Launch host free memory was 46%. Receipt `continuation/adapter-controls-v1.json` is SHA-256 `3aa3b82af9c0fcc9e25f2d43eefa6b1c65895c8c8751740a96542d759a10fac1`; observed child 49800 was absent in `18234c`. Exact invocation:

```sh
node .local-data/braid-analysis/f6c-whole-history-20260828/continuation/control-batch-monitor.mjs .local-data/braid-analysis/f6c-whole-history-20260828/continuation/adapter-controls-v1.json .local-data/braid-analysis/f6c-whole-history-20260828/continuation/adapter-controls-v1.config.json
```

Plainly: the old adapter path still passes its controls, and the proposed storage route passes its own author tests. Independent storage review and the separately authored fresh closure validator remain required before actual use. Repeating this test command requires a fresh receipt destination; existing evidence must not be overwritten.

The shared readiness owner was rehashed at 22:59 EDT and remains `04a889345ee7c4cd43b0f41bebb70cb2f176ac4d13af035204347f3c6904feaf`. No shared EOM source change or build has occurred in this campaign. The proposed first refinement batch is exactly parents `[3,4,5]`, with 8 MiB producer and 4 MiB comparison ceilings per parent plus a shared 4 MiB closure reserve: 40 MiB of predeclared suballocations within the unchanged 64 MiB operation limit. These are ceilings, not measured cost estimates or launch authority. A reusable source-bound preparation-only entry is being implemented so later batches do not require bespoke plan writers.

Closure goal: complete the fresh-batch acceptance path, measure an independently admitted bounded batch, and continue the whole history without changing the mathematics, resource limits or original twelve-hour deadline.

### Shared compute transfer at 23:15:50 EDT

F5 reported direct operator approval for its fixed experiment and requested the next exclusive window. After the remaining small controls closed, this campaign explicitly released compute at `2026-08-27 23:15:50 -0400`. Independent process census `ff25fb` found no owned F6c test/scientific process; root lock check `0f8a7b` confirmed the shared lock absent. F5 owns the subsequent parser/evolution/check window, subject to its own real admission, declared 12,600-second inclusive cap and original 08:56:11 EDT deadline. All F6c agents are source-only: no competing build, numerical job or process-control test until F5 reports closure and the window is coordinated again. This does not authorize any new scientific choice or change either campaign's limits. F6c's deadline remains 08:54:15 EDT.

Plainly: the accepted F6c checkpoint is idle computationally while the approved F5 experiment uses the machine. Source review and explicit invocation preparation continue; this pause neither counts as F6c scientific progress nor extends its clock.

Independent storage acceptance `packaging-review/generic-adapter-storage-acceptance.md`, SHA-256 `ec762269ab5534ec5eb1f3b419d6800825ec341cedb5a64bb41b3b239251f7be`, accepts adapter `a16c43017ad0b5df0cacd50c0d696f17d2d05e7c2b58100a18f433b130e3b99e` and controls `01918b2ead71cdaf4ea6060a6ab0f89dbfa901443ab4d1bc76b6ac79822bc6b4` at storage scope. There were 191 independent checks and 59 hostile rejections; 47 preexisting definitions and the extracted original-token/row/narrowing block were structurally unchanged. Real-host watched completion was 0.235025208 seconds, one sampled aggregate RSS observation of 54,640,640 bytes, with 46% launch free memory. Tool `8823dc` exited zero; child 50104 was absent in `79d4c4`. The fresh scientific authority table remains empty until the separate closure validator is independently accepted.

The new v4 caller transport is in progress under frozen expectations `packaging-review/streamed-caller-v4-storage-transport-expectations.md`, SHA-256 `8a46944fbe98193c413b3a1a3ac8e1a28915dc18e36ab3c2326f134c0421c1e5`. Original caller `f1dd28d4`, controls `aed08abe` and preparer `16d76f7b` were preserved with five-field identities in `continuation-source-archives/streamed-caller-before-fresh-evidence-identities.v1.json` before editing. No v4 process-control run or actual v4 invocation is admitted yet. The fresh-closure validator's first synthetic graph run is provisional: its author identified two control-construction hygiene issues, and root review found a tuple/list API mismatch and incomplete explicit role/control pins. These are being repaired before acceptance, with all earlier attempts retained. A proposed RSS-boundary concern was withdrawn after both reviewers read the actual frozen helper: `acceptRSS` requires strictly less than 2 GiB (`6efa1c`), and the validator already matches that executed guard. No resource limit changed.

Plainly: the storage layer has independent acceptance, but its scientific admission and caller integration are not yet a runnable accepted whole-history evaluator.

### Source-only integration checkpoint after the F5 launch

F5 reported actual launch at `2026-08-28T03:19:32.519Z`, with its caller owning the shared lock. Its later coarse-stage checkpoint reported normal scientific halting at incomplete root certification, followed by the medium stage; those are F5-reported dependency events, not independently measured F6c results. F6c has launched no process controls, preparation CLI, numerical evaluation or build during this exclusive window. Readiness was rehashed in tool `74d13f` and remains `04a889345ee7c4cd43b0f41bebb70cb2f176ac4d13af035204347f3c6904feaf`.

The proposed preparation configuration is `continuation/proposed-batch-3-4-5.preparation.v1.configuration.json`, SHA-256 `e65474ac0f776d7ced1e7e8dfa764b16dc235afe29c5ce8049b1f74be1060f7e`, 135,450 bytes. Its read-only source census is 236 physical files / 49,704,881 bytes, including 159 runtime bindings, the original 198-source ancestry and four historical stage logs. It routes only the actually consumed `46a827d1…` document archive. Its companion Markdown records the exact proposed preparation command and fresh output destinations. No preparation or scientific invocation has been executed from this configuration; current five-field identities must be revalidated after F5 releases compute.

Plainly: the next batch has a reusable, explicit input declaration. A saved declaration is not permission to launch or evidence that the batch has succeeded.

Independent batch transport review `numerical-review/parent-batch-independent-transport-review.md`, SHA-256 `61970d050ade6a42811855638d816cadf6be373ae7f906d50ffaabb770758449`, now records 71 passing independent cases against runner `566666cf…`, preparer `112f5abf…` and verifier `8345e495…`. This supersedes the earlier checkpoint's pending reserved-field repair at transport scope. Prepare-only writer source review `packaging-review/prepare-writer-source-review-v1.md`, SHA-256 `bd046d215e224f668b212974fae6091ba339902451e4d1ff53d7b58427c9318c`, finds its three identified defects repaired in writer `9ed04712…`; its separately authored controls `3fbaeabc…` remain unrun.

The independent fresh-closure checker is now source-reviewed at `3eefbb8767a0337024066f8949770fbf47f39edc308aaf598372cf95b3dba223`, with controls `f45ccfb0ff9609fe267f25c1ba2521ec58134f9caf7d128b09e0adfde9e6a979`. Repairs include exact role/control binding, tuple/list input normalization, complete stage source/output/resource attribution, publication-alias rejection before private capture, and duplicate claim-row rejection. The 58 planned synthetic cases are not executed results. Actual independently captured source/output/process observations and receipt issuance remain required; the adapter's fresh authority table remains empty.

Plainly: the checker can now be tested against the actual batch format without relying on an unchecked success flag. It still has neither measured control acceptance nor an actual batch to accept.

Root integration draft adapter `b4dc731c53c080e6494cbd450fa91842d85f4bd84b57aa4bdba4c03b216087e1`, controls `a1dc70f71b0f132db01c5d394ec8c45818c60a71d612aa6266fd735b2a014bb2`, and caller `281140b04161cf890b9df21182c5c959aad1ade572cd0c6a585bb94310f21ffd` address three independently identified v4 gaps: a public path matching its own private-alias pattern; incomplete comparison of historical v4 storage selections; and one-sided rather than exact fresh-source accounting. The adapter records physical reads only during its two fresh-evidence admission phases, including reused handles and logical reads from packages, then exposes an immutable fresh-source inventory. The caller requires exact equality with the advertised fresh sources before any provider call. A bounded conversion of immutable checker records preserves the frozen comparison helpers' exact plain-record input types. Independent source review accepts these repairs; new controls remain unrun and caller pins still name the prior accepted adapter generation, deliberately preventing draft invocation admission. Earlier bytes and original identities were preserved before changes, including tool `580e4b` and `continuation-source-archives/adapter-before-immutable-result-normalization.json`.

The finer-history compatibility review is `continuation/finer-history-compatibility-source-review.md`, SHA-256 `0aa6d809aa536e36a904fd143dd1ee9af6415a87cfadc75a1bedd5d2eedfd6ba`. It confirms that the harness can export variable-length histories without a representation-only EOM change, but frozen geometry/proposer interfaces limit histories to 1,760 pieces and frozen quadrature/aggregation require 80 frames. A genuinely general indexing layer needs independent mathematical acceptance; disguising a larger history as 80 frames is not valid. No finer history, generalized numerical authority or whole-history accuracy result is claimed.

Plainly: reusable storage and continuation are progressing, but the full requested result still needs both real calculations and an honest way to handle the larger declared histories. Neither interface work nor a zero-containing bound closes that obligation.

Closure goal: after the exclusive window closes, run the pending independent controls, freshly admit and measure the prepared batch, and continue toward complete-domain accuracy within the original 08:54:15 EDT deadline.

## Midnight source-review and host-admission checkpoint

At the 2026-08-28 00:00:56 EDT clock check (`c092cc`), the original 08:54:15 EDT deadline remains unchanged. The next independent synthetic closure-control launch was rejected before any child started: tool `2277d1`, exit one, `host admission`. Its exact failing memory percentage was not retained. A subsequent probe (`6ad250`) showed 40%; process observation `747bd3` and absence check `0cf63b` found no owned process, no shared lock and no control receipt. The exact rejection tool result is preserved in `.local-data/braid-analysis/f6c-whole-history-20260828/continuation/closure-independent-controls-v2.prelaunch-rejection.json`. No test or parent-three calculation ran. The idle exclusive reservation was released to the benchmark and F5 successor; a later launch needs fresh coordination and admission.

Plainly: host admission stopped this attempt before it could test anything. The later threshold reading does not retroactively admit it or establish the next run's admission.

The streamed caller is now draft generation `421ffc9d0d3039586dd081d9530fbc7b5f503942357bb179d91e808f58bbb2ec`, with its adapter/control pins updated to `b4dc731c`/`a1dc70f7`. The fresh scientific checker authority tables remain empty pending independent measured acceptance. Exact previous caller `281140b0` bytes and original identity were preserved (`8d3a0e`). No invocation of this draft is accepted.

Independent source contracts now cover the pure whole-parent snapshot overlay (`b2943533`), the proposed actual metadata monitor parameterization (`2c301f6b`) and actual batch acceptance issuer (`bbdf7488`). They are implementation prerequisites, not measured outcomes. The existing actual monitor's original `cf8952dd` bytes/identity were preserved before the authorized parameterization. A proposed history-generation witness explicitly distinguishes the accepted parent manifest's history token from the metadata snapshot's history/frame/parent composite digest; no old identity is relabeled.

Closure goal: complete the pending independent controls and actual bounded batch, then pursue complete-domain accuracy and costs without extending the original campaign deadline.

## Serial control window closed at 00:22 EDT

The repaired synthetic-only monitor `ed8ad345` passed 28 independently authored safety checks, including ten expected rejections, and eight author controls. Original `b6a1e974` bytes and five-field identity were preserved before replacement. The successful suites in this window were: 58 independent batch-closure cases (`3eefbb87`/`f45ccfb0`); 42 prepare-writer checks with 27 expected rejections; 34 independent snapshot checks with 48 expected rejections (`3bff657e`/`a36cce43`), plus 23 author tests; 121 adapter tests (`b4dc731c`/`a1dc70f7`); and 26 v4 helper checks with 13 expected rejections. The latter use explicit synthetic authority pins and do not admit nonempty production evidence. Real host observations during these controls were 41–42% free memory. No actual-history provider ran.

Plainly: these checks establish specific storage, source-accounting and cleanup properties. They are not new acceleration measurements or whole-history accuracy evidence.

The final caller suite `caller-v4-controls-v1.json` was rejected: 27 of 61 tests passed and 34 failed, with many synthetic targets failing before their intended test stage. Original final tool chunk `c6cae5`, session `74608`, exit one; receipt SHA-256 `098362feb116db8cf5ebdc308eb8bc5095527d6a8e8eaa5fa18824287f76776f`. The monitored elapsed stamp was 23.683976708 seconds, maximum sampled aggregate RSS 469,614,592 bytes, maximum sample gap 458.662916 milliseconds; sources were unchanged, no cleanup signal was needed, and the child closed. This is not a passing caller generation. Source diagnosis identifies an obsolete genuine-fixture helper pin and a synthetic adapter missing the newly required `fresh_provenance` property; repair and independent rerun remain pending.

Independent fresh closure (`dae74b`, `2026-08-28T04:22:14.646Z`) verified all 167 observed PIDs and 80 process groups absent by a fresh process table and zero-signal existence checks, with the shared lock absent and every recorded source identity unchanged. The complete receipt is `.local-data/braid-analysis/f6c-whole-history-20260828/continuation/control-window-independent-closure-v1.json`; original stored tool result objects and the exact independent observation script are in `control-window-tool-observations-v1.json` there. The first monitor call retains selected directly observed fields, explicitly not a reconstructed complete original wire. The benchmark task received the next bounded synthetic-test window; this task remains source-only until its release.

Plainly: the failed caller suite left no owned process running. Its failure is preserved and does not erase the separate passing checks or authorize a scientific launch.

Closure goal: repair the failed caller fixtures, complete independent acceptance issuance, and continue the declared history within the unchanged 08:54:15 EDT deadline.

## Preserved rejected launch

The preserved rejected attempt used this exact invocation from the repository root:

```sh
node scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs --spec /Users/markmorris/vibe/architrino/.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/invocation-three-refined-parents-v2.json --spec-sha256 e4f34003317e39d34bfc365be4d762c6d6935ff47771248fc34f089cb3ab6a61 --self-sha256 7554a3395ae2c4e87625d4e2db72edb6a1d5ed524161e61144d8b00a6e14eae4
```

Plainly: this command records what was attempted, not a reusable successful scientific run. Its output directory is already occupied by retained rejection evidence; a retry requires a fresh specification and output destination.

The caller returned exit one in tool chunk `ace48a`, tool-reported elapsed `0.098449916` seconds, with `host memory/disk resource stop`. The rejected outer directory is `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/pilot-three-refined-parents-v2-outer/`. A subsequent independent host probe reported 36% free memory; another probe at the 21:16–21:17 EDT checkpoint again reported 36%. The launch requirement remains 40%. The failing caller's exact host percentage was not retained, so the later readings are not relabeled as that earlier measurement. The caller observation separately records 13.179 seconds including approval/transport and observation delay; neither figure is a scientific evaluation cost.

Independent rejection closure `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/three-parent-v2-independent-rejection-closure.json`, SHA-256 `365eadc0441405650f4112c7476b2c41f8e1d9df99d46424611a0ca3f5719e47`, confirms PID/process group 33838 absent, no scientific child, no shared lock and no scientific output directory. The retained rejection record's `processesClosed:false` is not rewritten: it had no registered numerical stage receipt. External closure is separate evidence. No application was closed, memory purged or resource threshold relaxed.

## Earlier preparation checkpoints and remaining limitations

**Measured source admission:** the first prepared invocation was not launched. Independent review identified two historical document-link changes: member predeclaration `c67de8cc` (16,985 bytes) versus current `b5c83d28` (16,957 bytes), and full-resource plan `46a827d1` (13,021 bytes) versus current `320650a3` (13,003 bytes). Exact original Git blobs were recovered from commits `21dd2cf0fad25d99623e3d8e1921e793f7b99ee5` and `58b4219748c21cfdee4b5a697effe8b8f71ae724`; their byte-identical copies are retained under `.local-data/braid-analysis/f6c-parent-adapter-20260827/` as `<original-basename>.<first-eight-hash>.archive`. Current documents are unchanged. Independent review accepted the explicit two-document ancestry route; no historical hash or frozen mathematical reference was updated.

Plainly: the old evidence still refers to the document versions used at the time. The repair makes those exact versions available without undoing subsequent editorial work.

The six consumed adapter/caller/control generations were preserved before editing. Author adapter controls pass 99 tests in 5.158 seconds (`32310/bb21be`); independently repeated controls pass separately. Caller controls pass 52 tests in 47.530585875 seconds (`58277/cd2180`, exit zero), including real process cleanup against synthetic inputs. An earlier sandboxed caller run was not accepted: it encountered `spawn EPERM`, plus two subsequently repaired test setup/dependency-pin errors. These controls are not actual-history numerical evidence.

The data-only invocation is `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/invocation-three-refined-parents-v2.json`, SHA-256 `e4f34003317e39d34bfc365be4d762c6d6935ff47771248fc34f089cb3ab6a61`, 49,489 bytes. It fixes three advances and caller SHA-256 `7554a3395ae2c4e87625d4e2db72edb6a1d5ed524161e61144d8b00a6e14eae4`. Independent actual metadata admission `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/three-parent-v2-independent-metadata.json`, SHA-256 `0258a1087a8e284d1299a260506a99a9f43764ef51ab58044cb6fc809a6e71aa`, passed 805 checks over 308 physical sources totaling 62,126,546 bytes, in 23.618738709 seconds through closure. All 160 parents, eight histories, 81 frames and exactly refined parents zero through two were checked without numerical calls. Its predecessor remains unlaunched and preserved.

Plainly: both the caller controls and the actual saved-history input checks passed. The separate host resource requirement prevented calculation; no numerical accuracy conclusion follows from these checks.

**Frozen packaging prerequisites:** [expectation contract](2026-08-28-f6c-lossless-packaging-expectations.md) `75177ad5`, [literal inventory](../../../../tests/fixtures/f6c-lossless-packaging-expectations.v1.json) `79a91dae`, and independent Node decoder `328120d4` precede writer implementation review. The independently measured initial payload is 28 physical inputs totaling 8,083,912 bytes; the decoder passes 637 synthetic hostile-input rejections. No actual package has been created or accepted yet.

The pure package module `scripts/eom/f6c_evidence_package.py` generation `9d888682514f23652b39bfaa53fdfb3ceab66e6ba88cf34222c156d226764ad6` passed independent review against those frozen expectations: 225 assertions, 1,445 hostile-input rejections, 16 supplied controls, three independent decoder comparisons and four exact boundary cases. Acceptance is `.local-data/braid-analysis/f6c-whole-history-20260828/packaging-review/independent-acceptance.md`, SHA-256 `7f24d09808f475e4d800e9fa485b1200fcf2f7375087531b1d356a09fbbb2f0e`. A reusable bounded-operation coordinator and package invocation are under separate implementation/review. Pure module acceptance is not actual-data packaging, measured capacity relief, or scientific acceptance.

**Independently accepted storage mapping:** package-adapter expectations were frozen separately at `.local-data/braid-analysis/f6c-whole-history-20260828/packaging-review/adapter-integration-expectations.md`, SHA-256 `2eee1e580f23c57d98f4a4ef5edeb6379cf1c4c644126892e58c9c94552e52c1`, before implementation. Adapter generation `dff515459655b5795a5ffd60966fdf2a2a270b2e0602b12eabb318b0c17824c6` and controls `f561c331187b959219c12251cba07bf230758a4bb45c90cba7b838165242c4a3` passed 435 independent assertions, 321 rejection cases and 109 supplied controls. Forty-two prior definitions remained unchanged; mathematical references were not edited. Acceptance is `packaging-review/adapter-integration-acceptance.md` in that same local lane, SHA-256 `b81a1df14ad3ce6338f19d2f29796f0495b747eda9a18458455807c17ea07f7f`. Actual packaged metadata admission still requires the independently accepted real package, not a synthetic container.

Plainly: the reader's mapping preserves old records and counts the container as one physical source. This has passed independent synthetic checks, but the real container and the real full input inventory remain to be measured.

The package-aware caller now uses invocation schema `braid-program/f6c-streamed-leaf-invocation.v2`, with explicit `evidencePackage:null` for loose evidence or four separately fixed package/reader/control/inventory bindings. A 55-control generation passed in 48.556960458 seconds (`77424/8dadd3`). Independent review then found three preexisting transport/resource gaps: late byte-identical publication replacement, missing complete combined input-source limits, and incomplete F5/package competitor detection. Repairs and independent hostile rechecks are in progress. A subsequent 60-control run (`21947/720f37`) passed 58 controls and rejected two overstrict test expectations: both new source-limit/alias tests correctly stopped before the first numerical call, while their test group had expected a call. The expectations were corrected; they do not represent successful numerical runs. No numerical formula or frozen reference was changed.

**Host window reopened:** after later observations of 38% free memory, a fresh probe `d684fa` reported 43%, above the unchanged 40% floor. F5 and benchmark owners acknowledged a new exclusive F6c window, and the packaging reviewer closed and released its tiny synthetic process work. The next actual attempt still requires fresh invocation/source admission and its own live launch probe; the earlier 43% observation alone does not authorize it.

**Measured accuracy prerequisite:** independent read-only inventory inspected 2,604 F6c run manifests and found no run with either finer declared setting. The current accepted adapter and pure aggregation reference fix 1,760 pieces per member and 80 original frames. Therefore the three-setting requirement needs further properly reviewed history/interface work; repeating the same retained history at different arithmetic precision cannot satisfy it.

Plainly: the three required settings change the evolution step, stored-history spacing and root tolerance. They are not three ways to print the same answer.

Read-only review found that the existing attractor harness can express the two finer settings with the exact approved fifteen geometry lexemes, radial-breathing ratio one and coupling `10.304229970992187`. No new geometry or strength choice is needed. The current harness has no preparation-only mode; normal invocation immediately evaluates release roots and acceleration. A new source-bound build must first demonstrate setting-zero reproduction against complete retained tokens and accepted-frame decisions; matching model fingerprints alone does not establish historical identity. The original candidate cap is 360 seconds and must not be silently replaced by the outer 1,800-second operation allowance.

Finer histories have 3,200 and 6,400 past segments per member before future evolution. Existing export, guards, reconstruction, root-cover admission and geometry interfaces constrain the old census; the frozen GK and aggregation interfaces also fix 80 original frames. A count-independent admission/traversal layer must preserve every segment and every frame-sided acceleration, including its per-original-frame split budget. Downsampling, merging or refitting into the old census does not satisfy that obligation. These are source/interface findings, not measured new histories or accepted generalized mathematics.

Plainly: the requested settings can be specified exactly, but the present input plumbing cannot honestly treat their larger histories as the old smaller one. That reusable interface work remains necessary after the three-interval checkpoint.

**Derived conditional accuracy limitation:** independent read-only endpoint-Hermite analysis gives a peak upper-bound floor of at least `0.0001039009282879581004416498426478` for member `0-` on the first original frame under at most 20 splits. Thus a final peak interval with lower endpoint zero cannot meet the declared width `0.000001`. This does not establish general numerical impossibility: an independently established positive global lower endpoint may still permit a narrow nonzero mismatch interval. The proof and separately authored arithmetic are retained in `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/box-accuracy-prerequisite.md`, SHA-256 `36545d078ad6f35aef6c0639ef321e79c0749e14f6bb6770e20e30d0ef971c52`. No acceleration/root kernel was rerun for this check.

Plainly: a broad interval that includes zero cannot certify balance, and the current box method has a calculable resolution limit for that special case. This is a limitation of the enclosure, not a physical rejection of the candidate or proof that every possible accurate nonzero result is unattainable.

## 00:59 EDT control and source checkpoint

The latest real-process synthetic window is closed. Actual-metadata monitor generation `b4c1022f6d303d8cf0801713107342ddaf8086954ce2a132a0feac375e422d31` passed 26 author pure/file controls (`cf4cb81e`) and four independently authored real-Node-process controls (`05f1068c`): literal successful preparation, target failure, unexpected detached-child cleanup, and byte-identical source replacement rejection. The latter used genuinely observed host/RSS/process checks with inert targets, not a scientific provider. Its final outer completion was 8.590786208 seconds, maximum sampled aggregate RSS 328,646,656 bytes and maximum sample gap 320.413708 milliseconds. Receipt `continuation/actual-metadata-monitor-independent-process-controls-v1.json` in the local campaign lane has SHA-256 `0996f9aa8661f9066ac4e3751f0771183fdb396c72fdadba38da940c0924c64e`; exact initial/final tools were `b77962`/`1c5e03`, session `19924`, exit zero. The Python inherited-deadline path remains untested by these four cases and is not admitted for an actual issuer.

Plainly: safe completion of a construction-only Node target is checked; this does not yet authorize new parent evidence or extend the three-interval numerical result.

The initial author-control command failed on a relative fixture argument (`5f8b74`, retained receipt `4a7a09e6`); the required absolute-argument rerun passed (`5e3b9c`, receipt `64f07a54`) without changing the monitor or its controls. The parent-batch issuer controls failed before executing cases because a synthetic AST extraction included an `S` loader assignment but omitted its dependency `spec` (`4e3039`, receipt `62176044`). The issuer `bc0bc049` and mathematical references were not implicated by that failure. After independent source closure, the old `e9650315` control bytes and original five-field identity were preserved and the narrow loader exclusion produced source-only controls `43c5b5ac`; these are not yet executed.

Independent closure at `2026-08-28T04:59:38.331Z` (`cbcdb2`) checked all 23 recorded PIDs and 17 groups, including nested-monitor recorded births, by fresh process observation and zero-signal existence tests. All were absent; all 19 consumed source bindings retained exact bytes and identities; the shared lock was absent. The durable receipt is `continuation/metadata-control-window-independent-closure-v2.json`, SHA-256 `c1ab985df20a1368999eac36f338d8083750b95f3a7242406438f5f333066026`; exact retained tool objects and explicitly selected first-call observations are in `metadata-control-window-tool-observations-v1.json`. The earlier v1 closure checked only outer-observed births and is retained separately. Benchmark and F5 successor received the release; no subsequent build or scientific window is reserved.

Plainly: the failed controls are preserved as failures, and the complete process check is separate from any subject's own success claim.

The reusable segment-audit interface is frozen at `numerical-review/generic-segment-driver-interface.md`, SHA-256 `eabe054012913f351978b4bec36adbeb9efd93fc64070baa7ffab03aa7f4bea3`, and full-driver source work is authorized around the independently frozen arithmetic and exactly five previously approved identity-site adaptations. It admits only the existing 80-frame setting; finer history is not normalized into that census. The prepare-only harness extension preserved original `5100c4d5` bytes and received independent source review `1e055e73`. Its early return before scientific calls and ordinary-branch equivalence passed source review, but preparation-specific evidence labeling, exclusive publication and checked file-close behavior required repair. These repairs remain source-only; no fresh harness binary or history has been produced. Canonical `src/eom`, all historical binaries, frozen references and shared readiness `04a88934` remain unchanged here.

Closure goal: finish independently reviewed issuer/monitor process checks, then admit the bounded new-parent batch and continue toward complete-domain accuracy before the original 08:54:15 EDT deadline.

## 01:10–01:20 EDT: real-input obstruction and next executable boundary

The repaired issuer controls ran 45 cases at `43c5b5ac`: 43 passed, while the changed-input rejection expected a narrower error string and the complete positive fixture exceeded the unchanged physical-file cap. The retained fixture had 579 names/569 physical inputs because its 198 toy historical inputs were disjoint from 282 real test-runtime files. This is a rejected positive fixture, not accepted issuer execution. Receipt `continuation/parent-batch-issuer-controls-v4.json` has SHA-256 `8bdbe7b34325cd16a5b43a60441a448c82a1e8cef1ee8744eba53d8699fb3e42`; exact tool `f73014`, exit one. Source-only controls `e79c2c96` now retain all runtime capture while reusing 198 distinct already-consumed runtime/common files as opaque synthetic ancestry. The issuer `bc0bc049`, independent checker, 512-file and 1-GiB caps are unchanged; the new controls remain unexecuted.

The following inherited-Python control launch was rejected before any child at an actually recorded 39% free-memory observation, below the unchanged 40% launch requirement. Receipt `continuation/actual-metadata-monitor-independent-python-controls-v1.json`, SHA-256 `68d185eeeece82b5ca3aa37be8d163c3bd03a8cd311ed7368fbf54176995b30c`, records this rejection (`ad87f5`, exit one). No Python case ran. Independent closure at `2026-08-28T05:10:47.586Z` (`f1b2b6`) verified three recorded PIDs/groups and all 18 original source bindings, with the shared lock absent. Its receipt is `continuation/inherited-control-window-independent-closure-v1.json`, SHA-256 `a83945dcd535b6af3d2eaddf43163d2cde39101fa66f2b554525e774f75dbe63`; exact tool objects are retained beside it. The machine reservation was released, and subsequent benchmark control windows are separately coordinated.

Plainly: neither of these attempts advances numerical coverage. Their failures and complete cleanup are retained, with no resource waiver or automatic scientific retry.

Independent review of the actual prepare-only monitor configuration `2054325a` confirmed all 240 input hashes and original identities, totaling 49,938,156 bytes, and all nine proposed output roots absent. It then found a real source-admission defect: the monitor incorrectly required a singleton consumed input to have only one hard-link name anywhere. Accepted reconstruction and guard files each have another historical link; the system Git executable has additional platform links. The durable review is `numerical-review/actual-node-preparation-configuration-review-v1.md`, SHA-256 `c9c7c8fec1a4948eca619491d1c044bf6e04016b73a093ac27a36901a6e68ef3`. The pre-edit clarification `packaging-review/actual-monitor-input-link-expectations-v1.md`, SHA-256 `895a62c11977ae83608584f616f18d70e4d9b3f10ca5ec4c74f9c181562d375d`, preserves exact input identities and counts while distinguishing unconsumed external input names from forbidden consumed/output aliases. Only the two input-capture paths may change; all output/publication alias checks stay strict. No real preparation was launched.

The full reusable 80-frame numerical-audit source `da0898f7` and controls `4645caed` received independent source admission `packaging-review/generic-segment-audit-independent-source-review-v1.md` (`c097e0ab`). Its complete `audit_segment` entrypoint still needs a measured positive; the analytic controls alone are not that test. One thin, explicitly bound source reader/entrypoint is being prepared without a new numerical provider or acceptance ledger. A retrospective check of already accepted data cannot revive an old operation clock, and a future fresh segment requires the existing monitor's explicitly reviewed streamed-operation baseline support.

The preparation-only harness `9b50ef51` received independent source acceptance `d83494a8`; separately authored helper-only publication controls `54014514` received source review `44961fca`. Their 22 filesystem/fault cases contain no EOM calls and preserve the exact 12,107-byte helper block `a87cd6ce`. These are source decisions only: no fresh harness or control binary has been built, no publication control has run and no finer history has been generated.

Plainly: the next useful executable boundary is still actual bounded preparation and refinement of parents three through five, followed by the new complete audit path. Whole-history and three-setting accuracy remain open.

Closure goal: clear the actual input-link obstruction, independently validate the executable continuation path, and continue within the original 08:54:15 EDT campaign deadline.

## 01:30 EDT: actual input-link controls accepted, Python launch rejected

The input-link repair is source-admitted at monitor `ab788d79` and author controls `7e870a62`, with independent review `packaging-review/actual-monitor-input-link-independent-source-review-v1.md` (`b6ee4f1a`). All 32 author controls passed under the unchanged supervisor (`14ddfc`, exit zero; receipt `continuation/actual-metadata-monitor-author-controls-v5.json`, `9ffd52bd`). Four separately authored real-Node controls at `350706c6` also passed, including the literal successful target with an unused external hardlink created before original identity capture. Both actual input-capture paths were exercised. Exact tools `7d342f`/`5d2a47`, session `82109`, completed in 8.803855584 seconds with maximum sampled aggregate RSS 332,529,664 bytes and maximum sample gap 312.663375 milliseconds; receipt `actual-metadata-monitor-independent-process-controls-v2.json` is `25c4d2f9`.

The following Python control suite was rejected before its child started at an actual 38% free-memory observation, below the unchanged 40% launch floor (`226d69`, exit one). Receipt `actual-metadata-monitor-independent-python-controls-v2.json` is `de7c40c7`; no Python case ran, and the serial sequence stopped. This neither invalidates the completed Node controls nor admits the untested inherited-Python path. Independent closure at `2026-08-28T05:30:43.340Z` (`139f6b`) found all 22 recorded PIDs and 14 groups absent by fresh process census and zero-signal checks, reauthenticated all 14 consumed source bindings, and found the shared lock absent. The closure receipt is `continuation/linked-input-control-window-independent-closure-v1.json`, SHA-256 `a8f858a51899e845b1b3d0dd4c88b2d4d6ef0faefd261f6e57127c80ea6d8335`; exact tool objects are retained in `linked-input-control-window-tool-observations-v1.json`. Both external tasks received release. No new reservation or automatic retry is held.

Plainly: unused external names no longer prevent a legitimate original input from being read. The real Node path is checked, but the Python acceptance path remains unrun because the machine failed its launch requirement.

The actual Node preparation configuration was refreshed without replacing its predecessor: `continuation/proposed-batch-3-4-5.metadata-monitor.v2.configuration.json`, SHA-256 `6d92e3d0713f9f355633c947438b383abb67ef9866b11dfa7a71bec4605107e5`. Read-only capture revalidated all 236 original preparation-source identities and added the current monitor/control/link-expectation bindings, yielding 241 physical inputs and 49,947,297 bytes. Independent configuration review remains necessary before the actual construction-only writer. The issuer's corrected `e79c2c96` controls received independent source admission `5a73f965` but remain unexecuted. The thin generic numerical-audit reader `57d5190c` and controls `6a8cd1a3` are source-frozen under independent review; no full-API retrospective positive or actual new segment has run.

Closure goal: complete actual bounded preparation and the remaining executable checks, then refine and evaluate the declared history within the original 08:54:15 EDT deadline.

## 01:37–01:55 EDT: actual preparation accepted; remaining controls explicit

Actual construction-only preparation completed under configuration `cfb0a32222c6992f9f7a656a11cffb2dfb6488cdef7a97f24ab4435944d48ab5`, monitor `ab788d79`, exact tool `361ffb`, exit zero. The earlier v2 configuration `6d92e3d` was rejected before launch for noncanonical key order and remains preserved; v3 has identical parsed content with the required recursive key ordering and final LF. The output `continuation/prepared-batch-3-4-5-v1/batch.plan.json` is `42589bc128c93e9a9cd2da5f0176afe25075d6c015cbe23bf7dc1961f4b7206f`. Three exact parent plans plus the batch plan and preparation receipt total 546,149 bytes. No refinement, root or acceleration provider ran.

The final monitor completion reports 1.065086875 seconds before its final flush, eight samples, maximum sampled aggregate RSS 253,575,168 bytes and maximum sample gap 300.796917 milliseconds; actual launch/final free memory was 41%. The monitor receipt `9310afa6` retains seven samples, so the eighth observation must not be presented as a retained full census. The target's separate local duration was 0.34293475 seconds. Final accounting was 251 physical files and 50,776,710 bytes, with 11 charged output paths, 963,250 science-category metadata bytes and 2,561 log bytes. External prelaunch/final-tool audit artifacts add 161,699 bytes and are separately retained, not retroactively attributed to the monitor.

Independent process review `packaging-review/actual-node-preparation-process-closure-v1.md` (`a9546829`) verified the two recorded PIDs/groups absent by fresh census and signal-zero checks, all 242 original input bindings/identities and eight output bindings unchanged, and the shared lock absent. The separately authored result review `actual-node-preparation-result-acceptance-v1.md` (`e084b837`) accepted all five artifacts, original 198-source ancestry, exact parent/frame tokens, six ordered stage commands and allocation algebra. This is accepted preparation, not measured batch cost or scientific admission.

Plainly: intervals three through five now have a real, checked reusable launch plan. They still have no new accepted numerical result.

At 01:45 EDT the next issuer controls reached 45 cases: 44 passed; the full-main positive correctly rejected undeclared `locale.py`. Receipt `continuation/parent-batch-issuer-controls-v5.json` is `2b91728b`, exact tool `aa5633`, exit one; the failed fixture is retained. The following inherited-Python control attempt was rejected before any child at an actual 37% free-memory reading (`802dba`, receipt `2fa2a50e`), below the unchanged 40% floor. The generic analytic suite was consequently not started. Independent closure at `2026-08-28T05:45:18.510Z`, receipt `issuer-runtime-control-window-independent-closure-v1.json` (`efa364c3`), checked all three recorded PIDs/groups and 20 bindings, with no shared lock. Both external tasks received release.

Controls generation `9f2ac789` now adds only an eager locale import before runtime and synthetic-ancestry capture; exact prior `e79c2c96` bytes and original identities were preserved. Root's separately authored source review `packaging-review/issuer-controls-locale-independent-source-review-v1.md` (`42414487`) accepts that finite correction for execution, not as a passing run. The actual issuer `bc0bc049` and pure checker `3eefbb87` remain unchanged.

The delegated operator approval for covered unattended implementation, review, preparation and bounded execution is recorded in `operator-unattended-approval.md` in the local campaign lane. It does not alter scientific settings, technical admission, source integrity, resource limits or the original 08:54:15 EDT deadline. No repeated operator approval is needed for these covered decisions.

The previously independently accepted checker/control/contract tuple is now installed as the finite fresh-closure authority in the adapter and caller, with four associated test/pin updates. All six predecessor generations were preserved before editing in `continuation-source-archives/fresh-closure-authority-original-identities-v1.json`. This source-only integration remains under independent review and has not been tested. It cannot accept an actual batch until the independently observed closure evidence exists. The generic reader's retrospective-monitor amendment is separately frozen; fresh streamed review remains fail-closed until a truthful complete original-budget baseline is implemented.

Plainly: accepted historical evidence stays immutable, and new code is not treated as tested merely because its source review passes. Complete-domain results and three-setting accuracy remain open.

## Current next action and scoped changes

Continue the accepted prefix through reusable, independently checked bounded segments. The independent continuation contract was frozen before implementation at `.local-data/braid-analysis/f6c-whole-history-20260828/packaging-review/continuation-expectations.md`, SHA-256 `37ff8496aee7211e4a04f2c420add4fc38f1a5edbcc1e2fc7bb3c71e69e820bb`. It requires complete independently accepted segment bytes, exact frozen-protocol replay, unchanged consumed-parent evidence, preserved cumulative subdivision/evaluation budgets, zero new acceleration work during restoration and separate inherited/new measured costs. Source-bound wrappers and their consumed control generations are preserved under `continuation-source-archives/<full-sha256>/<original-basename>` in that local lane before edits. Frozen mathematical references and codec remain unchanged.

Current scoped paths are the variable-cell adapter and its controls; streamed caller, diagnostic/session wrappers and controls; the data-only invocation preparer; the package reader/writer and controls; the reusable bounded-operation coordinator and package composition with controls; the frozen package expectation/inventory; the new pure continuation module and controls; and this handoff. Exact current hashes will be frozen after the ongoing implementation and independent review, before any new invocation. F5 owns its separately coordinated CLI changes; this campaign has made no shared EOM source edit or build.

Plainly: the next process must pick up the exact accepted decision history and keep its spent budget. Starting another process cannot grant additional mathematical allowance or erase earlier costs.

Full-domain result, required emission refinement, three-setting accuracy, complete measured costs and a reusable whole-history invocation remain open. No complete evaluator is available to the parallel benchmark. If safety conditions block further useful authorized work, operator action—not relaxed thresholds or repetitive rejected launches—is required. The fixed campaign deadline remains `2026-08-28 08:54:15 -0400`.
