# BP-010 Parent 3–5 Historical Routing Audit

Date: 2026-09-02
Compatibility identifier: `aaa-corpus-advancement`
Status: source-bound routing negative; exact historical owner blob located in Git history; numerical continuation not authorized
Queue owner: [BP-010 — Asymmetric Counter-Breathing Representative Normalized Member Acceleration](../work-queue.md#bp-010--asymmetric-counter-breathing-representative-normalized-member-acceleration-predeclaration)

## Result

The read-only routing audit checks the prepared parent 3, 4, and 5 plans as one source generation. Their producer, producer controls, verifier, verifier controls, operational entry, and operational controls still match the exact historical bytes named by all three plans. The plans' historical acceptance-owner binding does not match the current owner and has neither a declared route nor a matching file in the private continuation archive.

The missing logical tuple is:

`reference/priorities/braid-program/evidence/2026-08-27-braid-search-launch-readiness.md`, 359,719 bytes, SHA-256 `04a889345ee7c4cd43b0f41bebb70cb2f176ac4d13af035204347f3c6904feaf`.

An exact read-only Git-history search locates those bytes at commit `0fb575921783188ce528a45c671090e9ecc00464`. The prepared plans remain non-executable because a Git object is not the separately captured physical archive relation required by the consumer. No old pin was relaxed, no current owner was substituted, and no numerical process was started.

Plainly: six wrapper files still have exactly the bytes the old plans expect. The historical evidence owner changed. Its old bytes are recoverable, but the execution path is not allowed to read them until they are copied into the approved private archive and the exact route receives independent review.

## Reproduction

Run:

```sh
node scripts/prescribed-path-analysis/audit-bp010-historical-routing.mjs
node --test tests/bp010-historical-routing.test.js
```

The audit source SHA-256 is `c0c858481b6d4c668260c116c856fc9dd35ebb66bdaf3c43c0a4f10d495f5fbe`; the focused test SHA-256 is `5b20dd0862c6f2c87f2e9df372d81f0c3da0de5c8bb7bb20034488855a6e3908`. The test proves that matching archive bytes do not make an undeclared route executable and that current bytes cannot impersonate a historical generation.

## Claim Grade And Falsifier

- **Measured source inventory:** the three prepared plans share one declared generation; six wrapper bindings match current bytes and the changed acceptance-owner binding lacks an admitted physical route.
- **Derived routing decision:** the plans cannot authorize parents 3–5 until the exact owner blob is materialized and routed under the existing fail-closed contract.
- **Not established:** historical acceptance renewal, parent 3–5 refinement, streamed acceleration evaluation, whole-history coverage, M05/M06, balance, retention, stability, binding, identity, score, or scientific acceptance.

A current byte mismatch in any reported matching wrapper, a different blob hash at the named commit, an already-declared exact acceptance-owner route missed by the audit, or a consumer that legitimately admits the present plan without that route falsifies the corresponding result.

Closure goal: materialize the exact owner blob from commit `0fb575921783188ce528a45c671090e9ecc00464` into the approved private archive, encode and independently review the one missing relation, then prepare a fresh current-code parent 3–5 invocation without changing historical logical bindings.
