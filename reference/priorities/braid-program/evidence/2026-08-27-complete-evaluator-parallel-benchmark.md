# Complete-Evaluator Parallel Benchmark

## Campaign boundary

Closure goal: demonstrate an actually completed, independently checked parallel batch of complete braid evaluations and measure its operating capacity before recommending a wider launch.

The operator requested this campaign in task `01a045de-6a34-77f3-bac4-90f2362f6ccd`. The first observed campaign time is **2026-08-27 20:56:50 -0400**, from `date '+%Y-%m-%d %H:%M:%S %z'` (tool chunk `25e80f`). The twelve-hour deadline is **2026-08-28 08:56:50 -0400**. Stop owned computation and close monitoring by that deadline; preserve a checkpoint if closure remains incomplete. Earlier completion requires independent verification of all closure conditions. Operator stops and missing scientific authorization take precedence.

The current stage is preparation only. No complete evaluator, scientific case set, serial baseline or parallel run is admitted by this document. A dispatcher, synthetic controls, earlier prescribed-root ladders and partial-history measurements do not satisfy the requested result. No large sweep, commit, push, unrelated regeneration or evidence deletion is authorized.

## Ownership and dependencies

| Responsibility | Owner | Initial handoff |
| --- | --- | --- |
| Parallel orchestration, benchmark evidence and integration | This task | Preparation in progress; no scientific launch |
| F6c whole-history mathematics, measurement settings and numerical admission | Task `01a045dc-2981-7e42-b970-4f659be06958` | No complete evaluator; next three-interval calculation remains partial with 157 requests expected pending |
| F5 scientific settings, ordinary evolution and outcome acceptance | Task `01a045dd-4365-7942-91eb-d1c5cd5f330e` | No complete evaluator; recommended interaction strength still awaits operator approval |
| Independent operational benchmark review | Subagent `/root/benchmark_review` | Read-only review of existing facilities; no scientific work |
| Independent final result and census audit | Separate reviewer to be assigned after frozen artifacts exist | Not begun |

Plainly: this task schedules and measures accepted calculations. It does not choose either family's scientific settings or turn a partial measurement into a complete result. Either complete evaluator may unblock the benchmark first; their outputs are never combined into a common score.

The F6c owner requires an exclusive numerical window for its forthcoming calculation and independent numeric audit. Its existing caller rejects competing numerical programs and reserves `.local-data/braid-analysis/f6c-continuous-reception-root-cover-20260827/.pilot.lock`. This task acknowledged that hold and will run no science or heavy tests during that window. The earlier provisional proposal of four shared heavy processes is **not admitted** and does not override that contract.

F6c's current unchanged limits are 1,800 seconds, 2 GiB aggregate resident memory, 64 MiB scientific output and 16 MiB combined logs. Launch requires 40 percent system memory free and 64 GiB available disk; running floors are 20 percent and 16 GiB. These are the owner's limits for its specific invocation, not an approved parallel benchmark budget.

Plainly: the machine is shared, but the next F6c calculation currently requires the numerical lane to itself. This campaign must wait for explicit process and lock closure before a competing numerical launch.

The shared readiness owner was read at 359,719 bytes and SHA-256 `04a889345ee7c4cd43b0f41bebb70cb2f176ac4d13af035204347f3c6904feaf`. This task does not edit readiness, registry or queue documents until affected owners acknowledge a safe point and every consumed generation is preserved. This separate evidence file is the only initial authored write surface.

## Predeclared benchmark requirements

1. Obtain a complete evaluator's accepted handoff: exact source/build/input/reference identities, invocation, unchanged scientific settings, independent review, result schema, scientific controls, tolerances and stop rules. Preserve the complete dependency handoff or blocker for both families, without requiring both to finish.
2. Freeze a small named case list only after scientific admission. For one accepted F5 geometry, every repeated execution is a throughput replica of that geometry. Repetition adds no geometry coverage. Do not change geometry, interaction strength, horizon, tolerances or stop decisions to improve timing.
3. Run the identical ordered list first with one worker and then with two workers. A four-worker run requires a separately recorded admission using the observed two-worker batch, host conditions and all other tasks' resource needs. Four is an absolute maximum, not a target or preauthorization.
4. Record one final disposition for every planned case in every phase, including failed, interrupted, rejected, not run and complete. Preserve planned identity, exact command, fresh exit, stop reason, scientific output identities and any skipped descendants. A successful process exit is not scientific acceptance.
5. Measure complete evaluations divided by observed batch wall time. Separately report computation, coordinator preparation/finalization, independent review and evidence publication intervals. CPU sums and worker counts are not speedup measurements.
6. Observe simultaneous aggregate resident memory across all owned process groups, including supervisors and helpers, with timestamps, sampling gaps and process identities. Label sampled peaks as sampled; do not substitute a sum of independent lifetime peaks. Record host pressure separately.
7. Measure scientific bytes, operational log bytes, complete output growth, file counts and free disk. Distinguish logical bytes from unique physical files where hard links occur. Enforce whole-batch ceilings rather than resetting a shared allowance per worker.
8. Independently compare conclusions, census, stopping decisions and evidence completeness against frozen references and the declared tolerances. Serial/parallel equality tests scheduling invariance; it does not independently prove the evaluator's mathematics.
9. Exercise bounded interruption and one-worker failure with distinct control identities and fresh output directories. The final control census must retain the failure and the peer's independent outcome. Verify all owned descendants and monitors close; preserve failure evidence. Synthetic controls demonstrate machinery only and do not replace the actual batch.
10. Recommend only the scale supported by complete measured work. Report the limiting resource, uncertainty from a small sample, untested worker counts and any remaining scientific admission. A measured ceiling is an acceptable outcome; absent measurements are not a capacity result.

Plainly: the comparison changes only how many accepted evaluations run at once. Both good and bad outcomes must remain visible, and all cost claims must come from the actual watched runs.

## Candidate protocol for independent review

This is a proposed operational shape, **not a frozen or authorized case set**. Once one complete evaluator is accepted, prefer two named replicas (`replica-01`, `replica-02`) of that exact accepted evaluation, each including all of its required scientific controls and refinement settings. Execute those two identities serially, then execute the identical two identities with two workers and fresh outputs. This supplies two complete evaluation attempts per phase and one geometry only if F5 supplies the evaluator. Do not add a four-worker phase to this two-case set: four-worker admission would need a suitably frozen case set and another reviewed phase. Two-worker capacity is sufficient for the requested small parallel demonstration.

Plainly: the smallest useful benchmark runs the same accepted calculation twice in sequence and twice together. Four completed evaluations still cover only the original scientific case.

Before any phase starts, require its complete declared worst-case duration plus bounded cancellation, cleanup, evidence finalization and checkpoint reserve to fit before the campaign deadline. Freeze those numeric allowances with the evaluator handoff; do not launch first and extend the twelve-hour campaign to finish it. Use separate phase and replica identifiers and an exhaustive terminal-status vocabulary fixed before spawning. Preserve the full planned census even when startup fails before the evaluator creates a result.

For each phase, retain the interval from coordinator entry through fresh process closure, the evaluator envelope from first evaluator start through last evaluator exit, preparation and finalization boundaries, and separate review/publication intervals. If an evaluator wraps scientific computation and publication, its process envelope is not compute-only time: obtain the owner's internal stage boundaries or mark the split unmeasured. Count only independently accepted complete evaluations in accepted throughput; also publish attempted/completed/failed/rejected/not-run totals so failures cannot improve the reported rate. Whole-phase throughput includes dispatch, monitoring and finalization; separately named rates state their exact denominator. A ratio between serial and parallel rates is a small-batch observation, not a population estimate or a claim about untested cases.

Retain measured coordinator and observation-helper CPU and resident memory separately from evaluator processes while including them in simultaneous aggregate memory and whole-phase elapsed time. Record supervisor preparation/finalization duration and sampling cadence/gaps. Explicitly identify any excluded measurement probes and the resulting measurement boundary; prefer including their observed rows without registering their transient groups as evaluator groups. These observations measure the instrumented workload; do not infer an uninstrumented runtime by subtraction, and do not call wall time minus summed concurrent compute time coordination overhead.

Plainly: the clock includes the machinery needed to produce trustworthy results. Concurrent work overlaps in time, so adding job times and subtracting them from elapsed time would not measure overhead. The deadline reserve ensures a late launch cannot extend the campaign silently.

For interruption/failure isolation, propose a separate two-entry control phase only after the actual benchmark passes and the evaluator owner admits the bounded replay. Terminate one owned replica through its declared cancellation interface after observable startup; allow the independently supervised peer to finish if host safety remains intact. Retain the interrupted replica's partial files and explicit disposition, independently admit the peer against its unchanged reference, and account for all processes and bytes from both. Additionally require operational controls for an unexpected nonzero worker exit and an exit-zero worker that omits its required terminal result, with a healthy peer and a complete final census. These may be synthetic operational workers with distinct non-scientific identities, excluded from scientific throughput. Do not change the scientific request or corrupt an accepted input to induce failure.

Plainly: the failure test targets process handling. It must show that an interrupted job remains visible and cannot overwrite or invalidate a healthy peer's evidence. Host safety may still require stopping the whole batch, in which case sibling survival has not been demonstrated.

Before invoking a compiled evaluator, require its owner's fresh accepted build and verify source timestamps and hashes again. Rehash the exact source, binary, input and independent reference generations before and after each phase; drift stops admission and preserves all attempted dispositions. An operational wrapper may own the machine lock once for the whole batch only if its separate review and all affected owners explicitly admit that arrangement. Existing exclusive callers must not be run with their locks disabled, renamed or impersonated. Reuse the existing registered-process supervision and simultaneous-memory observation helpers where their fixed contracts fit; keep historical source-bound callers unchanged.

### Independent preparation review

Subagent `/root/benchmark_review` independently inspected the existing dispatcher, registered launcher and response/F6c supervisors without modifying files or running tests. Its findings agree that the old dispatcher is fixed to sixteen A/B/C identities and the `8/32/128` prescribed-root ladder; it cannot directly execute the new evaluator. The response launcher's `parseObservation`, `selectOwnedRows` and `acceptRSS` are useful simultaneous-memory facilities, but their fixed 2-GiB aggregate contract and shared exclusive pilot lock need explicit treatment. The registered launcher supplies process birth-identity checks and bounded owned cleanup; its contract must be preserved in any composition. Static review establishes these implementation constraints only, not benchmark acceptance.

Plainly: reuse is possible without altering the scientific instruments, but the new composition still needs review once the exact complete evaluator is available.

The reviewer's second read-only pass requested explicit deadline closeout reserve, measured observer overhead, unexpected-exit and missing-result controls, phase/replica identities, post-phase source checks, and correctly labeled evaluator-versus-compute timing. Those safeguards are incorporated above. A third read-only pass independently confirmed all five findings resolved, with no evaluator, invocation, batch or capacity acceptance. The reviewer assessed that further useful evaluator-independent preparation is exhausted after those refinements; actual implementation, invocation, numerical budgets and launch admission depend on the accepted evaluator and safe machine window. No test or substitute science is launched merely to fill the dependency wait.

## Dependency events

The F6c owner subsequently reported that its independently admitted three-parent invocation rejected before numerical work: the host observation was 36 percent free memory, below its unchanged 40-percent launch floor. It reported exit one, no new partial or complete result, no remaining F6c compute, preserved failure evidence and release of the exclusive window for F5 subject to F5's own admission. This campaign records that as an owner-reported dependency event, not independently audited benchmark evidence. It does not weaken either launch threshold, authorize a different caller, or establish parallel capacity. The F6c owner continues separate source/package work; its complete evaluator remains unavailable.

Plainly: a resource safeguard prevented the calculation from starting. That is useful operational information, but it cannot substitute for a completed benchmark.

The F5 owner then reported accepted reference preparations and synthetic consumer checks, but no strength authorization or actual dynamics. Main inspected its live [evaluation owner](2026-08-27-f5-ordinary-evolution-evaluation.md), [draft declaration](2026-08-27-f5-ordinary-evolution-declaration.v1.json) and [runner interface](../../../../scripts/eom/run-f5-ordinary-evolution.mjs) without editing them. The draft complete evaluation contains three sequential refinement settings through time `0.5`, independent checks, a 12,600-second whole-evaluation ceiling and a single-process shared lock. These are pending F5-owner settings, not this campaign's choices or a benchmark authorization. Actual accepted timing must inform any later benchmark admission; the maximum allowance alone cannot justify fitting multiple evaluations within the remaining campaign window. The current caller prohibits concurrent replicas and must not be bypassed through its exported helpers. A separately reviewed operational composition remains necessary after scientific acceptance.

Plainly: F5 has made progress toward a measurable complete calculation. It has not yet produced one, and its present single-process permission does not automatically become parallel permission.

At its next safe point the F5 owner supplied a preparation-only restart checkpoint, `.local-data/braid-analysis/f5-ordinary-evolution-20260827/restart-checkpoint-v1.json`. Main independently read its 6,927 bytes and verified SHA-256 `652cf9262681e8a25239d9db12ab14cb60cad1865398c554d463c7338da9c065`. It explicitly records `closureSatisfied:false`, `evolutionExecuted:false`, `scientificResult:"not-evaluated"`, strength authorization false and no owned scientific/build processes or shared lock. It preserves the exact blocked command and reference/build/runtime review identities. This authenticates the checkpoint bytes, not the unexecuted scientific outcome or all of its transitive bindings. F5's original deadline remains unchanged. F6c meanwhile reported recovered launch headroom and requested a renewed exclusive numerical window; this campaign acknowledged that hold and continues to launch no computation.

Plainly: the dependency checkpoint is preserved and attributable, but it is deliberately not an accepted evaluator handoff.

The F6c owner next reported an actual three-interval completion in 67.750050708 seconds, maximum sampled aggregate resident memory 576,978,944 bytes, 275 observations and maximum sampling gap 287.7935 milliseconds. It reported 271,021 independent arithmetic checks passed and all nine observed process identities, three groups and the lock absent, with final wire/closure integration still underway. There remain 157 initial intervals pending, no whole-history metrics and no accuracy closure. These are attributed partial-evaluator observations from the F6c owner, not this campaign's independently audited throughput data. The owner retained the shared window for a bounded package-supervisor control and cached-runtime smoke check; this campaign continues its no-heavy-work hold until release.

Plainly: the partial numerical attempt now has real execution evidence. Completing three intervals still does not complete the scientific evaluation required by the benchmark.

The F5 owner subsequently received direct operator approval of the pending fixed experiment. Main read the frozen declaration at 56,324 bytes and SHA-256 `e908f1fc47acaeb0ba046818fc66a2781251492be2022cdb978e4727e0d72515`: it records the operator's approval, effective strength `0.002`, charge magnitude `1`, coupling `0.002`, wake speed `1`, horizon `0.5` and unchanged three-rung controls. The owner still owes final declaration review and actual independent outcome acceptance. This task acknowledged the exclusive F5 window and launches no competing work. A new independent read-only review now examines the concrete caller's eventual parallel integration boundary; its existing lock remains untouched, and no replica authorization is inferred from this first-experiment approval.

Plainly: the strength decision is resolved for the F5 owner's fixed experiment. The first actual result, later parallel composition and benchmark case admission remain separate requirements.

### Concrete F5 orchestration review

The independent benchmark reviewer authenticated the frozen declaration above and its runner, runner tests, request preparer, geometry verifier, dynamics checker and fixed oracle bindings. At that inspection the declaration's required final review file was not yet present; no acceptance was inferred from its pathname. Both main and reviewer inspected the complete public caller and found no current parallel reuse boundary: lock acquisition and release are inline, final receipts assert the caller released that lock, stage supervision uses process-global signal handlers, and each stage expects its direct child to lead the process group. The older A/B/C registration bootstrap also rejects the F5 caller's explicit child environment and cannot be inserted unchanged.

Plainly: starting two copies now would collide with the existing lock. Running two copies inside one Node process would also share cancellation handlers. Neither is an admissible shortcut.

The proposed later split preserves the existing public scientific sequence: the F5 owner would own its narrow operational-admission extension and existing runner tests; this task would own a thin outer coordinator and aggregate monitoring/census. Each complete replica would remain a separate operating-system process. Parent/lock identity, declaration and batch bindings, exact replica/output identity and a live cancellation channel must be authenticated before delegated execution; a flag or alternate lock pathname is insufficient. Only the outer owner may report release of the shared batch lock. Scientific request run IDs and prepared wire bytes remain unchanged; external phase and replica IDs distinguish runs. No concurrent calls to the stage helpers, solver/reference changes or duplicate evaluation implementation are proposed.

The F5 owner acknowledged this only as post-acceptance coordination. Its current runner, declaration and requests remain frozen throughout the first experiment. Actual accepted outcome, explicit process/lock closure and a new reviewed operational generation are prerequisites to any implementation or benchmark execution under this split.

Plainly: the proposed change concerns who owns and supervises whole evaluations. It does not replace their calculations or authorize them before the first experiment is accepted.

## Initial observations and claim limits

**Measured host snapshot, not reserved capacity:** at `2026-08-28T00:58:25.216Z`, Node's operating-system interface reported eight logical processors, 25,769,803,776 total memory bytes and 963,983,060,992 available filesystem bytes. Its `freemem` result is not the accepted `memory_pressure` host indicator and is not used for launch admission. No benchmark resource sample or throughput is available yet.

**Read-only implementation finding:** the existing A/B/C dispatcher and its accepted complete reception ladders concern prescribed-root certification. Their individual resident-memory peaks and CPU totals are not simultaneous aggregate memory or a measured complete-evaluator parallel speedup. Reuse must preserve that distinction and may not silently change those source-bound historical instruments.

Plainly: there is useful existing scheduling machinery, but neither historical results nor the current host snapshot demonstrate capacity for the new evaluations.

## Validation and remaining decisions

Initial `git diff --check` passed before edits and after the protocol refinements. This campaign has launched no scientific or synthetic job. Independent preparation review is limited to read-only facility and admission analysis. The start record and nine initial source bindings are preserved at `.local-data/braid-analysis/complete-evaluator-parallel-20260827/start.json`; a byte-identical readiness snapshot is preserved beside it under its full SHA-256. Exact executable commands and immutable case manifests will be added only after the evaluator and resource contract are accepted.

Remaining decisions: F5's frozen experiment and actual outcome still require their own independent acceptance; F6c complete-history and accuracy obligations belong with its owner; the benchmark's named case list, shared machine window and measured concurrency admission remain open. The F5 strength choice is resolved only at the fixed experiment's stated scope.

Closure goal: obtain one independently accepted complete evaluator and its scientific admission, then freeze the serial/two-worker measurement protocol without relaxing the owners' limits.
