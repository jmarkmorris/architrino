# asymmetric counter-breathing representative one-cell continuous-reception acceleration-range predeclaration

Date: 2026-08-27. Status: consumer/reference-interface declaration; no actual range calculation or range-output acceptance is recorded here. Subject: the reconstructed asymmetric counter-breathing representative history family already identified by the [accepted-frame reconstruction theorem](2026-08-27-f6c-accepted-frame-history-reconstruction.md), restricted to the accepted cached root pilot's first closed reception cell. This declaration does not authorize a full-range campaign, quadrature, a score, a trajectory-identity claim, or an EOM solver run.

Plainly: this is the plan for converting one already checked root-cover cell into acceleration and residual bounds. It does not rerun the roots or claim that the reconstructed curve is the historical EOM trajectory.

## Fixed reference and claim boundary

The separately authored and independently reviewed [continuous-reception acceleration reference](2026-08-27-f6c-continuous-reception-acceleration-reference.md) owns the inclusion mathematics. Its unchanged implementation is [`continuous_reception_acceleration.py`](../../../../scripts/eom/oracle/continuous_reception_acceleration.py), SHA-256 `abfc21f29d8bdd984118b1e0ba0cb62b88a081a75a961052eb11f31ea7bdd7b8`; its unchanged [30-control source](../../../../tests/test_eom_continuous_reception_acceleration.py) has SHA-256 `26b7c5455a57da5beba6e7fd32a0b7bfbc8e1f32630b663c55a33273e8cc1823`; its proof document has SHA-256 `c1a5358e1d887fab5b4753368dc14ec59ed220294f42d2afa4ac40f962ee537f`. Reference acceptance establishes a conditional range inclusion, not the truth of externally supplied history or root premises.

For $c_f=1$, each ordinary contribution retains the frozen sharp expression

$$
\mathbf A_{ij}(T)=\kappa q_iq_j\frac{\mathbf D_{ij}(T)}{R_{ij}(T)^3|D_{t,ij}(T)|}.
$$

The receiver factor is retained as a checked premise but is not a multiplier. The exact accepted-frame Hermite chain supplies the required acceleration $H_i''(T)$; its coefficients are derived with exact rational arithmetic from the original endpoint tokens. The returned normalized residual is $\boldsymbol\rho_i=L_0(H_i''-\sum_j\mathbf A_{ij})$, with the original literal $L_0=0.5320012303229503$. The reference also returns a nonnegative interval for $\|\boldsymbol\rho_i\|^2$. It adds neither frame-error inflation nor a rounded substitute cubic. The original literals remain $\kappa=10.304229970992187$ and $q_i=\pm0.1666666666666666666666666666666667$ in the fixed member order.

Plainly: the law-side acceleration uses the stored polarity and coupling values. The curve-side acceleration comes from the exact curve through the accepted frames. The reference bounds their difference without changing either definition or silently supplying a missing parameter.

Every candidate is `accepted:false`. Every authority flag in the reference's ten-field `claims` record remains false. The consumer's eight claims also remain false: historical trajectory identity, metric availability, score authorization, H3 eligibility, EOM execution, new root evaluation, independent range-comparison acceptance, and execution authorization. A later output comparison may establish that this candidate conforms to the separately derived interval calculation; it cannot promote any excluded claim. No numerical tightness, $10^{-6}$ width, integral, peak value, or full-history residual result is promised.

## Exact source generation

The fixed source census is encoded in the consumer's `FIXED` constant. All entries are captured as bounded regular files on one file descriptor each, authenticated against their original byte hashes, and rechecked before and after publication. The following entries identify the scientific generation and the original operational closure assertion; paths beginning with `.local-data` are repository-relative ignored evidence paths, not links to mutable search results.

| Role | Exact path or basename | SHA-256 |
| --- | --- | --- |
| Original export | `.local-data/braid-analysis/f6c-history-export-20260827.jUhLLg/retained-history.json` | `f479bb88a6425e9e98e00288f2524f33d5a3c0f4c2a14139dbaae4f468c46db1` |
| Reconstruction receipt | `.local-data/braid-analysis/f6c-accepted-frame-reconstruction-20260827.5o7jK3/reconstruction.json` | `7c30aae03d43f7720b79288a19a9c9f9a7c0ab6b7b16ac9a948828ca80b92b43` |
| Uniform-guard receipt | `.local-data/braid-analysis/f6c-retained-history-guards-20260827.hdrqLF/guards.json` | `86d7fa14ac64ee20930094ff1a59880fe4e1ef5c81758f5d8baf2c6777ee4880` |
| Cached pilot manifest | `subject/cover-manifest.json` | `19fae257f7f36d858fa60d9031125b3f29dbb8780e944802699aab5292275f4c` |
| Cached pilot comparison | `comparison.json` | `6bf2b50ef4f0b46f43ae77a9881f82a2f9d504d5df757bc0ad215deb8eac36c6` |
| Cached pilot admission | `pilot-admission.json` | `1a814c90279eed456546b2c4959a8504657213ffc2d25c063060831814e930ee` |
| Complete raw rows | `subject/rows.ndjson` | `786785b2597bcdf024e350ba89c129fb32115afed693169a6db3137c6bdca383` |
| Complete compact piece records | `subject/pieces.ndjson` | `2c064a5956e7684868cbda7aa7e312ac609e07760bf67f1cf121c934d6d4c411` |
| Cached pilot machine plan | [2026-08-27-f6c-cached-root-cover-pilot-launch.v1.json](2026-08-27-f6c-cached-root-cover-pilot-launch.v1.json) | `5f5afcced38878828d65e0c5482f1764092f6449c2cba36ac6b99a1bbf9f9f86` |
| Observed prior closure owner | [2026-08-27-f6c-cached-root-cover-full-resource-plan.md](2026-08-27-f6c-cached-root-cover-full-resource-plan.md) | `daeb71bee6260c38a6b7e5e6237110216d9315807fe23602fbd7cfcdddc5866b` |

Plainly: the five short cached-pilot paths share the exact base `.local-data/braid-analysis/f6c-continuous-reception-root-cover-20260827/pilot-cell-0-cached-v1/`. These are existing completed artifacts. The proposed consumer must reject a different generation, even if it has similar numbers or names.

The other six fixed bindings are the range implementation, its controls, its proof, the original [normalized member-acceleration predeclaration](2026-08-26-f6c-normalized-member-acceleration-predeclaration.md) at `c67de8cce1370eed779b560c269d5ca0a7505bdb175d39cff1276b75a7e69853`, the [conditional continuous-reception theorem](2026-08-27-f6c-continuous-reception-enclosure-contract.md) at `f20e4bdaaff8b6f0012fdc6135b15d568a817832fb55d5c42f80d8421a117f68`, and the accepted-frame reconstruction theorem at `6abbbbacc1671052bdd881790094dbd71ebb03d54904ac1f937edae1f3c9f936`. All 16 fixed files remain unchanged by this batch.

The original cached admission is a prepublication operational record. It is not treated as proof of its own future successful exit. The new, externally reviewed machine plan must include `priorCoverClosure` with authority `externally-reviewed-caller-observation`, the exact closure-owner and admission hashes, `matchingFreshCompletionObserved:true`, `exitCode:0`, `elapsedSeconds:"8.534247625"`, `processesClosed:true`, and `independentAuditAccepted:true`. The named closure owner records the original caller's observation; the new consumer copies this reviewed assertion rather than reconstructing it from a prematurely published receipt. The mutable launch-readiness document is deliberately not a fixed program input.

Plainly: the old run's successful finish is an externally observed premise. Hashing a file does not prove that its process later exited successfully, and the consumer does not pretend otherwise.

## Complete one-cell projection

The subject is [`prepare-f6c-continuous-reception-acceleration.py`](../../../../scripts/eom/prepare-f6c-continuous-reception-acceleration.py); its dedicated controls are [`test_f6c_continuous_reception_acceleration_preparation.py`](../../../../tests/test_f6c_continuous_reception_acceleration_preparation.py). `project_cell` is a pure, non-evaluating mapping function, not an authenticator or numerical oracle. Production calls it only after the captured source and receipt checks. It creates the frozen reference's immutable `CellRangeInput`; no root-library function, scalar root certificate, or subject geometry evaluator is imported.

The exact target is cell 0 on the closed reception interval $[0,0.001]$, contained in original accepted-frame interval 0. The full retained domain remains $[-8,0.13]$; every nonself emission box remains exactly $[-8,-0.05]$. There is no implicit root-box narrowing or new refinement. The original 81 frame records and 80 frame intervals supply their exact token ordering and interval endpoints; the consumer does not regenerate a uniform grid. The eight ordered labels are `0+`, `0-`, `1+`, `1-`, `2+`, `2-`, `3+`, `3-`, with original path keys 1 through 8 and all eight original 1,760-piece histories retained in the authenticated export.

Plainly: the input is precisely the first previously checked cell. The longer history remains available for delayed contributions, but the output describes only that one reception interval.

| Source field or obligation | Immutable reference mapping |
| --- | --- |
| Original `id`, `pathKey`, `polarity`, exact `charge`, and manifest `historyDigest` | Ordered `Member.label`, `path_id`, signed `charge`, `history_digest`; original fingerprint equality is checked separately and is not relabeled as a new digest |
| First two accepted-frame position and velocity `x,y,z` strings | `position_left`, `velocity_left`, `position_right`, `velocity_right`, unchanged lexical values |
| Frame times, exact cell and retained intervals, explicit constants | `frame_domain`, `reception`, `retained_domain`, `field_speed`, `coupling`, `ruler` |
| Every receiver-major raw row | One `RootRow`, preserving emission, oldest/lower/upper faces, displacement, distance, transmitter and receiver factors, count, endpoint exclusion, complement and boundary-contact fields |
| Every nonself row's two compact piece records | Exact original `clippedPiecesSha256` tokens after pointer, order, member digest, requested interval and complete index-range census checks |
| Eight self rows | Zero ordinary roots, excluded coincident endpoint, all geometry and both coverage fields `None`; no fictitious empty-list hash or self coverage record |
| Seven mathematical provenance roles | Original export, reconstruction, guards, cover manifest, cover comparison, original member declaration, and conditional theorem bindings in the reference's fixed order |

Plainly: the mapper copies the numbers and identities that the accepted comparison already checked. The separate range-output verifier must reconstruct this mapping independently; agreement with the same mapper would not be independent evidence.

All 64 rows are mandatory: 56 ordinary ordered nonself pairs and eight self exclusions. All 112 compact coverage records are mandatory. The subject checks strict face signs, positive distance, transmitter factor at least $10^{-24}$, positive receiver factor, exactly 90-digit interval metadata, original endpoint lexemes, absence of retained-boundary contact, and the conditional root-free complement. These are consistency checks on the already authenticated cover, not fresh root existence or completeness proofs. Original scalar and per-axis history allowances remain owned by the unchanged cover comparison, reconstruction and guards; the mapper neither inflates nor discards them and does not claim to re-prove them. The frame `positionErrorBound` is nonnegative provenance only and is not added to the exact Hermite required side.

## Candidate and separate comparison contract

The only scientific output is `range.json`, schema `braid-program/f6c-continuous-reception-acceleration-candidate.v1`, scope `cached-pilot-cell-0-range`, status `conditional-range-candidate`, and `accepted:false`. Its closed top-level record is `schema`, `scope`, `accepted`, `status`, `fixedBindings`, `launchPlan`, `consumer`, `declaration`, `rangeVerifier`, `runtimeBindings`, `operationalBindings`, `priorCoverClosure`, `projection`, `ranges`, `census`, `claims`, and `publicationRequires`. `projection` is the complete serialized immutable request. `ranges` is the frozen reference's complete result, including copied request fields, 64 pair boxes, eight member sums, eight required-side boxes, eight residual vectors and eight squared-norm intervals. The exact census is one cell, 64 pair rows, 56 ordinary pairs, eight self zeros, eight members and 112 piece records. No intermediate rounding is inserted into the reference's exact member sums.

Plainly: the output contains enough information to check both what was mapped and what was calculated. It is a candidate for independent comparison, not a self-issued pass.

Before any actual evaluation, a separately authored `scripts/eom/verify-f6c-continuous-reception-acceleration.py` must be reviewed and byte-bound in the machine plan. It consumes the candidate plus the externally pinned plan and original files. It must independently reconstruct the full raw-to-request mapping, including original history digests and clipped-piece hashes, and compare the copied metadata in both `projection` and `ranges`. Its acceleration and exact-frame derivative inclusions must use a separately derived route, such as exact endpoint-corner bounds and the normalized Hermite-basis second derivative. It must not import `project_cell` or `evaluate_cell`. The accepted prior cover comparison owns the root premise; this new instrument is a range-output conformance check, not a second root search or a forged scalar certificate.

## Invocation, runtime and publication

The eventual single fresh subject invocation has the following argument structure. `PLAN`, `PLAN_SHA`, `CONSUMER_SHA`, `NEW_OUTPUT` and `BOUND_GIT` are placeholders to be replaced only by the independently reviewed operational generation; this document is not a runnable launch authorization.

```text
SHARED_VENV_PYTHON -I -B scripts/eom/prepare-f6c-continuous-reception-acceleration.py
  --plan PLAN --plan-sha256 PLAN_SHA --consumer-sha256 CONSUMER_SHA
  --out-dir NEW_OUTPUT --budget-seconds REMAINING_SECONDS --git-binary BOUND_GIT
```

Plainly: a later watcher supplies the exact reviewed files and remaining time. This batch supplies no executable plan and starts no actual calculation.

The machine-plan schema is `braid-program/f6c-continuous-reception-acceleration-launch.v1`. Its exact keys are `schema`, `scope`, `consumer`, `controls`, `declaration`, `rangeVerifier`, `runtimeBindings`, `operationalBindings`, `limits`, and `priorCoverClosure`. File bindings contain exactly `path`, `sha256`, and positive `bytes`. The subject, controls, this declaration and separate verifier use their fixed repository paths. Runtime bindings must include the actual interpreter, resolved Git, all loaded Python source/cache files and any required platform/runtime inventory; externally reviewed operational bindings must include the independent verifier's controls and all launcher, watcher, monitoring and control sources used by the attempt. Runtime metadata is a captured execution premise, not hermetic operating-system attestation. The reference executes directly from captured reviewed bytes in a fresh private module, never from a cached project import.

The output directory must be a new absent direct child of the existing ignored lane `.local-data/braid-analysis/f6c-continuous-reception-acceleration-20260827`. A hash-bound Git check establishes that the exact output path is ignored. The subject writes and synchronizes a private `accepted:false` candidate, then exclusively links the intended public filename and synchronizes its directory. Existing output is never overwritten. A late or interrupted attempt removes only its own public inode and retains its private data; a different replacement inode is preserved. The temporary private link has no acceptance authority. Even an on-time public candidate requires fresh successful completion, independently rehashed output, a successful independent comparison, and external whole-attempt admission.

Plainly: an incomplete or late run can leave diagnostic data, but it cannot leave an admissible success. Reusing or overwriting an earlier output is prohibited.

The eventual operation retains the current fixed resource limits: one serial subject followed by one independent comparator, no EOM workers; inclusive 1,800 seconds beginning before imports and capture and ending after both stages, durable publication, input cleanup, final flushed completion and owned process closure; heartbeat every 15 seconds; aggregate owned-process RSS observation with gaps no longer than one second and a 2 GiB stop threshold; host admission at least 40% free memory and 64 GiB free disk; stop below 20% free memory or 16 GiB free disk; host observations every 15 seconds and after process closure with two-second query deadlines; 64 MiB per scientific input, candidate and comparison file; 16 MiB combined operational logs. There is no automatic retry, resume, budget enlargement, additional worker, or overlap with another resource-measured asymmetric counter-breathing representative/phase-varying display representative attempt. Sampled RSS is not an allocation ceiling. Local alarms and a representable positive remaining budget supplement, but do not replace, the external watcher.

The subject's fresh completion reports `completed:true`, `accepted:false`, exact output hash and byte size, one-cell census, measured subject process user/system CPU and individual maximum RSS, and explicit requirements for independent comparison and external inclusive deadline/process closure. It does not claim aggregate memory, closed descendants, total operation CPU, or authority from its own completion. The watcher must reject nonzero exit, missing fresh completion, wrong output, late stdout flushing, missing resource observation, changed binding, failure of either serial stage, or any nonclosed owned process group.

## Controls, falsifiers and remaining artifact

The consumer is frozen for independent review at SHA-256 `dca5f5b3b42b42347b0b6b49a8974663169171e2293c1eca9c4b4a8c7646a53e`; its dedicated control source is frozen at `616a4e9c464e95607140dd90b47a8288ae73183f74faa0cc5078b81b8c8767c8`. The shared-venv unittest invocation completed all 26 synthetic controls successfully in 0.306 seconds after the separate full-root-cover cost-isolation hold was released. This measured test duration is not a prediction of actual range-consumer cost. No actual export was evaluated and no range candidate was generated during this batch. The declaration's own final byte hash is carried by the later external plan rather than embedded circularly here.

Plainly: the code and its safety checks are ready for independent review. The real-data calculation has not run.

The dedicated controls use hand-authored synthetic mapping records; their 1,760-piece placeholder histories are explicitly structural plumbing, not scientific fixtures or accepted histories. They check lexical preservation, frame-error noninflation, self absence, complete pair/piece census, identity and polarity, precision and denominator thresholds, detached immutable projection, duplicate/nonfinite JSON rejection, null-versus-end-of-stream distinction, source changes and replacement, private reference capture, exact plan limits, prior receipt chains, ignored output placement, exclusive publication, late synchronization, interruption and final flushed-completion deadlines. A mocked real-main control exercises CLI routing and the real prior-receipt authentication call without evaluating an acceleration or treating a fabricated positive receipt as evidence.

Plainly: these tests check that the adapter is faithful and fails safely. They do not measure the actual asymmetric counter-breathing representative field.

A changed original byte hash, a mismatched raw-to-projection field, a missing pair or piece, a zero or invalid denominator, an unreviewed reference generation, an independent endpoint enclosure outside the reported range, a promoted claim flag, or a late/unclosed operation falsifies the corresponding candidate or admission. Broad but valid intervals are not falsified solely by being uninformative; no unannounced refinement is permitted to make them narrower. The next concrete artifact after consumer review is the independently authored range-output verifier and its synthetic controls, followed by one reviewed, byte-bound serial operational plan. No actual calculation is authorized by this declaration alone.

Closure goal: preserve the accepted one-cell conditional root evidence while producing a separately checkable acceleration/residual-range candidate, with no metric, score or historical-trajectory transfer.
