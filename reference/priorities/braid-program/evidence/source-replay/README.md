# Bound historical source replay

These manifests reconstruct exact historical inputs beside unchanged validation implementations in an isolated disposable directory. They restore the input identity required by retained tests without overwriting current authored sources or advancing historical pins. They confer no launch authorization or scientific acceptance.

| Manifest | Current consumer and retained boundary |
| --- | --- |
| [F5 replay](f5-source-replay.v1.json) | F5 reducer, preparation, prefix and Python conformance tests. The original approved configuration predates the display-history rename. Only inside the replay tree, its original bytes occupy the path declared by the frozen validator. BP-018's current display configuration and new-run admission remain separate. |
| [Orthogonal-plane replay](orthogonal-plane-source-replay.v1.json) | Fold-limit certificate tests use the original Master Equation bound by the August 29 protocol, with unchanged certificate and independent oracle code. Byte-for-byte reproduction tests determinism; it adds no independent scientific evidence. |
| [F6c source identity](f6c-refined-source-replay.v1.json) | The retained pilot's historical source-generation assertion verifies its exact original inputs. Current operational tests still exercise the current launcher. The current launch must continue to reject changed frozen inputs; this profile neither runs the historical programs nor qualifies a new scientific campaign. |

The `.source` files are raw original bytes, not rewritten prose or executable entry points. Each manifest records SHA-256, original destination and Git blob where the original was recovered from Git. F5's accepted enclosure report is the exact retained local input whose original digest remains bound by its preparation code. Keeping that compact input here makes the conformance tests reproducible from a source checkout. Current implementation files are referenced directly only when their complete bytes match the declared digest; a later implementation change fails replay admission. The F5 manifest separately labels its reviewed current orchestration dependency. Its September 12 host-capability recording change is documented in the AWT-015 review and grants no production toolchain or scientific acceptance.

Run `node scripts/dev/materialize-source-replay.mjs --manifest reference/priorities/braid-program/evidence/source-replay/f5-source-replay.v1.json --out .tmp/f5-review-root` from the repository with an existing parent and a new output directory. Substitute the other manifest for its profile. The materializer verifies every original source before writing, bounds file count and total size, rejects unsafe paths and creates the destination exclusively. It never executes captured code. Test helpers own and remove their disposable trees.

The materializer's known positive and negative controls in `tests/source-replay.test.js` passed before historical target use. Recovery also tested SHA-256 against the known `abc` digest before repository extraction; the F6c history locator passed a known-source selection before Git recovery. The [AWT-015 repair record](../../../aaa-work-threads/evidence/awt-015-residual-validation-2026-09-12.md#authorized-closure-repair-continuation) retains measurements, commands and limitations. Any original-byte mismatch, changed validator, missing dependency or stronger acceptance claim overturns this replay disposition.
