# asymmetric counter-breathing representative Continuous-Reception Root-Cover Predeclaration

Status: proposed bounded actual-data protocol, pending independent review, 2026-08-27. No consumer is implemented or actual root cover evaluated by this declaration. It is not execution authorization. The frozen library and its references remain unchanged.

## Subject And Exact Authority

**Claim grade: derived protocol; actual coverage and cost remain unmeasured.** The sole subject is the nonempty reconstructed family $\mathcal F_H$ defined by the [accepted-frame reconstruction](2026-08-27-f6c-accepted-frame-history-reconstruction.md): every permitted prehistory is joined with matching exact release position and velocity to the fixed accepted-frame future $H$. The accepted reconstruction receipt establishes $\varnothing\ne\mathcal F_H\subseteq\mathcal E$, where $\mathcal E$ is the coherent original position-and-velocity enclosure family. Bounds valid for all of $\mathcal E$ therefore apply to all of $\mathcal F_H$. No convenient individual past is selected. The [launch-readiness owner](2026-08-27-braid-search-launch-readiness.md#bp-010-reference-blocker) records the separate independent reviews and the family-versus-historical-attribution boundary.

Plainly: this checks all permitted saved pasts attached to the already specified fitted future. It does not identify any fitted path with the exact historical EOM trajectory.

The eight persistent labels are `(0+,0-,1+,1-,2+,2-,3+,3-)`, in path-key order `(1,2,3,4,5,6,7,8)`. Retained time is exactly `[-8,0.13]`, and reception time is exactly `[0,0.13]`. Each member retains all 1,600 prehistory and 160 evolved cubic pieces. Preserve every original coefficient, endpoint, per-axis radius, scalar radius, identity and polarity token. Use $c_f=1$. No coupling or charge magnitude enters this root-only calculation; their original export values remain bound and unchanged.

Plainly: neither the paths nor their uncertainty allowances are adjusted. This stage checks arrival geometry only, so it cannot supply an acceleration value.

## Frozen Inputs And Instruments

All paths in this table are relative to the repository root. Exact SHA-256 equality, not a filename or embedded success flag alone, is required.

| Role | Repository-relative path | SHA-256 |
| --- | --- | --- |
| Original-token export | `.local-data/braid-analysis/f6c-history-export-20260827.jUhLLg/retained-history.json` | `f479bb88a6425e9e98e00288f2524f33d5a3c0f4c2a14139dbaae4f468c46db1` |
| Accepted anchored-past and fixed-future receipt | `.local-data/braid-analysis/f6c-accepted-frame-reconstruction-20260827.5o7jK3/reconstruction.json` | `7c30aae03d43f7720b79288a19a9c9f9a7c0ab6b7b16ac9a948828ca80b92b43` |
| Accepted uniform-guard receipt | `.local-data/braid-analysis/f6c-retained-history-guards-20260827.hdrqLF/guards.json` | `86d7fa14ac64ee20930094ff1a59880fe4e1ef5c81758f5d8baf2c6777ee4880` |
| Conditional root theorem | `reference/priorities/braid-program/evidence/2026-08-27-f6c-continuous-reception-enclosure-contract.md` | `f20e4bdaaff8b6f0012fdc6135b15d568a817832fb55d5c42f80d8421a117f68` |
| Reconstruction theorem | `reference/priorities/braid-program/evidence/2026-08-27-f6c-accepted-frame-history-reconstruction.md` | `6abbbbacc1671052bdd881790094dbd71ebb03d54904ac1f937edae1f3c9f936` |
| Continuous-reception library | `scripts/eom/oracle/continuous_reception_roots.py` | `f38657eedb585f6066bf233cef05508ef4d4336146dbf1e44501dfa9b669e04c` |
| Library controls | `tests/test_eom_continuous_reception_roots.py` | `473cba3b039027879eeea6987515261faaadcf0833f3e4d2864fc610f5b7a144` |
| Unchanged cubic-history reference | `scripts/eom/oracle/certified_history.py` | `ca916b4bc979629a5e25c1490da07fd78a26b4e75cfba5677f35fbab658a29e7` |
| Unchanged directed decimal primitive | `scripts/eom/oracle/decimal_interval.py` | `fffc17270e149e6213315c1c82b518caa739657eb649822fd1955b8a2820e38a` |

Plainly: the two accepted receipts establish different premises: one supplies a coherent represented family, while the other supplies uniform geometric bounds. Neither receipt contains an actual root-cover result.

The receipt authors remain bound through their recorded input and instrument identities: `scripts/eom/verify-f6c-accepted-frame-reconstruction.py` at `80a96ebd0b306148b3eb96cb12e797c5cf80942e52ea457a8c6a72d58e8618a0`, and `scripts/eom/verify-f6c-retained-history-guards.py` at `efaed33a6d6e55be5788ffb7e4e6f596fbc0381466a8308154dbd550743896b9`. Their original pending labels and false historical-membership, root-execution and metric-authority fields are not rewritten. The separately recorded independent acceptance is authenticated by the coordinator. Before any pilot, the new consumer, independent comparison, launcher, exact Python interpreter and runtime identities, this declaration, and the complete actually imported source closure must be separately frozen and reviewed. No unreviewed import or cached module generation may stand in for captured source bytes.

Plainly: executable code still needs a reviewed identity before running. Existing proof outputs are reused at their original scope, not promoted by changing their labels.

## Exact Premise And Grid Checks

The comparison must independently recover both per-axis and scalar-mode guard results from the bound receipt and its original export mapping. For every member and every ordered nonself pair, verify the exact rational inequalities against

$$
\overline v=\frac{17}{20},\qquad \delta=\frac{27}{100},\qquad
\overline v^{\,2}-U_{v^2}>0.00262,\qquad L_{d^2}-\delta^2>0.001236.
$$

Plainly: these fixed simpler bounds sit strictly inside the already proved speed and separation margins. The inequalities must hold for both saved radius interpretations, not merely for one selected mode or rounded printed maximum.

An independent read-only audit reported these margins and the grid facts below before this declaration; it produced no separate durable audit file. They are explicit conditions to reproduce, not new actual-data results claimed by this author. Preserve and independently check all existing oldest-boundary strict-negative guard comparisons as well. The full retained-domain speed bound is essential: the upper-face argument crosses the interval between emission and reception, possibly across release and many original pieces.

Plainly: the prior audit motivates this finite protocol. The actual comparison must check its exact premises again from bound evidence.

Form the exact rational union of original retained-segment boundaries restricted to `[0,0.13]`. It must contain exactly 161 ascending knots, giving 160 positive-width closed cells. All eight members' future-knot lists must equal that union. The guard receipt's ordered `closedReceptionCells` must equal its 160 consecutive knot pairs, with the correct original member-segment indices. All 81 accepted-frame knots must equal every second union knot, including both endpoints. No rounded grid replacement, omitted knot or new adaptive reception partition is permitted in this stage.

Plainly: each cell lies between two original saved boundaries. Shared endpoints belong to both adjacent closed cells and both applicable original pieces; duplication at a boundary is deliberate coverage, not an extra root.

The knot identity is SHA-256 `11acd09b692fe175861d0f9478b5d1763c18e088682a0c6a16fc29d65453075c`. Its exact byte recipe is the concatenation, in ascending order, of each reduced rational's Python `str(Fraction(knot))` followed by one ASCII newline; integers therefore appear as `0`, not `0/1`. Verify the maximum cell width

$$
h_{\max}=\frac{200000000000001}{200000000000000000}.
$$

Plainly: the hash identifies exact decimal-derived rational times, including the original tiny spacing differences. The maximum occurs at zero-based cell 99, `[0.099000000000000005,0.10000000000000001]`; these tokens are not replaced by an ideal uniform grid.

## Fixed Emission Proposals And Their Derived Basis

For each closed reception cell $I_r=[a,b]$ and each of its 56 ordered nonself pairs, propose the same closed emission box

$$
I_t=[-8,\ a-1/20].
$$

Plainly: this deliberately broad box retains the saved oldest boundary and stops strictly before every reception in the cell. It is a completeness proposal, not a finely localized root estimate.

At the lower face, the accepted oldest-boundary guard gives $g_{ij}(T,-8)<0$ for every $T\in[a,b]$. At the upper face $s=a-1/20$, the same-time clearance and the same transmitter's global $C^1$ speed bound give, without assuming the root equation,

$$
\begin{aligned}
\|X_i(T)-X_j(s)\|
&\ge \|X_i(T)-X_j(T)\|-\|X_j(T)-X_j(s)\|\\
&\ge \delta-\overline v(T-s),\\
g_{ij}(T,s)
&\ge \delta-(1+\overline v)(T-s)\\
&\ge \frac{27}{100}-\frac{37}{20}\left(h_{\max}+\frac1{20}\right)
=\frac{702599999999999963}{4000000000000000000}>0.
\end{aligned}
$$

Plainly: even after allowing the transmitter to move as fast as its proved limit, separation still exceeds the wake travel distance at this upper face. This is an unrestricted geometric inequality, not a conclusion obtained by imposing a root in advance.

The frozen theorem also gives $g_{ij}(T,s_2)-g_{ij}(T,s_1)\ge(3/20)(s_2-s_1)>0$ for $s_2>s_1$. Continuity and the two strict faces therefore place exactly one nonself root strictly inside the proposed emission interval for every reception time. The same uniform sub-field speed excludes every positive-delay self root; the coincident reception endpoint is explicitly excluded. The fact that the proposal includes `-8` does not assert root contact there: its strictly negative face excludes such contact.

Plainly: the proposal has a mathematical reason to work for the represented family. Nevertheless, actual interval arithmetic can be too wide to certify it, and that outcome must remain unresolved.

## Actual Library Calls And Complete Census

Use the unchanged 90-digit library and transmitter-factor floor `1e-24`. Construct each history from all original cubic tokens with the original scalar position and velocity radii; independently verify that those scalar radii cover the original three axis radii. Represent the two fixed simplified bounds as exact decimals `0.85` and `0.27`; derive each upper emission endpoint by exact rational subtraction and exact finite-decimal serialization, without binary floating conversion. Do not import replacement coefficients or the constructive prehistory witness into the subject. The original boxes enclose the represented family without selecting a witness. The library's call-local copied-generation digest is not original-file provenance; bind both it and the export SHA-256.

Plainly: the same saved boxes remain the numerical input. The coherent reconstruction justifies which family they enclose; it does not replace their data.

Stream cells in exact ascending order. Invoke `enclose_root_cover` separately for each cell with all eight histories, a `ConditionalPremises` object whose retained domain is the full `[-8,0.13]` and whose reception domain is exactly that cell, and one complete `ReceptionCellProposal`. Use exact tuple containers, the eight independently matched member digests, eight speed bounds `17/20`, and the complete ordered clearance matrix with diagonal zero and nonself entries `27/100`. The common-history and uniform-guard assumption booleans are explicitly true only after external authentication of the accepted family premises. They are not newly proved by the call. Calling one cell while leaving a full-domain reception premise would violate the frozen library's complete-coverage contract and is prohibited.

Plainly: one call proves one complete cell. The separate comparison must then prove that the 160 cells together cover the entire requested time interval.

Every call must return `conditional_complete` and exactly 64 rows in receiver-major, transmitter-minor order: eight self exclusions and 56 single nonself roots. For every nonself row, check the actual returned unrestricted oldest and lower faces are strictly negative and the actual upper face is strictly positive, before relying on root-only distance/factor intersections. Here oldest and lower are the same face; the library's two recorded fields must agree, but they do not count as two different lower-face proofs. Require positive enclosed distance, transmitter factor at least `1e-24`, positive receiver factor, the exact proposed time boxes, complete original-piece coverage, a conditionally root-free complement, and no root retained-boundary contact. A derived face formula or scalar-time sample cannot substitute for these actual returned checks.

Plainly: the checker must succeed on the saved numbers and retain the bounds it used. A promising theorem or an apparently correct nominal root is not a completed numerical cover.

The full census is exactly 160 cells, 10,240 rows, 8,960 ordinary nonself roots, 17,920 distinct nonself lower/upper face checks, and 1,280 explicit self exclusions. These are derived counts, not completed results. No acceleration sum, RMS/peak residual, root-location tolerance, adaptive box refinement, three-rung evolution or score computation is part of this stage. If a fixed proposal is unresolved, stop and preserve the failure; a revised proposal requires a separately reviewed declaration rather than silent tuning.

Plainly: this is one finite coverage calculation on the existing refined record. It neither runs nor substitutes for the later acceleration measurement ladder.

## Bounded Pilot, Resources And Publication

Before any actual evaluation, independently accept this protocol and the frozen consumer, independent comparison and supervision code. Implementation proceeds in separate batches: first author, independently review and freeze the exact-data comparison/reference against the fixed mathematical and data contracts; only then implement the consumer as its separate subject. Do not develop or repair both sides together and call their agreement independent evidence. Freeze and review that consumer and its supervision before the pilot. The first actual-data run is only zero-based reception cell 0, `[0,0.001]`, with all 64 rows and the same complete source capture, premise checks, output and independent comparison obligations. Give this pilot an end-to-end 1,800-second ceiling; it does not automatically continue into cell 1. Measure wall time, user/system CPU, maximum observed individual-process resident memory, named output bytes, imported source generation and all piece-visit counts. Its measured end-to-end cost includes the separate independent comparison, not just the root-library call. The synthetic library timing is not an actual asymmetric counter-breathing representative cost measurement.

Plainly: the wide emission boxes can be much more expensive than small synthetic controls. The first real cell measures that workload before the full run is considered.

The pilot additionally requires a separately reviewed launch plan before it starts. Numerical memory and disk limits are deliberately reserved for that plan, not silently omitted or inferred from the wall limit. The plan must specify the exact observed memory quantity and process scope, its numerical stopping limit, minimum available disk bytes, maximum named-output bytes, observation commands or APIs, cadence no slower than 15 seconds, observation timeout, and exact boundary predicates. Missing, malformed, timed-out or failed observations must stop the attempt. No pilot is launch-ready until those testable limits and their supervisor implementation are fixed and independently accepted. The later measured full-run plan must explicitly carry or replace them before full execution.

Plainly: this document fixes the time budget, but it does not guess the machine's memory or disk allowance. Those remaining operational numbers must be declared and checked before even the first cell runs.

A separate reviewer must accept a measured full-run resource plan bound to the pilot output, completion and comparison hashes before the remaining full 160-cell attempt is launched. The plan reports `160 * pilotEndToEndWallSeconds` as a plainly labeled extrapolation, not an upper bound or capacity guarantee; later emission boxes touch more evolved pieces, and shared startup/finalization costs also differ. It must state the measured baseline, resource allowance and stopping predicate. This declaration fixes one serial evaluation worker and no EOM workers; it grants no parallel dispatch. A projection or resource observation that does not fit the unchanged 1,800-second full-run ceiling returns the plan without launching the full run.

Plainly: real timing informs the decision, while the hard deadline remains the protection against a poor estimate. More workers or a larger time allowance cannot be assumed.

The full attempt starts its 1,800-second clock before source capture, imports, data parsing or history preparation. It ends only after all 160 cells, the independent comparison, final source rechecks, durable output publication, final hash/summary checks and owned-process closure. The pilot has the same inclusive clock definition. An external supervisor must maintain a flushed 15-second heartbeat during synchronous library calls, comparison and finalization, and must interrupt timed-out work rather than relying only on checks between cells. Log elapsed time, current stage/cell, completed/passing/failed/not-run row census, and resource state. Use the shared Python environment specified by `AGENTS.md`; no compiled build or EOM subprocess is involved. Bind and measure any supervisor or profiler process separately and close it on every terminal path, following the [long-running-job policy](../../../op/long-running-test-heartbeats.md).

Plainly: parsing, checking, writing and cleanup all count. A stalled calculation or slow final write cannot escape the time limit by ceasing to emit progress.

Read inputs and source through bounded regular-file descriptors, with same-descriptor type/size checks and captured-byte hashes; reject FIFOs, invalid encodings, malformed exact tokens, missing files, oversized inputs and changed generations. Each parsed JSON input or captured project-source file is limited to 64 MiB; runtime binary hashing is bounded and streamed without requiring a full in-memory copy. Execute the reviewed captured source generation, not a later on-disk import or stale cached module. Rehash every bound input and source after computation and before final admission. A changed source, proof receipt, export, runtime or declaration invalidates the entire attempt, not just its last cell.

Plainly: the proof must remain attached to one exact set of data and code throughout the run.

Reserve a fresh create-exclusive attempt directory under `.local-data/braid-analysis/f6c-continuous-reception-root-cover-20260827/`; verify the lane is ignored before writing. No overwrite, resume, checkpoint reuse or append to a prior attempt is permitted. Preserve raw returned scalar/vector interval tokens and operational logs. Publish no successful receipt until the final elapsed check and process closure succeed. Use private durable staging and create-exclusive publication; a late or interrupted publication must not leave an admissible accepted result. Final admission requires a fresh successful completion with matching output hash, inclusive elapsed time at most 1,800 seconds, and independently observed closed owned processes. A file's embedded `accepted` field alone is never authority.

Plainly: incomplete outputs remain useful evidence of what happened, but cannot masquerade as a successful cover.

## Compact Evidence And Independent Comparison

Persist at most one library cell result in working memory at a time. Immediately serialize its 64 rows and release the full per-row piece arrays after compacting them. Preserve every returned geometry interval, face interval, pair/time identity, census and exclusion flag without binary floating conversion. For each receiver/transmitter coverage role, retain the original history digest, requested interval, touched-piece count, first/last indices, contiguous index-range description and SHA-256 of the complete ordered clipped-piece list. Its canonical hash input is one ASCII line per piece, `index<TAB>reduced-lo<TAB>reduced-hi<NEWLINE>`, with rational formatting as above. Shared endpoint singleton pieces must remain in that sequence. Bind each compact record to its row and role; reconstructability from the original export and exact requested interval is required. Do not retain or duplicate millions of full piece records merely to repeat information fixed by the bound source.

Plainly: compact coverage evidence records exactly which original pieces were used, including boundary pieces. It is not a sample, and a second checker can reconstruct every omitted list entry from the frozen input.

A separately authored exact-data comparison, importing neither the new consumer nor its root library as its oracle, must validate the source and receipt identities, original-token-to-history mapping, all scalar/per-axis relationships, family applicability, exact speed/clearance simplifications, oldest guards, full knot/cell mapping, all returned rows and their strict signs, exact interval and factor conditions, coverage-piece hashes, shared-endpoint treatment, row order and complete aggregate census. Its numerical checks must be independently justified by exact rational interval calculations and directed square-root endpoint inequalities or equivalent proved bounds, not by replaying the consumer. Independence and source hashes must be established before the actual pilot; agreement with an output copied from the same implementation is not evidence.

Plainly: the library has been reviewed on known examples, but the actual data mapping and every emitted row still need a genuinely separate check.

The final record binds the declaration, accepted resource plan where applicable, captured sources/runtime, original export and premise receipts, original-to-library history digest mapping, knot hash, row-stream hash/bytes, compact piece-audit hash/bytes, independent comparison, resource profiles, progress log and terminal completion. It distinguishes the library's unchanged false `premise_truth_authenticated`, `subject_membership_established`, `execution_authorized`, `metrics_available` and `h3_evidence_eligible` fields from the externally established applicability to $\mathcal F_H$. Do not flip those library fields or claim that externally authenticated family applicability establishes historical subject membership.

Plainly: the external record can explain why the conditional theorem applies to the proved reconstruction family. It cannot turn the library into an authority for historical identity, scores or another run.

## Stopping, Falsifiers And Excluded Claims

Stop the attempt at the first unresolved cell, invalid row, source mismatch, premise/mapping discrepancy, missing or extra piece/member/pair, empty intersection, non-strict face, factor-floor failure, deadline/resource contact, logging/publication failure or failed independent comparison. Preserve the exact failure and completed prefix. Every scheduled row has a disposition: completed conditional row, failed/unresolved row where identifiable, or `not-run`; no unvisited row is treated as passed. A pilot result covers only its one cell. A partial full run or resource return establishes neither full coverage nor a geometric negative; do not infer cost feasibility, H3, acceleration or favorable residuals from it.

Plainly: a stopped instrument leaves an unfinished question, not a hidden success or a rejection of the asymmetric counter-breathing representative geometry.

**Derived-claim falsifiers:** an admissible history violating the stated unrestricted-face inequality, a second retained nonself root under the strict speed premise, or a positive-delay self root under that same premise would overturn the applicable theorem. **Actual-result falsifiers:** an independently reconstructed original interval, member, pair, guard margin, source identity or returned enclosure disagreeing with the completed packet invalidates that application. Actual membership of a particular historical analytic past or EOM trajectory is not asserted and is not inferred from successful reconstructed-family coverage.

Plainly: the mathematical statement and its data application have different failure tests. Neither may borrow authority from the other when a premise is missing.

Excluded claims are historical EOM trajectory identity, an exact solution of the Master Equation, acceleration or residual values, `M05/M06` availability, three-rung refinement, H3 promotion, score changes, ordinary evolution, equilibrium, binding, retention, return, stability, physical realization, and execution authorization. The existing [member-acceleration predeclaration](2026-08-26-f6c-normalized-member-acceleration-predeclaration.md) remains unchanged. The open technical work is reviewed consumer/comparison/supervision implementation, the separately admitted one-cell cost pilot, acceptance of a measured full-run plan, and the actual complete cover; this document closes none of those by itself.

Plainly: this declaration fixes the next narrow calculation and its limits. It does not grant permission to run it or claim an outcome.

Closure goal: independently accept this fixed reconstructed-family root-cover protocol, then measure one actual cell before deciding whether the complete 160-cell calculation fits its unchanged budget.
