# phase-varying display representative ordinary evolution evaluation

Status: independently accepted as a completed bounded unresolved experiment at `2026-08-28T03:46:06.690568Z`, before the original campaign deadline. All three EOM runs halted before the requested horizon, the independent first-step comparison passed, and the completed independent final-snapshot root check failed. Validated motion, collapse, separation and full-horizon persistence are not established. Operational process and lock closure is independently verified.

The final independent receipt is actual-evaluation-independent-outcome-review-v1.json (`../../../../.local-data/braid-analysis/f5-ordinary-evolution-20260827/actual-evaluation-independent-outcome-review-v1.json`), SHA-256 `8af7fe88c538ac6684bfe85dd1ec080a060575c969893356e5e76757781c4847` (`63979` bytes). It records `experimentClosure:true`, `boundedUnresolvedOutcomeAccepted:true`, `declaredChecksCompleted:true`, `declaredChecksAllPassed:false`, and `validatedMotionAccepted:false`. The reviewer independently recomputed 72 first-step interval predicates and 96 sensitivity predicates, checked all pair/path/step censuses, all twelve stage records and 62 output files, all 194 declared bindings, final publication and independent process closure.

Plainly: the experiment is complete because its declared calculations and checks returned a checked unresolved result. Its failed scientific gate remains failed; experiment closure is not acceptance of a validated trajectory.

## Authorized resumption

The operator answered, “Yes, I approve your experimentation efforts going forward. Please proceed.” This is recorded in `.local-data/braid-analysis/f5-ordinary-evolution-20260827/operator-authorization-v1.json` as approval of the immediately preceding fixed-strength phase-varying display representative question: $K=0.002$, $q_0=1$, $\kappa=0.002$, $c_f=1$, horizon `T=0.5`, three numerical refinements and no strength search. No geometry change, benchmark replicas, commit, push or deadline extension is inferred. The original deadline remains `2026-08-28T08:56:11-04:00`.

Plainly: the missing scientific choice was supplied before declaration review and execution. It was not inferred from the geometry approval.

The frozen declaration is SHA-256 `e908f1fc47acaeb0ba046818fc66a2781251492be2022cdb978e4727e0d72515` (`56324` bytes). Its 194 source/runtime/evidence bindings were reauthenticated before freeze. Exact request envelopes live in `prepared-v1/`; their coarse/medium/fine SHA-256 values are `21f96044be1274b7c80ec5d020ba465c42ceb18506793ec13ea0044a1850bdf2`, `660b7dd20d46945f14e8bb29efe0a6fb7ed983b308a9182253fd809106ddd618`, and `cc77a7cb655cdc1de8f2f76644bb3519bc490deaeb9c34e45e84665aa7015ae3`. Byte-for-byte regeneration matched all three. The earlier unapproved draft and blocked checkpoint remain preserved as historical preparation records.

The sections below record preparation before this authorization; their pending-decision statements describe that earlier checkpoint, not the current authorization state.

## Actual execution

Independent frozen-declaration review passed at SHA-256 `712f6b74f36e3b2755f5b486b72bfe21befff7d41a26f477da7aa2d94fcb4d28`, in `frozen-declaration-review-v1.json` under the task directory. The reviewer checked all three full request wires, 194 bindings and 33 runtime aliases; actual parser and host gates remained mandatory. The receipt copies some explicitly historical limitations from the earlier preparation review, including its then-false authorization state; its current explicit authorization object records the operator's approval.

Plainly: this review accepted the declaration and exact inputs for execution, not a motion result.

After asymmetric counter-breathing representative explicitly released its compute window, the complete caller acquired the shared lock at `2026-08-28T03:19:32.519Z` (`2026-08-27 23:19:32.519 EDT`), launcher PID `51864`. The first actual parser admission passed before coarse EOM PID `51872` began. The caller enforces the unchanged declaration and records per-stage resources and complete output under `.local-data/braid-analysis/f5-ordinary-evolution-20260827/evaluation-v1/`. Its final stdout seal is captured in sibling `evaluation-v1.stdout.ndjson`; heartbeats and the external `/usr/bin/time -l` summary are captured in `evaluation-v1.stderr.log`. External maximum RSS is not aggregate simultaneous RSS. The predeclared observation wrapper is recorded in `declared-invocation-observer-v1.json`, SHA-256 `1db2cdd7c2500176c9b4306652a586b14d0f68d4649403c2aa17a54418574ce8`.

Plainly: this was an actual evolution invocation from the accepted past-only history. It generated future motion without receiving the prescribed positive-time path.

The actual coarse parser output has separate independent data-conformance acceptance in `actual-coarse-parser-independent-review-v1.json`, SHA-256 `a6551b3f227740faf0f95a3727140f9dfc9900777edc60362cfa0d5b70d3671f`. The reviewer checked all twelve ordered worldlines, 612 pieces, 12240 original segment decimal tokens, independently recomputed history fingerprints, 144 endpoint binary64 values and all declared/resolved controls. All parser-only authority flags remain false. This does not certify roots, integration or an outcome.

The same independent metadata obligations passed for actual medium and fine inspection outputs, respectively in `actual-medium-parser-independent-review-v1.json` (SHA-256 `5c32e030d0acdf518e56a058fae951180a3d7415597f3b66844ee43dfd11911f`) and `actual-fine-parser-independent-review-v1.json` (SHA-256 `5997cb7ab4dfc2ce9b9b18b6941ddc11c8e9033116a6bfa61471ea3393eb7f80`). Each also checks all 60 RUN tokens and its thirteen bound files against the completed actual inspection receipt. No additional evolution or reference calculation was performed by these parser audits.

Plainly: all three consumers received the declared data and controls. Parser acceptance remains separate from the outcome checks below.

### Completed rung checkpoints

The frozen scientific conditions are the approved phase-varying display representative geometry, exact retained history `[-1,0]`, release at zero, $K=0.002$, $q_0=1$, $\kappa=0.002$, $c_f=1$, sharp chart and requested end `0.5`. The three runs use these fixed numerical settings:

| Rung | Initial = maximum step | Minimum step | Root tolerance | Acceleration tolerance | Position = velocity tolerance | Correction tolerance |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| coarse | `0.00390625` | `0.0001220703125` | `2e-8` | `8e-6` | `1e-7` | `1e-6` |
| medium | `0.001953125` | `0.00006103515625` | `1e-8` | `4e-6` | `2.5e-8` | `5e-7` |
| fine | `0.0009765625` | `0.000030517578125` | `5e-9` | `2e-6` | `6.25e-9` | `2.5e-7` |

Plainly: only the predeclared numerical refinement changes between runs. Strength, geometry and the starting history do not change in response to the results.

Common controls are MPFR precision 128 through 512 bits, root depth 256 and 100000 cells, at most sixteen correction iterations, 4096 attempted and 128 rejected steps, fixed pairwise outward reduction, four published subsegments per accepted step, no adaptive growth and no far-field acceleration enclosure. Geometric checks use close-approach distance `0.02`, origin radius `0.8`, eight Bernstein subdivisions and at most 100000 panels. Numerical failure, minimum-step exhaustion, declared resource/deadline limits and operator interruption stop the corresponding work; no rescue chart, automatic retry or post-result budget expansion is allowed. The complete token-level controls, including dormant transport fields, remain in the [frozen declaration](2026-08-27-f5-ordinary-evolution-declaration.v1.json).

Plainly: the experiment has explicit boundaries even when it cannot reach its requested end. Close approach and radial excursion are limited geometric witnesses, not literal collision, permanent escape or whole-assembly classification.

**Measured by the frozen EOM executable and separately bounded by the exact-rational geometry checker:**

| Rung | Accepted end | Accepted / rejected attempts | EOM wall seconds through descriptor closure | Maximum sampled aggregate RSS, bytes | Solver halt |
| --- | ---: | ---: | ---: | ---: | --- |
| coarse | `0.017822265625` | 29 / 3 | 395.151682708 | 373309440 | `root_completeness_not_certified` |
| medium | `0.00836181640625` | 28 / 3 | 396.279316250 | 430178304 | `root_completeness_not_certified` |
| fine | `0.003662109375` | 23 / 3 | 335.016041666 | 369016832 | `root_completeness_not_certified` |

Plainly: these are completed positive-time computations, but none reached the declared `0.5` horizon. Their processes exited normally; the reported halt is a numerical certification boundary rather than a resource stop or physical event.

The geometry reference reports `unresolved-halted-prefix` for all three. On the generated coarse interval, minimum pair separation is bounded by `[0.131189595606261986, 0.131190450510155355]` and maximum origin radius by `[0.431402073746125725, 0.431402099379654604]`; on the medium interval these are `[0.131229055938648179, 0.131229706198482084]` and `[0.431395068493069649, 0.431395079586923752]`. There are no declared threshold witnesses. These bounds apply to the serialized cubic histories plus declared component error radii; they are not an independent enclosure of the exact dynamical solution.

Plainly: the stored prefixes stay well away from the declared close-approach and radial-excursion thresholds. Nothing is established about the uncomputed remainder of the horizon.

The fine generated interval has minimum pair-separation bounds `[0.131241896335886975, 0.131242318478099394]`, maximum origin-radius bounds `[0.431393470357888596, 0.431393793544906903]`, and no declared threshold witnesses. Its raw response SHA-256 is `776212cd40b38eb6f91d10a2a2f5534c23589a6bc3ac902c8f7a281a56c63432`.

Both adjacent-rung sensitivity checks passed the declared `1e-4` position and velocity tolerance, including both histories' error radii, on exactly two sampled times each: coarse/medium at `0` and `0.00836181640625`, medium/fine at `0` and `0.003662109375`. Each check has 48 rows (twelve paths, two quantities, two times). All sixteen positive times on the declared `1/32` grid are explicitly unreachable. Approximate square roots of the independently reported exact squared upper bounds are: coarse/medium maximum position difference `8.87151e-9`, velocity difference `9.94691e-7`; medium/fine `4.16761e-9` and `9.93678e-7`.

Plainly: these checks find no sampled disagreement above tolerance on the shared short prefixes. They do not test continuous-time convergence or the requested full horizon. The earlier halt under tighter settings is part of the measured sensitivity outcome and is not cured by agreement at the two reachable comparison times.

**Source-localized diagnostic, not a causal proof:** every coarse rejected attempt contains `numeric_precision_limit_exhausted` at the declared 512-bit ceiling, with `interior_root_not_surrounded/joint_root_history_missing` and no memory-boundary contact. The final rejection names `f5-axis-2-ring-2-negative-worldline` receiving from `f5-axis-1-ring-2-positive-worldline`, transmitter segment 31. Rejected `rootAccounting` records the first substep's accepted start snapshot; `rootFailures` scans candidate substep snapshots separately. Thus its apparently complete starting census does not contradict the candidate failure. The controller stopped when further subdivision would violate the minimum step.

Plainly: this identifies where the certificate was refused. It does not prove an uncertainty floor, a singularity, a missing physical root or a successful remedy. The serialized failure rows omit exact reception time, substep index and correction iteration; empty local-error rows exclude final publication recertification, but do not distinguish the candidate predictor and correction stages. Nonfinite residuals serialize as null and cannot provide that distinction. The independently read raw coarse response is SHA-256 `cf3f3b37e34699ce6913a5a530239bd2567129871e2bec85d848786dcccb93a9`; a conflicting raw row or changed source binding would falsify this localization.

### Completed independent dynamics check

**Measured by the separately authored, frozen Python dynamics reference:** the first-step calculation to `0.0009765625` returned an accepted step. All 72 position/velocity axis comparisons passed interval overlap and the declared `6.25e-9` midpoint tolerance. Approximate largest midpoint differences were `1.52229e-15` in position and `3.17649e-12` in velocity. The final snapshot at `0.003662109375` completed its full 144 ordered-pair census, returning 58 certified rows and 86 uncertified rows, with 173 unresolved cells and no memory-boundary contact. Its status is `uncertified`; its failure code is `unresolved_root_set`. The wrapper refused to compare partial acceleration totals and reported `uncertified-snapshot`.

Plainly: the independent calculation agrees with one short initial step, but does not independently validate the fine run's final acceleration. This is a completed negative certificate check, not an interrupted calculation or a claim that those physical roots do not exist. The reference's enlarged scalar uncertainty boxes and the EOM solver's correlated boxes are distinct declared constructions; their differing certification statuses alone do not establish an EOM defect.

The dynamics process completed in `51.764296333` seconds through descriptor closure, returning exit code 2 under its declared unresolved-result contract. The reference's own measured work was `51.420458708` seconds and `50.760354` CPU seconds. It did not exhaust the 840-second numerical-work budget or 900-second process budget. The complete caller preserved `localDynamicsChecksPassed:false`, `sensitivityChecksPassed:true`, `allRungsGeneratedPositiveTime:true`, and `allRungsReachedHorizon:false`. Its scientific status remains `unresolved-evidence-pending-independent-actual-output-admission`; it does not self-approve the experiment.

Plainly: a successfully completed evaluation can report failed scientific checks. Successful process closure does not turn those failed checks into validated motion.

### Measured resource use and operational closure

The twelve watched stages used `1126.447040624` seconds for the three EOM runs, `2.105832792` seconds for parser inspection and `128.535338499` seconds for independent geometry, sensitivity and dynamics checks. The complete caller reached its final publication at `1260.586` seconds and its final checks/stdout seal at `1260.901` seconds. External `/usr/bin/time -l` measured `1261.00` wall seconds, `1238.49` user CPU seconds and `54.84` system CPU seconds. The evaluation directory contains 62 files totaling `23272896` bytes. `actual-evaluation-resource-summary-v1.json` records stage boundaries and the bound raw receipts.

Plainly: these are measured execution costs, not estimates from geometry or step counts. Independent review and publication after the caller exited are separate from the 1261-second invocation cost.

Maximum sampled aggregate RSS during watched stages was `430178304` bytes; the largest sample gap was `0.769691709` seconds, below the declared two-second stop limit. Samples sum the launcher and owned descendants but do not continuously cover parent-only interstage or final-publication work. External maximum resident set size was `735903744` bytes; that value is not a simultaneous aggregate-memory measurement. The declared limits remained one scientific process, 2 GiB sampled aggregate RSS, 5 GiB minimum disk free, 20% minimum host memory free, 128 MiB per output file, 1 GiB aggregate output and 12600 seconds for the whole caller, also bounded by the original campaign deadline.

Plainly: the measurements retain their instrument limits. Neither sampled RSS nor the external single value is claimed to be an exact continuous whole-call aggregate peak.

The final stdout seal authenticates `invocation-receipt.json`, SHA-256 `4cde5cbb820fb0b3464875f17d2fcf1fcdfd797ee958aaaa1fb16465b92fc525`, and says `operationallyComplete:true`. Independent closure receipt `actual-evaluation-independent-process-closure-v1.json`, SHA-256 `a1b3e63cb68e8645614cb262d344f8055c04b3756d6b0f0a086c279a70d9e1cd`, authenticated all twelve closed stage receipts and their outputs, then performed a fresh process census and kernel existence checks. All thirteen observed PIDs and twelve groups were absent; the shared lock was absent before and after review publication. The parent observed caller session `54457` exit code 0; the independent reviewer separately checked launcher absence and the sealed output, not that tool event itself. The exclusive compute window was released to asymmetric counter-breathing representative; no further phase-varying display representative numerical work is planned for this declaration.

Plainly: the owned work is safely closed. This point-in-time operational evidence does not prove that an unobserved detached process could never have existed, and it grants no scientific acceptance by itself.

### Accepted evaluator handoff and exact invocation

The machine-readable accepted evaluator handoff (`../../../../.local-data/braid-analysis/f5-ordinary-evolution-20260827/accepted-evaluator-handoff-v1.json`) binds the original declaration, exact requests, source/build/reference identities, independent reviews, output schema and resource observations. The recorded complete invocation from the repository root was:

```sh
set -o noclobber
/usr/bin/time -l env -i PATH=/usr/bin:/bin LC_ALL=C LANG=C /opt/homebrew/Cellar/node/26.3.0/bin/node scripts/eom/run-f5-ordinary-evolution.mjs --declaration reference/priorities/braid-program/evidence/2026-08-27-f5-ordinary-evolution-declaration.v1.json --out .local-data/braid-analysis/f5-ordinary-evolution-20260827/evaluation-v1 > .local-data/braid-analysis/f5-ordinary-evolution-20260827/evaluation-v1.stdout.ndjson 2> .local-data/braid-analysis/f5-ordinary-evolution-20260827/evaluation-v1.stderr.log
```

Plainly: this command invokes the entire scientific sequence, including the failed final check. Its existing output and logs must not be overwritten. An independently authorized reproduction changes only the fresh output/log paths; the caller, declaration, scientific settings and request bytes stay unchanged. The original deadline and fresh shared-host admission still apply. This handoff does not authorize replicas, concurrency, new numerical settings or a deadline extension.

The complete output schema is `braid-program/f5-ordinary-evolution-evaluation.v1`; its index is `evaluation-v1/evaluation.json`, SHA-256 `fbf32d1b7a2d63923644769f58ccb19aa3febd41c2ca3967f58dc8c56e3c0fe2`. The final reference record is `evaluation-v1/independent-dynamics.json`, SHA-256 `ab70c22e7242ee4d139de681d045a40513313d277efeca5475eb79163217fd19`. Consumers must read the independent outcome receipt, not treat the caller's operational seal or exit zero as scientific acceptance. The caller owns an exclusive shared lock and does not permit parallel invocations or exported-stage bypass. Any later parallel composition is a separately reviewed operational change, outside this completed experiment.

Plainly: another task can reproduce and compare the same bounded experiment. It must preserve both the successful checks and the unresolved final-root result, and must obtain its own execution admission.

## Campaign boundary

The operator requested one independently checked ordinary EOM evolution evaluation of the approved phase-varying display representative geometry. The recorded start is `2026-08-27T20:56:11-04:00`; the hard stop is `2026-08-28T08:56:11-04:00`. Independently verified experimental closure may end the campaign earlier. Operator stops, safety limits and indispensable missing authorization take precedence. Preparation alone is not closure. No commit, push, unrelated regeneration, geometry change or deletion of historical evidence is authorized.

The task is `01a045dd-4365-7942-91eb-d1c5cd5f330e`. The parallel asymmetric counter-breathing representative task owns its scientific calculation and has requested that the shared [readiness owner](2026-08-27-braid-search-launch-readiness.md) remain byte-stable at SHA-256 `04a889345ee7c4cd43b0f41bebb70cb2f176ac4d13af035204347f3c6904feaf`. Its numerical execution excludes competing numerical programs. This task must coordinate an execution window before launching numerical work. The benchmark coordinator receives an evaluator only after actual independent acceptance; a pending declaration is not an accepted evaluator.

## Reauthenticated starting evidence

Read-only checks matched these existing artifacts to the live readiness owner:

| Artifact under `.local-data/braid-analysis/` | Bytes | SHA-256 |
| --- | ---: | --- |
| `f5-prehistory-handoff-20260827-v1/handoff.json` | 908879 | `4e0696a848a0d36ccbe5948295e71738c933b7ea120e9aee00e2effdd6ecc149` |
| `f5-prehistory-handoff-20260827-v1/handoff-conformance.json` | 8854 | `5b7ddc53d47a97f81383bd043c37e93daa80fe5a9b595d400faddd4c9724b622` |
| `prescribed-response-f5-release-20260827-v1/response.json` | 1297615 | `96b6068e92b9c58e44a87a8f1496a89f9103ed1da43685be38d5c8d23ef45e78` |

Plainly: the saved files still match the accepted records. This verifies their identities today; it is not a new execution of the earlier instruments.

The release-response record's nineteen direct source bindings were also rehashed successfully. The accepted starting history has twelve members and 51 retained cubic pieces per member on `[-1,0]`. Every coefficient, uncertainty allowance and polynomial origin is to be retained. Positive-time prescribed data is forbidden as evolution input. The existing [ordinary request preparer](../../../../scripts/eom/prepare-ordinary-evolution-request.mjs) is the preferred transport owner, subject to actual consumer and output checks.

## Historical preparation: strength decision

**Recommendation, heuristic:** one fixed effective strength $K=\kappa q_0^2=0.002$, represented by charge magnitude $q_0=1$ and coupling $\kappa=0.002$, with normalized wake speed $c_f=1$. There is no strength search and no post-result strength optimization.

Plainly: the twelve existing polarity signs determine the charge signs. The common charge magnitude fixes the charge unit, while the coupling sets the proposed interaction strength. The operator has not yet approved this recommendation.

**Measured scale comparison:** read-only Node arithmetic on the authenticated release-response component midpoints gives root-mean-square response magnitude `28.97337965111184`. Differentiating the retained final cubic twice at release gives root-mean-square prescribed cubic curvature magnitude `0.0541021595827011`. Their magnitude ratio is `0.0018673057901488186`; the rounded recommendation gives instantaneous RMS acceleration approximately `0.05794675930222368`.

Plainly: the proposed strength puts the instantaneous acceleration near the scale of the starting path's curvature. This comparison does not match vector directions, certify analytic acceleration bounds, derive a physical constant, establish balance or predict sustained motion. These are ordinary floating-point scale estimates, not new interval proofs or braid metrics. A changed input hash or an independently recomputed scale inconsistent with these values falsifies this rationale.

The authorization question was sent explicitly to the operator. Until an explicit answer is received, no request may gain execution authority and no strength-dependent phase-varying display representative calculation may run.

## Historical preparation: declaration and checks

The [draft declaration](2026-08-27-f5-ordinary-evolution-declaration.v1.json) proposes an observation horizon of `T=0.5` and three serial refinements, with initial steps `1/256`, `1/512` and `1/1024`. Root, acceleration and local integration tolerances tighten together. This tests bundled numerical sensitivity on the identical retained history; it does not isolate convergence order or reduce the starting history's uncertainty. All authorization fields remain false or null until the operator answers.

Plainly: the three runs would ask the same scientific question with progressively stricter numerical settings. They would not try three strengths or alter the approved geometry.

The geometric thresholds are post-run witnesses: a named pair reaching distance `0.02` is close approach, not literal collision or whole-assembly collapse; crossing radius `0.8` from the fixed origin is radial excursion, which can include rigid translation, not proof of constituent separation. The independent geometry checker can exhaust its `100000`-panel cap before the EOM attempt cap; that is an unresolved checking boundary, with no post-result expansion authorized.

The separate dynamics instrument uses the existing frozen Python oracle for a first-step numerical comparison and for independently recomputed roots and accelerations at the final accepted snapshot. The first-step end is the smaller of `1/1024` and the actual first accepted fine-step end; no accepted step means no generated-step audit. The oracle and EOM solver initialize centers and propagate uncertainty differently. Their overlap and midpoint comparison therefore does not independently validate the EOM uncertainty-transport rule or enclose the full solution.

Plainly: independent checks can test the generated motion, a short initial step and a final acceleration snapshot. They cannot establish the exact motion for all intermediate times, global uniqueness, stability or permanent retention.

## Historical preparation: strength-independent work

The existing request preparer and single-shot certified EOM interface are retained. The only shared EOM source edit is additional request inspection, accepted-step progress and result observability in [the existing CLI](../../../../src/eom/native/eom_borg_shadow_cli.cpp); no core integration or acceleration mathematics changed. The previous CLI bytes were preserved under `.local-data/braid-analysis/f5-ordinary-evolution-20260827/` with their full SHA-256 in the archive filename. Parser inspection explicitly reports that it has neither validated an evolution request nor evaluated roots or executed the EOM solver.

**Measured preparation:** task-specific Release build `build-v2/eom_borg_shadow_cli` has SHA-256 `b24bfb23bc80adce79061273d01ac5e2094653a213041ca27fe44f6021a873aa`. The watched configure and single-worker compile took `1.652212791` and `29.283524958` seconds through process closure. The maximum sampled aggregate build RSS was `564838400` bytes. All project EOM sources matched their pre-build hashes afterward. Exact commands, source bindings and observations are in `build-inputs-v2.json`, `build-completion-v2.json`, `build-configure-v2/` and `build-compile-v2/` under that task directory. The earlier sandbox-rejected observation attempt remains preserved as an unsuccessful attempt.

Plainly: the executable was rebuilt from the recorded current project sources. The reported memory is sampled process memory, not a continuous peak or a measurement of phase-varying display representative evolution cost.

Five existing or extended synthetic process-consumer tests passed against this exact binary: protocol, parser-only token/endpoint inspection, rejection of incompatible inspection modes, accepted-step progress/local errors, and generated-history publication. Test time was `0.377` seconds; watched process closure took `0.610855667` seconds with maximum sampled aggregate RSS `57769984` bytes. The log and resource receipt are in `consumer-controls-v1/`. These are synthetic interface tests, not an ordinary phase-varying display representative evolution result. All owned build/test processes were closed and the shared numerical window was released to the asymmetric counter-breathing representative task.

The independent geometry checker uses exact rational polynomial bounds and has a separately reviewed synthetic test suite. The revised dynamics wrapper has now passed independent source review at SHA-256 `cbf60f6b7e4a48a4622ce475393880689c155199c7f4f3513f0b382528761a6f`, with tests at `c582879d5f83810df4c5f3bf02e6f485d221b9038573da73bcc66d13e05d4998`. The reviewer reran fourteen synthetic tests, checked 500 independently generated valid chronologies and 1000 invalid mutations, and verified the archived previous wrapper and all five frozen oracle modules. Its separate receipt is `dynamics-wrapper-revision-review-cbf60f6b-v1.json`, SHA-256 `5c98a12e65d5878cc8e55689e944e2547d0724e4edf06fab2c60871ecfa37bd2`, under the task directory. This accepts the wrapper's bounded reference-consumer behavior, not a candidate computation or launch.

Plainly: the reference mathematics was kept fixed while a separately checked wrapper learned how to select the declared short audit interval from the actual accepted steps.

The full caller passed independent source review at SHA-256 `2844ad05ddba2e1246d89a431de42aab9f113fda5769f6faea91d7ea245934b3`, with tests at `9deecc9756bc518d13e6c8b9245425ebd57f1523cefb95987d3d53550b4ae739`. All twenty synthetic controls passed in `5.537` seconds, including four explicitly admitted process tests. Independent additional controls covered 300 JSON roundtrips, 300 escaped duplicate-key rejections, 100 partial-write schedules and eight status combinations. The separate receipt is `runner-independent-review-2844ad05-v1.json`, SHA-256 `ba215da15eadd03d55a34012bdda07f3af5ec60789626964276d02f531d213af`. It accepts code behavior, not strength, a frozen declaration, launch or an actual outcome. Process supervision retains the reviewed premise that invoked code does not detach descendants; ambiguous closure retains the shared lock.

Plainly: the supervisor was checked against children that ignore termination, broken monitoring, failed logging and other synthetic failures. Those tests show how it stops and preserves evidence; they say nothing about phase-varying display representative motion.

Runtime metadata records include the compiler dependency snapshot, isolated Python imports and 42 non-system executable/library bindings. Independent inventory review additionally identified and bound the actual Python application executable reached through the shared-venv launcher and the post-build static EOM library. The draft currently contains 190 source, runtime and evidence bindings. The operating-system kernel and system shared-cache libraries remain explicit platform premises.

The separate inventory audit accepted this preparation at draft SHA-256 `a292139e9207fa359c5425e8bb08aad84e6e95bc09cab9f9a02f077b9fa60f22`. It checked all 190 direct bindings, 57 project pre/post-build identities, 1316 compiler dependencies across 23 translation units, 85 Python metadata bindings, 42 library inventories, the actual Python application executable, the static EOM library and 62 resource samples. The receipt is `build-runtime-inventory-independent-review-v1.json`, SHA-256 `f975976e0ab74513d7f60ffb9404dbef85d7a863e19d6e5f8263811d842c12b1`. Its independently approved process snapshot found all 54 recorded process IDs and three owned groups absent; the byte-identical durable snapshot is `build-runtime-inventory-review-process-snapshot-v1.txt` in the task directory.

Plainly: this verifies the recorded preparation and its closed processes. It does not establish a hermetic build, continuous peak memory, platform correctness or a phase-varying display representative outcome. External compiler dependencies were captured after the build; the reviewer audited the five recorded consumer tests without rerunning them.

## Historical preparation: invocation and output contract

The complete caller is [run-f5-ordinary-evolution.mjs](../../../../scripts/eom/run-f5-ordinary-evolution.mjs). It composes the existing request preparer, the EOM solver, the separate geometric reference and the frozen dynamics reference. Its intended invocation from the repository root is:

```sh
env -i PATH=/usr/bin:/bin LC_ALL=C LANG=C /opt/homebrew/Cellar/node/26.3.0/bin/node scripts/eom/run-f5-ordinary-evolution.mjs --declaration reference/priorities/braid-program/evidence/2026-08-27-f5-ordinary-evolution-declaration.v1.json --out .local-data/braid-analysis/f5-ordinary-evolution-20260827/evaluation-v1
```

Plainly: this command is deliberately blocked by the current draft's missing authorization and review. It is not an accepted evaluator handoff. The output directory must be fresh, and the caller must be admitted to inspect processes outside the filesystem sandbox before any scientific launch.

Expected outputs are exact request envelopes and wire hashes; parser inspection and endpoint-bit admission for every rung; raw EOM responses with complete generated cubic tokens and accepted-step diagnostics; per-rung exact-rational geometry; adjacent-rung common-time sensitivity; independent first-step/final-snapshot dynamics; per-stage process/resource receipts; and a final evidence index. The caller never grants scientific acceptance itself. An independent reviewer must also authenticate complete invocation termination, shared-lock release and the final output seal. The existing exclusive `.pilot.lock` cannot be bypassed by a benchmark consumer or by invoking exported stages separately.

After strength approval, freeze the exact request and invocation, horizon, numerical refinement ladder, outcome thresholds, stopping conditions and measured resource limits; then obtain independent declaration review before execution. The fixed starting-history uncertainty must remain distinct from step-size and arithmetic sensitivity. An exhausted numerical budget or an unresolved solver halt is not collapse, separation or bounded persistence.

The existing certified EOM route must generate its own future. Full generated cubic tokens, actual consumed initial state, accepted progress, error controls, halt evidence and outside process/resource observations must be available to the separate checker. Independent checks must name their mathematical reference; agreement between two executions of the EOM solver alone is numerical sensitivity evidence, not independent correctness evidence.

## Historical preauthorization restart boundary and owned paths

The scientific result is **not yet evaluated**: no phase-varying display representative request has gained execution authority and no phase-varying display representative positive-time history has been generated. The indispensable next decision is the operator's approval or rejection of the single recommended strength. Preparation, synthetic tests and source-review acceptance do not satisfy experimental closure. The original `2026-08-28T08:56:11-04:00` deadline remains in force; waiting for a decision does not restart that clock.

After an explicit approval, record the exact operator message, fill the three strength tokens without changing geometry or numerical settings, prepare and bind the exact requests, verify the actual parser inspection, and obtain a separate frozen-declaration review. Reauthenticate all inputs, source/runtime aliases and build identities, coordinate an exclusive host window, and admit the unchanged resource limits before executing the complete caller. Preserve its final stdout seal alongside the on-disk index; then independently review actual outputs and numerical sensitivity. If approval arrives too late, preserve a checkpoint at the original deadline without silently extending the campaign.

Plainly: the experiment is ready for the remaining authorization and declaration steps, but there is no physical or numerical phase-varying display representative evolution result to report yet.

Owned implementation changes are the existing EOM CLI and its process tests, the phase-varying display representative caller and its tests, the independent phase-varying display representative geometry checker and its tests, and the phase-varying display representative dynamics wrapper and its tests. Owned task evidence is this document, the draft declaration, and the ignored `.local-data/braid-analysis/f5-ordinary-evolution-20260827/` and `.local-data/braid-analysis/f5-dynamics-reference-20260828/` artifacts. Existing frozen oracle mathematics, shared readiness, shared work queue and other candidates remain outside the write set. No commit, push, unrelated regeneration or historical-evidence deletion was performed.

The preceding restart instructions describe the preserved preauthorization checkpoint. They are superseded operationally by the completed execution and checks above, without altering the old checkpoint files.

## Current owned paths and remaining proof burden

The task's repository write set is:

- [EOM CLI observability](../../../../src/eom/native/eom_borg_shadow_cli.cpp) and [process tests](../../../../tests/test_eom_borg_native_process.py).
- [Complete phase-varying display representative caller](../../../../scripts/eom/run-f5-ordinary-evolution.mjs) and [caller tests](../../../../tests/f5-ordinary-evolution-runner.test.js).
- [Independent geometry checker](../../../../scripts/eom/verify-f5-ordinary-evolution.py) and [geometry tests](../../../../tests/test_f5_ordinary_evolution_outcome.py).
- [Frozen-oracle dynamics wrapper](../../../../scripts/eom/check-f5-evolution-dynamics.py) and [wrapper tests](../../../../tests/test_f5_evolution_dynamics_checker.py).
- [Frozen declaration](2026-08-27-f5-ordinary-evolution-declaration.v1.json) and this evidence document.

Plainly: the EOM core mathematics and existing oracle mathematics were not changed. Each checker remained separate from its subject; independent source and actual-output reviews are identified above. Task artifacts also live in the two ignored task directories already named, with historical records retained.

The remaining scientific burden is to diagnose the trial-root certification limit and the independent final-root uncertainty without interpreting either as a physical event, then obtain a separately declared and authorized experiment if different numerical controls, starting-history accuracy or strength are proposed. The current result does not establish convergence through `0.5`, independent validity of transported EOM uncertainty, a global solution enclosure, equilibrium, stability, permanent retention, physical realization or a score increase. Source/input hash drift, a failed independent interval predicate, a mismatched pair census or a missing/unfinished declared stage would falsify the bounded evaluation claim and require its admission to be withdrawn.

Plainly: this experiment can answer whether its fixed tools and controls produced a checked outcome. It cannot supply the missing longer-time dynamics or turn an unresolved certificate into a physical verdict.

Closure goal: met by the independently checked bounded unresolved experiment; the unchanged evaluator, measured costs, evidence and remaining proof burden are delivered without extending the campaign.
