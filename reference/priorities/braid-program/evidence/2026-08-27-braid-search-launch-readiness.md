# Braid Search Launch Readiness

## Independently accepted actual original-parent-2 emission refinement

Original parent index `2`; original caller session `95033`; final completion chunk `01a7f4`; exit zero; fresh elapsed seconds `274.738`; owned processes closed; independent audit accepted.

Binding `plan`: SHA-256 `928dbe46bd133ad7bfc26b21e34368afabedcbf09b310066393d3b58588f7b0e`; bytes `51509`.
Binding `manifest`: SHA-256 `2de3a29480b7303556b284aaa3e043f224f551b767b8019b92e9af0795095a5c`; bytes `112678`.
Binding `comparison`: SHA-256 `4c1dc04563a6a19a8be3a986ed4e78b1163b3996fb4129bb66110d65e2cdfc7f`; bytes `127767`.
Binding `operation`: SHA-256 `88f3b862f4901b1f6bf179722e3dfb1bb655446b96de4cd0e681693b0724f169`; bytes `710516`.
Binding `launcher_log`: SHA-256 `6d81a9d10e89e06af777202d07bb50797aa40fd69653b1cc81649645e44021cf`; bytes `7673`.
Binding `resource_log`: SHA-256 `7137eacbb311120dc9783393b595840f54928fe248d9bfde15bcaf9376284e02`; bytes `538514`.
Binding `queries`: SHA-256 `3cb0ede0e0c0a985c558f57fc2a6f10b9ede2c026f236d8f9c377a92484198a9`; bytes `1950950`.
Binding `rows`: SHA-256 `d6723b665ed38199892114893df8e2363e6470e30dbb8c26f3628b07eed8f115`; bytes `161211`.
Binding `pieces`: SHA-256 `570d7b6d902f6d0059d9e85cb32e40d1ba87ea5445da852f179e10fa735ad6c8`; bytes `49002`.

Plainly: these nine exact artifacts and this fresh caller completion identify the accepted attempt. The earlier rejected `pilot-parent-2-v1` remains separate and unaccepted.

**Measured conditional conformance:** independent post-run audit `b0c5fa` passes 23,787 checks over 247 paths and 55,683,547 bytes. It checks original parent two, its unchanged history generation, all 3,584 query records, 64 pair rows, 112 piece records, the frozen independent comparison reference and geometry reference, and complete source/owner attribution. Final check `72f5da` confirms all 228 original prelaunch/ancestry source identities and hashes unchanged. It does not use reproduction against the rejected attempt as correctness evidence or rerun the producer as its oracle. All 56 emission intervals narrow strictly on both sides: their widths lie between `51514547/67108864000` and `224889021/134217728000` (approximately 0.000767626569 to 0.001675553776), compared with the original width `994/125` (7.952).

Plainly: the third original interval now has independently checked, much narrower causal emission windows. This step has not evaluated its acceleration or established a complete-history result.

**Measured execution and closure:** the 274.738-second caller envelope includes launch-tool approval/transport latency and ends at tool-confirmed process exit; the runner's earlier `271.058199583`-second field is not substituted for that observation. All 1,091 resource samples are retained, with maximum aggregate resident memory 693,780,480 bytes and maximum sample gap 717.2692079999833 milliseconds. Candidate evidence totals 2,273,841 bytes and operational logs 552,888 bytes. The original deadline, memory, disk, file/log and single-worker limits remain unchanged. Independent checks `bcc9b2`, `d27b75` and `5f39fc` confirm all 28 recorded process/group identifiers, all five owned groups and the shared lock absent. Main's separate `40535c` source/resource check passes 869 assertions. Complete public/private file aliases and final log hashes pass the independent audit.

Plainly: the successful attempt finished under its declared limits and left no owned work running. Its accepted grade is conditional emission refinement only: no acceleration, complete-history metric, stability, retention, score or physical-realization claim is promoted.

The exact consumed owner `07762d5c1b0478e7030e9846b3aab237c1ca6d28581090061ab04b5c5d10fd42` (350,973 bytes) remains byte-identical at `.local-data/braid-analysis/f6c-parent-adapter-20260827/readiness-owner.07762d5c.md`. The fresh caller observation is `.local-data/braid-analysis/f6c-parent-emission-refinement-20260827/pilot-parent-2-v2-caller-observation.json`. A later adapter invocation must explicitly bind the current acceptance owner and route the historical plan's owner through that exact archive.

Plainly: recording this acceptance does not silently replace the older document bytes consumed by the run.

## Independently accepted generic streamed caller and final-layout repair

The [streamed caller](../../../../scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs), SHA-256 `d6c79e6209a24cff41dc95626aad5f813dce1343b9a2e259beeb373739ee060f` (57,435 bytes), and [controls](../../../../tests/f6c-streamed-leaf-diagnostic.test.js), SHA-256 `08d31f5c2fdbe897e42e2953ed713d1b04a91a2b25107db47944df407a25777c` (43,038 bytes), are independently accepted for generic original-parent descriptors, explicit historical-source/owner attribution and durable publication. Review `0195cb` supplies 175 assertions and 115 rejection cases. Review `aa4b56` adds 26 assertions and eight final-layout rejections. The first output/private-directory names and identities remain fixed through cleanup and before/after final output; rejection retracts only owned public links while preserving private and foreign evidence. Returned generations `3da28d0f` and `4c439e29` remain archived byte-identically (`e3a150`, 92,435 bytes).

Plainly: the caller can admit the newly checked parent without confusing historical versions, and it rejects a renamed or replaced private-evidence layout even when public bytes still match.

The author's 51-control run closes in `66880/f8edea`; the separate independent rerun closes in `74659/5a2296`, exit zero, with all 51 controls passing in 47.600803958 seconds and watched duration 47.668917917 seconds. All 167 observed processes and 62 groups are absent afterward, without fallback cleanup. Sixteen source/control/dependency files and the embedded Python body remain unchanged; final source hashes pass `8db9a7`. Synthetic host inputs are confined to test fixtures and exercise the unchanged real admission logic, including rejection below the 40%/64-GiB launch and 20%/16-GiB running boundaries. Those tests are not evidence that the current host is ready for a numerical launch. Earlier harness, host-admission and deliberately interrupted test outcomes remain separate from the successful rerun.

Plainly: implementation checks are complete, but the next actual streamed invocation still needs its own fixed inputs, real resource admission, watched run and independent numerical audit.

## Operator pause and exact restart point

The operator requested a pause after these acceptances. No numerical, test or monitoring job remains running, and no new streamed invocation or evidence-package implementation has been launched. Both relocation tasks accepted the protected-file hold; their completed document moves are preserved.

1. On explicit resumption, freeze and independently admit a three-parent streamed invocation using accepted parents zero, one and two, the current acceptance owner, explicit historical-owner/source archives and the accepted caller above. Then perform the separately bounded calculation and independent mathematical audit. Parent-two acceleration and full-history metrics remain uncomputed.
2. Remove the demonstrated evidence-file capacity obstruction through an independently specified lossless indexed package of already accepted evidence, beginning with parents one and two. Each existing parent requires thirteen evidence/stage-log files; historical readiness-owner versions also consume slots. Preserve complete bytes, original logical bindings and accepted closures; package historical owner text explicitly while keeping current-owner approval and all executable/runtime sources separate. Failed parent-two v1 is ineligible.
3. Measure the complete physical-source union, package bytes, elapsed time and memory before claiming capacity. The 512-file/1-GiB adapter and 1,800-second/2-GiB/64-MiB attempt limits remain unchanged. Do not assume nine-or-ten-parent numerical batches fit: the measured one-parent producer cost alone does not support that inference. Full-history leaf-stream capacity, three-rung accuracy and measured parallel throughput remain separate obligations. phase-varying display representative interaction strength remains an operator decision and is not selected here.

Plainly: this pause preserves a verified third-interval refinement and a checked execution connection. The larger braid-search goal remains unfinished; nothing here restarts automatically from this handoff.

Status: phase-varying display representative's separate enclosed prescribed-root restart and all sixteen exact sub-field circular configuration ladders are independently accepted at scoped `H3 P[M]`, 2026-08-27. The declared sixteen-row, four-worker machine run and independent final integration are complete. asymmetric counter-breathing representative's full conditional root cover, first-cell coarse acceleration range and first-cell emission refinement are independently accepted at their separate diagnostic scopes. The separately authored refined-input producer, independent comparison, operational composition and exact plan are accepted; their actual one-cell run and independent post-run audits pass, with fresh completion reporting 3.384976666 seconds. The resulting residual bounds are substantially tighter but still include zero. The complete-partition integral/supremum definition, pure reference, shared quadrature protocol and correlated residual envelope are independently accepted; the explicit restricted-projection adapter, single-leaf composition and actual bounded execution are independently accepted. The unsplit restricted result is preserved as a valid but wider bound. The independently accepted first-parent bisection reduces local integral uncertainty by 32.98842% versus the better prior result and lowers its peak upper bound by 26.84505%. The request-driven bridge and its first four genuine requests are independently accepted, with 156 initial requests still pending. In that saved four-request run, broad emission evidence prevented useful precision on the remaining three parents. The generic parent-specific emission comparison and separate pure producer are independently accepted and frozen; source-bound preparation and its independent checker are also accepted, and each matches independently frozen expectations for the actual original parent-one inputs. The exact supervised invocation and actual parent-one refinement are independently accepted, with fresh whole-attempt completion in 261.94229158400003 seconds and all 56 emission intervals narrowed. The adapter's record-lifetime repair also passes independent review, and the pure lossless evidence codec is independently accepted; the explicit streamed leaf composition, actual parent-one adapter selection and durable storage helper are independently accepted, and their actual supervised two-refined-parent calculation now passes independent mathematical and operational review in 57.891806040999995 seconds. That fresh run leaves 158 initial requests pending, with no full-history aggregate. The EOM solver's missing adaptive checkpoint memory is repaired and independently checked at its named control scope. Bounded full-history evidence handling, precision, resource cost and three-rung agreement remain open. These asymmetric counter-breathing representative results do not promote `H3`, metrics, historical-trajectory identity or physical claims. This packet preserves the reviewed reference boundaries and remaining exact-source and BP-010 obligations; it creates no new hard gate or score. The score-free axial-transverse local-landscape packet was withdrawn after zero root evaluations and zero scores and requires a fresh exact-identity freeze before any new use. The operator authorized removing parallel-search blockers. Implementation batches preserve their independently reviewed reference bytes. No commit, push, legacy evidence overwrite, or unreviewed root execution is included.

## phase-varying display representative independent prerequisite batch

The approved geometry and the [accepted interpolation enclosure](2026-08-26-f5-independent-interpolation-enclosure.md) are unchanged. The new verifier checks the actual serialized cubic history, not a second copy of a history generated by the same adapter. The [restart declaration](2026-08-26-f5-enclosed-root-restart-predeclaration.md) still owns the subsequent `8/32/128` root ladder.

| Instrument | Independent review scope | Reviewed SHA-256 |
| --- | --- | --- |
| `scripts/eom/oracle/decimal_interval.py` | Exact negation/absolute value/inflation endpoints and rational-square exactness in the square-root shortcut | `fffc17270e149e6213315c1c82b518caa739657eb649822fd1955b8a2820e38a` |
| `scripts/eom/oracle/f5_actual_cubic_conformance.py` | Continuous actual-cubic error bounds, exact midpoint, trigonometric enclosures, and fourth-derivative jets | `4a90227cd79a4acfe319c723a05b711df1947953cc229f87114c4bc7babf6e09` |
| `src/prescribed-path-analysis/F5EnclosedRootLedgerReducer.mjs` | Raw-byte/source binding, complete ordered-pair census, exact history token chain, and all-rung consistency; ledger only | `c41857a81ab0ba4e1f9a4f53e6608f097dea83a99f4a0fa002f5ed9590004fb6` |
| `scripts/eom/reduce-f5-enclosed-root-ledger.mjs` | Original-byte ingestion and create-exclusive output; ledger only | `9c4d5730613597d7931b59c37d77c405bcf928de2421d55d3c76f78b7228a73a` |
| `scripts/eom/oracle/f5_history_manifest_conformance.py` | Exact full-manifest binding, captured-source execution equivalence, final deadline, and fail-closed continuous proof | `7441fa7069b960e4bbce86267dc33348f21078a9ae5635f8724999de1751738c` |

Plainly: these independently reviewed checks were frozen before the new adapter. Neither a well-formed ledger nor a correct proof routine alone demonstrates that an actual adapter emitted the approved paths. Both instruments explicitly withhold H3 authority on their own.

The wrapper `scripts/eom/oracle/f5_history_manifest_conformance.py` binds the five frozen phase-varying display representative sources, every approved member identity and polarity, all 1,032 segments per member, the exact decimal grid and error widths, and the original manifest SHA-256. It calls the continuous proof for all 12,384 member-segments, stops on the first failed enclosure, and records progress and a 1,800-second resource ceiling. Its separate binding review passes on CPython `3.13.2`: the helper and proof core execute captured source bytes, and the executing wrapper code must equal compilation of its captured bytes. This establishes executing-code equivalence, not historical source origin. The actual adapter manifest passed this nominal-domain proof before the separately recorded root ladder ran.

### Actual phase-varying display representative adapter preparation

**Measured:** the new `eom_f5_enclosed_root_cli` adapter completed a fresh two-worker Release build in `16.358` seconds, emitted its actual 12-member history manifest in `4.295` seconds, and passed the unchanged independent nominal-domain proof for all `12,384` member-segments in `172.339` seconds. The supervised preparation ended successfully with no failure or resource contact. Its original write-once artifacts are in `.local-data/braid-analysis/2026-08-26-f5-enclosed-root-restart/prepared-20260827-v1/`.

| Artifact | SHA-256 |
| --- | --- |
| Frozen adapter source `src/eom/native/eom_f5_enclosed_root_cli.cpp` | `9f7661f4000174d631d4c60f7078e124d77ae9b2ddba6af36197f13096095f81` |
| Built adapter executable | `d4579244707376239ee448e051698abfd7045d9f49de912f6a7b92802e272455` |
| Built EOM library | `922c131381bb514d48fe0fb2bb704db340b51c64db024edf6cfec3ec77b59bcf` |
| `history-manifest.json` | `5c665fcd7eee92a105fd958929ee443e4eeaea6afc0222935739aad2622a1725` |
| `nominal-history-conformance.json` | `f862a7148a0a00b3bde5fbb0d164156fce2dbfc161597b0cdaa182457f3741e0` |

Plainly: the independently checked approximation is now the actual history emitted by the new adapter, not a synthetic example. This verifies the nominal continuous-history representation only; it is not a root-search or dynamics result.

A separate read-only adapter review found no source defect requiring a change. It checked all `247,680` serialized manifest tokens, independently recomputed the history fingerprints, confirmed source order and polarity, and inspected reconstruction, tangent, request controls, and first-failure dispatch behavior. Before the accepted root run, the separate API-domain supplement checked the binary64 endpoint overhangs and parsed-radius reserve, and the reviewed supervisor enforced independent per-rung checks, the fixed `8/32/128` order, measured cost projections, and external termination. The resolved Xcode compiler identity accompanies the dispatcher/build record. Nearest rounding and gradual underflow remain explicit runtime premises, not measured controls; no unsafe fast-math flags or floating-environment mutations were found.

**Measured API-domain supplement:** `scripts/eom/oracle/f5_api_domain_conformance.py`, SHA-256 `bdb9e449892e7710237d9559bc3937d2259d45e17ec5862ab1f32cee3cb787f8`, passed separate read-only mathematical review and then checked all `12,384` actual segments successfully. Its original receipt `api-domain-conformance.json` in the preparation directory has SHA-256 `440deb996eaeb646b7863e9276fb937f9897c11fdbd56fed11a32efb269fe746`. The outer supervised process completed in `182.660` seconds without resource contact and with its process group closed. The check includes both analytic constant interpretations, exact parsed error radii, endpoint overhangs, and the two-operation velocity reserve. Eighteen local algebra and rejection controls pass; those controls are distinct from this full actual-history result.

Plainly: both continuous-history checks pass for the exact stored phase-varying display representative paths, including the interval endpoints actually used by the solver. They became prerequisites of the subsequently accepted root ladder; neither history check alone licenses H3, retention, stability, or a physical claim.

### Derived continuous-error argument

For one coordinate, let $f$ be the analytic phase-varying display representative path, $Hf$ its exact endpoint-position-and-tangent cubic Hermite interpolant on $[a,b]$, and $q$ the actual serialized cubic. Write $h=b-a$, $e_0=q(a)-f(a)$, $e_1=q(b)-f(b)$, $d_0=q'(a)-f'(a)$, and $d_1=q'(b)-f'(b)$. The Bernstein control values of $q-Hf$ are

$$
e_0,\quad e_0+\frac h3d_0,\quad e_1-\frac h3d_1,\quad e_1,
$$

and those of its derivative are

$$
d_0,\quad \frac3h(e_1-e_0)-d_0-d_1,\quad d_1.
$$

Plainly: the largest enclosed absolute control value bounds the entire cubic error, including between endpoints. Matching endpoint positions alone cannot hide a wrong tangent or curvature.

If $|f^{(4)}|\le M_4$, the Hermite position remainder is bounded by $M_4h^4/384$. For the derivative error, Rolle's theorem supplies an interior zero in addition to the two endpoint zeros; quadratic interpolation of that derivative gives $M_4h^3/24$ as a conservative bound. The implemented divisors `300` and `8` are weaker and therefore conservative. The verifier adds the endpoint-defect bounds to these remainders and requires the total to fit the unchanged accepted widths. Refinement subdivides only the derivative proof domain; the remainder always uses the full original segment width.

Plainly: the proof accounts for the actual stored coefficients and the analytic curve between stored points. It does not assume that the adapter constructed an ideal interpolant.

The verifier separately encloses the exact source-decimal interpretation with mathematical $2\pi/3$ and the frozen binary64 constant interpretation used by the earlier instrument. A time-constant hull covers both in the derivative proof. Fresh derivative bounds use the exact serialized decimal endpoints, including the tiny decimal/binary64 endpoint differences. The frozen `0.286965499706333` derivative ceiling and the two accepted error widths are not increased.

**Measured controls:** the decimal primitive suite passes 5 tests; the actual-cubic suite passes 8 tests; the manifest wrapper suite passes 14 tests; the ledger suite passes 20 tests. Independent review additionally checked 27 rational square-root cases and 37 exact-rational trigonometric/jet cases. The existing root-oracle and acceleration-oracle suites pass 23 and 16 tests after the primitive repair. These tests establish their named local controls only. The oracle-generated Hermite example and mocked full-manifest traversal are explicitly plumbing tests, not independent adapter evidence.

Plainly: the tested mathematics is ready for an actual subject, but test totals do not upgrade the old zero-width pilot or establish physical validity.

### phase-varying display representative preservation and falsifiers

The independently reviewed prerequisite batch is closed at the hashes above. The separately authored adapter and supervised coordinator bound source, compiler, executable, EOM library, root API, raw history manifest, continuous-conformance certificates, and ledger receipts without modifying those reference instruments. The [enclosed-root closure receipt](2026-08-27-f5-enclosed-root-closure.md) now records all 24,192 accepted pair certificates and independent final review as scoped `H3 P[M]`. Preserve that generation. If a reference needs repair, stop affected acceptance and return to a separately reviewed prerequisite batch; do not change both sides to obtain agreement. Any changed source hash, incomplete coverage, actual-cubic escape, resource contact, wrong member identity, incomplete root certificate, nonpositive causal factor, or inconsistent repeated phase blocks acceptance. A build receipt remains necessary because a matching byte hash alone does not establish the role of an arbitrary binary.

The manifest verifier runs as a module with `--history-manifest` and a new `--out` path. The ledger CLI additionally requires that same raw manifest and the three rung files. The verifier's certificate cannot authorize H3 independently of the ledger and reviewed build receipt. Ordinary EOM evolution remains a later, separately declared step.

### Strength-Independent phase-varying display representative Evolution Prerequisite

**Read-only interface finding:** the accepted phase-varying display representative root-history manifest covers `[-1,19.63359163663986]`, while the ordinary-evolution handoff already fixes release at `0` and permits prescribed input only on `[-1,0]`. Segment 50 of each member crosses release, from `-0.0003104827209370331` to `0.019683307624644097`. The current `CoupledEvolution.cpp` request validation requires every supplied history to end exactly at the evolution start. The full root-history manifest is therefore not an admissible ordinary-evolution input. A separate prehistory-only artifact must retain the original first 51 segments per member, preserve their coefficients, errors, identities and origins, and restrict only the last endpoint to exact zero. The original full manifest and its accepted proofs remain unchanged.

Plainly: the root check legitimately used prescribed future motion. The next evolution must generate its own future, so its input needs a separately checked cut at release. Preparing that cut does not require an interaction-strength choice or a geometry change.

The independent restriction checker was accepted and frozen before the separate producer: [verify-f5-prehistory-restriction.py](../../../../scripts/eom/verify-f5-prehistory-restriction.py), SHA-256 `0298ba29b59d4477e8bfa2abcca135cae337bdbbe00bec9b7ae488346143f1c8`, with its 24-control test generation at SHA-256 `0f2c3b64d93b5a79831224849eb75b4696630c8ae8d4fb568896dca2ebe8bd7e`. Independent review checked 306 additional exact arithmetic cases and the closed twelve-member, 51-segment contract. A separately found final-cleanup timing defect was repaired and independently rechecked: success follows bound-input closure and watchdog teardown, with both overrun controls withholding success.

Plainly: the checking rule was fixed and reviewed before the data-producing step. Its timing controls prevent an incomplete attempt from being admitted as a completed check.

Its mathematical obligation is exact subset inheritance from the original nominal and API-domain conformance, including endpoint overhangs used only for outward enclosure evaluation. It must not relabel the original full-history fingerprint as the restricted history's identity, supply positive-time history as evolution input, or claim that a future EOM parser, build, release-state conversion, or request has already passed. This is a data prerequisite, not an evolution authorization.

Plainly: keeping the same polynomials on a shorter interval preserves an already proved enclosure, but only after the exact domains and unchanged data are checked. The future evolution request still has its own validation work.

**Measured data-only prerequisite closure:** the separately authored [prehistory producer](../../../../scripts/eom/prepare-f5-prehistory-restriction.mjs), SHA-256 `c3f15a1bb82cc345b1dac82174531ae89ed1ea03be0bebacbb9d755f044e19f9`, passed independent review and four focused controls before creating `.local-data/braid-analysis/f5-prehistory-restriction-20260827.XX7oGR/prehistory.json`. The 871,895-byte prefix has SHA-256 `8d14aa3bc5e0788f06c8b79e788a55df82e8db83736e2413c9800a78af63111b`. The fresh frozen verifier exited successfully in `0.5205029170028865` seconds through checked finalization; its 334,797-byte `conformance.json` in the same directory has SHA-256 `5a2e9158bf26c34a7a9755e53ea1337cc006765727d9afe1ef1304c3fcd140b0`. The main admission checked the completion hash, output identity, receipt census, and authority flags.

Plainly: phase-varying display representative now has a checked starting history containing only the past. This preparation does not generate its future or choose how strongly its members interact.

A separate read-only actual-data audit imported neither producer nor verifier. It compared all 612 pieces, including 7,344 coefficient strings, 3,672 error strings, 612 starting-time strings, 600 unchanged ending-time strings, and precisely twelve ending-time changes to `"0"`. It independently reconstructed all 612 nominal and 612 API domain inclusions, authenticated 612 original proof pairs, and matched 72 exact release position/derivative values and 144 analytic-enclosure endpoints. Its exact release-comparison chain is `1eb84eccaf27613aa7df6aaafc639556db36d2400d04a06bb7d93301335b88d5`; nine bound files totaling 94,020,428 bytes were authenticated before and after the audit. The original full manifest and conformance evidence remain unchanged. The positive-subnormal API endpoint above zero is an outward evaluation overhang only, not supplied future history. Current restricted-history fingerprint, EOM consumer/build, converted initial request, coupling, evolution, roots, and new H3 authority remain unestablished by this receipt.

Plainly: the independent audit confirms the cut is exact and preserves the earlier containment proof. The next evolution still needs its own properly constructed request and explicit scientific and numerical settings.

**Independently accepted handoff reference:** [verify-f5-prehistory-handoff.py](../../../../scripts/eom/verify-f5-prehistory-handoff.py), SHA-256 `6c94b0ca16dfe20bed4841a547adca349f2f36cdd5ec04211341d6b060032a68`, and its [36-control test generation](../../../../tests/test_f5_prehistory_handoff.py), SHA-256 `111e828c8ea3c26996ce51c83496ff7850d48b52cf7e874982c67e882ad6cadf`, are frozen before the separate producer. The checker binds the accepted prefix and restriction receipt, preserves every original token and polynomial origin, independently reconstructs binary64 conversions and fresh restricted-history fingerprints, and checks both raw final-piece and actual correlated endpoint-state boxes against both inherited analytic interpretations. The actual EOM handoff uses the retained history's endpoint-state operation, not an independent nominal-position override.

Plainly: this check verifies exactly what the EOM solver receives at release, including its uncertainty. Merely supplying the last polynomial's midpoint would not meet that obligation.

Independent review reran all 36 controls in `2.467` seconds and additionally checked 5,000 rounding cases, 300 nonconstant cubic cases with 7,200 release-bound bits, subnormal cases, five publication/capture controls and a nested executing-code mutation. A signed-zero limitation was resolved with a narrow fail-closed guard: reconstructed release-box endpoints must be nonzero before exact bit equality is admitted; zero endpoints remain unresolved. The permitted arithmetic cannot propagate a discarded intermediate zero sign into a different nonzero endpoint. Original signed-zero decimal-token parsing is preserved. Late failure resets all acceptance and conformance flags. These are reference controls, not an actual phase-varying display representative handoff result; producer/build origin and fresh execution remain separately authenticated obligations, and no coupling, request, evolution or metric is established.

Plainly: the edge-case guard rejects unsupported bit claims without changing the geometry or enlarging an uncertainty allowance. The separate producer, build and actual-data result are recorded below.

**Derived parameter reduction:** for the equal-magnitude signed members, write $q_i=\sigma_iq_0$ with the fixed polarity sign $\sigma_i$. The actual acceleration paths contain the product $\kappa q_iq_j=\sigma_i\sigma_j\kappa q_0^2$, so a common charge-unit convention and one effective interaction-strength choice suffice for the ordinary trajectory. The operator need not choose two independent scientific inputs. Actual charge and coupling tokens must nevertheless remain separately recorded because their factorization is part of provenance and need not give bit-identical interval rounding. Normalized $c_f=1$ does not select that strength. The already requested phase-varying display representative strength or bounded-strength-search decision remains pending; numerical controls and stopping rules belong in a fresh technical predeclaration.

Plainly: the unresolved scientific knob is how strongly the fixed signed members interact. The units, geometry, release time and canonical measurement ruler do not determine it, and this audit chooses no value.

**Independently reviewed producer source:** the separate History inspector has SHA-256 `b9aeb71f6ca48d77e6b22e2ba06b0adb91884b4569399d4c6fc1acd642298b36`, the producer wrapper `4c9a5d724cb4d0e24fa35dd3cefed661448d0ff69077171f9d6adc869f8a079c`, and its 13-control plumbing suite `ec1d99b6919fd2c666dd1dd157388f9577f054070744db3e38a4cc4b56062770`. Independent read-only review inspected all three sources and reran all 13 synthetic controls in 0.665 seconds. The inspector reads original tokens through a bounded protocol and reports the actual History API's parsed bits, fingerprints and release boxes; its rational polynomial values remain subject output. The wrapper binds both producer sources and the external build receipt, supervises its owned child, preserves exclusive outputs, and grants no mathematical or execution authority. The separately authenticated compiled build and actual handoff are recorded below.

Plainly: the source review, compiled result and real handoff have separate evidence identities. The independent mathematical checker remains unchanged.

**Independently accepted response predeclaration:** the [single-reception acceleration-response plan](2026-08-27-prescribed-acceleration-response-predeclaration.md), SHA-256 `c08d7f53616fc2843b3a192f7e3c10229f9a9fe7abc1a8670ddb1706d95756ef`, specifies phase-varying display representative at reception zero only: 144 original pair certificates, 132 ordinary contributions and 12 self exclusions. Independent review checked the factorization against both frozen sharp-kernel owners and the complete-piece state interface, reauthenticated all five frozen import hashes, and checked the stationary, axial and non-axial closed-form controls. The response coefficient holds the prescribed paths fixed and leaves effective strength symbolic. The reference, separate consumer reviews and accepted actual response are recorded below.

Plainly: the saved roots can support a useful instantaneous geometry calculation without choosing the pending interaction strength. This does not authorize evolution or imply that evolved paths scale linearly with that strength.

**Independently accepted response arithmetic reference:** [prescribed_acceleration_response.py](../../../../scripts/eom/oracle/prescribed_acceleration_response.py), SHA-256 `e630c2f4c48c9fcfc56866166e8b5977d70ab83c6ca3f2b08ad9ea4f3f5e910c`, and its [31-control suite](../../../../tests/test_prescribed_acceleration_response.py), SHA-256 `4b0e66feb308544aa6294b126a05f6a3c9fbb403580e8193d3140a7b52c370f1`, are frozen before the actual-data consumer. Independent review read the complete implementation and controls, reran all 31 in 0.328 seconds, and separately checked 600 binary64 round trips and 216 exact response-component inclusions across 72 affine controls. Those controls construct a known emission distance, unit direction and transmitter velocity first, then derive the reception position and exact response independently. They do not use actual phase-varying display representative data or search for roots.

Plainly: the response calculation now passes separately constructed known-answer checks, including sideways motion and different receiver velocities. This establishes the bounded arithmetic instrument, not a phase-varying display representative measurement.

The reference retains the original full-history token chain and fingerprints, requires the exact phase-varying display representative census and allowance tokens, lifts printed binary64 endpoints before interval use, includes every touched closed nominal piece, intersects independently valid distance and factor enclosures, and sums signed contributions in fixed order. Its immutable result stays conditional with `accepted:false`; complete original-certificate authentication, executing-code binding, bounded publication and fresh process completion belong to the separate consumer. All five prior reference hashes remain unchanged. The accepted actual response below supplies those separate obligations without selecting the ordinary-evolution strength.

Plainly: the saved evidence was connected to this unchanged calculation in a separate attempt. Its interval bounds, not just their midpoints, carry the result; no trajectory or score is authorized.

**Independently accepted response consumer:** [reduce-prescribed-acceleration-response.py](../../../../scripts/eom/reduce-prescribed-acceleration-response.py), SHA-256 `af7884573f834994dd18803e345d052de7f09d7ca87b543eea22214e6ef8d02f`, and its [25-control suite](../../../../tests/test_prescribed_acceleration_response_consumer.py), SHA-256 `2d97ad74c5a4ad1c33bf587ea4050cf4f028179e55ba1c817404714346cdb6d1`, passed independent source and mapping review. The reviewer reran 25 controls and 155 additional synthetic checks. Its mapping-only audit authenticated all eight fixed original inputs totaling 119,274,679 bytes, preserved all 12,384 segments and 247,680 numeric literals, selected exactly 144 reception-zero pairs, and independently checked 792 saved binary endpoints. It called neither the response evaluator nor a history-state or root kernel. Main review also read both complete sources and reran all 25 controls, most recently in 1.554 seconds.

Plainly: the saved scientific evidence now maps correctly into the separately checked response calculation. No actual phase-varying display representative response value has been calculated by this mapping audit.

The final source differs from the reviewed `e208c98e…` generation only in two documentation passages and one error label. Independent reversal recovered that exact earlier hash; all mathematical reference and test hashes remain unchanged. The clarification separates the final record's embedded, measured **completed-compute-stage** snapshot from the external watcher's later whole-attempt admission. Embedded publication completion refers to the private candidate already durably written; final payload length is derived and subsequently checked against the published bytes. The separate watcher must explicitly name that scope and independently observe final assembly, publication, source rechecks and all process closure within the original 1,800-second, memory and output limits. No predicted final time or predicted successful closure is admissible. The reviewed external watcher and actual response below satisfy these separate operational obligations.

Plainly: the output cannot claim that its own future write has already finished. A separate observer must verify final success after the calculation and publication processes close.

**Independently accepted publication-only bridge:** [publish-prescribed-acceleration-response.py](../../../../scripts/eom/publish-prescribed-acceleration-response.py), SHA-256 `4d67564c4ddc56ac616c334aeb43c2005028727bc97423d2ce35545497c30556`, and its [15-control suite](../../../../tests/test_prescribed_acceleration_response_publication.py), SHA-256 `c5805819a7e54f68b4ad63757752afd48efba8c2d01f7caed7ace3b048bb7dd1`, passed separate read-only review. The reviewer reran all 15 controls in 0.869 seconds and six independently constructed publication/failure controls in 0.112 seconds: exact published bytes, late private fsync, late directory fsync, interruption after linking, preservation of a replaced public inode, and failure after the publication helper returns. Main controls passed in 0.695 seconds. No actual response, history evaluation or root calculation occurred.

Plainly: the final result can be assembled from an already closed calculation without rerunning its mathematics, and publication failures are tested separately.

The bridge executes the captured frozen consumer, authenticates the closed publication job and fixed original bindings, derives only the final serialization byte count, and calls the unchanged pure assembly and exclusive-publication operations. Its completion remains `accepted:false` with external whole-attempt admission explicitly required. A failure after the helper returns can leave an `accepted:true` payload on disk; that payload is non-admissible without fresh successful publisher completion and exit, the external final rehash and complete whole-attempt resource/closure admission. The registered outer watcher enforces those conditions in the accepted actual attempt below.

**Independently accepted response operational entry:** [run-prescribed-response-pilot.mjs](../../../../scripts/eom/run-prescribed-response-pilot.mjs), SHA-256 `54101bd3c124d028b8d3843d0c6284e909fa74db194f610c868e96ef4f49d121`, and its 24-control suite at `e3de479240d67e9f70edd9b32c0948ed264cb11aa10b5157a87f60557d6e48a9`, passed independent full-source review. Another 4,006 independently constructed metadata cases checked exact nested values, duplicate and escaped-duplicate keys, malformed inputs and unsafe numbers. The exact 19 original bindings, registered stage arguments and completed-compute-to-publication relationships remain closed; the entry performs no response mathematics. Its metadata inventory is externally augmented with `import __future__` before launch without modifying this frozen source.

Plainly: the adapter preserves the exact approved inputs and refuses incomplete or misleading handoffs. These are software controls, not measured phase-varying display representative response values.

**Independently accepted response supervisor:** [launch-prescribed-response-pilot.mjs](../../../../scripts/eom/launch-prescribed-response-pilot.mjs), SHA-256 `a327d1ed9d3d6a4017f41ecc4d67eafc5d03abfe4ac60a0844c2624ced8be1f9`, passed full main source review and all 48 combined entry, launcher and process controls in 5.54129475 seconds. Launcher and process controls have SHA-256 `ba6536d5096888a67948a926d49f07ce0fbfa886c11b0ab4a1c447a9271587c8` and `5bf59bdb7c0ac2b266d7a22afb528cd70f77a9623c654323491bd481dcf052d5`. Eight controls use synthetic registered processes, including failed startup, lost monitoring, wrong stdout binding and slow final publication. The final output callback is awaited; a failed or stalled owned stream is destroyed. The embedded response record describes only the already closed compute stage. The separate supervisor receipt and fresh successful CLI closure must authenticate the complete compute-and-publication attempt within 1,800 seconds, aggregate sampled RSS below 2 GiB with gaps at most one second, and the fixed host and log limits. No phase-varying display representative response or EOM evolution was run by these controls.

Plainly: the computation and publication have separate checked exits, while one outside supervisor measures the whole attempt. Neither an intermediate passing file nor a successful software test can choose the physical interaction strength.

The [exact phase-varying display representative response launch plan](2026-08-27-f5-prescribed-response-pilot-launch.v1.json), SHA-256 `f5b53755ca555336dbb39b88936274afbf40ce75da66730f6b8e9dee428a57ce`, has 48,074 bytes and passed separate metadata/binding review. The reviewer authenticated 185 distinct files totaling 125,263,635 bytes twice, including the eight scientific originals totaling 119,274,679 bytes without parsing them. It fixes 19 ordered original bindings, 11 operational bindings and 155 runtime bindings; the latter comprise 153 independently enumerated metadata-loaded files plus the shared-venv configuration and resolved Git. Main checked both absent intended outputs against ignored, canonical paths using that bound Git executable and confirmed the shared asymmetric counter-breathing representative pilot lock was absent. The new data directory is `prescribed-response-f5-release-20260827-v1` under `.local-data/braid-analysis`, with a separate `-outer` operational sibling. No new roots, strength choice or ordinary evolution are included.

Plainly: the exact source and runtime were fixed before the one release-time response calculation below. Its result leaves the interaction strength symbolic and cannot establish sustained motion.

### Accepted actual phase-varying display representative release-time response

**Measured:** the single declared attempt completed compute, publication and external process closure successfully in `16.6266205` seconds. The fresh original launcher completion and exit zero were observed separately from the immutable prepublication receipt. The final `response.json` under `.local-data/braid-analysis/prescribed-response-f5-release-20260827-v1/` contains all 132 ordinary contributions and twelve summed response vectors at reception zero, using the original 144 pair certificates and complete prescribed histories. It performed no new root search. The fixed effective strength remains symbolic: the acceleration is $\mathbf A_i=K\mathbf G_i$, with $K=\kappa q_0^2$ and the polarity signs included in $\mathbf G_i$.

Plainly: the existing checked roots now yield a bounded instantaneous acceleration coefficient for each member. This is not a generated future, and it does not choose the interaction strength or show that the assembly persists.

| Actual artifact | Bytes | SHA-256 |
| --- | --- | --- |
| Final `response.json` | 1,297,615 | `96b6068e92b9c58e44a87a8f1496a89f9103ed1da43685be38d5c8d23ef45e78` |
| Private compute candidate | 1,297,089 | `c9f7459cfa336238a6ed5ece8efbb6aed81bad43078b96e09d387d6a751ed1aa` |
| Operational sibling `publisher-job.json` | 6,009 | `08e0eb5fd45da98ac6a846109b2ce0251bc2b0246413104a9593f71456be02a9` |
| Operational sibling `pilot-admission.json` | 332,496 | `e98cd0fb8749aefc6f7d0be8e3c05786148bac1a2d58b8ec16d4e90ff2cd2512` |
| Operational sibling `resource-observations.ndjson` | 34,717 | `694b6c9dd8e46279f727b61339fe63d6b7a1ccaf34378a66d31cf83fa85badd0` |

Plainly: these exact saved files distinguish the completed computation, its publication and the outside observation. A passing-looking payload without the successful external closure would not be admissible.

The operational sibling is `.local-data/braid-analysis/prescribed-response-f5-release-20260827-v1-outer/`. The compute completion reported `13.79582850006409` seconds; its externally closed-stage snapshot records `14.751727166999999` seconds. Publication reported `0.704985624877736` seconds. The whole attempt recorded 78 aggregate-memory observations, maximum sampled RSS `840,482,816` bytes and maximum gap `292.246708` milliseconds, within the unchanged 1,800-second, 2-GiB and one-second limits. Both stages exited zero; workers and monitors closed and the shared lock was released. The immutable prepublication receipt stops at 76 observations and `16.4063645` seconds; it does not claim to have observed its own later write. The final time and observations come from the fresh external completion.

Plainly: the complete attempt was actually watched through its end. The shorter embedded times describe earlier stages and must not be substituted for final process closure.

**Independent actual-data audit:** a separate read-only calculation imported neither consumer nor response reference and reran no root or history kernel. It authenticated 185 source/runtime/plan bindings, all 24,768 inherited segment-proof records, twelve original history fingerprints and mapped history hashes, all 144 pair records, 792 saved endpoint tokens and bits, and 264 closed-piece mappings. Exact rational/integer arithmetic independently checked all 72 directed 90-digit sum endpoints and 3,012 interval widths. All 36 final component intervals are finite and exclude zero; their widths range approximately from `1.2644931917515526e-5` to `5.4642835425362884e-5`. These widths are measured properties of this calculation, not a newly chosen acceptance threshold. The frozen reference's separately derived known-answer controls remain the mathematical reference; summing the output contributions independently does not by itself re-prove the kernel.

Plainly: the independent audit checked the actual saved inputs, sums and uncertainty widths. The result is a sign-resolved instantaneous response, not evidence of equilibrium, retention or stability.

The audit also matched the fresh completion to the stage records and original logs, independently summed all 78 memory observations, and checked that the seven recorded launcher/runner/gate/target processes and shared lock were absent afterward. The conditional reference-result hash `9b63314ad7d637b20ab7a2df1f08b0647ffa9b19984e861db7d7a5bd00e46bd7` is unchanged between private computation and final publication. All nine exclusion flags remain false: physical strength chosen, EOM executed, evolution authorized, braid metrics computed, score authorized, retention established, H3 evidence eligible, premise truth authenticated and analytic trajectory identity established. A changed original binding, omitted contribution or piece, non-enclosing sum, failed final completion or resource contact overturns this acceptance. Ordinary evolution still requires the pending interaction-strength choice and its own numerical and execution declaration.

Plainly: the release-time calculation is complete. The next dynamical question remains how the EOM solver generates the future from the checked past, at an explicitly chosen strength.

**Independently accepted fresh handoff build:** the [reviewed build plan](2026-08-27-f5-prehistory-handoff-build-plan.md), SHA-256 `772b45f7ecb2f2318e2e794175c3464d213068b02c1c84205b934aaf9c82dd98`, produced one fresh build under `.local-data/braid-analysis/f5-prehistory-handoff-build-20260827/recorded-v1/`. The 4,501,676-byte `preparation.json` has SHA-256 `731e23f08b9096b435efb90606f6d992670543d67836c2bf2e8b01cf2c4137c7`; the sibling `recorded-v1-outer/outer-admission.json` is 74,879 bytes with SHA-256 `ef42cfeddbc40e3841c1d2b90b508c948e22d59420fcc4068eefa44bfbbf6816`. Fresh outer completion and exit zero were observed in `35.224980083999995` seconds, including publication. All five resource observations passed; free memory was 52–53 percent. No phase-varying display representative data, roots or evolution were run.

Plainly: the actual inspector executable is now rebuilt and independently tied to its reviewed sources. This is a build result, not yet a history-conformance result.

Independent read-only review rehashed 1,436 unique files totaling 179,712,595 bytes in 2.571 seconds. All 54 source records, ten tools and 1,321 unique compiler dependency files were unchanged across the build; every one of the 23 translation-unit dependency sets matched its actual compiler output. All 22 archive object payloads matched the separately compiled object files. The 41 stages matched 41 registered target gates, all closed with exit zero and without stop flags; their recorded process identities were absent afterward. Raw executable load commands matched all nine runtime-dependency records, including the explicitly platform-trusted shared-cache boundary. The compiled inspector is 428,408 bytes, SHA-256 `8bee4717038b5475b856d7167ddd651e98a63e0e87f9c0cd176b752b9c546a99`. The independent audit authenticated the stored inner completion and first four resource observations; the outer task's fresh completion supplied the final elapsed time and fifth observation.

Plainly: the audit checked actual compiled inputs and process closure, not just a repeated source hash. macOS shared-cache libraries remain an explicit platform premise; no stronger runtime or mathematical claim is implied.

**Preserved single-attempt handoff declaration:** use the unchanged producer and compiled inspector above with the accepted `8d14aa3b…` past-only prefix and `5a2e9158…` restriction receipt. Produce into the new direct child `.local-data/braid-analysis/f5-prehistory-handoff-20260827-v1/`; no overwrite, resume or automatic retry. The shared-venv Python identity is `42f792544842512d02eb8a95a6009062591476558f09f07553c138661a0bca8e`. Each producer/checker command is externally watched through complete child-group closure with a 1,800-second inclusive limit and 15-second heartbeat. Authenticate the producer's fresh completion, exact handoff/execution hashes and unchanged wrapper, inspector, build, runtime and prerequisite bindings before invoking the unchanged independent checker. The checker receives the C++ inspector as `producer-source` and the inner `preparation.json` as `build-receipt`. Its final output must be rehashed after successful closure before conformance is accepted. No coupling, charge magnitude, future history or evolution request is introduced.

Plainly: this attempt transports only the approved past and checks what the EOM history interface actually receives. It remains separate from choosing interaction strength or running dynamics.

**Measured actual handoff conformance:** the single declared producer attempt completed successfully and produced the 908,879-byte `handoff.json` with SHA-256 `4e0696a848a0d36ccbe5948295e71738c933b7ea120e9aee00e2effdd6ecc149`. The unchanged independent checker then passed on those exact bytes; its 8,854-byte `handoff-conformance.json` has SHA-256 `5b7ddc53d47a97f81383bd043c37e93daa80fe5a9b595d400faddd4c9724b622`. It checked 12 members, 612 segments, 12,240 binary64-token comparisons, 3,600 exact join comparisons, 72 release scalars and 288 analytic-interpretation box comparisons. Both `source-decimal` and `frozen-binary64` inherited release enclosures pass, with no failures. The new restricted-history fingerprints are preserved in the conformance receipt; they do not replace the original full-history root identities.

Plainly: the actual EOM history interface receives the intended past, with the checked release-state uncertainty. No prescribed future is passed into a future evolution request.

The external execution records are under `.local-data/braid-analysis/f5-prehistory-handoff-watch-20260827-WDaN0I/`. `producer-operation.json` has SHA-256 `a01d00410184672ae53f83225f968b1bff8484dd8056b24a344417bb36a86447`, and `checker-operation.json` has SHA-256 `955f28657ff011a207968d124ad180413e033fbf3c5b07c85f7abed9e89e65ce`. Both original commands closed with exit zero, no timeout or interruption, and no remaining descendants. Measured watched times were `0.381105458` and `0.5456381250000001` seconds; complete external admissions including final hash checks took `0.42507325` and `0.5773013339999999` seconds respectively. Bound producer, reference, executable, build, runtime and input hashes matched before and after; main admission rehashed the final checker output after closure. The immutable checker deliberately leaves producer-origin and producer-execution authentication to the external caller; its internal conditional analysis retains `accepted:false`. The separate build and actual execution records supply those external obligations, not the mathematical check alone.

Plainly: the passing data check and the observed successful execution are separate facts tied to the same files. Neither a receipt copied from elsewhere nor a still-running process would satisfy this admission.

**Independent actual-data audit:** without importing or rerunning the producer or checker, a separate standard-library calculation recomputed all 12,240 parsed token bits, twelve restricted FNV fingerprints, 3,600 exact join comparisons, 72 nominal release scalars, 144 actual API boxes and their 288 endpoint bits, and 288 inherited interpretation/box inclusions. Every comparison passed, with strictly positive inherited containment margins. The original transport protocol, raw inspector output and twelve inspector events match the handoff. The audit rehashed 26 distinct bound files totaling 10,005,188 bytes; all nineteen before/after external bindings and twelve checker bindings match the accepted build and executable. Both original logs match the fresh completion objects and recorded successful, closed process groups. This audit does not treat their reproduced text as a new process execution.

Plainly: the second audit independently reconstructed the actual bits, history identities and release bounds from the saved originals. It confirms the handoff rather than merely replaying its checker.

This closes the actual past-only data handoff obligation only. It introduces no coupling, charge-magnitude choice, validated evolution request, ordinary release, new root result, metric, score, retention or physical claim. Changed original tokens, fingerprints, release-box bits, inherited containment, executable/build bindings or actual completion would overturn the corresponding result.

Plainly: data transport is no longer the blocker. The ordinary-evolution scientific settings and its independently declared numerical controls remain separate.

### Independently Accepted Non-Executing Evolution Request Preparer

The separate [request preparer](../../../../scripts/eom/prepare-ordinary-evolution-request.mjs), SHA-256 `b641292c3597baacb558c1c48db471dfc038d1260ddd79ec32b80b037f72f5f4`, [19-control suite](../../../../tests/ordinary-evolution-request-preparation.test.js), SHA-256 `b7d3dc45c134664b5260f1f1f015b8d09be616079a01ef6f88faa1557c6c3985`, and [preparation boundary](2026-08-27-ordinary-evolution-request-boundary.md), SHA-256 `78abe4cba9ae3c1f58d84ea7533bf8c8e2e967ff8d57b6d77e0b68d21d99a745`, passed independent read-only review. The reviewer reran all 19 controls in 0.651 seconds and checked 315 independently authored assertions against actual C++ transport fields, twelve synthetic segments, exact strength factorization, forbidden future/nominal overrides and process/write traps during complete encoding. Main also read the complete preparer and boundary, inspected the existing encoder and parser, and reran all nineteen controls. No EOM process or actual candidate history evaluation occurred.

Plainly: the request-building interface is ready without choosing physical settings or starting a run. Its tests use invented data, not a new phase-varying display representative or exact source-configuration trajectory.

The preparer reuses the existing public transport encoder and canonical budget validator; it neither creates a second protocol nor constructs the process client. phase-varying display representative templates fix only the already accepted `[-1,0]` interval and exact evidence pointers. Actual histories, interaction-strength factorization, horizon, numerical and operational settings remain unset. Exact source-configuration release/history choices also remain unset. Complete caller-supplied synthetic requests still keep all eight acceptance/execution/evidence flags false. Namespace mapping, endpoint-state construction, quarter-step publication and the disabled evolution far-field shortcut are explicit. This is not EOM request validation: for example, a generic coefficient token `1e-1000` can be preserved in the prepared wire while the later C++ parser rejects it. Actual source/build, parsed bits, endpoint boxes, fingerprints, history/proof correspondence and authorized settings remain separate admission obligations.

Plainly: this removes a software-preparation task, not the scientific choices or the receiving program's validation. A fully filled form cannot authorize itself.

## Parallel exact source-configuration scope

The [current-source census](2026-08-26-factual-configuration-h1-h2-census.md) leaves sixteen strictly sub-field rows: `Three-axis circular chart — coincident pair midpoints, common frequency`; `Three-axis circular chart — coincident pair midpoints, equal radius and common frequency`; `Three-axis circular chart — coincident pair midpoints, 3:2:1 frequency ratio`; `Three-axis circular chart — phase-compensated equal geometry`; `Three-axis circular chart — axially separated endpoint circles, common frequency`; `Three-axis circular chart — axially separated endpoint circles, equal radius and common frequency`; `Three-axis circular chart — axially separated endpoint circles, 3:2:1 frequency ratio`; `Axial-transverse three-binary interior reference`; `High-axial three-binary interior reference`; `Unequal-radius planar three-binary reference`; and the six coincident-center or coaxial-separated two-component circular configurations named below. They require no new geometry selection. `Three-axis circular chart — coincident pair midpoints`, its `4:2:1 frequency ratio` variant, and the axially separated `4:2:1 frequency ratio` variant remain speed-blocked; F1/F2 representation choices and F3/F4 missing fields remain separate.

The existing parallel manifest authorizes H1/H2 only and must not be relabeled to permit H3. A fresh, candidate-specific declaration must preserve literal center and circular vectors, phase, cadence, source order, and polarity. The existing EOM `uniform_circular` factory uses a narrower radius/tilt representation; substituting it without proving equality can change these frozen sources. A shared arbitrary-vector cubic-history carrier needs independent interpolation and construction-error bounds. Reception times in `[4,8)` with histories `[T-2,T]` stay within the already declared `[0,8]` domain.

The [independent root reference](2026-08-27-subfield-circular-root-reference.md) derives exactly one positive-delay ordinary root per nonself pair and none per self pair for all sixteen literal sources. Its full outward-arithmetic hypothesis calculation passed in `11.219` seconds without invoking the EOM solver. The preserved [two-phase pilot declaration](2026-08-27-subfield-circular-h3-pilot-predeclaration.md) fixes the candidate census, times, root controls, stopping rules, and isolation contract. Its actual carrier, conformance, and build prerequisites were separately accepted before the completed pilot recorded below.

The [independently reviewed circular construction reference](2026-08-27-subfield-circular-root-reference.md#circular-carrier-construction-reference) now closes the conditional numerical allowance for all sixteen candidates and 132 members. It fixes 1,000 exact `0.002` segments per history and positive dyadic error radii, and checks the unchanged polynomial on the small endpoint overhangs actually used by the EOM interval API. Eleven local controls and seven additional independent boundary checks pass. The representation-induced residual-zero spread is bounded below the unchanged `1e-8` tolerance. This is not an actual carrier acceptance or a promise that EOM interval overestimation fits that tolerance.

Plainly: the accepted pilot used one faithful circular-history bridge and its bounded execution declaration. The larger search uses the separately reviewed parallel coordinator recorded below; the older analytical runner supplies no EOM root authority.

The API review confirms the generic strictly sub-field self-exclusion route can use the arbitrary-vector cubic carrier with nonzero error widths. It does not require the narrower circular factory. The revised pilot explicitly requires complete certificates for self rows as well as nonself rows, uses one-row calls to stop at the first failure, distinguishes pilot-wide from candidate-local stopping, declares end-to-end cost projections and outer watchdogs, and fixes repeated-phase identity and factor-overlap checks. All four pilot-contract review findings and the two serialization/domain findings were accepted. Whole-manifest conformance, actual inflated velocity bounds, a reviewed adapter/build, and a complete raw ledger remain prerequisites.

The whole-manifest checker `scripts/eom/verify-abc-circular-history.mjs` is independently reviewed and frozen at SHA-256 `b213094bb38c73d291615042df27e8feac6711c8a7958b21688fc7414208630d`. Its eleven controls pass, including captured-source execution and nonregular-input rejection. The separately authored ABC carrier `src/eom/native/eom_abc_enclosed_root_cli.cpp` is reviewed and frozen at SHA-256 `58f62a47bde4688708e2e2c3b10de32a123aa24b69b9d563878e280191174f7e`. Two actual control manifests passed the unchanged continuous checker: `a1-1` at reception `4` (6,000 segments) and `c3` at `7.96875` (12,000 segments). These controls do not substitute for full-cohort pilot conformance, actual inflated-velocity checks, or root execution.

**Measured build provenance:** the fresh recorded build in `.local-data/braid-analysis/abc-h3-root-pilot-20260827-v1/recorded-build-20260827-v2/` completed in `19.043` seconds. Its immutable `preparation.json`, SHA-256 `be6f2e43cc2c608a568d128c79535eacc628ea80cfc62cfe273af7c434243866`, retains its original pending-review label; a subsequent independent read-only review accepted the build provenance after rehashing 2,152 bound files, checking unchanged before/after source, reference, tool, header, and library records, and confirming 40 clean supervised stages and 23 translation units. The executable SHA-256 is `5e414720f0da5cef13d212624c2a0b06c0b2349cd8e9fa8cde6b8fe253e684ed`. Eight preparation controls pass. The earlier failed build is preserved separately. No histories or roots were evaluated by this build receipt.

Plainly: the approved bridge now has a checked, reproducible executable and two independently checked example histories. The build review grants no scientific result.

The independent phase ledger is reviewed and frozen: `src/prescribed-path-analysis/ABCEnclosedRootLedgerReducer.mjs` has SHA-256 `3619634d4c884fd96447976ace3396430fb4a8e4c134d93d47dfa9c5b443b361`, and its fresh-process entrypoint `scripts/eom/reduce-abc-enclosed-root-ledger.mjs` has SHA-256 `6e5d63e73904f6e5871992430f4ed9a86e63050c5b59318eefdc48c3c26a988a`. Twenty-one local controls pass. The reviewer independently checked the outward reconstruction of the actual EOM velocity boxes, complete ordered-pair census, exact root widths, positive factor overlap, self endpoint exclusions, source/build identities, and repeated-phase consistency. The checker requires externally authenticated proof and build hashes and never promotes H3 by itself. Its API-speed proof remains conditional on finite IEEE nearest rounding, gradual underflow, and the stated square-root arithmetic premise.

The subject-side row-check worker is independently reviewed at `src/prescribed-path-analysis/ABCPhaseLedgerWorker.mjs`, SHA-256 `37ef2827d1542cd0b1092ca654f6217542649350909399fbc88a8b4d6619e567`; nine local controls pass, including interruption during synchronous preparation or row checking, already-interrupted launch, and isolation from a parent's module-mode launch flags. This worker gives scheduling permission only. The completed pilot separately consumed fresh independent phase ledgers and a complete summary. The runner is reviewed at SHA-256 `11dee965bfc2a859bd958dad2e349e17751a63d2d238ce1a4cc8027808f682e4` with fourteen controls; the external launcher is reviewed at `5aa154b1579909cc63f01d81023e2e1412c2a0bb277663d9e1cd118999795baa` with fifteen controls. Detached jobs register before starting, cancel their own current process group on lost control, and retain final post-write deadline admission.

Plainly: each saved result has an independent mathematical checker, and a stuck checker or detached job can be stopped. These controls were frozen before the successful actual pilot.

Plainly: the review removed mathematical and contract ambiguity. The separate actual-history pilot supplied the execution evidence below.

**Measured pilot closure:** all 32 phases and 2,448 original certificates pass independent audit: 2,184 ordinary roots and 264 excluded self endpoints, all at 53-bit precision. The complete process ended successfully in `363.281642125` seconds through final publication; all 129 registered gates closed. The outer receipt has SHA-256 `ded3e09eb666007aa0067e7871e5519d4a3dc944cd619555227ccead8726ee84`, and its final ledger `8b7beb5e607d63e1ec78806f5e1355bf7dfceb543e482c9ec49efb548bf841d1`. Independent review checked 2,488 unique bindings, every raw row, and all resource profiles. The [measured resource contract](2026-08-27-subfield-circular-h3-parallel-resource-plan.md) records the complete measurements and projections. The six-member estimates fit 1,800 seconds; the six twelve-member two-component configurations return for a separately reviewed resource allowance. No candidate-ladder H3 result or parallel throughput is inferred from this pilot.

Plainly: the actual pilot passed. The separately reviewed parallel plan now supplies the launch authority; no further geometry decision was needed.

**Reviewed parallel execution:** the candidate-rung runner is frozen at SHA-256 `03df64f107254a0435f82599963f80476f053ab750740557d4d60987cfda36ca`, and the dispatcher at `d8a67ca8b121f36ba726410b25ea5f547d9d1b1305e5341de5a390fba75101ca`. Independent review and all 28 focused controls pass, including whole-preparation deadlines, bounded source capture, resource-log failure cancellation, startup cleanup, and complete prior-rung authentication. The [exact dispatch plan](2026-08-27-subfield-circular-h3-parallel-resource-plan.v1.json), SHA-256 `4ad03e77372db0081c7acc739e418fd14fa1ff3fa42dccb606e65c723717d4ea`, independently binds those sources, the accepted pilot/build, and the unchanged resource contract. The ignored artifact path `.local-data/braid-analysis/abc-h3-root-pilot-20260827-v1/parallel-ladder-20260827-v1/` is preserved as literal execution provenance. Its sixteen fixed geometries cover 2,688 reception phases and 205,632 ordered-pair rows if fully executed; these are declared counts, not completed results. Four workers are active at most, with one EOM worker per candidate, fifteen-second observations, and no competing EOM campaign in this budget. The independently accepted full-ladder result is recorded below.

Plainly: the larger machine run and all independent candidate and dispatcher reviews are complete. The accepted result remains limited to the declared prescribed-root checks.

**Measured first-rung audit:** the completed eight-reception rungs for `a1-1`, `a1-2`, `a1-4`, and `a2` pass independent review: 32 phases, 1,152 ordered-pair certificates, 960 ordinary roots, and 192 excluded self endpoints. The reviewer parsed all 420,930,063 original raw bytes, checked every row against live-check and final-ledger hashes, compared repeated receptions with the pilot, and rehashed 467 unique evidence bindings totaling 780,387,498 bytes. All 132 registered gates closed cleanly; the certificates stayed at 53-bit precision with no unresolved difficult cells or retained-boundary contact. Each rung completed in approximately 78.2–78.4 seconds. Their immutable `outer-admission.json` hashes, in the candidate order above, are `c7e9ef40bc5dc32e3565e580b9bcedb2287a709d0ba913ce2e9ca66a3cdc7651`, `a622920d066c468b7fd1c65e92bbdf2ae7e7655d112547daabd90a34a0ec3c58`, `7d0cc8117973cf8f752624f01212fb5bae145ab9cc15e7c918b963bfe41bd455`, and `b0969a6d513b875237481f239b6746f7e3c3ccb9003365ef7afb19817aac8374`, under each candidate's `rung-8/` directory in the declared dispatch output. This review retains the earlier complete build audit and rechecks current key executable/source bindings; it does not claim a fresh read of every system header during the running search.

Plainly: four genuine parallel first rungs passed an independent audit. This first-rung evidence alone grants no H3 result; the subsequently accepted complete ladders are recorded separately below.

**Measured second-rung audit:** the same four candidates' completed 32-reception rungs also pass independent review: 128 phases, 4,608 ordered-pair certificates, 3,840 ordinary roots, and 768 excluded self endpoints. The reviewer parsed all 1,685,158,468 new raw bytes, checked 1,440 repeated-row comparisons against the accepted pilot and eight-reception results, and authenticated 1,463 unique bound files totaling 2,593,828,628 bytes. All 516 registered gates closed cleanly; every certificate stayed at 53-bit precision without unresolved difficult cells or retained-boundary contact. The measured rung durations were approximately 330.7–333.1 seconds. In the same candidate order, the immutable `rung-32/outer-admission.json` hashes are `459f673d0b35b3710658c947bcd2b042ac48f030050ce77e38fc3becfbde3bb6`, `80d45c5159a5f5661fc108c577789e8e1ff6e0d043ce7a3c4625c47a40c0dcb1`, `eaf95a50b5266ba6925eb2ac04ca81ced45963d0b5f711b4ee137bcb7013bffd`, and `0339aa9399fc15492213dfe7cc8523dc49c1543bdb5ea703d8148c4b4d0c0850`. The already accepted eight-reception raw records were not reread as new evidence.

Plainly: this audit covers the first four candidates' second rungs only and carries no H3 promotion by itself. Their subsequent 128-reception audit and complete-ladder acceptance are recorded below.

Candidate/run outputs remain create-exclusive and independent, with fixed-cadence progress and no shared database writes. The older compact diagnostic's six-worker result is not a speed estimate for this workload.

### Independently Accepted Complete Ladders

The current independently integrated set is `Three-axis circular chart — coincident pair midpoints, common frequency`; `Three-axis circular chart — coincident pair midpoints, equal radius and common frequency`; `Three-axis circular chart — coincident pair midpoints, 3:2:1 frequency ratio`; `Three-axis circular chart — phase-compensated equal geometry`; the common-frequency, equal-radius common-frequency, and 3:2:1-frequency variants of `Three-axis circular chart — axially separated endpoint circles`; `Axial-transverse three-binary interior reference`; `High-axial three-binary interior reference`; `Unequal-radius planar three-binary reference`; and the six exact two-component circular configurations: 2,688 phases and 205,632 certificates, comprising 183,456 ordinary roots and 22,176 self exclusions. The separately staged audits below retain their distinct evidence identities. All sixteen declared complete ladders and the final dispatcher are independently integrated.

Plainly: all sixteen planned geometries now have independently accepted complete root checks. None acquires a dynamics score from that result.

**Measured, source-bound prescribed result:** `Three-axis circular chart — coincident pair midpoints, common frequency`; `Three-axis circular chart — coincident pair midpoints, equal radius and common frequency`; `Three-axis circular chart — coincident pair midpoints, 3:2:1 frequency ratio`; and `Three-axis circular chart — phase-compensated equal geometry` pass scoped `H3 P[M]` on their declared `8/32/128` reception ladders. Each complete candidate chain has 168 phases and 6,048 certificates: 5,040 ordinary nonself roots and 1,008 excluded self endpoints. Together they cover 672 phases and 24,192 certificates, comprising 20,160 ordinary roots and 4,032 self exclusions. The earlier two-phase pilot is not included in those ladder counts. These results apply only to the exact declared prescribed sources and finite reception grids, under the reviewed interval-arithmetic and runtime premises; they do not establish continuous-reception coverage or ordinary EOM evolution.

Plainly: four geometries now pass the complete planned root check, including their history uncertainty. This is a real reduction of the root-validation debt, not evidence that their prescribed motion occurs under the law.

The independent final audit parsed all 6,754,405,504 new raw bytes from the four 128-reception rungs, checked all 18,432 new certificates against original, live-check, and final-ledger identities, and verified 6,048 repeated-row root-and-factor comparisons against earlier accepted evidence. It authenticated 5,447 unique bound files totaling 9,873,084,311 bytes and checked all 2,056 new registered gate profiles. Every new gate closed successfully without a signal or profile-reported swap; every certificate remained at 53-bit precision, with at most 1,000 visited cells and no unresolved difficult cell or retained-boundary contact. The worst checked inflated API velocity-box speed bound was approximately `0.9942764334740622`, below one. The complete earlier build audit remains the source of its system-header coverage; current before/after build arrays and key executable/source bindings were rechecked, not misreported as a new read of every historical build input.

Plainly: the final review checked the actual saved root records and their complete evidence chain. The already accepted pilot and earlier rungs were authenticated by their fixed identities rather than counted again as newly inspected raw output.

The following immutable files are under each candidate's `rung-128/` directory in `.local-data/braid-analysis/abc-h3-root-pilot-20260827-v1/parallel-ladder-20260827-v1/`:

| Candidate | `outer-admission.json` SHA-256 | `run/ladder-ledger.json` SHA-256 |
| --- | --- | --- |
| `Three-axis circular chart — coincident pair midpoints, common frequency` | `e40aa696b7164e0648a60e6b0b68aef64d4db8cd2b0d2a0b7a33abcefc545ae7` | `bb83e60326fed44d5e6200e8e24ac4225cc988a0bcb1261007a0b5ae9279fe57` |
| `Three-axis circular chart — coincident pair midpoints, equal radius and common frequency` | `ba82597430de9b6fc24d0a4c6ae00ec433e477f727650adf69152115037748d1` | `d2fb55d1f5d9b54fba94365aa4e4776772ee01f129b051b8cd7dc08a3e2cf578` |
| `Three-axis circular chart — coincident pair midpoints, 3:2:1 frequency ratio` | `b3790261a8cb823846040c14208cd884f33fa962f816f7a18ae18b6c401bf1ae` | `e758ec7dbb0d2143861612efa1b9de610ec718a2512da64b5b9a0875f7a4e519` |
| `Three-axis circular chart — phase-compensated equal geometry` | `7f16952de13b1b660866acc34d8ac8a81603e5d82ab1ccc187cf25e47a76c37c` | `39e16fe19ea16caf5651b9d0e56dbe51c5a3fb0280de471a3c30615dde16dea2` |

Plainly: these hashes identify the exact complete records accepted by the independent review. Their original machine-level `h3EvidenceEligible: false` and pending-integration labels remain unchanged; this separate integration records the limited H3 conclusion.

The four final rungs took approximately 1,448.3–1,451.3 seconds each, within the unchanged 1,800-second **per-rung** ceiling. Their complete three-rung wall-time sums were approximately 1,857.5–1,862.9 seconds; those sums are not per-rung violations. Maximum individual measured resident memory during the final rungs was 404,373,504–424,706,048 bytes, not an aggregate simultaneous-memory measurement. The search advanced automatically to the next four declared candidates, whose subsequent full audit is recorded below. A changed source or binding, missing pair or phase, failed enclosure, inconsistent repeated root/factor interval, or failed final operational admission would overturn the corresponding candidate acceptance. No metric, score, retained braid, stability, physical realization, or general solver-correctness claim changes.

Plainly: the first group stayed within its approved limits. Its acceptance does not stand in for another candidate's evidence.

### Second Group: Accepted Eight- And 32-Reception Rungs

**Measured partial audit:** the common-frequency, equal-radius common-frequency, and 3:2:1-frequency variants of `Three-axis circular chart — axially separated endpoint circles`, together with `Axial-transverse three-binary interior reference`, pass independent review of both completed early rungs, with 160 phases and 5,760 certificates: 4,800 ordinary roots and 960 excluded self endpoints. All 2,106,137,857 new raw bytes were parsed and matched to original, live-check, and final-ledger hashes. The reviewer authenticated 1,783 bound files totaling 3,207,736,446 bytes, checked 1,728 repeated-row root-and-factor comparisons against the accepted pilot and earlier rung, and verified all 648 registered gate profiles closed successfully. All certificates remained at 53-bit precision with at most 1,000 visited cells, no unresolved difficult cell, and no retained-boundary contact. The worst checked inflated API velocity-box speed bound was approximately `0.9528298772857265`, below one. The eight-reception rungs took approximately 97.4–97.5 seconds; the 32-reception rungs took approximately 387.6–388.8 seconds, within their unchanged per-rung limits.

Plainly: this partial audit covers only the second group's first two stages and grants no H3 promotion by itself. The subsequent complete-ladder acceptance is recorded below.

The immutable `outer-admission.json` bindings below are under the named candidate's `rung-8/` and `rung-32/` directories in the same dispatch output:

| Candidate directory | Eight-reception SHA-256 | 32-reception SHA-256 |
| --- | --- | --- |
| `a3-1` | `b57a08c1eb939c1a5ea2e011049d01355b4d5e6fb176502ca4c457fdeb9da8fa` | `5f46f2108757af11feecc4ff1620b18e52031ec1f9a9ca48a66bf3d360f32dc7` |
| `a3-2` | `ea5f007c6415d28b75bb00a8d44d50c91b98fb243eb0fce05a54a793eeb92925` | `ee1bbf4b722b33ea43fb3b436d44b50cad619eb63a3e0c8f4032a5aefe9d6b25` |
| `a3-4` | `27c97a29a8078245d4ae18c3cbaa99a2f9b76d3a8f8ab67512f21a72eb7c5a83` | `fe87b34bb71b6e8cd84e61122361bb38a416b90d1b0a49cb3d335aaa76958ef0` |
| `b1-1` | `81c2e4350551405cde2c8c0706c4dd5ed2d07b0a59f80708b1aee0d7b99ce2ac` | `0130055b6a02e23e4feff743001eb1cc5da8e6ee3eed66805b1a70bdd37a9dc6` |

Plainly: these fixed receipts preserve exactly what passed without changing the running candidate's source, earlier evidence, or final acceptance boundary.

### Second Group: Independently Accepted Complete Ladders

**Measured, source-bound prescribed result:** the common-frequency, equal-radius common-frequency, and 3:2:1-frequency variants of `Three-axis circular chart — axially separated endpoint circles`, together with `Axial-transverse three-binary interior reference`, pass scoped `H3 P[M]` on their complete `8/32/128` ladders. This group adds 672 phases and 24,192 certificates: 20,160 ordinary roots and 4,032 self exclusions. The independent final audit parsed 6,754,558,474 new raw bytes from 512 phases and 18,432 certificates, checked all 6,048 repeated-row root-and-factor comparisons against authenticated earlier evidence, and authenticated 5,451 files totaling 9,887,900,335 bytes. All 2,056 registered gate profiles closed successfully without timeout, signal, swap or resource contact. The main audit took `292.894` seconds; a separate exact census/width crosscheck took `27.564` seconds.

Plainly: the second group's complete root records pass the same independent checks as the first group's. Earlier accepted raw records were authenticated, not recounted as newly inspected evidence.

Every new certificate used 53-bit precision and visited exactly 1,000 cells. Each nonself certificate has 999 distinct excluded segments and one root segment; each self certificate excludes all 1,000 segments with no ordinary root. The exact maximum root width is `6153707/625000000000000`, or `9.8459312e-9`, below `1e-8`, at `Three-axis circular chart — axially separated endpoint circles, common frequency`, phase 5, pair `(2,0)`. The smallest positive normal-factor bound is `0.054876630922142004`, and the largest accepted inflated API speed bound is approximately `0.9528298827725962`, below one. Equal 2,152-binding before/after build arrays and current key sources, executable, library and reference bindings were checked. The accepted independent history/speed proofs and earlier full build audit were reused; this audit does not claim a new read of every system header.

Plainly: there is no unresolved root, duplicate root segment, lost self exclusion, oversized interval or failed source binding in this group. The result remains limited to the declared prescribed sources and reception grids.

The immutable final bindings are under the same dispatch base, in each candidate's `rung-128/` directory:

| Candidate | `outer-admission.json` SHA-256 | `run/ladder-ledger.json` SHA-256 |
| --- | --- | --- |
| `Three-axis circular chart — axially separated endpoint circles, common frequency` | `56a89633bf643fefe46e1251744deb145bbeff2d911dba6613fa7a0e1351dd45` | `b8530fbe2b88097aab4c3b6841890e437a5ee09282f19dfaee475a096edf34b3` |
| `Three-axis circular chart — axially separated endpoint circles, equal radius and common frequency` | `7af24f7ffde625749f7c193fd0ab13b52719e68f57b1f7beb99b78d019cdcf0d` | `cbd7b604d7f3f40e522aeb771b2b4c0191c2520fa68b589107b31357a791ee31` |
| `Three-axis circular chart — axially separated endpoint circles, 3:2:1 frequency ratio` | `f82146cf102ce4311e6f3366d0e167e1b59602ccee3fce83603e268c61fb065b` | `1744341ff9c0d5957c7175c134466e4b53ddcf3b7d2e237f2b71ab586ed8a639` |
| `Axial-transverse three-binary interior reference` | `23409f9d39ab77775488eca3d9c0e8e610396c5e67e1c9f9d3e84e1e32aea3b1` | `f167c2cc0a2405de52d0e2e03d70750945c32a3a898c659cad3aba6e9a41e1d6` |

Plainly: these identify the exact records accepted here. Their machine-level pending-integration labels and false H3-eligibility flags remain immutable; the limited conclusion is recorded separately in this owner.

The final outer rungs took `1575.3747022500002`, `1578.0400252920003`, `1581.1673224580004`, and `1580.97340575` seconds respectively, all within 1,800 seconds. Their complete three-rung dispatcher wall-time sums were approximately 2,060.6–2,067.4 seconds, not per-rung violations. Final-rung individual-process resident-memory maxima ranged from 400,211,968 to 404,389,888 bytes; these are not simultaneous aggregate memory. No ordinary evolution, continuous-reception certificate, metric, score, retention, stability or physical claim follows. Changed source bytes, missing/reordered rows, incompatible repeated intervals, loss of strict bounds, or failed timed publication would overturn the corresponding acceptance.

Plainly: this group stayed within its declared limits. The later six-member configuration acceptance is recorded below; the remaining twelve-member two-component integrations and final dispatch/resource-log closeout remain separate work.

### Third Group: Accepted Eight- And 32-Reception Rungs

**Measured partial audit:** `High-axial three-binary interior reference`, `Unequal-radius planar three-binary reference`, `Coincident-center two-component circular chart — co-rotating`, and `Coincident-center two-component circular chart — counter-rotating` pass independent review of their completed eight- and 32-reception rungs. The audit covers 160 phases and 14,400 certificates: 12,960 ordinary roots and 1,440 excluded self endpoints. It parsed 5,679,658,104 original raw bytes, checked 12,947,040 root-free cache cells through the unchanged independent row validator, matched original/live/final identities and sequential row checks, and checked 4,320 repeated-phase root-and-factor comparisons with identical carrier hashes. It authenticated 1,785 files totaling 7,264,686,798 bytes and reconciled all 648 child profiles and 648 registered gate profiles. Every owned group closed cleanly, without swaps or resource contact; all certificates remained at 53-bit precision and at most 1,000 visited cells. The maximum independently checked API speed upper bound was approximately `0.6922372308652148`, below one. The audit took `225.009862167` seconds and read no 128-reception output.

Plainly: these four candidates have passed their two early stages. The larger twelve-member two-component count is included explicitly; this partial audit grants no H3 result by itself.

The immutable outer admissions are under the same dispatch base:

| Candidate directory | `rung-8/outer-admission.json` SHA-256 | `rung-32/outer-admission.json` SHA-256 |
| --- | --- | --- |
| `b1-2` | `91702d4ce12d6796b8237f3f6a10db81b0ab8c8bbd8d3ca07967c854e27a7907` | `e468bebc368a442f09d8fceffc1f1334b5104ce36fc283718bb9d2ea06796743` |
| `b1-3` | `ff7115a32147f4573ede6ff761dd9f920f6a6e6270360792b9b1f11b7e79f44d` | `2f38c337251969cb3bdf743e81a8da7f75c25ed2894f969b3a9fc916f18c55e7` |
| `c1` | `77ca88aeed004ea2720934aefeb2915bdba3a82b310ee7fb98969635747d1052` | `dbba6f4a0af8f5b505a06c119d1b8cfa9c0d3eb4f6e458d87196464e42592f0f` |
| `c2` | `068cbf500fe3fd12b4868d4be55c147fe0e28a0504336cb3ca3d2e1c35901a65` | `098f9c84b33e92236692b7cd56294e9ded10b58808d0265407828bd97272dd3a` |

Plainly: these hashes identify only the completed early-rung evidence. Final-rung results and full-ladder acceptance retain their separate boundary.

The six-member configuration eight-reception rungs took approximately 100.2–100.7 seconds and their 32-reception rungs 390.6–393.1 seconds. The coincident-center co-rotating and counter-rotating two-component configurations took approximately 206.8–207.2 and 812.5–815.2 seconds respectively. All stayed within the unchanged six-member 1,800-second and twelve-member 3,600-second per-rung limits. Individual-process resident-memory maxima ranged from 338,132,992 to 463,077,376 bytes, not aggregate simultaneous memory. Total CPU reconciliation includes the runner's recorded nonnegative `process.resourceUsage` delta in addition to independently parsed child/gate profiles; it is not presented as a separately retained raw runner-usage packet. Current key bindings and equal 2,152-entry build arrays were checked against the earlier accepted build without claiming a fresh audit of all headers.

Plainly: the measured early stages stayed within their approved operating limits. No future-stage cost, score, retention or dynamics result is inferred from them.

### High-axial and unequal-radius planar three-binary references: independently accepted complete ladders

**Measured, source-bound prescribed result:** `High-axial three-binary interior reference` and `Unequal-radius planar three-binary reference` pass scoped `H3 P[M]` on their complete `8/32/128` ladders, adding 336 phases and 12,096 certificates: 10,080 ordinary roots and 2,016 self exclusions. Independent review parsed all 3,376,867,039 new 128-rung raw bytes and 9,216 certificates, including 7,680 ordinary roots and 1,536 self exclusions. It checked 7,672,320 root-free cache cells and 3,024 repeated-pair comparisons, including root and both normal-factor interval overlaps and identical carriers. The earlier accepted eight- and 32-reception raw outputs were not reread as new evidence. The audit authenticated 2,781 files totaling 5,033,925,057 bytes, checked current key bindings and the unchanged 2,152-binding build arrays, and completed in `134.003734375` seconds.

Plainly: both remaining six-member configurations pass their complete planned root check. At this stage the six twelve-member two-component configurations remained; the later exact-configuration audits below reduce the remaining set to the two coaxial separated two-planar-braid configurations.

All 1,028 new child profiles and 1,028 registered gate profiles closed cleanly without swaps or resource contact. Every certificate stayed at 53-bit precision and at most 1,000 visited cells. The largest accepted API speed upper bound was approximately `0.6922372309767676`, below one. Final 128-rung wall times were `1645.1691889580004` and `1636.9224295000006` seconds respectively, below the unchanged 1,800-second per-rung cap. Individual-process resident-memory maxima were 407,289,856 and 407,420,928 bytes. Complete three-rung dispatcher wall-time sums were approximately 2,139.1 and 2,127.8 seconds; these are not per-rung violations or aggregate memory measurements.

Plainly: both final rungs completed within their approved limits. The result does not establish continuous-reception coverage, ordinary evolution, retention or any metric.

| Candidate | `rung-128/outer-admission.json` SHA-256 | `rung-128/run/ladder-ledger.json` SHA-256 |
| --- | --- | --- |
| `High-axial three-binary interior reference` | `a0a4255fca4865d059163c319aeb1d5e9cf653a5f88ab4232c609a3146d261c3` | `464af6404e718395dcf2e5bbd8a4bc091122559b0e6c8c63346001c760d50513` |
| `planar common-center three-binary constraint` | `9a6215ef17fb5222b9caf397c7b828d8b74205fe358538d13a80e23af8a8f1b4` | `feb9183ccee803c9bb88c4034730a82a3d6c73daeb89e841b6267b7f2e42f503` |

Plainly: the immutable records remain under the declared dispatch directory, with their original machine-level false H3-eligibility and pending-integration labels. This separate owner records only the independently reviewed prescribed H3 conclusion; changed bindings, incomplete census, failed interval comparisons or failed operational admission would overturn it.

### Coaxial separated two-component circular configurations: accepted eight- and 32-reception rungs

**Measured partial audit:** `Coaxial separated two-component circular chart — co-rotating` and `Coaxial separated two-component circular chart — counter-rotating` pass independent review of their completed eight- and 32-reception rungs. The audit covers 80 phases and 11,520 original certificates: 10,560 ordinary roots and 960 self exclusions. It parsed 4,625,859,939 raw bytes, checked 10,549,440 root-free cache cells and 3,456 repeated-reception root-and-factor comparisons, and authenticated 945 file bindings totaling 5,752,907,351 bytes. All 324 child profiles and 324 registered gate profiles closed cleanly with zero reported swaps. All certificates remained at 53-bit precision and at most 1,000 visited cells; the maximum independently checked API speed bound was approximately `0.3856615922538009`, below one. The audit took `181.568627958` seconds and inspected no running 128-reception results.

Plainly: both candidates passed their first two rounds. This partial result alone grants no H3; their subsequent final-round acceptance is recorded below.

| Candidate directory | `rung-8/outer-admission.json` SHA-256 | `rung-32/outer-admission.json` SHA-256 |
| --- | --- | --- |
| `c3` | `d8c35fc3ea3ff05591b3218217c84384f457e732dc778b3b344df6bbe5846c77` | `12fc4d9c999471c3871eea7efba142f5f39cfcb66806b50f12a8b6ec92d8beb0` |
| `c4` | `9c9795f46235ddd6d5212f3beb131edef90d34d23d642217a3c23ae58a44fe43` | `f2d4d3ace6f23dc6d48139261da83700fc6f4795442874d4ce23ff1afbe22b76` |

Plainly: these hashes identify the completed early-rung admissions under the same declared dispatch directory. The main integration reauthenticated them and their four rung summaries.

The eight-reception rungs took approximately 250.4–250.8 seconds, and the 32-reception rungs 869.7–872.7 seconds, within their 3,600-second limits. Individual-process resident-memory maxima ranged from 441,434,112 to 487,227,392 bytes; these are not simultaneous aggregate memory. The accepted historical 2,152-binding build arrays and current key files were authenticated, without claiming a fresh reread of every header. Missing or changed bindings, failed interval comparisons, incomplete census or failed operational admission would overturn this partial acceptance. No continuous-time coverage, ordinary evolution, metric, score, retention or stability follows.

Plainly: the measured early rounds completed within their limits. Their timings do not predict the cost or outcome of a later round.

### Coincident-center two-component circular configurations: independently accepted complete ladders

**Measured, source-bound prescribed result:** `Coincident-center two-component circular chart — co-rotating` and `Coincident-center two-component circular chart — counter-rotating` pass scoped `H3 P[M]` on their complete `8/32/128` ladders. Together they add 336 phases and 48,384 certificates: 44,352 ordinary roots and 4,032 self exclusions. Independent review parsed every new 128-rung raw row: 256 phases, 36,864 certificates, 33,792 ordinary roots, 3,072 self exclusions and 14,838,216,790 raw bytes. It checked 33,758,208 root-free cache cells and 12,096 repeated-reception root-and-factor comparisons with identical carrier bindings. The previously accepted eight- and 32-reception receipts were authenticated without rereading their raw records as new evidence. The final audit authenticated 2,781 bindings totaling 18,046,048,416 bytes and exited successfully after `558.570400292` seconds.

Plainly: two twelve-member candidates pass their entire declared root check. At this stage four coaxial separated two-component configurations remained; the later circular-configuration audit below reduces that number to two.

All 1,028 child profiles and 1,028 registered gate profiles closed cleanly, with zero reported swaps. Every certificate stayed at 53-bit precision and at most 1,000 visited cells; the maximum independently checked API speed bound was approximately `0.5349106642322676`, below one. Final 128-rung wall times were `3532.116358875001` and `3523.5332356249996` seconds, within the unchanged 3,600-second limits. Their individual-process resident-memory maxima were 527,106,048 and 481,247,232 bytes. The accepted historical 2,152-binding build arrays and current key files were authenticated; this is not a new reread of every historical header.

Plainly: both final rounds stayed within their approved limits, with little time margin. The result is neither a throughput guarantee nor a measurement of simultaneous aggregate memory.

| Candidate | `rung-128/outer-admission.json` SHA-256 | `rung-128/run/ladder-ledger.json` SHA-256 |
| --- | --- | --- |
| `Coincident-center two-component circular chart — co-rotating` | `70dbc9b4e3c9217d006fedf43ce7663ded9bc5adcea3ca3e35f7000123d47811` | `04b2c29c67f3bf91c2b07d2e1f0384824d2940094ef0cf28858bcc870f0979c8` |
| `Coincident-center two-component circular chart — counter-rotating` | `ac69d8cd133e6ed547356b25093b3bc31033a1f703a18bf23664741af04ff1f4` | `6aa90116625c7ae9e56e7bfd240ead500806afeda75663d90c71a729aeed5b95` |

Plainly: these immutable records remain under the same declared dispatch directory. The main integration reauthenticated them, both rung summaries and both candidate dispositions before updating current owners.

Complete three-rung wall-time sums were approximately 4,554.7 and 4,543.0 seconds, not per-rung violations. Full-candidate named-output totals were 11,718,738,873 and 11,719,119,901 bytes, including all three outer admissions. The final-rung named totals including their outer admissions were 8,931,826,292 and 8,932,113,697 bytes; the inner pre-admission totals omit precisely the respective 1,532,444- and 1,532,753-byte outer records. CPU accounting includes the recorded runner delta and independently parsed child/gate profiles. Missing or changed bindings, incomplete coverage, failed repeated intervals, nonpositive required margins or failed timed publication would overturn the applicable acceptance. No continuous-reception root coverage, ordinary evolution, metric, score, retention, stability or physical claim follows.

Plainly: the evidence scopes and accounting differences are explicit. Completing a root ladder does not complete the candidate's dynamics or establish a retained braid.

### Coaxial separated two-component circular configurations: independently accepted complete ladders

**Measured, source-bound prescribed result:** `Coaxial separated two-component circular chart — co-rotating` and `Coaxial separated two-component circular chart — counter-rotating` pass scoped `H3 P[M]` on their complete `8/32/128` ladders. Together they add 336 phases and 48,384 certificates: 44,352 ordinary roots and 4,032 self exclusions. Independent review parsed all new 128-rung raw rows: 256 phases, 36,864 certificates, 33,792 ordinary roots, 3,072 self exclusions and 14,835,445,106 raw bytes. It checked 33,758,208 root-free cache cells and 12,096 repeated-reception root-and-factor comparisons with identical carriers. Earlier accepted eight- and 32-reception receipts were authenticated without rereading their raw outputs as new evidence. The audit authenticated 2,781 distinct bindings totaling 18,070,080,031 bytes and closed with exit zero after `434.068279708` seconds.

Plainly: another two twelve-member geometries pass their entire declared root check. At this stage only the co-rotating and counter-rotating coaxial separated two-planar-braid configurations remained; their subsequent complete acceptance is recorded below.

All 1,028 child profiles and 1,028 registered gate profiles closed cleanly, with retired process identities and zero reported swaps. Every certificate stayed at 53-bit precision and at most 1,000 visited cells; the maximum independently checked API speed bound was approximately `0.3856615923365238`, below one. Final-rung wall times were `3533.7478941249997` and `3525.9332238750003` seconds, within the unchanged 3,600-second limits. Their largest individual-process resident-memory observations were 502,743,040 and 522,272,768 bytes, not simultaneous aggregate memory. The accepted 2,152-binding historical build arrays and current key bindings were checked; this is not a fresh audit of every header.

Plainly: both final rounds stayed within the approved time limit, with little spare time. The observations describe these completed runs, not guaranteed future cost.

| Candidate | `rung-128/outer-admission.json` SHA-256 | `rung-128/run/ladder-ledger.json` SHA-256 |
| --- | --- | --- |
| `Coaxial separated two-component circular chart — co-rotating` | `6643047b292f57db98767f5e1eef768a8d654385696cf9bfa482b1db84324e8d` | `6e253b6934236efa329e12429db3c822571b4f1a52b0c6925eae19a602ba0d97` |
| `Coaxial separated two-component circular chart — counter-rotating` | `3bcb5894a48f08497ff6663d7b6fbbce2e898dce81f2df991f0aa4977629a43f` | `249a1dd88771d5ac675938f26c9c84e68319b0d9d7534576ea829aea940f513f` |

Plainly: these records remain under the declared dispatch directory. Main integration separately reauthenticated both admissions, full ledgers, rung summaries and candidate dispositions before updating status.

Complete three-rung wall-time sums were `4656.988233458999` and `4646.5614759579985` seconds; each limit applies to one rung, not that sum. Full-candidate named-output totals were 11,734,595,414 and 11,734,732,018 bytes across 1,703 files each. Final-rung named totals including the outer admissions were 8,943,908,023 and 8,944,011,694 bytes; their pre-admission totals omit precisely the respective 1,531,494- and 1,531,665-byte outer records. CPU accounting uses the runner's recorded usage delta plus independently parsed child/gate profiles, not a separately retained raw runner-usage packet. Changed bindings, incomplete census, failed interval checks, missing repeated-phase agreement or failed operational admission would overturn the corresponding acceptance. No continuous-reception coverage, ordinary evolution, H4/H5, metric, score, retention, stability or physical claim follows.

Plainly: the complete root obligation is closed only for these fixed prescribed paths and sampling levels. Their actual dynamical behavior remains a separate question.

### Coaxial separated two-planar-braid configurations: accepted eight- and 32-reception rungs

**Measured, source-bound intermediate result:** independent review accepts the completed eight- and 32-reception rungs for `Coaxial separated two-planar-braid chart — co-rotating` and `Coaxial separated two-planar-braid chart — counter-rotating` only. Their 80 phases contain 11,520 certificates: 10,560 ordinary roots and 960 self exclusions. The audit read 4,626,262,818 raw bytes and checked 10,549,440 root-free cache cells, 3,456 repeated-row checks including 3,168 ordinary-root and both-factor overlaps, and 945 file bindings totaling 5,739,548,622 bytes. All 324 child profiles and 324 registered gate profiles closed cleanly without swaps or resource contact. Every certificate stayed at 53-bit precision and at most 1,000 visited cells. The largest accepted API speed upper bound was approximately `0.6922372308653542`, below one. The independent audit closed successfully in `177.217890041` seconds; it checked the accepted 2,152-binding build arrays and current key bindings, without claiming a new full header reread.

Plainly: these two coaxial separated two-planar-braid geometries passed their first and second sampling levels. Their separate final-level acceptance is recorded below.

| Candidate/rung | Outer-admission SHA-256 | Wall seconds | CPU seconds including gates | Largest individual RSS bytes | Named bytes before outer record |
| --- | --- | ---: | ---: | ---: | ---: |
| Coaxial separated two-planar-braid chart — co-rotating / 8 | `acedd7211df8d06fb129afa1bc9b4f346f7ae6dd2a9ce9512596781a41927009` | 214.030148209 | 233.096877 | 424,083,456 | 557,209,127 |
| Coaxial separated two-planar-braid chart — co-rotating / 32 | `daee5e7bcdb73b5955e1b45a6facc158c8286b93fd3587cd893f2c8cacb7c0de` | 906.6282678329982 | 979.0697989999998 | 462,323,712 | 2,226,324,716 |
| Coaxial separated two-planar-braid chart — counter-rotating / 8 | `1fcf812327e0c868c3bdd9aac7ee1e7115463c0f10fb740465227f7dce9751f2` | 212.3026662499998 | 230.635641 | 423,313,408 | 557,213,760 |
| Coaxial separated two-planar-braid chart — counter-rotating / 32 | `eda6a1f037dbaeced6b4d0b54d97d9a3db1815d9fb65f463ec8c890cb16be9d9` | 905.2405395 | 977.8629919999994 | 486,719,488 | 2,226,356,999 |

Plainly: these are measured first- and second-level costs under the unchanged 3,600-second twelve-member per-rung cap. Individual RSS is not simultaneous aggregate memory, and the named-byte column excludes each outer-admission file itself.

The matching rung-summary hashes are respectively `4c7fd1a8dec0d4803b7b2c724d0d5c7dfec09ea643bb2e07a3fe48c155d00738`, `56fe9bcf66c8ee055a41ababee3511b633075daf6af336ddf2e779a7d3ffb401`, `7d73a7e9b87c92431eb5132d83f35872b7d687d47572f4e6ea40f80925e9feb4`, and `8c818fdde5a1cab6d6abd61fbe68e1830e8e0ae4bccefde7f4e9a89626581ac2`. Main integration separately rehashed all eight small outer/summary files. No final 128-rung raw output was inspected or accepted in this audit. No H4/H5, score, continuous-reception or physical claim follows.

Plainly: this evidence closes only the two named intermediate levels; the separate final checks below complete the ladders.

### Coaxial separated two-planar-braid configurations: independently accepted complete ladders

**Measured, source-bound prescribed result:** `Coaxial separated two-planar-braid chart — co-rotating` and `Coaxial separated two-planar-braid chart — counter-rotating` pass scoped `H3 P[M]` on their complete `8/32/128` ladders. Together they add 336 phases and 48,384 certificates: 44,352 ordinary roots and 4,032 self exclusions. Independent final review parsed every new 128-rung raw row: 256 phases, 36,864 certificates, 33,792 ordinary roots, 3,072 self exclusions and 14,836,755,925 raw bytes. It checked 33,758,208 root-free cache cells and 12,096 repeated-row comparisons, including 11,088 ordinary-row root and both-factor overlap comparisons with identical carriers. Earlier accepted eight- and 32-reception evidence was authenticated, not reread as new raw evidence. The audit rehashed 2,781 distinct bindings totaling 18,027,394,637 bytes and closed with exit zero in `330.699209083` seconds.

Plainly: the last two declared geometries pass their complete planned root check. All sixteen candidate ladders now have independent acceptance; no raw-row audit remains outstanding in this declared search.

All 1,028 child profiles and 1,028 registered gate profiles closed cleanly, with retired process identities and no reported swaps or resource contact. Every certificate stayed at 53-bit precision and at most 1,000 visited cells. The maximum independently checked API speed upper bound was approximately `0.6922372309767676`, below one. The accepted historical 2,152-binding build arrays and current key files were authenticated without claiming a new reread of all headers. Final-rung wall times were `2894.6578389999995` and `2892.0193423340006` seconds, each below the unchanged 3,600-second limit. Their largest individual-process RSS values were 520,601,600 and 516,325,376 bytes; these are not simultaneous aggregate memory.

Plainly: both final rounds completed within their approved resource limits. Their measured cost does not establish a general speed guarantee for other configurations or workloads.

| Candidate | `rung-128/outer-admission.json` SHA-256 | `rung-128/run/ladder-ledger.json` SHA-256 |
| --- | --- | --- |
| `Coaxial separated two-planar-braid chart — co-rotating` | `5f9dbe2e03060905688bc4ce8fbf87fac03338b458846dc1176fc317092f0b00` | `f49fae5e8b8cbc974c5984788de3afcd9bdbac46a47041c62095a6977bd21857` |
| `Coaxial separated two-planar-braid chart — counter-rotating` | `0b48d68535f3ecef2bd91499a0071c0fe389268c62023d8815e29377c4016da6` | `49cb0d0b23d16f549af7ac44f4ba04958a4c1f271554993d9d3e7f57bd8c9bf8` |

Plainly: these immutable records remain under the declared dispatch directory. Main integration reauthenticated both admissions, complete ledgers, rung summaries and candidate dispositions before changing current status.

The final-rung named-output totals including outer admissions were 8,922,570,016 and 8,922,677,262 bytes. The corresponding pre-admission totals omit exactly the 1,532,941- and 1,532,931-byte outer records. Full-candidate named totals including all three outer admissions were 11,706,575,925 and 11,706,720,078 bytes. Complete three-rung wall sums were `4015.441942707999` and `4009.693073667001` seconds; those sums are not per-rung limit violations. Recorded CPU totals include runner usage and independently parsed child/gate profiles. Changed bindings, missing coverage, failed repeated intervals, nonpositive required margins or failed operational admission would overturn the applicable result. No continuous-reception coverage, ordinary evolution, H4/H5, score, retention, stability or physical claim follows.

Plainly: the completed root checks establish the declared source-bound prerequisite, not the dynamical fate of either geometry.

### Independently Accepted Final Dispatcher

**Measured integration closure:** the original four-worker dispatcher exited zero with `accepted:true` and `sharedStopped:false`, as observed by the supervising task. Its immutable 14,819,500-byte `dispatch.json` has SHA-256 `b7ede1634c4cbb5cb65546230c7c65ad59750bdff1c211b2711daa8ec0321df1`. Independent review reconciled 134 final small files against the fixed plan: exactly sixteen candidate identities, 48 accepted rungs, 2,688 phases and 205,632 pair certificates, comprising 183,456 ordinary roots and 22,176 self exclusions. All separate candidate dispositions equal their embedded dispatcher entries, and all outer, rung-summary and complete-ledger chains agree. Main integration separately repeated the sixteen-disposition equality and complete phase/pair census. The candidate receipts retain their original machine-level pending-integration labels and false H3-eligibility flags; this separate owner supplies the independently reviewed scoped conclusion.

Plainly: the final summary accounts for every planned row, not just the best-looking candidates. There is no missing or undispositioned lane in the declared sixteen-candidate root search.

The 709,425-byte `resource-observations.ndjson` has SHA-256 `c5ce0ce079fdd5c224fde1adbc7d911d06c211bf4bc07076e4d6b626fd22145e`. Its 875 observations show free memory between 42 and 59 percent, minimum available disk 963,904,069,632 bytes, and maximum observation-start gap approximately 15.018 seconds; no declared threshold was contacted. The four-candidate/four-EOM-worker ceiling is enforced by the reviewed dispatcher source and fixed plan, not claimed as a separately measured aggregate concurrency trace. Candidate totals are 102,162,307,915 named bytes across 27,248 files, excluding shared and dispatcher self-publication files; candidate recorded CPU sums are approximately 51,180.361 seconds, with largest individual-process RSS 527,106,048 bytes. Dispatcher overhead was separately recorded as 188.184732 CPU seconds and 405,929,984 bytes peak RSS. These quantities are not simultaneous whole-machine usage.

Plainly: the stored resource observations support the declared operating limits. Parallel CPU sums, individual peaks and source-enforced worker limits describe different things and are not interchangeable throughput measurements.

The dispatcher's recorded `12492.360159916` wall seconds are measured before its own final publication; its externally observed successful exit supplies final closure, not an invented post-publication timestamp. All sixteen source-bound ladders and final dispatch integration are complete. Remove the completed ladder from the ranked next-object queue while preserving its evidence here. Later ordinary EOM evolution requires its own exact scientific and numerical declaration. No continuous-time root coverage, acceleration residual, metric availability, score, retained braid or physical realization is established by this completed search.

Plainly: this parallel root campaign is closed. The next scientific question is whether the law generates suitable motion, not whether these prescribed sampling ladders were completed.

## BP-010 reference blocker

The [asymmetric counter-breathing representative predeclaration](2026-08-26-f6c-normalized-member-acceleration-predeclaration.md) has an independently verified version-2 subject-provenance repair. Its durable Stage B summary and row manifest preserve the exact selected parameters and refined release record; Stage A selection and Stage B refinement are no longer conflated. The named shared-reference repairs, affected transitive checks, and offline data export below are complete. The continuous-reception measurement interface and separately authorized measurement remain outstanding; these bounded repairs do not certify every reference behavior.

The initial correlated-history caller `scripts/eom/oracle/certified_history.py`, SHA-256 `cf0f1e6b9e60c3411cd1aa11f23c08a013ab9dded0b7215f1a2fc0f6f333002f`, rounded an uncertainty radius inward before passing it to the repaired decimal primitive. Its single-segment `correlated_displacement_interval` and multisegment counterpart both used ambient-precision scalar products.

**Measured exact-rational counterexample:** on `[0,1]`, take a zero nominal cubic, position error `1`, and velocity error `0.123456789012345678901234567841`. The admissible linear error reaches exactly that latter value at time `1`. With ambient decimal precision `28`, the initial caller returned upper displacement `0.1234567890123456789012345678`, strictly below the admissible value. An exact `Fraction` comparison reproduced the under-enclosure. Raising ambient precision happens to avoid this example but is not an outward-rounding proof.

Plainly: the old calculation rounded away part of an allowed uncertainty before the interval checker received it.

The separately authored radius repair passes independent review and thirteen exact-rational controls frozen before implementation. The subsequent derivative/join repair uses exact integer coefficient scaling and exact-rational nominal endpoint comparisons. Independent review accepts that three-part source generation at SHA-256 `e74d52c54266201e79d884927d066bc1a8f9d6be521dabd433d178ae9bbfc247`; the twenty pre-frozen controls have SHA-256 `becf4f4c0e9390a23bf851bbb143430ac0500ba5e11020607c480c982039de80`. All twenty controls and sixty-five focused regression tests pass.

The subsequent width-only source generation `90c041ac38f6a4d3b0498fe05bf87db5a713091b88ad8581a04c56af6f49bb87` closes exact width reporting and four tolerance comparisons. Its seven pre-frozen controls, SHA-256 `9fdd547823fe9980492e7e91fa731164af19876eeb48b78f937c51b50236159d`, include two actual reference calls that previously accepted an oversized bracket. A final independently reviewed merge-width check produces source generation `ca916b4bc979629a5e25c1490da07fd78a26b4e75cfba5677f35fbab658a29e7`: every oversized final hull remains recorded, receives an explicit unresolved reason, and prevents complete authority. Its five new controls, SHA-256 `9afc3e370808fc7747cc72f53f7e62cc879cbc5dbc805ef2c5e0a3d0dcf36ef6`, distinguish the exact hull-width counterexample from synthetic finalization wiring. Seventy-seven focused tests pass; previous reference tests and the decimal primitive remain unchanged.

Transitive validation exposed two downstream failures: the evolution reference used half an endpoint interval's width as an error radius, although its stored center could differ from the interval midpoint. At precision `18`, an exact constant endpoint `1.000000000000000009` could be stored as `1.00000000000000000` with radius `5e-18`; the actual gap is `9e-18`. The separately reviewed repair in `scripts/eom/oracle/certified_evolution.py`, SHA-256 `00567dfef3163d40634dd5790d5eeb667cff8698394831130e8cb91937ddc80a`, takes outward endpoint distances from the actual stored position and velocity centers, then the maximum over axes. It preserves the exact join test. Its seven independent controls were frozen before implementation at `tests/test_eom_evolution_centered_radius.py`, SHA-256 `493f561cb3cf60a5194b67fb4146c943679c62683a520dbde4653672bcbeeea4`.

Plainly: the corrected constructor now covers the uncertainty around the number it actually stores, not around a different ideal midpoint.

**Measured regression result:** all 74 affected checks pass: centered-radius `7/7`, evolution `10/10`, phase-4 `12/12`, decimal `5/5`, and phase-varying display representative conformance `8/8`, `14/14`, and `18/18`. The supervised group completed in `23.169` seconds with all seven process groups closed. Independent read-only review accepted the chosen-center mathematics and sole production-call wiring and reran the seven new controls. The frozen history reference, decimal primitive, exact source-configuration instruments, and accepted phase-varying display representative evidence remain unchanged by this caller repair.

Plainly: the two failures are resolved without weakening the checker. These tests close the named rounding defects, not general evolution correctness or asymmetric counter-breathing representative's missing measurement.

The falsifier for these repairs is any admissible exact polynomial/error function outside the returned enclosure under the supported ambient precision range. No BP-010 exporter, measurement, score change, or EOM execution is authorized by these reference results.

**Measured existing-data inventory:** the original refined rung already retains `.tmp/f6c-dual-turn-stage-b-row12-refined-v2/row-000/assembly-view-record.json`, SHA-256 `7aa3369399664fb763c6d8dbca80d5f44c1c8bff6f358da311417277ce3667eb`, with all eight paths and 14,080 segment records: 1,600 prehistory plus 160 evolved segments per member, covering `[-8,0.13]`. Its `frames.jsonl`, SHA-256 `49d59797c09c9c933cf0ad1c97f3927f7d4991f1b812e0b73e91e24e993ff651`, contains 81 complete eight-member frames. The original checkpoint also remains available at SHA-256 `8ba02c63c0428f670ef4c33bc464e43933f3793b561322641b1388fde92b459d`. Read-only inventory checked the exact string tokens, contiguous time coverage, and scalar error radii covering every axis radius. This is data availability, not a continuous-history or acceleration certificate.

Plainly: recovering the existing first-rung history did not require another solver run. The offline, report-only adapter and actual data bridge are recorded below.

The adapter must keep the two measurement sides distinct: the history-required acceleration comes from cubic Hermite interpolation across the 80 accepted-frame intervals, while the Master Equation reference consumes the actual stored prehistory and evolved segments. The repaired reference's decimal-token constructors can represent the latter without changing the reference. Scalar-time root and acceleration interfaces do not by themselves supply the continuous-reception interval bounds needed for the predeclared quadrature and peak proof. That mathematical interface remains a separate measurement obligation; neither a data export nor the repaired reference tests make `M05/M06` available.

Plainly: having all the data removes one engineering obstacle. It does not yet bound the acceleration mismatch between sampling times.

The offline adapter `scripts/eom/export-f6c-retained-history.py` is independently reviewed at SHA-256 `e6b2d6946fe8fad0399616dd8b9e0bb9aa18435ea21466695a4c06ad8acc34aa`; its sixteen fixture controls, SHA-256 `014036159078a68c5a5746319bd8cb329f682e45d17ef4dab44c27aa5f03dd38`, pass. After the sixteen-configuration cost clock closed, the actual export completed without an EOM call at `.local-data/braid-analysis/f6c-history-export-20260827.jUhLLg/retained-history.json`, SHA-256 `f479bb88a6425e9e98e00288f2524f33d5a3c0f4c2a14139dbaae4f468c46db1`, 9,125,903 bytes. A separate original-data comparison, importing no exporter code, verified equality of all 14,080 segment records and all 648 frame rows' exact numeric lexemes. The checkpoint is hash-bound but not decoded or parity-verified. All roots, acceleration, metrics, trajectory-certification, continuous-reception-bound, and execution-authority flags remain false.

Plainly: the faithful data bridge is complete. The independent continuous-time acceleration measurement is not.

The [continuous-reception enclosure contract](2026-08-27-f6c-continuous-reception-enclosure-contract.md) passed independent mathematical review at SHA-256 `f20e4bdaaff8b6f0012fdc6135b15d568a817832fb55d5c42f80d8421a117f68`. Its conditional theorem proves finite-retained-history nonself root uniqueness, self exclusion, root transport, uniform reception-cell coverage, and the unchanged sharp acceleration enclosure. The receiver normal factor appears only in root transport, not as an extra acceleration multiplier. Applying the theorem still requires a globally compatible continuously differentiable history, subject membership, and verified uniform guards. The subsequent independent peak-evaluation clarification below resolves both one-sided limits as an equivalent evaluation of the already predeclared supremum; neither metric definition changes.

Plainly: the conditional theorem supplies no acceleration value or execution permission. The subsequent data proofs below establish compatibility, reconstructed-family containment, and uniform guards separately; they do not establish historical trajectory identity.

**Read-only semantics finding:** the current history implementation uses the serialized position and velocity errors as componentwise numerical enclosures; its join checks establish adjacent overlap, not selection of one globally continuously differentiable path. Coarse/fine endpoint error estimates do not alone certify continuous exact-solution containment. A constructive compatible-path witness would establish nonempty admissible histories, not identify that witness with the original evolved subject. There is also a historical-build distinction: the currently pinned search source emits a `v3` generating-spec label, whereas the preserved Stage B record identifies `v2-return-continuation`. The inspected Stage B files provide run/model/build labels, but no matching compiler, binary, and implementation hash chain was established by this audit. This does not assert that no historical build evidence exists elsewhere.

Plainly: the saved bytes and their identity remain preserved. Current code inspection cannot retroactively certify which executable produced them, and a path that fits their error bounds is not automatically the original solver trajectory. These limits do not reject asymmetric counter-breathing representative geometry or block the separate exact source-configuration search.

**Measured compatible-history existence:** the independently reviewed data-only verifier `scripts/eom/verify-f6c-retained-history-compatibility.py`, SHA-256 `646b30053c7c96379779b2f52062f1c3ab8ece5e72d4e8f276116efadd4f997b`, passes 27 controls and completed the actual frozen export check successfully in `5.7683487080503255` seconds through publication. Its 6,000-byte receipt at `.local-data/braid-analysis/f6c-history-compatibility-20260827.mRMiLH/compatibility.json` has SHA-256 `e68dc46612df3adea0f71fb8e0a9158d5eb7d29a88f59130ee1cae80de5299f3`. Main-process admission checked successful exit, the fresh completion hash, intended output, and publication time; a private or abandoned accepted file alone has no authority. Independent review then reproduced every comparison without importing the verifier, solving the correction polynomials in power basis and converting them generically to Bernstein controls.

Plainly: a separate calculation now confirms that coherent smooth paths fit inside the saved allowances. This is an existence result, not a claim that the fitted paths are the original solver trajectory.

The construction uses shared exact-rational endpoint positions and rates, then a cubic correction on every original segment. Bounds on the correction's four position controls and three derivative controls prove containment throughout the segment. All 8 members, 14,080 segments, 42,240 coordinate axes, 14,072 internal joins, and 591,360 original per-axis/scalar allowance comparisons pass. The largest used fractions of an allowance are approximately `0.0000065837970924208615` for position and `0.0000007282259333885893` for velocity. The original velocity boxes also have exact squared-speed upper bounds below one, with maximum approximately `0.7198768455220877`; this bound applies conditionally to any path inside those boxes. The independent audit matched all exact rational maxima, locations, identities, and before/after source bindings, not just these rounded summaries.

Plainly: the required join corrections use only a small part of the existing error budget, and the allowed velocities remain below wake speed. No error allowance was enlarged and no stored geometry was changed. This compatibility receipt alone does not identify the historical trajectory or supply the separate reconstruction and uniform-guard results recorded below; its subject-membership, trajectory, root, metric, H3, and execution-authority flags remain false.

The [accepted-frame reconstruction theorem](2026-08-27-f6c-accepted-frame-history-reconstruction.md) passed separate independent mathematical review at SHA-256 `6abbbbacc1671052bdd881790094dbd71ebb03d54904ac1f937edae1f3c9f936`; its original pending-review label remains frozen. It defines the exact frame-derived future and prehistories anchored to that future's release position and velocity. If those anchored prehistories exist and the fixed future fits every original evolved-piece allowance, their concatenations form a nonempty subset of the original full-history family. The exact Bernstein containment formulas, shifted polynomial origins, closed-endpoint coverage, and distinction between a sufficient-test failure and nonexistence were independently checked. The subsequent separately reviewed actual-data proof below discharges these two numerical containment hypotheses; the earlier preliminary calculations are not its evidence.

Plainly: there is now a reviewed way to define a coherent reconstructed history without claiming that it is the historical solver trajectory. Adopting it for the existing acceleration scores still requires the declared interpretation or bridge; no metric, knot convention, refinement rule, or execution permission changes here.

**Measured anchored-reconstruction containment:** the standalone verifier `scripts/eom/verify-f6c-accepted-frame-reconstruction.py`, SHA-256 `80a96ebd0b306148b3eb96cb12e797c5cf80942e52ea457a8c6a72d58e8618a0`, passed independent source review, all 25 local controls, and 210 additional exact-algebra comparisons using generic rational equation solving, binomial translation, and power-to-Bernstein conversion. Its actual frozen-export run completed successfully in `6.407681541983038` seconds through publication. The 16,663-byte receipt `.local-data/braid-analysis/f6c-accepted-frame-reconstruction-20260827.5o7jK3/reconstruction.json` has SHA-256 `7c30aae03d43f7720b79288a19a9c9f9a7c0ab6b7b16ac9a948828ca80b92b43`. Main-process admission verified the fresh successful exit, completion hash, intended output, and final elapsed time. A separate read-only audit then reproduced every actual comparison directly from the frozen export without importing the verifier or another oracle.

Plainly: the actual saved data now pass the specified reconstruction check, and a second independently written calculation confirms the result. A private staging file or an unobserved success flag was not used as acceptance.

All 591,360 original per-axis/scalar allowance comparisons pass: 12,800 anchored prehistory pieces, 12,792 internal past joins, eight exact release anchors with all 48 position/velocity components, 640 accepted-frame cubics with 7,680 exact endpoint identities and 632 future joins, and 1,280 future containment cells covering `[0,0.13]`. The independent audit exactly matched all eight worst-ratio records and their control, allowance, member, segment, axis, and control-index fields. The largest allowance fractions are approximately `0.0000065837970924208615` for past position, `0.00000010308976668197803` for past velocity, `0.006583432661219392` for future position, and `0.019922674408724843` for future velocity. These decimals summarize exact rational inequalities retained in the receipt.

Plainly: the fixed frame-derived future fits within about two percent of even its tightest velocity allowance, and the anchored past also fits. The defined reconstructed family is now proved nonempty and contained in the original enclosure family. This does not identify it with the historical EOM trajectory or authorize roots, acceleration metrics, score use, or a new evolution.

**Measured uniform history guards:** `scripts/eom/verify-f6c-retained-history-guards.py`, SHA-256 `efaed33a6d6e55be5788ffb7e4e6f596fbc0381466a8308154dbd550743896b9`, passed separate independent source review, all 28 local controls, and 171 additional exact-rational checks. The actual run completed successfully in `9.80751150008291` seconds through publication. Its 88,241-byte receipt `.local-data/braid-analysis/f6c-retained-history-guards-20260827.hdrqLF/guards.json` has SHA-256 `86d7fa14ac64ee20930094ff1a59880fe4e1ef5c81758f5d8baf2c6777ee4880`. Fresh completion, successful exit, elapsed time, intended output, and hash were checked before acceptance. Independent read-only recomputation then reproduced all 55,040 exact comparisons directly from the original export without importing the instrument: 28,160 speed checks, 8,960 same-time clearance checks, and 17,920 oldest-boundary sign checks, using both original allowance forms.

Plainly: the saved uncertainty boxes satisfy the three uniform geometric conditions needed by the reviewed finite-history theorem. These are whole-interval checks, not selected-time samples.

The audit exactly matched all 160 closed reception cells and member-segment indices, all 184 allowance-mode-specific member/pair extrema and locations, and comparison-chain SHA-256 `af06f5c5bc488f3841792c3c737c54d5c52368ca4c58ce3f57823824fba30816`. The largest squared-speed upper bound is approximately `0.7198796791107773`, below one; the smallest squared-separation lower bound is approximately `0.07413607715371087`, above zero; and the smallest oldest-delay-squared minus distance-squared margin is approximately `62.74745333127485`, above zero. These are rounded summaries of the exact rational comparisons. Clearance is checked for all 28 unordered pairs with an explicit 56-ordered-pair mapping; the oldest sign is checked separately for all 56 ordered pairs using unrestricted distance bounds, without imposing the root equation first.

Plainly: every allowed coherent history remains slower than its wake and separated from the other members, with the required causal sign at the oldest saved boundary. The separately accepted reconstruction supplies a nonempty represented subfamily. Historical trajectory identity, the interpretation needed for score use, explicit continuous-time root boxes, the residual enclosure, and the three-rung measurement remain open. No root evaluation, acceleration value, score change, or EOM execution follows from this guard receipt.

**Derived peak-evaluation clarification:** independent review resolves the frozen theorem's pending knot question against the actual predeclaration, which requires analytical Hermite differentiation on each accepted-frame interval and defines the peak as a supremum. Under the theorem's common-history and strict-guard hypotheses, each nonself root varies continuously and every acceleration denominator remains positive, so the law-side acceleration is continuous. On each closed frame piece, the norm of the residual between that acceleration and the piece's Hermite second derivative is continuous. The maximum over these finitely many closed pieces therefore equals the supremum over their open interiors and their essential supremum: endpoints are limits of interior values, and values arbitrarily near a maximum occupy positive-length intervals. Both internal one-sided limits must be included; only inward limits apply at the two measurement endpoints.

Plainly: including both curvatures at a shared frame boundary evaluates the already specified peak; it does not add a new metric or require choosing one side. A boundary maximum can be approached from within the measured interval.

The closed-interval notation is shorthand where a classical global second derivative does not exist at a knot. Assigning either one-sided residual vector or a convex mixture there preserves the same supremum. An arbitrary isolated value above the piecewise bound would change the ordinary supremum but not its essential supremum or RMS integral; such a spike is not supplied by the required Hermite differentiation. Because the chain has continuous velocity, its distributional second derivative has no Dirac impulse. This is a mathematical regularity statement, not an added physical mechanism. The frozen theorem's original proposal label remains unchanged; this independent clarification resolves its evaluation question without changing the predeclaration, normalization, tolerances, refinement, subject interpretation, or execution/score authority.

Plainly: the peak calculation has a precise existing meaning. The family-diagnostic subject is resolved below without selecting another history; stronger historical-score attribution remains separate.

**Independently reviewed continuous-reception interface:** [continuous_reception_roots.py](../../../../scripts/eom/oracle/continuous_reception_roots.py), SHA-256 `f38657eedb585f6066bf233cef05508ef4d4336146dbf1e44501dfa9b669e04c`, implements the frozen theorem's conditional root-box check without changing its scalar references. The 35-control test generation has SHA-256 `473cba3b039027879eeea6987515261faaadcf0833f3e4d2864fc610f5b7a144`; independent review reran all 35 in 1.266 seconds. The earlier mathematical review checked 750 exact root, distance, and normal-factor inclusions across 75 separately constructed three-dimensional affine families. The interface evaluates unrestricted whole-face residuals before root-only intersections, includes every touched closed original piece and both sides of each knot, requires the complete ordered-pair/time partition, preserves self exclusions, and enforces 90-digit arithmetic and the unchanged transmitter-factor floor.

Plainly: the missing whole-time root-checking interface is now reviewed on known mathematical examples. This does not yet say that the actual asymmetric counter-breathing representative root boxes have been proposed or accepted.

Review also corrected a capacity limit that would have excluded the declared finest asymmetric counter-breathing representative rung. The bounded interface now accommodates 8,192 pieces per member, above the nominal 7,040-piece requirement, and 16,384 reception cells, above the conservative 11,832-cell partition count including all members' original knots. These are implementation capacities, not enlarged refinement allowances or cost measurements; an actual census exceeding them remains unresolved. Histories must be a bounded sequence and are indexed only after the member limit is checked. No actual asymmetric counter-breathing representative history was evaluated in this library batch. Supplied common-history, subject-membership, uniform-speed and clearance premises remain explicit hypotheses requiring separate authentication; the interface grants no subject, execution, metric or H3 authority. Source-bound actual root-box coverage, the residual/quadrature/peak consumer, and the complete three-rung measurement remain outstanding.

Plainly: the interface can hold the already required refinement without relaxing its rules. Connecting it to the saved asymmetric counter-breathing representative record still requires the separate data and premise checks, followed by the actual measurement work.

**Measured synthetic performance and integrity repair:** immutable call-local history snapshots replace repeated whole-history validation without changing the arithmetic or closed-piece unions. An independent eight-member, 1,760-piece-per-member, one-cell synthetic check took 11.9026 seconds before the repair and 0.7071 seconds afterward, both with counters. Both made 448 state-enclosure calls; structural validations fell from 456 to eight. A separate uninstrumented optimized run took 0.6287 seconds. Forty-eight separately constructed affine families supplied another 3,840 exact checks. These synthetic measurements do not predict actual asymmetric counter-breathing representative cost.

Plainly: the checker no longer repeatedly validates the same saved history during one call. The speed improvement preserves the mathematical work, but the actual-data cost must still be measured.

The final independent review also resolved a mutable premise-record counterexample. Speed, clearance and digest bindings now require exact immutable tuples; the same checked premise object is used throughout and retained in the result. Fifteen independent during-call and after-call mutation attempts passed. Removing only the final guards and three added controls reproduced the preceding reviewed source and tests exactly, confirming that the history, arithmetic and coverage paths did not change in that repair. The final source and tests were frozen before any actual-data consumer implementation. Altered source bytes, a mutable accepted premise, or a missed exact inclusion would overturn the corresponding acceptance; authenticated premise truth and metric authority remain separate.

Plainly: a result now keeps the assumptions that were actually checked. This protects the record; it does not prove those assumptions true.

**Derived reconstruction-family attribution:** independent read-only review resolves the next data-only subject without an operator-selected replacement history. Write $\mathcal F_H=\{Y_{\rm past}\cup H_{\rm future}:Y_{\rm past}\in\mathcal P^H\}$ for every anchored admissible past joined to the already specified accepted-frame Hermite future. The accepted reconstruction proves $\varnothing\ne\mathcal F_H\subseteq\mathcal E$, where $\mathcal E$ is the original coherent-history enclosure family. A valid law enclosure over all of $\mathcal E$ therefore also bounds the law on every member of $\mathcal F_H$. Subtracting that law enclosure from the prescribed $H_{\rm future}''$ bounds the same-history residual for every allowed anchored past. No favorable past is selected, and no original box is replaced or narrowed in the law evaluation.

Plainly: the next diagnostic can cover every permitted past while using the future interpolation already fixed by the measurement recipe. Defining that conservative family-wide diagnostic does not require another geometry or interpretation choice from the operator.

The original harness uses its analytic harmonic function to construct cubic prehistory and passes retained histories, not an analytic callback, to the EOM solver. Saved frames are midpoint samples of retained position/velocity enclosures. Consequently, intended analytic-seed containment is a separate conformance claim, not a prerequisite for this retained-data family result. Historical attribution remains distinct: the available position/velocity containment proves neither membership of a particular historical past with the exact anchors nor curvature equality with $H''$. Small rapidly varying perturbations can preserve position/velocity bounds while changing acceleration. A family-wide residual must not be relabeled an exact historical-trajectory residual or automatically transferred to historical score authority. Actual complete law enclosures, RMS/peak bounds and the unchanged refinement requirements remain uncomputed; changing the historical score subject would require separate authority.

Plainly: the technical calculation may proceed at its stated family scope. Exact historical identity and score attribution are stronger claims, and a user-selected substitute past would not prove them.

**Independently accepted next protocol:** the [continuous-reception root-cover predeclaration](2026-08-27-f6c-continuous-reception-root-cover-predeclaration.md), SHA-256 `765e6663cdd60323f84b9e1af52ba1399345322eb747727f2a0898b4dd0fd079`, fixes the exact 161-knot, 160-cell original reception partition and broad emission proposals. Independent review accepted its unrestricted-face derivation, reconstructed-family scope, complete 10,240-row obligation, per-cell library use, compact reconstructable piece evidence and inclusive stopping/publication rules. These are derived obligations, not completed rows. The independent actual-data comparison must be frozen before the separate consumer; a reviewed pilot launch plan must also fix numerical resource limits. Only then may one complete cell be evaluated and independently checked, with its measured cost informing a separate full-run decision.

Plainly: the next calculation is specified without changing the geometry or selecting a favorable past. Its implementation, first-cell cost measurement and full coverage remain unfinished; accepting the protocol grants no launch or metric authority.

**Independently accepted actual-cover comparison reference:** [verify-f6c-continuous-reception-root-cover.py](../../../../scripts/eom/verify-f6c-continuous-reception-root-cover.py), SHA-256 `2d25103e0fb6ab584485b7954465afe0fa5de556b3a7e111c56d20156b7011fd`, is frozen before its separate consumer. Its 35-control suite has SHA-256 `5f501e0b8cf60030d214fc9637e1292faa93a615c396e787ef77fc7b261991c5`. Independent review checked 400 exact Bernstein identities, 2,200 exact polynomial point inclusions, and 80 distance-intersection cases without importing the root-cover subject implementation. The reference independently reconstructs complete closed-piece coverage from original tokens, uses exact rational squared inequalities for face and norm bounds, establishes nonempty distance intersections before using them in factor checks, and requires exact row/piece census and unchanged false authority flags. It grants only conditional reconstructed-family enclosure conformance after source, deadline and publication admission.

Plainly: a separately authored checker can now test the proposed asymmetric counter-breathing representative coverage against the original history mathematics. Its synthetic controls do not yet establish that the actual asymmetric counter-breathing representative coverage passes.

The final independent review found and closed two operational defects: a JSON `null` at the end of a stream no longer masquerades as end-of-file, and a positive exact budget too small to produce a positive advancing machine timer is rejected before input capture. The author recorded six failing baseline subcases before repair; the independent reviewer reran all 35 final controls in 0.709 seconds and confirmed that reversing only those two changes reproduces the preceding source hash exactly. The mathematical paths and schema were unchanged. The frozen root library, arithmetic, history interface and predeclaration remain untouched. The separate consumer, external watcher and exact launch-manifest reviews are recorded below; actual one-cell coverage and comparison are separate obligations.

Plainly: the last repairs protect complete record accounting and a real time limit. They do not change the geometry or turn a synthetic check into an actual-data result.

**Independently accepted separate root-cover consumer:** [prepare-f6c-continuous-reception-root-cover.py](../../../../scripts/eom/prepare-f6c-continuous-reception-root-cover.py), SHA-256 `4ce6436c09c445030192aeb5b894239b7fa04cee578e6067f1088151695a5e9e`, and its [25-control suite](../../../../tests/test_f6c_continuous_reception_root_cover_preparation.py), SHA-256 `68a940c40b2e3b463555b95858031f96796e2ac94963a86b3a9ae6fd74dc3742`, passed independent read-only review. The reviewer reran all 25 controls in 1.970 seconds and independently checked 2,240 exact affine-history distance, factor and strict-face inclusions across eight synthetic cells, including comparison with the unchanged rational checker. A truncated same-descriptor input was rejected and closed. Main review read both complete sources and reran the 25 controls in 2.075 seconds. No actual asymmetric counter-breathing representative data were evaluated.

Plainly: the separate program preserves original allowances, identities and closed-piece coverage and passes known-answer checks. That is readiness to try one real cell, not evidence that the real cell already passes.

The consumer executes three captured library modules in a private package and emits only the closed conditional manifest, row and piece schemas with `accepted:false`. Default scope is cell zero; full scope requires an explicitly matching separate plan. The independently reviewed [one-cell resource plan](2026-08-27-f6c-root-cover-pilot-resource-plan.md), SHA-256 `36b72681c116cedf1803cc89ead8b48a7d9604bae7f9bffd7b0f95b33c3bb9b4`, fixes one consumer followed by one comparison, a single inclusive 1,800-second limit, 15-second external heartbeats, at-most-one-second aggregate RSS samples with a 2 GiB stop, host memory/disk floors, and fixed output/log bounds. These are limits, not measured costs. The registered external watcher and exact machine-readable launch plan must pass separate review before the actual cell; no automatic full run or retry is permitted.

Plainly: the next work is a supervised, independently checked first-cell attempt. Neither test counts nor a launch plan complete the continuous-time root obligation or authorize a dynamics score.

**Independently accepted first-cell operational composition:** [run-f6c-root-cover-pilot.mjs](../../../../scripts/eom/run-f6c-root-cover-pilot.mjs), SHA-256 `6b9c1b6489214039e8de485eb5bfdbbc1e896c0433bb8cdebb12acf629fcf00a`, and [launch-f6c-root-cover-pilot.mjs](../../../../scripts/eom/launch-f6c-root-cover-pilot.mjs), SHA-256 `a0ce2d3eeeb248b43cda5bb8ebcca4b0c6544af8093d28bae1c233c53236d705`, passed full main source review and all 32 synthetic controls in 5.887033167 seconds. The controls are frozen at `3855c2d1211c0834d93e1e4b94b3c3ce55301ca05caf306e8e8554441544f0fd` and `e210e69057908a3d036eb2055fa74323ca594fea6a02d7b75eb2276a8e88dcfc`. Six controls exercise real registered synthetic child processes: sequential closures, first-target failure, slow final publication, startup interruption, lost monitoring and log failure. Final stdout is awaited within the original deadline and final RSS-observation gap; a stalled output stream is destroyed on failure. The runtime inventory includes both loaded `__future__` files. Neither repair touched a mathematical source.

Plainly: the supervisor has been checked against hangs, interruption and misleading partial success. These tests contain no actual asymmetric counter-breathing representative calculation.

The separately accepted [exact one-cell machine plan](2026-08-27-f6c-root-cover-pilot-launch.v1.json) has SHA-256 `bcac71226b855efee534ffa29fb13e6148068d2663708f0d29e9d201ab1a9e3d` and 45,245 bytes. Independent same-descriptor review authenticated all 181 plan/fixed bindings plus the two operational-control files: 183 distinct files totaling 15,561,142 bytes. Its 158 runtime bindings cover 156 metadata-loaded files, the shared-venv configuration and resolved Git. Main independently verified that the absent intended output is under the ignored, canonical lane using the hash-bound Git executable before creating that lane; the consumer repeats its own ignored-output check. The output will be a new `pilot-cell-0-v1` child. Actual coverage, independent comparison and measured cost remain separate from this launch admission. A changed source, incomplete runtime inventory, failed process closure, missed bound, or late final flush invalidates admission; no automatic retry, full run, historical-trajectory identity, score or EOM evolution is authorized.

Plainly: the program, data and runtime are now fixed for one supervised attempt. A passing launch review is not a passing scientific result.

### Accepted Actual asymmetric counter-breathing representative One-Cell Cover And Full-Run Resource Return

**Measured, source-bound conditional result:** the actual `pilot-cell-0-v1` attempt completed with fresh successful launcher output and exit zero in `22.353614791` seconds. The frozen consumer produced all 64 pair-cell rows on `[0,0.001]`: 56 ordinary nonself rows and eight self exclusions, with 112 distinct nonself face checks, 112 compact geometry-piece records and 89,208 returned geometry-piece visits. The separately frozen exact-rational comparison accepted the complete conditional cover. A subsequent independent read-only audit imported neither consumer nor reference, checked complete raw-record identities, interval signs and census, normalized the equivalent original binding representations, and reauthenticated 195 distinct files totaling 16,223,099 bytes. Both registered stages and their two gates closed with exit zero, no signal and empty final owned-process census.

Plainly: the first actual time cell passes the separately written checker. This is a concrete result for the anchored-past/fixed-future reconstructed family, not a claim that the saved fitted future is the historical EOM trajectory.

All files below remain under `.local-data/braid-analysis/f6c-continuous-reception-root-cover-20260827/pilot-cell-0-v1/`.

| Actual artifact | SHA-256 | Bytes |
| --- | --- | --- |
| `subject/rows.ndjson` | `786785b2597bcdf024e350ba89c129fb32115afed693169a6db3137c6bdca383` | 136,659 |
| `subject/pieces.ndjson` | `2c064a5956e7684868cbda7aa7e312ac609e07760bf67f1cf121c934d6d4c411` | 44,394 |
| `subject/cover-manifest.json` | `a91b88191483826111653a330c70cafe7b7bae52b1eec296af2339d0d150d2b1` | 41,385 |
| `comparison.json` | `8cb1699f43ab70baa660942b3a351f9c2f2ee48b363255a7ee9a425ec43b0d94` | 41,486 |
| `pilot-admission.json` | `1514c3226ea7d87f350b74fa9865025d9693bdf69b957e2d99d6496212a13699` | 302,171 |

Plainly: the first three files preserve the conditional calculation; the fourth is its independent comparison, and the fifth records their supervised handoff. File contents alone do not replace the separately observed successful launcher exit.

Independent resource review confirmed 99 aggregate RSS samples, a maximum of 376,569,856 bytes, maximum sample gap `0.279311208000001` seconds, 263,924 named subject/comparison bytes and 50,617 operational-log bytes. Final observation-to-closure gap was approximately `0.071520917` seconds. Consumer and comparison reported elapsed times of `17.39154341700487` and `3.9261386250145733` seconds; the whole-attempt time above includes both plus capture, publication and cleanup. Recorded coordinator, entry, Python and registered-gate CPU totals were `22.072105` user seconds and `0.301161` system seconds, excluding monitoring-probe CPU. The immutable admission's 98 samples and `22.230326916000003` seconds are explicitly pre-publication observations; they are not substituted for the fresh final measurements. Main rehashed final outputs and all 182 receipt source bindings after the original CLI closed and confirmed the shared lock was absent.

Plainly: the pilot stayed inside its limits, and the resource figures describe their actual measured scope. They do not predict the full run's cost or imply a continuous hard memory cap.

**Independently accepted resource return:** the [full-run resource disposition](2026-08-27-f6c-root-cover-full-resource-plan.md), SHA-256 `46a827d13a5e8f7a068e73e642f74d679ebf18e0b2e8f42ab53aab4de26598ef`, preserves the governing test: `160 × 22.353614791 = 3576.57836656` seconds exceeds the unchanged 1,800-second full-run allowance. That prescribed extrapolation is neither an upper nor a lower bound on actual full cost, but it fails the declared launch rule. The separate full attempt was not launched; all its 10,240 rows remain `not-run`. Its unchanged 160-cell target, serial worker count, file/memory limits and frozen mathematical sources are preserved. External acceptance here closes the audit labels recorded as pending when that immutable return document was drafted.

Plainly: the next full calculation is stopped by its existing resource rule, not by a geometric failure. A separately reviewed performance investigation or separately authorized protocol change is needed before reconsidering it. No full coverage, acceleration measurement, historical identity, H3 promotion, metric, score, evolution, retention, stability or physical claim follows from this pilot or resource return.

### Independently Accepted Call-Local Cache Reference

The separate [cache-equivalence argument](2026-08-27-f6c-call-local-state-cache-equivalence.md), SHA-256 `798858e87058b5a1a2d478c89edad3154a2e4993f3c14cab089b4aabf3434ee3`, and [cached reference successor](../../../../scripts/eom/oracle/continuous_reception_roots_cached.py), SHA-256 `daa4cc227cb8685de673fc400d817a19666b4fc7323e6c3a56f475a463b23acf`, passed independent main review before any consumer or binding changes. The [28 new controls](../../../../tests/test_eom_continuous_reception_roots_cached.py) are frozen at `a5ac7c8b26c5d0a193f20305f4bdbad93939756780bdaefd9cbf569f42a487eb`. Main read the full source delta, unchanged state/formula paths, arithmetic premises, proof and controls; all 63 baseline-plus-successor tests passed in 0.969 seconds. A separately constructed exact-rational check additionally passed 5,760 cubic enclosure endpoint inclusions across 96 synthetic histories and 480 known-root/distance/factor inclusions across twenty common-velocity cases, including non-axial motion. It evaluated no actual asymmetric counter-breathing representative data.

Plainly: the optimization has an independently checked mathematical reason to preserve the answer. The extra tests use invented histories with known answers, not the real asymmetric counter-breathing representative evidence.

The derived equivalence is limited to immutable captured history identity, exact Decimal endpoint representations and fixed arithmetic. Only successful state computations are cached, all time validation remains, and the 32-entry cache is discarded across cells and calls. Formula bodies, complete closed-piece unions, original limits, row ordering, failed prefixes and all false authority flags are unchanged. The author measured synthetic eight-member median function times of approximately 1.180 seconds before caching and 0.185 seconds afterward; these exclude whole-attempt overhead and are not an actual-data capacity claim. Baseline library, mathematical dependencies, consumer, comparison, declaration, launcher and prior pilot remain byte-identical. Separate binding/protocol and consumer batches plus a fresh admitted actual pilot remain necessary; the existing full-run resource return is not lifted by this reference review.

Plainly: this closes the optimization's reference review only. It neither changes the accepted old result nor permits a full run based on synthetic timing.

**Independently accepted successor comparison/binding batch:** the [cached-root-cover declaration](2026-08-27-f6c-cached-root-cover-predeclaration.md), SHA-256 `7c2a8b0bb06f46da158e0dfe2cb313dd72e2edff3c411e87c1588aa6d028f9e4`, [comparison successor](../../../../scripts/eom/verify-f6c-cached-continuous-reception-root-cover.py), SHA-256 `19c57e9b638b0beb866c86b061b2325f9567add2a85608f0c42ef1f7612d9132`, and [controls](../../../../tests/test_f6c_cached_continuous_reception_root_cover.py), SHA-256 `2fd2080b3b4facdc80b85cdc65610c2bfeefdd8eab5f7234e207d3d4908bc117`, passed independent main review before a successor consumer was implemented. Main read the complete delta, declaration and controls, reran all 42 tests, and independently checked that only four top-level binding assignments changed from the frozen comparison. Every numerical, capture, stream, publication and closure function remains unchanged. All twenty fixed original bindings were independently rehashed; the enlarged list explicitly preserves the baseline and resource-return provenance. The new declaration retains the exact scientific target, all prior limits and `160 × fresh pilot whole-attempt time <= 1800` launch rule. It requires a fresh reviewed consumer/supervisor and actual one-cell pilot; neither the old pilot nor synthetic timing grants full-run authority.

Plainly: the independent checker now has an explicitly reviewed address for the optimized library, without changed mathematics. Connecting a separate consumer and measuring its real cost are still separate steps.

**Independently accepted separate cached consumer:** [prepare-f6c-cached-continuous-reception-root-cover.py](../../../../scripts/eom/prepare-f6c-cached-continuous-reception-root-cover.py), SHA-256 `af53f5af2f9dd7eda4869af2a7533f869f4e3866003c90bf9a8487b2e5636386`, and its [33-control suite](../../../../tests/test_f6c_cached_continuous_reception_root_cover_preparation.py), SHA-256 `9abc7c3a80ad670e7bc7ad9f94a95f1fcd8924de425991032d6d26bba3372427`, passed independent main review after the comparison/reference batch was frozen. Main read the entire source delta and controls, reran all 33 synthetic tests, and independently verified that only six top-level binding assignments differ from the preserved consumer; all function bodies and the twenty-entry fixed-binding structure match their declared source/reference. The private module alias remains `continuous_reception_roots`, but captured filename and SHA-256 explicitly select the cached successor; a poisoned global alias cannot substitute its code. All original 25 consumer obligations plus eight new binding/alias controls pass. No actual history or root-cover evaluation was performed by this review. The separately reviewed operational composition and exact runtime plan remain prerequisites of a fresh pilot.

Plainly: the separate consumer now selects the approved optimization without changing its data handling or mathematics. This is software acceptance, not another measured asymmetric counter-breathing representative cell.

**Independently accepted cached pilot composition and machine plan:** the separate [entrypoint](../../../../scripts/eom/run-f6c-cached-root-cover-pilot.mjs), SHA-256 `51cd543f9f28d87299a59614805cdbf949dceabcabad1a64a701b0b2b8ea6c57`, [launcher](../../../../scripts/eom/launch-f6c-cached-root-cover-pilot.mjs), SHA-256 `03ef68dbdc9e578402771490c274ab246f02688c443f8ecba8d3006f3d35a5ec`, and controls `ad50f3dcb230e3a5181c5ac205f55b29584689eb8a434a7adbd01cd23dc856d9` and `f0055410c58fbfce291c235a0cd03339a742923619fa1bc6063fe294fb1c5411` passed independent review. All 32 original obligations remain, with five new binding/address controls; all 37 passed. Main additionally read the final source and control deltas and reran the 31 unit controls. The independently reviewed [machine plan](2026-08-27-f6c-cached-root-cover-pilot-launch.v1.json), SHA-256 `5f5afcced38878828d65e0c5482f1764092f6449c2cba36ac6b99a1bbf9f9f86`, binds 188 distinct source/runtime files totaling 15,662,723 bytes before the plan and operational-control files. Its 158 runtime bindings exactly match the earlier runtime generation. All twenty fixed comparison premises, original limits, pilot-only scope, shared lock, old/new competitor exclusion, private source capture, final-output flushing and cleanup obligations remain intact. No actual computation occurred in these reviews.

Plainly: the optimized pilot was independently cleared as a separate program with a separate exact launch plan. It did not inherit permission from the old pilot's output.

### Accepted Actual Cached asymmetric counter-breathing representative Pilot And Full-Run Planning

The fresh optimized cell-zero attempt completed successfully in original caller session `59688`, final completion chunk `95bd96`, with exit zero and whole-attempt elapsed time `8.534247625` seconds. The separately authored rational comparison independently accepts the same 64 conditional pair-cell rows, 56 nonself ordinary rows, eight self exclusions, 112 strict faces and 112 compact piece records. The result applies to the unchanged reconstructed family, not an identified historical EOM trajectory. The old 22.353614791-second pilot and its resource return remain preserved.

Plainly: the optimization made the same first-cell calculation faster without changing its scientific target. It has not yet completed the remaining cells.

Independent actual audit authenticated the exact 189-entry source closure totaling 15,708,017 bytes twice, matched it against the reviewed program and machine plan, and checked the final output/resource chains. Including baseline streams and controls, it audited 204 distinct files totaling 16,532,124 bytes. The new manifest is `19fae257f7f36d858fa60d9031125b3f29dbb8780e944802699aab5292275f4c` (42,932 bytes), comparison `6bf2b50ef4f0b46f43ae77a9881f82a2f9d504d5df757bc0ad215deb8eac36c6` (43,397 bytes), and admission `1a814c90279eed456546b2c4959a8504657213ffc2d25c063060831814e930ee` (311,695 bytes). Full paths and all seven artifact hashes are frozen in the [new full resource plan](2026-08-27-f6c-cached-root-cover-full-resource-plan.md), SHA-256 `daeb71bee6260c38a6b7e5e6237110216d9315807fe23602fbd7cfcdddc5866b`. The row and piece streams are byte-identical to the baseline; that agreement establishes deterministic preservation only. Independent mathematical authority remains the separately authored rational comparison.

Plainly: the reviewed result is tied to the actual new run. Matching an old file is not being substituted for an independent mathematical check.

Fresh final measurements were 44 aggregate RSS samples, maximum sampled RSS 343,523,328 bytes, maximum sample gap 279.5677919999998 milliseconds, final observation-to-closure gap 66.160167 milliseconds, 267,382 named raw-output bytes and 22,521 operational-log bytes. Non-overlapping recorded coordinator, entry, Python and registered-gate CPU totaled 8.237288 user seconds and 0.235115 system seconds, excluding monitoring-probe CPU. The admission's 43 samples and 8.427305542000001 seconds are explicitly before publication, not final measurements. Seven owned process IDs and the shared lock were independently confirmed absent; main also rehashed all receipt source bindings and final outputs after the original successful exit.

Plainly: the complete attempt, including its checker and cleanup, stayed inside the unchanged limits. These sampled measurements are not a hard continuous memory guarantee.

The new full resource plan independently passes the unchanged declared arithmetic: `160 × 8.534247625 = 1365.47962` seconds is below 1,800 seconds. The product is an extrapolation, not a full-cost bound. All 160 cells must be recomputed in a separate attempt, with no pilot-prefix reuse, cap increase or additional worker. The earlier baseline resource return is not rewritten. Full operational composition and exact machine-plan review are the remaining launch prerequisites; the full attempt is not-run at this handoff. No acceleration, residual, quadrature, peak, metric, H3 promotion, score, ordinary evolution, historical identity or physical claim follows from this pilot or resource-plan acceptance.

Plainly: the measured timing obstacle is removed for the new implementation. The full-run safeguards still need their own final clearance before execution.

**Subsequent independent full-launch clearance:** the separate full [entrypoint](../../../../scripts/eom/run-f6c-cached-root-cover-full.mjs), SHA-256 `1398a005510480d073d3882c7b9508b1cd2f91f0d7bb7ae5757b4893ed73352b`, [launcher](../../../../scripts/eom/launch-f6c-cached-root-cover-full.mjs), SHA-256 `0f11fe5e51ef52f95c02605b776fe6b94c72a67a4a5f2b69671870f5f548ae17`, and controls `80f70033a282831e7e0b5ec3ac63d959ac1b4d2de010d43d8449cdf0dee5cfaa` and `74c59ed2c8f0693a14a1015f9be75b393a4a55c9d079382acb0c3a6ac5f728d8` passed independent review and all 40 controls. The original 32 operational obligations remain, with eight full-scope/census/binding controls. Main separately read all source/control deltas and reran all 34 unit controls. The only operational changes are full scope, exact census/progress bounds, addresses and resource/pilot bindings; mathematical programs, all limits, process ownership, final flushing and cleanup are unchanged. The separately authored [full machine plan](2026-08-27-f6c-cached-root-cover-full-launch.v1.json), SHA-256 `5dd7e27084a2e8e5b2c3ed8daf8cf66248a437108710ce91281977e728197ddc`, independently passes exact source/runtime and scope review: 197 distinct pre-plan bindings totaling 16,318,565 bytes, including all 158 unchanged runtime bindings and all measured pilot inputs. The fresh ignored output is `full-cached-v1` under the existing root-cover lane; it and the shared lock were absent at admission. These reviews clear one separately watched attempt, not its outcome or any onward authority.

Plainly: the full-run launch prerequisites are now closed. Only the actual complete run and its independent final audit can establish full coverage.

### Independently Accepted Actual Full asymmetric counter-breathing representative Conditional Cover

The separately cleared full attempt completed successfully in original caller session `13512`, final completion chunk `c21aa7`, with exit zero and fresh whole-attempt elapsed time `862.951823625` seconds. Independent post-closure review accepts all 160 closed reception cells, 10,240 pair-cell rows, 8,960 ordinary nonself rows, 1,280 self exclusions, 17,920 strict faces and 17,920 compact piece records. The independently authored rational comparison owns the mathematical root-cover result. The post-closure audit did not rerun the numerical kernels; it independently reconstructed all eight original history digests, the exact 161-knot and 81-frame grids, all closed clipped-piece hashes and indices from the original 14,080 segments, including 18,144 singleton-endpoint visits. The 14,639,800 recorded geometry-piece visits are an evidence census, not a runtime cost model.

Plainly: the complete reconstructed-family root cover now passes, including every required pair and shared endpoint. This closes the full-coverage blocker without changing the geometry or claiming that the reconstructed curve is the historical EOM trajectory.

All outputs are under `.local-data/braid-analysis/f6c-continuous-reception-root-cover-20260827/full-cached-v1/`. The independent audit derived the expected source closure from the reviewed entrypoint and machine plan, not the output receipt: 198 files totaling 16,363,847 bytes, rehashed before and after. Including controls and outputs, it checked 211 distinct files totaling 48,675,990 bytes. Main separately verified the same exact source closure and seven final output bindings.

| Output | SHA-256 | Bytes |
| --- | --- | ---: |
| `subject/rows.ndjson` | `28491edb2f1faec7adf248f535d29a1600b8bd69f5a46706fd26dbb3eb848b5c` | 22585784 |
| `subject/pieces.ndjson` | `b3a2ddf2c8cd5b586ef7b374eee94afc395f63496c849ec574e71bf1f487a9ab` | 7505144 |
| `subject/cover-manifest.json` | `61b0cdfad85696a0b5ead7df838119c9005a28656e9ac3daa26df139054410e2` | 42922 |
| `comparison.json` | `1c423aece2009a2d7d0852e9558c16464c640abbc5bea3743211af3805b6eed2` | 43377 |
| `full-admission.json` | `8fe8f0f9651fd8de15467a69f0534f08bbe19e0e3fdb64a86c6422be857eb77f` | 332567 |
| `launcher-stderr.log` | `b976d8deb556d8faba5a3aff73a09b77ec26c6da84e42726167eec4ec7a43314` | 30969 |
| `resource-observations.ndjson` | `66eb0cfa1811d0a834d18d3bd8e749a941e1964f7276898b80a4e12136d69d03` | 1710278 |

Plainly: these fingerprints identify the actual completed run. Any changed source, missing pair, altered piece mapping or mismatched output overturns this scoped acceptance; the named evidence and frozen independent comparison are the places to check.

Fresh final resource measurements were 3,447 aggregate RSS samples, maximum sampled RSS 378,322,944 bytes, maximum sample gap 444.0009999999893 milliseconds, final observation-to-closure gap 56.200916999951005 milliseconds, 30,177,227 named raw-output bytes and 1,755,092 operational-log bytes. The consumer took 472.00690241600387 seconds and the comparison 389.47123654186726 seconds. Non-overlapping recorded coordinator, entry, Python and registered-gate CPU totaled 852.587968 user seconds and 5.704341 system seconds, excluding monitoring-probe CPU. All 62 host observations remained within the declared floors: minimum free-memory percentage 53 and minimum free disk 964,136,280,064 bytes; 57 external heartbeats were recorded. The stored admission's 862.577186208 seconds and 3,444 samples are explicitly prepublication, not final closure measurements. All seven core process IDs, all 60 observed owned IDs including probes, and the shared lock were independently confirmed absent.

Plainly: the entire calculation, independent comparison and cleanup finished in about 14.4 minutes, below the unchanged 30-minute limit. Memory figures are sampled observations, not a continuous hard cap. The baseline resource return and earlier pilots remain unchanged.

This is conditional reconstructed-family root coverage only. Acceleration/residual evaluation, quadrature, peak enclosure, the independent three-rung measurement and historical-score attribution remain separate. All library authority flags and outer EOM, H3, metric, historical-identity and onward-full-run flags remain false. No score, ordinary evolution, retention, stability or physical claim is promoted.

Plainly: the next obstacle is measuring how the prescribed curve's required acceleration compares with the delayed law, not finding missing root-cover cells.

### Independently Accepted Continuous-Reception Acceleration Reference

The separate [conditional range reference](../../../../scripts/eom/oracle/continuous_reception_acceleration.py), SHA-256 `abfc21f29d8bdd984118b1e0ba0cb62b88a081a75a961052eb11f31ea7bdd7b8`, [controls](../../../../tests/test_eom_continuous_reception_acceleration.py), SHA-256 `26b7c5455a57da5beba6e7fd32a0b7bfbc8e1f32630b663c55a33273e8cc1823`, and [mathematical/interface declaration](2026-08-27-f6c-continuous-reception-acceleration-reference.md), SHA-256 `c1a5358e1d887fab5b4753368dc14ec59ed220294f42d2afa4ac40f962ee537f`, passed independent review before any actual-data consumer was implemented. Main read all three artifacts, reran all 30 tests in 0.120 seconds and independently checked 72 exact cubic-derivative endpoints plus six stationary signed-kernel inclusions. A separate reviewer also passed all 30 tests and 6,562 independently constructed interval-corner, polynomial, complete-member, adversarial and outward-rounding checks. All three frozen hashes remained unchanged.

Plainly: the new mathematical instrument has been checked against independently known answers. No actual asymmetric counter-breathing representative measurement was made by these synthetic checks.

The derived conditional result uses the original sharp acceleration law with no receiver-factor multiplier, exact rational intermediate arithmetic, exact accepted-frame Hermite second derivatives and outward 90-digit output bounds. It requires all 64 ordered pair rows, preserves eight excluded-self zeros and 56 nonself contributions, forms each member's complete acceleration and normalized residual, and encloses its nonnegative squared residual norm. Self coverage fields remain absent rather than acquiring invented audit hashes. The caller must separately authenticate all source bytes, frame/pair mappings, complete root premises and coherent-history applicability. All authority flags remain false; no root search, refinement, quadrature, peak, metric, historical identity, score, evolution or physical claim is supplied. A separately authored source-bound one-cell consumer and independent output comparison are the next implementation obligations; the reference remains frozen during those batches.

Plainly: this removes a mathematical implementation blocker, not the remaining evidence checks. The next program must prove that it feeds the reference the correct saved data.

**Subsequent independent consumer acceptance:** the separate [one-cell consumer](../../../../scripts/eom/prepare-f6c-continuous-reception-acceleration.py), SHA-256 `dca5f5b3b42b42347b0b6b49a8974663169171e2293c1eca9c4b4a8c7646a53e`, its [26 controls](../../../../tests/test_f6c_continuous_reception_acceleration_preparation.py), SHA-256 `616a4e9c464e95607140dd90b47a8288ae73183f74faa0cc5078b81b8c8767c8`, and [predeclaration](2026-08-27-f6c-continuous-reception-acceleration-predeclaration.md), SHA-256 `3ef8fb9020bae71833b1e06a119672b49a4beb5395f697dcb3d037d088e7891e`, passed independent review with the reference frozen. Main read all three artifacts and passed all 26 synthetic controls in 0.312 seconds. A separate reviewer passed them in 0.311 seconds, then independently checked eight member records, 1,088 projected row fields, 112 coverage pointers, 528 malformed projection cases, 23 plan negatives, six parser negatives and three publication/late-completion controls. The numerical-reference call was trapped during these independent mapping checks; no actual range was evaluated. All six consumer/reference hashes remained unchanged.

Plainly: the adapter is now independently checked to feed the correct original data into the frozen mathematical reference. Its separate output checker and guarded launch still need acceptance before the actual one-cell measurement; this review supplies no measurement or execution result.

**Integrated regression check:** after the full-cover and consumer handoffs, main reran the independent prescribed-worldline conformance and nonexecuting ordinary-request suites together: all 27 controls passed in 2.668479209 seconds. The baseline/cached root and acceleration-reference suites passed all 93 controls in 1.132 seconds, and the cached consumer/comparison suites passed all 75 controls in 0.982 seconds. The root-reference tests require the repository import path; an initial isolated-interpreter test invocation could not import that package, and the corrected standard test invocation passed without source changes. A separate five-owner status audit verified all seven completed full-output hashes and sizes, the complete census and unchanged claim boundaries. Whitespace validation passes. Strict content validation still reports zero errors and the same two unrelated generated-index warnings; no regeneration was performed.

Plainly: the completed coverage and new adapter coexist with the original geometry and frozen references. The two repository-wide index warnings remain separate from these passing scoped checks.

**Forward capability check, not refinement admission:** a separate read-only inspection confirms that the frozen root library accepts exact restricted reception partitions and pair-specific retained emission boxes, rechecking the oldest boundary and both unrestricted whole-face signs before enclosing geometry. The acceleration reference already accepts a subcell within one original frame. The existing [uniform-face theorem](2026-08-27-f6c-continuous-reception-enclosure-contract.md#uniform-root-boxes-not-scalar-samples) supplies this mathematical restriction rule; no new root formula is needed solely to use narrower boxes. The accepted campaign wrappers deliberately fix their original partition and broad emission boxes, however. If the actual range result is too wide, refinement would require a separate deterministic, source-bound protocol, independent comparison and consumer successors, and a fresh measured resource pilot. No refinement, tolerance choice or actual range evaluation occurred in this capability check. Original-frame and segment knots, complete pairs, unchanged uncertainty and the existing measurement/subdivision limits remain mandatory; narrower boxes need not meet the requested width or cost less.

Plainly: the underlying mathematics can support a tighter follow-up if the first measurement needs it. The current run will not silently change its settings, and tighter boxes are not being promised to solve the measurement problem.

**Subsequent independent output-checker acceptance:** the separately authored [range-output checker](../../../../scripts/eom/verify-f6c-continuous-reception-acceleration.py), SHA-256 `cc26f5a45d0e09a472e3066d0d62ae8192492a7c3e0ab18a3658781a0274b299`, and its [36 controls](../../../../tests/test_f6c_continuous_reception_acceleration.py), SHA-256 `be741dccccd90c349849b19dc15df1acd4ea5752ec6f8a9e98e7ae14013c52c6`, passed independent main review with the consumer and numerical reference unchanged. Main read the complete source, tests and deltas, passed all 36 controls in 10.232 seconds, and independently passed 480 exact checks: 288 sign-aware sharp-kernel cases and 192 direct cubic-curvature cases. These synthetic checks establish the comparison instrument's scoped correctness, not an actual asymmetric counter-breathing representative range result.

Plainly: a separately checked program can now test the acceleration output against independently derived answers. No actual acceleration or residual range has been calculated by this acceptance.

A separate mapping-only audit authenticated all 16 original files totaling 9,989,547 bytes, reconstructed eight history mappings, all 64 rows and 112 compact piece records, and checked the prior evidence chain. Numerical range functions were explicitly trapped, and every input was rechecked after the audit. The next step is independent review of the guarded launch composition and exact machine plan, followed by the one-cell evaluation and its independent output comparison. Full conditional root coverage remains accepted; quadrature, peak enclosure, three-rung measurement and historical-score attribution remain separate. No actual range, metric, score, EOM execution, H3 promotion or historical-trajectory identity is asserted.

Plainly: the saved-data mapping and checker are ready. The remaining immediate work is to review and run the guarded calculation, not implement another checker.

### First Actual Acceleration Attempt: Runtime Rejection

The guarded [acceleration entry](../../../../scripts/eom/run-f6c-acceleration-pilot.mjs), [launcher](../../../../scripts/eom/launch-f6c-acceleration-pilot.mjs), [interface controls](../../../../tests/f6c-acceleration-pilot.test.js) and [process controls](../../../../tests/f6c-acceleration-pilot-process.test.js) passed independent operational review at respective SHA-256 values `00e7a28decff2a4b012381d7364851405e0564b98251585718462ed0baeb6884`, `2ccbaa6b4b5aa4ecbcbdf95b8efe40665748ad023c99988a6648e83ed09c7550`, `853a5fc5c1a2352aed6fe2f7eadc24ea02e90889ce1d8783e98ee462dd04677a` and `00d16c95d5cd26750e960731978d9ba6fa46e22044c2bd9802e0872701f39812`. All 33 synthetic controls passed independently in 7.325112375 seconds. The review covered registered child processes, closed serial stages, late publication, lost monitoring, unread diagnostic pipes and preservation of unresolved cleanup identities. These tests did not evaluate the actual geometry.

Plainly: the operational safeguards passed their tests. That did not guarantee that the real calculation would load only the initially inventoried library files.

The exact [first machine plan](2026-08-27-f6c-continuous-reception-acceleration-launch.v1.json), SHA-256 `98fe1710a612bb708c059802f5a22a8f3979091c7eb504a79252156f8d14ed09`, 44,314 bytes, passed separate source-binding review: 189 distinct files totaling 16,180,826 bytes, including 158 runtime bindings totaling 5,448,895 bytes. The first actual attempt used ignored data child `.local-data/braid-analysis/f6c-continuous-reception-acceleration-20260827/pilot-cell-0-v1` and its `-outer` operational sibling. The original invocation exited nonzero. Its consumer reported `stage:source-rechecks`, `completedCells:1` and `failure:late runtime outside plan`. Source-order inspection establishes that the in-memory range calculation had returned and all captured inputs had been rechecked, but the late runtime check rejected the attempt before candidate construction and private publication. No range candidate, comparison, admission or accepted numerical value exists from this attempt.

Plainly: this was an actual calculation attempt, not an accepted measurement. Its failure concerns an incomplete execution-file inventory, not a verdict on asymmetric counter-breathing representative geometry or its acceleration residual. The failed log alone does not identify the missing library file.

The preserved rejection has SHA-256 `f67f9d792f871c3e04e91fcecd72c87c0aa3a5fa10d974c94d0c53a581e10695` (65,606 bytes); consumer stderr `58679ae7a503ffdc609588bd1d9bf9f6195d43127480a4cd633384a5f55e189f` (262 bytes); launcher stderr `8c1aade84b36e6c076afffe4c3b992bfaa81fcc534b834c4f03f890a2982898d` (143 bytes); and resource observations `c132c8d36dab679121cf17e3f934ba1d337ab0b8abacef82ce7c780d0803798f` (2,805 bytes). Consumer stdout is empty, SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. The independent failure audit reauthenticated all 189 source files and all five evidence files, found the data directory empty, and confirmed all five observed process IDs and the shared lock absent. The rejection records 0.7073584580000001 seconds, nine RSS observations and maximum sampled RSS 301,236,224 bytes; these are failed-attempt observations, not a successful whole-run cost. The captured rejection retains the original entry bytes. The first plan and output remain preserved; any corrected inventory requires separate review and a fresh output path, not an automatic retry or overwritten evidence.

Plainly: the attempt closed safely and its failure is auditable. The next task is to correct the runtime inventory while leaving the mathematical reference, consumer, checker, geometry and scientific limits unchanged.

### Independently Accepted Actual One-Cell Acceleration Range

The runtime repair changed only the acceleration entry's metadata inventory and its dedicated interface control. It exercises bounded public integer division before inventorying loaded files, thereby capturing Python's operand-dependent `_pylong.py` source at SHA-256 `2d078829f36ba378e879fcac388fec443d5dfee7e0061a2656f8703bec124dbb`; the named cache path does not exist and was not invented. Removing the exact three-line inventory addition recovers the first entry's byte hash. The repaired entry has SHA-256 `5a6342980f3f304ff356e3b2a0eaf560b076e0a098db1a60dffa07cf98b1bd87`, and its controls have SHA-256 `7112a37195cad4f6c2a9282265895adb588b52bc51841eaf13e7b24795291032`. Independent review reproduced the compatible lazy-import cause through the frozen exact-fraction addition route, passed all 33 controls in 7.287187875 seconds, and confirmed that the scientific reference, consumer, checker, launcher, supervisor and reusable helpers were unchanged. The failed log did not record the missing path, so this is a reproduced compatible cause, not a retrospective execution trace.

Plainly: the repair made the execution inventory complete for the observed large-integer behavior. It did not change an acceleration formula, root box, geometry or scientific setting.

The replacement [machine plan](2026-08-27-f6c-continuous-reception-acceleration-launch.v2.json), SHA-256 `688f1ba9154d9fb45daefe95213996cd007bde151504077399d714dd93523b6e`, 44,557 bytes, passed independent review before the fresh attempt. It binds 190 distinct source, runtime, operation and plan files totaling 16,194,484 bytes; its 159 runtime bindings total 5,460,725 bytes. Relative to the first plan, only `_pylong.py` and the reviewed entry/control generations differ. All fixed data, mathematical sources, limits, excluded claims and prior-cover closure fields are identical. The first rejected plan and evidence remain preserved.

Plainly: the replacement plan says exactly which extra standard-library file the real arithmetic can load. Nothing from the rejected attempt was reused as a result.

The fresh actual attempt at `.local-data/braid-analysis/f6c-continuous-reception-acceleration-20260827/pilot-cell-0-v2` completed with caller-observed exit zero in 2.3950473750000003 seconds. The candidate `range.json` has SHA-256 `8d33a9c877ee6c671791e3a23dd2bebf71561961d881cc2d8d80061b39c6faf1` and 352,371 bytes. The separate comparison has SHA-256 `347bcdf158e0a791c736242457b5e492db5a855589c62618435f531a9cec87e8` and 44,194 bytes. The outer admission has SHA-256 `d984df83bde6227d4e0ef958a6e31c70679f01c566facd61ced9f58749369447` and 337,313 bytes. Its launcher log is `7e8e0dc99b668f7970fcbde01babd940baa819ee0b2f6c8b95dae35534fce5af` (745 bytes), and its resource observations are `b4f77604ce6c1cc390a879ba235667a58a53bee411b1cc592bf8517a793faa0a` (7,105 bytes). Both supervised stages emitted one exact bound completion, exited zero and closed their registered process groups before final publication.

Plainly: the candidate calculation and independently derived output comparison both finished successfully. This makes the saved ranges admissible at their narrow conditional scope.

Independent post-closure audit rehashed all 190 source bindings, all nine public evidence files and both private hard-link aliases; independently reconstructed the eight member identities and polarities, original frame lexemes, all 64 raw pair rows, and all 112 piece pointers; and confirmed the complete one-cell census of 56 ordinary pairs and eight self exclusions. It checked 192 pair components and 80 member intervals. All ten reference, eight candidate and seven outer physics-authority flags remain false. The final operation records 20 RSS samples, maximum sampled RSS 321,896,448 bytes, maximum sample gap 282.3561669999999 milliseconds, final observation-to-closure gap 75.57883300000003 milliseconds, 396,565 raw output bytes and 10,458 log bytes. All five host observations remained above the fixed floors, with minimum free memory 47 percent and minimum disk 964,389,724,160 bytes. Non-overlapping recorded coordinator, Python, entry and gate CPU totaled 1.943735 user seconds and 0.256643 system seconds, excluding monitoring-probe CPU. All ten observed process IDs and the shared lock were independently confirmed absent.

Plainly: the actual result, its independent check and its cleanup all passed. The resource figures are observations of this one-cell attempt, not a full-campaign cost claim.

Every one of the 24 Cartesian member-residual intervals contains zero. Their approximate widths range from 450.4990977259219 to 715.7808440959897. All eight squared-norm lower bounds are zero, with upper bounds ranging approximately from 229,907.4515548539 to 664,211.6913216062. These are valid but uninformative inclusions: they establish neither acceleration balance nor acceleration imbalance, and they do not supply RMS, peak, a metric, a score, historical trajectory identity, H3 eligibility or a full-range result. The original broad emission boxes, not a changed geometry, are the next precision target. A deterministic uniform-face refinement declaration and independently checked successors are required before any tighter actual run.

Plainly: this first successful measurement cannot yet tell whether the prescribed curve follows the delayed acceleration law. The correct next move is to narrow the possible emission times while keeping the same curve and proving that no root is discarded.

### Independently Accepted Emission-Only Refinement Rule

The separate [emission-refinement predeclaration](2026-08-27-f6c-emission-refinement-predeclaration.md), SHA-256 `53f3398ba083218948c9efd93f10db09cbf5d617bc0270988f5adea24c48f037`, 21,345 bytes, passed independent mathematical and public-interface review with all five underlying mathematical owners unchanged. It preserves reception cell `[0,0.001]`, original frame zero, all original histories and uncertainties, coupling, charges, ruler and wake-speed units. For each of the 56 nonself ordered pairs it declares 32 exact midpoint queries for the lower face and an independent 32 for the upper face. Only a strict whole-cell negative or positive sign moves the corresponding certified face. An indeterminate sign redirects exploration only; it never discards a root or proves the opposite sign. The full 64-row and 112-piece cover must be freshly certified after all 3,584 exploratory queries.

Plainly: this is a checked method for safely trying smaller emission-time boxes. It changes neither the receiver interval nor the geometry, and it may still return broad bounds.

Independent review checked 120 synthetic monotone-family cases and 7,680 exact midpoint, sign and retained-face invariants. The exact dyadic time operands need at most 34 fractional places and 35 significant digits, within the unchanged 90-digit arithmetic. The declaration keeps the 1,800-second and 2 GiB limits, introduces no reception subdivision or root-width target, and does not borrow the member-measurement subdivision allowance. Public sign queries freshly validate histories; no cross-call cache or runtime saving is assumed. The next artifact is the separately authored pure query/final-cover comparison and its known-answer controls, frozen before the proposer. No actual emission refinement, refined cover, new range, metric, score, EOM execution or historical-identity claim follows from this declaration's acceptance.

Plainly: the rule is ready, but the program that independently checks its output comes first. Its actual accuracy and cost remain to be measured in a later guarded pilot.

### Independently Accepted Pure Emission-Refinement Comparison

The separately authored [pure query and final-cover comparator](../../../../scripts/eom/oracle/f6c_emission_refinement_conformance.py), SHA-256 `ec0eaaeae3da4ffb597ac92ff3ac1a5700a8cf88916144a7d994912270c4157a`, and [synthetic controls](../../../../tests/test_f6c_emission_refinement_conformance.py), SHA-256 `bac7357186fb05c5b7ea35154c5564e7527075a9a94177a8b600f9a02119adb5`, passed independent review before the separate producer was started. The comparator uses the unchanged independently derived Bernstein helper generation `19c57e9b638b0beb866c86b061b2325f9567add2a85608f0c42ef1f7612d9132`; it neither imports a proposer nor calls its root library. It checks all 3,584 whole-face query inclusions, exact midpoint and search-state transitions, strict retained-face proofs, independent upper-search resets, and the complete final 64-row, 56-ordinary-pair, eight-self-exclusion and 112-piece census. The original oldest face at `-8` and each newly retained lower face are checked separately.

Plainly: the independent checker can reject an unjustified search decision or final bound. It does not merely replay the calculation that produced them.

The supplied 17 controls passed in the author's run and two separate review runs; the latter measured 7.328 and 7.502 seconds. Additional independent controls passed 168 non-axis-aligned moving-affine whole-face inclusions and rejected 336 insufficient enclosures in approximately 0.0766 seconds. A separate pre-freeze closed-form grid-neighbor calculation supplied exact answers for two rotated stationary geometries: 226 endpoint and substituted-oldest-face checks passed in 2.547973250038922 seconds. These are synthetic instrument checks, not asymmetric counter-breathing representative measurements or actual-run cost projections. The rule, original history arithmetic, root library and independent geometry helper remain unchanged.

Plainly: known mathematical answers and deliberately wrong bounds were used to test the checker. No actual asymmetric counter-breathing representative refinement has yet been evaluated.

The pure result remains `accepted=False`, with all 15 authority flags false. It copies bounded inert inputs before progress callbacks and preserves completed-prefix counts on failure. Genuine small synthetic histories are supported, while the actual source-bound caller must separately authenticate the exact eight-by-1,760 original-piece census, all original tokens and premise receipts, the captured reference and runtime generation, publication and fresh process closure. Separate producer, source-bound checker and operational-adapter implementation is now the next artifact; the first guarded attempt is limited to the refined cover, with acceleration evaluation following only after that cover is accepted. No historical identity, metric, score, EOM execution, equilibrium, retention or physical-realization claim follows from this comparison's acceptance.

Plainly: the mathematical comparison is ready. Its connection to the real saved inputs and a safely completed run still has to be built and checked.

### Independently Accepted Refinement Producer And Source-Bound Comparison

The separate [refinement producer](../../../../scripts/eom/prepare-f6c-emission-refinement.py), SHA-256 `16e46276bc015f9022b5634fc33047de5d3973aa58852daa40a8020846c2a736`, and [32 synthetic controls](../../../../tests/test_f6c_emission_refinement_preparation.py), SHA-256 `ac988a906eabceeb66f2a2d9340e7638722a7e1ec6ab102d85eb0e28dbea40b7`, passed independent review. The independently implemented [source-bound checker](../../../../scripts/eom/verify-f6c-emission-refinement.py), SHA-256 `1b3e39eea14c2a21be76ab2a3fe2bdbcc055b5a73325d03de9734c79a8017c33`, and [26 controls](../../../../tests/test_f6c_emission_refinement.py), SHA-256 `1d3af80d89834b31968cfe5dd7fb016bb0ac4eee6ce77fbabaeea21a9a905bc7`, also passed independent review. Their shared contract binds the original 16-source ancestry, 15 subject sources and controls, exact eight-by-1,760 original-piece mapping, fixed 90-digit query context and unchanged scalar bounds. Queries are durably flushed before completed-prefix counts advance. The checker uses only the frozen independent comparison and geometry helper, never the new producer as its oracle.

Plainly: both sides now connect the checked mathematics to the saved-source contract. A broken input, incomplete stream or failed publication cannot acquire a complete-result label from these wrappers.

Main review reran all 32 producer and 26 checker controls. Independently derived, pre-producer-freeze answers for a co-moving affine family matched all 112 final emission endpoints across 56 ordered pairs, followed by independent comparison of all 3,584 queries, 64 rows and 112 piece records. Separate producer review reran the 32 controls and checked three additional affine families with 504 exact assertions and complete independent comparison of 10,752 queries, 192 rows and 336 pieces. These are synthetic known-answer and integration controls, not actual asymmetric counter-breathing representative measurements or cost projections. The original mathematical references and their controls remain unchanged.

Plainly: moving paths with known answers test the complete producer-to-checker route. They do not tell us what the real asymmetric counter-breathing representative refinement will find.

The initial machine-plan review generation, SHA-256 `07af3c594e6bec7f244e4ce9c9b3d18ee13434a7b16d06df12b5f70439cf731e`, correctly bound 202 unique files totaling 16,463,586 bytes, including 159 runtime files totaling 5,460,725 bytes. Fresh isolated metadata inventory matched the preceding accepted range runtime exactly. However, independent operational review returned that launch before execution: an asynchronous failure of the mirrored diagnostic stream could exit the coordinator outside its normal cancellation and cleanup path. A bounded real-pipe control reproduced uncaught `EPIPE` with no active error listener; it spawned no scientific target and therefore does not establish an actual orphaned computation. The required repair is an attempt-lifetime diagnostic error guard, an active-target failure control, and a freshly reviewed exact launch-plan generation. No actual emission-refinement query, refined cover or acceleration successor has been run.

Plainly: the source list was correct, but the first launch review found an operational defect before any scientific run began.

The repaired [supervisor](../../../../scripts/eom/launch-f6c-emission-refinement-pilot.mjs), SHA-256 `89b23af09f57aa50e3ebfc0780189f2f0d1a409a7e13004af0cb48167894b944`, passed independent review with the unchanged entry generation `291a27e6b4d6059c9338a304b279695e7b9421cdd325f5c7db23e997c9c1d0db`. All 42 synthetic controls passed in the author's, main and independent review runs; main measured 7.855654667 seconds and independent review measured 7.798022667 seconds, both with successful closed test sessions. A separate independent active-pipe control confirmed that the target had actually started and that its process group, coordinator and shared lock were absent before fallback cleanup; fallback cleanup was unused. No comparison, admission or accepted completion was emitted. The attempt-lifetime diagnostic guard preserves the first failure through owned-process cancellation, cleanup and final output barriers. This repair changes no mathematical reference, geometry, precision or resource limit.

Plainly: losing the diagnostic connection now causes a checked shutdown, not an unobserved background computation. This is launch-safety evidence, not a measurement of asymmetric counter-breathing representative.

The refreshed [one-cell machine plan](2026-08-27-f6c-emission-refinement-launch.v1.json), SHA-256 `2c33a8635a8bd1631d388e84167e725a160287a1a86e13ed1fc90cd08d7d690d`, is independently source-admitted: 202 unique bound files total 16,471,857 bytes, including the unchanged 159-file, 5,460,725-byte runtime inventory. All three plan validators pass. Reversing only the repaired supervisor and two control-file bindings reproduces the initial review hash above; no scientific setting changed. The output and operations directories and shared lock were independently confirmed absent before launch. This admission permits the declared single serial producer-to-comparison attempt only, subject to fresh host preflight and the unchanged whole-attempt limits. It does not itself certify a refined cover or authorize an acceleration successor.

Plainly: the exact repaired run is now ready to start. Its result still needs complete production, independent comparison, observed shutdown and a separate post-run audit.

### First Actual Emission-Refinement Attempt: Metadata Rejection

The first actual attempt under plan `2c33a8635a8bd1631d388e84167e725a160287a1a86e13ed1fc90cd08d7d690d` exited with code 1 during producer input capture, before any refinement query or final-cover evaluation. Its producer diagnostic, `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v1-outer/producer-process/runner-stderr.log`, SHA-256 `84172395aec89e6f96e34e7dca87fbc7cb0cc52c881be7bd09718bc58b64567f`, records `JSON token limit`, zero completed queries, rows and pieces, no private publication and no cleanup failures. The traceback identifies the prior metadata authentication call. The rejection, `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v1-outer/pilot-rejection.json`, SHA-256 `4b675261a4fd5ab3bf19a960e2aad44ea1ec34d7e1eb8b0d88bf9efad65bf863`, records closed failed producer processes and no comparison stage or accepted publication. Its 0.562947084-second elapsed field precedes final coordinator closure and is not an accepted whole-attempt cost. All 202 bound source files were independently rehashed before repair, with no changes or mismatches.

Plainly: the run failed while reading an existing operational receipt. No asymmetric counter-breathing representative geometry, root or acceleration failed this attempt.

Independent metadata inspection found four provenance command strings of 52,587 or 52,671 characters in the hash-bound prior cached-pilot admission. They exceed the new producer's 8,192-character data-token limit; the other three prior metadata documents pass. The unchanged independent checker and previously accepted range consumer already parse the complete receipt. The repair is confined to a separately typed operational-receipt decoding class with a finite 65,536-character string bound, retaining the existing data-token and other parser bounds. The original bytes are preserved byte-for-byte in ignored snapshots of the producer, `.local-data/braid-analysis/f6c-emission-refinement-20260827/failed-v1-source-snapshot/scripts/eom/prepare-f6c-emission-refinement.py`; controls, `.local-data/braid-analysis/f6c-emission-refinement-20260827/failed-v1-source-snapshot/tests/test_f6c_emission_refinement_preparation.py`; and entry, `.local-data/braid-analysis/f6c-emission-refinement-20260827/failed-v1-source-snapshot/scripts/eom/run-f6c-emission-refinement-pilot.mjs`. The executed v1 plan and failed attempt remain unchanged; a retry requires independently reviewed repaired source bindings and a fresh v2 plan and output location.

Plainly: long provenance text belongs to the operational receipt format, not to the scientific-number format. Correcting that distinction does not change a mathematical rule or permit a larger search.

The independent failed-attempt audit rehashed all five evidence files, totaling 86,800 bytes, and observed all eight recorded process identities and all three recorded process groups absent, with the shared lock, data directory, comparison and admission absent. Nine RSS samples reached 214,073,344 bytes with a maximum recorded sample gap of 177.575 milliseconds. These observations establish preserved failure evidence and observed cleanup only, not a successful refinement or complete cost measurement.

Plainly: the failed attempt is safely closed and preserved. It left no admitted scientific output or continuing owned job.

### Independently Accepted Operational-Receipt Compatibility Repair

The repaired producer, SHA-256 `bb9e1a287552483f7ad0fc1431162c1fbdb2da84fd862bc9b1c4799eef993600`, and controls, SHA-256 `c238f49b3316e43244ee0db3fdf3263b2390b4f6f91a67f6a6b7133ccfff000b`, passed independent review. All 37 portable controls pass: the original 32 remain unchanged, and five new controls cover the separate 8,192/65,536-character classes, their immediate rejection boundaries, unchanged parser guards, closed class selection and a synthetic four-document provenance chain. Only `decode` and the operational-receipt classification in `authenticate_prior` changed; the other 17 functions and classes, including all mathematics, publication and main execution, remain byte-identical. A separate metadata-only integration check authenticated all four original documents totaling 443,318 bytes while helper, proposal, cover, main, compile and import entry points were trapped; no scientific route was called. Main measured 37 controls in 2.407 seconds and independent review measured 2.390 seconds.

Plainly: the corrected reader accepts the known receipt format while preserving the scientific parser and every mathematical calculation. Routine tests remain portable and do not depend on this machine's ignored evidence files.

The entry refresh, SHA-256 `20466a4540dc972c248d30b8dd1987a19b00ca59a4f883752ec1c7c8b96d68ac`, changes only the two reviewed producer/control hash pins. The repaired supervisor and independent checker remain unchanged. All 42 operational controls pass again in 7.799126209 seconds with a successfully closed test session. The fresh [v2 launch plan](2026-08-27-f6c-emission-refinement-launch.v2.json), SHA-256 `295d1d8a8366942c4aa3f0c586e028faae08e2ac64517c837f0fe73a6b8b8a88`, binds 202 unique files totaling 16,479,218 bytes; its runtime inventory is unchanged. Independent exact-plan admission and fresh host preflight remain required before this separate attempt.

Plainly: the retry has its own source identity, plan and output location. It cannot overwrite or relabel the failed first attempt.

### Independently Accepted Actual One-Cell Emission Refinement

The separately reviewed v2 plan passed its complete source and runtime admission, then the watched actual attempt completed successfully. The original caller session closed with exit 0 and a matching fresh completion at **238.116677375 seconds**, including producer, independent comparison, final source checks, observed cleanup and output flushing. The admission, `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v2-outer/pilot-admission.json`, SHA-256 `51f0b3774bfb489bbab4fddd7f7612c6d4132f2654a36aa4091e5445eca9b51c`, records the two closed serial stages; its 237.98697625-second prepublication field is not the whole-attempt measurement. No geometry, original history, reception cell, precision, search count or resource limit changed.

Plainly: the corrected attempt completed the declared calculation and its independent check. The first failed attempt remains preserved separately.

| Complete scientific artifact | Bytes | SHA-256 |
|---|---:|---|
| Queries — `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v2/queries.ndjson` | 1,989,686 | `44d59ae62f8d7d9a9e7afd1d684e8ee15b8aeadf4dc92d489a787e5e224029fa` |
| Rows — `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v2/rows.ndjson` | 160,384 | `b6309b3c90f75590ba8270ea6cea1644be46727692a19be6aa364163e108035f` |
| Closed pieces — `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v2/pieces.ndjson` | 48,186 | `075007966aa14b3d0c9ff896d5cb752d06e410f1002dc2bc39a8e7d8db55340a` |
| Manifest — `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v2/cover-manifest.json` | 60,086 | `d4ec0d60631dd46cf2872ace941677dfd980af6f8cdbe540347b5252d336ebb6` |
| Independent comparison — `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v2-outer/comparison.json` | 60,290 | `eed41550c3c743df419efab8d0f9ad6094b43fa1efe3f4949365e731c1f7c63e` |

Plainly: these five original-byte artifacts bind the complete query transcript and final cover to the separately authored comparison. Their combined 2,318,632 bytes are scientific output, not retained-braid evidence.

The independent comparison accepts all 3,584 fixed queries, 56 ordinary pairs, 64 total rows, eight exact self exclusions, 112 closed-piece records, 112 strict final-face checks and 56 separate oldest-boundary checks. All 56 retained emission intervals strictly narrow both original faces. Exact widths range from `0.00076855891384184360504150390625` to `0.001678963936865329742431640625`, versus the original width `7.95`: approximately 4,735–10,344 times narrower. A separate exact readout reconciles 976 lower strict-negative retentions, 860 upper strict-positive retentions, 816 lower exploratory moves and 932 upper exploratory moves. Of the 1,748 exploratory moves, 1,164 residual intervals contain zero and 584 have the opposite strict sign; exploration is therefore not uniformly an indeterminate-sign result. All 56 oldest-boundary residuals remain distinct from the refined lower-face residuals.

Plainly: the admissible emission-time windows narrowed substantially for this one reception cell. This is not yet evidence that the acceleration residual narrowed, and it does not transfer to the other 159 reception cells.

The independent post-closure audit rehashed 202 source bindings plus 12 public files and five private hard-link aliases: 219 named paths totaling 22,043,417 bytes, unchanged across both checks. It independently recomputed all eight original-history digests over 14,080 segments and all 112 clipped-piece hashes from exact original knots, without rerunning root or geometry mathematics. The refined cover visits 244 closed pieces: 56 receiver records each cover three pieces, and transmitter records cover two pieces in 20 cases and one in 36 cases. All private aliases match their public inode and bytes. Fresh process inspection observed all 20 recorded process identities, five process groups and descendants absent, with the shared lock released. The 955 RSS samples reach 384,843,776 bytes with maximum gap 412.525125 milliseconds; the fresh final-observation gap is 78.7825 milliseconds. Twenty host observations preserve at least 52% free memory and 964,329,439,232 available disk bytes. Final operational logs total 499,829 bytes.

Plainly: the result is complete, independently checked, within its fixed monitored limits and no longer running. The narrowed cover is now a usable input artifact, not permission to broaden the experiment.

**Claim boundary at cover acceptance:** this accepts the conditional reconstructed-family root cover for reception cell zero only. The producer, pure comparison and outer receipt preserve every excluded authority flag: no acceleration calculation, EOM execution, historical-trajectory identity, eligible `H3`, available metric, score, equilibrium, retention or physical-realization conclusion follows. At cover acceptance, the next artifact was a separately checked source-bound projection into the unchanged acceleration-range interface, preserving each pair's own emission interval, the distinct oldest face at `-8`, exact frame-0/1 literals, charges, coupling and ruler. The old common-emission adapter cannot consume this new cover unchanged. Quadrature, three-rung measurement and historical-score attribution remain separate obligations. Any source/output hash mismatch, omitted or reordered query/row/piece, failed strict face, mismatched closed-piece hash or unclosed owned process falsifies this admission.

Plainly: the connection to the existing acceleration calculation was the next local obstacle at cover acceptance. Its subsequent closure is recorded below, without a new geometric choice or physical claim.

### Independently Accepted Refined-Cover Acceleration Projection Contract

The [projection definition, conditional proof and one-cell predeclaration](2026-08-27-f6c-refined-cover-acceleration-projection.md), SHA-256 `c491ada9b781d7aedf20a9f49b0a2dca92f4f5985660c1de56b83686976aab9d`, passed independent review before implementation. Its seven mathematical binding roles, eight members, 64 rows and 112 closed-piece records preserve original numeric strings, each pair's emission interval, the original oldest face at `-8`, exact frame `[0,0.002]` and reception `[0,0.001]`, signed charges, coupling and ruler. The proof composes the accepted family/root inclusions with the unchanged sharp acceleration and Hermite residual operations; it promises no narrower residual, equilibrium or physical identity. Coverage reconstruction must distinguish the exact requested interval for a given transmitter. The unchanged independent range checker and its existing controls are explicitly included in the successor's reference closure.

Plainly: the contract defines how to carry the improved input into the same calculation without changing what is being measured. Its proof is conditional mathematics, not a new numerical result.

The new source consumer's operational-receipt format is separately bounded at 131,072 characters per string, because the accepted refined admission contains captured command strings up to 73,179 characters. Scientific input strings and decimal limits remain unchanged; the already accepted refinement producer's 65,536-character class continues to serve its older ancestor receipt unchanged. The successor requires portable boundary controls and a separate metadata-only check against the actual new receipt before any range run. The independent projection/range core, source-bound wrapper, actual-record transport checks, separate producer and fresh operational admission were subsequent implementation obligations at contract acceptance; their completed reviews and actual one-cell result are recorded below. This contract alone supplied no acceleration result or full-cell campaign authorization.

Plainly: the next reader's format is checked before launching another calculation. The accepted cover and every earlier failed or successful attempt remain untouched.

### Independently Accepted Pure Refined-Cover Projection Checker

The [pure projection/range checker](../../../../scripts/eom/oracle/f6c_refined_acceleration_conformance.py), SHA-256 `63db48f604d0b1abdf61f0efcb3894feac9d30a25af26a4d96f01bda6522e2a2` (20,730 bytes), and [portable controls](../../../../tests/test_f6c_refined_acceleration_conformance.py), SHA-256 `3fb6eabd03a56b982f2601f11b535c60208f03df519e41ea29d4ba018a0e531e` (19,056 bytes), passed independent review before any successor producer was authored. All 24 portable tests passed for the author in 24.623 seconds and for the integrator in 24.656 seconds; both watched processes closed successfully. Main read the complete source and controls. A separate contract review accepted the immutable capture, pair-specific coverage, distinct oldest/lower faces, self exclusions and explicit division between conditional comparison and source authentication. The existing `cc26` comparison, `be741` controls, `abfc` acceleration mathematics and `c491` projection contract remain byte-identical.

Plainly: the checker is now a fixed, independently reviewed reference for the next implementation. It does not read or authenticate scientific files, launch a calculation, or decide whether the geometry is physically realized.

**Independent mathematical control:** a second reviewer froze a review-only in-memory stationary family, exact expected projection and exact expected ranges before reading the new core. Eight members at $X_i(T)=(i,0,0)$ with alternating signed charges give emission $s_{ij}=T-|i-j|$, zero Hermite curvature and directly calculable signed pair accelerations. Its 3,449,785-byte fixture has SHA-256 `4a92f26ffc81cb425cdb36d8f4cec2279cc58276364572b5a37809531bb141d1`; the independently fixed projection and range serializations have hashes `ddb27d340cb4f4cdd3581e776735d03d346d676a113475aef23d7703222da93b` and `18f83a2f83d728ec7e961a167df56c949d6f70c5684568b69cef22c6dac70378`. This separate control passed all 64 pair vectors, eight member sums, eight residuals, eight squared norms and 112 coverage records: 192 pair components and 80 member intervals. Eight adversarial mutations were rejected, and callback mutation of every caller-owned tree could not alter the captured result. The watched control closed successfully in 9.150961 seconds. These review-only fixture hashes are not claims that new fixture files were published.

Plainly: the expected answers came from a separately worked exact example, not from the program being checked. Agreement establishes this conditional mapping and arithmetic check, not a result about the actual asymmetric counter-breathing representative candidate.

**Separate actual-metadata control:** the frozen core's receipt reader accepted the unchanged 427,106-byte `pilot-cell-0-v2-outer/pilot-admission.json`, SHA-256 `51f0b3774bfb489bbab4fddd7f7612c6d4132f2654a36aa4091e5445eca9b51c`, in operational-receipt mode and rejected it in default data mode. Its four long strings measured 73,095, 73,095, 73,179 and 73,179 characters. Scientific imports and projection/comparison entry points were trapped; no scientific function ran. Same-descriptor regular-file capture and before/after hashes preserved both reader and receipt bytes. The check closed successfully in 0.01690595899708569 seconds. This authenticates those captured byte identities and format compatibility only, not the complete source, query, comparison, runtime or operational chain. The actual ignored receipt is not a mandatory portable test fixture.

Plainly: the next reader can handle the real evidence record without relaxing scientific-number limits. No actual acceleration result was produced by this format check.

The separately reviewed source-bound wrapper below authenticates the complete refined four-file cover, independent comparison, admitted completion, original history and unchanged reference/runtime chain before invoking this frozen core. A separate producer and fresh bounded operational composition remain necessary for the proposed one-cell acceleration comparison. The mapping-only actual-data control below is complete; the successor acceleration run, residual quadrature, three-rung measurement and historical-score attribution remain uncompleted. Source/field mismatch, a missed independent endpoint, mutation-dependent results, promoted authority or an incomplete captured chain falsifies the corresponding admission. No `H3`, metric, score, equilibrium, retention, historical identity or physical claim changes here.

Plainly: one implementation blocker is closed. The connection to real evidence and the actual improved-input calculation remain explicit work; the accepted geometry and earlier evidence are untouched.

### Independently Accepted Source-Bound Wrapper and Actual-Record Compatibility

The first source-bound wrapper generation, SHA-256 `9f0f3101ca86ce67f277c2cd312aca593422ddddad6c06622dbc78456fb872ba`, was returned for a transport-format repair, not accepted for an actual range run. Its 29 portable controls passed for the author, integrator and independent reviewer. The separate pre-frozen stationary fixture also passed through the wrapper and unchanged mathematical core: 192 pair components and 80 member intervals, with 17 independent metadata/numerical mutations and nine stream-ending controls rejected. The independent integration closed successfully in 6.080704 seconds. These controls did not establish compatibility with every actual operational record.

Plainly: the mathematical checks passed, but the real evidence reader still had a defect that would prevent the next calculation from starting.

The integrator's first separately reported actual-metadata-only control failed before any scientific call: the original reconstruction and guard receipts contain 16 and 12 exact integer file timestamps above the pure core's safe-integer limit, including `1787811652561200925`. Other proof/comparison records contain fractional timing metadata and therefore cannot use the strict scientific-data decoder. The independently reviewed repair adds a bounded wrapper-local operational decoder: preserve exact signed 64-bit integers and bounded decimal metadata, retain 8,192-character strings for proof/comparison/log records, and reserve the 131,072-character allowance for the semantic operational-admission role. Scientific decoding and every frozen mathematical/reference byte remain unchanged; no filename/hash exception or automatic larger-limit retry is permitted.

Plainly: this repairs how existing evidence is read, not the geometry, calculation or earlier result. No new acceleration, root search, metric, score or physical claim follows from this review.

The repaired [source-bound wrapper](../../../../scripts/eom/verify-f6c-refined-acceleration.py), SHA-256 `3f49831a2e63d2526125c1585c1250330079fa423986ec1b36901bb3cecde6ae` (44,220 bytes), and [portable controls](../../../../tests/test_f6c_refined_acceleration.py), SHA-256 `4d8bc9e7eaf1166a7c8e42133d3a3e8812c3f228c1fb13c9215994338972f72a` (35,015 bytes), are independently accepted. All 35 controls passed for the author, integrator and independent reviewer in 0.940, 0.930 and 0.923 seconds respectively, with each process closed successfully. The same pre-frozen stationary integration, 17 adversarial mutations and nine stream-ending checks passed again in 5.957952 seconds. The additional controls cover signed-integer boundaries, exact fractional metadata, role-specific string limits, unchanged scientific rejection, structural limits and the complete mocked publication/cleanup failure flow. The frozen pure core, independent range comparison, acceleration source and projection declaration remain byte-identical.

Plainly: the checker is now accepted at a fixed source generation before the separate producer is written. Its synthetic mathematical answers and failure controls remain distinct from an actual candidate result.

The integrator's repaired actual-metadata-only control authenticated 229 captured paths totaling 20,126,715 bytes before and after inspection, including the complete prior 202-source closure, all 28 large timestamps, the 3,584/64/112 query/row/piece census, original stage completions, and final operational logs. The record preserves 19 prepublication versus 20 final host observations, 954 prepublication versus 955 final RSS observations, and 499,829 final log bytes; the separately observed whole-attempt time remains `238.116677375`, not a time reconstructed from these logs. The control closed successfully in 0.46384558314457536 seconds, with scientific imports and projection/range/history evaluations trapped and zero calls. A separate reviewer independently checked all six operational records, 947,392 bytes, preserving 28 exact timestamps and 96 decimal values in 0.013477 seconds; only the two admission records needed long strings. No files or scientific outputs were published by these controls.

Plainly: the real evidence chain can be read completely without changing its content or relaxing mathematical input limits. At this wrapper-review stage the separate producer and actual run remained pending; their subsequent acceptance is recorded below.

**Separate actual-data mapping-only control:** the frozen checker reconstructed the typed input directly from the original export and refined rows/pieces after the complete prior-chain authentication. All eight members, 64 pairs, 112 closed-piece records and seven mathematical bindings passed. The 56 ordinary pairs retain 14 distinct emission boxes; every oldest-boundary residual remains distinct from its corresponding refined lower-face residual. All eight self rows retain null emission and geometry. The review-only in-memory projection, serialized as sorted compact JSON plus one newline, is 150,769 bytes with SHA-256 `40450da8055f313344a1ebe9d597cf3e83071e174e0d05f6613887a8b17a0408`. The watched check closed successfully in 1.4915635001379997 seconds, rechecking the same 229 captured paths before and after. Numerical acceleration/range evaluation and root/subject imports were trapped with zero calls; original-history and closed-coverage reconstruction were the only mathematical mapping helpers used. No projection file or producer candidate was published. An initial review-script assertion used `lo/hi` rather than the frozen interface's `lower/upper` field names; only that review assertion was corrected before the successful check, with no source or evidence changes.

Plainly: the accepted real geometry now maps correctly into the existing calculation interface. This is still a data-mapping result, not an acceleration result or evidence of balance, retention or physical realization.

### Independently Accepted Separate Refined-Acceleration Producer

The separate [producer](../../../../scripts/eom/prepare-f6c-refined-acceleration.py), SHA-256 `fdd07944609ebf8a2651999626edb607a4c8b136d906a59e638aa8c43351f6c0` (64,417 bytes), and [portable controls](../../../../tests/test_f6c_refined_acceleration_preparation.py), SHA-256 `33e90600fccf602a63bcdda14dffa143e2b869e6557365958c5b9951a268c922` (55,178 bytes), are independently accepted. The direct mapper was authored after the checking instruments were frozen and imports neither their projection nor their comparison functions. It preserves each pair's original restriction, separate oldest/lower faces, null self fields, exact original frame/charge strings and seven mathematical binding roles, then calls the unchanged captured acceleration evaluator once. The future candidate remains unaccepted, with all eight candidate flags and ten evaluator authority flags false; a successful evaluator call is not independent or external admission.

Plainly: the program that makes the result and the program that checks it build the input separately. Their mathematical references and the represented geometry have not changed.

The first producer generation, `b43f9abd56c7b819ee38d9c9bb6c9db7eee628b020b07da9e3d8e382ad8dbd38`, passed 42 supplied controls and the independent mathematical examples but was returned for three transport repairs: bound a later metadata-to-data capture by the original file size, restore the declared closed parser selectors, and reject actual NUL path characters. The repaired nested-capture regression uses a disposable three-byte source that grows after its first recheck: only the original three bytes and one extra EOF-check byte are read, no new captured data is retained, and no candidate is published. The final 44 controls pass for author, integrator and independent reviewer in 0.722, 0.734 and 0.726 seconds respectively. Independent replay of the pre-frozen stationary example produces the exact expected projection and range serializations with one traced evaluator call, all 192 pair components and 80 member intervals, eight rejected mapping mutations, detached output, and separately derived signed-component/Hermite checks. That watched review closes successfully in 6.627637 seconds. No checker, root, EOM or independent-core execution supplies the producer's expected answers.

Plainly: the review found and closed an operational defect even though the mathematical examples passed. The successful tests remain tests, not an actual acceleration result for asymmetric counter-breathing representative.

**Separate actual producer mapping-only control:** after authenticating the original chain, the integrator ran only the producer's direct mapping with evaluator and prohibited imports trapped. Its sorted compact projection plus newline is exactly the independently established 150,769-byte `40450da8055f313344a1ebe9d597cf3e83071e174e0d05f6613887a8b17a0408` record. All eight members, 64 rows and 112 coverage records agree. The check rehashed 228 paths totaling 20,138,780 bytes before and after, including the current producer and controls; it closed successfully in 0.3369287920650095 seconds with zero acceleration/root evaluations and no published candidate. At producer acceptance, the separately reviewed operational composition, exact machine plan and actual serial comparison remained pending. Their subsequent closure is recorded below; this mapping-only control itself supplies no acceleration value, metric, score, historical identity or physical claim.

Plainly: the real inputs agree between independently built mappings. That pre-run agreement checks the data mapping; the later numerical result has its own evidence below.

### Independently Accepted Refined-Acceleration Operational Composition

The [operational entry](../../../../scripts/eom/run-f6c-refined-acceleration-pilot.mjs), SHA-256 `5dc9020cfc6238dc4db3566d267cb9f153755c91c563a7d300c841a8981adf28` (36,324 bytes), and [launcher](../../../../scripts/eom/launch-f6c-refined-acceleration-pilot.mjs), SHA-256 `e94d2b3379dd9f84736ea4907d0c326ecddebf3e41422f49a108efffd2c73428` (24,427 bytes), passed separate independent review. The [unit controls](../../../../tests/f6c-refined-acceleration-pilot.test.js) and [process controls](../../../../tests/f6c-refined-acceleration-pilot-process.test.js) are frozen at `425c7481ea7b63f8d79a455c332401beeb294a5bc482fca312ecfcdbf72355bb` and `73c79732408ce0be6673890a38dc65150496a385b0b4d8043b8b9114dbfb777a`. All 43 controls pass for author, integrator and independent reviewer in 7.716279708, 7.757388291 and 7.737074167 seconds respectively. Initial restricted-environment runs could not observe child processes and failed with `EPERM`; the authorized reruns exercised the actual process-cleanup branches and closed successfully. Those environment failures did not dispatch scientific targets or require a source repair.

Plainly: the supervisor passed both ordinary tests and deliberate failure tests, including a broken diagnostic pipe while a target was active. These are safety checks, not measurements of asymmetric counter-breathing representative.

The [exact machine plan](2026-08-27-f6c-refined-acceleration-launch.v1.json), SHA-256 `7bc86129cb4c5a6bd6f3fbcd5e6a272e1e698e067668cdc139ff6f593f79d647` (45,748 bytes), passed independent review before execution. Its 11 named sources, 159 runtime bindings and nine operational bindings retain the complete original source/receipt chain. Same-descriptor before/after checks authenticated 243 source bindings totaling 20,479,836 bytes, or 244 files and 20,525,584 bytes including the plan. The fresh metadata inventory and public large-integer-division exercise reproduced the canonical runtime-array hash `f0fcb347e140884ba5b59e5ff349be8108eb38387a8c50ed04082fd9e58302a5`; no scientific function ran during inventory. The two prospective captured command strings are bounded by 71,833 and 71,917 characters for the actual paths: both fit the declared receipt class and fail the default short-string class. All three plan validators passed. The fresh output siblings and shared lock were absent, and the canonical ignored parent was created and checked before launch.

Plainly: the actual input files, executable sources and runtime were fixed before the attempt. No geometry, interaction strength, resource cap, mathematical reference or earlier evidence was changed to obtain a passing launch.

The composition preserves one serial producer followed by one independent comparison, with the existing 1,800-second inclusive deadline, 2 GiB observed aggregate-memory stop, at-most-one-second memory-observation gaps, host memory/disk floors, 15-second heartbeats and fixed file/log bounds. Current stages are `consumer` then `comparison`; the preceding emission-refinement stages remain `producer` then `comparison`. Their different historical receipt roles are not relabeled. Source checks, current-process closure, final diagnostic flushing and exclusive publication remain necessary; the old refinement's cost does not become the new attempt's cost.

Plainly: a successful earlier run cannot stand in for watching and checking this one. Passing the operational review alone does not establish a numerical result or authorize a full-history campaign.

### Independently Accepted Actual One-Cell Refined Acceleration

**Measured, source-bound conditional result:** the reviewed refined-input producer and independent comparison completed their fresh serial attempt in `.local-data/braid-analysis/f6c-refined-acceleration-20260827/pilot-cell-0-v1` and its `-outer` sibling. Original session `30920` closed with exit zero; its fresh completion reported `3.384976666` seconds. Both stages, the file worker and monitoring closed, the shared lock was released, and the post-flush deadline/observation guard passed. The final independent operational audit accepted the complete source and process evidence. No new root search or EOM evolution ran.

| Original artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `pilot-cell-0-v1/range.json` | 404,025 | `5ff9ba49bb063f08f2563bc6fc96706e7c49b35d9248f970ea7b4895b905f390` |
| `pilot-cell-0-v1-outer/comparison.json` | 48,477 | `feea7cfd7c385544b7911c9731a25d51e035bb649f9673fbcd9688a781946c26` |
| `pilot-cell-0-v1-outer/pilot-admission.json` | 400,221 | `bd983992eb6e797d39442a8c9dac94f415578035c94f2f16bafe1669b8bde44c` |
| `pilot-cell-0-v1-outer/launcher-stderr.log` | 728 | `bafd677eb069e14be14d784fbdf506126b2d1fb68be73ad6ff56b18054a9e2f9` |
| `pilot-cell-0-v1-outer/resource-observations.ndjson` | 8,997 | `298d7560ebe83f13126a7845194ab8f63e9358a51f234f53266cb75b3cc94b6f` |

Plainly: the separately checked, narrower emission intervals have now been used in an actual acceleration calculation. These are new write-once results; the old broad result and all preceding evidence remain intact.

The independent scientific audit executed the frozen source-bound checker, pure projection core and separately authored rational comparison, without importing the producer or running roots. It reconstructed all eight original 1,760-segment histories, original frame data and 112 closed-piece records; checked 64 rows, 56 ordinary pairs and eight self exclusions; and independently checked all 192 pair components and 80 member intervals. The projection exactly matches the pre-producer `40450da8055f313344a1ebe9d597cf3e83071e174e0d05f6613887a8b17a0408` expectation. Its 248 same-descriptor file checks, totaling 21,731,254 bytes, were unchanged before and after the audit, which closed successfully in 1.640578250 seconds. The complete operational audit separately checked 255 named paths totaling 21,843,859 bytes before and after, including all 243 sources, the plan, nine public output/log files and two private aliases. Each alias retains its public file's inode. The actual captured entry, bootstrap, gate and Python source payloads match their declared generations.

Plainly: the checker rebuilt the input independently and tested the numerical output against separate rational mathematics. Matching saved output alone was not used as evidence of correctness.

**Measured precision improvement:** exact-rational comparison with the preserved coarse candidate `8d33a9c877ee6c671791e3a23dd2bebf71561961d881cc2d8d80061b39c6faf1` shows all 24 new residual-component intervals nested strictly inside their earlier bounds. New widths range from approximately `0.005732763018686539` to `0.017013179626302485`; paired old/new width ratios range from approximately `27,600.3787` to `104,339.8443`. Every residual component still contains zero. All eight squared-norm lower bounds remain zero, while their upper bounds range from approximately `0.00009611573997544` to `0.00015214192410662`. The independent audit also checked nesting of all 192 pair-component boxes, 48 acceleration/residual boxes and eight squared-norm boxes. Original frame curvature, charges, coupling, ruler, history family and reception cell are unchanged.

Plainly: the earlier enormous uncertainty has been reduced substantially. Zero is still possible in every mismatch component; the calculation neither proves exact equality nor rules it out.

**Derived local bound:** the largest exact recorded squared-norm upper endpoint is strictly below `0.0001522756`, which is exactly `0.01234` squared. Hence every member's normalized mismatch norm is below `0.01234` throughout `[0,0.001]`, conditional on the already specified reconstructed family and admitted root-cover premises. This is a local uniform bound, not a percentage of the acceleration, a full-history RMS/peak metric, an equilibrium proof or identification of the historical EOM trajectory.

Plainly: the checked result gives a small explicit upper bound on the mismatch over this one interval. It says nothing by itself about the rest of the history or whether the assembly remains a braid.

The operational audit independently recomputed 24 aggregate-memory observations with maximum 367,378,432 bytes and maximum reported gap `283.899542` ms, plus five host observations with minimum free-memory percentage 55 and minimum available disk 964,280,004,608 bytes. The two scientific outputs total 452,502 bytes; all final logs total 13,050 bytes. At `2026-08-27T15:40:50.349Z`, a separate live process observation found all ten recorded PIDs, five process groups and matching descendants absent, with no shared lock. The stored admission records 22 memory observations, four host observations and elapsed time `3.2649948749999997` before publication; it is not substituted for the fresh final observation. The fresh completion's `73.66462500000034` ms gap field precedes the main final flush; its later reported elapsed-time point implies approximately `73.912458` ms from the last sample start. The post-flush guard and successful process exit establish compliance with the final one-second gap and inclusive deadline, without claiming an unrecorded exact later timestamp.

Plainly: publication and cleanup were included in the checks. The prepublication receipt, fresh reported clock and final checked limit are distinct pieces of evidence, not interchangeable timestamps.

The candidate retains all eight false authority flags, the evaluator retains its ten false flags, and the pure comparison retains its fifteen false flags. External acceptance is only source-bound one-cell conditional projection/range conformance. `H3`, historical trajectory identity, `M05/M06`, score, equilibrium, retention and physical realization do not advance. The successful first-cell acceleration cost does not cover the separately measured 238.116677375-second emission refinement or predict a complete 160-cell measurement. The integral/supremum foundation, pure reference, shared quadrature protocol and correlated residual envelope are separately accepted below. Source-bound whole-history inputs, bounded actual execution and independent three-rung measurement remain separate. A changed source, original-to-typed mismatch, uncovered interval, independently valid value outside a bound, failed process closure or resource violation falsifies admission at its corresponding scope.

Plainly: preserve the useful result and proceed to the missing measurement mathematics. Do not promote it into a whole-history score or start a larger run using undeclared settings.

### Independently Accepted Integral and Supremum Definition

The [residual integral/supremum definition](2026-08-27-f6c-residual-integral-supremum-enclosure.md), SHA-256 `945441097fdd2934434dd2ff6d9dd6f06a77898752db6bcac90745a76420eb4b` (27,167 bytes), passed full independent mathematical review after drafting and before implementation. Its original pending-review label remains frozen; this paragraph records the separate acceptance. For a complete exact partition of all 80 original frames and all eight members, duration-weighted squared-residual bounds enclose each integral; the total divided by `8 × 0.13`, followed by an outward square root, encloses the unchanged RMS. Complete pointwise coverage bounds the peak above, while uniform cell lower bounds or separately justified same-family, frame-side witnesses bound it below. Both limiting curvatures at each original frame knot remain included. The proof fixes one coherent history before applying the finite sums and then quantifies over the existing family; it never selects another past independently on each cell.

Plainly: complete interval bounds can now be combined into rigorously bounded averages and maxima without changing what is being measured. This mathematical result does not supply the missing actual full-history inputs.

The same artifact proves an exact finite-polynomial subtraction identity and a general quadrature-remainder enclosure without assuming unproved higher derivatives, positive weights or polynomial exactness. Every node, weight and residual bound remains a separately justified premise. Intersections are valid only for the same integral, and an empty intersection fails explicitly. The independent reviewer checked 193 exact-rational assertions in 0.012305625 seconds: 80 nonuniform synthetic frames and eight members, seven common-family parameters, twelve polynomial/signed-weight remainder cases, frame-side/endpoint peaks, and counterexamples to selected-history or cancellation shortcuts. A separate polynomial example gives two agreeing zero-valued nodal estimates while its exact omitted area is `1/840`. All referenced mathematical and owner files remained unchanged; no actual asymmetric counter-breathing representative calculation ran during this review.

Plainly: agreement between two sample formulas is not itself an error bound. The accepted identity explains what additional bounds a valid quadrature calculation must supply.

The integrator additionally checked all 13 display equations and 83 inline expressions with the repository's vendored KaTeX 0.16.11 renderer in strict error mode; all passed without changing the definition's bytes. This is a rendering-syntax check, not a visual-layout or mathematical-proof check.

At definition acceptance, the next artifact was the separately authored pure aggregation/remainder reference and its independent known-answer checks; their subsequent acceptance is recorded below. No Gauss--Kronrod rule or order, interpretation of the 20-subdivision allowance, node-point API, actual-data adapter, full run or three-rung execution is selected or authorized by this acceptance. The global `10^-6` RMS/peak width target and historical-subject requirements remain unchanged. A valid enclosure need not meet the required width; no convergence, cost, metric, score or physical claim follows from the proof alone.

Plainly: the mathematical foundation is closed at its conditional scope. The separate implementation is recorded below; the precise execution protocol remains distinct.

### Independently Accepted Pure Integral and Supremum Reference

The separately authored [pure integral/supremum reference](../../../../scripts/eom/oracle/f6c_residual_integral_supremum.py), SHA-256 `fc170a91b2747923bda89ef00b58d529c98bf96b01cc7b2c05c035042fc79c5a` (20,129 bytes), and its [portable controls](../../../../tests/test_f6c_residual_integral_supremum.py), SHA-256 `d80ca8bab38bface925fbdee1530f43919c83b331a878f004ef1601b2cf09b24` (24,532 bytes), passed independent review after the theorem was frozen. All 31 supplied controls pass for author, integrator and reviewer, reporting 0.205, 0.202 and 0.199 seconds respectively. The reviewer independently checked 482 assertions in 0.272817334 seconds: the 193 previously frozen theorem assertions plus 289 API checks, including 135 outward-square-root cases checked with independent integer binary search and 39 invalid-input rejections. The review plan and exact expected mathematics were fixed before implementation was read. All processes closed successfully, and the theorem and earlier mathematical references remained unchanged.

Plainly: both the calculation code and its separately established expected answers have been checked. These are synthetic mathematical tests, not a new asymmetric counter-breathing representative measurement.

The reference requires 80 ordered original frames, exact full-domain coverage from zero to `0.13`, fixed eight-member ordering and consistent caller-declared family/frame/source labels. It rejects gaps, positive-duration overlap, reordered cells, frame crossings, invalid bounds and inconsistent intersections. Exact rational arithmetic supplies duration-weighted integrals, normalized RMS, complete-cover peak bounds, optional uniform frame-side witnesses, polynomial integrals, subtraction-residual enclosures and conditional quadrature remainders. Outward decimal presentation uses exact integer square-root comparisons rather than ambient decimal rounding. Separate output booleans report whether the global magnitude-width targets are met; all fifteen authority flags remain false, including source authentication, quadrature completion, subdivision verification, metrics and execution.

Plainly: the component can combine valid complete inputs and report whether its bounds are narrow enough. Matching declared labels does not prove where an input came from, and a narrow synthetic answer is not an accepted metric.

Residual and quadrature inputs explicitly remain premises about the same actual polynomial argument and fixed linear rule. The future producer must derive and bind those quantities to that polynomial; this pure reference does not authenticate their origin or turn arbitrary asserted bounds into proofs. Independent review found no inclusion defect in this conditional API. Decimal decade carry can retain trailing zeros in the displayed coefficient while representing exactly the correct directed 90-significant-digit value; no canonical coefficient-string length is claimed. Data-capacity checks are not an interpretation of the 20-subdivision allowance.

Plainly: the reference verifies the mathematical combination, not the truth of every supplied premise. Its eventual actual-data consumer must preserve that distinction.

At pure-reference acceptance, the next artifacts were the whole-history numerical protocol and source-bound consumer. The shared quadrature protocol and correlated residual envelope are subsequently accepted below; source-bound inputs and bounded actual execution remain outstanding. No actual run is supplied by this component. The existing asymmetric counter-breathing representative metric/score/trajectory limitations and three-rung obligations remain unchanged; the successful one-cell result is not replayed as a full-horizon measurement.

### Independently Accepted Correlated Residual Envelope

The separately derived [common-time box-envelope proof](2026-08-27-f6c-correlated-residual-box-envelope.md), SHA-256 `4180faad5d631af4bdbaf9ebd11500b0cc158b50da6ccc81295cf4d84a82bd41` (16,640 bytes), is accepted at its conditional mathematical scope. The [implementation](../../../../scripts/eom/oracle/f6c_correlated_residual_enclosure.py), SHA-256 `b86907236e849124f3fa9c6bcad0f65492ecc6fbeb1b51a27438655c45b037b1` (7,830 bytes), and [controls](../../../../tests/test_f6c_correlated_residual_enclosure.py), SHA-256 `327b1be489baa06d5785de9c306c06ffbe2f5c825700abfd87ac18345a9ac9ff` (8,958 bytes), were frozen before independent implementation review. The proof's original pending-review label is preserved; this entry records its subsequent acceptance. The integral reference, its controls and its earlier theorem remain unchanged.

Plainly: this supplies a checked remainder calculation for the existing integral method. It does not evaluate another physical history.

The operation retains the same time in the exact affine required curvature, three uniform law-side acceleration boxes and the exact auxiliary quadratic. It computes the exact extrema over that independent-box relaxation by at most nine algebraic cuts and the endpoints and interior vertex of each resulting quadratic. These are internal algebraic cases, not additional reception requests or scheduler subdivisions. Exact rational curvature coefficients are preserved even when their decimal expansions do not terminate. The result is one same-key whole-leaf residual interval, rounded outward to 90 significant digits; signed polynomial-subtraction residuals are not clipped to zero. All fifteen authority flags remain false.

Plainly: separately bounding two time-varying terms can lose their cancellation. This calculation keeps their shared time long enough to preserve that cancellation, without assuming new smoothness or requesting new roots.

All 17 supplied controls pass for the author and independent reviewer, reporting 0.011 and 0.013 seconds. The reviewer separately passes 1,870 assertions in 0.118340250 seconds: 24 exact known-answer cases, 120 further exact cases checked by a different active-set/corner optimization, 840 interior-point checks, and 12 hostile input rejections, with associated containment, identity and authority assertions. The independently fixed known-answer plan has SHA-256 `96ea187231baad292212ba642eef4c29fbdc74903150493404f61e556e717285`; the nonterminating-rational case was fixed separately after that interface choice and before final review. The first author run had one incorrect expected algebraic-piece count; the expected numerical extrema passed and only that test count was corrected before freezing. Main read the complete proof and verified its 3 display and 129 inline expressions with strict vendored KaTeX 0.16.11 syntax checking; this is not visual layout inspection. Every process closed and all frozen hashes remained unchanged.

Plainly: the second check did not reuse the implementation's piece-selection algorithm. The examples test exact cancellation, both kinds of interior turning point, signed values, all nine cuts and the repeating-fraction representation boundary.

This proves inclusion and exact extrema for the supplied box relaxation, not that its endpoints are attained by the actual family or that real-data bounds meet the required width. Source-bound uniform acceleration boxes and original-frame curvature remain external premises. When an old squared-residual bound came from narrower unrounded intermediates, do not assume that this separately serialized box envelope is automatically nested inside its naive subtraction; intersect independently valid bounds and reject inconsistency. No actual-data evaluation, historical-trajectory identification, full quadrature, three-rung agreement, EOM execution, metric, score or retention claim follows. A valid point in the declared time/acceleration box outside the returned enclosure, a missed branch or vertex, a wrong source/frame premise or an altered frozen hash falsifies the corresponding acceptance.

### Independently Accepted Shared Quadrature Protocol

The separately authored [Gauss–Kronrod protocol](2026-08-27-f6c-gk13-execution-protocol.md), SHA-256 `66ec97315dd8caf08d0628e2b23326044ac0a2f5b86b29d7c2f5542dc879cc85` (18,601 bytes), [implementation](../../../../scripts/eom/oracle/f6c_gk13_protocol.py), SHA-256 `a70a15481f793e913440628068f9c53bab611fe9d92f36206a401c01e91478eb` (24,388 bytes), and [controls](../../../../tests/test_f6c_gk13_protocol.py), SHA-256 `4b53d57f19de401348830b809600f01a9ae0c0c88d19d406a6a85e5ac8c5a241` (22,604 bytes), are independently accepted at pure conditional scope. The document's original pending-review label is preserved, with subsequent acceptance recorded here. Its reference and the separately accepted correlated-envelope operation remain frozen, separate dependencies.

Plainly: the measurement now has an executable numerical recipe. It still needs correctly bound answers from the preserved history before any actual measurement can be made.

The rule uses one Gauss node and three Kronrod nodes, whose positions, positive weights and polynomial moments are derived exactly. Fixed positive-width neighborhoods enclose the actual nodes. A unique auxiliary quadratic is chosen from rational neighborhood and value midpoints, then its coefficients are floored to 90 significant digits; the resulting exact polynomial, not its unrounded predecessor, owns the whole-leaf subtraction bound. The embedded-rule difference is diagnostic only. The frozen integral reference requires a valid whole-leaf remainder and complete original-frame/member coverage; independently boxed subtraction alone promises no gain. Node-neighborhood bounds also supply correctly sided peak witnesses.

Plainly: agreeing sample formulas cannot hide an error between sample locations. The recipe retains a bound across the entire interval, and the separate common-time operation can supply a tighter such bound when its premises hold.

Independent review accepts the conservative implementation restriction of at most 20 total split events per original accepted frame, counting every mandatory original-knot cut and sharing the allowance across all eight members and both measurements. There is no per-member, child, method or restart reset. The original predeclaration is not rewritten or claimed to have uniquely specified this interpretation. Global RMS and peak widths must both meet the unchanged `10^-6` target; incomplete coverage remains pending, and exhaustion remains unresolved. There are at most 41 evaluated leaf generations per frame, three node neighborhoods per generation, no precision retry, and zero new root or emission-refinement operations in this protocol. These bounds do not predict runtime or guarantee numerical success.

Plainly: the recipe shares one small work allowance and explicitly stops when it cannot establish the requested accuracy. It does not obtain more work by renaming an interval or switching measurements.

All 32 supplied controls pass for author, integrator and independent reviewer, reporting 10.882, 10.732 and 10.761 seconds. Independent controls separately pass 1,039 exact assertions and 34 rejection cases in 1.396699 seconds, including an independently computed square-root bracket, centered-quadratic and signed-rounding checks, three nonconstant quadratic leaf cases, 80 complete synthetic leaf evaluations across eight members, and malformed or mismatched input rejection. The known constant case has exact total integral `663/25`, squared RMS `51/2` and peak `8`. The review plan and quadratic addendum were frozen before implementation inspection at SHA-256 `02b7715ce7523820605e2c3de9e38888ba747f9a66f152e9502609c9a50ca3ba` and `875b386cfef6a707fec78f817babe5d44b38b48ca928cb608306f3ee92bdcbee`. Full-budget supplied controls explicitly distinguish mocked leaf arithmetic from the real scheduler and aggregate. Two initial review-script representation comparisons were corrected to compare exact rational values; expected mathematics and subject code did not change. All processes closed and frozen hashes remained unchanged.

Plainly: the independent test checked exact answers and incorrect inputs, not just reproduction of saved output. The large-budget control tests bookkeeping; it is not evidence of a physical calculation.

Main also connected the frozen protocol, correlated-envelope operation and integral reference in three separately calculated synthetic quadratic cases. All 24 member integrals contain their exact closed-form areas and meet the stated synthetic width check, for 48 assertions in 0.075123167 seconds. This is a composition check, not actual-data accuracy. Strict vendored KaTeX 0.16.11 accepts the protocol's 6 display and 73 inline expressions, with all six relative links resolved; it is syntax checking, not visual layout inspection. The unchanged 31 integral-reference controls also pass on this batch.

Main's source-bound metadata-only audit preserves the original export SHA-256 `f479bb88a6425e9e98e00288f2524f33d5a3c0f4c2a14139dbaae4f468c46db1` (9,125,903 bytes) before and after reading. Exact decimal-integer comparisons find 80 original frames and exactly one mandatory history-knot cut inside each: 160 initial leaves and 19 remaining adaptive events per frame under the accepted restriction. Recorded frame widths retain their original decimal variations; no idealized time grid is substituted. For this input grid the arithmetic bound is 40 evaluated leaf generations per frame, not a cost projection or an authorized full run.

Plainly: the actual file fits the recipe's partition rules without silently discarding its required cuts. This inspection did not evaluate a root, acceleration, residual or metric.

The next direct artifact is the source-bound variable-cell adapter and independent comparison: authenticate the preserved export, original frame and knot grid, accepted parent covers and exact mathematical dependencies; provide valid whole-leaf and node-neighborhood ranges plus a same-polynomial correlated remainder. Restricting an already proved parent reception interval preserves its valid universal cover, but does not justify a narrower emission interval. Inherited wide emission boxes may prevent useful contraction; the adapter must report that limitation rather than silently spend an earlier refinement budget or launch another coarse full-history batch. Any new root/emission work needs its own separately accepted bounded protocol. Actual whole-history execution still requires a measured operational plan and closed process/resource audit. Historical-score attribution, three-rung measurement, `M05/M06`, asymmetric counter-breathing representative score, `H3`, retention and physical claims remain unchanged. Wrong exact containment, missing coverage, altered source identity or a shared-counter violation falsifies acceptance at its corresponding scope.

Plainly: this closes the pure aggregation implementation blocker. It does not authorize a larger calculation or select the still-unset phase-varying display representative interaction strength.

### Independently Accepted Single-Leaf Diagnostic Composition

The [fixed first-leaf composition](../../../../scripts/eom/f6c_single_leaf_diagnostic.py), SHA-256 `046d81775aee64507f757ad5e6aa732da2e356e0f0d403b5ebdaf8f87db9dd62`, and [synthetic controls](../../../../tests/test_f6c_single_leaf_diagnostic.py), SHA-256 `c8cddfd6305c292c45904c8031629e1041ba1ffc2b77776d15fad1e104fd1ec4`, are independently accepted as conditional arithmetic glue. They use the unchanged range, integral, correlated-envelope and quadrature references. A fresh adapter supplies exactly four ordered range evaluations: the refined first reception cell `[0,0.001]`, followed by its three prescribed positive-width node neighborhoods. All eight correlated residual calls use the original whole-cell evaluation and their exact node-derived polynomial. One leaf calculation then produces eight local integrals. Explicit counters reject additional work; exceptions stop without retry. No adaptive state machine, subdivision, root query, emission refinement, global aggregation or EOM evolution is called.

Plainly: this is the smallest connected trial of the checked calculation pieces. It cannot silently turn into a search or report a whole-history measurement.

All nine supplied controls pass for the author and reviewer in 0.051 seconds. The independently authored constant-integrand fixture, separate from the author's fixture and the acceleration/correlated evaluators, passes 75 assertions and 35 failure controls in 0.085989 seconds. It checks eight exact integrals, ordered node neighborhoods, original-whole-cell ownership, matching polynomials, all twelve range/residual failure positions and all thirteen progress-callback interruption positions. Adaptive and aggregate entry points were trapped. The author's first test run rejected an empty initial request path; the composition was corrected to the required initial-cell path before freezing and rerunning. All processes closed and frozen hashes remained unchanged. These are synthetic composition results, not actual-data accuracy or source authentication.

The actual adapter's source capture and independent mapping/lifecycle review remain outstanding at this checkpoint. The planned actual diagnostic still requires its separately reviewed foreground orchestration, unchanged external resource limits, final source recheck and verified process closure. The existing registered supervisor and monitoring primitives are reusable; this composition is not a launcher and grants no execution authority. Every returned authority flag remains false. A wrong exact integral, substituted whole-cell result, extra numerical call, retry after failure or changed source generation falsifies the corresponding acceptance.

Plainly: the numerical connection is checked, but its real inputs and watched execution must still pass. No larger batch has been launched by this acceptance.

**Derived fixed-box limitation, independently checked:** let one acceleration coordinate have interval midpoint $c_k$, half-width $h_k$, and distance $d_k=|B_k(T)-c_k|$ from the required acceleration at a fixed time. Its squared-distance range has width $(d_k+h_k)^2-\max(d_k-h_k,0)^2\ge h_k^2$. Independent coordinate-box extrema add, and subtracting any common value $p(T)$ leaves that width unchanged. Consequently the raw common-time box envelope of $L_0^2\|B(T)-A\|^2-p(T)$ on any nonempty time interval has width at least $L_0^2\sum_k(b_k-a_k)^2/4$ when the law-side boxes $[a_k,b_k]$ are held fixed. This is a limitation of the independent-box method, not a lower bound on the actual history family's variation or on a final independently intersected integral, RMS or peak width.

Plainly: extra time samples cannot erase uncertainty that is deliberately left unchanged in the acceleration boxes. This does not prove that the requested final accuracy fails; it identifies information that sampling alone cannot supply.

Main and the independent reviewer separately evaluated that exact expression from the previously accepted serialized acceleration output SHA-256 `5ff9ba49bb063f08f2563bc6fc96706e7c49b35d9248f970ea7b4895b905f390` (404,025 bytes), verifying its hash before and after. The eight raw-envelope width floors range approximately from `8.3343591e-5` to `1.2791391e-4`; the reviewer also passes twenty fixed-time checks of the inequality. These read-only rational reductions took 0.001801666 and 0.002136 seconds and invoked no new acceleration or root kernel. They neither establish a physical mismatch nor authorize a new refinement budget. The falsifier is a valid independent-box slice narrower than the stated exact bound, or a changed input fingerprint.

### Independently Accepted Actual First-Leaf Integral Diagnostic

The [source-bound variable-cell adapter](../../../../scripts/eom/f6c_variable_cell_adapter.py), SHA-256 `9f31d0dd9a670e80e4054e3cba31770849edd17de8d8494b4dc502efd6bf3028`, and its [controls](../../../../tests/test_f6c_variable_cell_adapter.py), SHA-256 `a439cae5f43a671f77351a5a3b39a11c1fcd0c167e424e2302c31dd56fd7c0e1`, are independently accepted at their inherit-only contract. Forty supplied tests pass. Separate synthetic mapping review passes 4,577 assertions; lifecycle review passes 51 assertions and 25 rejection controls. Actual metadata-only construction captures 261 files containing 53,130,181 bytes in 20.797409084 seconds through context closure, without projection or numerical evaluation. Independent actual mapping review passes 414,940 assertions in 33.027832334 seconds, checking all 160 parents and 320 parent/subcell projections against original histories, including exact Hermite curvature and closed-knot ownership. No acceleration, residual or root evaluation occurs in those mapping checks. The complete source-provenance digest is `16c364ac06ba26eb50c15894df2758055ee4d9d8981946dc06164c82b352a9a0`, calculated from the ordered path/hash/byte triples as compact JSON without a trailing newline. An initial audit-harness newline mismatch was corrected before the successful mapping audit; no input or subject changed.

Plainly: smaller reception intervals now reach the checked arithmetic through independently verified original inputs. This adapter deliberately retains the parent's emission and geometric bounds; it does not silently refine them.

The first watched attempt, `pilot-first-refined-leaf-v1`, failed before source construction or scientific calculation: its launcher passed an absolute Node clock value into Python's different monotonic-clock domain. Independent probes measured an approximately 6,441-second offset. The adapter correctly rejected that deadline. The failed attempt, original invocation `06e74560b39e7437734bff039ffae66ac6c7ecebe87685c82460dc3d0ac0c3bc`, and rejection `680013a46eaecfa9346370eace67a00e03353e21d176129a662680067c4d3544` remain preserved. The separately reviewed repair transfers the remaining duration into a supplementary Python-local deadline while retaining the original external 1,800-second deadline as authoritative through startup, execution, publication and cleanup. Twenty supplied controls and fourteen independent delayed-start assertions pass. No mathematical source, history, geometry, runtime inventory or resource limit changed.

Plainly: this was a launcher failure, not a negative result about the candidate. The repair does not renew the time budget or permit an automatic retry.

The subsequently admitted `pilot-first-refined-leaf-v2` completes exactly four range evaluations, eight correlated-envelope calculations and one leaf calculation on original frame zero and reception interval `[0,0.001]`. It performs no root queries, emission refinements, adaptive subdivisions, full-history aggregation or EOM evolution. Its ignored invocation is `preparation-v2.Tr2YiR/invocation.v2.json`, SHA-256 `e1d90bd72ec70ddac28f9c8866c7c4f0d06ba45be592ceed557c6c8bc337b3a8` (39,819 bytes), under `.local-data/braid-analysis/f6c-single-leaf-diagnostic-20260827/`. It fixes coordinator `5e34ad604b4c16d60b6915f62311715b357226064bb87bb35b95a4ec07688782`, controls `20a0a2845e40c32b5e7a394d9d2402820cfaee2ab735a67818c8e6162ac96d37`, embedded Python `cb225d84d946a2104604067c547a447541f165a7cb3d26a9f35b8d722ecee326`, and all 168 runtime files before admission. Caller session `58466` closes with exit zero and final completion `cb2d91`. Its fresh output is stamped at **22.1678675 seconds before the final stdout flush**. The operation's 21.991377625 seconds is explicitly before publication; neither stamp is an exact whole-process-exit timing.

The accepted raw output is `pilot-first-refined-leaf-v2/diagnostic.json`, SHA-256 `2c2a926f75fd3b715b3264cf48ad1afda754c90872e6c87722d8448f92beca66` (1,678,214 bytes). Its operational record is `pilot-first-refined-leaf-v2-outer/operation.json`, SHA-256 `a43b0f0c09b89d1139eebdcd6f1a7e80f77d58c2a8f365d83b92cd9facbcca89` (286,017 bytes). Independent operational review rehashes 279 named paths containing 57,140,178 bytes, including the private output alias. All 94 memory observations satisfy the unchanged limits: maximum sampled aggregate memory is 534,724,608 bytes and maximum observation gap is 283.144625 milliseconds. All three host observations report 55% free memory; combined logs total 46,696 bytes. The exact remaining duration agrees across the registered command, entry event, raw record and fresh Python completion. All observed processes and process groups are absent, the shared lock is released, and no invalidation record exists.

Plainly: the connected calculation completed with its exact inputs and resource limits checked through shutdown. Sampled memory is an observed maximum, not a claim about an unobserved instantaneous peak or the cost of a larger run.

**Measured independent numerical conformance:** a separately authored audit passes 146,995 assertions in 1.234017084 seconds through source closure, authenticating and rechecking 273 files containing 55,129,251 bytes. It reconstructs four mappings from original data, applies the frozen rational comparison to 768 pair components and 320 member intervals, derives eight auxiliary polynomials independently, checks eight exact correlated envelopes using 216 active-state and 64 corner candidates, and verifies eight integral intersections and 24 frame-side witnesses. It does not call the adapter, acceleration subject, quadrature subject or correlated-envelope subject numerically. The summed local integral width is approximately `9.330650781212344e-7`, versus `9.930306565030217e-7` for duration times the serialized whole-cell squared-residual ranges: a **6.03864% width reduction**. Individual reductions are approximately 5.32819% for positive labels and 6.48747% for negative labels. The quoted comparison uses the exact unrounded duration-times-serialized-range baseline; the audit separately respects every outward-rounded intermediate. The peak bound is unchanged and remains strictly below `0.01234` on this leaf only. All eight integral lower endpoints and all 24 witness lower bounds remain zero.

Plainly: preserving the common time modestly tightens the integral estimate, but the checked output still establishes neither balance nor imbalance. The small integral-width number is not the required whole-history RMS or peak accuracy, and cannot be compared directly with that accuracy threshold.

All fifteen scientific-authority flags remain false. No whole-history metric, three-rung agreement, historical trajectory identity, equilibrium, retention or score follows. The consumed readiness version, SHA-256 `724e16df503a0db3662cca56d2e3f51b55f040b81bff6c8bcfb62e3ec7726d4a` (248,078 bytes), was copied byte-identically to ignored `preparation-v2.Tr2YiR/readiness-owner.724e16.md` and compared before this canonical update. That archive preserves consumed text; it was not an original input path and does not make the old invocation replayable against the changed canonical owner. Historical bindings remain unchanged. An independent interval miss, wrong original mapping, changed consumed generation, extra numerical call, resource-limit failure or absent fresh process closure falsifies the corresponding acceptance.

**Subsequent disposition:** the separately reviewed pure geometry-restriction operation and its actual four-interval comparison are independently accepted below. They preserve each accepted emission box, oldest boundary, strict face signs, original history allowances and root identity. Whole-history coverage and three-rung measurement remain outstanding.

Plainly: the next useful improvement is to carry the smaller time interval into the geometric bounds themselves. Repeating evaluations of deliberately unchanged parent bounds cannot remove their uncertainty.

### Independently Accepted Actual Reception-Cell Geometry Restriction

**Derived conditional operation:** the [pure reception-cell restriction](../../../../scripts/eom/f6c_reception_geometry_restriction.py), SHA-256 `e4bc1ff8bd23346f58a934ace429dbf65b11d0b2bb71ebc55dc34036ab9c51e7` (17,602 bytes), applies the already accepted continuous-reception enclosure theorem to a smaller positive-width reception interval. It retains the same pair-specific emission intervals, strict boundary evidence, self exclusions and original history allowances. Only displacement, root distance, transmitter/receiver factors and receiver piece coverage are recomputed. Original scalar allowances continue to contain every preserved axis allowance; both original pieces remain included at a shared closed knot. No optional intersection with old parent geometry is used. The frozen history/interval modules and independent Bernstein geometry reference are unchanged. The [portable controls](../../../../tests/test_f6c_reception_geometry_restriction.py) have SHA-256 `b6c4b4e6a82a11b4ee84c782bf208df4b141860bb8d01f1ad2b2a1ca749a6c7b` (19,827 bytes).

Plainly: the shorter reception interval now narrows the geometry used by the calculation. It does not introduce a different history, search for another root or declare the original premises true.

Thirty supplied synthetic tests pass for the author and both reviewers. Independently fixed expectations, SHA-256 `36dab9068087e6aaf9a26f279552fa7c443ac1bd8b3862bb12908c62ea0b9ccf`, preceded inspection of the implementation. Their separate audit passes 21,423 assertions and 47 rejection controls across eight positive cases, including stationary and moving closed forms, distinct transmitter/receiver factors, uncertainty allowances, cubic Bernstein bounds, closed knots, immutable generations and excluded calls. It checks 448 ordinary rows and 896 coverage records in 0.581696958 seconds; all nine bound source/control/proof files containing 227,774 bytes remain unchanged. These synthetic results alone establish no actual-data accuracy.

The separately reviewed ignored composition is `preparation-geometry.tz8uYA/` under `.local-data/braid-analysis/f6c-single-leaf-diagnostic-20260827/`. Its coordinator is SHA-256 `fd14791d5b0fa308f15251941ca60bcee55f116695068478337426a13e2c1f36` (45,486 bytes), controls `7142944b06502cb66f88e55d206ffd9fba2cb0e58cfc7a1ca3ce5953f5564801` (24,379 bytes), and embedded Python `3b796380cc53d7c75611ff37014d993ee20d7ec837ce14bf920969fd4353208c` (15,197 bytes). All 24 operational controls pass for the author and independent reviewer; a separately authored admission audit passes 41 assertions and 38 semantic rejection controls. Review corrected two connection defects before actual execution: metadata coverage-cache entries were incorrectly counted as numerical work, and a broad forbidden-call list included imported standard-library serialization. Literal composition controls now exercise both actual API obligations, along with context mismatch, wrong call counts, forbidden scientific calls, promoted claims, delayed startup and post-link failure. No scientific source changed in those corrections.

Plainly: the run connection was tested separately from the geometry. A cached input record is not a new calculation, and ordinary result serialization must not be mistaken for one.

The fixed invocation, `preparation-geometry.tz8uYA/invocation.v1.json`, SHA-256 `0458e7611206826c9d71b66a1646de11163a3eff8601dda43d6c741e13bdeee7` (42,884 bytes), binds 25 source files and the unchanged 168-file runtime inventory before launch. The actual run uses the original first refined parent `[0,0.001]` and three positive-width neighborhoods read as fixed data from accepted diagnostic `2c2a926f75fd3b715b3264cf48ad1afda754c90872e6c87722d8448f92beca66`; it does not calculate new quadrature nodes. The metadata-only adapter remains at `9f31d0dd9a670e80e4054e3cba31770849edd17de8d8494b4dc502efd6bf3028`. Exactly four geometry calls perform 64 public history-state queries each, producing 256 rows, 224 ordinary pairs, 32 self exclusions and 448 coverage records. There are zero adapter projections, acceleration evaluations, correlated-envelope evaluations, root/face queries or emission refinements.

Caller session `75641` closes with exit zero and final completion `0bb197`. The accepted raw record is `pilot-geometry-first-refined-leaf-v1/geometry.json`, SHA-256 `e2fc5e3303d42d93869e0abd84fbb54a2a25cebf2fb2ad116a407cb9d2d0935f` (970,886 bytes). Its operational record is `pilot-geometry-first-refined-leaf-v1-outer/operation.json`, SHA-256 `4caa14038780985fd674223fa4703279264bf2554984a5e3f8e141acdc9428f2` (370,069 bytes). The measured geometry stage, including call accounting, takes 70.281606500 seconds. The operational record is stamped at 92.098437792 seconds before publication; the fresh caller output is stamped at 92.277989375 seconds before its final stdout flush. These are not exact whole-process-exit timings. Successful post-flush checks occur before 93.241613375 seconds under the unchanged last-observation bound, and final process exit is observed separately. The original 1,800-second deadline remains authoritative; no time budget is renewed.

Plainly: the bounded run takes about 92 seconds, including input preparation and safeguards. The finer timing labels identify what each measurement actually includes; they are not a forecast for a full-history or parallel run.

Independent operational review rehashes 275 distinct input paths containing 55,439,127 bytes and 283 total named paths containing 57,975,327 bytes, including the private output alias. All 373 memory observations satisfy the unchanged limits: maximum sampled aggregate memory is 543,293,440 bytes, maximum observation gap is 313.026417 milliseconds, and all eight host observations report at least 55% free memory. Combined logs contain 181,475 bytes. Ten observed process IDs and three process groups are absent; the shared lock is released and no invalidation record exists. The actual 261-file adapter provenance contains 53,138,569 bytes and has compact-JSON/no-newline digest `df2ac4877538b54751efff01fae0531b608138d9aabf0b83b1feb58e256d52fd`. Only the readiness-owner version differs from the prior accepted adapter capture; the old provenance is not relabeled.

**Measured independent numerical conformance:** caller session `49826`, exit zero, completion `994388`, passes 226,285 assertions in 2.289759209 seconds through source closure. The separate audit authenticates and rechecks 278 files containing 56,822,966 bytes, reconstructs all eight generation labels from all 14,080 original segments and their unchanged tokens, and checks the original pair-specific emission and boundary evidence. Frozen reference `19c57e9b638b0beb866c86b061b2325f9567add2a85608f0c42ef1f7612d9132` supplies an independently authored exact Bernstein state calculation, nonempty root-distance intersection checks and separate transmitter/receiver factor checks. It verifies 672 displacement components, 224 distances, 448 factors, all 256 rows and all 448 ordered coverage records, including 640 closed-piece visits. Its 88 cached reference-state calculations are separate from the subject's verified `[64,64,64,64]` call counts. No subject geometry, root, face or acceleration calculation is replayed as the reference.

Plainly: the smaller boxes were checked against independently reconstructed original histories, not merely against a second execution of the same geometry code.

All 336 whole-cell geometry intervals equal their parent intervals. At each of the three smaller neighborhoods, all 336 geometry intervals are strictly narrower and contained within their respective parents: 1,008 narrowed intervals in total, with no widening, failed containment or zero-width-parent case. The following measured percentages compare the sum of endpoint widths within each indicated field, not a norm, residual, score or physical observable. Main's separate exact-rational reduction of the stored records agrees with the independent audit.

| Neighborhood | Displacement widths | Distance widths | Transmitter-factor widths | Receiver-factor widths |
| --- | ---: | ---: | ---: | ---: |
| First | 45.376230% smaller | 52.347661% smaller | 26.238496% smaller | 87.548616% smaller |
| Middle | 45.376230% smaller | 42.052209% smaller | 22.631514% smaller | 86.676956% smaller |
| Last | 45.376230% smaller | 52.335988% smaller | 26.242352% smaller | 87.557624% smaller |

Plainly: carrying the shorter reception interval into the geometry removes a substantial part of the box uncertainty in this local comparison. It does not show how much the acceleration, integral, RMS or peak calculation will improve; none of those calculations was performed here.

All ten scientific-authority flags remain false. The separate `accuracy_guaranteed` flag is also false on each result. No whole-history metric, three-rung agreement, equilibrium, retention, historical trajectory identity or score follows. The consumed owner `ebb984f40c0252ed766da950e717c3bd34c18812e63dc1ee8cc0492d60978ea5` (256,466 bytes) is preserved byte-identically as `preparation-geometry.tz8uYA/readiness-owner.ebb984.md`. The accepted adapter and its controls are also preserved byte-identically there as `adapter.9f31d0.py` and `adapter-controls.a439ca.py`. These archives preserve consumed bytes; their new paths are not substituted for original input paths and do not make old invocations replayable against later canonical edits. An independent enclosure miss, omitted original allowance or closed knot, changed emission/boundary evidence, extra numerical call, changed consumed generation or missing fresh operational closure falsifies the corresponding acceptance.

**Subsequent disposition:** the explicit restricted-projection adapter, single-leaf composition and actual bounded comparison are independently accepted below. The connection is valid, but its local integral bound is wider than the prior accepted bound. Preserve both results and the inherit-only route; whole-history coverage, required accuracy, three-rung measurement and large parallel search readiness remain open.

Plainly: the geometry restriction is checked. The completed connection and integral comparison below establish what the tighter intermediate bounds do, and do not, improve.

### Independently Accepted Actual Restricted First-Leaf Integral Comparison

**Accepted implementation, with unchanged numerical references:** the [canonical adapter](../../../../scripts/eom/f6c_variable_cell_adapter.py), SHA-256 `b2bd53ed4614441a42220a6190696c9ca550caad618de21e5fc405c0c6182bde` (53,233 bytes), now provides an explicit `project_restricted()` route while retaining inherit-only `project()`. It computes and validates the restricted rows itself, then issues one genuine owned projection; callers cannot substitute rows or manufacture evaluation ownership. Construction maps original history tokens and authenticated guards without numerical evaluation. Separate accounting records attempted/completed restrictions, actual public history-state calls and successfully issued restricted projections. Its [controls](../../../../tests/test_f6c_variable_cell_adapter.py), SHA-256 `a8c4173f616decef982d5ce59819de5cc0dfd003837c8cb23cf6bba04fda2b07` (50,553 bytes), pass all 57 tests. Independent review first returned a missing deadline check after final validation; the repaired generation checks immediately before issuance. All four late-validation/construction controls across both routes now reject without issuing a projection. The independent reference harness passes 13,301 assertions and 41 rejection cases; the earlier 94 lifecycle checks and 49 repair/source checks pass, with all 31 frozen dependencies unchanged.

Plainly: restricted geometry now enters through the normal ownership rules. The deadline repair prevents a result from being issued after the allowed time expires; it does not change any mathematical reference.

The [single-leaf diagnostic](../../../../scripts/eom/f6c_single_leaf_diagnostic.py), SHA-256 `1d693a8cf07549caafa98c0f48d87f4b2d50a9681596378130d455f4f7ac0b96` (8,180 bytes), adds `measure_restricted()` and an explicit restricted-mode wrapper around the unchanged legacy result. Its [controls](../../../../tests/test_f6c_single_leaf_diagnostic.py), SHA-256 `0d6897eb31e4d01260190c27e2486690301b47fc5ad604eb73ea8197de774215` (17,061 bytes), pass 18 tests. Before successor implementation, independent expectations were frozen at `013debec41bd8e614cc3eb21f9f5eb8e2ab4f9971c7bbb06c791b5e85692266d` (5,024 bytes, 32 groups). Independent review passes 4,525 assertions and 205 rejection cases, including four analytical constant-acceleration cases, exact legacy-result preservation, observed unequal state-call deltas, and numerical-work injection at every progress callback. None of these synthetic controls evaluates the actual retained history.

**Measured actual execution:** ignored `preparation-restricted.KpXnOb/coordinator.mjs` has SHA-256 `b20445c1a569f9447223261c2a087b3999c5cf2c873d08d1d452c44b690cbc6b` (36,376 bytes); its controls are `3363d4487700e16760c2398d358ac0bfb4d75a0b6145e386698edec6d5d9de15` (20,632 bytes), and captured Python is `6afb66ef2d4f425cbb63c2523a5d6833d5593e884393f17f3b2b60adc9656f3a` (8,854 bytes). Main and independent review each pass all 23 process controls; 40 independent admission assertions and 38 semantic rejection cases also pass. The original 1,800-second deadline, duration bridge, memory/disk thresholds, shared lock, process ownership and guarded final output are unchanged. Definition-only loading confirms the existing 168-file runtime inventory without numerical calls.

Plainly: the calculation uses the already reviewed safeguards. A successful process is necessary but is not the independent numerical check.

The fixed invocation `preparation-restricted.KpXnOb/invocation.v1.json`, SHA-256 `5c63c5d727112df3e837a729bde456644662108d489b7f95876c01a116596a37` (39,850 bytes), binds 11 explicit sources and 168 runtime files. Before the first numerical call, the actual context verifies 263 distinct source bindings, eight histories with 1,760 original pieces each, 81 accepted frames, 160 parent cells and zero numerical counters. It then performs four restrictions with 64 public history-state evaluations each, four acceleration-range evaluations, eight correlated residual evaluations and one leaf evaluation. There are zero root queries, emission refinements, adaptive splits or full-history aggregations.

Fresh caller session `50255` closes with exit zero in completion chunk `621571`. The operation records `35.834906124999996` seconds before publication; the fresh final-stdout stamp is `36.01903654099999` seconds before its final flush, not an exact whole-process-exit duration. Independent operational audit rehashes 280 distinct named files totaling 55,604,722 bytes; the adapter portion contains 263 files totaling 53,210,985 bytes, with compact provenance-array SHA-256 `d3db695f5e849f9604690136ccbf879366cf6cd6ee15af378b995cfedac218c9`. All 149 resource samples pass, with maximum aggregate RSS 542,261,248 bytes and maximum observation gap 290.303667 milliseconds. Four host checks remain at least 55% free with at least 964,164,132,864 available disk bytes. Combined logs total 73,947 bytes. All six observed owned PIDs and three process groups are absent, the shared lock is released, and no terminal invalidation exists.

Plainly: this one bounded calculation completed in about 36 seconds and used about 517 MiB at its measured peak. Those observations do not forecast the cost of a whole-history or parallel search.

The frozen raw output is `pilot-first-restricted-leaf-v1/diagnostic.json`, SHA-256 `e4a1c55ec1bc65a87ecd4d83aa3ca077e1fcb47aeb735839f21c25e9c4716a0e` (1,679,383 bytes), with operation `pilot-first-restricted-leaf-v1-outer/operation.json`, SHA-256 `a494ed8d1ae4b1afeca4bcc57a570486062defa675990891e5261e5a9d3fad33` (302,781 bytes). Independent actual expectations were frozen at `55d6ff96daf423d99fc5768521194d536f6505fb31b1b108bb174625a873aec2` (3,989 bytes, 15 obligations). The separate audit closes in session `27041`, chunk `43da82`, with 188,360 assertions over 17 necessary frozen files totaling 14,344,013 bytes, checked before and after in 1.457183833 seconds. All 256 projected mathematical rows match the independently accepted geometry; 448 coverage records preserve 640 closed-piece visits. The frozen independent acceleration comparison checks 768 pair components and 320 member intervals; separate arithmetic derives eight polynomials, checks 216 active-state and 64 corner candidates, and validates eight integral intersections and 24 witnesses. It makes no numerical calls to the adapter, geometry subject, acceleration subject, quadrature subject or correlated-envelope subject.

**Measured precision outcome:** the exact local summed integral width is approximately `9.627503750961199e-7`, versus the prior accepted `9.330650781212344e-7`: the new bound is **3.18148% wider**, not an improvement. It remains 3.04928% narrower than duration times the serialized whole-cell squared-residual ranges, approximately `9.930306565030217e-7`. Positive-member integral widths widen by about 0.92996%; negative-member widths widen by about 4.62151%. The recorded exact rational leaf width and the sum of outward-rounded serialized integral widths are distinct: serialization adds approximately `2.71667e-96` here and `1.36667e-96` in the prior result. This rounding distinction does not change the reported precision comparison. Peak bounds are unchanged and remain below `0.01234` on this leaf only. All 96 residual coordinates still contain zero; all eight integral lower endpoints and 24 witness lower bounds remain zero.

Plainly: smaller intermediate geometry boxes did not automatically improve this integral method. The new node bounds change the chosen auxiliary polynomial, while the whole-cell acceleration boxes stay unchanged. Its polynomial integral decreases, but its uniform remainder grows by more; the independently checked total becomes wider. This is a valid calculation with a worse precision result, not a rejection of the geometry.

Preserve both accepted results and the better prior integral bound. All fifteen scientific-authority flags remain false; there is no full-history RMS/peak accuracy, three-rung agreement, equilibrium, retention, historical trajectory identity or score promotion. The consumed owner `c9e8d185a517dab6b8746565e4a01350456190e0829f92de652d624284995476` (266,881 bytes) is preserved byte-identically as `preparation-restricted.KpXnOb/readiness-owner.c9e8d1.md`. The old diagnostic/control bytes are also preserved in `preparation-geometry.tz8uYA/single-leaf.046d81.py` and `single-leaf-controls.c8cddf.py`. These archive paths preserve consumed bytes and are not substituted into historical source bindings. A changed consumed generation, independent enclosure miss, omitted original allowance or closed knot, extra numerical call, resource failure or missing fresh operational closure falsifies the corresponding acceptance.

**Subsequent disposition:** the bounded first-parent bisection and its independent actual comparison are accepted below. It preserves the original frame/parent identity, immutable history and emission evidence, and existing shared split allowance; the local integral and peak bounds both improve. Whole-history accuracy and three-rung measurement remain outstanding.

Plainly: shrinking the whole interval improves this local bound. It does not turn a partial partition into a full-history metric.

### Independently Accepted Actual First-Parent Bisection

**Implementation and independent controls:** the [canonical diagnostic](../../../../scripts/eom/f6c_single_leaf_diagnostic.py), SHA-256 `eee06dbacc6c7fb114c6ba9cefb1fbf4e52ed905b99930fe7d71c62d62e1ad73` (19,474 bytes), adds `measure_bisected_restricted()` for the two closed children `[0,0.0005]` and `[0.0005,0.001]`. Its [controls](../../../../tests/test_f6c_single_leaf_diagnostic.py), SHA-256 `0cac28e332a1716a7a22f11fa39798b1596eb70ccb0f44801345f853d7a53fcf` (33,759 bytes), pass all 33 tests. Independent expectations were frozen at `2c75ba7effbd62c355426e081fb8caca821663e257347e12f7951d1821055cf6` before successor inspection. Independent review passes 33,175 assertions and 169 rejection controls, including separately derived constant, quadratic and mixed-member answers and genuine synthetic-adapter composition. The existing adapter and all 31 mathematical dependencies remain unchanged. Callback identity and same-member witness bounds at closed shared endpoints are checked explicitly.

The measured diagnostic/control generation is also preserved byte-identically as `preparation-bisected.C3NRIb/single-leaf.eee06d.py` and `single-leaf-controls.0cac28.py`; later connector implementation does not replace these consumed bytes.

The local request generations `160/161` and paths `[0,0]/[0,1]` are reserved diagnostic identities, not requests issued by a running global protocol. The shared frame budget counts the mandatory `0.001` cut and the new `0.0005` cut: two of twenty events are used, with eighteen remaining. The diagnostic does not choose a global-priority leaf, execute the adaptive state machine or aggregate the complete history. Both older single-leaf entry points retain their exact results; the preceding consumed diagnostic/control bytes are archived as `preparation-restricted.KpXnOb/single-leaf.1d693a.py` and `single-leaf-controls.0d6897.py`.

Plainly: this is one additional subdivision of the same parent interval, not a new geometry or a completed full-history calculation. The split allowance is shared, not reset for each child or metric.

**Measured execution:** `preparation-bisected.C3NRIb/invocation.v1.json`, SHA-256 `cc207557672f82dac602f4081d741452441dd8976b06cc22ece9c9c220d4da0f` (39,848 bytes), binds eleven explicit sources and 168 runtime files. Its coordinator is `56a9ff08ca948f0079685b1704e1f09bb0ce36a98abf4bd7b79e837ff7c708d2` (38,224 bytes), controls `fa88f6b26aa8319e0fc47e9c5e47ebac510a6b95aac29341e423997c2a7b446f` (22,741 bytes), and captured Python `b66f80b3269611918afa6ab8e46568eb554feeb883232278935e530d972ba79b` (8,931 bytes). Main and independent reviews each pass 28 process controls; independent operational review adds 3,097 assertions and 104 rejection cases. Definition-only loading finds no runtime additions. The original 1,800-second deadline, clock bridge, memory/disk thresholds, shared lock and final-output safeguards are unchanged.

Fresh session `87754` closes successfully in chunk `f6610b`. Before numerical work, the context verifies 263 sources, eight histories of 1,760 pieces, 81 accepted frames, 160 parents and zero numerical counters. The actual calculation performs eight restricted projections, eight acceleration evaluations, sixteen correlated residual evaluations and two leaf evaluations. Each restriction uses 64 public history-state calls, totaling 512; these are observed counts, not a hardwired acceptance requirement. There are no new root queries, emission refinements, global-priority decisions or full-history metrics.

Independent operational audit checks 280 named files totaling 57,382,495 bytes. The adapter's 263-source provenance array totals 53,220,430 bytes and hashes to `80ef06c676d6d118a3d3bd73f951d6263f7ddbe4ff4c7a7fcfeae50ede4ad8b9`. All 210 resource observations pass: maximum aggregate RSS is 546,275,328 bytes; maximum observation gap is 293.333875 milliseconds. Five host checks stay at least 55% free with 964,156,809,216 available disk bytes. Combined logs total 104,322 bytes. All six observed owned processes and three process groups are absent, the shared lock is released and no terminal invalidation exists. The operation records 51.008399959 seconds before publication; the fresh final-stdout stamp records 51.199613167 seconds before its final flush, not an exact whole-process-exit duration.

Plainly: the bounded test completed in about 51 seconds with about 521 MiB measured peak memory. This is observed local cost, not a prediction for 160 parent cells or many parallel workers.

The raw output is `pilot-first-bisected-parent-v1/diagnostic.json`, SHA-256 `eeb93fb5ad232b97972c6357c8774af7b68ca9383173183574e2f546f26535a4` (3,378,326 bytes); its operation is `pilot-first-bisected-parent-v1-outer/operation.json`, SHA-256 `3499b38a8ef29a16f124092e0d8860c6139d9cda39f6d9d0e30239192992ca74` (309,844 bytes). Independent actual expectations were frozen at `79697b6b724adf32a5aa3c64c9ab95e0de379982e8dbafacd5e6b06e35d9e574` before execution. The separate numerical audit closes successfully in session `82442`, chunk `658016`, with 225,568 assertions over sixteen necessary frozen files totaling 16,774,609 bytes, rechecked before and after in 3.358577417 seconds. From the original histories, the frozen independent geometry reference checks all 512 rows, 896 coverage records and 1,168 closed-piece visits; all 2,688 geometry bounds are strictly narrower and contained in their parent bounds. Eight frozen independent acceleration comparisons check 1,536 pair components and 640 member intervals. Separately derived local-coordinate arithmetic checks sixteen polynomials and Hermite acceleration functions, 432 active-state candidates, 128 corners, sixteen integral intersections and 48 witnesses. It makes no numerical calls to the adapter or geometry, acceleration, quadrature or correlated-envelope subjects.

**Measured local precision:** the sum of exact child integral widths is approximately `6.252616560786139e-7`: 32.98842% narrower than the better prior inherit-only result and 35.05464% narrower than the restricted unsplit result. Summing outward-rounded serialized child endpoints adds only `4.1875e-97`; this is recorded separately from the exact leaf-width sum. The local peak upper bound falls by 26.84505%, to approximately `0.009023357730558125`, while its lower bound remains zero. All 192 residual-coordinate intervals still contain zero; all sixteen child integral lower endpoints and 48 witness lower bounds remain zero. Preserve both preceding accepted records; this result improves the bound only over their common first-parent interval.

Plainly: reducing the whole-child interval improved precision where narrowing node neighborhoods alone did not. It does not demonstrate a nonzero mismatch, acceleration balance, retention or physical validity. No complete-history accuracy or three-rung agreement follows, and all fifteen scientific-authority flags remain false.

**Historical next step, now completed below:** connect genuine outstanding requests from the existing frozen `start/request/respond` protocol to a request-driven restricted leaf-response provider. Use each request's original frame and domain, preserve genuine adapter ownership, hold one injected integral-reference identity throughout the protocol state, and leave leaf evaluation to `respond` exactly once. Derive the original frames and mandatory parent cuts from captured metadata; do not manufacture global request generations or change the twenty-event shared budget. That bisection diagnostic is fixed to frame zero and did not supply this bridge; the independently accepted successor below now does. Only parent zero currently has refined emission intervals; the other 159 retain their broader parent evidence. Complete-domain cost, retained-record memory/output size, precision and the three-rung ladder remain separate unmeasured obligations; a local improvement is not parallel-search readiness.

The consumed readiness owner `a57830cdc0edc9892150da54373788298292eb0ec9c0299772b77b692f349d4a` (276,326 bytes) is preserved byte-identically at `preparation-bisected.C3NRIb/readiness-owner.a57830.md`. The archive preserves consumed bytes without replacing any historical source path. Changed consumed bytes, an independent enclosure miss, an omitted closed history piece, an extra numerical call, incorrect local/global identity, budget reset, or missing fresh process/resource closure falsifies the corresponding acceptance.

Plainly: the local bisection blocker was closed at this stage. The subsequent connector test below completes the connection and identifies the next precision and resource obligations.

### Independently Accepted Actual First Four Genuine Requests

**Connector closure:** the [canonical diagnostic](../../../../scripts/eom/f6c_single_leaf_diagnostic.py), SHA-256 `5b890b4dbafa676bd1e7e67734701467dca6615ad49f46adecd4ba65ef40092f` (31,044 bytes), now supplies `LeafResponseSession`. Its [controls](../../../../tests/test_f6c_single_leaf_diagnostic.py), SHA-256 `6c925c3bc8b6764d007f92f3055f2a02c688182cf5b052123e826c346275d3c4` (57,484 bytes), pass all 48 tests. The independently frozen expectations `535ed1dfd3603e72ae7f54d64da4166a586a1eaf7b27c654ad31c483487b14be` precede the successor; independent review passes 52,181 assertions and 330 rejection controls, including separately derived constant, quadratic and mixed-member cases. The three older diagnostic entry points preserve their results and progress sequences. The old global-protocol exclusion test now checks the complete helper-call graph of the local bisection entry point, rather than incorrectly prohibiting the new protocol connector elsewhere in the module. The adapter and all 31 mathematical dependencies remain unchanged.

The session captures one integral-reference identity, starts the frozen protocol once and supplies only its genuine outstanding request. A response provision performs restricted geometry and correlated-residual work but never leaf evaluation; `advance` calls the frozen `respond` exactly once, which owns that evaluation. Original frame endpoints and mandatory cuts come from captured metadata. Foreign, duplicate, stale, reentrant or partially failed work cannot be retried as a fresh request or reset its budget.

Plainly: the validated local calculation is now connected to the full-history controller. This closes the missing software connection, not the full-history measurement.

**Measured execution:** the independently reviewed composition is `preparation-requested.FMruCE/invocation.v1.json`, SHA-256 `91a8a9bfdacfd10502370603363624e263faef2222df9a4ddcbafe2099444eb5` (39,842 bytes). It binds eleven explicit sources and the unchanged 168-file runtime. Its coordinator is `f281779947b44f2cd9960d62f743637c304066fddabe426fa7b9196e7e9862f7` (43,335 bytes), controls `4b02410b5822842a377f122a7ed389c68f6de4bfddc2a9a510b1010150a7ecf8` (26,355 bytes), and captured Python `f85636066cda88e8a37f62e3cfe15f5e848017f3b0cbba7ab76fa8cb1a5ebb10` (12,277 bytes). Main and independent reviews each pass 34 synthetic operational controls; the independent operational review adds 3,452 assertions and 170 rejection cases. Definition-only loading introduces no runtime dependency. Original deadline, resource thresholds, shared lock, source checks and final-output safeguards are unchanged.

Fresh session `22229` closes successfully in chunk `0ac50c`. Its four genuine initial requests cover `[0,0.001]`, `[0.001,0.002]`, `[0.002,0.0030000000000000001]` and `[0.0030000000000000001,0.0040000000000000001]`, in original frames `0,0,1,1` with generations `0,1,2,3`. The second frame's mandatory cut is the original token, not a newly rounded midpoint. The calculation performs sixteen restricted projections, sixteen acceleration evaluations, 32 correlated-residual evaluations and four protocol-owned leaf evaluations. The observed sixteen history-state call counts are four counts of 64 followed by twelve counts of 16, totaling 448. No root query, emission refinement, adaptive split or full-history aggregate occurs. The final protocol state retains 160 leaves, four evaluated and 156 pending, with the original eighty split counts all equal to one; its next unconsumed request belongs to frame two, generation four.

Plainly: this is a completed four-request test across the first two original frames. It has not processed the remaining 156 initial requests or started adaptive refinement.

**Operational evidence:** the raw output is `pilot-first-two-frames-v1/diagnostic.json`, SHA-256 `1d0b865b63a1cbddae80357c7a0fb89966cd4755cd7ab0aff95e9ba804c0cdc2` (6,363,528 bytes), and its operation is `pilot-first-two-frames-v1-outer/operation.json`, SHA-256 `1b13b4d1593880cb5acb7c298584f099ff2485906cdc69713b7dc28b4a39af1b` (345,173 bytes). Independent operational audit `7706d9` rechecks 280 files totaling 60,525,976 bytes; the adapter's 263-source array totals 53,229,079 bytes and hashes to `fea778ac5e3777caef3478d393baa01bd5c2cf049c0985547b2c8b75973bda48`. All 349 resource observations pass, with maximum aggregate RSS 548,061,184 bytes and maximum gap 291.01875 milliseconds. Seven host checks stay at least 53% free with 964,302,753,792 available disk bytes. Combined logs total 174,609 bytes. All eight observed owned processes and three groups are absent, the shared lock is released and no terminal invalidation exists. The operation records 86.224904916 seconds before publication; the final stdout stamp records 86.4028005 seconds before its final flush, not an exact whole-process-exit duration.

Plainly: the test completed in about 86 seconds with about 523 MiB measured peak memory. These are measured costs of four requests, not a certified cost for all 160 parents or parallel workers.

**Independent mathematical evidence:** expectations `075bb149ddb1543403b1597fdfe89335786ee7544b11b41edd7a8e5a9b88486d` were frozen before execution. Session `78873` closes successfully in chunk `46c108`, with 369,188 assertions over 21 necessary frozen files totaling 53,350,687 bytes, rechecked before and after in 10.978606292 seconds. From the original export and original parent evidence, the frozen independent geometry reference checks 1,024 rows, 1,792 coverage records and 1,070,800 closed-piece visits. Sixteen independent acceleration comparisons check 3,072 pair components and 1,280 member intervals. Separate exact arithmetic checks 32 local-coordinate polynomials, Hermite acceleration functions, correlated envelopes and integral intersections, plus 96 witnesses. Whole-cell geometry is unchanged; every node-neighborhood bound is contained in its original parent. No numerical call to a subject adapter, geometry evaluator, acceleration evaluator, quadrature evaluator or correlated-envelope implementation supplies this reference.

**Measured precision boundary:** exact integral widths are approximately `9.627503750961199e-7`, `3575.638634267788`, `3574.896268402274` and `3574.220472545866`. The first request exactly matches the earlier restricted single-leaf integral and peak result. Its interval alone is comparable with the preceding first-parent and bisection records; the four-request sum spans a different domain and is not an improvement or regression against those one-parent totals. All 384 residual-coordinate intervals contain zero, and all 32 integral lower endpoints and 96 witness lower bounds remain zero. Only parent zero uses refined emission intervals; the next three retain their much broader original emission evidence. Their large uncertainty widths are not measured residual magnitudes and establish neither balance nor imbalance.

Plainly: the connection works, but useful precision does not transfer from the first refined parent to the unrefined parents. The next obstacle is the width of their certified emission-time intervals, not a demonstrated failure of the asymmetric counter-breathing representative geometry.

**Named field artifact, independently accepted below:** independently checked parent-specific emission refinement for original parent one, reception `[0.001,0.002]` and inherited emission interval `[-8,-0.049]`. The [parent-specific comparison contract](2026-08-27-f6c-parent-emission-refinement-reference.md), SHA-256 `652d77241f9b5c082e7d15e2bb62328f346760548f9f13e4ffe7562c4cad0733`, and its pure comparison are now independently accepted as recorded below. The independently accepted source-bound preparation, verification and actual run are recorded below. The next connection supplies this checked parent to the existing adapter. Preserve all original history allowances, member identities, closed-piece coverage and causal bounds. The existing cell-zero recipe's `[-8,-0.05]` interval and negative-upper-endpoint restriction cannot be transplanted: later admitted parents have different emission endpoints, including positive upper endpoints that still strictly precede their reception windows. The generic comparison validates each declared parent's inherited intervals and exact finite-decimal midpoint bounds; authentication against the complete original cover remains the caller's separate obligation. Retain the fixed two-sided 32-query algorithm, original arithmetic precision, original complete-cover checks and unchanged watched limits; a new parent attempt needs its own reviewed invocation and fresh closure. No full 160-parent refinement or parallel cost is established by this accepted one-parent step.

Complete-history output and retained-memory bounds remain separate implementation obligations. The measured four-request record contains 5,687,939 bytes of repeated full range records and repeats a 50,978-byte source array in each provision. A compact stream may share metadata and retain reconstruction-complete evidence, but dropping records or clearing the adapter's private ownership maps is not authorized. Reuse of identical transmitter-state calculations is a separately identified optimization, not an implemented or measured speed improvement. Neither optimization may hide logical work, reset counters or weaken independent reconstruction.

Plainly: prepare the next certified parent interval and a bounded full-run evidence path before scaling. No large parallel launch, full-history accuracy, three-rung agreement, score increase, equilibrium, retention or physical claim follows from the connector test.

The consumed readiness owner `0b1e0c70a2da771f97b627765be1af769327291b1d9827dbd9b2747ef5842ac0` (284,975 bytes) is preserved byte-identically as `preparation-requested.FMruCE/readiness-owner.0b1e0c.md`; original source bindings continue to name their consumed paths and hashes. All fifteen scientific-authority flags remain false. A changed consumed generation, independent enclosure miss, altered parent token, omitted closed history piece, extra or duplicated numerical call, budget reset, resource failure or missing fresh closure falsifies the corresponding acceptance.

## Independently accepted parent-specific emission comparison

The [pure comparison](../../../../scripts/eom/oracle/f6c_parent_emission_refinement_conformance.py), SHA-256 `ffe91ad7cbfe4e41bf92203fe73b4195e0ad1437176dace9d12751e68aa2cbec` (21,854 bytes), is independently accepted against the unchanged exact Bernstein reference `19c57e9b638b0beb866c86b061b2325f9567add2a85608f0c42ef1f7612d9132` and the separately reviewed mathematical contract `652d77241f9b5c082e7d15e2bb62328f346760548f9f13e4ffe7562c4cad0733`. Its [controls](../../../../tests/test_f6c_parent_emission_refinement_conformance.py), SHA-256 `18c21d6e84d0d6ae7e3b4ea35861a75b38d362d8aad1e0cc14715cea167a5a04` (31,746 bytes), pass 33 tests in both reviews. Main session `86822` closes successfully in chunk `095d2f`: 36.242 seconds for the suite and 36.333529167 seconds for its watched wrapper, with all three source/control/proof pins unchanged.

The independent expectation contract `13ef2eefe90093bf58c5762ae56e644505bb480a318c1ca7398271dd1f424c2f` and known-answer fixture `b8739bd8bed0313a3a43fb21ba6019e780488fd1a136bedb146f23f0289a8bee` were frozen before subject review. The review passes 56,476 assertions and 114 rejection controls. Seven separately derived cases cover 25,088 queries, 448 rows and 784 piece records, including stationary and moving histories, pair-specific original intervals, positive causal upper endpoints, 51-place derived times, closed-knot ownership, and nonzero scalar allowances with smaller axis allowances. Nested calls, callback mutation, acknowledged prefixes, invalid inputs and old cell-zero parity are checked. All review jobs close successfully; 44 source/reference/control files totaling 1,151,428 bytes remain unchanged. These are synthetic instrument checks, not evaluations of the actual retained histories.

Plainly: the measuring instrument can now check a different original parent without assuming that it has the first parent's time interval. It has not yet tightened that next interval or measured the complete history.

The interface preserves the original parent identity and time spelling, uses local query/row/piece indices, and binds the complete closed original-history records including axis and scalar allowances. Each side starts again from its pair's original emission interval; the oldest boundary at `-8` remains distinct from the retained lower face. Only strictly signed whole-face bounds can move a retained face. The reference copies bounded inputs before helper work or callbacks and retains an immutable completed prefix on failure. Declared hashes and original-parent indices are not self-authenticating: the source-bound caller must still establish original-cover membership, the exact eight-by-1,760 piece census, unchanged premises and dependencies, and successful bounded execution.

The separately authored production proposer is subsequently accepted below; source-bound preparation and checking are also accepted below. The reviewed parent-one invocation and its independently checked actual result are recorded below. The production library divides proposals at interior original reception knots; the single-parent producer rejects such a proposal before numerical work rather than hiding extra rows. Original closed boundary pieces and emission-side interior knots remain required. This production scope is narrower than the pure comparison's mathematical domain and does not change that domain. Metadata inspection of unchanged export `f479bb88a6425e9e98e00288f2524f33d5a3c0f4c2a14139dbaae4f468c46db1` confirms that all eight members have the exact original segment `[0.001,0.002]` for parent one. No actual numerical evaluation or full-history execution follows from that inspection.

Plainly: the pure checker, producer and bounded original-parent-one execution are now independently accepted below. Full-history evidence storage, precision and three-rung agreement remain open. All fifteen scientific-authority flags remain false. A missed whole-face enclosure, altered original token, omitted closed piece, false prefix or changed frozen generation falsifies the corresponding acceptance; no metric, score, equilibrium, retention or physical claim is promoted.

## Independently accepted parent-specific emission proposer

The separately authored [pure proposer](../../../../scripts/eom/f6c_parent_emission_refinement.py), SHA-256 `1517575f3df783af36d2bf2b758d19427e8ec85247efec892783716c263b7c27` (27,692 bytes), and [controls](../../../../tests/test_f6c_parent_emission_refinement.py), SHA-256 `f1650b5e73a06ecd7ed05bff10ba97949b42aa5330e84fb3514c2f868eff0fc2` (27,393 bytes), are independently accepted. The expectation contract `f4d2b6b28a55468df204750edde3cae25bf61355b951b44e604a5a9f68f6a00b` and exact-answer payload `cc0b4ec986aa51f27908e2463283d03d2d98d6304ecc0e85afcf5a352dd31312` were frozen before reading the subject. Main session `96842` closes successfully in chunk `4e3a5b`, with 25/25 supplied tests passing in 45.742 seconds. Independent review adds 226,704 assertions and 63 rejection checks in session `67001`, chunk `76b558`, closing successfully in 59.407858917 seconds; eleven actual dependency/source/control files totaling 326,926 bytes remain unchanged.

Seven predeclared exact cases check 25,088 queries, including moving and stationary histories, positive causal emission endpoints, per-pair original intervals, 51-place derived times and strict sign equality. Additional controls check complete closed pieces, uncertainty allowances, callback precision changes, nested calls, attempted-versus-acknowledged counts and incomplete output. The producer uses only the unchanged public production helpers, performs 3,584 whole-face queries followed by one complete-cover call, preserves original reception spelling and parent identity, and emits local row/piece indices. It never imports its independent comparison. All fifteen authority flags remain false on success and error; a successful sink callback acknowledges a record but does not by itself establish durable publication.

Plainly: the calculation that will tighten the next interval now has a checked implementation. These were synthetic cases, not a run on the saved asymmetric counter-breathing representative histories.

**Source-bound prerequisite, completed below:** preparation and an independently authored checker for original parent one. Both reconstruct the input from the accepted full original cover: rows `64–127`, pieces `112–223`, original parent index `1`, frame `0`, reception `[0.001,0.002]` and per-pair original emission `[-8,-0.049]`. Authenticate the original global indices before creating local output indices; do not relabel old files or borrow cell-zero membership. They independently derive the eight closed history records and their complete token-generation hash, including all axis and scalar allowances. The historical ancestry and current execution/runtime bindings are separate inventories. A reviewed one-parent invocation must retain the original inclusive deadline, memory and file limits, unchanged-file checks, exclusive publication and fresh process closure. A bad source binding, missing piece, false prefix or independent enclosure miss falsifies admission. No full 160-parent refinement, full-history accuracy or parallel capacity is established.

## Independently accepted original-parent preparation and checking

The source-bound preparation and separately authored checker preserve the frozen pure proposer and independent mathematical references.

| Artifact | Reviewed SHA-256 | Bytes |
| --- | --- | ---: |
| [Preparation](../../../../scripts/eom/prepare-f6c-parent-emission-refinement.py) | `492882b63f074fd46253ee92974524c4fd6b43ae6190db23797c307251ed8544` | 57,641 |
| [Preparation controls](../../../../tests/test_f6c_parent_emission_refinement_preparation.py) | `06cd99bc1f74c3b7dead6089ef20f468f7be8af41ae6702f45ec85d83a1a36ab` | 40,808 |
| [Independent checker](../../../../scripts/eom/verify-f6c-parent-emission-refinement.py) | `0bb16c232736c895c4f3e38a75e2a0562084710ffdba2503b3ab4457216127fc` | 46,134 |
| [Checker controls](../../../../tests/test_f6c_parent_emission_refinement_verification.py) | `92da2b09c629ecbc0fdcdddac9de69353da0e29795e0b1d3bf2d23a05a9a26f7` | 39,696 |

Plainly: these tools bind the saved inputs to the next calculation and its independent comparison. Their acceptance is not evidence that the next refinement has run.

Preparation's 35 supplied controls and the checker's 27 controls pass independently. The checker additionally rejects six externally injected cleanup mutations with 37 independent assertions; its review closes in session `72739`, chunk `c9607c`, and the separate mutation check in `7efe5f`. Repairs cover runtime checks around the captured proposer, immediate registration of opened output streams, exact public/private hardlink identity and bounded source recapture after cleanup. Final checks retain original byte lengths, hashes and file identities; they reject a changed source or byte-identical replacement rather than treating it as the original generation. Foreign replacements are not deleted. The fixed nineteen dependency files, totaling 465,429 bytes, remain unchanged. The preparation review closes in session `38116`, chunk `a04930`, with 35/35 supplied controls passing in 25.359 seconds. Its separate 87-assertion, seventeen-rejection audit closes in session `77262`, chunk `4f614f`, after 2.836214083 seconds, including twelve external source/plan cleanup mutations. All twenty-three reviewed files, totaling 649,708 bytes, remain unchanged.

**Measured saved-input validation:** main expectations `b801a5ada5c5706035db561c7be387e3d47ea5fb64d212404b5f970ece2db39e` (11,207 bytes) were frozen before either wrapper was implemented. Each wrapper independently matches those expectations: all eight closed 1,760-piece histories, complete original 10,240-row and 17,920-piece streams, original parent `1`, frame `0`, reception `[0.001,0.002]` and all 56 original emissions `[-8,-0.049]`. Their full closed-history generation is `2ebdc2c9c7ccdabaaf798eae16ee1b333f25707736602e7246c1b12fb6459cd1`; the exact parent object hashes to `b5543258e7a3b6a9d03486755aa49236ed21c088f57bffe4cd9634f8efb7533c`. Both derive and authenticate the original 198-source ancestry. The checker audit closes in session `10897`, chunk `b3e4ca`, after 38.248746 seconds over 213 captured files totaling 49,045,781 bytes; the preparation audit closes in session `38318`, chunk `ed6d24`, after 9.980649 seconds over 215 files totaling 49,144,230 bytes. These are metadata-only audits with zero numerical calls, not new root queries or refinements.

Plainly: the tools select the intended saved interval and preserve every history token. Agreement is checked against expectations written before either tool, not merely against each other.

The consumed readiness owner `c0c890e5016248aaf3bfe82b8867ec7f4d0dfd1648cd0af0aa2056c3de1ceaf5` is preserved byte-identically at `preparation-requested.FMruCE/readiness-owner.c0c890.md`; comparison passes before editing the live owner. Historical source paths are not relabeled. That supervised invocation, fresh one-parent run and independent output audit are now accepted below. The next artifact connects the checked parent to the existing calculation. Its original inclusive deadline, resource limits, source checks, complete candidate evidence and closed owned processes remain necessary. Wrong parent membership, changed tokens, missed source replacement, incomplete output or an independent enclosure miss falsifies the corresponding acceptance. No full-history accuracy, parallel capacity, H3, score, retention or physical claim follows.

## Independently accepted actual parent-one emission refinement

The [supervised launcher](../../../../scripts/eom/run-f6c-parent-emission-refinement-pilot.mjs), SHA-256 `398d604f9e5f8a5d85247df0d619c23726c727980881d185d3cc61545df563f6` (48,579 bytes), and [controls](../../../../tests/f6c-parent-emission-refinement-pilot.test.js), SHA-256 `231427f4a98561b8a4377a0a4894e7f7be31ffa8d5f77966d86f77daada4a3e0` (20,889 bytes), passed independent review: 22/22 supplied controls plus four separately injected failure cases and 64 assertions. Process, group and lock cleanup was checked, and all 28 reviewed source/dependency files remained unchanged. The [exact launch plan](2026-08-27-f6c-parent-emission-refinement-launch.v1.json), SHA-256 `2ef79411d22b646136352b83a92dfb18b42e2d01733aec6dff828ab258dc68d4` (51,509 bytes), passed 1,089 independent metadata checks in `31ec82`: 23 subjects, 159 runtime files, eight operational bindings, twelve original bindings and the versioned owner match. Its source union and independently derived historical ancestry comprise 230 distinct files totaling 49,557,826 bytes, verified unchanged twice.

Plainly: both the launcher and this exact saved-input invocation were checked before numerical work. The new run did not change the geometry, mathematical references or resource limits.

**Measured, conditional:** original parent one, frame zero, reception `[0.001,0.002]` and original emission `[-8,-0.049]`, completed in original caller session `9158`, final completion chunk `1eda87`, exit zero. The fresh completion reports `261.94229158400003` seconds for the whole supervised attempt. The producer closed before its independent checker began. The checker reconstructed all 3,584 queries and accepted 64 rows, 56 ordinary pairs, eight self exclusions and 112 closed-piece records. Complete history generation `2ebdc2c9c7ccdabaaf798eae16ee1b333f25707736602e7246c1b12fb6459cd1` and parent identity `b5543258e7a3b6a9d03486755aa49236ed21c088f57bffe4cd9634f8efb7533c` match the previously frozen expectations.

The following outputs are under `.local-data/braid-analysis/f6c-parent-emission-refinement-20260827/`:

| Relative output | SHA-256 | Bytes |
| --- | --- | ---: |
| `pilot-parent-1-v1/queries.ndjson` | `cf4a6c7464f773782c5450d562a999543c60c2d906bf2fa91dc5f6207564db77` | 2003114 |
| `pilot-parent-1-v1/rows.ndjson` | `ad9734afd944cf92994852f3cfbb3c3b64ebcecf090e243cbf7a37ab5c019624` | 161022 |
| `pilot-parent-1-v1/pieces.ndjson` | `928a7d56104e528170b50f971c3217f157f5ba462a815cd646afba73fd354e46` | 48578 |
| `pilot-parent-1-v1/cover-manifest.json` | `952aa6a5951407af57b68478a3f19381d81dd2a970a82d3f3475bdd075936df6` | 113112 |
| `pilot-parent-1-v1-outer/comparison.json` | `8015590d0c2a39557411e1f3e3e3e6892565afc2a0b7741a225952c2e79f13f7` | 127948 |
| `pilot-parent-1-v1-outer/operation.json` | `d912a2b1daeddf0f6bafe852a876fde517b9d1da49260a557144ecacfb925029` | 657555 |
| `pilot-parent-1-v1-outer/launcher-stderr.log` | `4e5f4ca8c305b409a27bab70339dc1aed2f0a1b6962843b0e340118538dd6a71` | 7488 |
| `pilot-parent-1-v1-outer/resource-observations.ndjson` | `ccc61d5b04f22baf85fe3981db293903c446b788e77ae852ca6b41311dc27dda` | 520798 |

Plainly: the accepted result is a complete, separately checked refinement for this one interval. These hashes identify its evidence, not a new trajectory or full-history result.

The independent post-run requirements `d36f7d65c1e7577d6cb0bdc41a82258183dc9de4e8041a2370ace0c58a720403` were frozen before output review. Audit session `7059` closes successfully in `76942e`, with result `34d41d`: 44,626 metadata, transcript and identity assertions pass in 0.938004958 seconds, without another root calculation. All 248 named source/evidence paths totaling 55,976,509 bytes are unchanged before and after checking, including private publication aliases and the archived owner. All 24 recorded PIDs, five process groups and the shared lock are absent. The 1,055 resource observations agree with the fresh maximum sampled aggregate RSS of 604,340,224 bytes and maximum gap of 531.4321250000066 milliseconds. Nineteen host observations retain at least 48% free memory and 964,183,330,816 free disk bytes. Candidate data totals 2,325,826 bytes and combined logs 534,863 bytes. Prepublication time `261.792440459`, the pre-return observation gap `35.350250000017695` milliseconds and fresh final completion time are distinct observations; the accepted launcher enforces the original deadline and observation-gap bound after final flushing as well.

All 56 emission intervals narrowed strictly on both sides. Exact endpoint subtraction gives widths from `0.00076809466327540576457977294921875` to `0.0016772609488107264041900634765625`, compared with original width `7.951`. This is conditional emission-enclosure improvement only: neither an acceleration calculation nor evidence of a small dynamical residual. Main's separate exact-rational metadata check `9acd98` agrees with these recorded endpoints and the prefrozen parent identity.

Plainly: the next interval now has much more precise emission bounds. It still has to be supplied to the existing acceleration and history calculations before their precision can be measured.

The consumed owner `7b4fb29001fac6cd21b91f8e3e0b6f38a5fc93a53a52c4f7939a75304e548d7c` (318,717 bytes) is preserved byte-identically as `readiness-owner.7b4fb2.md` in the same parent lane; comparison `35c95c` passes before editing the live owner. Historical paths and records remain unchanged. The next connection must explicitly distinguish historical source identity from a separately bound archive, preserve the existing default parent-zero route, and overlay only the independently admitted original parent. No private adapter mutation or alternative mathematical path is authorized.

All fifteen scientific-authority flags remain false. Acceleration evaluation, EOM execution and whole-history metrics also remain false. A changed original token, omitted piece or query, incorrect retained face, failed source/alias check, unmatched fresh completion or resource-limit failure falsifies the corresponding acceptance. Full-history precision, three-rung agreement, durable streamed publication and measured parallel capacity remain open.

## Independently checked adapter record lifetime

The [adapter](../../../../scripts/eom/f6c_variable_cell_adapter.py), SHA-256 `e2c20664e81cf03c9a6b6276ff19fc351e048522640bba528f557f4874482d47` (54,205 bytes), now uses weak exact-identity registrations for issued projections and evaluations. Separate cumulative successful-operation counters preserve the lifetime limit of 16,384 projections. An already-evaluated marker remains while its projection lives, even if the evaluation object is collected; collection cannot reopen work or reset a budget. Cleanup checks the exact registration before removing it, so an old callback cannot remove a later reused-identity registration. No public clearing API, numerical rule or weaker mode is added.

The [adapter controls](../../../../tests/test_f6c_variable_cell_adapter.py), SHA-256 `6e7cbd1ca23a884e6fe4f5d8a169e182929f314c4ec64459dd97cb642eb310ad` (60,652 bytes), pass 70/70, including all 57 unchanged original test bodies. The [driver controls](../../../../tests/test_f6c_single_leaf_diagnostic.py), SHA-256 `8861ebe7278ca7cfb534a9d72853fb269799d68a8071266b73a213828e3efd0a` (57,484 bytes), change only their two dependency hashes and pass 48/48. The driver itself remains `5b890b4dbafa676bd1e7e67734701467dca6615ad49f46adecd4ba65ef40092f`. Main watched session `57654` closes successfully in chunk `8e443c`: 3.131 seconds for the adapter suite, 14.677 seconds for the driver suite and 18.03754925 seconds for the combined wrapper, with all three edited pins unchanged.

Plainly: the adapter no longer keeps every completed large calculation alive solely to remember that it happened. It still remembers the work count and rejects repeated use.

Main independent conditions `8e893f19e22aa8c36fd96d113e6063eec2c43c445395386194b5aa92708ef8bf` were frozen before editing. Audit `a58b9b` passes 95 additional checks, including inherited/restricted old-generation numerical parity, unchanged mathematical calls, all 57 original test bodies, exact driver-pin-only edits and collection without lost evidence. Sixteen small synthetic range evaluations are exercised in that audit; twelve retained evidence packets leave zero live projection/evaluation registrations while cumulative counts remain `12/12/96` for projections/evaluations/residuals. The separate 32-request lifecycle control uses substituted kernels to isolate ownership, reaching cumulative `128/128/256` with peaks of four live projections and four evaluations and zero between requests. Neither object-count result is a measurement of full-run RSS or wall time. The frozen quadrature state and its compact historical evaluations are unchanged.

Old adapter `b2bd53ed4614441a42220a6190696c9ca550caad618de21e5fc405c0c6182bde`, adapter controls `a8c4173f616decef982d5ce59819de5cc0dfd003837c8cb23cf6bba04fda2b07` and driver controls `6c925c3bc8b6764d007f92f3055f2a02c688182cf5b052123e826c346275d3c4` are preserved byte-identically as `preparation-requested.FMruCE/adapter.b2bd53.py`, `adapter-controls.a8c417.py` and `driver-controls.6c925c.py`. Historical receipts retain their original source paths and hashes. The next evidence-handling artifact is exact streamed publication with shared metadata and reconstructible complete request records, without accumulating every provision. File, aggregate, time and memory caps remain unchanged; sharding cannot silently enlarge them. Actual runtime admission and measured full-history cost remain open.

Plainly: this removes one cause of growing memory use. It does not yet show that the complete search fits its limits. Lost evidence, repeatable completed work, reset counters, altered numerical results or a source mismatch falsifies this repair's acceptance; no metric, score, retained braid or physical claim is promoted.

## Independently accepted lossless evidence codec

The [pure evidence codec](../../../../scripts/eom/f6c_leaf_evidence_codec.py), SHA-256 `371f6eff5a7a50514816b9af04c98fdae18084cc364b35b565fc53acae76a79f` (22,926 bytes), and its [controls](../../../../tests/test_f6c_leaf_evidence_codec.py), SHA-256 `af39c302680ce1a9d55c5539dfa3084f20132a12113d9b6285d6abbec074c648` (22,489 bytes), are independently accepted. The twenty-group expectation payload `0b9ba6cdadac26c6ffc1725205762c3d51304e547e30f5c9bc98da213ee00c85` (5,546 bytes) preceded implementation. Independent review passes all 28 supplied tests, 853 separately authored assertions and seventeen saved-output transport checks; watched session `16078` closes successfully in chunk `ab677b` after 1.170572125 seconds. Eight frozen source, control and saved-output files remain unchanged. Two review findings were repaired before acceptance: a normal sink return must be counted before a later guard fails, and shared empty dictionaries must obey the same expansion-depth rule as their literal representation.

Plainly: the new format can retain complete calculation evidence without repeating every shared structure. The independent checks test exact reconstruction and rejection of damaged records, not the truth of a new calculation.

Each provision and transition uses a fresh ordered value table; only the declared context and source provenance are shared across records. Exact integer values, decimal and rational strings, member order, original rows and complete piece records are preserved. Floating-point inputs are rejected. The entire encoded stream, including header, tables, indices, line endings and footer, remains bounded by 64 MiB; expanded records and individual values have separate finite limits. A synchronous sink acknowledges one immutable line by returning `None`; this acknowledgement is not a durability guarantee. Failure permanently poisons that stream. The decoder confirms completion only after a valid footer and explicit end-of-file, rejecting missing, extra or altered records. It does not reconstruct an executable quadrature state, retry a request or confer scientific authority.

The saved first-four-request output `1d0b865b63a1cbddae80357c7a0fb89966cd4755cd7ab0aff95e9ba804c0cdc2` was transported in memory as 3,118,690 encoded bytes and reconstructed exactly by a separately authored literal decoder for all four complete provisions and evaluations. The old output does not contain intermediate public state summaries; the transport check used explicit null markers, not invented states. This is exact value reconstruction of the selected evidence, not byte equality of the old outer envelope, a new numerical run, a durable publication or a full-history capacity measurement. No new evidence file was written by that transport check.

Plainly: the saved evidence survives the new encoding exactly, but the format still needs its source-bound caller and durable publication connection. Full-history accuracy, memory cost and parallel throughput remain unmeasured. Lost or changed tokens, accepted truncation, false acknowledgement counts or an aggregate size escape falsifies codec acceptance; no metric, score, retention or physical claim follows.

## Independently accepted streamed leaf composition

The [explicit streamed caller](../../../../scripts/eom/f6c_streamed_leaf_session.py), SHA-256 `4127eed0de63d39e96de41cc17615cf35130aa8b33e347a4268c6feb0a862d5c` (15,173 bytes), and its [controls](../../../../tests/test_f6c_streamed_leaf_session.py), SHA-256 `f8935b0ac78236ebac1628c2d66a251022cd44cbd5475b85bc15ce5a4dcff187` (22,827 bytes), are independently accepted against the preimplementation 24-group contract `b141453e1199460d3f848c4a2f869b92e42ea526257d63d6e34961e7ea065ad5` (9,539 bytes). Main's 25/25 tests close in session `2705`, chunk `ac0f2e`, after 8.023 seconds. Independent review closes in session `24605`, chunk `bc5f31`, with 25/25 passing in 8.000 seconds; 798 additional assertions and 169 rejection checks pass in `bdae17` after 0.089750875 seconds. All 39 reviewed files totaling 1,027,414 bytes remain unchanged in final scan `fb1ca6`.

Plainly: one explicit request now records its complete inputs before consumption and its actual resulting evaluation afterward, without keeping a growing list of large evidence packets.

The caller owns a fresh unchanged leaf-response session and unchanged evidence encoder. It adds neither a numerical method nor an automatic execution loop. Exact integer, rational and decimal tokens and declared field order survive bounded conversion. Every key counts toward node and depth limits. A sink's normal `None` return remains an observed acknowledgement even when a later state check rejects the attempt; a failed constructor preserves actual header attempts and acknowledgements. Callback mutation, swallowed reentry, partial writes and exceptions permanently stop the caller without retry, invented success or erased evidence. These two failure-accounting and conversion-limit corrections were made only in the new caller and its controls; the accepted driver, encoder and mathematical references were unchanged.

The genuine synthetic bridge exercises four requests with 16 projections, 16 acceleration evaluations and 32 residual evaluations. Its completed stream still records 156 initial requests pending and no full-history aggregate. Separate handwritten event and literal-value expectations check ordering and complete reconstruction; round-trip agreement is not a new mathematical oracle. Existing driver and codec regressions also pass 48/48 and 28/28.

Plainly: this closes the explicit in-memory evidence-connection blocker, not the durable file publisher or the full-history measurement. Source-bound invocation, durable acknowledgement, original resource limits and independent actual-output admission still belong to the next executor. Lost accepted-prefix accounting, uncounted object keys, changed complete evidence or unexpected extra work falsifies this acceptance; no metric, score, retention or physical claim follows.

## Independently checked adaptive checkpoint growth memory

The separate [EOM-003 repair receipt](../../app-solver/work-log.md#2026-08-27--adaptive-growth-memory-checkpoint-repair) closes the concrete omitted-controller-memory defect identified by the CT-004 audit. The six-file repair preserves the exact growth-headroom count in the request, certificate, checkpoint and resume path without changing the growth rule or numerical tolerances. Checkpoint `v7` / `EOMCPV4` explicitly rejects older incomplete formats; no legacy artifact is overwritten or silently given a zero count. The edited EOM files do not overlap the asymmetric counter-breathing representative adapter's frozen 263-source generation.

Measured validation includes a fresh scoped build, 36/36 passing existing and new tests, and 366 additional main independent checks against a finite-state recurrence frozen before implementation and the constant-history closed form. Diagnostic cuts after one, two and four accepted steps preserve the original `2→2.08` horizon, subsequent numerical decisions and all complete retained-history tokens. Restart's first-step snapshot-reuse counter differs, two versus three, because the preceding invocation-local snapshot is recomputed; that performance observation is explicitly checked and is not claimed identical. This accepts the named restart repair, not all EOM-003 modes, campaign orchestration, long-horizon cost or any candidate's dynamics.

Plainly: a long search can no longer lose this particular piece of step-controller memory at a checkpoint. General restart sufficiency, the complete-history asymmetric counter-breathing representative measurement and measured parallel capacity remain separate work. A changed numerical decision or retained-history token in the named control, or silent admission of a missing legacy count, falsifies this acceptance. No metric, score, retained braid or physical claim follows.

## Independently accepted parent-one adapter integration

The [adapter](../../../../scripts/eom/f6c_variable_cell_adapter.py), SHA-256 `e8d8adfccd13360d902f05c85ee4824defd449c944532f91a000d48b16641554` (76,057 bytes), now accepts an explicit immutable parent-refinement declaration. The empty declaration preserves the existing parent-zero route. The independently accepted original-parent-one invocation is the only additional admitted generation; its local row and piece indices remain distinct from its original parent index. Historical acceptance-owner bytes are resolved only through an explicit exact archive relation, while physical provenance retains the current owner and archive separately. All mathematical reference bytes remain unchanged.

**Measured, implementation conformance:** [controls](../../../../tests/test_f6c_variable_cell_adapter.py), SHA-256 `7f0bc610e83feabc20ce897a7269bb017952cb53e42cae747967191813c60441` (72,385 bytes), pass 85 tests, retaining all 70 prior test bodies. Independent review `07539c` passes 733 assertions and 175 rejection checks, including all 112 individually corrupted piece records. Four old-generation and four new-generation synthetic evaluations agree with independently derived stationary signed acceleration components and squared norms. Four draft integration findings were repaired before acceptance: relative/absolute source views, a mistyped comparison hash, four omitted historical stage-log bindings and exact primitive-type checks. Main's downstream suites pass 48 driver tests (`0488c2`) and 25 streamed-session tests (`c5f78b`); their changes are dependency hashes only.

Plainly: the new connection supplies the checked interval without changing the existing mathematical calculation or allowing an unapproved replacement.

**Measured, actual metadata conformance:** requirements `a190584e0f8576050c0f84879545578354fa782c047d272b8c04e0cb291041d9` were fixed before implementation. Session `3694`, completion `008013`, exits zero in 24.091852791 seconds under the unchanged 60-second inspection limit. All 288 physical source files containing 57,705,795 bytes are unchanged, all handles close, and numerical counters and function tripwires remain zero. The complete selected-parent representation has SHA-256 `d7d1cf745d8485f653289f8836dbbf325089fec5bfd50ac072d807bd322140b1`; parent-one rows have SHA-256 `b6c125a6bb01ab076800910f6a46b3a16f1734b86452a61773a56658e9c6506a`. Both match the independent preimplementation expectations; parent zero and parents 2–159 are unchanged. An earlier global-profile inspection exceeded 60 seconds and is retained as a harness-overhead failure, not mapping evidence; the accepted inspection used a smoke-tested lower-overhead tripwire without increasing the limit.

The consumed owner `af7db5d77b285a74d51d22fbdc0817bd89230746014b689a0de77b6f32fc5192` (329,483 bytes), adapter `e2c20664e81cf03c9a6b6276ff19fc351e048522640bba528f557f4874482d47`, adapter controls `6e7cbd1ca23a884e6fe4f5d8a169e182929f314c4ec64459dd97cb642eb310ad`, driver controls `8861ebe7278ca7cfb534a9d72853fb269799d68a8071266b73a213828e3efd0a` and streamed controls `f8935b0ac78236ebac1628c2d66a251022cd44cbd5475b85bc15ce5a4dcff187` are preserved byte-identically under `.local-data/braid-analysis/f6c-parent-adapter-20260827/`. Historical records remain unchanged.

Plainly: actual saved data now reaches the correct adapter interval. A changed unaffected parent, mismatched historical identity, hidden numerical call or failed final source check falsifies this acceptance. At that metadata-only stage, the new parent had not yet been evaluated; the later two-refined-parent calculation below closes local evaluation, not whole-history precision.

## Independently accepted durable leaf storage

The [durable publication helper](../../../../scripts/eom/f6c_leaf_stream_publication.py), SHA-256 `5a638da1aa91ce80b9dcdfe503f4e2193a981c43e903719e4e18b5bb17c9658e` (15,594 bytes), preserves the frozen codec's exact bytes and acknowledges a record only after its complete write and synchronization to storage. It retains incomplete private evidence on failure, validates complete framing through explicit end-of-file, and publishes only an owned matching file link. The caller remains responsible for closing the adapter, authenticating unchanged sources and runtime, and enforcing the original process/resource envelope before publication.

**Measured, storage conformance:** [controls](../../../../tests/test_f6c_leaf_stream_publication.py), SHA-256 `b1c8e0a789ec2215d12a20e601dfc5175df3516c450373a624c8049b060d49ae` (24,104 bytes), pass 33 tests; main's independent `6b220d` check adds 31 assertions over real synchronization-before-acknowledgement, all four record types losing acknowledgement after durable storage when the deadline expires, fresh-instance invalid inputs, independent byte hashes, matching file identity and preservation of foreign replacement files. The helper, controls, codec and streamed-session bytes remain unchanged. This acceptance is storage-only; no numerical run or caller source/runtime acceptance is implied.

Plainly: evidence can now be saved incrementally and safely rejected after an interruption. A premature acknowledgement, lost private prefix, accepted incomplete stream, deleted foreign file or size-limit escape falsifies this acceptance. The later two-refined-parent calculation below closes the supervised connection and measures its local precision and resource use; full-history capacity remains open.

## Independently accepted two-refined-parent streamed calculation

The supervised connection is now independently accepted at its explicit two-request diagnostic scope. The [caller](../../../../scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs), SHA-256 `2e2fea0bb9e10012a9433d8cfb2cd4f5296814f84ee2639e2a559e1353ecebac` (51,531 bytes), and its [controls](../../../../tests/f6c-streamed-leaf-diagnostic.test.js), SHA-256 `71c6c7270daa784d73e3a3fcd74e9eb740c0ef0ca3a573c64ae9631bb3366d4a` (29,208 bytes), pass 36 controls and a separate eight-case, 82-assertion operational review. Runtime discovery retains imported runtime files even when historical provenance also lists them. Cancellation retains its first cause, and final launcher/resource-log identity and hash checks precede success. The mathematical references, evidence codec, streamed session and durable publication helper remain unchanged.

Plainly: the caller now connects the checked calculation to durable evidence under the original limits. Its successful completion requires the real work, source checks, storage and process cleanup to finish.

The genuine [single-leaf driver](../../../../scripts/eom/f6c_single_leaf_diagnostic.py), SHA-256 `087443710e476cab134986a83e45b5d04d470762ea413e899721eac588097bd4` (31,357 bytes), preserves each authenticated parent's refinement flag instead of requiring that only parent zero be refined. Its [controls](../../../../tests/test_f6c_single_leaf_diagnostic.py) at `50c6c6c151330181310f210342f11ccc7a310700334651e62db9bdab9c0daf1f` pass 51 tests; the [streamed controls](../../../../tests/test_f6c_streamed_leaf_session.py) at `c618af01aa455fa4525d139950e845244dbf8f7221e04a0cd8f5a817e7935b0e` pass 25 tests with a dependency-pin-only change. A separately frozen stationary closed-form reference passes 13,932 additional checks (`9000db`), and the actual adapter/provider construction check (`ffb064`) admits all 160 initial leaves with zero numerical evaluations. This repairs a genuine integration incompatibility discovered before launching the actual run; it does not change the numerical method.

Plainly: the previously checked second interval reaches the real calculation without losing its identity or being mislabeled as unrefined.

**Measured execution:** the exact 43,992-byte invocation `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/invocation-two-refined-parents-v2.json`, SHA-256 `eb441785b3172850c29353571324f7cece990a1d37377e9a168236bcd56fb917`, completes in session `47483`, chunk `2a8350`, exit zero. Fresh whole-attempt time is `57.891806040999995` seconds; the earlier prepublication time is not substituted for it. The 237 samples show maximum aggregate resident memory of `541278208` bytes and maximum observation gap of `291.4772499999999` milliseconds. Complete scientific evidence is `1764652` bytes and operational logs total `238972` bytes. The original 1,800-second, 2-GiB, 64-MiB scientific-evidence and 16-MiB log limits are unchanged. Independent operational audit `bf1967` passes 8,536 assertions and confirms all 305 original source bindings unchanged, both final log hashes, six durable records, all seven recorded processes and three process groups absent, and the shared lock absent. Separate frozen-codec audit `46b2bb` checks complete end-of-file framing and all claim flags false.

Plainly: the run finished, saved complete evidence and released its resources. These measured costs describe two requests, not a projection for a complete or parallel campaign.

The six-record `pilot-two-refined-parents-v2/leaf-evidence.ndjson` has SHA-256 `69f862f0cb6aa68f239072b77c361197cfc3530ea68d7a36ee2f31d86363ea3d`. Its sibling `pilot-two-refined-parents-v2-outer/operation.json` has SHA-256 `17819838b6cdfe4c54cc2a4c39f851d869a12bba0938c17e77140107bc3d482a`. The independent arithmetic instrument `two-request-independent-audit.py`, SHA-256 `5b760e9e4b808219d7c0d5c561cc535852b49d961f059b3000c6d1143ae1a280`, retains the prior independently authored numerical checking block unchanged and uses the frozen independent geometry and acceleration-mapping references. It passes 251,283 checks in session `3334`, chunk `8e5228`, exit zero, with zero subject numerical calls. Its durable `pilot-two-refined-parents-v2-independent-math.json` receipt has SHA-256 `1fa12c9b6626831486fd53ff027041719c243e2833d314896539675e7c1313e7`. All named files in this paragraph are under `.local-data/braid-analysis/f6c-streamed-leaf-diagnostic-20260827/`.

Plainly: separate mathematical reasoning checked the saved result; replay agreement alone is not being used as proof.

**Measured conditional bounds:** the integral uncertainty widths for original parents zero and one are respectively `9.6275037509612e-7` and `9.635543276242955e-7`. Their partial total is `1.9263047027204155e-6`, with partial peak upper bound `0.012363567788016288`. Comparing the same parent-one domain with the independently checked earlier broad result gives a width ratio of about `3.710884308e9`. All evaluated residual components still include zero, and all integral lower bounds are zero. This fresh two-request run has 158 initial requests pending and no full-history aggregate; it neither replaces nor adds to the old four-request run's 156-pending census.

Plainly: the second interval's uncertainty is vastly narrower, not evidence of a vastly smaller physical mismatch. Balance, imbalance, retention, stability, complete-history metrics and stronger braid status remain unproved.

Before editing, main preserved nine byte-identical files (`767567`, 737,230 bytes) under `.local-data/braid-analysis/f6c-parent-adapter-20260827/`, named `<original-basename>.<first-eight-SHA-256-characters>.archive`: the six preparation/checker/coordinator and control files at `492882b6`, `06cd99bc`, `0bb16c23`, `92da2b09`, `398d604f`, `231427f4`; adapter and controls at `e8d8adfc`, `7f0bc610`; and consumed readiness owner at `5990ee33`. The later consumed owner `26beeee22167417a34ef12a9a12af0d2f9ae2fc843f2498c833f2697e737dd7a` (342,401 bytes) is preserved byte-identically as `readiness-owner.26beeee2.md` in the same directory (`523029`). Old logical source bindings resolve only through explicit exact archive relations, with physical archives and current sources recorded separately. Full-history evidence capacity, three-rung accuracy and measured parallel capacity remain open; the 64-MiB aggregate limit must not be reset per worker or file.

Plainly: preserved evidence stays attributable to the version that produced it. A wrong interval identity, changed bound source, failed independent enclosure, incomplete stream, violated resource limit or unclosed process falsifies acceptance. No geometry, interaction strength, score, retention status or physical claim changes.

## Independently accepted generic original-parent selection

The preparation, independent-comparison wrappers and operational coordinator now accept one explicitly selected original parent from zero through 159. The mathematical producer and independent numerical reference remain frozen; this extension changes selection, source attribution and operational checks, not their mathematics. The adapter admits explicitly sorted unique refinements from one through 159 while retaining accepted parent zero implicitly. Each historical owner/source generation has its own explicit archive mapping; admitting an index does not assert that every possible combined source inventory or full-history output fits the unchanged capacity limits.

Plainly: the same checked method can now address the next original interval without confusing it with an earlier interval or silently substituting newer source files.

| Frozen source or control | SHA-256 | Bytes |
| --- | --- | ---: |
| [prepare-f6c-parent-emission-refinement.py](../../../../scripts/eom/prepare-f6c-parent-emission-refinement.py) | `ff488499f2737860034602ce9559c3ebc817aa8413b827007fb31027815679d2` | 58397 |
| [test_f6c_parent_emission_refinement_preparation.py](../../../../tests/test_f6c_parent_emission_refinement_preparation.py) | `517cc307251611177ec19cc5d71938a4086806f48583bcf8e3f2d04e9afb8d9f` | 43836 |
| [verify-f6c-parent-emission-refinement.py](../../../../scripts/eom/verify-f6c-parent-emission-refinement.py) | `53595cc12589ab56c73a1613922bba2739704cbc78465e3d646d5ae6a43813db` | 46615 |
| [test_f6c_parent_emission_refinement_verification.py](../../../../tests/test_f6c_parent_emission_refinement_verification.py) | `889d8721d2b51520c0fef78f6a954f9b510cbb46fdf9019205199dfa3658b5a9` | 42419 |
| [run-f6c-parent-emission-refinement-pilot.mjs](../../../../scripts/eom/run-f6c-parent-emission-refinement-pilot.mjs) | `462247cf723339dbdc9ce9b4b897720cd4edcedc9b85c22b70694c41663f5c1b` | 56022 |
| [f6c-parent-emission-refinement-pilot.test.js](../../../../tests/f6c-parent-emission-refinement-pilot.test.js) | `dd88eae5729d8ecc5947a27966edb215074d12687f3b5cd0bfc3be69d0400bc1` | 33303 |
| [f6c_variable_cell_adapter.py](../../../../scripts/eom/f6c_variable_cell_adapter.py) | `42f2ff25ec3cedb0e04254cfe1d604946d6593aa360a254e7abe6955783367aa` | 88210 |
| [test_f6c_variable_cell_adapter.py](../../../../tests/test_f6c_variable_cell_adapter.py) | `e4a8c694d978354793b8c99c1c0eeaa16ef5bb6d8250fa265f0b046d53434383` | 94665 |

Plainly: these exact file versions, rather than whichever files happen to be current later, are the reviewed implementation.

**Measured selection and attribution checks:** the preparation and verification controls pass 39 and 30 tests independently. Review `886345` closes in session `92773`, exit zero, with 506 subject assertions, 106,788 independently derived original-source checks and 52 rejection cases over original parents zero, one, two and 159; all 28 captured files remain unchanged and no scientific calculation is invoked. The adapter passes 92 distinct tests; separate review includes 224 rejections, and the simultaneous historical-parent-one/generic-parent-two reader check `182653` passes 28 assertions and 23 rejection cases. Main's genuine retained-source adapter/provider construction check `a17925` closes in session `29542`, exit zero, in 27.422852999996394 seconds: all 160 initial parent identities agree with separately captured original tokens, only zero and one are marked refined, and all numerical and geometry counters remain zero.

Plainly: these checks establish that the correct saved interval and source generation reach the real calculation interface. They are not new numerical results.

**Measured operational repair:** the first generalized runner was returned after an independent late-directory-rename reproduction showed that unchanged public bytes could conceal a broken private-evidence layout. Its exact runner/control generations `6119480a` and `738951ae` remain archived (`feca4a`, 82,607 bytes). The repaired runner at `462247cf` preserves original private-directory and file identities and checks the complete candidate/comparison layout before and after final output. Independent rerun `32054/0152c5` passes all 52 controls in 45.192497708 seconds, exit zero. The original reproduction now rejects, retracts only owned public outputs, preserves private evidence and closes all owned processes and the lock (`1630/a4b7ce`, 21 assertions). All 28 bound files remain unchanged (`77523d`). The runner's reported elapsed field precedes its final source/layout checks; actual caller-exit duration must therefore be separately observed and must not be replaced by that field.

Plainly: a damaged evidence layout now fails even when the visible file contents still match. The next step is the separately reviewed original-parent-two launch plan and its actual bounded run; neither has been accepted as a numerical result here. The streamed-caller extension is still under independent review. Full-history coverage, three-rung measurement and measured parallel capacity remain outstanding.

## Rejected original-parent-two attempt after concurrent owner relocation

**Measured rejection, not numerical acceptance:** the exact [first parent-two plan](2026-08-27-f6c-parent-2-emission-refinement-launch.v1.json), SHA-256 `0ebf65d591a55e3eca280e5768232099adfec9badade649c5752830430b339ec` (51,509 bytes), passed independent admission `a671a8` with 6,933 checks and the fresh 159-file runtime comparison `487a51`. Its actual caller session `43437` closed with chunk `b6c9a3`, exit one. The separately observed caller envelope was 276.96 seconds, measured from before the launch-tool request through tool-confirmed process exit and including approval/tool latency; it is not the runner's pre-final-check elapsed field. The producer completed 3,584 queries, 64 rows and 112 piece records. The independent checker reported 3,584 queries and 64 rows processed, then rejected during `source-rechecks` with `truncated input`; it published no comparison.

Plainly: the calculation reached its final source checks, but a completed numerical stage is not a completed, accepted attempt.

The captured readiness owner `caf11b8d8a8ac696bd39e7da107f70ffb1ba4b49321d7d58207cf64145f4d17e` (347,707 bytes) became `90413be0e7471215be1a6b9e6399a3710b4babf45192be7d9c8f51cd1f483a12` (347,696 bytes) on the same file identity while the run was active. Independent source checks `ca7dd3`, `1d4a16` and `b272d1` isolate this as the only changed source. The exact diff `a89e48` moves one write-set reference from `mapping-electromagnetism/braid-candidate-requirement-adjudication.md` to `braid-program/braid-candidate-requirement-adjudication.md`. The frozen reader seeks to the start and requires the originally captured byte count; the shorter file explains early end-of-file. The checker itself did not print the failing path, so this attribution comes from the independent source/identity comparison. The relocation is preserved, and no numerical or verifier repair is indicated.

Plainly: this is a correct source-integrity rejection caused by a concurrent document edit, not evidence against the geometry or a reason to weaken the source guard.

Independent closure check `5026ba` confirms all 26 recorded process/group identifiers and the shared lock absent. Check `3df7d3` confirms four exact private evidence files totaling 2,273,841 bytes retained, all public candidate links removed, no comparison or operation publication, and all 15 claim flags false. The rejection record has SHA-256 `e879451ceaad3c9de21239df49a944955c84deddf5c37db5bb99c2f8f0ea6380`. The failed attempt and caller observation remain under `.local-data/braid-analysis/f6c-parent-emission-refinement-20260827/` as `pilot-parent-2-v1`, its `-outer` sibling and `pilot-parent-2-v1-caller-observation.json`. Both readiness generations are preserved byte-identically under `.local-data/braid-analysis/f6c-parent-adapter-20260827/` as `readiness-owner.caf11b8d.md` and `readiness-owner.90413be0.md`.

Plainly: the failed evidence remains inspectable and grants no accepted refinement. After a stable relocation handoff, a fresh separately reviewed plan must bind the updated owner and use a new output directory; the numerical sources and original limits remain unchanged.

## Current integration validation

The paused handoff passes 353 relative-link checks and confirms 38 distinct existing integration paths (`6f9a28`); scoped whitespace checks pass (`367537`). The frozen adapter accepts the exact parent-two owner section and all nine evidence-role bindings (`18ca3f`), with zero scientific calls. No new full-content job was started for the pause; the preceding strict-content result below remains separately attributed.

The generic-parent integration link check `e2f987` passes 332 relative targets, and `e814e5` confirms 37 distinct existing paths before adding the separately reviewed retry plan. Scoped whitespace check `1a7bce` passes. The strict content check closes in session `23181`, chunk `58405f`, exit one, with zero errors and two index-drift warnings: `content/scenes/philosophy_history/information_and_the_wake.json` and `content/markdown/aaa/philosophy-history/one-nature-many-theories.md` are missing from generated indexes. These independent index changes are outside this Braid edit batch. The exact regeneration command is `node scripts/validate-content.mjs --write`; no regeneration was run.

Plainly: scoped checks pass, while the repository-wide strict check is not green because two generated index entries are missing. That does not invalidate this accepted interval calculation or authorize a broader braid claim.

## Current integration write set

This integration batch covers the following thirty-eight source, control, plan and owner paths, including the separately reviewed next-parent launch plan. It does not edit the frozen mathematical references, configuration JSON, Borg records, brainstorming files, README files or prior evidence outputs. The ignored source/control and readiness-owner archives described above preserve consumed generations and are additional archival copies, not replacement evidence. No commit, push or generated-index refresh is performed.

```text
reference/priorities/braid-program/configurations/candidate-registry.md
reference/priorities/braid-program/evidence/2026-08-27-braid-search-launch-readiness.md
reference/priorities/braid-program/priorities.md
reference/priorities/braid-program/work-queue.md
reference/priorities/braid-program/analysis/braid-candidate-requirement-adjudication.md
reference/priorities/app-solver/work-queue.md
reference/priorities/app-solver/work-log.md
scripts/eom/oracle/f6c_parent_emission_refinement_conformance.py
tests/test_f6c_parent_emission_refinement_conformance.py
scripts/eom/f6c_parent_emission_refinement.py
tests/test_f6c_parent_emission_refinement.py
scripts/eom/prepare-f6c-parent-emission-refinement.py
tests/test_f6c_parent_emission_refinement_preparation.py
scripts/eom/verify-f6c-parent-emission-refinement.py
tests/test_f6c_parent_emission_refinement_verification.py
scripts/eom/run-f6c-parent-emission-refinement-pilot.mjs
tests/f6c-parent-emission-refinement-pilot.test.js
reference/priorities/braid-program/evidence/2026-08-27-f6c-parent-emission-refinement-launch.v1.json
reference/priorities/braid-program/evidence/2026-08-27-f6c-parent-2-emission-refinement-launch.v1.json
reference/priorities/braid-program/evidence/2026-08-27-f6c-parent-2-emission-refinement-launch.v2.json
scripts/eom/f6c_leaf_evidence_codec.py
tests/test_f6c_leaf_evidence_codec.py
scripts/eom/f6c_streamed_leaf_session.py
tests/test_f6c_streamed_leaf_session.py
scripts/eom/f6c_leaf_stream_publication.py
tests/test_f6c_leaf_stream_publication.py
scripts/eom/f6c_variable_cell_adapter.py
tests/test_f6c_variable_cell_adapter.py
scripts/eom/f6c_single_leaf_diagnostic.py
tests/test_f6c_single_leaf_diagnostic.py
scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs
tests/f6c-streamed-leaf-diagnostic.test.js
src/eom/include/architrino/eom/CoupledEvolution.hpp
src/eom/include/architrino/eom/Checkpoint.hpp
src/eom/src/CoupledEvolution.cpp
src/eom/src/Checkpoint.cpp
src/eom/native/eom_native_evolution_fixture_cli.cpp
tests/test_eom_native_coupled_evolution.py
```

Plainly: the parent-one refinement, actual adapter selection, streamed leaf composition and durable storage helper are independently accepted at their stated scopes, alongside the checkpoint and record-lifetime repairs. Their supervised source-bound connection and actual two-refined-parent calculation are independently accepted at the recorded scope. Generic later-parent admission, full-history precision and evidence capacity, three-rung agreement and measured parallel capacity remain open. No new physical terminology or stronger braid status is introduced.

## Axial-Transverse Three-Binary Interior Local-Landscape Boundary

The dated 377-row score-free axial-transverse local-landscape packet measured only agreement between its frozen inputs and the code then present. It evaluated zero causal roots and computed zero scores. The packet, its verifier, and its two receipts are withdrawn. Any new use requires a fresh exact-identity freeze bound to the present source configuration before root evaluation or scoring.

Plainly: the old check showed that old bytes could still be read. It did not reconstruct the current exact configuration or produce a scientific result, so it is not carried forward as current evidence.

The [facts-first local-landscape design](../campaigns/axial-transverse-three-binary-interior-score-landscape-predeclaration.md) remains a non-executable predeclaration. Any center-only capability pilot must first freeze a new manifest from current `assemblyId + modelRevisionSha256` source pairs, a current protocol, a separately reviewed analytical runner, and an independent dense-root audit. No remaining 376-row landscape, score, or EOM campaign is authorized.

## Verification and separate-change boundary

The following checks were run on this prerequisite batch:

```bash
node --test tests/f5-enclosed-root-ledger-reducer.test.js tests/f5-independent-interpolation-enclosure.test.js tests/parallel-braid-agent-search-manifest.test.js
node --test tests/abc-subfield-root-reference.test.js tests/circular-history-conformance.test.js
"${AAA_VENV:-../.venv}/bin/python" -m unittest discover -s tests -p "test_f5*conformance.py"
"${AAA_VENV:-../.venv}/bin/python" -m unittest discover -s tests -p "test_eom_decimal_interval.py"
node scripts/validate-content.mjs --check --strict
git diff --check
```

Results: the five Node suites pass `45/45` together (`29` existing controls and `16` circular/root-reference controls); the Python suites pass `22/22` and `5/5`; whitespace validation passes. Strict content validation reports zero errors and two warnings because the scene and Markdown indexes omit `one_nature_many_theories.json` and `one-nature-many-theories.md`. Those index files were not changed. The exact regeneration command is `node scripts/validate-content.mjs --write`; it was not run because unrelated regeneration is outside this edit batch. No unobserved test or campaign remains running.

Plainly: all 72 focused controls pass. The repository-wide check is not reported as clean: two unrelated generated-index warnings remain, and neither is evidence against these mathematical checks.

The independently reviewed reference batch was completed before its adapter subject was implemented. Its recorded instrument hashes and preserved reports define the frozen boundary; subsequent subject batches may not edit those references. A separate read-only policy review confirmed that `AGENTS.md` and the phase-varying display representative restart require separate implementation batches, not a Git commit. The earlier request for commit permission was an optional checkpoint choice, not an execution prerequisite. Commit and push remain unauthorized. The completed prerequisite batch comprises these paths, excluding the separate runner-hardening and status-owner edits elsewhere in the working tree:

```text
reference/priorities/braid-program/evidence/2026-08-26-f5-independent-interpolation-enclosure.md
reference/priorities/braid-program/evidence/2026-08-26-factual-configuration-h1-h2-census.md
reference/priorities/braid-program/evidence/2026-08-27-subfield-circular-h3-pilot-predeclaration.md
reference/priorities/braid-program/evidence/2026-08-27-subfield-circular-root-reference.md
reference/priorities/braid-program/evidence/2026-08-27-braid-search-launch-readiness.md
scripts/eom/derive-abc-subfield-root-reference.mjs
scripts/eom/derive-abc-circular-history-budget.mjs
scripts/eom/derive-f5-independent-interpolation-enclosure.mjs
scripts/eom/oracle/decimal_interval.py
scripts/eom/oracle/f5_actual_cubic_conformance.py
scripts/eom/oracle/f5_history_manifest_conformance.py
scripts/eom/reduce-f5-enclosed-root-ledger.mjs
src/prescribed-path-analysis/F5EnclosedRootLedgerReducer.mjs
src/prescribed-path-analysis/CircularHistoryConformance.mjs
tests/abc-subfield-root-reference.test.js
tests/circular-history-conformance.test.js
tests/f5-enclosed-root-ledger-reducer.test.js
tests/f5-independent-interpolation-enclosure.test.js
tests/test_eom_decimal_interval.py
tests/test_f5_actual_cubic_conformance.py
tests/test_f5_history_manifest_conformance.py
```

Plainly: the independently reviewed measuring instruments are fixed before the code they will check is written. Continuing implementation does not require a commit, and does not grant permission to modify the references or publish anything. The closed reference batch includes no adapter, generated Borg record, frozen pilot fixture change, or geometry change.

Closure goal: preserve phase-varying display representative's prescribed-root closure and the accepted sixteen-row pilot, preserve the independently accepted sixteen complete parallel ladders, and close the remaining named measurement obligations without changing their claim boundaries.
