# phase-varying display representative Enclosed Prescribed-Root Closure

Status: independently reviewed `H3 P[M]`, restricted to the approved prescribed history and declared `8/32/128` reception ladder, 2026-08-27. This is not an ordinary EOM evolution, retained braid, stability result, score change, or physical-realization claim. The finite IEEE nearest-rounding and gradual-underflow premises remain explicit.

## Subject and independent references

The [restart predeclaration](2026-08-26-f5-enclosed-root-restart-predeclaration.md) owns the source, complete pair census, error allowances, solver controls, and stopping rules. The approved configuration remains SHA-256 `e92e450c8ea83086b60184d31ff5b07fe8a470b1e20088ea312592f2b38800fb`; the preserved nominal-pilot fixture remains `bda39fe695e8b446ac91aee96a9f867c7f48b8228f2c9f6ac547c8172e0da344`. No geometry, cadence, retained depth, or tolerance was tuned in this restart. Every numerical row uses normalized $c_f=1$.

The [launch-readiness receipt](2026-08-27-braid-search-launch-readiness.md) records the independently reviewed mathematical instruments and actual-history proofs. The new adapter was authored against those frozen references. Its actual twelve histories contain 1,032 segments each. Both the nominal continuous proof and the separate EOM endpoint-domain/radius supplement accept all 12,384 member-segments. The latter includes the binary64 endpoint overhangs and two-operation velocity-radius reserve; its arithmetic premise is finite IEEE binary64 nearest rounding with gradual underflow. That premise is explicit, not a measured floating-environment control.

Plainly: the root calculation uses nonzero, independently checked error allowances around the actual stored histories. It does not inherit the old zero-width pilot's authority or force the geometry's future to obey the equations of motion.

## Complete pair results

**Measured by the EOM solver and accepted by the separately authored frozen ledger reducer:**

| Reception phases | Ordered pair certificates | Nonself ordinary roots | Self rows with excluded zero-delay endpoint | Maximum visited cells | Highest achieved precision |
| --- | --- | --- | --- | --- | --- |
| 8 | 1,152 | 1,056 | 96 | 52 | 53 bits |
| 32 | 4,608 | 4,224 | 384 | 52 | 53 bits |
| 128 | 18,432 | 16,896 | 1,536 | 52 | 53 bits |

Plainly: every ordered pair was accounted for, including every self history. Each nonself pair had one ordinary positive-delay root at every tested reception. The reported 53-bit route means these rows did not require the available higher-precision escalation; it is not permission to remove that route from future runs.

Every accepted certificate is complete, has an empty failure code and a root-free complement, and avoids retained-memory boundary contact. Every ordinary root has positive transmitter and receiver factor margins and meets the unchanged `1e-8` bracket-width limit. Every self row has no ordinary positive-delay event and owns its coincident-endpoint exclusion. The genuine eight-phase and eight-plus-thirty-two-phase prefixes passed the frozen validator composition before their successors ran. The unchanged public three-rung reducer then accepted all 24,192 certificates and the repeated-reception consistency checks.

This is a finite, declared reception-grid result over the retained search intervals. It does not certify every possible reception phase, an infinite causal past, perturbation stability, or a future generated by the EOM solver.

## Original evidence and execution history

The actual-history preparation lives under `.local-data/braid-analysis/2026-08-26-f5-enclosed-root-restart/prepared-20260827-v1/`. The accepted root packets live under the sibling `root-ladder-20260827-v2/` directory. All originals are write-once.

| Evidence | SHA-256 |
| --- | --- |
| Actual `history-manifest.json` | `5c665fcd7eee92a105fd958929ee443e4eeaea6afc0222935739aad2622a1725` |
| `preparation.json` | `31e6540b80701863753a594c19c548fd3ae92cba52a11309e644e8d49cd634ff` |
| `nominal-history-conformance.json` | `f862a7148a0a00b3bde5fbb0d164156fce2dbfc161597b0cdaa182457f3741e0` |
| `api-domain-conformance.json` | `440deb996eaeb646b7863e9276fb937f9897c11fdbd56fed11a32efb269fe746` |
| Original and profiled `ledger-reduction` JSON, byte-identical | `37b934854075b500239a733df1b5e70a7ff355f0e56bbdc382adad952288a3a5` |
| Combined scientific ledger digest reported by the reducer | `cde5b4f0812d0e745b1fd1388e0dd61219a1dd53c9f99f40d4efbd5d47e161fb` |
| Frozen adapter source | `9f7661f4000174d631d4c60f7078e124d77ae9b2ddba6af36197f13096095f81` |
| Reviewed build toolchain record | `977082a60a74aa6bda8aa4c8adbe9634936df82921c40a090a15317f5952ee38` |
| Reviewed v2 coordinator | `3a26191ba03afafb1b357df56f06e19e3ff143418324dec942609377f20dcb98` |
| v2 `reviewed-build.json` | `5c8a9c36804b8bfed45b7f98834c0c104e758465ca0d19402bf0c328d81f9710` |
| Preserved stopped v2 `run.json` | `9ebfc69c11b2b1bf34c5904140e9d4e1f6e1dc3f9e1063e3f773bb7235d83532` |
| Successful `profile-final-reduction.log` | `cee357e450ce6461036bc1db7b33c2833a151c9fcab95562fb053c89655658d6` |
| v2 `rung-128.ndjson` | `495a3f60664905f021567c821f47969277624550eb305d883c63faa8f6ef0aa7` |
| v2 `rung-128.json` | `119b27b438d4dab010b99ddfd9bbd6939f83c3a0f4cbb072fcba8d182e010698` |

Plainly: these hashes identify the exact histories, checking instruments, build, and outputs. Rechecking the same saved ledger is a reproducibility and resource measurement, not a second independent mathematical proof.

The separate closeout review rehashed all 81 unique run-bound files without drift, checked the actual per-call request census, compared every one of the 24,192 original NDJSON rows with its sorted packet without loss, alteration, or duplication, and revalidated the prefix/final receipt identities. It accepted the scoped H3 disposition without recomputing roots or changing the subject or references.

The first attempt, `root-ladder-20260827-v1`, obtained passing solver certificates at all three sizes but stopped while pretty-printing the 128-phase packet: its formatted row body exceeded JavaScript's maximum string length. Its raw certificates remain preserved. The reviewed coordinator repair writes compact JSON one row at a time, preserving every certificate field and hashing the exact emitted bytes. No mathematical reference, solver, history, or root control changed.

The second attempt completed and the independent full reducer wrote its accepted receipt. Its surrounding memory profiler subsequently returned a sandbox permission error while reading a kernel counter. Accordingly, the original `run.json` still says `stopped`; it has not been relabeled or overwritten. A separately supervised, permission-approved rerun of only the unchanged reducer and profiler exited successfully, closed its process group, and produced the byte-identical `ledger-reduction-profiled.json`. No roots were rerun for that profiling retry. The final evidence disposition must read the accepted receipts together with this explicit operational history, not treat the stopped orchestration status as a scientific failure or silently erase it.

## Measured cost and parallel-use boundary

The v2 root calls, including reconstruction and raw output, took `4.492`, `5.744`, and `10.932` seconds at 8, 32, and 128 phases respectively. The first two independent prefix checks took `1.102` and `3.161` seconds. The successfully profiled final independent reduction took `11.10` seconds wall time, `13.19` seconds user CPU time, and `0.48` seconds system CPU time. Its maximum resident set was `5,152,899,072` bytes and peak memory footprint `5,219,132,800` bytes, with zero reported swaps. The enclosing watcher measured `11.105` seconds through process closure.

Plainly: final validation is a significant memory consumer even though this particular root calculation is quick. These measurements justify managing validation concurrency separately from pair-call concurrency; they do not establish the cost of the A/B/C pilot or a large cohort.

## Falsifier and next artifact

Reject this result if any bound source, instrument, executable, library, history, raw packet, or receipt changes; if an accepted row violates its complete-root conditions; if repeated receptions disagree; or if either independent history containment proof fails. The preserved raw files and the frozen reducer provide the operator-checkable test. No score or candidate disposition changes automatically.

The next scientific artifact is a separately predeclared ordinary EOM evolution. It must use the approved history only as prehistory on `[-1,0]`, initialize at zero, and let the EOM solver generate every future state. The prescribed continuation is comparison data only. Coupling and charge normalization, horizon, numerical refinement, and stopping rules must be explicitly selected rather than borrowed from unrelated fixture defaults.

Closure goal: preserve the completed scoped H3 result and declare the ordinary-evolution test without importing a prescribed future or a physical claim.
