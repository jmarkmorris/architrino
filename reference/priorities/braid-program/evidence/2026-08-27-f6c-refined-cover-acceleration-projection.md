# asymmetric counter-breathing representative refined-cover projection into the unchanged acceleration reference

Status: independently accepted definition, conditional proof and one-cell predeclaration; implementation pending and not a launch plan. The refinement attempt named below has completed and passed its separate independent scientific/source/operational audit. The projection definition and proof passed independent review before implementation. No acceleration evaluation is performed or authorized by this document; acceptance of its prior cover does not grant acceptance or execution authority to a new adapter implementation.

This is a narrow successor to the [one-cell acceleration predeclaration](2026-08-27-f6c-continuous-reception-acceleration-predeclaration.md), using the output of the [emission-refinement predeclaration](2026-08-27-f6c-emission-refinement-predeclaration.md). It changes only the authenticated root-cover input and its projection, meaning the exact mapping from stored fields to the existing typed mathematical input. It preserves the [acceleration reference](2026-08-27-f6c-continuous-reception-acceleration-reference.md), [reconstructed-history family](2026-08-27-f6c-accepted-frame-history-reconstruction.md), [continuous-reception enclosure theorem](2026-08-27-f6c-continuous-reception-enclosure-contract.md), original frame data and [member-acceleration definition](2026-08-26-f6c-normalized-member-acceleration-predeclaration.md).

Plainly: the next calculation would use the same model and acceleration law with the narrower, separately checked emission intervals. This draft defines how to pass those intervals faithfully; it does not report the resulting acceleration or permit a run.

## Named input, evidence boundary and immutable mathematics

Let `D` denote the repository-relative directory `.local-data/braid-analysis/f6c-emission-refinement-20260827/pilot-cell-0-v2`; `D-outer` denotes its sibling `pilot-cell-0-v2-outer`. The proposed input is the complete four-file refinement cover, not a selection of favorable pairs. The following exact identities identify the candidate source chain. The three JSON receipt/manifest hashes were reread while drafting; stream identities are those recorded consistently by the manifest and comparison, not a new independent stream audit in this document.

| Input | Bytes | SHA-256 |
| --- | ---: | --- |
| `D/queries.ndjson` | 1,989,686 | `44d59ae62f8d7d9a9e7afd1d684e8ee15b8aeadf4dc92d489a787e5e224029fa` |
| `D/rows.ndjson` | 160,384 | `b6309b3c90f75590ba8270ea6cea1644be46727692a19be6aa364163e108035f` |
| `D/pieces.ndjson` | 48,186 | `075007966aa14b3d0c9ff896d5cb752d06e410f1002dc2bc39a8e7d8db55340a` |
| `D/cover-manifest.json` | 60,086 | `d4ec0d60631dd46cf2872ace941677dfd980af6f8cdbe540347b5252d336ebb6` |
| `D-outer/comparison.json` | 60,290 | `eed41550c3c743df419efab8d0f9ad6094b43fa1efe3f4949365e731c1f7c63e` |
| `D-outer/pilot-admission.json` | 427,106 | `51f0b3774bfb489bbab4fddd7f7612c6d4132f2654a36aa4091e5445eca9b51c` |
| [Refinement launch v2](2026-08-27-f6c-emission-refinement-launch.v2.json) | 47,868 | `295d1d8a8366942c4aa3f0c586e028faae08e2ac64517c837f0fe73a6b8b8a88` |

Plainly: these hashes name the exact records the adapter must consume. Merely reading an `accepted:true` field is insufficient: the subsequent implementation must authenticate the files and their independently accepted completed-run chain.

The coordinator reported fresh completion of original session `15793`, exit zero, with whole-attempt elapsed time `238.116677375` seconds. The separate independent audit accepted 219 named binding checks totaling 22,043,417 bytes, including 202 source bindings totaling 16,479,218 bytes; recomputed all eight original history digests across 14,080 segments and all 112 closed-piece hashes without reevaluating roots; and checked disappearance of 20 observed PIDs, five process groups, descendants and the shared lock. Its resource audit found 955 RSS observations, maximum 384,843,776 bytes, maximum gap `412.525125` ms, and 20 host observations with minimum free-memory percentage 52 and minimum available disk 964,329,439,232 bytes. These are reported measurements from that independent audit, recorded in the [accepted refinement closeout](2026-08-27-braid-search-launch-readiness.md#independently-accepted-actual-one-cell-emission-refinement), not new measurements by this draft. The stored admission's `237.98697625` seconds is explicitly before publication, not the whole-attempt duration. The failed earlier refinement attempt and its plan remain unchanged and supply no accepted result.

Plainly: the refined input now has a completed independent audit. Its measured cost establishes only the cost of that refinement attempt, not the cost or outcome of a later acceleration calculation.

The unchanged mathematical subject is [`continuous_reception_acceleration.py`](../../../../scripts/eom/oracle/continuous_reception_acceleration.py), SHA-256 `abfc21f29d8bdd984118b1e0ba0cb62b88a081a75a961052eb11f31ea7bdd7b8`, with its [independent controls](../../../../tests/test_eom_continuous_reception_acceleration.py) at `26b7c5455a57da5beba6e7fd32a0b7bfbc8e1f32630b663c55a33273e8cc1823` and proof declaration at `c1a5358e1d887fab5b4753368dc14ec59ed220294f42d2afa4ac40f962ee537f`. Its public `CellRangeInput`, `evaluate_cell` and `CellRangeResult.to_record` contracts remain byte-frozen. The separately authored range comparison mathematics in [`verify-f6c-continuous-reception-acceleration.py`](../../../../scripts/eom/verify-f6c-continuous-reception-acceleration.py), SHA-256 `cc26f5a45d0e09a472e3066d0d62ae8192492a7c3e0ab18a3658781a0274b299`, and its [existing controls](../../../../tests/test_f6c_continuous_reception_acceleration.py), SHA-256 `be741dccccd90c349849b19dc15df1acd4ea5752ec6f8a9e98e7ae14013c52c6`, also remain unchanged. Both comparison files must be captured in the successor's reference closure; they are not already supplied by the prior 16-role ancestry or the 15-member refinement subject closure.

Plainly: no formula, rounding rule or numerical reference needs to change. The work is a new source adapter and independently checked mapping around those existing functions.

## Fixed family, cell, identities and seven mathematical bindings

The subject remains the already defined family $\mathcal F_H$: admissible globally $C^1$ prehistories, meaning continuously differentiable histories, inside the original retained enclosures and anchored to the exact accepted-frame future $H$ at zero. It is not asserted to contain the historical EOM trajectory. The reception cell is exactly $I=[0,0.001]$ inside the first accepted frame $[0,0.002]$; the retained domain is $[-8,0.13]$. Both frame endpoints and every position/velocity coordinate are copied as original decimal strings from `acceptedFrames[0]` and `acceptedFrames[1]`, with their exact edge in `acceptedFrameIntervals[0]`. No binary floating-point conversion, reconstructed endpoint substitution, frame-error inflation or selection of a particular prehistory is permitted.

The exact literals remain field speed `1`, coupling `10.304229970992187`, ruler `0.5320012303229503`, and charges with magnitude `0.1666666666666666666666666666666667`. That recorded magnitude is not replaced by exact $1/6$. Labels remain `0+`, `0-`, `1+`, `1-`, `2+`, `2-`, `3+`, `3-`, in that order, with path keys 1 through 8 and alternating polarities. The refined manifest's explicit `charge` must agree lexically with the original export as well as with polarity. Original history fingerprints retain their provenance meaning; `historyDigest` identifies the reconstructed root-library history and is not a substitute for a file hash.

Plainly: only the interval in which each pair's emission can occur changes. The path family, frame curve, charges, normalization and member ordering do not.

`CellRangeInput.bindings` remains an exact tuple of seven `Binding(role,path,sha256,bytes)` values, in the following existing order. Every binding is authenticated before construction; the mathematical function itself does not read files.

| Existing mathematical role | Exact source for this projection |
| --- | --- |
| `original_export` | Original export SHA-256 `f479bb88a6425e9e98e00288f2524f33d5a3c0f4c2a14139dbaae4f468c46db1` |
| `reconstruction_receipt` | Original reconstruction receipt `7c30aae03d43f7720b79288a19a9c9f9a7c0ab6b7b16ac9a948828ca80b92b43` |
| `guards_receipt` | Original uniform-guard receipt `86d7fa14ac64ee20930094ff1a59880fe4e1ef5c81758f5d8baf2c6777ee4880` |
| `root_cover` | Refined `D/cover-manifest.json`, `d4ec0d60631dd46cf2872ace941677dfd980af6f8cdbe540347b5252d336ebb6` |
| `root_cover_comparison` | Refined `D-outer/comparison.json`, `eed41550c3c743df419efab8d0f9ad6094b43fa1efe3f4949365e731c1f7c63e` |
| `member_acceleration_predeclaration` | Unchanged member predeclaration `c67de8cce1370eed779b560c269d5ca0a7505bdb175d39cff1276b75a7e69853` |
| `continuous_reception_enclosure_contract` | Unchanged root theorem `f20e4bdaaff8b6f0012fdc6135b15d568a817832fb55d5c42f80d8421a117f68` |

Plainly: the seven-role mathematical interface stays intact. The refined manifest and its independent comparison replace only the two old root-cover bindings; operational and supporting provenance belongs outside this tuple.

The exact new `CellRangeInput` has `scope='f6c-reconstruction-family'`, `precision=90`, `cell_index=0`, `frame_index=0`, `cover_status='conditional_complete'`, the domains and literals above, eight immutable `Member` values and 64 immutable `RootRow` values. `Member` receives label, decimal path-ID string, exact charge, authenticated history digest, and the four original coordinate triples `position_left`, `velocity_left`, `position_right`, `velocity_right`. Original frame `positionErrorBound` and `stateFlags` remain checked provenance, not additional allowances applied to $H$. Source interval records have precision 90; their exact lower/upper strings become `Bounds(lower,upper)` without reformatting. All nested containers passed to the reference are exact tuples, with detached immutable leaves; no caller-owned mutable alias survives construction.

## Complete pair and closed-piece projection

The new source manifest must have schema `braid-program/f6c-emission-refinement-cover.v1`, scope `pilot-cell-0-emission-refinement`, status `conditional_complete` and `accepted:false`. Its exact census is one cell, eight members, 3,584 face queries, 64 pair rows, 56 ordinary nonself rows, eight self exclusions and 112 piece records. These are pair-cell certificates: each ordinary row states exactly one root for every reception in the entire closed cell, not one isolated root for the cell. All 56 restriction records and the 32-lower/reset/32-upper query order must agree with the authenticated independent comparison. The projection does not run or alter that search.

For row index $n=8i+j$, require exact receiver-major ownership, cell index zero, original member IDs, the common reception record, `rootFreeComplementConditional:true` and `retainedBoundaryContact:false`. For $i\ne j$, its emission record must equal that pair's authenticated restriction $E_{ij}=[L_{ij},U_{ij}]$, with $-8\le L_{ij}<U_{ij}\le-0.05<0$. Preserve the row's exact strings and fields as follows.

| Stored row field | Existing `RootRow` field |
| --- | --- |
| `receiverId`, `transmitterId`, `reception`, `emission` | `receiver_id`, `transmitter_id`, `reception`, `emission` |
| `ordinaryRootsPerReception`, `coincidentEndpointExcluded` | `ordinary_roots_per_reception`, `coincident_endpoint_excluded` |
| `oldestResidual`, `lowerFaceResidual`, `upperFaceResidual` | `oldest_residual`, `lower_face_residual`, `upper_face_residual` |
| Three `displacement` intervals, `distance` | Three `displacement` bounds, `distance` |
| `transmitterFactor`, `receiverFactor` | `transmitter_factor`, `receiver_factor` |
| Referenced receiver/transmitter `clippedPiecesSha256` | `receiver_coverage_sha256`, `transmitter_coverage_sha256` |
| `rootFreeComplementConditional`, `retainedBoundaryContact` | `root_free_complement_conditional`, `retained_boundary_contact` |

Plainly: every pair keeps its own checked emission interval and geometric bounds. The adapter cannot average pairs, exchange receiver and transmitter, reuse another pair's interval, or change a stored number while renaming its field.

For an ordinary row require count one, no coincident-endpoint exclusion, strictly negative upper endpoints for both `oldestResidual` and `lowerFaceResidual`, strictly positive lower endpoint for `upperFaceResidual`, positive distance lower endpoint, transmitter-factor lower endpoint at least $10^{-24}$, and positive receiver-factor lower endpoint. The oldest residual is evaluated at the original retained boundary $A=-8$; the lower-face residual is evaluated at $L_{ij}$. They are distinct obligations even when a particular $L_{ij}$ happens to equal $A$. Never impose or synthesize `oldestResidual == lowerFaceResidual` for a refined pair.

For each self row require count zero, `coincidentEndpointExcluded:true`, and `None` for emission, all three face residuals, displacement, distance, both factors and both coverage hashes. Its two source piece pointers must likewise be null. There are no self piece records. The unchanged reference produces an exactly zero self contribution because the certified noncoincident self-root set is empty, not because an ordinary kernel is evaluated at zero distance.

Plainly: the oldest retained boundary still proves that no earlier root was lost. A refined lower face cannot replace that evidence. Self interactions retain their separate exclusion rule and never divide by a zero separation.

For each nonself row consume exactly two piece records in order, receiver then transmitter, with sequential record indices across all 56 rows. Verify the row pointer, row index, role, member ID, history digest, requested interval, first/last original segment index, contiguous index range, count and SHA-256. Receiver coverage uses $I$; transmitter coverage uses this row's $E_{ij}$. Independently reconstruct every closed intersection with all 1,760 original segments of that member, including both singleton pieces when a request touches a shared knot. The canonical clipped-piece hash is the existing `clipped_coverage` definition: concatenate UTF-8 `index`, tab, exact rational clipped lower endpoint, tab, exact rational clipped upper endpoint, newline, in original order. It is not a hash of a reformatted JSON list.

Any coverage memoization must key on the authenticated history identity and exact requested interval, for example `(historyDigest, lowerRational, upperRational)` within the fixed precision/mode call. Metadata ownership and exact requested strings are still checked separately for each record. The old independent adapter's `(member, role)` cache key is insufficient: one transmitter occurs with different refined intervals in different receiver rows. Recomputing coverage without a cache is equally valid. Receiver reuse is legitimate only because its independently checked interval is identical. Exactly 112 records must be consumed, with no unvisited null record or tail in any stream.

Plainly: the hash must describe the history pieces actually needed by that pair. Reusing a transmitter's earlier piece list just because its name is unchanged would break the source-to-calculation link.

## Conditional preservation theorem

Assume the authenticated reconstruction and uniform-guard premises for $\mathcal F_H$, the independently admitted complete refined cover, and the exact projection above. For every $X\in\mathcal F_H$, every $T\in I$ and every $i\ne j$, the cover gives one ordinary emission $s_{ij}(T;X)\in E_{ij}$. The recorded displacement, distance and factor intervals contain their corresponding values at that root. Their required strict faces and retained-boundary evidence establish completeness for this family. Projection copies these enclosures unchanged, and therefore preserves that universal root-domain implication; it does not assert that an arbitrary emission in $E_{ij}$ is a root.

With $\mathbf D_{ij}$ the recorded receiver-minus-transmitter displacement, $R_{ij}$ its norm and $D_{t,ij}$ the transmitter factor, the unchanged per-hit acceleration is

$$
\mathbf a_{ij}(T;X)=\kappa q_iq_j\,\frac{\mathbf D_{ij}(T,s_{ij})}{R_{ij}(T,s_{ij})^3\,|D_{t,ij}(T,s_{ij})|}.
$$

Plainly: the refined evidence encloses the actual root contribution for every permitted history and reception. It does not replace the root by the midpoint of its interval or pretend every time in the interval is a physical hit.

The distance and factor lower bounds make division valid, and $D_{t,ij}\ge10^{-24}>0$ makes its absolute value equal to itself on these intervals. Exact rational interval arithmetic therefore encloses each component of this expression with its recorded signed charges. The receiver factor $D_{r,ij}>0$ remains a root-transport premise and is copied and checked; it is not a multiplier in this sharp kernel. Empty self-root sets contribute exact zero. Summing all eight transmitter rows encloses the complete acceleration for each receiver without selecting roots or dropping sign contributions.

The accepted-frame Hermite curve $H_i$ on $[0,0.002]$ is identical before and after this projection, so its second derivative is the same affine function of reception time. Endpoint extrema on the closed cell enclose $H_i''$ exactly. The unchanged residual and its squared norm are

$$
\boldsymbol\rho_i(T;X)=L_0\left(H_i''(T)-\sum_{j=0}^{7}\mathbf a_{ij}(T;X)\right),\qquad \|\boldsymbol\rho_i(T;X)\|^2=\sum_{k=1}^{3}\rho_{i,k}(T;X)^2.
$$

Plainly: narrowing the root input leaves the required motion and residual definition unchanged. The new output, if checked, would be an enclosure of the same one-cell acceleration mismatch, not a new metric or a claim of balance.

The proof composes three existing inclusions: the reconstructed family lies within the original history allowances; each actual root lies in its independently checked pair box; the unchanged acceleration/residual operations enclose their inputs. Exact rational operations followed only by outward 90-significant-digit serialization preserve these inclusions. This is a derived conditional statement, not a numerical result for the named files. It neither guarantees narrower output than the earlier broad-box range nor establishes a sign, small residual, equilibrium, retention or stability. Correlated interval overestimation can remain even after valid emission refinement. The left endpoint uses the first future frame's right-sided curvature; no prehistory curvature or knot average is substituted, and no peak convention changes.

## Independent reconstruction and source closure

The old producer's `project_cell` and old checker's `reconstruct_projection`/`authenticate_prior` cannot consume the new manifest directly. Their old schema, common emission interval, oldest-equals-lower assertion, three-output prior completion and cached-pilot receipt contract remain correct only for their original inputs. Do not weaken those frozen functions or disguise the new manifest as the old schema. A separately reviewed successor mapping must authenticate the new four-file chain and typed fields above. It may reuse the old prior authenticator unchanged solely to check the old ancestry with its old role map; that does not authenticate the refined result.

The new independent comparison must reconstruct its expected projection from the original export and original refined row/piece bytes, not from a producer-supplied expected input or an import of the producer. The unchanged `original_history` and `clipped_coverage` helpers can reconstruct original digests and requested closed coverage. With the new projection independently established, the frozen `compare_ranges`, `corner_acceleration`, `basis_curvature` and `check_contains` functions remain applicable without mathematical edits: independent signed corner extrema check 192 pair components; direct Hermite differentiation, interval sums/differences and squared-norm bounds check 80 member intervals. The result must also preserve every copied binding, member and root field exactly. Agreement here checks a separately derived rational enclosure, not merely the subject's determinism.

Plainly: the source adapter and its checker must build the same input independently. The existing mathematical checker can then test the acceleration output without rerunning or changing the root search.

Outside the seven mathematical bindings, the successor must capture and authenticate the following original bytes and relationships, retaining their exact path/hash/size triples and source roles rather than flattening them into new mathematical premises:

- All four refined cover files, the refined comparison, the refined admission, the v2 machine plan and the durable external prior-closure observation. Manifest, comparison, admitted stage outputs and plan must name the same bytes. The producer stage must have completed with `accepted:false`; the independent comparison stage must have completed with its narrow conditional acceptance. Stored elapsed-before-publication fields are not whole-attempt measurements.
- The refinement declaration, actual producer and controls, source-bound verifier and controls, pure refinement comparator and controls, frozen cached root library and its controls, history and decimal primitives and required controls, old captured producer/helper and its controls, and cache-equivalence declaration. Authenticate the complete declared 15-member subject closure and its plan correspondence, not a hand-selected subset. The exact producing generation is the v2 plan's generation, including the reviewed receipt-parser compatibility repair.
- The complete original 16-role ancestry mapping, including original export, reconstruction, guards, historical broad-cover files/comparison/admission/plan, prior-closure owner, unchanged acceleration source/controls/proof, member predeclaration, root theorem and reconstruction theorem. These remain provenance and family premises; the two broad-cover records do not occupy the new mathematical `root_cover` roles.
- The actual runtime and operational closure of the prior attempt, as listed by the admitted v2 plan, and the later successor's separately reviewed runtime, source, controls and operational plan. Include the public large-integer-division metadata exercise when capturing the Python runtime inventory so its lazily loaded existing helper files are not omitted. No runtime inventory is a hermetic operating-system attestation.
- Original completion logs, resource observations, stage exit/retired-group evidence and final flushed coordinator completion/exit needed to authenticate the prior admission, including the independently accepted external postpublication audit. The next operation must make its own such observations. Do not transfer prior timing or memory measurements to a new acceleration attempt.

Plainly: seven bindings are sufficient for the pure mathematical input but not for trusting where that input came from. The surrounding adapter keeps the complete source and completed-run chain without falsely turning operational metadata into a physics premise.

All parsers must be bounded, reject duplicate keys, nonfinite numbers, wrong types, extra records and incomplete finalization, and preserve numeric strings. This new successor declares a closed document-class selector: default `data` permits strings of at most 8,192 characters; `operational-receipt` permits at most 131,072 characters, the 128 KiB limit for these ASCII provenance strings. Unknown or nonstring class selectors fail. Scientific decimal tokens retain the stricter unchanged 1,152-character, 1,024-digit and absolute-exponent-1,000 limits; the receipt class cannot relax them. The existing bounded structure checks remain depth at most 24, key length at most 4,096, at most 20,000 array entries and 10,000 object members, with the unchanged 64 MiB whole-file limit. Receipt classification follows the semantic operational-admission role, never a filename/hash exception or automatic retry with a larger limit.

The coordinator's metadata inspection of the new refined admission found four captured provenance command strings of lengths 73,095, 73,095, 73,179 and 73,179 characters. They fit this successor's 128 KiB receipt class but not the accepted refinement producer's 64 KiB class. That already accepted producer consumes the older ancestor admission, whose maximum corresponding string length is 52,671; its parser and evidence remain unchanged. This is a new consumer-format declaration, not a retroactive relaxation of the producing attempt. All scientific-file, log, memory, time and observation limits remain unchanged.

Plainly: the new receipt contains longer records of how its processes were started. The next adapter must read those records completely without widening any mathematical input or resource limit, and without changing the accepted earlier program.

Same-descriptor regular-file capture, exact executed-source capture, before/after binding checks and complete source generation checks remain required. The manifest's original seven false `claims`, the comparison's copied `candidateClaims`, and its pure analysis's false authority flags remain distinct from the source-bound checker's conditional success. The acceleration result retains all ten false flags of the unchanged `Claims` dataclass, including `accepted`, `root_coverage_established`, `subject_membership_established` and `historical_trajectory_identity_established`; a later external comparison records its own narrower result separately.

## Review sequence, bounded operation and falsifiers

First independently review this definition/proof, preserving the separately accepted prior audit. Next author, review and freeze the source-bound independent projection/range comparator and its controls before a separate producer implementation uses it. Preserve all frozen mathematical/reference bytes and existing actual evidence. The producer's input adapter is the subject of that independent mapping check; neither side may import the other as its oracle. A later exact operational composition and machine plan must be reviewed separately. This document supplies neither executable source hashes for those unbuilt components nor a machine plan.

The only proposed subsequent evaluation is this one closed reception cell, using the already completed refined cover without new residual queries, root calls, reception subdivision or changed emission proposals. It retains one serial acceleration producer followed by one independent comparison, inclusive 1,800 seconds through capture, both stages, publication, cleanup, final flushed completion and owned-process closure; 15-second heartbeats; aggregate observed RSS below the unchanged 2 GiB stop threshold with observation gaps no longer than one second; host admission at least 40% free memory/64 GiB free disk, stopping below 20%/16 GiB, with the existing 15-second/two-second observation contract; 64 MiB scientific-file and 16 MiB combined-log limits; fresh ignored output and exclusive private-to-public publication; the existing shared phase-varying display representative/asymmetric counter-breathing representative resource lock. Sampled RSS is not a hard allocation ceiling. There is no automatic retry, cap relaxation, prefix reuse, additional worker or concurrent resource-measured attempt.

Plainly: a fresh one-cell calculation still needs its own reviewed launch and complete measurements. Neither the successful refinement timing nor this proof authorizes execution or a full 160-cell acceleration campaign.

Synthetic controls must separately expose wrong source generation, changed charge/frame lexemes, a dropped or reordered pair, a self row with fabricated geometry, pair-specific intervals accidentally replaced by a common one, copied oldest/lower residuals, coverage reused across two distinct requests for the same transmitter, omitted knot singletons, wrong piece ownership/count/hash, trailing null/extra records, output missing an independently derived endpoint, promoted authority, and late/unclosed publication. Include genuinely different pair domains for the same transmitter and independently hand-derived signed-kernel/Hermite cases; never derive the expected answer from the producer. Portable parser controls must accept/reject lengths 8,192/8,193 in the default class and 131,072/131,073 in the receipt class, reject invalid class selectors and preserve duplicate/nonfinite/depth/key/array/object/whole-file guards and scientific-token limits. Before any actual range run, a separately reported metadata-only control must decode and authenticate the exact new admission with its 73,179-character command strings in receipt mode and reject it in default mode, with scientific imports/evaluation/root calls trapped. This actual-file control is not a mandatory fixture dependent on ignored local evidence. Any actual-data mapping-only control must likewise trap numerical evaluation and be reported separately from portable fixtures.

The source-bound projection is falsified by any original-to-typed mismatch, missing input, unsupported complete-run assertion or violated interval/coverage condition. The conditional numerical enclosure is falsified by a valid independently derived value outside a reported bound or failure of the named family/root premise. A valid but wide or zero-containing residual remains unresolved evidence, not balance or imbalance; changing settings to obtain another answer requires a separate declaration. No M05/M06 quadrature, three-rung metric completion, score change, H3 promotion, EOM evolution, historical-trajectory identity, physical realization, equilibrium or retention conclusion follows here.

Closure goal: preserve the exact refined pair-cover evidence while defining its independently checkable projection into the unchanged one-cell acceleration law, without changing the represented family or granting execution or metric authority.
