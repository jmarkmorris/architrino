# asymmetric counter-breathing representative Emission-Only Refinement Predeclaration

Status: bounded mathematical and algorithmic declaration for independent review, 2026-08-27. No refinement implementation, actual sign-query calculation, refined cover, acceleration range or execution authorization is supplied by this document.

## Subject And Direct Consumer

The subject is the same reconstructed asymmetric counter-breathing representative history family and the same closed reception cell zero, $I_r=[0,0.001]$, used by the [one-cell acceleration declaration](2026-08-27-f6c-continuous-reception-acceleration-predeclaration.md). The only proposed numerical change is a separately certified restriction of each nonself emission interval inside its original $[-8,-0.05]$ box. Reception time is not subdivided. The direct consumer is a separately bound successor of the existing one-cell acceleration-range mapper and its independent output comparison; the unchanged [range reference](2026-08-27-f6c-continuous-reception-acceleration-reference.md) already accepts pair-specific emission intervals.

Plainly: this attempt would ask where, within the already covered past, each arrival can occur. It keeps the same receiver-time window and the same possible histories. A broad but valid acceleration interval is not a negative result about the geometry.

The [accepted-frame reconstruction theorem](2026-08-27-f6c-accepted-frame-history-reconstruction.md) identifies all admissible anchored pasts joined to the fixed accepted-frame Hermite future. The [uniform-face theorem](2026-08-27-f6c-continuous-reception-enclosure-contract.md#uniform-root-boxes-not-scalar-samples) supplies the restriction rule. The [member-acceleration predeclaration](2026-08-26-f6c-normalized-member-acceleration-predeclaration.md) retains ownership of the measurement definitions, normalization, three-rung requirements and subdivision limits. This artifact adds no candidate gate, score row or substitute historical trajectory.

Plainly: a narrower certified time interval restricts the location of arrivals, not the family of permitted paths. The existing measurement recipe and its stronger historical claims are unchanged.

## Frozen Sources And Inherited Premises

The original export remains `.local-data/braid-analysis/f6c-history-export-20260827.jUhLLg/retained-history.json`, SHA-256 `f479bb88a6425e9e98e00288f2524f33d5a3c0f4c2a14139dbaae4f468c46db1`. Preserve all eight ordered identities `(0+,0-,1+,1-,2+,2-,3+,3-)`, path keys `(1,2,3,4,5,6,7,8)`, all 1,760 original pieces per member, complete retained domain `[-8,0.13]`, coefficient origins and every original coefficient, endpoint, per-axis error and scalar error token. The original scalar allowances must continue to cover their three corresponding axis allowances. Neither allowance form is reduced or replaced.

Plainly: none of the saved uncertainty is spent to obtain a more attractive result. Every restricted state box must still contain the original permitted coordinate and velocity values at every touched piece, including both pieces at a shared endpoint.

Retain the externally authenticated reconstruction receipt at SHA-256 `7c30aae03d43f7720b79288a19a9c9f9a7c0ab6b7b16ac9a948828ca80b92b43` and uniform-guard receipt at `86d7fa14ac64ee20930094ff1a59880fe4e1ef5c81758f5d8baf2c6777ee4880`, with their exact paths and roles from the one-cell declaration. Their premises concern one common continuously differentiable history and its own derivative, not independently chosen points from position and velocity boxes. The existing cached-pilot cover, comparison and caller-observed closure bindings remain the starting evidence; the later [acceleration launch v2](2026-08-27-f6c-continuous-reception-acceleration-launch.v2.json), SHA-256 `688f1ba9154d9fb45daefe95213996cd007bde151504077399d714dd93523b6e`, is provenance for the preceding range attempt, not authorization to run this successor. Every future input must be authenticated by its original bytes; a prepublication receipt alone does not prove successful final process closure.

Plainly: the existing records justify the history family and its uniform guards. The new calculation must bind those records faithfully and must earn its own completed-result evidence.

We work in normalized wake-speed units with $c_f=1$. Keep exact speed bound `0.85`, nonself clearance bound `0.27`, 90-decimal-digit directed arithmetic, transmitter-factor floor `1e-24`, coupling `10.304229970992187`, original signed charge magnitudes `0.1666666666666666666666666666666667`, and ruler $L_0=0.5320012303229503$. Do not substitute exact $1/6$ for a recorded charge. The unchanged required acceleration remains the analytical second derivative of the original first accepted-frame Hermite polynomial, with its original frame endpoints, positions and rates. No new frame, frame-error inflation, receiver-factor multiplier, softened interaction or new physical setting is introduced.

Plainly: the law and the ruler do not change. Only the past-time boxes supplied to that law may become smaller after a proof that no arrival is lost.

| Frozen mathematical owner | SHA-256 |
| --- | --- |
| [Uniform-face theorem](2026-08-27-f6c-continuous-reception-enclosure-contract.md) | `f20e4bdaaff8b6f0012fdc6135b15d568a817832fb55d5c42f80d8421a117f68` |
| [Cached root library](../../../../scripts/eom/oracle/continuous_reception_roots_cached.py) | `daa4cc227cb8685de673fc400d817a19666b4fc7323e6c3a56f475a463b23acf` |
| [Original history arithmetic](../../../../scripts/eom/oracle/certified_history.py) | `ca916b4bc979629a5e25c1490da07fd78a26b4e75cfba5677f35fbab658a29e7` |
| [Directed decimal arithmetic](../../../../scripts/eom/oracle/decimal_interval.py) | `fffc17270e149e6213315c1c82b518caa739657eb649822fd1955b8a2820e38a` |
| [Acceleration-range reference](../../../../scripts/eom/oracle/continuous_reception_acceleration.py) | `abfc21f29d8bdd984118b1e0ba0cb62b88a081a75a961052eb11f31ea7bdd7b8` |

Plainly: these are existing frozen mathematical instruments, not files to revise while implementing their new caller. Their existing controls and proof documents also remain unchanged and must be included in the successor's exact binding set.

## Whole-Face Query

For receiver $i$, transmitter $j$, receiver time $T_r$ and proposed emission time $m$, use the existing unrestricted residual

$$
g_{ij}(T_r,m)=\|\mathbf X_i(T_r)-\mathbf X_j(m)\|-(T_r-m)
$$

Plainly: the query compares the geometric separation with the wake's available travel distance. The receiver time ranges over the entire fixed cell; the emission time is one exact proposed face. A query is not a scalar-reception root certificate.

The public `unrestricted_residual(receiver, transmitter, reception, emission)` API supports this query with reception `DecimalInterval[0,0.001]` and point emission `DecimalInterval[m,m]`, both at precision 90. It copies and validates the original histories, forms complete closed-piece unions, and evaluates distance minus delay without imposing a root equation. The future caller must use a fixed local decimal context with precision 90 and the root library's exponent bounds, and exact finite-decimal midpoint serialization. Root-only distance/delay or factor intersections are forbidden during a face query. Record the returned interval $G=[G_-,G_+]$ exactly, including its precision and the original member ownership.

Plainly: the actual API can check the proposed face without alteration. The interval must contain every possible residual value across the receiver-time cell and the original history allowances; a sample or an already root-constrained distance would not do that job.

## Deterministic Two-Sided Search

Let $A=-8$ and $B=-0.05$ be the original proved emission faces. Visit the 56 nonself pairs in receiver-major, transmitter-minor order, skipping only the eight explicit self pairs. For each pair perform the following lower search and then an independent upper search. Each search uses exactly 32 queries if no input, arithmetic, binding or resource failure interrupts it. There is no success-based early stop, changed precision, extra query round or automatic retry.

Plainly: every pair receives the same fixed amount of exploratory work in a reproducible order. The number 32 limits algorithmic effort; it is not a root-location tolerance, physical parameter or promise of useful acceleration accuracy.

For the **lower search**, initialize exploratory endpoints $(\ell,h)=(A,B)$ and the retained certified lower face $L=A$. On each of 32 iterations compute the exact midpoint $m=(\ell+h)/2$ and the whole-face interval $G$:

- If $G_+ < 0$, retain $L=m$ with this negative-face proof and set $\ell=m$.
- Otherwise set only $h=m$. Leave the certified face $L$ and its proof unchanged.

Plainly: only a strictly negative upper endpoint proves that the whole face lies before every arrival. Any other result merely redirects the exploratory search; it does not prove that the face is positive or that a root is absent.

For the **upper search**, independently reset exploratory endpoints $(\ell,h)=(A,B)$ and retained certified upper face $U=B$. On each of 32 iterations compute the exact midpoint and whole-face interval:

- If $G_- > 0$, retain $U=m$ with this positive-face proof and set $h=m$.
- Otherwise set only $\ell=m$. Leave the certified face $U$ and its proof unchanged.

Plainly: only a strictly positive lower endpoint proves that the whole face lies after every arrival. This search starts from the original interval, not the lower search's exploratory state. A result touching or spanning zero never moves a certified face.

The final proposal is $[L,U]$. Preserve the original certified face whenever its search finds no improvement. An exploratory interval is never itself a root bracket. A query returning a malformed/nonfinite interval, failed original-piece coverage or an exception stops the attempt with its completed prefix; it is not the ordinary indeterminate-sign branch. Before final certification require exact $A\le L<U\le B<0$. A crossed, equal or invalid final interval is unresolved and must not be clipped or silently reverted to conceal the failure.

Plainly: even an unsuccessful refinement can retain the original safe interval. A broken or inconsistent calculation is different: its failure must remain visible rather than being converted into a favorable result.

## Derived Safety And Exact Representability

**Claim grade: derived, conditional.** Under the original common-history, uniform-speed, clearance and oldest-boundary premises, each nonself pair has one retained root at every reception time. The frozen theorem gives strict increase of $g_{ij}(T_r,m)$ with emission time, with increase at least $(1-0.85)$ times the emission-time increase. A newly retained negative face therefore lies strictly below every such root; a newly retained positive face lies strictly above every such root. Initially the original faces have those properties. Induction over the 64 queries preserves both certified inequalities, independently of every exploratory branch that failed its strict sign test.

Plainly: each retained face has its own valid sign proof. Since the residual rises as emission time advances, a negative face cannot lie after the unique arrival and a positive face cannot lie before it. Moving only proved faces keeps every permitted arrival inside the final box.

Consequently the final certified emission interval contains all roots for the same entire history family and the same reception cell; it does not select a favorable past. The opposite halves discarded from the *exploratory search* need not be root-free and are never published as such. The argument proves safe containment, not an optimal bracket, a minimum amount of narrowing, or a strictly narrower acceleration result. Interval overestimation can redirect the exploratory searches before a sharper face is found.

Plainly: the method may fail to discover an available improvement, but it may not earn a false improvement by throwing away uncertain arrivals. Its safe result and its numerical usefulness are separate questions.

At midpoint level $k\le32$, every exploratory endpoint and query is of the form

$$
m=A+\frac{(B-A)n}{2^k}
=-8+\frac{159n}{20\,2^k},\qquad 0\le n\le2^k
$$

Plainly: repeated halving creates a rational grid within the original interval. The integer $n$ identifies the grid point and $k$ counts halvings; the two original endpoints remain included.

The denominator $20\,2^k=5\,2^{k+2}$ divides $10^{k+2}$, so exact serialization needs at most $k+2\le34$ fractional places. Since every value has magnitude at most eight, at most 35 significant decimal digits suffice. The reception endpoints are also exact finite decimals. Thus all proposed time operands fit the existing precision 90 and bounded token/exponent interface without rounding. Construct them by exact rational arithmetic or an equivalent checked integer-decimal operation; never pass through binary floating point. This representability statement concerns time operands only, not exactness of polynomial, square-root or division results, which still require directed enclosure.

Plainly: the proposed time grid is much simpler than the available arithmetic precision. This prevents accidental time-grid changes, while the actual geometry calculation still needs outward error bounds.

## Final Complete Cover And Range Mapping

After all 56 pair searches finish, call unchanged `enclose_root_cover` with all eight complete original histories, retained domain `[-8,0.13]`, reception domain exactly `[0,0.001]`, and one `ReceptionCellProposal` containing every pair's final interval. Keep the exact immutable member-digest, speed and clearance premises. The final call must freshly pass its oldest-boundary check and both unrestricted final-face checks, then its positive distance, transmitter-factor floor, positive receiver factor, complete original-piece coverage and conditional root-free-complement checks. Require `conditional_complete`, 64 receiver-major rows, 56 ordinary nonself roots per reception and eight explicit excluded self endpoints with no singular geometry evaluation. Self geometry and coverage fields remain absent, not fabricated hashes.

Plainly: exploratory sign proofs do not replace the final complete-cover calculation. The ordinary library checks the finished set of boxes as a whole and preserves the same 64 pair identities.

Every final nonself row retains its two compact original-piece coverage records, giving 112 such records; their touched-piece counts and hashes must be reconstructed for the actual narrower intervals. Original shared-knot singleton pieces remain included. No historical root row, piece hash or broad-box geometry is relabeled as if it were recomputed on a narrower box. The independent range mapper then passes these newly authenticated rows to the unchanged acceleration reference, retaining original frame zero, all endpoint lexemes, all charges and the fixed ruler. The output remains eight member residual vectors and their nonnegative squared-norm intervals, not an integral, peak or score.

Plainly: narrower time intervals require their own corresponding geometry and piece-coverage evidence. The following acceleration calculation uses those new bounds without changing what the law or the frame-derived curvature means.

## Independent Comparison And Bounded Execution

The maximum exploratory query census is $56\times(32+32)=3584$. This is not the entire workload: the final complete cover, independent replay/comparison, range calculation, publication and closure are additional work. Retain, within the successor's ordinary output, each query's pair, search side, ordinal, exploratory endpoints, exact midpoint, exact returned interval, branch decision and retained certified face. An independent replay must reconstruct the exact search state, validate every query enclosure and branch, and verify every retained face inclusion against original source bytes without importing the proposer as its oracle. Same-code replay alone checks determinism, not mathematical correctness. Freeze and independently review this reference/comparison route before implementing its separate proposer and consumer; do not alter the existing frozen mathematical libraries or their controls in either batch.

Plainly: the query record lets the independent comparison check every decision that moved a bound. It belongs to the result the existing consumer needs, not a new candidate-status system.

Before an actual run, a fresh, independently reviewed machine plan must bind the source generation, original evidence and caller-observed prior closure, all successor controls, all actual runtime dependencies and their existing cache files, new absent output paths, and the serial operation order. Retain the existing inclusive 1,800-second ceiling, 2 GiB aggregate sampled-RSS stop, maximum one-second RSS observation gap, 15-second heartbeat/host checks, 64 MiB per named raw output, 16 MiB combined operational logs and shared competitor exclusion. Host admission requires at least 40 percent free memory and 64 GiB available disk; stop below 20 percent free memory or 16 GiB available disk. The same single-attempt ceiling covers capture, all queries, final cover and independent comparison, any declared range evaluation/comparison, durable publication, final rehashes, owned process/worker/monitor and lock cleanup, final standard-stream handling and observed exit. No additional worker, cap increase, resume, automatic retry or silent precision escalation is permitted.

Plainly: the larger amount of mathematical work must still finish inside the already established operating limits. A count of 3,584 proposals does not demonstrate that it will do so.

The public `unrestricted_residual` query copies and validates histories on each invocation; it does not expose the private cache used within `enclose_root_cover`. This declaration neither bypasses those checks nor adds cross-call caching. Measure the actual fresh pilot's inclusive wall time, separately scoped CPU, sampled aggregate memory, named bytes and complete query/face/piece counts. Do not infer cost from narrower boxes or fewer touched pieces. Any later performance change would require its own separately reviewed source generation.

Plainly: the existing public interface may spend substantial time rechecking the same history. That is a cost to measure, not permission to reach into private implementation state during this attempt.

## Stopping And Claim Boundary

The 32-query allowance is new bounded algorithmic effort for this emission-only diagnostic; it is not borrowed from the member measurement's 20 subdivisions per original accepted-frame interval. This attempt performs zero reception subdivisions and introduces no root-width target. It neither changes nor completes the original three-rung EOM/history/root-tolerance ladder, the RMS and peak enclosure widths, their quadrature/branch-and-bound rules, or the one-sided evaluation of the predeclared supremum. Original history uncertainty can impose a nonzero attainable width even after the exploratory intervals become small.

Plainly: no accuracy requirement is relaxed and no extra frame-refinement budget is quietly granted. A complete result may still be too broad to say whether the path closely follows the law.

Permitted outcomes are a complete independently conformant refined cover and declared range output, a complete but unchanged or uninformative restriction, or an explicit unresolved/failed attempt with its prefix preserved. Non-strict exploratory signs alone are not failures; missing final coverage, an invalid denominator, an independent mismatch, a binding change, or resource/publication/closure failure prevents acceptance. A failed or broad family diagnostic cannot be interpreted as proof against every asymmetric counter-breathing representative geometry or as a favorable residual. Historical EOM trajectory identity, original analytic-past membership, metrics `M05/M06`, H3 promotion, scores, EOM execution, equilibrium, binding, retention, return, stability and physical realization remain excluded.

Plainly: the result must distinguish a safe but unhelpful bound from a broken calculation, and both from an actual geometrical counterexample. None grants historical score authority.

**Falsifiers:** a retained face without its strict whole-cell sign proof; a permitted root outside a final certified box; a copied or omitted original-piece interval; a midpoint or branch that differs from the exact 32-step algorithm; altered source tokens, uncertainty or normalization; use of root-constrained distance to establish a face; an independently reconstructed range outside the reported enclosure; or acceptance without complete census and fresh bounded closure overturns the corresponding claim. The next concrete artifact is the separately authored exact-query replay/comparison and its synthetic known-answer controls, frozen before the proposer. No refinement is executed by this declaration.

Plainly: every claimed improvement must be traceable to an exact proposed face, an independent sign check, the unchanged source history and a completed operation.

Closure goal: obtain independent review of this single-cell, source-preserving emission-refinement rule before implementation or any actual refinement attempt.
