# Fixed-$120^\circ$ Orthogonal-Plane Weave Fold-Limiting Exclusion

**Disposition: bounded no-balance theorem for the fixed relative-phase locus on $\beta_f\in[0.25,12]$.** The fourteen boxes left unresolved by the ordinary interval certificate are now closed by the caustic routing already declared in the canonical Master Equation plus directed interval bounds on both sides of every fold. The result contains one coincident-self boundary, six persistent circular folds, and seven transverse cross-binary folds. No unresolved parameter box remains.

Plainly: the exact six-worldline history cannot supply its prescribed circular acceleration at any speed in the declared bounded interval. This closes only the equal-radius, equal-frequency, fixed-plane history with relative phases $0$, $2\pi/3$, and $4\pi/3$. It does not reject other three-dimensional $3{:}3$ histories.

## Frozen Inputs and Existing Fold Law

The extension leaves four controls unchanged: the committed ordinary interval certificate, its independent Python oracle, the canonical Master Equation, and the focused six-worldline evaluator. Their SHA-256 bindings are recorded in the machine receipt. The prior certificate already covers $497$ ordinary cells of total width $11.749999944$ and identifies fourteen disjoint fold boxes of total width $5.6\times10^{-8}$.

The canonical Master Equation supplies the relevant event routing. A nondegenerate transverse fold has ordinary branch strength proportional to $|T_r-T_{r,*}|^{-1/2}$ and finite integrated impulse, but no finite pointwise acceleration is assigned at $D_t=0$. A persistent interval with $D_t=0$ is outside that finite-impulse lemma and remains a regularized-chart failure. A coincident same-transmitter root birth is not an accepted transition, and the coincident $x=0$ self root is excluded by convention.

Plainly: no new fold law was invented for this result. The calculation applies the law already stated by the Master Equation: cross an ordinary finite-order caustic through its integrated chart, but do not pretend that the singular branch sum has a finite instantaneous value; reject persistent and coincident-self degeneracies from the ordinary chart.

## Local Fold Reduction

At reception phase $\phi=0$, use $x=\beta_f\Delta$. The four scalar causal equations are

$$
H_s=2-2\cos x-\frac{x^2}{\beta_f^2},
\qquad
H_p=2+2\cos x-\frac{x^2}{\beta_f^2},
$$

$$
H_+=2+2\sin x-\frac{x^2}{\beta_f^2},
\qquad
H_-=2-2\sin x-\frac{x^2}{\beta_f^2}.
$$

Every interior fold satisfies $H=H_x=0$ with $H_{xx}\ne0$ and positive delay. For self and partner rows the circular causal equation depends only on delay, so $D_r=D_t$. Their six interior folds therefore have $D_r=D_t=0$ at every reception phase: they are persistent folds, not caustic transits.

For a cross transmitter of endpoint sign $\sigma$, the reception-phase equation is

$$
F_\sigma(\phi,x;\beta_f)
=
2-2\sigma\cos\phi\sin(\phi-x)-\frac{x^2}{\beta_f^2}.
$$

At each cross fold, $F_x=0$ and the fold equation gives

$$
D_r
=
-\frac{\beta_f^2(F_\phi+F_x)}{2x}
=1.
$$

Thus all seven cross folds are nondegenerate and transverse in reception time. Their two born roots have the same transmitter, polarity, limiting line of action, and relevant transverse sign. The pair therefore adds rather than cancels as $|D_t|\to0$.

Plainly: the same-binary folds stay singular for the whole circular history and cannot be treated as isolated events. The cross-binary folds are isolated events, but both merging wakes point the same way for a given receiver, so their singular transverse accelerations reinforce one another.

## Complete Box Exclusion

Let $\beta_*$ be one interior fold speed. The lobe-minimum theorem gives exactly three cases.

1. For $\beta_f<\beta_*$, the target root pair is absent. The frozen root-sheet oracle encloses every remaining phase-zero contribution across the whole fold box, and the relevant transverse interval excludes zero.
2. For $\beta_f>\beta_*$, the target pair exists. Each target contribution has one fixed transverse sign, while every other root remains simple and bounded. A directed lower bound on the combined target magnitude exceeds the worst opposing bound from all other rows.
3. At $\beta_f=\beta_*$, a persistent circular fold is a canonical chart failure, while a transverse fold has no finite pointwise acceleration. Either disposition fails the declared requirement that the Master Equation equal the finite prescribed acceleration at every phase.

The $\beta_f=1$ boundary is separate. Since $2-2\cos x<x^2$ for every $x>0$, no nontrivial self root exists at exactly $\beta_f=1$. After excluding the coincident root, the remaining tangent acceleration lies in $[0.0739381489,0.0739381557]$. Immediately above $1$, the born self contribution is positive and divergent; immediately below, the same remaining tangent interval excludes zero.

Plainly: every box is closed on its no-root side, root side, and exact fold. The point $\beta_f=1$ is not hidden inside a caustic convention; its canonical five-root ledger is finite and already has the wrong tangent acceleration.

| fold class | count | smallest no-root-side transverse separation from zero | smallest root-side dominance margin |
| --- | ---: | ---: | ---: |
| coincident-self boundary | $1$ | $0.0739381489$ | $2.0833333\times10^7$ |
| persistent self/partner folds | $6$ | included in global minimum | included in global minimum |
| transverse cross-binary folds | $7$ | included in global minimum | included in global minimum |
| all fourteen boxes | $14$ | $0.0450332058$ | $197.3912463$ |

The prior ordinary width and newly closed fold width satisfy

$$
11.749999944+0.000000056=11.75,
$$

with a directed accounting residual below $10^{-110}$. The frozen JavaScript six-worldline evaluator independently checks both outer boundaries of every box: all roots are complete and simple there, the no-root-side projection lies inside the frozen-oracle interval, the root-side projection exceeds the declared dominance margin with the predicted sign, and the root count increases by one at the coincident-self birth or two at every interior fold.

Plainly: the analytical certificate covers the continuum, while the independently authored earlier evaluator checks representative ledger arithmetic at all twenty-eight box boundaries. Agreement is not being claimed between two implementations edited together; both frozen controls predate this fold extension.

## Physical Finding, Claim Grade, and Falsifiers

The fixed-$120^\circ$ equal-radius orthogonal-plane weave does not satisfy complete-phase Master Equation acceleration balance anywhere on $\beta_f\in[0.25,12]$. The phase-zero ordinary certificate supplies the necessary-condition rejection away from folds, and the fold-limit extension closes the omitted boxes. This is a negative result for the exact prescribed locus, not a stability calculation and not a family-level rejection.

The caustic routing is derived in the frozen Master Equation. The persistent/transverse classification and $D_r$ identities are derived from the exact circular and cross-phase equations. The box exclusions are measured by directed interval arithmetic with analytical dominance bounds. The bounded fixed-locus conclusion follows from the certified partition.

The result is falsified by an ordinary point inside a closed box where both transverse components vanish, an opposite-sign leading target pair, another simultaneous unbounded row that cancels the target divergence, failure of the exact identities $D_r=D_t$ or $D_r=1$, a changed frozen-input hash, or a canonical replacement that assigns a finite pointwise $D_t=0$ acceleration and invalidates the recorded routing.

Plainly: no retention, stability, binding, physical realization, alternative relative phasing, variable-speed history, breathing history, precessing-plane history, other $3{:}3$ locus, or $N>3$ family follows from this theorem.

## Reproduction

- Protocol: `src/prescribed-path-analysis/protocols/orthogonal-plane-weave-fold-limiting-exclusion-protocol.v1.json`
- Fold-limit certificate: `scripts/prescribed-path-analysis/oracle/orthogonal_plane_weave_fold_limit_certificate.py`
- Machine record: [2026-08-29-orthogonal-plane-weave-fold-limiting-exclusion.v1.json](2026-08-29-orthogonal-plane-weave-fold-limiting-exclusion.v1.json)
- Directed certificate tests: `tests/test_orthogonal_plane_weave_fold_limit_certificate.py`
- Frozen-subject comparison tests: `tests/orthogonal-plane-weave-fold-limit.test.js`
- Reproduction: `VIRTUAL_ENV="${AAA_VENV:-../.venv}" "${AAA_VENV:-../.venv}/bin/python" scripts/prescribed-path-analysis/oracle/orthogonal_plane_weave_fold_limit_certificate.py --write-receipt reference/priorities/braid-program/evidence/2026-08-29-orthogonal-plane-weave-fold-limiting-exclusion.v1.json`
