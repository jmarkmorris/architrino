# Noether Braid Chapter Review — 2026-07-23

Scope: full read of the rewritten `content/markdown/aaa/noether-braid/` scene (all 15 files, in the reading order declared in `noether-braid.md`), cross-checked against the `foundations/` and `dynamics/` canon. Comments are graded: **E** = error/inconsistency, **X** = cross-corpus consistency, **O** = omission, **C** = clarification, **P** = positive finding worth protecting, **I** = innovation/suggestion. Line numbers are approximate against the current files.

Overall verdict first: the rewrite is strong. Claim discipline is enforced uniformly (every family chapter carries a Claim Boundary with an explicit falsifier), the taxonomy/requirements/mathematics/methodology separation of ownership is clean, quarantined numerical residuals from the pre-2026-07-12 era are correctly absent, and the reader-facing on-ramps ("Simple Picture", "Plain language" callouts, the engineer-reads-a-requirements-spec framing) follow the house style. The debts below are concentrated in four places: Family-C bookkeeping, one operator-content leak into the methodology chapter, one substrate-scale conflation in `braid-mathematics.md`, and one notation collision in the topological charge chapter.

---

## Errors and internal inconsistencies

**E1. `noether-braid.md` family map is stale on Family C.** The overview table (line ~55) says: "Family C | An assembly composed of two complete B1 braids. | C1 has the same circulation sense; C2 has the opposite circulation sense." Under the current taxonomy, C1/C2 are the *general* twelve-worldline coaxial members that explicitly do **not** require B1 decomposition (`braid-family-c.md` line ~113: "Family C does not require its twelve worldlines to decompose into two B1 braids"); the two-B1 members are C3/C4, and the two-B1.3 members are C5/C6. The front-door chapter therefore misdefines the family and misassigns the circulation split. Fix the overview row to match `braid-taxonomy.md` §Family C.

**E2. Braid-count bookkeeping for Family C is inconsistent.** The Assembly Composition coordinate is defined as "Number of complete braids in the assembly" (`braid-taxonomy.md` line ~15). C3–C6 are declared "1, with two B1 components" in the Assembly Composition Master Table — but their components are *complete B1 braids* by the family-C definition, and the Individual Braid Master Table gives C3–C6 two braid-index rows each. By the column's own definition, C3–C6 have braid count 2. Separately, C1/C2 (twelve worldlines, six binaries) are recorded as "braid count 1" even though a braid is defined as a six-architrino/three-binary object — so C1/C2 are not one braid under the scene's own definition; they are a coaxial worldline train. Recommend: braid count 2 for C3–C6; for C1/C2 either braid count NA with an explicit "coaxial train, not a braid" note, or a new column semantics.

**E3. Broken anchor (the only one in the scene).** `noether-braid-configuration-space.md` line ~281 links `../dynamics/master-equation.md#return-map-symplectic-residual-for-action-derived-branch-promotion`. The target text exists (master-equation line ~4257) but as a **bold run-in paragraph**, not a heading, so the anchor does not resolve. Either promote it to a heading in `master-equation.md` or link the enclosing section. I mechanically checked every relative link and anchor in the 15 files; all others resolve.

**E4. Self-referential link.** `noether-braid-configuration-space.md` line ~74: "The three-row or rank-three search region is developed in [Noether Braid Configuration Space](noether-braid-configuration-space.md)" — the chapter cites itself. Presumably "developed below"; fix the sentence or point to the specific section.

**E5. Notation collision: `D_t` means two things in the same scene.** `noether-braid-topological-charge.md` defines the signed self-sector degree as $D_t$ (with partner $D_p$), while $D_t$ is the canonical transmitter-side factor $c_f-\mathbf V_j\cdot\hat{\mathbf r}$ throughout the scene (A2 kernel, methodology, envelope interface) and the whole dynamics canon. Worse, "t" suggests *transmitter* while the sector is the *self/same-transmitter* sector. Binary Dynamics uses $D_{ij}$ for the signed degree. Recommend $D_s$/$D_p$ (or $\chi_s/\chi_p$, see X12) for the degrees; this is a high-priority rename before downstream consumers cite the triple.

**E6. "Force closure" violates the acceleration-first terminology policy.** `braid-recovery-requirements.md` line ~228 uses "force closure" three times ("a resolved causal-root ledger does not imply force closure; …"). The ledger rows in question are architrino-level acceleration rows; per the theory-layer discipline, force language is reserved for assembly-level bookkeeping. Suggest "acceleration-balance closure" or "dynamical closure."

**E7. Who defines the geometry — tables or depiction?** `braid-taxonomy.md` line ~5: "The member's table entries and Borg depiction define the geometry." But line ~251 says "The first three tables carry the geometry" and line ~343 says the navigation table "does not define the geometry." The Borg depiction lives in the navigation table. Line 5 should say the geometry record (the first three master tables plus the source record) defines the geometry; the depiction renders it.

**E8. Misdirected reading-discipline pointer.** `braid-mathematics.md` §Scoped Anti-Damping, item 3 (frozen rigid octahedral chart): "the reading discipline for that chart is recorded in [Noether Braid](noether-braid.md#neutral-braid-base)". The neutral-braid-base section carries the inventory/curve definitions, not the rigid-carrier reading rules; those live in `braid-recovery-requirements.md` line ~228 ("A negative result for one rigid carrier hypothesis is not a rejection of…"). Repoint the link.

**E9 (adjacent canon, for a dynamics fix, not a braid edit).** `dynamics/causal-action-functional.md` line ~178 claims "On the non-translating circular chart, $D_r=D_t$ and $W^{\mathrm{acc}}=1$." The first equality is right (playback $m=D_r/D_t=1$); the second conflates playback with the acceleration weight, which is $W^{\mathrm{acc}}=c_f/|D_t|=1/|J|\ne1$ on that chart ($J_p=1+\beta\sin\xi$, $J_s=1-\beta\cos\xi$). The braid scene itself uses the canonical weight everywhere — but its Action/Tail rows cite the causal-action chapter, so the upstream line should be fixed before a braid packet inherits it.

---

## Cross-corpus consistency (foundations / dynamics)

**X10. `d_0` conflation in the fold-geometry click analysis.** `braid-mathematics.md` §Fold Geometry of the Click calls $d_0$ "the point-transceiver short-distance self-regularization scale … of order the near-field two-body scale $\kappa\epsilon^2/c_f^2$." In the dynamics canon the short-distance regulator is $\epsilon_c$ (core scale; $\eta$ is the wake width), and $d_0:=R_{\mathrm{MCB}}$ is a *conditional emergent unit* whose existence hangs on the unproven MCB-attractor hypothesis; foundations define neither $d_0$ nor $R_{\mathrm{MCB}}$ at all. As written, a substrate-level regularization claim is grounded in an emergent, hypothesis-dependent scale. Recommended repair: state that the coincidence-stratum transacted amount is set by the *declared spatial regularization* $\epsilon_c$, and separately grade the identification of the physical value of that scale with the MCB/near-field scale $\kappa\epsilon^2/c_f^2$ as a hypothesis. The scoped-negative conclusion (single-site self-hit clicks are not chart-clean) survives unchanged; only its scale attribution needs regrading.

**X11. Speed-symbol discipline is correctly observed — protect it.** The recovery targets use $\gamma_0(v_{\mathrm{eff}})$ with $c_0$; the mathematics chapter's speed hierarchy and Transverse Causal Budget Lemma refuse to identify $c_f$, $c_{\mathrm{eff}}$, $c_\gamma$, $c_0$ without a declared dressing map. This matches the canonical five-row speed table in foundations exactly. (One nit: `noether-braid-configuration-space.md` §Speed table says "below the *local* field-speed hinge" — $c_f$ is a global constant; drop "local".)

**X12. Unify the signed-degree story with foundations.** The fold-pair law in `noether-braid-topological-charge.md` ($\Delta N=\pm2$, $\Delta D=0$) reproduces the foundations signed causal-root ledger $\chi_{\mathrm{root}}=N_+-N_-$ and its fold-pair surgery rule (`foundations/absolute-timespace.md` §causal-root ledger), and the master-equation delay-map pack. The chapter cites only the dynamics side. Add the foundations cross-cite and note $D_\sigma=\chi_{\mathrm{root}}$ restricted to sector $\sigma$ — this is exactly the multi-route "seam of the ontology" that `noether-braid.md` line 11 says should be logged and unified.

**X13. Proper-time certificate convergence — also worth an explicit cite.** The certified-braid residual in `noether-braid-configuration-space.md` ($\mathcal R_{\mathrm{cert}}\le\epsilon_{\mathrm{cert}}$ plus positive non-symmetry stability margin) uses the same symbols and structure as the clock-validity certificate in `foundations/absolute-time-defense.md` ($\mathcal R_{\mathrm{cert}}(\mathcal A)\le\epsilon_{\mathrm{cert}}$, $\Delta^{\perp}_{\mathrm{Floquet}}>0$). That is intentional convergence (proper time is a braid phase functional), but neither file points at the other. One sentence in Candidate And Certified Braids would close the loop.

**X14. Foundations ladder is now stale on family names.** `foundations/emergence-of-structure.md` rung 4 names "three-binary braid candidates (A1, A2, B1 classes)"; the taxonomy now carries A3 and Family C. A foundations-side refresh item, not a braid edit.

---

## Omissions

**O15. No certificate instantiation for Family C.** `braid-recovery-requirements.md` instantiates the contract only for the six-body case ($|\Pi_{\mathrm{all}}|=30$). Family C has twelve worldlines ($12\times11=132$ ordered pairs) plus optional six-site Accessory Configuration inventory, and C3–C6 add a two-component reduction that, by the chapter's own rule, "may compress the all-pairs ledger only after its reduction map proves how the compressed entries are inherited." None of this is stated anywhere. At minimum add one paragraph: Family C inherits the general contract with the 132-pair root row, and the two-B1 members owe a reduction map.

**O16. The framing/linking row promised by foundations has no home in the certificate.** Foundations make the framed self-linking row $Lk=\operatorname{Wr}+\operatorname{Tw}$ a protected branch-certificate row and treat $Lk$ jumps as branch transitions (`absolute-time.md` §exchange, `constructing-the-absolute-frame.md`). The braid scene carries chirality entries ($\chi_c$ in $\Lambda_{A1}$, $\chi_{\mathrm{fr}}$ with its $\Sigma_{\mathrm{frame}}$ wall), and the spin-statistics recovery row demands the exchange sign come "from the same retained row that supplies spinor closure" — but the retained-branch certificate $\mathsf R(B,W)$ has no framing row and never says which row owns $Lk$ data. Either add a Framing row or state explicitly that the Event row owns framing-wall crossings and $\chi_{\mathrm{fr}}$/$Lk$ data, so the exchange-sign obligation has a named source.

**O17. The anti-damping conclusion never routes to the canonical export ledger.** `braid-mathematics.md` §Scoped Anti-Damping ends exactly right: "a retained branch must have somewhere to put the pumped action." The canonical bookkeeping for that somewhere already exists — wake escapement $E_{\mathrm{esc}}(W)$ in `dynamics/energy.md`, with its entropy-arrow tie — but no braid chapter links it. One cross-link from the anti-damping section (and/or the certificate's Tail/Noether rows) to the energy chapter's wake-escapement section would connect the scene's central obstruction to the ledger that must absorb it.

**O18. Cloud/throughput operations content is misplaced in a textbook chapter.** `braid-analysis-methodology.md` §"Local and Google Cloud Throughput Supplement" (and parts of §Performance Measurement) carries: an Apple M3 MacBook Air worker benchmark table, a dated GCE C4A on-demand price (USD 0.60608/hr, us-central1, 2026-07-23), Spot-VM discount discussion, deterministic-merge worker architecture, price-performance formulas, and the internal script path `scripts/eom/benchmark-compact-monte-carlo-workers.mjs`. This is operator/campaign infrastructure, not reader-facing physics; the authoring policy bars workstream content and repo-internal paths from `content/markdown/aaa`. `reference/priorities/braid-program/analytical-campaign-performance.md` already exists as the natural home. Recommend moving everything from "Local and Google Cloud Throughput Supplement" through "Price-Performance Measurement" there, leaving one sentence in the textbook ("cost is a measured property; campaign throughput and price-performance discipline are maintained in the operational campaign records"). The three-stage campaign list and the coverage/full-adjudication lanes *are* methodology and should stay.

**O19. A3.3's selection machinery and the methodology's measure set don't meet.** The A3.3 chapter defines an exactly computable selection apparatus for prescribed records — resonance-block decomposition of $C_L(m,n;\phi)$, the harmonic-overlap lemma, the polygon condition, the $A_2$-fold tail budget $\varepsilon_L=O(L_{\mathrm{eff}}^{-2})$ — while `braid-analysis-methodology.md`'s measure table has only a generic "Spectral ledger" row. Adding a resonance-block cancellation-score row keyed to a declared lock $(m,n)$ would let the Monte Carlo campaign rank locks with existing machinery instead of leaving the A3.3 functional uninstrumented.

---

## Clarifications

**C20. The rotating A2 channel *is* a B1 record — say so.** This is the most consequential unstated fact in the scene. The rigidly rotating face-opposite seed (rotation about $\hat{\mathbf n}=(1,1,1)/\sqrt3$) is, read in taxonomy coordinates with the common axis $\hat{\mathbf n}$: one common midpoint, one common axis, one common frequency, one circulation sense, equal radii $R$, equal axial half-separations $h=R/\sqrt3$, equal transverse radii $\rho=R\sqrt{2/3}$, phases $120^\circ$ apart — an exact interior B1 record (in the B1.1 domain, $h/\rho=1/\sqrt2$), and precisely the cyclic-symmetric B1 sublocus. Consequences the chapters should carry:

1. The A–B family boundary is currently stated only at $\lambda_A=1$; the rotating channel is a second, exact bridge that has nothing to do with flattening.
2. The A2 invariant-channel lemma is therefore the only proved symmetry channel *on the B1 locus* — `braid-b1-symmetry.md` should claim it.
3. The rigid-rotating-wave axial no-balance derivation (`braid-mathematics.md` anti-damping item 5) is then a statement about the cyclic-symmetric two-ring B1 locus: every rigid polarity-segregated interior B1 record (the B1.1/B1.2 two-ring subloci) fails axial balance at any sub-field rim speed, leaving only the all-equatorial boundary B1.3 for the rigid family. `braid-b1-symmetry.md` argues *for* rigid co-rotation (harmonic matching) without mentioning that the axial derivation forces its interior members planar — the two chapters should cite each other.
4. There is a chart-assignment ambiguity to name: the same paths admit an A2 label (orthogonal binary axes with the half-separation vectors precessing) and a B1 label (fixed common axis). Since member identifiers are chart labels, dual-chart membership should be recorded the way A1 ⊂ A3 already is.

Also worth one sentence in `braid-family-a.md`: at the fixture's snapshot coordinates ($h=R$, $\rho=0$), the A2 *taxonomy* motion (rigid rotation about each binary's own axis) is static — the face-opposite seed under taxonomy motion is exactly the zero-angular-momentum static seed, and the rotating channel is a different prescribed motion. Currently a careful reader can't tell whether the rotating channel is an A2 taxonomy record (it is not, with fixed axes) or a separate seed class.

**C21. "Photon-channel response endpoint" at $\lambda_A=1$ reads as a photon-identity claim.** `braid-taxonomy.md` line ~156 assigns the flat Family-A limit to "the photon-channel response endpoint," while the photon hypothesis itself is the coaxial contra-rotating polarity-conjugate planar pair owned by Electroweak Bosons (`braid-family-c.md` §Interface). If the Family-A endpoint means "the response geometry a braid adopts in the photon-channel regime," rephrase to say that; as written it invites reading flattened Family A as the photon.

**C22. Kinematic momenta in A2 presuppose a linear momentum function.** $\mathbf P_{\mathrm{kin}}=\sum\mathbf v_i$ implicitly takes the energy chapter's unspecified momentum function $P(s)\propto s$ with equal weights. The direction/screw conclusions survive any isotropic $P(s)$ by the same projector symmetry, but one line saying so would keep the no-primitive-mass discipline airtight (same for $\mathbf J_{\mathrm{kin}}$).

**C23. $D_{\mathrm{plane}}$ is defined five times across the scene** (twice within `noether-braid-configuration-space.md` — §Angular-Momentum Frame and §Why Three Retained Rows partially duplicate each other — plus A3.3, topological charge, and implicitly in the frame-orthogonality section). Pick the configuration-space definition as canonical, merge the two overlapping sections, and cross-reference elsewhere.

**C24. Unexplained "Hinge | None" row.** The B1 constraint table in `braid-family-b.md` has a "Hinge" row that appears nowhere else as a member coordinate. Either define hinge occupancy as an optional member column (it is currently an analysis diagnostic in configuration space) or drop the row.

**C25. `d_C = 1.10` has no stated units.** The methodology calls it "the canonical constrained-display value" and demands campaigns publish units for every coordinate, but the value itself carries none. State the catalog normalization convention where the value first appears.

**C26. Mislabeled self-link.** `braid-recovery-requirements.md` line ~68: the link text "[Neutral Braid]" points to this file's own §Base-Family Certificate Instantiation, while the neutral-braid definition is said to live in `noether-braid.md`. Relabel to "Base-Family Certificate Instantiation" so "Neutral Braid" has one target in the scene.

**C27. State the aggregation map from $\Lambda_{A1}$ to $(N_s,M_p)$.** In the six-body braid, "partner-hit" aggregates intra-binary and inter-binary channels; $\Lambda_{A1}$ keeps them split ($\mathcal G_a$, $\mathcal G_{ab}$). One displayed identity ($N_s=\sum_a$ self-rows of $\mathcal G_a$; $M_p=$ partner rows of $\mathcal G_a$ plus all rows of $\mathcal G_{ab}$, or whatever the intended convention is) would prevent the two labels from disagreeing silently when both are reported for one branch.

**C28. The envelope interface diagnostic needs a plain-language on-ramp.** §Assembly-Noether Sea Interface Diagnostic in `braid-envelope-geometry.md` is ~500 lines of projector and tolerance machinery with no bright-first paragraph, unlike every other heavy section in the scene. One opening paragraph — "the diagnostic asks, at a given point and for a declared purpose (clock, corridor, packing, penetration): whose wake dominates here, the assembly's locked wake or the ambient sea's?" — would restore the house pattern.

---

## Positive findings worth protecting

**P29.** The realization-independent certificate + first-failure ladder (`braid-recovery-requirements.md`) is the strongest piece of editorial engineering in the scene: it makes "a favorable export diagnostic cannot rescue an open retention row" structurally enforceable, and the margin entry ("a row that fails by a certified interval is worth more than a row that merely lacks evidence") is exactly the right evidential economics.

**P30.** The A2 conditional no-return certificate is a real theorem-grade advance: the signed inverse-square lower bound plus energy-integral margin converts "does the seed return?" into the sharp named target "exhibit an absorber that ends sub-field speed before the margin is crossed." The escape-margin logic checks out ($E=\tfrac12\dot R^2-K/R$ nondecreasing while $\dot R>0$ under $\ddot R\ge-K/R^2$).

**P31.** The action-clicks escapement picture, and especially the coincidence-versus-finite-chord fold split (single-site self-hit clicks are cusp-born at vanishing chord and hence not chart-clean; cross-hit caustic clicks at finite chord are the surviving route), is the best new physics writing in the scene — an honest mechanism proposal with its own scoped negative built in.

**P32.** The topological-charge chapter's refusal to let $c_1$ masquerade as a Chern class ($H^2(S^1;\mathbb Z)=0$; degree-pair semantics until a genuine two-torus curvature chart exists) and the A3.3 chapter's harmonic-overlap lemma with the $A_2$-fold exponent $p_{\mathrm{fold}}=3/2$ and the $\Delta C_L>2\varepsilon_L$ stopping rule are both correct as mathematics (verified: $|x|^{1/2}$ cusp → $\ell^{-3/2}$ coefficients → $O(L_{\mathrm{eff}}^{-2})$ tail) and correctly graded as targets.

**P33.** Quarantine compliance: none of the quarantined pre-2026-07-12 numerical residuals (flutter exponents, pump values, $R_M^{\mathrm{eq}}$, $\kappa_{\mathrm{eq}}$, etc.) appears anywhere in the scene; the anti-damping family survives only as scoped qualitative negatives with per-chart scoping, which is exactly what the claims-triage ledger prescribes.

---

## Innovations / suggested new work

**I34. Generalize the no-return certificate into a family-general lemma.** The A2 argument's ingredients — same-polarity radial-sign cancellation, floor-bounded opposite-polarity terms, a speed/weight cap, an energy-integral escape margin — are not A2-specific. A "bounded-weight inverse-square escape lemma" in `braid-mathematics.md`, parameterized by (speed cap, separation floor, weight cap, polarity inventory), would let every family chapter instantiate one result instead of re-deriving channel by channel, and would sharpen where each family's absorber must act.

**I35. Name the cyclic-symmetric B1 locus.** Following C20: the $h/R=1/\sqrt3$ equal-coordinate B1 point is the unique B1 record that inherits the A2 invariant-channel lemma — the B1 analogue of A2's maximal-symmetry role in Family A. Naming it in the B1 catalog (a distinguished locus, not necessarily a new decimal member) gives the B program a symmetry-protected reference fixture and makes the A/B bridge citable.

**I36. Axially alternating polarity placement as the missing axial support.** The axial no-balance derivation kills rigid *polarity-segregated* (two-ring) interior B1. The endpoint-polarity assignment is a declared discrete source coordinate (`braid-analysis-methodology.md` DOF table), so B1 records with alternating axial polarity (positrino-up for some binaries, positrino-down for others) break the two-ring structure the derivation needs — same-"side" contributions are no longer one-signed. That is a concrete, cheap, currently unsampled search stratum for the balanced per-member campaign, and the first question (does any alternating assignment produce an axial equilibrium of the rigid chart?) is answerable with the existing prescribed-record mismatch measure $\mathbf R_i^{\mathrm{path}}$.

**I37. Pursue the layer-parity law.** The topological-charge status list names the reachable target $D^{(a)}\equiv f(k_a)\pmod 2$. The circular self-branch birth sequence from Binary Dynamics ($\cos(\delta_m^*/2)=1/s$ with births at Jacobian nulls, $s_m^*\to\pi m+\pi/2$) already orders fold births along one layer; combining it with the lifted-strip fiber-intersection formula looks like a finishable derivation, and it would be the first *global* constraint tying the charge entries together.

**I38. Wire the resonance-block score into the campaign** (see O19) — lowest-effort, highest-leverage: the amplitudes $A_{k,\ell}$ are extractable from the same prescribed-record root ledgers the methodology already computes, so lock ranking becomes one more reducer over existing case rows.

---

## Mechanical verification performed

- All relative links and heading anchors across the 15 files checked programmatically: 1 failure (E3), all others resolve, including cross-directory targets into foundations, dynamics, spacetime, assemblies, quantum, philosophy-history.
- Disallowed-terminology scan: no instances of the barred causal-delay terms; "force closure" flagged (E6); "native solver" absent; `$\mathbb{A}\mathbb{A}\mathbb{A}$` styling used where the TLA appears.
- Spot-checked derivations: A2 dipole projector identity ($\mathbb I+\varrho+\varrho^2=3\hat{\mathbf n}\hat{\mathbf n}^{\top}$) — correct; momentum-screw projection — correct; FCC center density $1/(4\sqrt2 R^3)$ — correct; rest-level scalings ($R\propto N$, $v\propto N^0$, $f\propto N^{-1}$ fixed-speed; $R\propto N^2$, $v\propto N^{-1}$, $f\propto N^{-3}$ inverse-square) — correct; envelope return-time ratio $\xi\to1/\gamma$ — correct; $F_{\mathrm{raw}}=T_{\mathrm{ret}}\sum_j|q_j|$ normalization — consistent; hinge delay equations $\delta_p=2s\cos(\delta_p/2)$, $\delta_s=2s\sin(\delta_s/2)$ — match Binary Dynamics; fold Fourier exponent $3/2$ and tail budget $L_{\mathrm{eff}}^{-2}$ — correct; covering/lcm structure of the $1{:}2{:}4$ lock — correct; A2-rotating-seed B1 coordinates ($h=R/\sqrt3$, $\rho=R\sqrt{2/3}$) — verified.

Suggested triage order: E1/E2 (front-door correctness), O18 (policy violation), E5 (notation, before external citation), X10 (claim grading), E3/E4/E8/C26 (link hygiene), then the C/O/I items as convergence-campaign fodder.
