# A1.1 Static-Level Kinematic Scaffold and Optional Frequency-Step Ledger

Status: `plan-approved`, `solver-less-static-level-derivation`,
`bounded-root-predeclaration-frozen`,
`prescribed-path-interval-certifier-implemented`,
`root-sheet-36-channel-diagnostic-closure`,
`prescribed-structural-root-ledger-complete`,
`receiver-phase-projection-monotonicity-certified`, `null-score`, and
`priority-only`. This
document does not authorize an EOM solver run or
advance a claim beyond the prescribed A1.1 chart, the symbolic kinematic and
causal-root-topology consequences derived in Sections 5.1 and 5.2, and the
diagnostic capability receipts in Section 5.2.9.

Promotion classification: `priority-only`. The frozen prescribed-path family
now has a complete, null-score diagnostic disposition across all $36$ ordered
channels. That closure does not establish an EOM-retained branch or any
physical candidate, so nothing in this document is ready for promotion into reader-facing
$\mathbb{A}\mathbb{A}\mathbb{A}$ prose.

## 1. Purpose

The primary object is a family of static/discrete A1.1 common-frequency level
records indexed by $n$. The current mathematical stage derives only
within-level kinematics. A dynamical transition between adjacent labels is an
optional later extension. The ledger is intended to keep nine questions
separate:

1. what geometrical and eventual causal-root data define each level record;
2. which relations hold within one level without asserting a transition;
3. how work, rotational action, and angular-momentum exchange would eventually
   be booked in an optional later transition extension without mixing records;
4. how many independently indexed braid records could be coarse-grained without
   inventing continuum quantities;
5. which observer-level general-relativity benchmarks a future effective
   source record would have to recover;
6. whether a resolved population of conditionally high-energy, small Noether
   braids has a convergent collective stress or pressure response;
7. whether A1.1 deformation under a Noether-sea gradient and under imposed
   accelerated translation agrees in the locally matched channel required by
   equivalence-principle recovery;
8. whether the A1.1 Noether-braid route supports exact microscopic scale
   invariance, only a dimensionless family relation, or only an emergent
   macroscopic scaling law; and
9. whether principal-envelope deformation supplies a convergent compact
   response description across resting, accelerated-translation, and
   gravity-side protocols.

Plainly: this is a blueprint for later derivation and measurement. It says
which receipts would be needed and in what order. It does not fill those
receipts with physical results.

## 2. Strict Claim Boundary

The input is the exact [A1.1 prescribed source](configurations/family-a-a1-1-equal-frequency.v1.json),
whose declared grade is `chart-hypothesis`, evidence status is `display-only`,
and geometry is imposed rather than generated by the EOM solver. The
[Family-A owner](../../../content/markdown/aaa/noether-braid/braid-family-a.md#a1-constrained-variants)
defines A1.1 as the zero-axial-offset A1 member with one common frequency while
the three radii and phases remain independently assignable.

This plan makes no claim of:

- a physically realized A1.1 braid;
- EOM solver retention, persistence, settling, binding, or stability;
- an accepted energy, angular momentum, action quantum, or conserved charge;
- an intrinsic loss, radiation, or escaping-energy fraction;
- a derived field-speed pinning mechanism;
- physical superluminal transport, observer-level signal propagation, or an
  EOM-retained trajectory inferred from a prescribed super-field-speed path;
- an accepted many-braid constitutive law;
- recovery of any GR relation;
- a collective pressure or high-energy small-braid phase; or
- equivalence between gravitational and accelerated A1.1 deformation; or
- exact microscopic scale invariance, a universal dimensionless braid family,
  or an emergent/macroscopic scaling law; or
- an ellipsoidal equilibrium law, a relativistic rotating-medium relation, or
  a claim that any ellipsoid solves general relativity.

Plainly: the present record is a drawing with exact paths and provenance. It is
useful for defining a controlled calculation, but it is not evidence that
nature or the EOM solver follows those paths.

The boundary methodology is the
[same-record energy-ledger methodology](same-record-energy-ledger-methodology.md).
It permits a future prescribed-path work-and-drive diagnostic only after the
moving receiver, root, acceleration, environment, and kinetic-convention rows
are complete. It reserves action-derived energy and conservation claims for a
later accepted delayed action and realized branch.

## 3. Current Inputs and Undeclared Assumptions

### 3.1 Known current inputs

| Input | Current source fact | Authority carried into this plan |
| --- | --- | --- |
| Member | `A1.1` | Prescribed Family-A chart only. |
| Geometry endpoint | $\lambda_A=0$ with mutually orthogonal binary axes | Display-coordinate choice, not a retained response path. |
| Binary geometry | $z_{a,\mathrm{half}}=0$ and $\rho_a=R_a$ for $a\in\{1,2,3\}$ | Exact prescribed circular geometry. The plan uses $z_{a,\mathrm{half}}$ for axial half-separation; it maps to the source file's literal `h_a` field. Zero means that the prescribed A1.1 geometry has no axial offset. |
| Common frequency | $f_1=f_2=f_3=0.25$ in the current source | One illustrative source coordinate, not a selected physical cadence. |
| Radii | $(R_1,R_2,R_3)$ independently declared | No numerical radius is selected for this derivation plan. The linked A1.1 source remains the current display input, but its display values do not define a physical scale or preferred geometry here. |
| Phases | $(\phi_1,\phi_2,\phi_3)=(0,0.7,2.2)$ | Independently selected display coordinates. |
| Group motion | Zero group velocity | Initial reference baseline for the first solver-less static-level scaffold only. It is a source-record choice, not a permanent restriction and not coverage of a moving or gravity-side protocol. |
| Return and history window | Return period `4`, record interval $[0,8]$, delay horizon `2` | Prescribed-record sampling contract only. |
| Field-speed convention | Any future numerical instantiation uses $c_f=1$ | Workspace numerical policy; no pinning result follows. |
| Energy boundary | Current prescribed packets are below work-ledger level 3 | The current packet cannot support realized work or total energy. |

Plainly: the table lists what is already fixed by files in the repository. The
numbers describe the existing display source. They are not fitted values,
measured energies, or preferred physical scales.

The symbol $h$ is reserved for Planck action and $\hbar$ for the reduced
angular-momentum unit. This stage uses $\mathbf J_n$ only as the
angular-momentum accounting label of static level $n$. Its origin, projection
axis, subsystem boundary, and sign remain undeclared.

**Locked operator modeling assumption:** for deformation under the planned
translation or gravity-side protocols, the braid's angular-momentum accounting
is provisionally taken to remain quantized in one fixed Planck action unit. The
frame-identification assumption is

$$
h_{\mathrm{abs}}=h_{\mathrm E},
\qquad
\hbar_{\mathrm{abs}}=\hbar_{\mathrm E},
$$

where `abs` labels the project's declared absolute-frame unit and `E` labels
the standard Einstein/local-frame unit. This identification is ledger
normalization only. It is not a derivation of Lorentz invariance, gravitational
equivalence, or action quantization, and each remains a later recovery test.

Plainly: the plan temporarily uses the same Planck action units in the
absolute-frame and local-frame bookkeeping. It has not shown why those units
agree physically.

A fixed-axis level family may carry the locked indexing convention

$$
J_{n+1}^{(\hat{\mathbf u},\mathcal B)}
-
J_n^{(\hat{\mathbf u},\mathcal B)}
=
\hbar,
$$

where $\hat{\mathbf u}$ is the predeclared projection axis and $\mathcal B$ is
the predeclared subsystem. This equation labels adjacent levels by one reduced
Planck action unit under the operator's locked modeling assumption. It is not
a calculation of a transition, transfer, $\Delta\mathbf J$, drive, emission,
or conserved angular momentum. The symbol $h$ remains the full Planck action
unit, while $\hbar$ is the reduced unit used for the angular-momentum level
spacing.

Plainly: neighboring labels are one $\hbar$ apart by assumption. The present
stage does not say that the braid moves between them or releases anything.

The symmetric phase tuple
$(0,120^\circ,240^\circ)$ is the **approved declared baseline for the
solver-less scaffold**, not a fact about the current A1.1 display source. The
current source remains exactly the tabled $(0,0.7,2.2)$ tuple. This document
does not alter or generate a prescribed record with the scaffold baseline.

### 3.2 Declared and remaining assumptions

Section 5 fixes the solver-less scaffold assumptions. Every concrete level
record or later extension must additionally freeze the applicable choices
below before it advances:

1. the static level index $n$ and the separately identified record belonging
   to that label, with no transition meaning inferred;
2. the approved symmetric scaffold phase baseline, with any later level-family
   variant required to declare a different tuple explicitly, and the radii,
   flattening, center, and environment coordinates defining that level;
3. the cyclic plane-basis and circulation convention, and whether
   `nonintersection` means disjoint swept loci, same-time endpoint collision
   avoidance, or later finite-envelope clearance;
4. the working A1.1 specialization in Section 5.2.7: binary layer $a=2$ is the
   exact field-speed-pinned reference with $\alpha_{2,n}=1$, while any other
   pinning scope requires a separately identified baseline;
5. the strict symbolic radius order
   $0<\alpha_{1,n}<1<\alpha_{3,n}$, with no numerical ratio selected, and the
   resulting outer-layer super-field-speed path permitted only as prescribed
   static-level geometry for this baseline;
6. the required same-binary partner, self, and inter-binary root classes,
   finite-history reach, simple-root condition, and behavior at a causal-root
   fold, with every outer-layer root or topology issue handled fail closed;
7. the isolation or Noether-sea embedding record and every environment exchange
   admitted by that choice;
8. the kinetic or rotational-response convention used only for the future work
   diagnostic;
9. the delayed-action identity, regulator, core convention, endpoint
   convention, and finite-memory limit required for an action charge;
10. the within-level cycle alignment and history window needed to define one
   complete static record;
11. the angular-momentum origin, projection axis, subsystem boundary, and sign
    convention;
12. the population, size, action/energy, boundary, and scale-separation
    declarations needed for a collective constitutive test;
13. the matched field-gradient and accelerated-translation protocols, including
    their common baseline, coordinate crosswalk, local matching rule, and
    allowed tidal mismatch;
14. the reference length and time, proposed scaling transformation, scaling
    weights, quantities held fixed, regulator and retained-history scaling,
    dimensionless comparison packet, and allowed scale-breaking inputs;
15. the staged nonzero group-velocity and gravity-side protocols, each with its
    own coordinate crosswalk, clock convention, root-history record,
    environment-exchange boundary, and comparison rule;
16. the level index, projection axis, subsystem, and sign convention for the
    assumed one-$\hbar$ spacing between adjacent labels, plus the later test
    that could recover or reject the locked frame-identification and
    quantization assumptions; and
17. the principal-envelope extraction, volume and strain conventions,
    orientation/rotation separation, stress construction, and comparison
    rules for the later ellipsoidal deformation benchmark.

Plainly: these choices control the meaning of every later number. Fixing them
after seeing a result would turn the ledger into a fitted story, so the plan
requires them to be chosen first.

## 4. Additional Conditional Planning Targets

### 4.1 Scalability test for collective pressure or constitutive response

**Claim grade:** conditional hypothesis and derivation target. The question is
whether a volume containing many extremely small Noether braids with a future
accepted high-energy label develops a collective stress or pressure response.
The words `small` and `high-energy` are not current results. `Small` must be
defined by a retained envelope scale, and `high-energy` remains unavailable
until the single-braid action/energy stages in this plan have been accepted.

The homogeneous isotropic Noether sea at rest is only the reference state. A
future accelerated-translation or inhomogeneous gravity-side protocol is not
permitted to collapse its response to one scalar pressure.

The plan must define these variables before any test:

| Variable | Required definition |
| --- | --- |
| Braid population | Number of admitted Noether braids in the control volume, their retained identities, and the preparation rule. |
| $\rho_{\text{NS}}$ and $n$ | Physical and normalized Noether braid density from the same resolved population record. |
| $R_{\mathrm{env}}$ | Retained characteristic envelope scale of each braid, including its distribution rather than only a mean. |
| $E_{\mathrm{braid}}$ | Same-record action-derived braid energy, unavailable until its action, lower bound, and partition are accepted. |
| Internal-state distribution | Level frequency, phase, orientation, flattening, strain, root topology, and level-label distributions; transition-state distributions remain deferred. |
| $\ell$ and $V_\ell$ | Coarse-graining scale and control volume, with braid envelopes resolved below $\ell$ and population gradients resolved above it. |
| $S_{ij}$ | Coarse-grained Noether sea stress tensor derived from boundary exchange and retained microscopic ledgers. |
| $P_\ell=\operatorname{tr}(S)/3$ | Isotropic pressure part at scale $\ell$, retained as one projection of the full stress rather than the whole response. |
| $S_{ij}^{\mathrm{dev}}=S_{ij}-P_\ell\delta_{ij}$ | Anisotropic or deviatoric stress, retained with its principal directions and shear components. |
| $\partial_kS_{ij}$ | Spatial stress gradients on the declared coarse cell, including gravity-side gradient and boundary contributions. |
| Deformation observables | Principal strains, principal-axis changes, orientation, shear, and volume response from the same resolved population record. |

Plainly: the population count, density, braid size, braid energy, internal
states, averaging scale, stress, and pressure perturbation all need explicit
records. In particular, a high cadence or small drawn radius cannot stand in
for high energy.

The continuum target is a sequence of resolved population records in which
braid count and control volume increase at fixed declared density and
state-distribution law, while $R_{\mathrm{env}}$ remains resolved below
$\ell$ and $\ell$ remains smaller than the macroscopic gradient scale. The
continuum row advances only if density, cadence, strain, stress, boundary
transport, and uncertainty settle as population count, cell size, history
depth, root resolution, and boundary placement are refined.

The planned observable is the full accepted coarse-grained stress tensor
$S_{ij}$ together with $P_\ell$, $S_{ij}^{\mathrm{dev}}$,
$\partial_kS_{ij}$, and the deformation observables. Pressure is only one third
of the trace. Its anisotropic remainder and spatial gradients stay reported
rather than being hidden inside a scalar. Every row must be derived from
retained boundary momentum/action exchange or from a reversible volume/strain
response on the same population record. None may be defined from geometric
overlap, prescribed confinement, wake exposure, or an acceleration norm
alone. This target is owned downstream by the existing
[pressure-dependent Noether sea constitutive response](../master-equation-closure/pressure-dependent-noether-sea-constitutive-response.md).

Required controls:

1. hold density and envelope-size distribution fixed while varying only the
   accepted internal action/energy-state distribution;
2. hold the accepted coarse energy density fixed while varying braid count and
   envelope-size distribution;
3. compare aligned or phase-correlated populations with randomized
   orientation and phase populations under the same preparation rule;
4. compare the interacting population with an explicitly diagnostic sum of
   isolated-braid records that omits cross-braid causal interaction;
5. separate free-boundary, externally confined, and driven preparations, with
   all boundary and drive work retained;
6. compare the resting isotropic reference, accelerated translation, and
   gravity-side gradient records without changing the tensor extraction or
   coarse-graining rule;
7. preserve first and higher gravity-side gradients as tidal response rather
   than absorbing them into the locally uniform acceleration row; and
8. repeat the reduction across cell, time-window, root, history, population,
   and spatial-gradient refinements.

Plainly: these controls ask whether any pressure-like signal comes from
interactions among braids, from their internal state, or merely from how the
test volume was packed and held in place.

Falsifiers:

- the stress tensor, its isotropic/deviatoric split, its spatial gradients, or
  the deformation observables fail to converge under the declared continuum
  and numerical refinements;
- the apparent response is fully explained by the external confiner, imposed
  paths, preparation work, or omitted boundary transport;
- the interacting population is indistinguishable from the isolated-record
  diagnostic within the declared sensitivity;
- a scalar pressure is reported while a material anisotropic stress, shear,
  principal-axis response, volume response, or stress gradient remains;
- the response requires different retained states or separately fitted
  coefficients for pressure, clocks, signal delay, inertia, or metric export;
  or
- a branch change, dissipation channel, or population reclassification is
  hidden inside a supposedly reversible constitutive row.

Prescribed-path packing, wake-exposure norms, normal causal-wake flux,
envelope-overlap counts, same-implementation reductions, and a settling
finite-size trend are diagnostics only. They can identify candidate dependence
on density, size, cadence, orientation, or phase, but they cannot confirm
physical pressure. Physical advancement would require an accepted population
of branches retained by the EOM solver, complete same-record causal and action
ledgers, independently checked stress construction, boundary/drive closure,
and a converged continuum limit.

### 4.2 A1.1 deformation route for equivalence-principle recovery

**Claim grade:** equivalence/recovery target. The planned comparison asks
whether two different substrate triggers perturb the same A1.1 internal ledger
in the locally matched weak-response channel:

- an inhomogeneous Noether sea record whose observer-level projection is an
  effective gravitational field; and
- externally driven accelerated translation through an otherwise matched
  homogeneous Noether sea record.

This does not assume that the two substrate mechanisms are identical. The
recovery target is local agreement after matching the uniform response at the
braid center, while separately retaining the gradient-dependent or tidal
deformation that a spatially uniform accelerated protocol does not reproduce.

Plainly: the comparison asks whether gravity-like loading and ordinary
acceleration reshape the braid in the same local way. It also keeps the
expected difference from field gradients visible rather than forcing every
inhomogeneous effect to match a uniform acceleration.

The two protocols must share:

1. one exact A1.1 baseline source or, later, one retained baseline branch;
2. the same internal phases, polarities, root-history depth, regulator, and
   comparison window;
3. the same braid-center event, local orientation frame, and absolute-time
   alignment, with an explicit crosswalk between the two center histories;
4. one matched Noether sea state at the center except for the declared
   gravity-side gradient and acceleration-side drive;
5. one local matching rule between the uniform part of the effective
   gravity-side response and the center acceleration of the driven record;
6. separately retained first and higher spatial gradients so tidal response is
   not absorbed into the uniform comparison; and
7. complete environment, external-drive, wake, action, and boundary rows for
   both protocols.

Plainly: both tests must start from the same braid state and use the same clock,
orientation, history, and environment conventions. The only intended
difference is the declared source of loading, plus the explicitly preserved
tidal gradient on the gravity side.

Matched observables include:

- binary radii, frequencies, relative phases, axis directions, and flattening;
- full-envelope principal dimensions, strain, orientation, and boundary-leading
  path changes;
- pairwise separations and the Gram matrix of the three binary axes;
- partner/self-root identities, multiplicities, ordering, and fold margins;
- rotational/action-ledger partitions and wake/environment exchange, once
  those ledgers are accepted; and
- later clock cadence, ruler projection, signal delay, and effective-metric
  readouts from the same retained response record.

The coordinate-independent comparison must be built from pairwise distances,
relative axis angles, phase differences, root-topology labels, cycle/action
labels, and envelope principal-axis ratios after common center translation and
rigid frame rotation are removed. Coordinate components may be retained for
debugging, but they are not equivalence evidence by themselves.

The equivalence target compares the matched full local response packet

$$
\mathcal R_{\mathrm{loc}}
=
\left(
S_{ij},
P,
S_{ij}^{\mathrm{dev}},
\partial_kS_{ij},
\text{principal strains},
\text{axis changes},
\text{orientation},
\text{shear},
\text{volume response}
\right),
$$

not pressure alone. The uniform local part is compared at the matched braid
center. Gravity-side spatial variation remains a separate tidal response and
is not required to equal the uniform accelerated-translation row.

Plainly: equivalence is tested against the complete local pattern of stress
and deformation. Matching one pressure number is insufficient, and the extra
shape change caused by a field gradient stays visible as a separate tidal
quantity.

Required controls:

1. an unaccelerated A1.1 baseline in a homogeneous Noether sea;
2. a matched uniform gravity-side response and accelerated-translation pair;
3. an inhomogeneous gravity-side record with the center response held fixed,
   exposing the tidal residual;
4. reversal of the gradient and acceleration directions in the same frame;
5. orientation and phase variants fixed before evaluation; and
6. at least one different admitted assembly composition later in the program,
   because equivalence recovery cannot be established from A1.1 alone.

Falsifiers and failure modes:

- locally matched protocols produce different invariant internal deformation
  or different full local response tensors beyond the declared residual after
  drive and environment exchange are closed;
- apparent agreement disappears when coordinate translation or frame rotation
  is removed;
- the comparison demands that tidal deformation match uniform acceleration
  rather than reporting it as the allowed gradient-dependent residual;
- pressure agrees while deviatoric stress, stress gradients, principal
  strains, axis changes, orientation, shear, or volume response disagree;
- the effective gravitational field is inserted independently of the Noether
  sea record that supplies the braid response;
- the accelerated record omits its external drive or uses a different retained
  history, phase, regulator, or environment;
- matching requires binary-index reassignment, hidden parameter retuning, or
  apparatus-specific response coefficients; or
- different admitted assembly or clock designs produce incompatible local
  response maps.

Prescribed-path deformation, matched envelope pictures, coordinate-component
agreement, or parity between reducers are diagnostic only. They can test
whether the comparison protocol is well posed. They cannot establish the
equivalence principle. Advancement requires realized retained branches, an
independent tensor/deformation comparison, and the same response record
feeding the
[equivalence-principle channels](../../../content/markdown/aaa/spacetime/general-relativity.md#equivalence-principle-channels),
[clock and ruler validation](../../../content/markdown/aaa/spacetime/proper-time-and-time-dilation.md),
signal propagation, PPN rows, and the
[effective-metric map](../../../content/markdown/aaa/spacetime/emergent-metric.md#admcartan-reconstruction-surface)
without private retuning.

### 4.3 Scale-invariance target and constraint

**Claim grade:** conditional derivation target and constraint. Scale invariance
is not established for A1.1. A future review must keep three possible meanings
separate:

| Target | Meaning to test | Claim boundary |
| --- | --- | --- |
| Exact microscopic scale invariance | The full causal-root, acceleration, delayed-action, regulator, endpoint, external-work, and retained-history problem maps between scaled records without adding or retuning a dimensional scale. | The scale-invariant form of one kernel or one prescribed chart does not establish this full-system symmetry. |
| Dimensionless family relation | A declared set of dimensionless A1.1 geometry, cadence, root, and ledger observables agrees across a family of differently sized records. | Such agreement may classify a chart family even when a regulator, action normalization, or environment breaks exact microscopic scale invariance. |
| Emergent/macroscopic scaling law | A coarse-grained population observable transforms with a stable scaling weight over a declared continuum regime. | A limited-regime continuum law does not imply a microscopic symmetry or a universal law outside that regime. |

Plainly: the strongest target says the complete microscopic problem has no
hidden preferred size. The middle target says differently sized braids share
the same ratios. The weakest target says a large population follows a regular
size law over a stated range. Evidence for one target cannot be promoted into
either of the stronger targets.

A future predeclaration must choose a reference length $L_\star$ and the
corresponding reference time $T_\star=L_\star/c_f$. It must nondimensionalize
at least:

- positions, binary radii, axial half-separations, envelope dimensions,
  separations, cell sizes, and spatial cutoffs by $L_\star$;
- absolute-time intervals, root times, cadence periods, comparison windows,
  history horizons, and retained-history steps by $T_\star$;
- frequencies by $T_\star^{-1}$, velocities by $c_f$, and accelerations by
  $c_f^2/L_\star$;
- braid number density through a dimensionless occupancy such as
  $\rho_{\text{NS}}L_\star^3$, while keeping physical density and occupancy
  distinct;
- root brackets, core widths, smoothing widths, interpolation intervals,
  tolerance floors, and boundary distances by their matching length or time
  reference; and
- work, energy, action, stress, and pressure only after each has an
  independently declared reference normalization and scaling weight; and
- angular momentum by the locked provisional $\hbar$ unit while leaving its
  scaling weight and physical recovery as explicit test obligations.

Plainly: every dimensional entry must be converted to a ratio before two
scales are compared. The plan leaves the work, action, stress, and pressure
normalizations open. It uses the locked Planck-unit identification above only
for provisional angular-momentum bookkeeping; the scaling weight and physical
recovery of that unit remain open. No primitive architrino mass or additional
observer-level constant may be inserted to fill the other blanks.

For a positive scale factor $\lambda$, the proposed microscopic comparison is

$$
\mathbf X\mapsto\lambda\mathbf X,
\qquad
T\mapsto\lambda T,
\qquad
c_f\mapsto c_f,
$$

with the source center, radii, envelope lengths, causal-history extent,
sampling intervals, and other dimensional protocol lengths or times co-scaled
as declared. The corresponding target transformations are

$$
R_a\mapsto\lambda R_a,
\qquad
f_a\mapsto\lambda^{-1}f_a,
\qquad
\mathbf V_a\mapsto\mathbf V_a,
\qquad
\mathbf A_a\mapsto\lambda^{-1}\mathbf A_a.
$$

These equations define the proposed comparison only. They are not a derivation
that the canonical dynamics, delayed action, external ledger, or retained
branch obeys it. Any scaling weight for work, energy, action, $\mathbf J_n$, a
later $\Delta\mathbf J$, stress, or pressure must be derived from the accepted
ledger rather than assigned here.

Plainly: the proposed comparison enlarges every length and time by the same
factor, so the field-speed convention stays fixed. It also states how a future
test should relabel cadence, velocity, and acceleration. Whether the complete
record actually follows that relabeling remains the question.

Candidate invariant or covariant observables are:

- the field-speed ratio $2\pi f_aR_a/c_f$, radius ratios, envelope aspect
  ratios, relative phases, relative axis angles, and the axis Gram matrix;
- dimensionless causal-root times, root multiplicity and ordering, fold
  margins, and the transmitter- and receiver-side speed ratios;
- dimensionless prescribed-path residuals and later dimensionless
  action/work/angular-momentum closure residuals;
- work, action, $\mathbf J_n$, a later $\Delta\mathbf J$, stress, and pressure
  transformed covariantly under scaling weights derived from their accepted
  constructions, rather than presumed invariant; and
- for the many-braid route, dimensionless occupancy, envelope-to-cell and
  cell-to-gradient ratios, anisotropy ratios, and normalized constitutive
  observables from one retained population record.

The comparison must predeclare which observables are expected to remain
invariant, which are expected to transform covariantly, and which are allowed
to change. A topology label can remain invariant only away from a declared
root fold or branch change.

Potential scale-breaking inputs that must be varied or held visible include:

- the primitive field speed $c_f$: it is compatible with the proposed joint
  length/time scaling, but it would obstruct an independently chosen length or
  time scaling;
- the external-work, kinetic-response, energy, and action normalizations, plus
  the locked but unproved absolute/local Planck-unit identification used in the
  angular-momentum ledger;
- a core size, mollifier, root cutoff, collision clearance, boundary distance,
  or other fixed length;
- a fixed braid density, inter-braid spacing, control-volume boundary,
  Noether-sea correlation length, or imposed gradient scale;
- a finite history horizon, retained-history discretization, sampling
  interval, time step, interpolation rule, root bracket, or absolute tolerance
  that is not co-scaled;
- a finite transition window, cycle count, preparation cadence, external-drive
  profile, or prescribed record interval; and
- later clock, ruler, signal, or effective-metric calibration scales.

Plainly: a clean curve collapse would be uninformative if the cutoff, density,
history grid, drive normalization, or observer calibration quietly selected
the scale. Those inputs must remain in the record so a real failure is not
hidden as a numerical or preparation choice.

Connections to the other planned routes are mandatory:

1. **Static-level family.** Test the same within-level dimensionless packet
   across more than one declared $L_\star$, co-scaling the radii and histories,
   and compare the field-speed ratio while preserving the explicit
   middle-layer working pin as an assumption rather than a recovered result,
   without introducing an adjacent-level transition.
2. **Many-braid pressure and scalability.** Separate a change in braid size at
   fixed dimensionless occupancy from a change in physical density, and derive
   any stress or pressure scaling weight only after the microscopic exchange
   and continuum limits close.
3. **Effective gravity and equivalence.** Match the gravity-gradient and
   accelerated-translation protocols at the same dimensionless braid size,
   gradient scale, acceleration, history, and environment ratios. Later clock,
   ruler, signal, and effective-metric readouts must use the same scaled record
   and may not introduce a private calibration for either protocol.

Falsifiers and failure modes:

- scaled records fail to agree in the predeclared dimensionless path,
  causal-root, or ledger observables after regulator, history, root, and
  sampling refinement;
- agreement requires retuning a dimensionless geometry coordinate, polarity,
  root policy, action convention, or environment response;
- root topology or retained branch identity changes outside a predeclared fold
  or bifurcation;
- an apparent family collapse disappears when cutoff, boundary, history,
  density, or discretization effects are separated;
- a macroscopic scaling weight depends materially on control-cell size,
  population preparation, history window, or density path in the declared
  continuum regime;
- the static-level map, pressure response, or matched equivalence protocols
  require mutually incompatible scaling weights; or
- evidence for a dimensionless family relation or a limited macroscopic law is
  reported as exact microscopic scale invariance.

Prescribed scaled charts, curve collapse, same-implementation parity, and
finite-resolution covariance are diagnostic only. They can reject a proposed
scaling packet or identify a candidate relation. They cannot confirm physical
realization, retention, microscopic symmetry, a collective constitutive law,
or observer-level scale recovery.

## 5. Common-Frequency Static Level Family

Let $n$ index a family of separately identified static/discrete level records,
and let $a\in\{1,2,3\}$ label the persistent A1.1 binaries. Within level $n$,

$$
\omega_{a,n}=\omega_n
$$

for every $a$. Each level may carry its own symbolic $\omega_n$,
$R_{\star,n}$, and radius ratios $\alpha_{a,n}$. No equation relating
$\omega_n$ to $\omega_{n+1}$, or any other data between adjacent labels, is
assumed or derived in the current stage.

Within Section 5, $n$ without spatial arguments is the discrete level index.
The field $n(\mathbf X,T)$ elsewhere in the document remains normalized
Noether braid density; the two meanings are not interchangeable.

Plainly: each level is its own common-cadence record. Ordering two records by
$n$ does not describe how one changes into the other.

The field-speed/radius relation is a within-level target only. For an exactly
circular level-$n$ path,

$$
v_{a,n}=\omega_nR_{a,n},
\qquad
v_{a,n}\stackrel{?}{=}c_f
$$

only for a binary layer explicitly declared as pinned within that level. The
question mark is part of the claim boundary: the scaffold does not assert the
equality or a mechanism that maintains it.

A common frequency and all-channel pinning would constrain all pinned radii to
the same locus. That equal-radius direction is not the active target in this
packet: A1.1 is prioritized over A1.2, and the working specialization retains
three unequal radii. Section 5.2.7 fixes the planning baseline to selected
middle-layer pinning,

$$
p=2,
\qquad
\alpha_{2,n}=1,
\qquad
\beta_{2,n}=1.
$$

This assigns binary index $2$ the reference role only inside the reviewed
planning baseline. It is not a derived permanent physical role. Exact
field-speed occupancy also does not by itself establish an active retained
self-hit branch, because the history reach, root inventory, and fold
regularity remain undeclared.

For this equal-common-frequency A1.1 baseline, the operator also permits the
outer layer to occupy the algebraically required super-field-speed part of the
prescribed static geometry. This allowance keeps that symbolic geometry inside
the test family; it is not evidence of physical superluminal transport, an
EOM-retained branch, stability, or physical realization.

Plainly: this packet now studies unequal-radius A1.1 with its middle layer used
as the field-speed ruler and does not reject the prescribed outer circle merely
because its path speed exceeds $c_f$. It does not switch to the equal-radius
A1.2 program or claim that nature selects or realizes any layer.

Every level declaration must state its dimensionless form. Agreement of the
field-speed ratio or another dimensionless cadence-radius observable across
levels would be a family diagnostic only until the full microscopic
scale-invariance obligation in Section 4.3 is met.

### 5.1 Dimensionless A1.1 kinematic scaffold

This is the first authorized solver-less derivation. It uses only prescribed
circular geometry and dimensional consistency. It does not use the causal
acceleration law, solve a root equation, or calculate a transition, work,
$\Delta\mathbf J$, $\Delta E$, radiation, pressure, or a GR relation.

Assumptions:

1. the persistent binary labels are $a\in\{1,2,3\}$;
2. each binary follows an exactly circular prescribed path with
   $z_{a,\mathrm{half}}=0$ in its declared plane;
3. within level $n$, the symbolic radii are positive and parameterized by
   $R_{a,n}=\alpha_{a,n}R_{\star,n}$, with $R_{\star,n}>0$ and
   $\alpha_{a,n}>0$;
4. all three binaries at level $n$ share one positive angular frequency
   $\omega_n$;
5. the scaffold adopts
   $(\phi_1^{(0)},\phi_2^{(0)},\phi_3^{(0)})
   =(0,2\pi/3,4\pi/3)$ as its declared symmetric phase baseline only, not as a
   statement about the unchanged display-source phases or a selected physical
   state;
6. group velocity is zero for this first reference scaffold, with no
   translation-side or gravity-side deformation included;
7. $c_f$ remains symbolic, and Section 5.2.7 selects layer $p=2$ as the exact
   pinned reference only for the working planning baseline; and
8. the locked one-$\hbar$ adjacent-label spacing is an indexing assumption,
   not a transition law; and
9. no root topology, action, work, angular-momentum transfer, energy,
   radiation, stability, retention, or physical-realization property is
   inferred from the kinematics.

The common phase and speed rows are

$$
\phi_{a,n}(T)=\omega_n T+\phi_a^{(0)},
\qquad
v_{a,n}=\omega_n R_{a,n}.
$$

Substituting the symbolic radius family gives

$$
v_{a,n}
=
\omega_n\alpha_{a,n}R_{\star,n}.
$$

Define the dimensionless reference-speed ratio and binary speed ratios by

$$
\beta_{\star,n}
=
\frac{\omega_n R_{\star,n}}{c_f},
\qquad
\beta_{a,n}
=
\frac{v_{a,n}}{c_f}.
$$

The dimensionless kinematic family is therefore

$$
\beta_{a,n}=\alpha_{a,n}\beta_{\star,n},
\qquad
\frac{R_{a,n}}{R_{b,n}}
=
\frac{\alpha_{a,n}}{\alpha_{b,n}}.
$$

Plainly: one overall radius and one common cadence set the overall speed scale.
The three $\alpha_{a,n}$ values retain all information about the relative radii.
Nothing in these equations chooses those ratios.

For an explicitly predeclared pinned layer $p$, the conditional field-speed
row is

$$
\beta_{p,n}=1
\quad\Longrightarrow\quad
\alpha_{p,n}\beta_{\star,n}=1
\quad\Longrightarrow\quad
R_{\star,n}
=
\frac{c_f}{\omega_n\alpha_{p,n}}.
$$

If and only if the pinning convention normalizes the pinned layer by
$\alpha_{p,n}=1$, this reduces to

$$
R_{\star,n}=\frac{c_f}{\omega_n}.
$$

This fixes a conditional overall reference scale. It does not derive the
pinning mechanism, select $p$, or determine the other radius ratios.

For any positive scale factor $\lambda$, define the scaffold transformation

$$
\mathcal S_\lambda:
\quad
R_{\star,n}\mapsto\lambda R_{\star,n},
\quad
R_{a,n}\mapsto\lambda R_{a,n},
\quad
T\mapsto\lambda T,
\quad
\omega_n\mapsto\lambda^{-1}\omega_n,
\quad
c_f\mapsto c_f.
$$

Under $\mathcal S_\lambda$, the products $\omega_n T$ and
$\omega_n R_{a,n}/c_f$ are preserved. Therefore the following quantities remain
dimensionless invariants of this kinematic scaffold:

- $\alpha_{a,n}=R_{a,n}/R_{\star,n}$ and every within-level radius
  ratio $R_{a,n}/R_{b,n}$;
- $\beta_{\star,n}=\omega_n R_{\star,n}/c_f$ and
  $\beta_{a,n}=\omega_n R_{a,n}/c_f$;
- the declared phase differences
  $\phi_a^{(0)}-\phi_b^{(0)}$;
- the zero axial ratio $z_{a,\mathrm{half}}/R_{\star,n}=0$; and
- the relative binary-axis angles and axis Gram matrix.

The prescribed positions scale covariantly with $\lambda$, while the path
speeds $v_{a,n}$ remain unchanged. This establishes only a dimensionless family
relation for the prescribed kinematics. It does not establish exact
microscopic scale invariance of the causal-root, regulator, history, action,
or environment problem.

**Non-uniqueness result.** For every positive triple
$(\alpha_{1,n},\alpha_{2,n},\alpha_{3,n})$ and every positive pair
$(R_{\star,n},\omega_n)$, the equations above define one common-frequency
circular level record. Common frequency supplies no equation among the
$\alpha_{a,n}$. One declared field-speed pin supplies the single condition
$\alpha_{p,n}\beta_{\star,n}=1$; after the normalization
$\alpha_{p,n}=1$ fixes the overall scale, the two remaining independent radius
ratios are still free. The symmetric phase baseline constrains phase
differences only. Therefore the kinematic scaffold cannot select the
$\alpha_{a,n}$ ratios within any level, and it supplies no law relating the
ratio families of different levels.

Plainly: infinitely many symbolic radius families satisfy the current
kinematics. A unique radius claim must come from additional independently
declared geometry, causal-root, action, or ledger constraints.

#### Declarations used by the within-level admissibility stage

The next solver-less stage remains within one level. It uses the following
declaration packet and does not infer any relation between level labels:

| Within-level row | Required declaration |
| --- | --- |
| Identity | Level index $n$, exact source identity, prescribed geometry grade, and evidence status. |
| Geometry | $R_{\star,n}$, the symbolic positive ratio tuple $(\alpha_{1,n},\alpha_{2,n},\alpha_{3,n})$, binary planes and axes, zero axial half-separation, and the selected phase baseline. |
| Cadence | Common $\omega_n$, absolute-time convention, comparison cycle, and the statement that no relation to $\omega_{n+1}$ is being used. |
| Conditional pinning | Working specialization $p=2$, $\alpha_{2,n}=1$, and $\beta_{2,n}=1$ as an exact planning assumption; any alternative requires a separately identified baseline, and pinning remains underived. |
| Dimensionless packet | $\alpha_{a,n}$, $\beta_{\star,n}$, $\beta_{a,n}$, radius ratios, phase differences, and axis Gram matrix. |
| Level-unit label | Projection axis $\hat{\mathbf u}$, subsystem $\mathcal B$, sign convention, and the locked one-$\hbar$ adjacent-label spacing stated only as an indexing assumption. |
| Advancement boundary | No transition profile, $\Delta\mathbf J$, $\Delta E$, work, drive, radiation, emission, numerical root solution, complete retained six-body root inventory, stability, retention, or conservation claim. |

Section 5.2 answers the authorized question: how far symmetry,
nonintersection, field-speed, and solver-less causal-root-topology conditions
reduce the positive ratio family
$(\alpha_{1,n},\alpha_{2,n},\alpha_{3,n})$ without data from another level or an
observer-level fit.

**Optional later extension:** only after separate authorization may the
document add adjacent-level transition profiles, external-work inputs,
angular-momentum transfer $\Delta\mathbf J$, energy change $\Delta E$, drive,
radiation, or emission ledgers. The one-$\hbar$ level spacing does not
authorize or supply any of those calculations.

Plainly: the next mathematical object is a better-defined static level, not a
motion from one level to another.

### 5.2 Solver-less within-level admissibility map

This subsection derives necessary conditions, sufficient conditions, and
unresolved conditions for one static level. It does not evaluate the Master
Equation acceleration, select a retained root branch, or establish physical
admissibility. Here `admissibility` means only consistency with the declared
prescribed geometry, kinematics, finite-history root equations, and
simple-root chart boundary.

#### 5.2.1 Coordinate form of the prescribed circles

Let $\Theta_n=\omega_nT$. Choose the right-handed cyclic plane bases

$$
\begin{aligned}
\mathbf q_1(\theta)&=(0,\cos\theta,\sin\theta),\\
\mathbf q_2(\theta)&=(\sin\theta,0,\cos\theta),\\
\mathbf q_3(\theta)&=(\cos\theta,\sin\theta,0).
\end{aligned}
$$

Their plane normals are respectively the prescribed $x$, $y$, and $z$ axes.
For endpoint sign $\sigma\in\{+1,-1\}$, the zero-group-velocity scaffold is

$$
\mathbf X_{a,\sigma,n}(T)
=
\sigma R_{\star,n}\alpha_{a,n}
\mathbf q_a\!\left(\Theta_n+\phi_a^{(0)}\right).
$$

The two endpoints in one binary are antipodal at every common time, and their
instantaneous separation is

$$
\left\|
\mathbf X_{a,+,n}(T)-\mathbf X_{a,-,n}(T)
\right\|
=2R_{\star,n}\alpha_{a,n}>0.
$$

Plainly: this is a coordinate realization of the already declared yz, xz,
and xy circles. The cyclic basis fixes what the proposed phase baseline means
for this scaffold; it does not modify the display-source record or prove that
the EOM solver retains these paths.

#### 5.2.2 Symmetry constraints

The orthogonal-axis Gram matrix, common center, common frequency, and equal
$120^\circ$ phase spacing are already fixed in the scaffold. None of those
conditions equates the radii. The following symmetry statements have different
strength:

| Symmetry statement | Radius condition | Status |
| --- | --- | --- |
| Orthogonal-axis and common-cadence scaffold | $\alpha_{a,n}>0$ independently | Positivity is necessary for three nondegenerate circular paths; these scaffold symmetries impose no radius equality. |
| Pairwise exchange of two circular loci $a$ and $b$ by a distance-preserving rotation | $\alpha_{a,n}=\alpha_{b,n}$ | Necessary for the two loci to be geometrically exchangeable; not sufficient for exchange of endpoint polarity, circulation, phase history, or provenance. |
| Cyclic exchange of all three unlabeled circular loci | $\alpha_{1,n}=\alpha_{2,n}=\alpha_{3,n}$ | Necessary and sufficient for the swept-locus geometry under the declared cyclic spatial rotation; not a full-record binary-permutation symmetry. |
| Full time-parametrized record symmetry | Equal radii plus compatible phase, circulation, polarity, endpoint identity, and history rows | Unresolved and not required by A1.1. |

Plainly: the symmetric phase baseline is a phase choice, not a radius equation.
Equal radii create a stronger symmetry sublocus, but they do not by themselves
make the labeled six-worldline record symmetric.

#### 5.2.3 Nonintersection and collision constraints

For two different prescribed circle loci $a\ne b$ in orthogonal planes, the
minimum and maximum distances between arbitrary points on the two loci are

$$
d_{ab}^{\min}
=
\left|R_{a,n}-R_{b,n}\right|,
\qquad
d_{ab}^{\max}
=
R_{a,n}+R_{b,n}.
$$

Therefore the two complete swept loci intersect if and only if

$$
R_{a,n}=R_{b,n}
\quad\Longleftrightarrow\quad
\alpha_{a,n}=\alpha_{b,n}.
$$

Pairwise distinct positive ratios are thus necessary and sufficient for
pairwise-disjoint swept circular loci:

$$
\alpha_{1,n}\ne\alpha_{2,n},
\qquad
\alpha_{1,n}\ne\alpha_{3,n},
\qquad
\alpha_{2,n}\ne\alpha_{3,n}.
$$

Plainly: unequal radii keep the three entire circular tracks apart. Equal
radii make two tracks cross at two points on their common coordinate axis,
but a track crossing is not yet a collision between architrinos.

Because the two endpoints of each binary occupy opposite points, their
same-time occupancy angle is defined modulo $\pi$. In the declared cyclic
bases, a same-time collision between equal-radius circles would require one of

$$
\begin{aligned}
\phi_1^{(0)}-\phi_2^{(0)}&\equiv\frac{\pi}{2}\pmod{\pi},\\
\phi_2^{(0)}-\phi_3^{(0)}&\equiv\frac{\pi}{2}\pmod{\pi},\\
\phi_3^{(0)}-\phi_1^{(0)}&\equiv\frac{\pi}{2}\pmod{\pi}.
\end{aligned}
$$

For the approved baseline
$(0,2\pi/3,4\pi/3)$, each left-hand side relevant to the cyclic pair is
congruent to $\pi/3$ modulo $\pi$, not $\pi/2$. Hence the ideal six-point
scaffold has no same-time inter-binary endpoint collision for any positive
radius triple, including the equal-radius sublocus.

This is a sufficient collision-avoidance result only for the declared cyclic
plane bases, common circulation convention implicit in the phase formula, and
exact baseline phases. It is not an exclusion-envelope separation result, a
finite-core result, or a retained-history result.

Plainly: the approved phase offsets make the points miss one another in time
even when their tracks cross in space. A later finite-envelope or causal-wake
test can still reject that geometry.

#### 5.2.4 Field-speed bands and circular same-binary roots

The field-speed classification follows directly from
$\beta_{a,n}=\alpha_{a,n}\beta_{\star,n}$:

$$
\begin{aligned}
\alpha_{a,n}\beta_{\star,n}<1
&\quad\Longleftrightarrow\quad v_{a,n}<c_f,\\
\alpha_{a,n}\beta_{\star,n}=1
&\quad\Longleftrightarrow\quad v_{a,n}=c_f,\\
\alpha_{a,n}\beta_{\star,n}>1
&\quad\Longleftrightarrow\quad v_{a,n}>c_f.
\end{aligned}
$$

If a declared pinned layer $p$ is normalized by $\alpha_{p,n}=1$, then
$\beta_{\star,n}=1$ and every other $\alpha_{a,n}$ is also its speed ratio.
Consequently:

- a protocol requiring every path to be at or below field speed requires
  $\alpha_{a,n}\le1$ for all $a$;
- a protocol requiring only $p$ to be pinned and every other path strictly
  below field speed requires $\alpha_{a,n}<1$ for $a\ne p$; and
- a non-pinned path with $\alpha_{a,n}>1$ is super-field-speed under this
  normalization and must carry the corresponding causal-root-history burden.

Plainly: after one pinned radius is chosen as the reference radius, the two
remaining radius ratios say directly whether those paths are slower or faster
than the field speed. These inequalities classify a prescribed path; they do
not supply a pinning mechanism.

Let $H_n>0$ be a separately declared retained-history horizon and define

$$
\delta_{H,n}=\omega_nH_n,
\qquad
\chi_n=\frac{\delta_{H,n}}{\beta_{\star,n}}
=\frac{c_fH_n}{R_{\star,n}}.
$$

The dimensionless history reach $\chi_n$ is preserved by
$\mathcal S_\lambda$ only when the history horizon co-scales as
$H_n\mapsto\lambda H_n$.

For one circular worldline, write
$\xi=\omega_n(T_r-T_t)/2>0$. A nontrivial self root must satisfy

$$
\left|\sin\xi\right|
=
\frac{\xi}{\beta_{a,n}},
\qquad
0<\xi,
\qquad
2\xi\le\delta_{H,n}.
$$

The consequences are:

| Condition | Solver-less consequence | Strength |
| --- | --- | --- |
| $\beta_{a,n}\le1$ | No nontrivial circular self root exists. At equality the only onset is the excluded coincident limit $\xi=0$. | Necessary and sufficient for absence on the exact circular scaffold. |
| $\beta_{a,n}>1$ | The full circular past has a principal nontrivial geometric self root. | Necessary and sufficient for geometric existence on the full exact circular past; not sufficient for retention inside $H_n$. |
| $2\xi\le\delta_{H,n}$ | A particular geometric root lies inside the declared history horizon. | Necessary for that retained root. |
| $D_t\ne0$ at the root | The root lies on a simple-root chart. | Necessary for ordinary branch use; not a retention or acceleration result. |

Plainly: exact field-speed pinning is the boundary of circular self-root onset,
not an active simple self-hit. A super-field-speed circular path has a
geometric self-root candidate only if the relevant past interval is retained,
and no acceleration or stability conclusion follows.

Higher circular self-root pairs can be born only on fold boundaries satisfying

$$
\tan\xi^\star=\xi^\star,
\qquad
\beta_{a,n}^\star
=
\left|\sec\xi^\star\right|.
$$

At those boundaries $D_t=0$, so they are excluded from the ordinary
simple-root region and require a separately declared fold treatment. The
number of retained self roots is unresolved until $\beta_{a,n}$, $H_n$, the
excluded-coincidence convention, and the fold policy are fixed.

For the opposite endpoint in the same binary, the circular partner equation is

$$
\left|\cos\xi\right|
=
\frac{\xi}{\beta_{a,n}},
\qquad
0<\xi,
\qquad
2\xi\le\delta_{H,n}.
$$

For every $\beta_{a,n}>0$, its principal positive-cosine sheet has one unique
geometric root before $\xi=\pi/2$, and that principal root is transverse. It is
retained only when the declared history horizon reaches its delay. Older
partner sheets and their complete root count remain unresolved here.

Plainly: each antipodal binary has a delayed partner-root candidate even
though its endpoints never collide at the same time. Same-time separation and
causal contact answer different questions.

#### 5.2.5 Inter-binary causal-root bounds

For reception by endpoint $(a,\sigma)$ at phase $\Theta_n$ and emission by
endpoint $(b,\tau)$ at delay
$\delta=\omega_n(T_r-T_t)>0$, define the dimensionless separation

$$
\begin{aligned}
\mathcal D_{ab}^{\sigma\tau}
(\Theta_n,\delta)^2
={}&
\alpha_{a,n}^2+\alpha_{b,n}^2\\
&-2\sigma\tau\alpha_{a,n}\alpha_{b,n}
\mathbf q_a\!\left(\Theta_n+\phi_a^{(0)}\right)
\mathbin{\cdot}
\mathbf q_b\!\left(\Theta_n+\phi_b^{(0)}-\delta\right).
\end{aligned}
$$

The exact solver-less inter-binary causal-root equation is

$$
F_{ab}^{\sigma\tau}(\Theta_n,\delta)
\equiv
\mathcal D_{ab}^{\sigma\tau}(\Theta_n,\delta)
-\frac{\delta}{\beta_{\star,n}}
=0,
\qquad
0<\delta\le\delta_{H,n}.
$$

Every such root necessarily obeys

$$
\beta_{\star,n}
\left|\alpha_{a,n}-\alpha_{b,n}\right|
\le
\delta
\le
\beta_{\star,n}
\left(\alpha_{a,n}+\alpha_{b,n}\right).
$$

Plainly: the delay cannot be shorter than the minimum distance between the two
tracks or longer than their maximum distance after both are converted into
field-travel time. This bounds possible roots without finding any root.

The distance bounds give two useful one-sided certificates:

| History condition | Consequence | Strength |
| --- | --- | --- |
| $\chi_n<\lvert\alpha_{a,n}-\alpha_{b,n}\rvert$ | No inter-binary root can occur inside the retained horizon for that circle pair. | Sufficient for root absence; not necessary. |
| $\chi_n\ge\alpha_{a,n}+\alpha_{b,n}$ and the reception-time endpoints are noncoincident | At least one inter-binary root exists for every reception phase and endpoint-sign pair by continuity. | Sufficient for existence; not necessary and not a root-count certificate. |
| $\lvert\alpha_{a,n}-\alpha_{b,n}\rvert\le\chi_n<\alpha_{a,n}+\alpha_{b,n}$ | Existence and count depend on phase, endpoint signs, orientation, and history. | Unresolved by distance bounds. |

Plainly: a short history can be certified too short for the wake to bridge two
unequal tracks, while a sufficiently long history must contain at least one
bridge. The intermediate range needs an actual complete root analysis.

A simple inter-binary root additionally requires

$$
\partial_\delta
F_{ab}^{\sigma\tau}(\Theta_n,\delta)\ne0.
$$

A simultaneous equality
$F_{ab}^{\sigma\tau}=0$ and
$\partial_\delta F_{ab}^{\sigma\tau}=0$ is a fold boundary, not an ordinary
root. No symbolic radius ratio may be accepted by silently deleting such a
boundary. This subsection does not enumerate the roots or decide a fold
regularization.

#### 5.2.6 Constraint intersection and remaining radius freedom

Define the symbolic within-level map

$$
\mathcal A_n^{\mathrm{symbolic}}
=
\mathcal A_n^{+}
\cap
\mathcal A_n^{\mathrm{sym}}
\cap
\mathcal A_n^{\mathrm{sep}}
\cap
\mathcal A_n^{\mathrm{speed}}
\cap
\mathcal A_n^{\mathrm{root}},
$$

where:

- $\mathcal A_n^{+}$ requires positive radii;
- $\mathcal A_n^{\mathrm{sym}}$ records either the generic A1.1 symmetry rows or
  an explicitly requested equal-radius geometric sublocus;
- $\mathcal A_n^{\mathrm{sep}}$ states whether the requirement is disjoint
  swept loci, same-time point-collision avoidance, or a later finite-envelope
  clearance;
- $\mathcal A_n^{\mathrm{speed}}$ records the declared sub-, exact-, or
  super-field-speed bands and any predeclared pinned index; and
- $\mathcal A_n^{\mathrm{root}}$ records the finite history reach, required
  self/partner/inter-binary root classes, and excluded fold boundaries.

This map is a classification of symbolic prescribed records. Membership does
not mean that the Master Equation generates or retains a physical braid.

Plainly: the map intersects only conditions that have actually been declared.
It prevents a favorable meaning of symmetry, separation, speed, or roots from
being chosen after inspecting a candidate.

With one selected pinned layer normalized by $\alpha_{p,n}=1$, the pin also
sets $\beta_{\star,n}=1$. The radius family then has two independent positive
ratios. The derived conditions reduce them as follows:

| Declared constraint set | Surviving ratio freedom |
| --- | --- |
| Approved phase baseline and same-time point-collision avoidance | Two continuous ratio degrees of freedom remain. |
| Pairwise-disjoint swept loci | Two continuous degrees remain, with the equality lines removed. |
| Pinned layer plus all other paths strictly below field speed and pairwise-disjoint swept loci | Two continuous degrees remain in $0<\alpha_{a,n}<1$ for $a\ne p$, with $\alpha_{a,n}\ne\alpha_{b,n}$. |
| Exact equal-radius geometric cyclic locus | No ratio degree of freedom remains after pinned normalization: all $\alpha_{a,n}=1$. This locus fails the disjoint-swept-locus requirement but passes the declared same-time point-collision test. |
| All three paths pinned under one common frequency | No ratio degree of freedom remains: all $\alpha_{a,n}=1$. This is a special sublocus, not generic A1.1. |
| Root-existence, root-absence, and simple-root inequalities without fixed counts | The ratio plane is partitioned into regions and fold boundaries; no unique point is selected. |

Plainly: the generic selected-channel program still has two free radius ratios.
The new constraints carve out regions and boundaries but do not choose one
radius family. Zero ratio freedom occurs only after imposing equal radii or
all-channel pinning, and the equal-radius choice conflicts with strict
disjointness of the swept tracks.

**Solver-less non-uniqueness conclusion.** Symmetry of the approved phase
baseline, same-time collision avoidance, field-speed classification, and the
derived causal-distance bounds do not select
$(\alpha_{1,n},\alpha_{2,n},\alpha_{3,n})$. Pairwise-locus nonintersection and
simple-root requirements impose inequalities or remove fold/equality sets;
they do not remove the two generic ratio degrees of freedom after one pinned
normalization.

#### 5.2.7 Middle-layer-pinned unequal-radius specialization

The operator's working A1.1 baseline now specializes the generic map by
declaring binary layer $2$ to be the middle radius and the exact
field-speed-pinned reference. The reference-radius convention is

$$
p=2,
\qquad
R_{\star,n}=R_{2,n},
\qquad
\alpha_{2,n}=1,
\qquad
\beta_{2,n}=1.
$$

The pinning relation then gives

$$
\beta_{\star,n}=1,
\qquad
R_{2,n}=R_{\star,n}=\frac{c_f}{\omega_n}.
$$

These are working planning assumptions and their algebraic consequences. They
do not derive why layer $2$ is pinned or why the pin is maintained.

Plainly: the middle circle supplies the unit radius and travels at field speed
by declaration. Its dimensional radius is still not a number because the
common frequency remains symbolic.

Retaining A1.1 as a strictly unequal-radius family and using `middle` as a
radius-order statement gives

$$
0<R_{1,n}<R_{2,n}<R_{3,n},
$$

or equivalently

$$
0<\alpha_{1,n}<\alpha_{2,n}=1<\alpha_{3,n}.
$$

The corresponding speed ordering is derived rather than separately chosen:

$$
0<\beta_{1,n}=\alpha_{1,n}
<
\beta_{2,n}=1
<
\beta_{3,n}=\alpha_{3,n}.
$$

Thus layer $1$ is sub-field-speed, layer $2$ is exactly at field speed, and
layer $3$ is super-field-speed within this prescribed circular scaffold.

**Operator baseline decision.** For this equal-common-frequency A1.1 test, the
algebraically required $\beta_{3,n}>1$ outer path is permitted as a declared
prescribed/static-level geometry allowance. `Permitted` means that this
symbolic test family is not rejected merely because its outer prescribed path
is super-field-speed. It is not evidence of a retained EOM branch, physical
superluminal transport, stability, or physical realization.

Plainly: once common frequency and middle-radius pinning are fixed, smaller
means slower than field speed and larger means faster. The outer speed class is
accepted for the drawing-level test, not promoted to a claim about transport
or an EOM solution.

The independent dimensionless radius ratios are

$$
\frac{R_{1,n}}{R_{2,n}}=\alpha_{1,n},
\qquad
\frac{R_{3,n}}{R_{2,n}}=\alpha_{3,n},
\qquad
\frac{R_{1,n}}{R_{3,n}}
=
\frac{\alpha_{1,n}}{\alpha_{3,n}},
$$

with

$$
0<
\frac{\alpha_{1,n}}{\alpha_{3,n}}
<
\alpha_{1,n}
<
1
<
\alpha_{3,n}.
$$

The allowed dimensionless ratio domain is therefore

$$
\mathcal D_{n}^{(p=2)}
=
\left\{
(\alpha_{1,n},\alpha_{3,n})
:
0<\alpha_{1,n}<1<\alpha_{3,n}
\right\}.
$$

It has two continuous radius-ratio degrees of freedom. The pin removes the
reference normalization freedom but does not select either remaining ratio.
One common dimensional scale/cadence freedom also remains through
$R_{2,n}=c_f/\omega_n$ until $\omega_n$ is separately declared.

Plainly: this specialization narrows the positive ratio plane to an open
inner-versus-outer region. It still contains infinitely many unequal-radius
A1.1 records.

The three pairwise swept-locus gaps, normalized by $R_{2,n}$, are

$$
\frac{d_{12}^{\min}}{R_{2,n}}=1-\alpha_{1,n}>0,
\qquad
\frac{d_{23}^{\min}}{R_{2,n}}=\alpha_{3,n}-1>0,
$$

$$
\frac{d_{13}^{\min}}{R_{2,n}}
=
\alpha_{3,n}-\alpha_{1,n}
=
(\alpha_{3,n}-1)+(1-\alpha_{1,n})>0.
$$

Hence pairwise-disjoint swept circular loci follow automatically from the
strict middle-radius order. The approved
$(0,2\pi/3,4\pi/3)$ phase baseline also retains the same-time
point-collision avoidance derived in Section 5.2.3, although locus
disjointness already supplies the stronger point-path separation statement for
the ideal circles.

Plainly: under this radius order the circular tracks do not cross at all, so
their phase offsets are not needed to prevent point collisions. The phase
baseline remains relevant to the later delayed geometry.

Because $\beta_{\star,n}=1$, every inter-binary root delay must lie in the
pair-specific interval

$$
\begin{aligned}
1-\alpha_{1,n}
&\le\delta_{12}\le1+\alpha_{1,n},\\
\alpha_{3,n}-1
&\le\delta_{23}\le\alpha_{3,n}+1,\\
\alpha_{3,n}-\alpha_{1,n}
&\le\delta_{13}\le\alpha_{3,n}+\alpha_{1,n}.
\end{aligned}
$$

Without selecting a history horizon, the existing distance certificates reduce
to

$$
\chi_n
<
\min\!\left\{
1-\alpha_{1,n},
\alpha_{3,n}-1
\right\}
$$

as a sufficient condition for absence of every inter-binary root inside the
retained horizon, and

$$
\chi_n\ge1+\alpha_{3,n}
$$

as a sufficient condition for at least one inter-binary root for every
reception phase and endpoint-sign pair. The region between these one-sided
certificates remains unresolved without a complete retained-root inventory.

Plainly: the radius order simplifies the shortest and longest possible causal
delays, but it does not choose the history length or count any roots.

The same ordering also fixes the solver-less self-root classification:

| Layer | Derived speed row | Circular self-root consequence |
| --- | --- | --- |
| $a=1$ | $0<\beta_{1,n}<1$ | No nontrivial circular self root exists. |
| $a=2$ | $\beta_{2,n}=1$ | Only the excluded coincident onset occurs; there is no nontrivial circular self root. |
| $a=3$ | $\beta_{3,n}>1$ | The full exact circular past has a principal geometric self root, but its presence inside the retained horizon is unresolved. |

Plainly: only the outer circular path is geometrically self-root-capable in
this specialization. That is not a claim that an active self-hit belongs to a
retained A1.1 branch.

Any later retained-root inventory must cover every outer-layer self-root
candidate inside the declared history horizon, every ordered partner or
inter-binary root class involving layer $3$, and every associated
root-topology or fold boundary. Missing, accumulated, nonisolated, or otherwise
unresolved outer-layer roots fail closed as unresolved under the predeclared
protocol. They may not be silently clipped, omitted, or reclassified as
candidate failure or acceptance. This requirement does not authorize that
inventory or select a fold policy.

Plainly: allowing the outer prescribed circle does not waive the causal-root
accounting. If the future instrument cannot resolve an outer-layer root or
topology change under its declared rules, the record stops as unresolved.

#### 5.2.8 Declaration-ready baseline

The following rows are fixed or derived for the next static-level
predeclaration:

| Row | Declaration-ready content | Status |
| --- | --- | --- |
| Active member | A1.1 unequal-radius family; A1.2 is not the active target. | Operator priority and planning scope. |
| Geometry | Orthogonal yz, xz, and xy circular planes; common center; zero axial half-separation. | Prescribed A1.1 scaffold. |
| Phase baseline | $(0,2\pi/3,4\pi/3)$. | Approved planning baseline, distinct from the unchanged display-source phases. |
| Pinned reference | $p=2$, $R_{\star,n}=R_{2,n}$, $\alpha_{2,n}=1$, and $\beta_{2,n}=1$. | Locked working assumption for this packet, not derived pinning. |
| Unequal-radius order | $0<\alpha_{1,n}<1<\alpha_{3,n}$. | Operator baseline expressed dimensionlessly. |
| Speed bands | $0<\beta_{1,n}<1=\beta_{2,n}<\beta_{3,n}$. | Derived from common frequency, the radius order, and middle-layer pinning. |
| Outer-layer geometry allowance | The derived $\beta_{3,n}>1$ outer path remains in this prescribed/static-level test family. | Operator baseline decision; not a retained-branch, transport, stability, or realization claim. |
| Swept-locus separation | All three normalized pair gaps are positive. | Derived from the strict radius order. |
| Same-time point collision | Absent for the ideal scaffold. | Derived from locus separation; independently compatible with the phase baseline. |
| Self-root capability | Layers $1$ and $2$ have no nontrivial circular self root; layer $3$ has a full-past geometric candidate. | Derived circular geometry only; retained activity unresolved. |
| Radius freedom | $(\alpha_{1,n},\alpha_{3,n})\in\mathcal D_n^{(p=2)}$. | Two continuous dimensionless degrees of freedom remain. |
| Bounded inventory ratio box | $7/8\le\alpha_{1,n}\le15/16$ and $17/16\le\alpha_{3,n}\le9/8$. | Operator-approved and frozen for the first retained-root inventory; no point in the box is selected physically. |
| Inventory history reach | $\chi_n=9/4$. | Operator-approved and frozen certificate-level reach. |
| Root/fold protocol | The nine rules in Section 5.2.9 covering all $36$ ordered channels. | Operator-approved, frozen, and completed at prescribed-path diagnostic grade by the root-sheet treatment recorded below. |

Plainly: these rows were fixed before the root inventory and were not retuned
after its result. They are either operator-approved assumptions or
consequences of those assumptions.

Section 5.2.9 freezes the bounded ratio region, retained-history reach, complete
$36$-channel inventory, and fail-closed root/fold logic. The speed bands, locus
gaps, causal-delay bounds, and full-past self-root-capability rows are derived
from the frozen ratio box and may not be independently tuned.

The prescribed-path interval instrument and the later root-sheet treatment now
materialize the executable declarations. Their diagnostic execution is
recorded in Section 5.2.9. This completion still cannot establish retention,
stability, action, energy, angular-momentum dynamics, radiation, pressure, GR
recovery, physical superluminal transport, or physical-candidate selection.

Plainly: the bounded root bookkeeping is now executable and complete for the
prescribed circles. Its authority stops before any EOM or physical claim.

#### 5.2.9 Frozen bounded retained-root predeclaration

The operator approves and freezes this bounded packet exactly as stated below:
the ratio box $\mathcal B_n$, the history reach $\chi_n=9/4$, and the nine-rule
fail-closed root/fold protocol. This approval is not execution authority. It
does not materialize a prescribed source, execute a root inventory, or select
a physical candidate. The frozen packet specializes only the static circular
scaffold and follows the complete-root and boundary-stratum discipline in the
[braid-analysis methodology](../../../content/markdown/aaa/noether-braid/braid-analysis-methodology.md)
and the priority-only
[topological causal-root ledger proof target](../master-equation-closure/topological-causal-root-ledger-proof-target.md).

**Frozen ratio region.** Define the positive normalized inner and outer
offsets from the pinned middle radius by

$$
u_n=1-\alpha_{1,n},
\qquad
w_n=\alpha_{3,n}-1.
$$

The frozen closed ratio box is

$$
\mathcal B_n
=
\left\{
(\alpha_{1,n},\alpha_{3,n})
:
\frac{1}{16}\le u_n\le\frac{1}{8},
\quad
\frac{1}{16}\le w_n\le\frac{1}{8}
\right\},
$$

or equivalently

$$
\frac{7}{8}\le\alpha_{1,n}\le\frac{15}{16},
\qquad
\alpha_{2,n}=1,
\qquad
\frac{17}{16}\le\alpha_{3,n}\le\frac{9}{8}.
$$

The box is invariant under exchange of the two offset coordinates
$(u_n,w_n)\mapsto(w_n,u_n)$. This is the limited
symmetry-respecting meaning used by the proposal: the inner and outer
departures from the pinned middle radius receive the same bounds. It is not a
full exchange symmetry of the labeled planes, endpoint identities, polarity,
circulation, or retained history, and it does not select the symmetric line
$u_n=w_n$.

The box supplies uniform strict margins:

$$
\frac{d_{12}^{\min}}{R_{2,n}}\ge\frac{1}{16},
\qquad
\frac{d_{23}^{\min}}{R_{2,n}}\ge\frac{1}{16},
\qquad
\frac{d_{13}^{\min}}{R_{2,n}}\ge\frac{1}{8},
$$

and preserves the declared speed classes

$$
\frac{7}{8}
\le\beta_{1,n}\le\frac{15}{16}
<1
=\beta_{2,n}
<
\frac{17}{16}
\le\beta_{3,n}\le\frac{9}{8}.
$$

Plainly: the frozen box stays close to the middle reference while keeping
all three circular tracks distinct. Its equal inner/outer offset bounds make
the search region balanced around the pinned radius, but no point or line
inside the box is preferred physically.

**Frozen retained-history reach.** Set

$$
\chi_n=\delta_{H,n}=\frac{9}{4},
\qquad
H_n=\frac{9}{4\omega_n}
=\frac{9R_{\star,n}}{4c_f}.
$$

This is the smallest uniform **certificate-level** reach supplied by the
present elementary bounds for the whole frozen ratio box. For every
same-binary self or partner root, the circular equations imply

$$
\xi\le\beta_{a,n}\le\frac{9}{8},
\qquad
\delta=2\xi\le\frac{9}{4}.
$$

Equality at the memory edge cannot occur for a nontrivial same-binary root:
it would require $|\sin\xi|=1$ or $|\cos\xi|=1$ at
$0<\xi\le9/8<\pi/2$, with the equality value also equal to
$\xi/\beta_{a,n}=1$. For inter-binary roots, the largest possible upper
distance bound over $\mathcal B_n$ is

$$
\max_{a\ne b}(\alpha_{a,n}+\alpha_{b,n})
=
1+\frac{9}{8}
=
\frac{17}{8}
<
\frac{9}{4}.
$$

Therefore every possible nontrivial circular self root, partner root, and
inter-binary root for this bounded scaffold lies strictly before the frozen
memory edge. The packet does not claim that $9/4$ is the smallest
root-specific horizon. A smaller complete horizon would require a tighter
analytic root bound or a separately authorized root inventory.

Plainly: the frozen history is long enough to contain every causal contact
that these bounded exact circles can geometrically produce. It is deliberately
only as long as the current simple bounds require; it does not say which roots
actually occur.

**Frozen complete root/fold protocol.** The inventory domain is

$$
\mathcal B_n
\times
\left(\mathbb R/2\pi\mathbb Z\right)
\times
\left(0,\frac{9}{4}\right],
$$

where the second factor is the reception phase $\Theta_n$ and the third is the
positive dimensionless delay. For the six endpoint worldlines, the inventory
must cover all $36$ ordered receiver/transmitter channels:

- six same-transmitter self channels, excluding only the trivial
  zero-delay coincidence;
- six ordered opposite-endpoint channels within the three binaries; and
- twenty-four ordered inter-binary endpoint channels.

For each ordered channel, the frozen protocol is:

1. bind the row to the exact level, source/protocol, endpoint, polarity,
   circulation, plane-basis, phase, ratio-box, and history identities;
2. identify $\Theta_n=0$ with $\Theta_n=2\pi$ so a root crossing the phase seam
   is not duplicated or dropped;
3. partition the full ratio-phase-delay domain and certify every partition as
   root-free when the causal-root residual enclosure excludes zero;
4. certify a simple-root partition only when the delay derivative enclosure
   excludes zero and the residual bracket isolates exactly one root;
5. retain every isolated root regardless of derivative sign, order roots by
   increasing emission time for each transmitter, and preserve the isolated
   interval, residual enclosure, derivative enclosure, and endpoint
   provenance;
6. when the residual and its delay derivative may vanish together, treat the
   partition as a fold or caustic boundary, not as an ordinary root; isolate
   the boundary and keep the adjacent root-count regions separate;
7. record the zero-delay self coincidence as an excluded endpoint stratum and
   use the analytic bound above to certify the frozen memory edge root-free;
8. preserve every root-free, simple-root, fold, endpoint, parameter-boundary,
   and omitted-row disposition in one complete topology ledger; and
9. if any possible root or fold remains after the declared subdivision,
   interval, iteration, or resource limit, return the entire affected packet
   as `drawn-not-evaluated` with null score, the unresolved box, reason,
   channel identity, and exact rerun declaration. It may not become candidate
   failure, candidate acceptance, or a silently shortened inventory.

This protocol adopts the live
`all-retained-roots/event-specific-isolation-certified.v2` logic as its
root-free/simple-root rule. Extending that fixed-event logic across the
two-parameter ratio box and reception phase requires an interval-capable
instrument or a certified finite partition; the current stationary-probe
analysis contract must not be treated as if it already supplies this
moving-endpoint, parameter-family inventory.

Plainly: every ordered causal channel must be either proved empty, assigned an
isolated simple root, marked as a declared boundary, or left visibly
unresolved. The outer super-field-speed layer receives exactly the same
complete accounting and may not be clipped to make the packet finish.

**Why this is sufficient for a first inventory.** The ratio box is compact,
strictly unequal-radius, offset-exchange symmetric, and bounded away from
track intersection. The history reach contains every possible exact-circular
root admitted by the box. The protocol covers every ordered endpoint channel,
the full common reception cycle, both derivative signs, and every root-count
boundary named by the live owners. These facts are sufficient to define a
bounded, reproducible, fail-closed root-topology inventory; they are not by
themselves sufficient to prove completion. The executable protocols and
receipts below supply that separate result.

The frozen mathematical packet alone does not supply the following executable
declarations; the prescribed-path protocols below supply them for this
bounded root inventory only:

1. the exact static level and materialized source/protocol identity;
2. the endpoint polarity and common circulation provenance for that source;
3. whether the frozen box is certified as one topology region or subdivided
   into a predeclared finite parameter partition;
4. the interval arithmetic or independent enclosure instrument used for the
   ratio-phase extension;
5. root residual, root-separation, and positive transversality floors;
6. initial partition, maximum subdivision depth, candidate-interval limit,
   iteration limit, and resource ceiling;
7. the precise isolated-fold output schema and independent residual
   recomputation required before any result exceeds diagnostic grade; and
8. separate execution authority.

Plainly: the frozen mathematical packet fixes the geometry, history length,
channel inventory, and fail-closed logic. The executable protocols add the
source identity, interval method, floors, resource budget, and granted
execution authority without changing that frozen family.

**Implemented prescribed-path executable boundary.** The operator selected a
new prescribed-path interval certifier as the diagnostic owner. The exact
[protocol](../../../src/prescribed-path-analysis/protocols/a1-1-continuous-ratio-phase-root-inventory-protocol.v1.json)
binds the frozen domain, symmetric phase baseline, normalized $c_f=1$ and
$\omega_n=1$, current display-source endpoint/frame/polarity/circulation
provenance, all $36$ ordered channels, numerical floors, partition and resource
ceilings, fold schema, and secondary squared-residual recomputation. The
instrument does not use EOM interval machinery, evolve a path, or call the EOM
solver.

The first execution is recorded in the
[diagnostic receipt](evidence/2026-07-27-a1-1-continuous-ratio-phase-root-inventory.md)
and its
[durable summary](evidence/a1-1-continuous-ratio-phase-root-inventory-summary.v1.json).
All six same-transmitter channels and all six same-binary partner channels
received complete diagnostic dispositions. All $24$ inter-binary channels
remain `drawn-not-evaluated` because $144{,}536$ partitions reached the declared
subdivision depth and $1{,}688$ reached the per-channel cell ceiling. The
packet status is therefore `drawn-not-evaluated` with null score.

The point controls passed: the declared outer self channels supplied the
expected nontrivial diagnostic roots, the declared inner self channels supplied
the expected no-root controls, and the separate direct-coordinate
squared-residual implementation agreed at the sampled roots within the
declared floor. Because both implementations entered in the same change, this
is implementation parity rather than independent mathematical acceptance.

Plainly: the selected prescribed-path instrument now accounts for every
channel and fails closed where its interval reach ends. It has not completed
the inter-binary topology and has not selected, rejected, or physically
realized a braid.

**Exact-circular inter-binary reduction.** A follow-up
[diagnostic receipt](evidence/2026-07-27-a1-1-exact-circular-inter-binary-reduction.md)
and
[version-2 summary](evidence/a1-1-continuous-ratio-phase-root-inventory-summary.v2.json)
record the exact distance-identity, phase-coordinate, interval-preconditioning,
and endpoint-inversion reduction. The frozen box, $\chi_n=9/4$, all numerical
floors, subdivision and resource ceilings, nine fail-closed rules, all $36$
channel rows, fold visibility, independent residual recomputation, null score,
and diagnostic-only authority remain unchanged.

The reduction evaluates $12$ exact endpoint-inversion symmetry
representatives for the $24$ inter-binary channels. It closes six
inter-binary channels, leaves eighteen `drawn-not-evaluated`, and reduces
actual interval evaluations from $480{,}018$ to $180{,}210$ without raising a
resource cap. The remaining represented unresolved partitions comprise
$97{,}016$ depth-limit rows and $1{,}136$ resource-pending rows. No possible
fold row was emitted, but the pending unresolved domain prevents an
absence-of-fold claim.

That prerequisite is now satisfied by the separately declared
[root-sheet treatment](../../../src/prescribed-path-analysis/protocols/a1-1-root-sheet-monotonic-enclosure-treatment-protocol.v1.json),
its
[receipt](evidence/2026-07-28-a1-1-root-sheet-monotonic-enclosure-treatment.md),
and its
[durable summary](evidence/a1-1-root-sheet-monotonic-enclosure-treatment-summary.v1.json).
The treatment uses the squared residual
$G=D^2-\delta^2$ with $\delta$ as the dependent root variable. Six
representatives use exact reception-fixed or emission-fixed phase charts: if
the inner layer is the moving endpoint in the applicable chart, its
prescribed speed is at most $15/16$, so the causal residual has delay
derivative at most $-1/16$ and cannot fold. The other three representatives
use the declared interval enclosure of $G$ and $\partial_\delta G$.

All nine representatives satisfy the endpoint, seam, no-fold, anchor-count,
and independent residual obligations without increasing a resource ceiling.
The three interval-treated representatives used $5{,}404$ evaluated boxes and
reached maximum depth $12$; the nine anchor inventories used $288$ boxes. The
largest independently recomputed normalized anchor residual was
$3.101836110544228\times10^{-16}$. The prior six inter-binary channels remain
sealed regression controls and were not re-evaluated.

The complete accounting is twelve sealed self/partner channels, six sealed
inter-binary channels, nine newly evaluated representatives, and nine exact
endpoint-inversion reuses. Thus all $36$ ordered channels have a diagnostic
disposition and none remains unresolved. The packet status is
`evaluated-diagnostic` with null score.

Plainly: the separately declared phase charts remove the two resource
blockers without raising a cap, and the remaining interval cases finish under
the old ceilings. This completes the root topology of the imposed circular
family only; it does not select or physically realize a braid.

**Bounded prescribed structural ledger.** A separately predeclared
[structural-ledger protocol](../../../src/prescribed-path-analysis/protocols/a1-1-prescribed-structural-root-ledger-protocol.v1.json),
[receipt](evidence/2026-07-28-a1-1-prescribed-structural-root-ledger.md), and
[durable summary](evidence/a1-1-prescribed-structural-root-ledger-summary.v1.json)
use the sealed $36$-channel inventory as input. The ledger has no composite
score. It retains root-count provenance, sampled delay witnesses, sheet and
reception conditioning, phase and ratio sensitivities, receiver-phase
projection rows, endpoint-inversion symmetry, and raw provenance.

The bounded execution retained $5{,}204$ sampled roots. All declared
topology, independent-residual, direct-derivative, endpoint-inversion,
phase-seam, and synthetic projection-detector controls passed. The largest
independently recomputed normalized residual was
$2.5089753066770497\times10^{-15}$. The sampled inter-binary delays ranged
from $0.2603788643805174$ to $2.088421952853519$. The smallest absolute
sampled root derivative was $0.12275221068428979$ on an outer self-root row,
and the largest sampled radius sensitivity was also on an outer self-root
row.

Plainly: the imposed circular family now has a reproducible structural map of
its roots on the declared grid. The relatively shallow and radius-sensitive
outer self rows identify where later diagnostics deserve care; they do not
establish instability, retention, or physical transport.

The structural ledger first found positive receiver-phase projection
derivatives at all $24$ declared phase nodes for the six emission-fixed
classes. The separately predeclared
[continuous projection protocol](../../../src/prescribed-path-analysis/protocols/a1-1-receiver-phase-projection-monotonicity-protocol.v1.json),
[receipt](evidence/2026-07-28-a1-1-receiver-phase-projection-monotonicity.md),
and
[durable summary](evidence/a1-1-receiver-phase-projection-monotonicity-summary.v1.json)
now replace that sampled boundary with an exact continuous enclosure.

At a causal root, the shared-coordinate dot component $q$ and its
reception-fixed delay derivative $q_\delta$ obey
$q^2+q_\delta^2\le1$. Combining this identity with the frozen radius
intervals gives strictly negative reception-fixed and emission-fixed
squared-root derivatives over all three symmetry representatives. Therefore

$$
\frac{d\theta}{d\epsilon}
=
\frac{\left.\partial_\delta G\right|_\theta}
{\left.\partial_\delta G\right|_\epsilon}
>0
$$

continuously. The conservative lower bound is $8/22103$ for the two
middle-transmitter channels and $1/798$ for the four outer-transmitter
channels. Endpoint inversion supplies the paired ordered rows.

Plainly: phase order cannot reverse between the old sample nodes. Together
with the degree-one phase seam, this converts each certified one-root
emission sheet into a one-root reception-phase sheet within the same imposed
circular family. It remains a prescribed-path diagnostic result, not an EOM,
stability, or physical claim.

### 5.3 How radii could be determined, not assumed

Kinematics alone cannot uniquely determine three independently assignable
A1.1 radii at any level. The planned derivation therefore parameterizes them
within level $n$ as

$$
R_{a,n}=\alpha_{a,n} R_{\star,n},
\qquad
a\in\{1,2,3\},
$$

where the working A1.1 specialization fixes
$R_{\star,n}=R_{2,n}$ and $\alpha_{2,n}=1$. It also fixes the symbolic order
$0<\alpha_{1,n}<1<\alpha_{3,n}$. The first inventory box is frozen in Section
5.2.9, but no pointwise ratio, physical scale, or relation between levels is
assigned.

Under the locked middle-layer pinning assumption, the reference scale is

$$
R_{\star,n}=R_{2,n}=\frac{c_f}{\omega_n}.
$$

This is the consequence of the declared pin, not a derived pinning mechanism
or a settled numerical radius. It does not determine $\alpha_{1,n}$ or
$\alpha_{3,n}$.

Plainly: one pinned cadence can propose an overall ruler for the braid, but it
does not say how the three radii compare with one another. Those ratios need
independent constraints.

The staged determination program is now:

1. retain the Section 5.2 symmetry, phase, nonintersection, field-speed, and
   causal-distance constraints as the current symbolic map;
2. require a complete retained root/fold classification before any candidate
   advances beyond the symbolic map, without treating field-speed occupancy as
   proof of an active self-hit branch;
3. report the surviving two-dimensional domain
   $\mathcal D_n^{(p=2)}$ rather than choosing a preferred point;
4. impose a stationary or variational condition only after one delayed action
   has been accepted and independently checked in a separately authorized
   stage; and
5. use later many-braid constitutive, effective-gravity, and equivalence
   comparisons as external tests of a radius family, not as targets used to
   fit the radii.

The current solver-less map contains multiple ratio families, so it does not
support a unique-radius claim. Any later uniqueness claim is falsified if more
than one ratio family survives the newly declared root, action, or ledger
conditions. The ledger must report that unresolved family rather than select
one representative.

### 5.4 Group-motion and gradient sequence

The initial ledger uses the zero-group-velocity source only as a common
solver-less reference. Later planning must create separately identified
protocol pairs in this order:

1. **Uniform-translation sequence.** Compare progressively nonzero, predeclared
   uniform group velocities in a matched homogeneous Noether sea reference,
   while retaining the same declared within-level kinematic and scale packet.
2. **Gradient sequence.** Compare separately declared inhomogeneous effective
   gravitational-field or Noether-sea-gradient protocols, retaining their
   first and higher spatial gradients as tidal observables rather than
   absorbing them into the uniform group response.

Each moving or gravity-side protocol must declare its own absolute-coordinate
description and crosswalk to the reference record, clock and synchronization
convention, complete root-history window, Noether-sea and external-drive
environment exchanges, scale packet, center-matching rule, comparison window,
and invariant observables. The records must remain distinct even when some
dimensionless inputs are matched.

Plainly: the stationary display record is the first controlled baseline. It
does not answer how the braid behaves while translating or in a Noether-sea
gradient. Those are later comparisons with new provenance and extra exchange
rows, and tidal differences remain visible.

Advancement fails if a nonzero-velocity or gravity-side conclusion reuses the
zero-velocity source identity as though it already contained the required
coordinate, clock, history, environment, or gradient data.

### 5.5 Ellipsoidal deformation benchmark

**Claim grade:** later-stage comparison and constitutive/equilibrium target.
This solver-less benchmark asks whether a single braid's envelope, or a
coarse-grained population envelope, admits a compact principal-axis response
description across:

1. the resting zero-group-velocity reference;
2. the progressively accelerated uniform-translation protocols in a matched
   homogeneous Noether sea; and
3. the inhomogeneous effective gravitational-field or Noether-sea-gradient
   protocols, with tidal terms retained separately.

The exact project geometry remains an `oblate spheroidal envelope` when two
transverse principal axes are equal. `Ellipsoid` is used here only for the
generic or triaxial comparison class. A fitted ellipsoid does not assert that
the underlying swept envelope is exactly a quadric.

The compact response packet is planned as

$$
\mathcal E_{\mathrm{def}}
=
\left(
\mathcal R_1,\mathcal R_2,\mathcal R_3,
V_{\mathrm{env}},
\boldsymbol\varepsilon_{\mathrm{env}},
\mathcal Q_{\mathrm{env}},
\boldsymbol\Omega_{\mathrm{env}},
\mathbf S_{\mathrm{env}}
\right),
$$

with:

- principal envelope semiaxes
  $(\mathcal R_1,\mathcal R_2,\mathcal R_3)$ from one predeclared boundary or
  geometric second-moment extraction;
- envelope volume $V_{\mathrm{env}}$, kept separate from axis ratios so scale
  change is not mistaken for shape change;
- strain tensor $\boldsymbol\varepsilon_{\mathrm{env}}$ relative to the exact
  matched reference packet;
- orientation frame $\mathcal Q_{\mathrm{env}}$ after common center
  translation is removed;
- rotation readout $\boldsymbol\Omega_{\mathrm{env}}$, separated from a rigid
  coordinate-frame rotation; and
- accepted single-braid or coarse-grained stress tensor
  $\mathbf S_{\mathrm{env}}$, with its isotropic and trace-free parts both
  retained.

Plainly: the benchmark compresses a complicated deformed envelope into its
three principal sizes, total volume, change of shape, orientation, rotation,
and stress. It is a comparison record, not evidence that the braid is a fluid
ellipsoid or that the compact description is dynamically complete.

Classical Maclaurin spheroids and Jacobi ellipsoids, relativistic
rotating-fluid shape studies, and relativistic reference ellipsoids are
comparison frameworks only. They may supply observer-level questions about
axis ratios, symmetry breaking, rotation, volume, and stress after the native
ledger exists. Their equations, matter models, and equilibrium conditions may
not be imported into the architrino or braid dynamics as premises. This plan
makes no statement that an ellipsoid solves general relativity.

The future question is:

> Does a coarse-grained retained braid population admit a constitutive or
> equilibrium relation among principal envelope axes, volume, strain,
> orientation, rotation, and stress that can be compared with a relativistic
> rotating medium without fitting the native response to that comparison?

Required controls:

1. use separately identified resting, accelerated-translation, and
   gravity-side records with their declared coordinate, clock, root-history,
   environment-exchange, and comparison rules;
2. use one predeclared principal-axis and volume extraction across every
   protocol and report the residual when the envelope is not well represented
   by an ellipsoid;
3. remove only common center translation and rigid frame rotation before
   comparing intrinsic orientation, rotation, and strain;
4. retain volume change separately from axis-ratio change and retain
   trace-free stress separately from scalar pressure;
5. match phase, symbolic radius family, static level packet, scale packet,
   preparation, comparison window, and Noether sea baseline before varying the
   declared translation or gravity-side input;
6. preserve gravity-side first and higher spatial gradients as tidal
   observables rather than absorbing them into a uniform deformation fit;
7. keep a single-braid envelope response distinct from the coarse-grained
   population response, including finite-size, orientation-distribution, and
   boundary effects; and
8. book external drive, environment exchange, wake history, angular momentum,
   and boundary stress on the same record before testing an equilibrium or
   constitutive relation.

Falsifiers and failure modes:

- the principal axes, volume, strain, orientation, rotation, or stress do not
  converge under boundary, time-window, history, root, population, or
  coarse-graining refinement;
- the reported deformation changes under a rigid coordinate translation or
  rotation after the declared quotient is applied;
- an ellipsoid fit hides material non-ellipsoidal residuals, branch changes,
  folds, topology changes, or tidal structure;
- shape, volume, rotation, angular momentum, and stress are assembled from
  different source, environment, or protocol identities;
- the resting, translation, and gravity-side rows require separately fitted
  constitutive coefficients or incompatible scale weights;
- a scalar pressure or one axis ratio is reported while material anisotropic
  stress or triaxial response remains;
- the apparent equilibrium relation is supplied by an imported Maclaurin,
  Jacobi, rotating-fluid, relativistic-reference-ellipsoid, or GR equation
  rather than by the native ledger; or
- more than one constitutive family fits all currently declared observables,
  in which case no unique equilibrium relation may be claimed.

Prescribed envelope drawings, best-fit ellipsoids, visual overlays, and
same-implementation reductions are diagnostic only. They may test whether the
response packet is well posed. They cannot establish physical realization,
retention, stability, a constitutive law, equilibrium, equivalence, or GR
recovery.

## 6. Optional Later Single-Braid Transition Ledger Route

This route is not part of the approved first mathematical stage. It is retained
only as a possible later extension, separately authorized after the static
level family is defined. If opened, it is ordered so that kinematics cannot be
promoted into work, and work cannot be promoted into a conserved action
charge.

| Stage | Planned artifact | Required contents | Advancement boundary |
| --- | --- | --- | --- |
| `S0` | Sealed before/after source pair | Exact source hashes, all path coordinates, persistent identities, polarities, separately authorized transition declaration, and complete history windows | Prescribed geometry only; adjacent level labels do not supply a transition. |
| `S1` | Transition kinematic ledger | Position, velocity, required prescribed acceleration, radius/phase changes, common-frequency checks, and field-speed target classifications | No interaction, work, angular-momentum, or energy conclusion. |
| `S2` | Complete causal-response inventory | Every ordered partner root, declared nontrivial self-hit row, inactive intervals, $D_t$, $D_r$, line of action, per-hit acceleration contribution, regulator, and completeness status | Fail closed on a missing or unresolved root; still a prescribed-path diagnostic. |
| `S3` | External-work and drive ledger | Actual moving receiver rows, complete interaction and environment accelerations, declared kinetic convention, interaction power, required prescription acceleration, external-drive power, and cycle/window integrals | Work supplied by the prescription is not intrinsic braid energy or loss. |
| `S4` | Angular-momentum exchange ledger | Declared origin and axis, path contribution, wake-history contribution, environment exchange, separately derived external increment $\Delta\mathbf J$, subsystem-boundary term, transition condition, units, and vector residual in $\mathbf J$ | The assumed one-$\hbar$ label spacing may not substitute for transferred angular momentum; no primitive architrino mass or conserved-angular-momentum result is introduced. |
| `S5` | Transition-action ledger | One accepted delayed action, Euler residual, rotational and time-translation Noether charges, endpoint/core terms, transition action, and independent toy-model oracle | No acceptance if the action fails to reproduce the canonical acceleration row or lacks an independent check. |
| `S6` | Single-record reconciliation | Work, angular-momentum, action, wake-history, drive, environment, and boundary rows bound to one source/protocol/action identity | Only the rows proved by this ledger may advance; retention, stability, and physical realization remain separate EOM solver questions. |

Plainly: each stage adds one new kind of bookkeeping. `S0` and `S1` describe
the imposed paths. `S2` adds their delayed interactions. `S3` asks what the
external prescription must supply. `S4` tracks rotational exchange. `S5`
derives the charge from an action. `S6` checks that none of those answers came
from a different record.

The work ledger must preserve the acceleration-first substrate law. It may use
a declared assembly-level response coefficient to translate acceleration into
work bookkeeping, but that coefficient is not architrino mass and cannot be
selected after inspection. Likewise, the angular-momentum ledger must include
the active wake history and subsystem-boundary exchange rather than treating
the six path endpoints as an isolated instantaneous system.

## 7. Later Many-Braid Continuum and Coarse-Graining Route

This later route is also outside the approved first mathematical stage. It
begins only after the within-level records have declared meanings and any
required single-braid ledger has separately closed. It proceeds in the
following order:

1. **Ensemble declaration.** Specify the braid count, spatial cell, absolute-time
   window, A1.1 coordinate distribution, orientation distribution, phase
   distribution, environment record, and preparation rule.
2. **Static-level ledger.** Count accepted, rejected, and unresolved
   independently identified level records and retain each source/protocol
   identity. Do not infer transitions between their labels.
3. **Microscopic sum.** Sum only separately accepted single-braid level,
   causal-root, action, wake, environment, and any later-authorized exchange
   rows; do not reconstruct missing rows from the aggregate residual.
4. **Coarse-graining limit.** Vary cell size, time window, braid count, and
   phase/orientation resolution to test whether the event rate and exchange
   densities converge with uncertainty bounds. Run a distinct scale path that
   changes braid size at fixed dimensionless occupancy so it is not confused
   with a density change.
5. **Continuum current.** Define a cadence-space step current only after the
   accepted forward-minus-reverse event ledger converges and its boundary
   transport is explicit.
6. **Noether-sea coupling.** Couple the continuum row to one declared Noether-sea
   response record, with braid/sea exchange closing on the same coarse-graining
   window.
7. **Effective-source export.** Export observer-level energy, angular momentum,
   stress, and size labels only after their map from the accepted microscopic
   ledgers is independently validated.

Plainly: a smooth medium law must be the large-population summary of counted
braid events. The plan does not allow a continuum energy or torque density to
be guessed first and then attributed to microscopic braids afterward.

## 8. GR Benchmark Ladder

GR enters only as an observer-level recovery target. It is not used to choose
the A1.1 level labels, radius family, action, field-speed pin, or microscopic
ledger.

| Rung | Recovery target | Required handoff | Barred shortcut |
| --- | --- | --- | --- |
| `G0` | Native ledger closure | Accepted single- and many-braid action, work, angular-momentum, boundary, and Noether-sea rows | Importing a GR energy, mass, or angular-momentum relation into the substrate calculation. |
| `G1` | Effective source labels | One independently validated coarse-graining map exporting effective total source energy or mass, angular momentum, stress, and characteristic size from `G0` | Assigning observer-level mass to individual architrinos or fitting each label separately. |
| `G2` | Weak-field exterior and observer scale | One shared Noether-sea record producing clock, ruler, signal, drift, lensing, timing, acceleration, PPN, and local-scale rows through the [effective-metric map](../../../content/markdown/aaa/spacetime/emergent-metric.md#admcartan-reconstruction-surface), with one clock/ruler calibration across the matched equivalence protocols | Treating one scalar delay, potential fit, or privately rescaled observer channel as GR recovery. |
| `G3` | Stationary nonrotating and rotating exteriors | The same `G1` source labels and `G2` effective metric recover the nonrotating exterior and the rotating frame-dragging/multipole comparison without retuning | Substituting Schwarzschild or Kerr geometry for the native derivation. |
| `G4` | Compact-object scale packet | The same effective record recovers the exterior Schwarzschild, photon-orbit, and innermost-stable-orbit benchmark scales named in the [exterior GR benchmark packet](../../../content/markdown/aaa/spacetime/black-holes.md#exterior-gr-benchmark-packet) | Calling a native radius a horizon radius because the numbers appear similar. |
| `G5` | Strong-field and first-law packet | One strong-field record supplies local interface, global exterior, energy/angular-momentum exchange, radiation or release, and the effective first-law comparison | Claiming a horizon, GR relation, or thermodynamic law from the prescribed A1.1 chart. |

Plainly: the ladder moves from the native microscopic bookkeeping to an
effective source, then to weak gravity, rotating exteriors, compact-object
scales, and finally strong-field thermodynamics. A rung cannot borrow the
answer from GR; it must show that the same underlying record produces the GR
benchmark as an output.

No GR formula is evaluated in this plan. The exact benchmark relation, source
class, comparison window, tolerance, and observer channel must be separately
predeclared before a future rung is tested.

## 9. Proof Obligations and Falsifiers

| ID | Proof obligation | Operator-checkable falsifier |
| --- | --- | --- |
| `P1` | Bind every static level record to its exact source and protocol identity. | A path, polarity, history, or protocol row changes without a new level identity. |
| `P2` | Show that all three frequencies are common within each declared level without asserting an adjacent-level transition law. | One within-level frequency differs, or a relation between $\omega_n$ and $\omega_{n+1}$ is introduced without separate authority. |
| `P3` | Preserve layer $p=2$ as the predeclared middle-layer pinned reference for this working baseline without promoting it to a permanent physical role. | A different index is selected after inspecting roots, the pin is reported as derived, or the index changes without a separately identified baseline. |
| `P4` | Certify complete partner and declared self-hit root inventories separately for any level that later advances beyond kinematics. | A root or fold is unresolved, an inactive interval is unproved, or a speed or level label substitutes for root enumeration. |
| `P5` | In an optional later extension, close any prescribed work/drive identity with a complete acceleration and environment inventory. | Work is calculated in the current stage, or an imposed cycle exports work without booking drive, environment, or storage exchange. |
| `P6` | In an optional later extension, close any vector angular-momentum transfer ledger including wake and boundary exchange. | $\Delta\mathbf J$ is calculated in the current stage, or a later residual discards wake history, changes the origin without a crosswalk, or omits external angular impulse. |
| `P7` | In an optional later extension, derive any transition action and charges from one accepted delayed action checked by an independent oracle. | The extension proceeds without separate authority, the Euler residual persists, the reference changes with the subject, or parity is the only evidence. |
| `P8` | Demonstrate coarse-graining convergence and retain microscopic provenance. | The continuum current changes materially with cell/window refinement or cannot be traced to accepted events. |
| `P9` | Derive all effective source labels from the same coarse-grained record. | Energy or mass, angular momentum, stress, and radius require separate fitted microstate selections. |
| `P10` | Recover every claimed GR benchmark from one effective metric/source record. | A benchmark requires imported substrate premises, hidden retuning, a different source identity, or an unreported observer-channel change. |
| `P11` | Derive the full collective stress tensor, isotropic/deviatoric split, spatial gradients, and deformation observables from an accepted retained population and converged continuum reduction. | A row is supplied by confinement, prescribed overlap, an isolated-record sum, unresolved anisotropy, a hidden gradient, or a nonconvergent cell/window limit. |
| `P12` | Compare the matched full local response tensor under gravity-gradient and accelerated-translation protocols while retaining tidal response separately. | Only pressure agrees, agreement is coordinate-only, tidal terms are hidden, drive/environment rows differ, or later clock/ruler/signal exports require private retuning. |
| `P13` | Classify and test the scale target using predeclared dimensionless variables, scaling weights, and scale-breaking controls. | Scaled records require hidden retuning, fail refinement, use incompatible weights across the static-level, pressure, and equivalence routes, or a weaker family/macroscopic relation is promoted as exact microscopic invariance. |
| `P14` | Determine the radius-ratio family from independent constraints before claiming unique radii. | More than one ratio family satisfies all declared kinematic constraints, or later external benchmarks are used to fit rather than test the radii. |
| `P15` | Keep the stationary, uniformly translating, and gravity-gradient protocols separately identified and completely declared. | A moving or gravity-side conclusion reuses the zero-velocity source identity, omits its clock/history/environment crosswalk, or hides tidal response inside the uniform comparison. |
| `P16` | Keep the absolute/local Planck-unit identification and one-$\hbar$ adjacent-level spacing explicit as modeling assumptions until independently recovered. | A level row omits its index, axis, subsystem, or sign convention, or the label spacing is reported as a transition, Lorentz, equivalence, quantization, or conservation result. |
| `P17` | Test one principal-envelope response packet across resting, accelerated-translation, and gravity-side records before comparing it with rotating-medium ellipsoid frameworks. | The packet is coordinate-dependent, nonconvergent, assembled from different records, hides non-ellipsoidal or tidal residuals, or imports a comparison equation as the native constitutive law. |
| `P18` | Keep swept-locus intersection, same-time endpoint collision, finite-envelope clearance, and delayed causal contact as separate conditions. | One condition is used as proof of another, or the cyclic plane-basis and phase convention are omitted from a collision claim. |
| `P19` | Preserve the symbolic root-chart boundaries and report the surviving ratio dimension without selecting a preferred point. | A fold is silently deleted, a finite-history bound is reported as a complete root census, or an inequality region is reduced to one radius tuple without an additional accepted condition. |
| `P20` | Preserve the unequal-radius specialization $0<\alpha_{1,n}<1<\alpha_{3,n}$ and distinguish its assumed order from the speed, separation, and self-root rows derived from it. | An equal-radius A1.2 row enters the active packet, a derived speed band is independently tuned, or the outer geometric self-root candidate is reported as an active retained branch. |
| `P21` | Preserve the explicit outer-layer super-field-speed geometry allowance at prescribed/static-level grade and fail closed on every unresolved outer-layer root or topology issue. | The allowance is reported as physical superluminal transport, a retained EOM branch, stability, or realization; or an outer-layer root, fold, or unresolved interval is clipped, omitted, or silently reclassified. |

Plainly: each row says what later work must prove and exactly what would stop
the claim. A failed obligation stays failed or unresolved; it does not become
evidence against A1.1 outside the declared source and motion class.

## 10. Evidence Destinations

Future work, if separately authorized, should land as follows:

- this file remains the reviewed planning and derivation-order owner;
- an exact executable predeclaration belongs under
  [`campaigns/`](campaigns/README.md), and its presence still does not authorize
  execution;
- large run artifacts, raw ledgers, checkpoints, and local diagnostics belong
  under `.local-data/braid-program/` and remain ignored;
- a closed human-readable result or fail-closed blocker belongs in the
  write-once [`evidence/`](evidence/README.md) directory;
- queue lifecycle changes belong in [work-queue.md](work-queue.md) only after
  the operator accepts a concrete executable object;
- accepted prescribed-path work methodology may later update
  [Braid Analysis Methodology](../../../content/markdown/aaa/noether-braid/braid-analysis-methodology.md);
- accepted action/energy construction belongs first in the
  [action-energy validation program](../../../content/markdown/aaa/validation/simulations/action-energy/delay-dynamics-energy.md)
  and only then in
  [Energy](../../../content/markdown/aaa/dynamics/energy.md);
- accepted effective-metric or GR recovery belongs in its existing spacetime
  owner, not in this priority plan;
- accepted population stress or pressure response belongs first in the
  [pressure-dependent Noether sea constitutive-response owner](../master-equation-closure/pressure-dependent-noether-sea-constitutive-response.md);
- accepted equivalence recovery belongs in the existing General Relativity,
  clock/ruler, and effective-metric owners after the matched retained-response
  record closes;
- a scale result must be graded as microscopic invariance, a dimensionless
  family relation, or a macroscopic scaling law and land first with its
  derivation and falsifiers in the existing Master Equation, units, braid,
  Noether sea, or effective-metric owner appropriate to that grade; and
- an accepted principal-envelope response construction belongs first in
  [Braid Envelope Geometry](../../../content/markdown/aaa/noether-braid/braid-envelope-geometry.md)
  and the Noether sea constitutive owner. Rotating-fluid and ellipsoid
  comparisons remain benchmark material unless the later effective-metric and
  GR recovery records independently close.

Plainly: plans, executable specifications, raw outputs, durable evidence, and
reader-facing theory each have different homes. Keeping them separate prevents
an unrun plan from being mistaken for a result.

## 11. Explicit Exclusions for the Current Scope

This initial pass excludes:

- analyzing a dynamical transition between adjacent level labels;
- calculating any energy, work value, action increment, angular momentum, or
  continuum coefficient;
- calculating $\Delta\mathbf J$, $\Delta E$, drive work, radiation, or
  emissions;
- calculating a pressure, stress response, deformation residual, equivalence
  residual, or effective gravitational response;
- calculating a scaling exponent, fitting a curve collapse, selecting a
  numerical reference scale, or determining numerical radii;
- choosing or evaluating any GR relation;
- numerically evaluating the field-speed/radius target;
- changing the A1.1 prescribed source or generating a new record;
- running any further prescribed-path evaluator outside the completed bounded
  root treatment, or running the EOM solver, campaign harness, oracle,
  benchmark, or browser;
- implementing any new packet, reducer, schema, checker, gate, or fixture
  outside the completed bounded root treatment;
- modifying the Braid Program queue, priorities, brainstorming log, work log,
  evidence records, or corpus prose;
- asserting the working field-speed pin as a derived or physically retained
  result, an active retained self-hit branch, a complete retained six-body root
  inventory, retention, stability, binding, accepted energy, physical
  realization, or GR recovery;
- interpreting the prescribed outer-layer condition $\beta_{3,n}>1$ as
  physical superluminal transport, observer-level signal propagation, a
  retained EOM branch, stability, or physical realization;
- asserting a collective pressure phase, high-energy small-braid realization,
  universal deformation response, or equivalence-principle recovery;
- asserting microscopic scale invariance, a universal dimensionless radius
  family, a macroscopic scaling law, unique radii, or observer-level scale
  recovery;
- asserting that a braid or population is an equilibrium ellipsoid, that a
  rotating-medium constitutive relation has been derived, or that an
  ellipsoid solves general relativity;
- asserting that the locked Planck-unit identification derives Lorentz
  invariance, gravitational equivalence, action quantization, or angular-
  momentum conservation;
- treating the zero-group-velocity source as evidence for nonzero uniform
  translation or a Noether-sea-gradient response;
- treating adjacent level labels or their assumed one-$\hbar$ spacing as a
  transition path, rate, mechanism, selection rule, emitted packet, or energy
  difference; and
- committing unrelated shared-checkout work.

## 12. Declaration Block for a Future Non-Root Review

Before any later non-root stage can become an executable predeclaration, an
operator-reviewed revision must fill the applicable fields:

- static level index $n$ and exact source identity:
- within-level common angular frequency $\omega_n$:
- explicit statement that no $\omega_n$ to $\omega_{n+1}$ law is used:
- planning phase baseline: fixed at $(0,2\pi/3,4\pi/3)$, distinct from the
  current display-source phases:
- fixed coordinates:
- adjustable coordinates:
- symbolic radius ratios: fixed
  $\alpha_{2,n}=1$ and $0<\alpha_{1,n}<1<\alpha_{3,n}$; the first inventory
  ratio box is operator-approved and frozen as
  $7/8\le\alpha_{1,n}\le15/16$ and
  $17/16\le\alpha_{3,n}\le9/8$:
- reference-radius convention: fixed
  $R_{\star,n}=R_{2,n}=c_f/\omega_n$ under the working pin:
- independent constraints allowed to narrow the radius family:
- cyclic plane-basis and root-inventory circulation convention: fixed by
  Section 5.2.1 and the executed root protocol; any later-stage circulation
  meaning must be declared:
- swept-locus and same-time point separation: derived; any finite-envelope
  clearance still to be declared:
- pinning scope: fixed to middle layer $p=2$ for this working baseline:
- pinning meaning: exact equality as a working assumption, not a derived
  mechanism:
- outer-layer speed class: fixed at $\beta_{3,n}>1$ by the declared radius
  order and common frequency; permitted only as prescribed/static-level
  geometry, not as a retained-branch or physical-transport claim:
- isolation or Noether-sea environment:
- required self, same-binary partner, and inter-binary root classes: complete
  at prescribed-path diagnostic grade for the frozen bounded family:
- emission-to-reception phase-chart conversion: complete at prescribed-path
  diagnostic grade for the six emission-fixed classes under the frozen
  relative phases; the continuous projection derivative is strictly positive:
- outer-layer root/topology coverage: complete inside the declared horizon and
  fail closed on every unresolved root, fold, or topology boundary; completed
  at prescribed-path diagnostic grade for the frozen bounded family:
- simple-root and fold policy: operator-approved and frozen as the nine-rule
  complete, fail-closed protocol in Section 5.2.9; certification floors and
  resource limits are fixed by the two executed prescribed-path protocols:
- regulator, core, and memory ladder:
- dimensionless history reach $\chi_n=c_fH_n/R_{\star,n}$:
  operator-approved and frozen as $\chi_n=9/4$:
- within-level comparison cycle and history window:
- $\mathbf J_n$ level-label origin, projection axis, subsystem, sign, and
  locked one-$\hbar$ spacing convention:
- zero-velocity reference protocol identity:
- nonzero uniform group-velocity sequence and matched homogeneous environment:
- moving-record coordinate, clock, history, and environment crosswalk:
- independent recovery test for $h_{\mathrm{abs}}=h_{\mathrm E}$ and
  $\hbar_{\mathrm{abs}}=\hbar_{\mathrm E}$:
- ensemble/coarse-graining domain:
- population size and boundary preparation:
- accepted meaning of `small` and `high-energy`:
- full $S_{ij}$ construction, $P=\operatorname{tr}(S)/3$ projection,
  deviatoric stress, and spatial-gradient reporting:
- principal strains, axis changes, orientation, shear, and volume response:
- continuum and finite-size refinement sequence:
- gravity-gradient and accelerated-translation protocol pair:
- gravity-side coordinate, clock, root-history, and exchange rules:
- local matching rule and allowed tidal residual:
- invariant deformation observables:
- reference length $L_\star$ and reference time $T_\star$:
- scale-factor domain and quantities held fixed:
- invariant observables and derived covariant scaling weights:
- cutoff, regulator, density, boundary, and retained-history scaling:
- exact-microscopic, dimensionless-family, or macroscopic target grade:
- diagnostic-only and acceptance boundaries for scale evidence:
- principal-envelope extraction and non-ellipsoidal residual:
- ellipsoidal benchmark resting/translation/gravity protocol packet:
- comparison-only Maclaurin, Jacobi, relativistic rotating-fluid, and
  relativistic reference-ellipsoid rows:
- later clock/ruler/signal and effective-metric handoff:
- first GR benchmark rung allowed:
- optional later transition-extension authority:
- optional transition profile, $\Delta\mathbf J$, $\Delta E$, work, drive,
  radiation, and emission declarations:
- optional kinetic/rotational diagnostic convention:
- optional delayed-action candidate and independent oracle:
- tolerances and fail-closed dispositions:
- estimated cost and execution authority:

Plainly: the bounded prescribed-path root fields are complete. The remaining
blank fields belong to later, separately authorized stages; leaving them blank
prevents root closure from quietly becoming energy, action, EOM, collective,
equivalence, scaling, or GR execution authority.

## 13. Owner References

- [Braid Program charter](README.md)
- [Braid Program live state](priorities.md)
- [A1.1 prescribed source](configurations/family-a-a1-1-equal-frequency.v1.json)
- [Braid Family A](../../../content/markdown/aaa/noether-braid/braid-family-a.md)
- [A1 Dynamics](../../../content/markdown/aaa/noether-braid/braid-a1-dynamics.md)
- [Master Equation](../../../content/markdown/aaa/dynamics/master-equation.md)
- [Same-Record Energy-Ledger Methodology](same-record-energy-ledger-methodology.md)
- [Angular Momentum and Spin](../../../content/markdown/aaa/philosophy-history/theory-bridges/angular-momentum-and-spin.md)
- [Braid Envelope Geometry](../../../content/markdown/aaa/noether-braid/braid-envelope-geometry.md)
- [Noether Sea](../../../content/markdown/aaa/spacetime/noether-sea.md)
- [Parameter Ledger](../../../content/markdown/aaa/validation/parameter-ledger.md)
- [$\mathbb{A}\mathbb{A}\mathbb{A}$ and SI Base Units](../../../content/markdown/aaa/validation/architrino-si-base-units.md)
- [Pressure-Dependent Noether Sea Constitutive Response](../master-equation-closure/pressure-dependent-noether-sea-constitutive-response.md)
- [Emergent Metric](../../../content/markdown/aaa/spacetime/emergent-metric.md)
- [General Relativity](../../../content/markdown/aaa/spacetime/general-relativity.md)
- [Proper Time and Time Dilation](../../../content/markdown/aaa/spacetime/proper-time-and-time-dilation.md)
- [Black Holes](../../../content/markdown/aaa/spacetime/black-holes.md)
- [Ellipsoidal Deformation and Effective Metric Source Packet](../cross-theory-mapping/effective-metric-deformation.md)
