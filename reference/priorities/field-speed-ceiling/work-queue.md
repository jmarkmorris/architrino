# Field-Speed Ceiling Work Queue

## Ranked Next Objects

1. `field_speed_ceiling_compatibility_map` — FSC-001. Status:
   `discussion-scoped`.
2. `equal_radius_phase_offset_braid_speed_domain` — FSC-002. Status:
   `deferred / blocked` behind FSC-001's exact ceiling and threshold semantics.
3. `capped_collinear_endpoint_reanalysis` — FSC-003. Status: `Awaiting verification`
   under the provisional closed-domain assumption.

## Discussion-Scoped Investigation

1. **FSC-001 — field-speed-ceiling compatibility map**
   - Status: `discussion-scoped`
   - Request: Compare a closed primitive domain $\|\mathbf V\|\le c_f$ with
     the present open velocity domain. Identify every explicit corpus,
     priority, EOM, and prescribed-diagnostic dependency; distinguish accepted
     results from hypotheses and tests.
   - Required boundary analysis: state the exact threshold alternatives without
     selecting one: no new boundary root; one separately declared threshold
     root; or a changed evolution/update rule. For each, say how the incoming
     speed-increasing partner contribution is represented and how causal-root
     ownership is preserved.
   - Acceptance: one evidence-bounded decision packet giving (a) the unchanged
     results, (b) the material hypotheses and diagnostics removed or rewritten,
     (c) the minimum new postulates, and (d) falsifiers or consistency checks.
   - Nonclaims: no adoption, derivation, continuation, passage, rebound,
     regularization choice, root deletion, conservation, physical realization,
     or closure-score movement.

Plainly: first learn exactly what a ceiling would cost and what it would need to
say at the boundary. Only then decide whether to adopt it.

## Deferred / Blocked

2. **FSC-002 — equal-radius phase-offset braid speed-domain calculation**
   - Status: `deferred / blocked` behind FSC-001.
   - Request: On one explicitly declared common-radius, phase-offset braid
     geometry, calculate every constituent's Euclidean speed from its internal
     motion and any declared assembly translation. Compare it with the closed
     field-speed domain and identify whether a geometry change is required
     under a declared $H$-increment or decrement.
   - Acceptance: a geometry-only certificate with stated radius, phase,
     translation, speed, and root-domain assumptions; clear separation of
     exact kinematics from any candidate Lorentz-recovery interpretation.
   - Nonclaims: no braid retention, angular-momentum derivation, quantization,
     conservation, Lorentz recovery, or physical realization.

## In Progress

3. **FSC-003 — capped collinear endpoint reanalysis**
   - Status: `Awaiting verification` under the provisional postulate
     $\|\mathbf V\|\le c_f$.
   - Request: Reanalyze the stationary mirror-symmetric collinear encounter
     through first arrival at field speed, forbid a super-field-speed history,
     and identify the resulting exact root ledger and endpoint evolution
     condition.
   - Acceptance: a separate calculation that distinguishes absence of a
     super-field continuation from finiteness of the canonical acceleration
     integral and determines any required boundary evolution statement.
   - Nonclaims: no adoption of the ceiling, continuation, passage, rebound,
     root deletion, regularization, conservation, or theory advancement.
   - Result: [capped collinear endpoint reanalysis](capped-collinear-endpoint-reanalysis.md)
     preserves MEC-007 through first threshold arrival and removes the specific
     super-field newborn self root, but finds that a nonzero unit-speed plateau
     produces a degenerate continuum of candidates. The cap alone does not
     select outgoing evolution.
