# App Borg Work Log

This file is the chronological work log for the `app-borg` priority area. Use it for dated agent status, proof-attempt notes, checker narratives, handoffs, failed paths, and operator/developer communication that must remain discoverable but should not crowd the live priority tracker.

Use `brainstorming.md` for provisional ideas, insights, conceptual maps, and draft corpus-promotable text when this priority area has one. Use `priorities.md` for strategy, status, blockers, and promotion routing, and use `work-queue.md` for accepted executable tasks and their local order. Keep focused proof packets, certificates, app specs, and requirement notes in their own sibling files when they need a stable structure.

## Log Entries

### 2026-07-27 - App-Idea Ownership Consolidated

- Accepted IDEA-007 through IDEA-014 from App Ideas as BORG-006 through BORG-013.
- Moved the prescribed-translation packet into this directory and queued that bounded feature behind the five existing Borg objects.
- Kept taxonomy morphs, harmonics, Family-A exclusion, Gell-Mann classification, polarity bookkeeping, conserved-account flow, and material routing deferred. Borg owns their eventual display surface, while the relevant scientific lane must supply every classification, mapping, conserved-account, and constitutive row.
- The local rank-1 object and global Borg score remain unchanged. Promotion triage: `priority-only`.

### 2026-07-26 - Borg Documentation Consolidation

- Consolidated the first-screen requirements into [requirements-and-design](requirements-and-design.md), while retaining `BORG_APP_SURFACE_DESIGN_V1` in [BorgAppManifest.js](../../../src/apps/borg/BorgAppManifest.js) as the executable screen-contract authority.
- Reconciled the former bridge capability audit against live code and queue ownership. The unresolved EOM products are the wake/history, shell, conservation, residual, and acceleration-contribution rows owned by [BORG-001](work-queue.md#borg-001--native-wake-history-and-boundary-residuals), followed by the measured velocity-scale sampling evidence owned by [BORG-003](work-queue.md#borg-003--velocity-scale-sampling-evidence). Implemented or display-only rows no longer remain in a separate audit packet.
- Consolidated the live-run browser-retention explanation into [requirements-and-design](requirements-and-design.md), while retaining [BorgLiveRunRetentionPolicy.js](../../../src/apps/borg/BorgLiveRunRetentionPolicy.js) as the executable authority for numeric limits, compaction, snapshots, and runtime status.

Plainly: one requirements packet now explains the screen and browser-retention boundaries, the queue owns the remaining EOM work, and executable constants stay in source.

### 2026-07-24 - Targeted Browser Claim Invalidation And Legacy-Budget Decoupling

- Adjudicated the missing-history defect by build, transport, grade, and continuation identity. Only Claim-grade chunk index 1 or greater produced through `BorgEomHttpClient` while the all-grade history transform was live is invalidated, together with conclusions that depend on it. Claim chunk zero's accepted extension remains valid for this defect, although its shortened returned history cannot support a continuation. No persisted browser Claim packet in the repository matched the predicate, so zero checked-in result artifacts were deleted or quarantined. Claim grade: derived call-path and commit-history adjudication plus bounded repository inventory. Falsifier: a retained artifact satisfies every predicate condition or an invalidated continuation independently proves it carried complete required history.
- Retained direct EOM solver CLI, Node process-client, independent-oracle, prescribed-record, and Display-only artifacts at their declared authority. Historical browser Claim observations remain runtime/failure observations only; post-first-chunk physics interpretation is withdrawn unless route provenance proves exemption. Claim grade: provenance classification. Falsifier: one of those retained artifact classes is shown to depend on the faulty browser continuation transform.
- Removed the historical release-budget disposition module, its live runtime import, and its diagnostics rows. The historical manifest and sweep remain unchanged reference-only evidence; current EOM ceilings require a separately authorized current EOM release budget plus live EOM measurements. Claim grade: derived implementation plus source contract regression. Falsifier: `src/apps/borg` imports either historical artifact, renders legacy-budget fields, or live measurements create ceilings without an authorized current EOM budget.

Plainly: the ruling rejects only browser Claim continuations that could have lost required history, keeps independent work intact, and leaves the old budget files available for audit without allowing them to control the app.

Artifact: [browser Claim-grade history invalidation](browser-claim-history-invalidation-2026-07-24.md).

### 2026-07-24 - Borg Code-Review Repair

- Fixed both severity-1 findings from `borg-code-review-2026-07-24.md`. The browser now applies history-prefix release and client-window bounding only to Display requests, so certified chunk two retains the full causal history. The certified response firewall now checks a client-owned allowed evidence-status set instead of comparing the server's status to itself. Claim grade: derived implementation plus focused regressions. Falsifier: the certified browser-client chunk-two request loses any retained segment, or a completed certified response with an invented evidence status is accepted.
- Isolated runtime state by run/workspace, excluded asynchronous generation and prefill guards from snapshots, registered every mount listener for disposal, separated replay from live EOM retention/calibration, repaired tube and ArrowHelper display state, and bounded the reviewed timeline and path-key edge cases. The frame hot path now uses indexed lookups and reusable interpolation rows; prescribed analysis caches immutable record validation and digest work. Claim grade: derived implementation plus focused runtime, scene, retention, replay, and provider regressions. Falsifier: a reviewed state leak or display edge case reproduces under its named regression, replay changes measured EOM calibration, or frame interpolation resumes allocating a per-frame map and particle rows.
- Reclassified the 2026-07-01 release-budget sweep as historical measurement of the deleted pre-EOM browser path. Its ceilings no longer gate the current EOM surface; current limits require measurements from live EOM chunks. The historical JSON remains unchanged as provenance-bound evidence. Claim grade: provenance disposition plus contract regression. Falsifier: current runtime controls consume a ceiling from the historical manifest or the historical artifact is silently relabeled as an EOM measurement.

Plainly: certified runs keep the past they need, evidence labels are checked by the client, switching views no longer mixes run state, and old browser measurements cannot masquerade as current EOM limits.

### 2026-07-21 - Auto-Tuned Playback And Persistent Trail Duration

- Removed the playback-pace selector. Playback now always requests the controller's $1\,T/s$ ceiling, while measured solver production, the safety factor, the retained lead, and any chunk still in flight automatically reduce the actual playback pace. Claim grade: operator decision plus derived implementation. Falsifier: the timeline still exposes a playback-pace selector or playback bypasses `getBorgAdaptivePlaybackRate` and its in-flight lead protection.
- Kept `Trail 30 s`, `Trail 60 s`, `Trail 90 s`, `Trail 180 s`, and `Trail 360 s` visible in random simulation, prescribed geometry, and sealed replay. A direct prescribed-record startup whose source duration is not an exact menu value rounds upward to the next offered duration, while in-app geometry changes preserve the operator's current selection. Claim grade: operator decision plus derived implementation. Falsifier: any Borg workspace hides the trail selector, shows it blank, changes the chosen duration during a geometry switch, or fails to apply the selection to `pathTrailDuration` and the visible path window.
- Live browser QA selected `Trail 90 s`, switched from Random architrinos to the illustrative prescribed spindle, retained `Trail 90 s` visibly in the timeline, found no playback selector, and recorded no browser warning or error. Claim grade: measured UI behavior. Falsifier: repeat that sequence and observe a missing/reset trail choice, a playback selector, or a browser warning/error.

### 2026-07-21 - Single-Choice Grade And Starting-Geometry Lists

- Replaced the `Run grade` dropdown with two visible radio choices, `Display grade` and `Claim grade`, and replaced the `Starting geometry` dropdown with a complete radio list containing the random workspace plus every current prescribed-geometry catalog entry. Each group uses one shared HTML radio name, so the browser enforces exactly one selection. Existing reset, workspace restore, prescribed-record loading, and Display-branch grade locking now read and set the radio groups explicitly. Claim grade: operator decision plus derived implementation, measured by selection/restore browser interaction and the full Borg source suite. Falsifier: either group permits two checked choices, a selected prescribed entry does not load, returning to Random fails, or a grade choice does not reset into its selected fixed grade.
- Standardized ordinary left-panel labels, controls, radio choices, help text, status text, drawer headings, and form values at `12px`. The `Borg` title and section-heading hierarchy remain intentionally larger. Browser measurement found the representative panel roles at `12px`, exactly one checked choice in each group, no horizontal overflow, and no warning/error logs. Claim grade: measured UI behavior. Falsifier: a normal left-panel control or explanatory row computes to another font size, either radio group has a checked count other than one, the panel overflows horizontally, or the browser logs a warning/error.

### 2026-07-21 - Explicit Trail And Playback-Pace Controls

- Made the simulation timeline controls self-identifying: the trail choices now read `Trail 30 s` through `Trail 360 s`, and the playback choices read `Playback Slow`, `Playback Normal`, and `Playback Fast`. The trail selector remains visible for random and prescribed Display simulations and remains hidden only during sealed-record replay. Claim grade: operator decision plus derived implementation, measured by the full Borg source suite and live browser inspection. Falsifier: the trail selector is absent during simulation, visible during sealed replay, or does not offer all five durations.
- The playback selector caps only how quickly already-computed frames advance: `Playback Slow` is $1/12\,T$ per wall second, `Playback Normal` is $0.6\,T$ per wall second, and `Playback Fast` is $1.0\,T$ per wall second. It does not change EOM solver computation speed, and the adaptive buffer controller may reduce the selected pace when frame production is slower. Removed end-user `realtime` wording from the selector and diagnostics; diagnostics now report `Playback pace` and `Solver production` in `T/s`. Claim grade: derived from the playback controller and measured by source tests plus browser QA. Falsifier: changing the selector changes solver request parameters or chunk production, any visible control still says `Realtime`, or playback exceeds the selected cap.

### 2026-07-21 - Remove Redundant Header Status Lozenges

- Removed the `Forward EOM` and `Developer test` header lozenges and their runtime status wiring. Compute state remains visible through Play/Pause, the fixed-width clock, run status, and the solver with not advanced banner; source/mode authority remains visible under `Starting geometry` and in `Run grade`. Claim grade: operator decision plus derived implementation, measured by the full Borg source suite and live browser inspection. Falsifier: a compute failure loses its banner, a prescribed source loses its mode/authority description, either removed lozenge reappears, or the header removal produces a browser warning.

### 2026-07-21 - Extreme Cap-Tilt Inspection Variant

- Added a separately selectable prescribed spindle parameter variant with cap angles $70^\circ$, $80^\circ$, and $85^\circ$. It retains the original radii, common angular frequency, phases, polarity assignments, coaxial frame, and one-period trail depth. These declared inputs exaggerate the canon-defined cap-tilt coordinate for inspection; they do not tilt the three orbit-plane normals independently and do not assert a physically selected braid. Claim grade: declared input and display-only chart hypothesis. Falsifier: the source record changes any retained parameter, carries a non-coaxial plane normal, or receives stronger provenance.
- Kept all three transverse radii and carrier speeds strictly positive, so the inspection member remains animated instead of degenerating to the static full-cap boundary. Claim grade: derived from $r\cos\alpha>0$ for every declared angle below $90^\circ$ and measured by the analytical record test. Falsifier: any generated binary has zero transverse radius/carrier speed or its site is unchanged after a quarter turn.
- Live browser QA selected the extreme cap-tilt entry, displayed all six polarity-colored architrinos and their circular trails, showed the declared $70^\circ$/$80^\circ$/$85^\circ$ values in the geometry table, and advanced from keyframe 0 to keyframe 12 at $T=0.6$ after Play. The warning/error log was empty. Claim grade: measured display behavior. Falsifier: the same entry loses a point or trail, reports different angles, remains at keyframe 0 after Play, or logs a warning/error.

### 2026-07-21 - Prescribed Replay Identity Repair And Static Boundary Catalog Removal

- Fixed direct prescribed-record startup so its display buffer begins empty instead of retaining the random simulation seed rows. The first sealed replay chunk now owns the path identities that construct both particle objects and polarity-colored trails. Claim grade: derived from the buffer/path-id flow and measured by focused runtime tests plus browser QA. Falsifier: a direct tilted-spindle load again shows no red/blue architrinos or falls back to white trails.
- A prescribed Display branch is no longer saved in `simulationWorkspaceSnapshots` under its source record's catalog id. Returning to a prescribed entry reloads its sealed record, while the independent random simulation workspace remains restorable. Claim grade: derived from selector state ownership. Falsifier: leave a source record through its Display branch, return to the same catalog id, and observe branch state instead of chart pose.
- Replaced the changing `Start display simulation from T=<cut>` button label with the stable `Continue with Display simulation` label. Plain-language help now explains that this is an optional EOM solver continuation at non-promotable Display grade; the status line carries the selected history cut at fixed three-decimal precision. Claim grade: measured UI behavior. Falsifier: moving the timeline changes the button text or the cut display changes decimal width.
- Removed the full-cap axial-pair boundary from the animated catalog while retaining its source specification, sealed record, and analytical test. At cap angle $\pi/2$, $r\cos\alpha=0$, so all transverse radii and carrier speeds vanish and the six endpoints reduce to three static antipodal axial pairs. Claim grade: derived from the prescribed chart equation and measured by the boundary regression. Falsifier: the generated record contains a nonzero transverse radius or changes position with time.
- Live browser QA loaded the tilted spindle directly, switched to the planar boundary, and switched back in place. Both moving records showed all six red/blue architrinos and polarity-colored trails; the selector contained no full-cap static entry; the Display-branch button stayed `Continue with Display simulation` while its status reported `T=4.000`; and the warning/error log was empty. Claim grade: measured. Falsifier: repeat the same selector sequence and observe a missing point, fallback-white trail, static full-cap option, variable button label, variable decimal width, or browser warning/error.

### 2026-07-21 - Exact-History Disk Ownership And Abort Recovery

- The `exact history disk block has invalid format` abort was caused by all `BorgNativeEomProcessClient` instances sharing one temporary root. Creating or restarting any second client—including a test client—could delete files still owned by the live worker. Each client now receives a unique PID-tagged temporary root; startup removes only roots whose owner process is no longer alive, and client disposal removes only that client's root. Claim grade: derived from the client lifecycle and measured by a concurrent-client ownership regression. Falsifier: two live clients receive the same root or starting one removes the other's ownership marker.
- Disk reads can fail, so `RetainedHistory::t_start()` and `t_end()` no longer promise that they cannot throw. A halted server request now clears both the incremental cache and its disk store, allowing the browser's required full-history retry to start cleanly instead of terminating through `noexcept` or reopening damaged files. Claim grade: derived implementation plus measured corruption/retry regression. Falsifier: deleting active block files produces `SIGABRT`, kills the worker, leaves block files behind, or prevents a complete-history retry from succeeding.
- A rebuilt live 3:3 Display run reached `T 00:00:20.4` while exact history occupied disk blocks, with no browser warning or worker failure. Ending the run through `New distribution` reduced the active `.aehb` file count to zero. This is a storage-path control, not an indefinite-runtime result. Claim grade: measured. Falsifier: the same current build reproduces the format abort without another process modifying its client-owned root, or run disposal leaves an active block file behind.

### 2026-07-21 - Unified Starting Geometry And Prescribed Display Branch

- Replaced the separate random-simulation and prescribed-replay surfaces with one persistent Borg workbench and one `Starting geometry` selector. Prescribed records load in place, random and live prescribed branches keep separate saved workspace state, and record-only replay constructs no EOM client. Claim grade: derived implementation plus measured browser and source-test checks. Falsifier: selecting a catalog entry navigates away, switching back loses the prior live run, or record-only replay constructs or calls the EOM client.
- Added the explicit `borg-prescribed-display-v1` profile and `Start display simulation from T=<cut>` branch. The adapter requires an exact common source segment boundary, transfers the exact piecewise-cubic source prefix and explicit id/polarity map to retained history, starts directly at Display grade, and marks the child run promotion-ineligible. It does not reconstruct history from rendered frames or alter the sealed record. Claim grade: derived from the adapter and measured by exact-coefficient, cut-boundary, profile-isolation, runner-start, and browser checks. Falsifier: any supplied coefficient differs from the record, an interior cut is accepted, Claim grade becomes selectable, or the source record changes.
- The unified selector is catalog-generic. Live browser QA loaded and started a prescribed Display branch from both the illustrative spindle and a newly added boundary record without modifying the selector or branch code. Claim grade: measured for those records. Falsifier: a complete catalog entry requires geometry-specific runtime wiring or fails the same branch eligibility rules.

### 2026-07-21 - Prescribed Spindle Boundary Catalog

- Expanded the immutable prescribed-geometry catalog from one to three sealed records: the illustrative tilted iso-frequency spindle member, the planar tri-binary boundary member, and the full-cap axial-pair boundary member. The catalog order is family member, planar boundary, axial boundary. These are one spindle family member plus two boundary members, not three braid families. Claim grade: derived from source taxonomy and measured by catalog/record tests. Falsifier: any catalog id, label, ordering, or source-carried classification differs from `tests/borg-braid-record-catalog.test.js` and `tests/spindle-chart-record.test.js`.
- Added source-carried family, taxonomy-class, variant, and canon-source metadata to prescribed provenance and exposed it in Borg's Provenance drawer. The catalog still carries only id, label, and record URL; geometry remains in the source specification and sealed record. Claim grade: derived from the data path and measured by focused tests. Falsifier: Borg displays a browser-inferred taxonomy value or a catalog entry gains a geometry field.
- The planar boundary inherits the illustrative member's radii, frequency, phases, polarities, frame, and interval and sets all cap angles to $0$. The full-cap boundary inherits the same values and sets all cap angles to $\pi/2$. Tests establish exact coplanarity and period return for the first, and static antipodal axial pairs for the second. Claim grade: derived analytical identities plus measured implementation checks. Falsifier: a planar ansatz point has nonzero axial coordinate beyond tolerance, or a full-cap site changes position with time.
- Symmetric shell, nested shell, `3:2:1`, `4:2:1`, and volumetric three-plane catalog members remain unavailable because their source charts do not yet fix all numerical inputs. Missing inputs include shell/support radii, exact paths or plane normals, absolute frequency scale and binary-role mapping where applicable, phases/circulation, polarity membership, and record interval. No substitute values were inferred. Claim grade: derived from the current taxonomy and configuration-chart sources. Falsifier: a current canonical source supplies a complete numerical chart for any named entry.
- Live browser QA selected all three catalog entries. The planar table showed $0^\circ$ cap angles and zero axial spacing; the full-cap table showed $90^\circ$ and axial spacings $0.44$, $0.64$, and $0.88$. Play advanced the planar record, perspective depth remained visible in both boundary views, neither page overflowed horizontally, and the browser warning/error log was empty. Claim grade: measured. Falsifier: a fresh selector pass shows different source values, Play leaves the keyframe unchanged, the camera flattens either chart, the page overflows, or a warning/error is logged.

### 2026-07-20 - Fixed-Envelope Causal-History Release

- Added the optional `borg-causal-history-retention/v1` Display route. The EOM worker releases only a complete leading segment whose outward-enclosed causal sphere has passed the entire declared spherical receiver envelope. It refuses to publish a candidate segment that leaves the envelope. The certificate's retained start and segment count are consumed by the worker, server, browser cache, and runner, so release is physical memory ownership rather than a display-only trim. Claim grade: derived implementation plus measured cache regressions. Falsifier: a cleared prefix remains owned by any cache, the next prefix count uses the old length, or an envelope-crossing candidate appears in output.
- A persistent 40-chunk stationary-envelope control reached `T=8.0` with no retained-history growth beyond nine segments and more than seventy solver-cleared releases. A full local HTTP-stack Borg runner reached `T=5.0`, retired 103 segments, and ended with thirteen. Claim grade: measured for those stationary controls only. Falsifier: either control grows linearly when rerun from the current binary or a released segment fails the native clearance inequality.
- The policy is not enabled for the ordinary app configuration because Borg currently continues to evolve raw identities outside the dotted sphere. Indefinite bounded-memory operation therefore still requires one explicit boundary decision: remove outbound identities and introduce new boundary-generated inbound identities/histories, or preserve the unbounded active domain with disk-backed exact history. Claim grade: derived boundary condition. Falsifier: the live solver already removes escaped identities from ordered-pair evaluation or proves released all-space history unreachable under its unrestricted velocity domain.

### 2026-07-20 - Host-Aware Display Memory Envelope

- Replaced Display grade's fixed 64 MiB native admission ceiling with `borg-display-host-memory-envelope/v1`. The local service samples macOS available memory through `memory_pressure -Q` and worker RSS through `ps` at no more than one sample per second. It reserves the larger of 20% of installed RAM or 2 GiB and advances the next native ceiling from the prior native estimate by only the bytes available above that reserve. Claim grade still requires the certified allocation's fixed 64 MiB value. Claim grade: derived from the service, encoder, and response-provenance branches and measured by focused policy/grade-isolation tests. Falsifier: a Claim request carries a host envelope, Display uses raw free pages instead of macOS available memory, or the computed envelope consumes the declared reserve.
- A default 3:3 Display run crossed the former `T=118.8–119.4` limit and was paused at `T=142.0`. At `T≈129`, the native retained-history estimate was approximately 72.8 MiB, above the former gate, while the effective envelope was approximately 12.1 GiB. The measured process state was approximately 90.5 MiB RSS for the dev server and 35.7 MiB RSS for the worker. Claim grade: measured for this run and host only; it is not an unlimited-duration guarantee. Falsifier: the same build reports `memory_budget_exhausted` at 64 MiB, the response omits its host envelope, or a repeat cannot cross the old boundary while macOS reports memory above the reserve.
- This run used the then-current append-only history. Protocol V10 now provides an opt-in fixed-envelope retirement certificate; it remains inactive for ordinary runs until their outbound identity lifecycle is defined.

### 2026-07-20 - Display Retained-History Prefix Transport

- Replaced complete-history JSON retransmission between Display-grade increments with `borg-eom-http-history-prefix/v1`. The first request establishes a worker-owned prefix; later requests carry an opaque cache token, exact per-path segment counts, path metadata, and only appended input segments. The normal continuation request therefore carries zero `SEG` rows while the worker keeps the full causal history. Responses carry only newly published segments, and the browser reconstructs the same logical append-only history. Claim grade: derived from the browser/server/process/native call graph and measured by browser, process-client, and native-worker regressions. Falsifier: the second unchanged-prefix request contains a retained segment, a returned extension is omitted or reordered, or cached and cold second-increment outputs differ.
- The cache requires verification for advancement. A mismatch in token, path domain, charge, or segment count rejects cached reuse; the browser then performs one full retransmission. A halted request, worker restart, client disposal, or binary replacement clears cache authority. Display completion now persists native history even though Display has no certified acceleration snapshot; Claim-grade snapshot reuse remains conditional on an actual accepted snapshot. Claim grade: derived and measured by stale-prefix retry, halt-reset, binary-refresh, cached-Display, and cached-versus-cold parity tests. Falsifier: stale cache data reaches native evaluation, a display cache is accepted after halt/restart, or Claim grade reuses an absent Display snapshot.
- Both promise queues now settle to `undefined` instead of retaining their last successful response value. Live browser QA ran the default 3:3 Display workload to `T 00:00:28.6` and paused it cleanly; afterward the dev server measured approximately 66 MiB RSS and the worker approximately 14 MiB RSS, versus the prior audit's approximately 895 MiB server state after a large complete-history request. The full Borg suite passed `112/112`; the native process suite passed `15/15`. Claim grade: measured for those instruments and that run only. Falsifier: a heap-retainer trace shows either queue still owns a completed response, the same default run resumes complete-prefix request growth, or either named suite fails.

### 2026-07-20 - Long-Run Memory Audit And Fixed-Width Clock

- Replaced the lower-right rate-and-lead string with only `T hh:mm:ss.s`, using tabular digits and one decimal place. The clock rolls seconds and minutes correctly, so the operator-reported `T=119.4` renders as `T 00:01:59.4`. Playback and production rates remain diagnostics rather than primary timeline text. Claim grade: measured by formatter tests and live browser QA. Falsifier: the timeline contains `realtime` or `lead`, changes character width below 100 hours, or formats 119.4 differently.
- The operator's default Display-grade run reached `T=119.4`, the longest reported Borg run, before a memory-related stop. The audit found that rendered frames and trails are bounded, but the EOM retained histories are append-only and copied through the browser request, server JSON body, native protocol, native retained history, and full response. Display grade does not use the existing native wire-prefix cache, and both server promise queues retain the last successful full response. Claim grade: operator-measured endurance plus derived code-path audit. Falsifier: a heap/profile capture of the same run shows another allocation dominates or the display request carries only an appended suffix.
- The stop matches the native Display-grade 64 MiB admission formula. Six paths have 16,283.5 segment-equivalents after fixed overhead; at `T=119.4`, exhausting that allowance implies an average accepted step near `0.0441`, inside the selected adaptive range and below the `0.05` ceiling. This is a deterministic request-budget stop, not evidence that macOS exhausted RAM: the audit measured 53% system memory free, a 107 MiB native-worker peak, and approximately 895 MiB resident in the dev server after the large request. Claim grade: derived match plus measured process/system state; the exact failed response payload was not captured. Falsifier: the retained segment count and response `memoryEstimateBytes` from the failed request remain below 64 MiB or the halt code is not memory-related.

### 2026-07-20 - Selectable Simulation-Time Path Trails

- Added a path-history trail selector to the bottom timeline after the run-duration limit and before playback speed. The simulation-workspace choices are 30, 60, 90, 180, and 360 units of simulation time $T$, with 30 selected at startup. A change redraws the current trail window without resetting, restarting, or changing the selected run grade. Prescribed replay hides this selector and keeps its source-carried `displayTrailPeriods`. Claim grade: derived implementation and measured by focused source tests plus live browser interaction. Falsifier: the selector appears in another position, changes forward run state, is visible in prescribed replay, or its chosen duration does not reach `setVisibleWindow`.
- The control bounds rendered history, not EOM solver input. A segment may leave causal storage only after a solver-owned causal-support test shows that no future requested receiver event can reach it; elapsed display age alone is insufficient. That separation prevents the trail control from recreating `display_insufficient_history_depth`. Claim grade: derived from the delayed-history input obligation. Falsifier: changing the trail selector advances the retained-history start supplied to the EOM solver, or a solver-owned reachability rule later proves the stated boundary too strict.

### 2026-07-20 - Fixed Claim-Grade And Display-Grade Runs

- Replaced the automatic claim-prefix/display-suffix crossover with a fixed `Run grade` selector immediately below `Simulation workspace`. `Display grade` is the startup selection and enters point evolution at `T=0`; `Claim grade` remains certified and stops on a halt or execution failure. Changing the selector resets the simulation to its initial cut and does not start playback. Claim grade: operator decision plus derived implementation, measured by the full `108/108` Borg JavaScript suite and browser interaction. Falsifier: a display run sends a certified request, a claim run changes to display, a selector change preserves forward frames or auto-starts motion, or any display output becomes promotion eligible.
- The display route point-projects the accepted input history once before its first request. Every completed display increment is then retained and supplied to the next display increment with the same grade. No grade state is encoded in path color. A live browser control completed 21 display chunks through retained-history `T=6.3` at a measured production rate of `18.97× realtime`; this is a current-workload speed measurement, not a general performance guarantee. Claim grade: derived from the runner request sequence and measured by the direct two-increment regression plus the live browser. Falsifier: the first display request retains interval error authority, the second request does not begin at the first accepted endpoint, the selected grade changes between requests, or the same current control does not reproduce the recorded grade and rate.
- A later live run exposed `display_insufficient_history_depth` because Borg still applied the claim-grade rolling window to display history: at retained-history `T=6.3`, the oldest retained time had advanced to `T=5.25`. Display grade now keeps its point-projected input plus every computed segment append-only. A fresh browser run retained its original `T=-1.01` start through 93 chunks and `T=27.9`, with a measured production rate of `1.19× realtime` at that longer history and no history-depth stop. The full Borg suite remains `108/108`. Claim grade: derived cause and measured regression/browser control. Falsifier: display provenance reports a later retained-history start, the append-only regression loses its first segment, or the same current control stops with `display_insufficient_history_depth` before `T=27.9`.

### 2026-07-20 - Timeline-Owned Run Controls And Exact Display Continuity

- Verified directly from the sealed ansatz coordinates that every spindle member path has zero span along its binary-plane normal and that each pair occupies equal-and-opposite parallel planes. The apparent unequal tilt comes from perspective projection, not the prescribed geometry. Prescribed replay retains perspective because the visible depth and tilt are important to reading the braid; a pair-planarity regression guards the source geometry instead of flattening the display. Claim grade: derived from the emitted coordinates and measured by the regression plus browser QA. Falsifier: any member curve varies along its declared normal or pair offsets fail equal-and-opposite symmetry.
- Restored the catalog-backed `Prescribed Geometry` selector inside record replay, removed the replay-only `Recorded coverage`, strobe, and one-period-loop controls, and added a disabled `Export animation` action beside the working static export. The prescribed top status now says `Prescribed Geometry`; the machine contract retains `chart-hypothesis`. Claim grade: derived from the mode routing and measured by focused source tests. Falsifier: the selector does not navigate through the catalog, any removed control is rendered, animation export is enabled, or the displayed claim wording remains `Chart hypothesis`.
- The simulation workspace now opens Initial conditions by default. Display-grade boundary handling is directly regression-tested at `6.499`, which floors to `6.4` as an exact decimal before display continuation and therefore cannot overclaim `6.5`. Claim grade: measured by the boundary-unit test. Falsifier: the drawer starts collapsed or the helper returns any token other than `6.4` for `6.499`.

- Replaced the live-workspace braid dropdown with `Open prescribed geometry workspace`, so random live initial conditions and prescribed record replay are distinct entry paths. Moved the green/red grade lozenge immediately below `Simulation workspace` and renamed its green state `Claim grade`; path colors remain unrelated to grade. Claim grade: derived from the current DOM/runtime routing and measured in the live browser. Falsifier: a prescribed record appears as a live initial-condition choice, the grade lozenge is below the detailed controls, or a grade change recolors a path.
- Removed `Apply & run`, `Start / restart`, and `Stop` from the left panel. Editing an initial-condition field now debounces into a new accepted seed at `T=0` without starting motion; the timeline Play control starts evolution from that state. The random-distribution control now uses a die icon and likewise returns to the ready state. The duration control moved beside playback speed with `1 min`, `5 min`, and `No limit`; initial conditions, run status, simulation envelope, and provenance default collapsed. Claim grade: measured by focused runtime tests and browser interaction. Falsifier: a panel edit leaves prior forward frames active, starts playback by itself, disables Play, or any named drawer opens by default.
- Preserved exact accepted boundary tokens between EOM chunks instead of converting them through binary64 text. The prior conversion could make adjacent segment endpoints differ as exact decimals and produced `engine_exception: retained-history segments must be contiguous` near `T=7.5`. A native display run now crossed `T=7.5` and continued through `T=8.1` in 0.3-time-unit chunks. Claim grade: derived cause and measured regression. Falsifier: the next request start token differs from the prior accepted token, or the same native display harness fails before `T=8.1` with a contiguity error.

### 2026-07-20 - Claim-Grade Prefix And Display-Grade Suffix

- Borg now begins each live run with the green `Claim grade` indicator. When certified evolution cannot publish the next candidate, Borg keeps the last accepted prefix, records the boundary time and failure code, turns only the panel authority indicator red, and continues through the EOM service's non-certifying point-evolution route. Trajectory materials and path colors are unchanged. Claim grade: derived from runtime state transitions and measured by focused UI-contract tests plus browser inspection of both panel states. Falsifier: a path color changes at downgrade, the panel lacks the boundary, or reset does not restore green claim grade.
- The red suffix is permanently `display-only`: all later response and segment records carry that grade, point projection discards interval claim authority, and promotion remains false. A certified execution timeout crosses at the prior accepted cut and rejects the unfinished candidate; display timeouts, other engine exceptions, nonfinite evaluation, insufficient history, unresolved display roots, and memory/resource failures remain hard stops. Claim grade: derived from the runner/native failure routing and measured by JavaScript and native process tests. Falsifier: any red suffix reports canonical authority, a hard failure retries indefinitely, or a rejected or unfinished certified candidate enters the accepted prefix.

### 2026-07-20 - Source-Defined Spindle Chart And Braid Selector

- Added a global Borg braid-record selector backed by an immutable catalog containing only stable id, label, and sealed-record URL. Selection enters the existing `eomRecord` bootstrap route, and replay still mounts without constructing a live EOM client. Claim grade: derived from the routing call graph and measured by focused tests. Falsifier: a catalog entry contains geometry or physics, selection bypasses `BorgBootstrap.js`, or replay constructs the EOM client.
- Added prescribed chart playback for the illustrative spindle member while keeping chart pose as its initial display. Borg shows `prescribed-geometry`, `chart-hypothesis`, `display-only`, and the no-physics declaration; its trails and animation are declared chart paths rather than simulated history. Claim grade: measured by record-ingestion tests and browser QA. Falsifier: Borg labels the record evolved, enables workspace run controls in replay, or displays a path not carried by the sealed record.
- The built-in catalog does not close the external packet/manifest carrier, synchronized comparison transforms, field-speed carrier, or spin/polarity-dipole carriers. Claim grade: derived from the catalog schema and adopted record contract. Falsifier: the instrument gate ratifies one of those carriers or the catalog begins carrying it.
- Live browser QA caught and closed two integration defects that source tests alone did not expose: prescribed coordinates were initially translated by the ordinary EOM center, and replay particle objects were initially built before the first sealed path ids arrived. The corrected chart uses its source-carried response center as the viewport origin and rebuilds particles on the first replay rows. Desktop and 390-by-844 captures show six sites, six ansatz curves, three antipodal binaries, one dotted shell, the global selector, co-rotating playback, and no horizontal overflow; warning/error logs were empty. Claim grade: measured. Falsifier: a fresh replay omits any red/blue site, translates the chart outside the shell, shows a second shell, overflows at 390 px, or logs a warning/error.
- Operator display refinements make Play switch from chart pose into prescribed-path playback, render one dashed source-carried axis row per binary, use light purple for chart curves, and double the desktop left rail from `310px` to `620px`. Coincident axes are not displaced to make them look separate. Claim grade: measured by the scene/runtime tests and browser QA. Falsifier: Play remains disabled in chart pose, an explicit axis row is omitted, the curve material differs from `0xc6b6ff`, the desktop rail is not `620px`, or the narrow layout overflows.
- Radius and other spindle variations are supported by the existing source-specification and generator route, but each variation must be a separate sealed raw record and catalog entry. No `I:M:O` mapping or new radius values were inferred in this change. Claim grade: derived from the generator/catalog boundary. Falsifier: Borg computes a variation in the browser or a variant record exists without a complete source specification.
- Consolidated replay status to one top `Chart hypothesis` lozenge, moved the record date into the viewport toolbar, placed the subtitle beside the Borg title, removed the redundant single-record selectors, and moved optional record, playback, provenance, and camera controls into collapsed drawers. Camera defaults collapsed and keeps its mode and three actions together. Claim grade: measured by desktop and 768-pixel browser QA. Falsifier: the replay surface repeats the claim label, horizontally overflows, or opens Camera by default.
- Added a full source-carried Braid Taxonomy table with one column per binary and rows for source id, radius, ordinary frequency, cap angle, axial spacing, phase, transverse radius, carrier speed, and polarity assignment. Long unavailable fields now wrap without squeezing their labels. Claim grade: measured against the sealed spindle record and browser render. Falsifier: a displayed cell differs from its record value or the table creates a horizontal page overflow.
- Prescribed chart records now carry `displayTrailPeriods`; the illustrative spindle record declares one period. At ordinary frequency $0.25$, Borg starts chart pose at $T=4$ and shows the full four-time-unit rotation behind every site. This is declared path-history display, not EOM solver evolution. Claim grade: derived from $1/0.25=4$ and measured by session tests plus browser playback. Falsifier: the displayed trail duration differs from the declared period count or replay invokes the EOM solver.
- Replay now draws geometrically coincident source-carried binary axes once as one neutral solid axis; opposite normal direction counts as the same axis, while a different line remains separate. The redundant load-state `Replay complete` chip and simulation-source Provenance drawer are hidden in replay, leaving the replay-specific Provenance drawer as the sole record provenance panel. Strobe controls share a bottom alignment, and the record-date lozenge uses the same six-pixel corner radius as the other viewport controls. Claim grade: derived from the axis grouping rule and measured by focused scene/runtime tests. Falsifier: the current three coaxial declarations render more than one axis, a noncoincident axis is lost, either duplicate replay panel remains visible, or the control geometry differs from the declared CSS.
- Removed the prescribed-workspace Display selector and hid the random-distribution control in replay. Chart pose remains the initial prescribed presentation and Play enters prescribed-path animation without exposing that internal transition as a user mode. The Path layer now owns both the recorded trail and chart-pose ansatz curves, Velocity owns only velocity vectors, and the duplicate `Diag` layer button is removed while the dedicated diagnostics-panel button remains. Claim grade: derived from the layer ownership wiring and measured by focused scene/runtime tests. Falsifier: replay shows either removed control, Path leaves a chart curve visible, Velocity changes trail visibility, or two diagnostics controls appear in the viewport toolbar.

### 2026-07-20 - Record-only Assembly Viewer Core

- Implemented the Assembly Viewer inside the canonical Borg runtime. `BorgBootstrap.js` selects record-only startup before live-client construction; `EomHistoryDataset.mjs` rejects foreign schemas, undeclared claim grades, missing required provenance, non-finite values, missing authoritative segments, inconsistent coverage, and incomplete evolved-segment provenance. Claim grade: derived from the call graph and measured by focused tests. Falsifier: a replay bootstrap invokes `createEomClient`, or any rejected fixture mounts successfully.
- Added focused session, control, and scene modules for persistent provenance, chart/evolved labels, delay-horizon trails, coverage-clamped playback, chart pose and ansatz curves, co-rotating camera, strobe, one-period loop, display-only swept envelope, static image export, raw source-order navigation, source filters, and source-carried $S_3$ grouping. Claim grade: measured by Node tests and local browser QA. Falsifier: a control produces a time outside record coverage, changes a workspace initial condition, sorts a raw record, or displays an inferred unavailable diagnostic.
- Browser QA at `http://127.0.0.1:5173` exercised simulation workspace, evolved replay, chart pose, two-record source-order navigation, incompatible comparison, co-rotation, strobe, looping, swept envelope, export, coverage clamping, and a 390-by-844 viewport. The viewport retained one dotted outer boundary shell and no continuous guides or central-ball sphere; browser warning/error logs were empty. Claim grade: measured. Falsifier: a fresh run shows enabled EOM controls in replay, an out-of-coverage frame, horizontal narrow-width overflow, a second shell, or a browser warning/error.
- Kept `assembly_viewer_replay_mode` open and contract-blocked. `assembly-view-record.v0` lacks ratified time/unit transforms for synchronized comparison, a multi-record packet/manifest carrier, a required field-speed carrier, and spin/polarity-dipole vector fields. Borg exposes these as missing-contract states and does not define a packet or silently assume identity transforms. Claim grade: derived from the adopted schema and runtime validation. Falsifier: the instrument gate ratifies any named carrier or an existing accepted field is found in the schema.
- Preserved `assembly-explorer.html`, its runtime, and its tests. Borg now matches raw source order, unquotiented record retention, optional source-carried $S_3$ grouping, source-only filtering, and raw-record navigation, but external collection intake and synchronized comparison remain blocked. `assembly_explorer_surface_disposition` therefore remains a separate pending architectural decision. Claim grade: derived from the live file set and measured by both runtime suites. Falsifier: any standalone explorer file or test is absent, or a Borg collection test loses the named navigation invariant.

### 2026-07-02 - Partition Resume Audit

- Resumed the paused Borg app work after reading the current priority partition policy in [../README.md](../README.md), then the live tracker, brainstorming file, and this work log before writing.
- Confirmed the active priority directory is `reference/priorities/app-borg/`; the old `reference/priorities/borg-app/` path is absent in the current branch state.
- Confirmed focused artifacts held the release/runtime decisions: [borg-release-budget-manifest.v1](borg-release-budget-manifest.v1.json) records measured browser runtime budget ceilings, while the live-run browser-retention policy records forever-mode display retention with compacted display-only path history. The retention explanation now lives in [requirements-and-design](requirements-and-design.md), and its executable authority remains [BorgLiveRunRetentionPolicy.js](../../../src/apps/borg/BorgLiveRunRetentionPolicy.js).
- No loose conceptual notes were migrated into [brainstorming](brainstorming.md) during this audit. The live tracker remains compact because the current queue and next action are already stated there.
- Current tracker next build burden remains `build-native-wake-history-and-boundary-residual-fixture`: extend the EOM contracts and native implementation for retained wake/interaction rows, row-conservation counts, boundary-to-central residual rows, and acceleration-contribution diagnostics without app-local physics.

### 2026-07-16 - Assembly Viewer Consolidation

- Moved the operator-ratified assembly-viewer requirements from the Braid Program campaign directory into [assembly-viewer-requirements](assembly-viewer-requirements.md), because `app-borg` owns Borg design, requirements, and build handoffs while the Braid Program retains schema acceptance and campaign booking in its instrument gate.
- Mined the older Assembly Configuration Explorer priority packet and live runtime. Retained requirements: raw source-order preservation, `unquotiented-labeled` tri-binary rows, optional source-carried $S_3$ grouping without evidence deletion, local/manifest collection intake, stable-sector navigation, and source-carried branch/capture filters.
- Did not import the older runtime's app-computed speed regimes, $D_{\mathrm{plane}}$, energy differentials, permutation fingerprints, or demo rows into replay authority. Borg may display those values only when a source record or packet carries them with provenance.
- Recorded two distinct Borg modes: the simulation workspace may invoke the EOM solver after explicit Start; assembly-view replay consumes sealed `assembly-view-record.v0` files only. The old explorer priority directory was retired after its useful requirements were captured; its standalone implementation remains until a separate parity-based disposition task.

### 2026-07-17 - Spherical Envelope Geometry Epoch

- Operator decision implemented forward-only: Borg's app-layer envelope is one sphere with `outerRadius = 0.625`, `centralBallRadius = 0.5`, and `radialBufferMargin = 0.125`; the EOM solver remains unaware of this display/seeding bookkeeping. Claim grade: derived from the manifest and call graph. Falsifier: any EOM request or engine type contains an envelope-shape field, or any active Borg source retains the removed cube-envelope fields.
- The population rule is $N_{\mathrm{calc}}=\lceil N_{\mathcal C}((r_{\mathrm{central}}+b_{\mathrm{radial}})/r_{\mathrm{central}})^3\rceil$. The current values give `5.859375` before the ceiling and preserve the 3:3 total of six. Claim grade: derived arithmetic. Falsifier: `validateBorgManifest` computes a different ceiling from the declared radii and central count.
- Seed positions use bounding-box rejection sampling but accept only points inside the central ball; this produces the correct uniform-volume radial law before the unchanged pair-separation rejection. The 64-path seed-0 control uses the same random route and retains the `0.2` minimum-separation certificate. A dense request that cannot be placed inside the declared attempt budget does not advance instead of changing distribution family. Claim grade: derived algorithm plus measured tests. Falsifier: a seeded row has radial distance above `0.5`, the single-particle mean of $(r/0.5)^3$ leaves the declared test band, or the 64-path seed-0 separation certificate fails.
- The app's causal-history separation bound is the sphere diameter `2 * outerRadius = 1.25`; with zero initial speed and `0.01` safety/sample interval, the default history depth is `1.26`. Claim grade: derived and measured in the browser source panel. Falsifier: the bootstrap or new-distribution path passes any other geometry bound, or the browser reports a different default history depth.
- Browser verification at `http://127.0.0.1:5173/borg.html` showed three great circles for each of the central ball and outer shell, correct camera fit, the new envelope fields, and no console errors. Claim grade: measured with the Codex in-app browser. Falsifier: a fresh local load crops either shell, renders a filled/Cartesian envelope, shows an old field, or logs a runtime error.
- Operator visual adjudication rejected the two three-circle sphere guides. The current viewport instead uses the Ideal Noether Braid surface-sample pattern for one dotted outer boundary shell, with no second rendered central-ball sphere. Claim grade: measured by source inspection and a fresh browser render. Falsifier: Borg renders any `LineLoop`, more than one shell point cloud, or a central-ball guide.
- Operator follow-up aligned the rendered and seeded volumes: the dotted shell now uses the Ideal Noether Braid point opacity `0.88`, and every initial position is sampled uniformly through the outer spherical envelope rather than placing the total outer population inside the smaller central ball. Claim grade: derived from the shared `outerRadius` consumer and measured by the radial-law test. Falsifier: the renderer opacity differs from `0.88`, the sampler consumes `centralBallRadius`, or the normalized cubic-radius mean leaves its declared test band.
- Operator startup-default update set `outerRadius = 0.5`, retained the density ratio with `centralBallRadius = 0.4` and `radialBufferMargin = 0.1`, set `coupling = 0.05`, set the per-axis speed maximum to `0.03`, and made each browser startup draw a fresh recorded seed. The envelope now combines the bright light-gray dotted shell with three orthogonal light-gray great circles at that same radius. Claim grade: derived from the manifest/runtime wiring and measured by browser and startup-seed controls. Falsifier: two injected startup seeds produce identical rows, any startup value differs from the declared defaults, or any circle uses a radius other than `outerRadius`.
- The rebuilt `eom_borg_shadow_cli` was newer than its relevant `CoupledEvolution.cpp` and Borg CLI sources. The seed-0 3:3 display-grade profiler, using `h_max = 0.025`, first warned at `T = 1.80234375`; chunk `[1.80,1.85]` cost `1.57084 s`. It continued to accepted `T = 1.99619375`, then halted `caustic_entry_uncertified` because root completeness remained uncertified. Claim grade: measured by `scripts/eom/profile-borg-incremental-chunks.mjs` on 2026-07-17. Falsifier: the same binary, manifest, seed, and controller settings produce a different first-warning time or terminal record.
- A six-chunk browser run failed its strict run-grade provenance check because the EOM response exposed internal routed-pair retry bookkeeping as `causticWarningPairs` while reporting zero accepted warnings. The public pair ledger now contains only pairs carried from prior accepted warnings or added by warnings accepted in the current chunk; routed-pair state remains internal. The seed-0 display-grade profiler reproduced the contradiction before the fix at `[0.35,0.40]` and, after rebuilding, continued through twelve chunks to `T = 0.6` with warning count, warning time, warning rows, and warning pairs mutually consistent. Claim grade: measured by the profiler and the native evolution regression fixture on 2026-07-17. Falsifier: a response with zero warnings again carries a nonempty public warning-pair ledger, or the same twelve-chunk control again trips `inconsistent run-grade provenance`.
- The startup camera now fits the visible outer envelope instead of the unrendered central ball. With the unchanged `outerRadius = 0.5`, the camera distance increases by 10% from the prior startup framing. The shell dots and all three great circles consume the same light-gray color and opacity constants. The run-grade warning chip remains present for warned encounters but an explicit `[hidden]` rule removes its empty lozenge before the first warning. Claim grade: derived from the camera ratio and measured in a fresh Borg browser render on 2026-07-17. Falsifier: startup framing changes the declared radius, dots and circles resolve to different guide constants, or the empty third status lozenge is visible before a warning.
- The operator-set startup maximum for each velocity component is `0.01`. The manifest validator, initial-settings form, seeded-position controls, and current design packet consume that one default; explicit nondefault fixtures retain their own test values. On the seed-0 3:3 display control with `h_max = 0.025`, changing only this bound from `0.03` to `0.01` moved the terminal coupled-correction boundary from `T = 0.683203125` to `T = 0.55390625`; therefore the lower speed is a configuration decision, not a demonstrated cure for the correction boundary. Claim grade: derived wiring and measured paired control on 2026-07-17. Falsifier: a fresh initial-settings pane starts at any value other than `0.01`, a seeded component exceeds it, or the paired profiler does not reproduce the stated boundaries.
- The coupling control now occupies half of its former full-width row. Its other half contains `Step height` and `Adaptive minimum`: the applied height supplies both `initialStep` and `maximumStep`, while the applied minimum supplies `minimumStep`. The panel rejects values outside `0 < minimum <= height <= 0.05` rather than exposing an unreachable height above the chunk length. Claim grade: derived from the runner-option mapping and measured by the Borg runtime controls. Falsifier: an applied edit does not appear in the next run request, adaptive growth exceeds the applied height, or the controller attempts a height below the applied minimum.
- Operator visual decision removed all three great-circle guides from the envelope. The viewport retains exactly one light-gray dotted outer boundary shell and no central-ball guide. Claim grade: derived from the scene graph and measured in the live Borg viewport on 2026-07-17. Falsifier: the runtime constructs a `LineLoop`, the viewport shows a continuous spherical guide, or more than one envelope shell is rendered.
