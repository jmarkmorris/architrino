# App Borg Work Log

This file is the chronological work log for the `app-borg` priority area. Use it for dated agent status, proof-attempt notes, checker narratives, handoffs, failed paths, and operator/developer communication that must remain discoverable but should not crowd the live priority tracker.

Use `brainstorming.md` for provisional ideas, insights, conceptual maps, and draft corpus-promotable text when this priority area has one. Use the main priority tracker in this directory for the compact current queue, blockers, promotion routing, and next action. Keep focused proof packets, certificates, app specs, and requirement notes in their own sibling files when they need a stable structure.

## Log Entries

### 2026-07-20 - Fixed Claim-Grade And Display-Grade Runs

- Replaced the automatic claim-prefix/display-suffix crossover with a fixed `Run grade` selector immediately below `Simulation workspace`. `Display grade` is the startup selection and enters point evolution at `T=0`; `Claim grade` remains certified and stops on a halt or execution failure. Changing the selector resets the simulation to its initial cut and does not start playback. Claim grade: operator decision plus derived implementation, measured by the full `108/108` Borg JavaScript suite and browser interaction. Falsifier: a display run sends a certified request, a claim run changes to display, a selector change preserves forward frames or auto-starts motion, or any display output becomes promotion eligible.
- The display route point-projects the accepted input history once before its first request. Every completed display increment is then retained and supplied to the next display increment with the same grade. No grade state is encoded in path color. A live browser control completed 21 display chunks through retained-history `T=6.3` at a measured production rate of `18.97× realtime`; this is a current-workload speed measurement, not a general performance guarantee. Claim grade: derived from the runner request sequence and measured by the direct two-increment regression plus the live browser. Falsifier: the first display request retains interval error authority, the second request does not begin at the first accepted endpoint, the selected grade changes between requests, or the same current control does not reproduce the recorded grade and rate.

### 2026-07-20 - Timeline-Owned Run Controls And Exact Display Continuity

- Restored the catalog-backed `Prescribed Geometry` selector inside record replay, removed the replay-only `Recorded coverage`, strobe, and one-period-loop controls, and added a disabled `Export animation` action beside the working static export. The prescribed top status now says `Prescribed Geometry`; the machine contract retains `chart-hypothesis`. Claim grade: derived from the mode routing and measured by focused source tests. Falsifier: the selector does not navigate through the catalog, any removed control is rendered, animation export is enabled, or the displayed claim wording remains `Chart hypothesis`.
- The simulation workspace now opens Initial conditions by default. Display-grade boundary handling is directly regression-tested at `6.499`, which floors to `6.4` as an exact decimal before display continuation and therefore cannot overclaim `6.5`. Claim grade: measured by the boundary-unit test. Falsifier: the drawer starts collapsed or the helper returns any token other than `6.4` for `6.499`.

- Replaced the live-workspace braid dropdown with `Open prescribed geometry workspace`, so random live initial conditions and prescribed record replay are distinct entry paths. Moved the green/red grade lozenge immediately below `Simulation workspace` and renamed its green state `Claim grade`; path colors remain unrelated to grade. Claim grade: derived from the current DOM/runtime routing and measured in the live browser. Falsifier: a prescribed record appears as a live initial-condition choice, the grade lozenge is below the detailed controls, or a grade change recolors a path.
- Removed `Apply & run`, `Start / restart`, and `Stop` from the left panel. Editing an initial-condition field now debounces into a new accepted seed at `T=0` without starting motion; the timeline Play control starts evolution from that state. The random-distribution control now uses a die icon and likewise returns to the ready state. The duration control moved beside playback speed with `1 min`, `5 min`, and `No limit`; initial conditions, run status, simulation envelope, and provenance default collapsed. Claim grade: measured by focused runtime tests and browser interaction. Falsifier: a panel edit leaves prior forward frames active, starts playback by itself, disables Play, or any named drawer opens by default.
- Preserved exact accepted boundary tokens between EOM chunks instead of converting them through binary64 text. The prior conversion could make adjacent segment endpoints differ as exact decimals and produced `engine_exception: retained-history segments must be contiguous` near `T=7.5`. A native display run now crossed `T=7.5` and continued through `T=8.1` in 0.3-time-unit chunks. Claim grade: derived cause and measured regression. Falsifier: the next request start token differs from the prior accepted token, or the same native display harness fails before `T=8.1` with a contiguity error.

### 2026-07-20 - Claim-Grade Prefix And Display-Grade Suffix

- Borg now begins each live run with the green `Claim grade` indicator. When certified evolution cannot publish the next candidate, Borg keeps the last accepted prefix, records the boundary time and failure code, turns only the panel authority indicator red, and continues through the EOM service's non-certifying point-evolution route. Trajectory materials and path colors are unchanged. Claim grade: derived from runtime state transitions and measured by focused UI-contract tests plus browser inspection of both panel states. Falsifier: a path color changes at downgrade, the panel lacks the boundary, or reset does not restore green claim grade.
- The red suffix is permanently `display-only`: all later response and segment records carry that grade, point projection discards interval claim authority, and promotion remains false. A certified execution timeout crosses at the prior accepted cut and rejects the unfinished candidate; display timeouts, other engine exceptions, nonfinite evaluation, insufficient history, unresolved display roots, and memory/resource failures remain hard stops. Claim grade: derived from the runner/native failure routing and measured by JavaScript and native process tests. Falsifier: any red suffix reports canonical authority, a hard failure retries indefinitely, or a rejected or unfinished certified candidate enters the accepted prefix.

### 2026-07-20 - Source-Defined Spindle Chart And Braid Selector

- Added a global Borg braid-record selector backed by an immutable catalog containing only stable id, label, and sealed-record URL. Selection enters the existing `eomRecord` bootstrap route, and replay still mounts without constructing a live EOM client. Claim grade: derived from the routing call graph and measured by focused tests. Falsifier: a catalog entry contains geometry or physics, selection bypasses `BorgBootstrap.js`, or replay constructs the EOM client.
- Added prescribed chart playback for the illustrative spindle member while keeping chart pose as its initial display. Borg shows `prescribed-geometry`, `chart-hypothesis`, `display-only`, and the no-physics declaration; its trails and animation are declared chart paths rather than simulated history. Claim grade: measured by record-ingestion tests and browser QA. Falsifier: Borg labels the record evolved, enables workspace run controls in replay, or displays a path not carried by the sealed record.
- The built-in catalog does not close the external packet/manifest carrier, synchronized comparison transforms, field-speed carrier, or spin/polarity-dipole carriers. Claim grade: derived from the catalog schema and adopted record contract. Falsifier: the instrument gate ratifies one of those carriers or the catalog begins carrying it.
- Live browser QA caught and closed two integration defects that source tests alone did not expose: prescribed coordinates were initially translated by the ordinary EOM center, and replay particle objects were initially built before the first sealed path ids arrived. The corrected chart uses its source-carried response center as the viewport origin and rebuilds particles on the first replay rows. Desktop and 390-by-844 captures show six sites, six ansatz curves, three antipodal binaries, one dotted shell, the global selector, co-rotating playback, and no horizontal overflow; warning/error logs were empty. Claim grade: measured. Falsifier: a fresh replay omits any red/blue site, translates the chart outside the shell, shows a second shell, overflows at 390 px, or logs a warning/error.
- Operator display refinements make Play switch from chart pose into prescribed-path playback, render one dashed source-carried axis row per binary, use light purple for chart curves, and double the desktop left rail from `310px` to `620px`. Coincident axes are not displaced to make them look separate. Claim grade: measured by the scene/runtime tests and browser QA. Falsifier: Play remains disabled in chart pose, an explicit axis row is omitted, the curve material differs from `0xc6b6ff`, the desktop rail is not `620px`, or the narrow layout overflows.
- Radius and other spindle variations are supported by the existing source-specification and generator route, but each variation must be a separate sealed raw record and catalog entry. No `I:M:O` mapping or new radius values were inferred in this change. Claim grade: derived from the generator/catalog boundary. Falsifier: Borg computes a variation in the browser or a variant record exists without a complete source specification.
- Consolidated replay status to one top `Chart hypothesis` lozenge, moved the record date into the viewport toolbar, placed the subtitle beside the Borg title, removed the redundant single-record selectors, and moved optional record, playback, provenance, and camera controls into collapsed drawers. Camera defaults collapsed and keeps its mode and three actions together. Claim grade: measured by desktop and 768-pixel browser QA. Falsifier: the replay surface repeats the claim label, horizontally overflows, or opens Camera by default.
- Added a full source-carried Braid Taxonomy table with one column per binary and rows for source id, radius, ordinary frequency, cap angle, axial spacing, phase, transverse radius, carrier speed, and polarity assignment. Long unavailable fields now wrap without squeezing their labels. Claim grade: measured against the sealed spindle record and browser render. Falsifier: a displayed cell differs from its record value or the table creates a horizontal page overflow.
- Prescribed chart records now carry `displayTrailPeriods`; the illustrative spindle record declares one period. At ordinary frequency $0.25$, Borg starts chart pose at $T=4$ and shows the full four-time-unit rotation behind every site. This is declared path-history display, not EOM solver evolution. Claim grade: derived from $1/0.25=4$ and measured by session tests plus browser playback. Falsifier: the displayed trail duration differs from the declared period count or replay invokes the EOM solver.
- Replay now draws geometrically coincident source-carried binary axes once as one neutral solid axis; opposite normal direction counts as the same axis, while a different line remains separate. The redundant load-state `Replay complete` chip and simulation-source Provenance drawer are hidden in replay, leaving the replay-specific Provenance drawer as the sole record provenance panel. Strobe controls share a bottom alignment, and the record-date lozenge uses the same six-pixel corner radius as the other viewport controls. Claim grade: derived from the axis grouping rule and measured by focused scene/runtime tests. Falsifier: the current three coaxial declarations render more than one axis, a noncoincident axis is lost, either duplicate replay panel remains visible, or the control geometry differs from the declared CSS.
- Removed the prescribed-workspace Display selector and hid the random-distribution control in replay. Chart pose remains the initial prescribed presentation and Play enters prescribed-path animation without exposing that internal transition as a user mode. The Path layer now owns both the recorded trail and chart-pose ansatz curves, Velocity owns only velocity vectors, and the duplicate `Diag` layer button is removed while the dedicated diagnostics-panel button remains. Claim grade: derived from the layer ownership wiring and measured by focused scene/runtime tests. Falsifier: replay shows either removed control, Path leaves a chart curve visible, Velocity changes trail visibility, or two diagnostics controls appear in the viewport toolbar.

### 2026-07-20 - Record-only Assembly Viewer Core

- Implemented the Assembly Viewer inside the canonical Borg runtime. `BorgBootstrap.js` selects record-only startup before live-client construction; `EomHistoryDataset.mjs` rejects foreign schemas, undeclared claim grades, missing required provenance, non-finite values, missing authoritative segments, inconsistent coverage, and incomplete evolved-segment provenance. Claim grade: derived from the call graph and measured by focused tests. Falsifier: a replay bootstrap invokes `createEomClient`, or any rejected fixture mounts successfully.
- Added focused session, control, and scene modules for persistent provenance, chart/evolved labels, delay-horizon trails, coverage-clamped playback, chart pose and ansatz curves, co-rotating camera, strobe, one-period loop, display-only swept envelope, static image export, raw source-order navigation, source filters, and source-carried $S_3$ grouping. Claim grade: measured by Node tests and local browser QA. Falsifier: a control produces a time outside record coverage, changes a workspace initial condition, sorts a raw record, or displays an inferred unavailable diagnostic.
- Browser QA at `http://127.0.0.1:5173` exercised simulation workspace, evolved replay, chart pose, two-record source-order navigation, incompatible comparison, co-rotation, strobe, looping, swept envelope, export, coverage clamping, and a 390-by-844 viewport. The viewport retained one dotted outer boundary shell and no continuous guides or central-ball sphere; browser warning/error logs were empty. Claim grade: measured. Falsifier: a fresh run shows enabled EOM controls in replay, an out-of-coverage frame, horizontal narrow-width overflow, a second shell, or a browser warning/error.
- Kept `assembly_viewer_replay_mode` open and contract-blocked. `assembly-view-record.v0` lacks ratified time/unit transforms for synchronized comparison, a multi-record packet/manifest carrier, a required field-speed carrier, and spin/polarity-dipole vector fields. Borg exposes these as missing-contract states and does not define a packet or silently assume identity transforms. Claim grade: derived from the adopted schema and runtime validation. Falsifier: the instrument gate ratifies any named carrier or an existing accepted field is found in the schema.
- Preserved `assembly-explorer.html`, its runtime, and its tests. Borg now matches raw source order, unquotiented record retention, optional source-carried $S_3$ grouping, source-only filtering, and raw-record navigation, but external collection intake and synchronized comparison remain blocked. `assembly_explorer_surface_disposition` therefore remains a separate pending architectural decision. Claim grade: derived from the live file set and measured by both runtime suites. Falsifier: any standalone explorer file or test is absent, or a Borg collection test loses the named navigation invariant.

### 2026-07-02 - Partition Resume Audit

- Resumed the paused Borg app work after reading the current priority partition policy in [../README.md](../README.md), then the live tracker, brainstorming file, and this work log before writing.
- Confirmed the active priority directory is `reference/priorities/app-borg/`; the old `reference/priorities/borg-app/` path is absent in the current branch state.
- Confirmed current focused sibling artifacts already hold the release/runtime decisions: [borg-release-budget-manifest.v1](borg-release-budget-manifest.v1.json) records measured browser runtime budget ceilings, and [borg-live-run-retention-policy.v1](borg-live-run-retention-policy.v1.json) records forever-mode display retention with compacted display-only path history.
- No loose conceptual notes were migrated into [brainstorming](brainstorming.md) during this audit. The live tracker remains compact because the current queue and next action are already stated there.
- Current tracker next build burden remains `build-native-wake-history-and-boundary-residual-fixture`: extend the EOM contracts and native implementation for retained wake/interaction rows, row-conservation counts, boundary-to-central residual rows, and acceleration-contribution diagnostics without app-local physics.

### 2026-07-16 - Assembly Viewer Consolidation

- Moved the operator-ratified assembly-viewer requirements from the Braid Program campaign directory into [assembly-viewer-requirements](assembly-viewer-requirements.md), because `app-borg` owns Borg design, requirements, and build handoffs while the Braid Program retains schema acceptance and campaign booking in its instrument gate.
- Mined the older Assembly Configuration Explorer priority packet and live runtime. Retained requirements: raw source-order preservation, `unquotiented-labeled` tri-binary rows, optional source-carried $S_3$ grouping without evidence deletion, local/manifest collection intake, stable-sector navigation, and source-carried branch/capture filters.
- Did not import the older runtime's app-computed speed regimes, $D_{\mathrm{plane}}$, energy differentials, permutation fingerprints, or demo rows into replay authority. Borg may display those values only when a source record or packet carries them with provenance.
- Recorded two distinct Borg modes: the simulation workspace may invoke the EOM solver after explicit Start; assembly-view replay consumes sealed `assembly-view-record.v0` files only. The old explorer priority directory was retired after its useful requirements were captured; its standalone implementation remains until a separate parity-based disposition task.

### 2026-07-17 - Spherical Envelope Geometry Epoch

- Operator decision implemented forward-only: Borg's app-layer envelope is one sphere with `outerRadius = 0.625`, `centralBallRadius = 0.5`, and `radialBufferMargin = 0.125`; the EOM solver remains unaware of this display/seeding bookkeeping. Claim grade: derived from the manifest and call graph. Falsifier: any EOM request or engine type contains an envelope-shape field, or any active Borg source retains the removed cube-envelope fields.
- The population rule is $N_{\mathrm{calc}}=\lceil N_{\mathcal C}((r_{\mathrm{central}}+b_{\mathrm{radial}})/r_{\mathrm{central}})^3\rceil$. The current values give `5.859375` before the ceiling and preserve the 3:3 total of six. Claim grade: derived arithmetic. Falsifier: `validateBorgManifest` computes a different ceiling from the declared radii and central count.
- Seed positions use bounding-box rejection sampling but accept only points inside the central ball; this produces the correct uniform-volume radial law before the unchanged pair-separation rejection. The 64-path seed-0 control uses the same random route and retains the `0.2` minimum-separation certificate. A dense request that cannot be placed inside the declared attempt budget fails closed instead of changing distribution family. Claim grade: derived algorithm plus measured tests. Falsifier: a seeded row has radial distance above `0.5`, the single-particle mean of $(r/0.5)^3$ leaves the declared test band, or the 64-path seed-0 separation certificate fails.
- The app's causal-history separation bound is the sphere diameter `2 * outerRadius = 1.25`; with zero initial speed and `0.01` safety/sample interval, the default history depth is `1.26`. Claim grade: derived and measured in the browser source panel. Falsifier: the bootstrap or new-distribution path passes any other geometry bound, or the browser reports a different default history depth.
- Browser verification at `http://127.0.0.1:5173/borg.html` showed three great circles for each of the central ball and outer shell, correct camera fit, the new envelope fields, and no console errors. Claim grade: measured with the Codex in-app browser. Falsifier: a fresh local load crops either shell, renders a filled/Cartesian envelope, shows an old field, or logs a runtime error.
- Operator visual adjudication rejected the two three-circle sphere guides. The current viewport instead uses the Ideal Noether Braid surface-sample pattern for one dotted outer boundary shell, with no second rendered central-ball sphere. Claim grade: measured by source inspection and a fresh browser render. Falsifier: Borg renders any `LineLoop`, more than one shell point cloud, or a central-ball guide.
- Operator follow-up aligned the rendered and seeded volumes: the dotted shell now uses the Ideal Noether Braid point opacity `0.88`, and every initial position is sampled uniformly through the outer spherical envelope rather than placing the total outer population inside the smaller central ball. Claim grade: derived from the shared `outerRadius` consumer and measured by the radial-law test. Falsifier: the renderer opacity differs from `0.88`, the sampler consumes `centralBallRadius`, or the normalized cubic-radius mean leaves its declared test band.
- Operator startup-default update set `outerRadius = 0.5`, retained the density ratio with `centralBallRadius = 0.4` and `radialBufferMargin = 0.1`, set `coupling = 0.05`, set the per-axis speed maximum to `0.03`, and made each browser startup draw a fresh recorded seed. The envelope now combines the bright light-gray dotted shell with three orthogonal light-gray great circles at that same radius. Claim grade: derived from the manifest/runtime wiring and measured by browser and startup-seed controls. Falsifier: two injected startup seeds produce identical rows, any startup value differs from the declared defaults, or any circle uses a radius other than `outerRadius`.
- The rebuilt `eom_borg_shadow_cli` was newer than its relevant `CoupledEvolution.cpp` and Borg CLI sources. The seed-0 3:3 display-grade profiler, using `h_max = 0.025`, first warned at `T = 1.80234375`; chunk `[1.80,1.85]` cost `1.57084 s`. It continued to accepted `T = 1.99619375`, then halted `caustic_entry_uncertified` because root completeness remained uncertified. Claim grade: measured by `scripts/eom/profile-borg-incremental-chunks.mjs` on 2026-07-17. Falsifier: the same binary, manifest, seed, and controller settings produce a different first-warning time or terminal record.
- A six-chunk browser run failed its strict run-grade provenance check because the EOM response exposed internal routed-pair retry bookkeeping as `causticWarningPairs` while reporting zero accepted warnings. The public pair ledger now contains only pairs carried from prior accepted warnings or added by warnings accepted in the current chunk; routed-pair state remains internal. The seed-0 display-grade profiler reproduced the contradiction before the fix at `[0.35,0.40]` and, after rebuilding, continued through twelve chunks to `T = 0.6` with warning count, warning time, warning rows, and warning pairs mutually consistent. Claim grade: measured by the profiler and the native evolution regression fixture on 2026-07-17. Falsifier: a response with zero warnings again carries a nonempty public warning-pair ledger, or the same twelve-chunk control again trips `inconsistent run-grade provenance`.
- The startup camera now fits the visible outer envelope instead of the unrendered central ball. With the unchanged `outerRadius = 0.5`, the camera distance increases by 10% from the prior startup framing. The shell dots and all three great circles consume the same light-gray color and opacity constants. The run-grade warning chip remains present for warned encounters but an explicit `[hidden]` rule removes its empty lozenge before the first warning. Claim grade: derived from the camera ratio and measured in a fresh Borg browser render on 2026-07-17. Falsifier: startup framing changes the declared radius, dots and circles resolve to different guide constants, or the empty third status lozenge is visible before a warning.
- The operator-set startup maximum for each velocity component is `0.01`. The manifest validator, initial-settings form, seeded-position controls, and current design packet consume that one default; explicit nondefault fixtures retain their own test values. On the seed-0 3:3 display control with `h_max = 0.025`, changing only this bound from `0.03` to `0.01` moved the terminal coupled-correction boundary from `T = 0.683203125` to `T = 0.55390625`; therefore the lower speed is a configuration decision, not a demonstrated cure for the correction boundary. Claim grade: derived wiring and measured paired control on 2026-07-17. Falsifier: a fresh initial-settings pane starts at any value other than `0.01`, a seeded component exceeds it, or the paired profiler does not reproduce the stated boundaries.
- The coupling control now occupies half of its former full-width row. Its other half contains `Step height` and `Adaptive minimum`: the applied height supplies both `initialStep` and `maximumStep`, while the applied minimum supplies `minimumStep`. The panel rejects values outside `0 < minimum <= height <= 0.05` rather than exposing an unreachable height above the chunk length. Claim grade: derived from the runner-option mapping and measured by the Borg runtime controls. Falsifier: an applied edit does not appear in the next run request, adaptive growth exceeds the applied height, or the controller attempts a height below the applied minimum.
- Operator visual decision removed all three great-circle guides from the envelope. The viewport retains exactly one light-gray dotted outer boundary shell and no central-ball guide. Claim grade: derived from the scene graph and measured in the live Borg viewport on 2026-07-17. Falsifier: the runtime constructs a `LineLoop`, the viewport shows a continuous spherical guide, or more than one envelope shell is rendered.
