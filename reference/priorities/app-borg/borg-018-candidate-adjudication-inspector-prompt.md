Closure goal: Implement BORG-018 so every exact assembly inspected in Borg presents a current, identity-bound, fail-closed account of its modeling, analytical, bounded-release, and retained-branch evidence without converting unknown, unindexed, or scoped results into global falsification.

# BORG-018 Candidate-Adjudication Status Inspector

Resume the Borg priority lane from live state and complete [BORG-018](work-queue.md#borg-018--candidate-adjudication-status-inspector). This is an implementation task with authority to edit the Borg priority packets, the Assembly Library and corresponding workbench inspection surfaces, the Borg library service and focused modules, a versioned machine-readable adjudication projection, and focused tests. Preserve unrelated work in the shared checkout. Do not change a scientific disposition in the Braid Program merely to simplify the app.

## Startup

Run `git status --short --untracked-files=all`, then read `AGENTS.md`, `reference/op/agent-startup-orientation.generated.md`, `reference/research-office/cto/prompts/priority-lane-resume.md`, `reference/priorities/README.md`, and the live Borg `priorities.md`, `work-queue.md`, `requirements-and-design.md`, `assembly-identity-relation-contract.md`, `work-log.md`, and `brainstorming.md` when present. Read the complete BORG-018 row before acting. Then read the current Braid Program owner at `reference/priorities/braid-program/braid-candidate-requirement-adjudication.md`, its directly linked controlling evidence needed for the first implementation set, the current `library-finding-relations` schema and consumer, the exact-assembly inspector code, and the applicable UI and software-architecture canon. Do not rely on remembered counts or statuses; remeasure the live matrix and current catalog.

## Scientific contract

Treat the adjudication owner as scientific authority and Borg as a read-only consumer. Preserve the meanings of `H1` through `H5` exactly:

- `H1`: closed declared state and identity-bearing model inventory;
- `H2`: geometric and coincidence admissibility;
- `H3`: causal admissibility and required root coverage;
- `H4`: nonzero-horizon ordinary EOM compatibility without prescribed continuation; and
- `H5`: a positive-width retained or metastable branch with the declared return, continuation, prehistory, and neighborhood obligations.

Plainly: an `H4` pass says that ordinary evolution followed one realization for a bounded interval. Only an accepted `H5` pass establishes a retained branch, and any stronger stability word must be supported by the scientific owner's equilibrium and neighborhood obligations.

Enforce this verdict vocabulary in data, rendering, tests, and documentation:

- accepted `H5 P[...]`: `Retained branch established`;
- `H5 U`: `No retained branch established yet`;
- exact `H4 F[...]`: `This exact realization failed bounded release`;
- actual `H5 F[...]`: `Retained-branch claim falsified within the tested scope`;
- bounded search with no success: `No solution found in the searched domain`, with the domain named;
- no exact relation: `No adjudication linked`; and
- invalid or outdated relation: `Projection stale or invalid`, with the cause.

Never translate `U`, missing output, an unindexed configuration, or absence of a successful row into falsification. Never extend a measured failure from one realization, slice, horizon, parameter envelope, or instrument domain to a whole candidate family. Use a global falsification statement only when a source-owned covering theorem, exhaustive certified search, or equivalent accepted argument explicitly reaches the entire named family and predicate.

## Data and identity architecture

Do not parse human Markdown prose or table layout in the browser. Design one versioned machine-readable adjudication projection. Extend or supersede the current finding-relation path cleanly; do not leave two canonical evidence-status implementations. The projection must be owned or explicitly accepted by the Braid Program and checked against the current adjudication source.

Every relation must use a stable opaque candidate/evidence relation id and bind only through exact `assemblyId + modelRevisionSha256` when exact equivalence is justified. Carry the adjudication revision and source anchor, requirement state and claim grade, exact tested realization and domain, horizon and instrument where applicable, evidence links, assumptions, `establishes`, `doesNotEstablish`, `currentBlocker`, and an operator-checkable `falsifier`. Encode broader-family and slice-only relations explicitly; do not make them exact configuration relations. Labels, filenames, source paths, visual resemblance, current preview geometry, and group representatives are not identity evidence.

Fail closed on duplicate relations, unsupported tokens, missing anchors, broken evidence links, exact-identity mismatch, changed model revision, stale adjudication revision, and incomplete group coverage. Preserve active, superseded, and withdrawn lifecycle states; only current accepted relations contribute to a current verdict. Prove that presentation renaming preserves a valid binding and that identity-bearing scientific change invalidates it.

## Inspector behavior

Add a dedicated scientific-status panel to the Assembly Library exact-record inspector and the corresponding exact-assembly inspection surface in the Borg workbench. Reuse a focused shared view model or renderer where practical so both surfaces use the same language and evidence state.

The panel must include:

1. a concise overall status with no score-like compression;
2. an accessible `H1`--`H5` table showing pass, fail, unknown, or not applicable and the claim grade;
3. Modeling, Analytical, Simulation, and Retention sections;
4. tested realization, parameter domain, assumptions, horizon, and instrument where carried;
5. `Establishes`, `Does not establish`, `Current blocker`, and `Falsifier` statements;
6. direct links to controlling evidence; and
7. projection source, revision, lifecycle, and freshness.

Explain unavoidable technical terms immediately in plain language. Preserve exact scientific language at its source grade. Borg must not calculate dynamics, causal roots, equilibrium, return, basin width, or stability locally.

An exact leaf receives only its own exact evidence plus explicitly labeled broader-context relations. Evaluate evidence before grouping. A variant set or other group may display aggregate coverage only when it counts exact members separately by pass, scoped fail, unknown, unindexed, and stale state. Never transfer the example preview's adjudication to the group. If complete aggregation cannot be established, show incomplete evidence coverage and require member drill-down.

## Current-state audit

Before implementation, produce a read-only census of the live adjudication and its exact Borg mappings. The current expected boundary is three candidate rows with bounded `H4 P[M]` evidence and zero `H5` passes, with most `H5` cells unknown and negative results scoped to named realizations or requirements. Treat this as a drift-sensitive expectation to verify, not a hard-coded UI constant. Identify every matrix row that cannot be bound exactly to a current Borg configuration and preserve it as unlinked or broader context rather than inventing a match.

## Validation and acceptance

Add focused coverage for:

- schema validation and current-owner revision binding;
- exact-identity matches and mismatches;
- label rename versus model-revision change;
- `H5 U` wording and prevention of automatic falsification;
- scoped `H4 F` and `H5 F` rendering;
- missing, unindexed, stale, superseded, and withdrawn relations;
- slice-only versus whole-candidate scope;
- broken evidence links and unsupported state/grade tokens;
- exact filtering before variant grouping;
- aggregate group counts and representative noninheritance;
- saved exact-selection links and accessible machine-readable status hooks; and
- parity of the two Borg inspection surfaces.

Run browser QA at desktop and narrow widths over prescribed-only, accepted `H3`, bounded `H4` pass, scoped failure, `H5 U`, and unindexed configurations. Confirm readable layout, working evidence links, correct source/revision display, and zero warning/error logs. Run the focused Borg suites, strict content validation, applicable check-only generators, and `git diff --check`. Do not run generator `--write` commands unless separately authorized by repository policy.

Update the Borg requirements and work log with the implemented contract, exact coverage census, validation receipts, claim grade, and falsifiers. If BORG-018 is actually complete, apply the work-queue lifecycle rule; otherwise leave it open with the precise remaining blocker. Report scoped edits separately from ambient shared-checkout work.

Closure goal: Deliver an exact-identity scientific-status inspector whose strongest verdict never exceeds the current Braid Program evidence and whose missing-data behavior cannot be mistaken for falsification.
