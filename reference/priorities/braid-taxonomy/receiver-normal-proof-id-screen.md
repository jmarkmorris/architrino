# Receiver-Normal Proof ID Screen

Claim level. Priority-only audit packet. This file screens `NB-0`, `NSH-0`, and `PL-*` retained-branch language against the current receiver-normal branch-strength standard. It does not change any proof status, certify any branch, or reject any branch family.

## Screening Rule

Geometry-only, taxonomy, support, chart, and negative-diagnostic material can remain where it is. A retained-branch, force/action, stability, observer-export, pressure, or mass-response claim may consume a packet only after the same branch record supplies:

- accepted branch identity and history segment;
- active causal-root identities on the same support and event convention;
- same-record $D_s$, $D_T$, and $W^{\mathrm{rec}}=\lvert D_T/D_s\rvert$, or the runtime fields `sourceNormalDenominator`, `receiverNormalNumerator`, `receiverNormalFactor`, `unsignedReceiverNormalFactor`, and `branchWeight`;
- action/energy, event, wake, stability, convergence, and support rows consumed by the claim.

Source-normal, Jacobian-only, quotient, fixture, frozen-ledger, old force-residue, app-provider, or cross-row evidence remains diagnostic until those rows are rebuilt on one branch record. The cleanup status for such evidence is `diagnostic-only`, `not_retained`, or `receiver-normal-restart-required`, depending on the packet's own vocabulary.

## Screened Proof IDs

| Proof ID | What survives without restart | What needs receiver-normal restart | Current decision |
| --- | --- | --- | --- |
| `NB-0` | Neutral six-site inventory, base certificate theorem target, fixed rigid octahedral root-ledger success marker, scoped no-go diagnostics, frozen-ledger speed-ODE obstruction, and support-open finite-mode search. | Any claim that the neutral branch is retained, force/action balanced, action-derived, stable, observer-export ready, or mass/pressure consumable from source-normal/Jacobian/frozen-ledger rows. | Keep `NB-0` as the broad no-declared-shell rest target. Do not promote retained-branch claims until a live neutral branch record emits same-record receiver-normal root weights plus action, event, stability, convergence, and support rows. |
| `NSH-0` | Nested shell reduction row, binary-partition and radial-support theorem targets, nested support geometry, role-map language, and shared-certificate scaffolding. | Any claim that nested support has retained a branch, that exact binary rows are active, that rank-three frame rows are retained, or that moving/mass/topology exports can consume the nested branch before same-record receiver-normal closure. | Keep `NSH-0` as the three-support-band rest target. Treat $A_0$, mass-response, observer-export, and topology material as support-dependent downstream consumers until the retained nested branch certificate exists. |
| `PL-NB-*` | Lower-rank neutral comparison language and chart-routing notes. | Any claim that lower-rank neutral behavior is retained or observer-export ready without a declared lower-rank neutral branch record and receiver-normal rows. | `PL-NB-0` remains not started; `PL-NB-L` remains blocked until a retained `PL-NB-0` branch exists. |
| `PL-SH-*` | Possible lower-rank shell comparison language and geometry-only shell material. | Any claim that a lower-rank shell branch retained, or that moving export can consume it, before the local packet declares one-band shell support and same-record receiver-normal rows. | `PL-SH-0` remains unassigned; inspect local shell packets before mapping. `PL-SH-L` remains blocked until a retained `PL-SH-0` branch exists. |
| `PL-NSH-*` | Planar reduced-chart, $D_{\mathrm{plane}}=0$ wall, terminal-boundary, and photon-bridge material when stated as chart or downstream evidence. | Any claim that planar lower-rank nested shell behavior is a retained branch rather than a chart-boundary or terminal diagnostic without a declared retained lower-rank nested record. | `PL-NSH-0` remains chart-boundary started, not retained. `PL-NSH-L` remains blocked until a retained `PL-NSH-0` branch exists. |

## Authored-Corpus Patch Decisions

- [Noether Braid Proof Map](../../../content/markdown/aaa/noether-braid/noether-braid-proof-map.md) now carries cleanup rows for `NB-0`, `NSH-0`, and `PL-*`.
- [Noether Braid Taxonomy](../../../content/markdown/aaa/noether-braid/noether-braid-taxonomy.md) now names receiver-normal branch-strength rows in the proof-search roadmap and retained-branch status.
- [Nested Shell Braid Dynamics](../../../content/markdown/aaa/noether-braid/explored-braid-geometries.md#nested-shell-braid-dynamics) now describes the planar reduced chart as projecting declared branch rows until the residual closes, rather than assuming retained rows at the chart-definition step.

## Next Audit Boundary

Do not bulk-edit the large shell-braid or proof-program corpora from this screen. If a later cleanup touches one of those packets, inspect the packet locally and classify it as geometry/taxonomy, row evidence, diagnostic/rejection, retained-branch candidate, downstream consumer, or uninspected corpus before changing retained-branch language.
