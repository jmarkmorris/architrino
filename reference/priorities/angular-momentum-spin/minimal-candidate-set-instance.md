# Minimal Candidate-Set Instance

Status. Priority instance for `tri_binary_partition_rule`, downstream of [finite-branch-candidate-set-packet.md](finite-branch-candidate-set-packet.md), [branch-selection-law-packet.md](branch-selection-law-packet.md), and [minimal-four-substep-certificate-instance.md](minimal-four-substep-certificate-instance.md). This file instantiates the finite retained candidate-set format around the clean minimal four-substep branch. It is priority material only and does not promote branch uniqueness, spinor closure, measurement response, or Bell recovery to theorem status.

Claim level. Defer with blocker. The packet shows how the solved reduced minimal branch enters a finite candidate audit trail. The algebraic scalar and vector rows inherited from the minimal certificate remain conditionally populated, and finite reduced competitor controls are now populated, but root replay, phase lock, torque consistency, normalized causal-wake pullback, section stability, retained energy routing, and retained competitor exclusion are not yet evaluated. The retained candidate is therefore blocked rather than forbidden.

Promotion decision. Defer with blocker. Promote only after at least one finite retained candidate set has populated root, wake, torque, phase, stability, and routing rows, and after the deterministic branch-selection residual can compare all evaluable retained candidates.

## Instance Inputs

Fix the minimal transition window

$$
W_{\min}=[t_i,t_f],
$$

the reduced pre-branch chart $B_{\min}^-$, and the coupling datum

$$
\Gamma_{\min}
=
\left(
+1,
\Delta E_{\mathrm{tx}},
\Delta\mathbf J_{\mathrm{tx}},
\hat{\mathbf a},
\mathrm{Geom}_{\min}
\right).
$$

The retained budget is the smallest budget that can represent one continued branch word, one layer-retune block for each layer, and the two inner self-hit rows named by the reduced minimal certificate:

$$
N_{\min}
=
\left(
N_{\mathrm{act}}^{\min},
N_{\mathrm{inact}}^{\min},
N_{\mathrm{fold}}^{\min},
N_{\mathrm{sep}}^{\min},
N_{\mathrm{grp}}^{\min},
N_{\mathrm{route}}^{\min},
N_{\mathrm{box}}^{\min}
\right).
$$

The present instance does not assert that $N_{\min}$ is globally minimal. It is a local audit budget for replaying the already-solved reduced branch inside the finite-candidate-set contract.

## Candidate Set

The generated set for this reduced instance is

$$
\mathcal A_{N,\min}
\left(
B_{\min}^-,
\Gamma_{\min},
W_{\min}
\right)
=
\{\mathfrak a_{\min}\}.
$$

The retained candidate record is

$$
\mathfrak a_{\min}
=
\left(
B_{\min}^+,
\boldsymbol{\Delta I}_{\min},
\mathrm{core},
\mathfrak m_{B_{\min}^+},
\lambda_{\min},
\mathcal P_{\min}^{\mathrm{red}},
\mathcal Q_{\min}^{\mathrm{iso}},
\upsilon_{\min}
\right),
$$

with

$$
\boldsymbol{\Delta I}_{\min}
=
\left(
\Delta I_{\text{inner}}^{\min},
\Delta I_{\text{middle}}^{\min},
\Delta I_{\text{outer}}^{\min},
\Delta I_{\text{wake}}^{\min}
\right).
$$

The reduced certificate supplies the conditional algebraic rows

$$
\mathcal R_I^{B_{\min}}=0,
\qquad
\mathcal R_{\mathbf J}^{B_{\min}}=\mathbf 0,
\qquad
\mathcal R_{\perp}^{B_{\min}}=\mathbf 0,
$$

under the fixed-normal, no-transport, no-retained-wake assumptions recorded in [minimal-four-substep-certificate-instance.md](minimal-four-substep-certificate-instance.md). Those rows identify the candidate. They do not make it evaluable for deterministic branch selection.

## Audit Partition

The instance audit partition is

$$
\mathcal A_{N,\min}^{\mathrm{eval}}=\varnothing,
\qquad
\mathcal A_{N,\min}^{\mathrm{blk}}=\{\mathfrak a_{\min}\},
\qquad
\mathcal A_{N,\min}^{\mathrm{excl}}=\varnothing.
$$

Equivalently,

$$
\upsilon_{\min}=\mathrm{blocked},
\qquad
\mathfrak a_{\min}\in\mathcal A_{N,\min}^{\mathrm{blk}}.
$$

The candidate is not locally excluded because no row proves an impossible branch, invalid parity, unsourced nonzero wake term, or forbidden routing event. It is blocked because the retained data needed by the branch-selection residual remain absent.

Reduced solver projection 2026-06-20, updated for unordered search semantics 2026-06-21 and solver metadata on 2026-06-22. [tri-binary-offset-family-runner.mjs](../../../scripts/angular-momentum/tri-binary-offset-family-runner.mjs) now emits raw search labels `B_1:B_2:B_3`, role-assigned `I:M:O` projection fields, and outer-normalized translation metadata. The role labels are a candidate role map on an unordered three-binary row in the general domain $\widetilde{\mathcal C}_{3B}$, not the primary search semantics. The same reduced rows therefore remain useful evidence, but a retained-promotion replay must preserve the raw labeled records $(B_1,B_2,B_3)$, report any $S_3$-equivalent repeats, and compute any `inner`, `middle`, or `outer` interpretation as a post hoc role assignment. It supplies a reduced branch-chart projection for the role-assigned $(f+2,f,f-1)$ middle-hinge candidate family, the $(f+1,f,f-1)$ symmetric control, the dyadic $(4f,2f,f)$ control, and finite general integer-lock controls $(nf,mf,f)$ with outer-normalized translation $1:m:n$. This adds machine-readable proxy evidence for sampled root population, sampled active-row lineage, same-row force / partition / torque / wake diagnostic row IDs, Jacobian floor, outer speed, middle hinge, inner self-hit, integer-cycle phase closure, solver phase-at-hit rows, the index-level self-root parity target, the clean energy-frequency target $\omega_\ast$, and a frequency-triplet search summary. It selects `index-ratio:f2` as the first partial retained-lineage / phase / torque-wake diagnostic payload because that case has the largest positive inner self-hit span separation. The selected time-window torque probe adds 65 trapezoidal fixed-receiver samples over $W$ on the same endpoint row IDs, giving residual norms $3.009907861145633\times10^{-14}$ for the outer row, $0.23789379538011096$ for the middle row, and $0.13919373729527945$ for the inner row under the unit diagnostic convention. The older work-integral wake reconstruction is populated only as a diagnostic: the layer torque integral sum has $z=0.09870005808480142$, so the no-boundary wake-torque reconstruction has $z=-0.09870005808480142$. The selected binary-to-binary path-history probe adds 32 linearized path segments per layer, 96 path rows, and 9,216 source/receiver segment-pair checks, producing 1,477 candidates and 606 refined roots/hits across all nine layer-pair channels without truncation. The replayed binary-to-binary root-ledger detail pass builds 2,612 detail rows for 950 sampled hit candidates, including 606 active-root detail rows and 1,056 inactive-gap rows, with maximum active normalized residual $9.896914280289656\times10^{-14}$. The replay now classifies 941 chronological snapshot edges through the solver root-ledger transition classifier, producing 775 transitions: 379 retained, 173 appeared, 174 disappeared, 49 folded, and no ledger-rerun rows. Inactive-gap margins are populated with 1,056 finite gap rows, maximum width $0.1963495408493623$, and mean width $0.13760504931328713$. All nine layer-pair channels have some retained hit-time coverage, but their common retained time-domain intersection has no positive width; it only touches at two hinge-point candidates, $t=1.3744467859455345=7\pi/16$ and $t=1.5707963267948966=\pi/2$, with cycle fractions $7/32$ and $1/4$. The point-event witness pass populates all nine layer-pair witnesses at both hinge candidates, with the same retained root key `2856731379702547500` and boundary-only incidence in every channel. The point-event diagnostic separates the three diagonal identity witnesses from the six off-diagonal force-bearing channels. At both hinge candidates, all three diagonal identity witnesses populate, all six off-diagonal force-bearing endpoint diagnostics populate, and the off-diagonal unit point-torque cancels below the candidate tolerance $10^{-12}$: $2.7755575615628914\times10^{-16}$ at $7\pi/16$ and $2.7755575615628914\times10^{-17}$ at $\pi/2$. The candidate point-event admissibility row therefore passes at both hinge candidates. The branch-transport incidence diagnostic also passes at the layer-role level for both hinges. The $7\pi/16$ hinge remains pair-map count-blocked, with one incoming-only channel, two outgoing-only channels, and same-source endpoint residual $0.47994903798433036$ on its only attempted match. The $\pi/2$ hinge now has a populated topology pair-map: two incoming-only channels and two outgoing-only channels match by one same-source middle route and one same-receiver middle route. It also has one middle field-speed hinge-capture candidate: the middle layer has speed residual $0$ against tolerance $0.015$, both pair-map routes run through the middle layer, and two of two hinge-chart rows pass with maximum chart residual $0$. It is still delayed-endpoint-geometry blocked because only one of two endpoint-continuity rows passes: the same-receiver middle residual is $0$, while the same-source middle row has source-point residual $0.2032833773475938$, middle emission-clock jump $0.2085874165895798$, wrapped middle phase jump $0.4171748331791605$, and maximum causal-endpoint-to-hinge-chart residual $0.6280004778252143$ against tolerance $10^{-12}$. A single-f retained scan over `index-ratio:f3` through `index-ratio:f8` did not rescue the middle-hinge family: f=3 and f=5 through f=8 have no common retained hinge point, while f=4 has one retained hinge point but no topology pair-map because all four one-sided channels are incoming-only. These rows do not change the audit partition above because the point-event, transport-incidence, topology pair-map, hinge-time chart, and middle field-speed hinge-capture rows are candidate-only with `retainedBranchClaim=false`, only the `outer->outer` channel carries a common active root key across its whole replay path, the other eight layer-pair channels have only partial retained chains, the middle and inner fixed-receiver torque residuals are nonzero, the diagnostic wake reconstruction is nonzero before any boundary term, the inner rank-zero samples coexist with additional active roots over part of $W$, and time-integrated retained torque consistency, binary-to-binary retained phase lock, retained physical vector partition, energy routing, section stability, and retained competitor rows remain missing; the route-local unit vector-partition balance target below is not a retained vector-ledger acceptance. The machine-readable blocker is now narrower: torque consistency requires a retained point-event rule, geometrically continuous branch-transport pair-map, or retained chart row that authorizes hinge-time chart transport despite the same-source delayed-emission discontinuity or upgrades those boundary-only hinge contacts into an accepted retained point event; wake completion now requires an action-boundary wake-energy increment law on the accepted normalized boundary charge and accepted retained crossing-domain pullback.

Frequency-triplet ranking update 2026-06-20. The same runner now emits `branchCertificateFamilyRanking` in the frequency-triplet search summary. That ranking treats inner self-hit span as a tie-breaker only after branch-certificate criteria, so it separates the representative `index-ratio:f2` span diagnostic from family disposition. In the current role map, the middle-hinge family $(I,M,O)=(f+2,f,f-1)$ ranks first with self-root parity proxy coverage 14/14 and all seven phase-lock cases covered. The adjacent integer-lock controls $(3f,2f,f)$, $(4f,3f,f)$, and $(5f,4f,f)$ only hit that proxy at $f=2$; the dyadic and high-span integer-lock controls have zero self-root parity proxy coverage. This keeps the middle-hinge role map on the priority path while leaving `retainedBranchClaim=false`.

Equal-frequency candidate addition 2026-06-21, updated for unordered search semantics. The priority set now includes the common-frequency energy-radius candidate, tracked in [equal-frequency-energy-radius-candidate.md](equal-frequency-energy-radius-candidate.md). Its raw form is $(f_1,f_2,f_3)=(f,f,f)$ on the unordered labeled row $(B_1,B_2,B_3)$; the legacy $(I,M,O)=(f,f,f)$ notation is only a candidate role map attached after a retained branch supplies inner/middle/outer roles. It should not be treated as an ordinary low-scoring competitor under the current `self_root_parity_index_proxy` ranking, because that proxy tests the offset hypothesis $n_I-n_M=2$ and gives zero by construction for equal frequency. The equal-frequency candidate asks a different question: can the three binaries share one frequency while retained branch energy places them at different effective lever arms and speeds inside the velocity-deforming tri-binary Noether braid,

$$
s_O=\rho_O\omega_f<c_f,
\qquad
s_M=\rho_M\omega_f\approx c_f,
\qquad
s_I=\rho_I\omega_f>c_f,
$$

with phase offsets, root multiplicity, wake routing, or action partition carrying the doubled inner burden? It also changes the transaction-frequency burden because $\omega_\ast=(\omega_O+\omega_M+2\omega_I)/4$ collapses to $\omega_f$ under the displayed role map. The v35 solver report keeps this as the descriptor-driven `equal-frequency-energy-radius` search lane paired with the `equal-frequency` policy. The primary branch model is still `retained-tri-binary-noether-braid-deformation-branch`; the current executable chart is named `flattened-limit-effective-lever-arm-projection-chart`; and `aaa-tri-binary-noether-braid-branch-state-contract.v4` makes the retained branch state $B_{3B}(q,v)$ the source object for frequency, phase, deformation, return-period or locked-harmonic frequency rows, plane-sector rows, coupling rows, and event/domain binding. The $2\le f\le8$ replay emits `aaa-equal-frequency-energy-radius-audit.v34`, `aaa-tri-binary-frequency-triplet-search-summary.v36`, and `aaa-tri-binary-frequency-triplet-candidate-set-review.v32`: 35 equal-frequency cases populate, with seven priority `triadic-120` cases and 28 phase controls. The v32 candidate review also emits `aaa-tri-binary-frequency-triplet-comparison-axis-review.v2`, which compares equal-frequency, offset, dyadic, and finite $m:n$ candidates on frequency, radius/velocity, phase, and retained-acceptance evidence rather than letting the old offset proxy exclude another family. It also emits `aaa-tri-binary-frequency-triplet-residual-matrix-review.v3`: ready=0; equal-frequency is strongest with accepted=0, currentProxy=7, partialProxy=1, missing=0; the new r_E and r_J current-proxy entries come from `aaa-equal-frequency-energy-angular-momentum-closure-audit.v3`; its nested `aaa-equal-frequency-sigma-hbar-scale-law-audit.v2` reports that the finite scale candidate is frequency-linear across the sampled rows while a universal constant scale fails, and `aaa-equal-frequency-sigma-hbar-scale-law-provenance-audit.v1` reports that the frequency-linear proxy is induced by current $\rho_a\omega_f$ lever-arm normalization plus fixed target inertia until a retained branch-scale law is derived. The `aaa-equal-frequency-rho-omega-deformation-slice-audit.v1` reports that the target $\rho_a\omega_f$ speed split passes on the current oblate slice and flattened-limit projection, fails on the restricted spherical endpoint and formal flat-limit forecast, and is therefore not an all-slice deformation invariant. The `aaa-equal-frequency-field-speed-hinge-slice-selector-audit.v1` selects only `current_oblate_slice` and `flattened_limit_projection` by requiring middle at $c_f$, inner above $c_f$, outer below $c_f$, and the exact target speed split; retained slice-selection acceptance remains zero. Its nested `aaa-equal-frequency-field-speed-hinge-slice-selection-functional-audit.v1` turns that selector into a squared residual target with stable zero minimizers `current_oblate_slice` and `flattened_limit_projection`, positive spherical and flat-forecast control scores, and zero retained functional acceptance. The `aaa-equal-frequency-slice-energy-action-independence-audit.v1` shows that unit inertia independently selects the same two slices for the inner half-action condition, while full $2:1:1$ action selection remains blocked because unit inertia misses the middle/outer split and the target-weighted effective-inertia row also accepts the spherical positive-lever-arm control. The new `aaa-equal-frequency-wake-coupling-transfer-target-audit.v2` converts that selected-slice miss into a stable signed transfer target: in `I:M:O` order, $\Delta\ell=(0,-0.07,+0.07)$ across all seven sampled priority rows and both hinge-selected slices, so the retained burden is now a middle-to-outer wake/coupling angular-momentum transfer law on $S_{\mathrm{eq}}$. Its nested `aaa-equal-frequency-wake-coupling-transfer-law-scan.v1` reports `quadratic_middle_outer_pair_equalization_exact_on_hinge_slices_retained_law_missing`: the quadratic middle/outer pair-equalization law is exact on 14 of 14 selected entries and zero controls, while direct target residual is tautological and linear/full-gap laws fail. `triadic-120` still ranks first by equal-spacing residual, but the phase-deformation balance audit reports the blocker: phase-only balance passes seven of seven priority rows, while effective-lever-arm weighted, unit-inertia action weighted, and target $2:1:1$ root/action weighted balance each pass zero of seven rows. The phase-lattice audit scans 144 rows at $30^\circ$ spacing and separates two exact phase-only triadic rows from one exact $2:1:1$ weighted-action opposition row, `phase-lattice-12-i0-m6-o6`, with zero overlap. The return-period audit still has seven reduced-chart passes and zero retained certificates; the plane-sector audit still classifies the current rows as coplanar planar-$\mathbb{Z}_3$ rows with zero retained sector certificates. The retained frequency/phase packet now creates seven $S_{\mathrm{eq}}$ row-level packet candidates for $f=2,\ldots,8$: all seven have current return-frequency, coplanar-sector, and triadic holonomy proxy evidence, and zero have retained acceptance. The retained row-set scaffold names $S_{\mathrm{eq}}$ as one finite retained branch chart for the same six-body polarity-neutral Noether braid, with current proxy evidence from seven subaudits, including the same-clock energy/angular-momentum closure audit, zero retained acceptance, and 14 row-set blockers. The retained event/domain lift target is populated current-proxy-only, and the retained event/domain selector chooses `point_event_current_proxy_first` with one current-proxy route and zero accepted routes: accepted full point-event rule, positive-width retained domain, global retained row-set identity, binary-to-binary retained history rows, retained payload rows, retained energy routing, same-event momentum/angular-momentum rows, and Noether sea event/domain binding remain false. The retained replay acceptance blueprint now defines $R_{\mathrm{eq}}=(r_{\mathrm{rows}},r_{\mathrm{evt}},r_P,r_{\rho},r_E,r_D,r_W,r_J)$ on one retained row set and one accepted retained event or positive-width retained domain; it reports eight current-evidence conditions, zero accepted retained conditions, and eight blockers. The current dynamics/foundations refresh also prevents overreading the phase result: $1+e^{i2\pi/3}+e^{i4\pi/3}=0$ is a planar $\mathbb{Z}_3$ cyclic identity unless retained plane normals, phase-bundle holonomy, and $D_{\mathrm{plane}}$ classify the row as a coplanar cyclic sector, near-orthogonal tri-binary sector, or another retained sector. The report explicitly marks $\rho_I>\rho_M>\rho_O$ as an effective-lever-arm condition inside one role map and fails the direct nested-shell-radius reading $\rho_I<\rho_M<\rho_O$ until a retained tri-binary Noether braid replay supplies a projection, envelope-deformation map, incidence map, or branch-radius map.

Unordered configuration-space impact 2026-06-21. The new [tri-binary configuration-space](../../../content/markdown/aaa/dynamics/tri-binary-configuration-space.md) chapter changes the binary-frequency-search packet again, but it does not invalidate the existing rows and it is not a `start_over` condition. The practical condition is `schema_update_required`: replay packets should preserve the raw labeled row $(B_1,B_2,B_3)$, report the input-order ratios $f_1:f_2:f_3$, $r_1:r_2:r_3$, $E_1:E_2:E_3$, and $s_1:s_2:s_3$, emit an $S_3$ permutation-canonical key for duplicate detection, and attach any `I:M:O` interpretation as a role map rather than as the primary search order. Repeated $S_3$-equivalent solutions should be kept as confirmation evidence, while quotient-sector collapse is analysis-only.

Principal-direction impact 2026-06-21. The eigen-braid principal-direction row in [tri-binary configuration-space](../../../content/markdown/aaa/dynamics/tri-binary-configuration-space.md) changes promotion burden for the binary-frequency-search packet, but it is not a `start_over` condition. The practical condition is `schema_update_required`: the next replay should emit branch-total momentum $\mathbf{P}_{\mathfrak B}$, branch-total angular momentum $\mathbf{J}_{\mathfrak B}$, the declared weights used to compare $\mathbf{J}_{\mathfrak B}$ with the three plane-sector normals or angular-momentum bivectors, an axis-alignment residual, and the moving-envelope axis status. Frequency, root, hinge, and energy rows remain useful evidence, but a branch whose retained record returns without momentum-axis closure remains a return-map candidate rather than a promoted eigen-braid. This is a derived-ledger row, not an assumption that the six constituent paths are axial.

Foundation/dynamics impact 2026-06-21. The updated [dyadic-resonance-lock](../../../content/markdown/aaa/dynamics/dyadic-resonance-lock.md) chapter changes the promotion burden for this binary-frequency-search packet, but it does not invalidate or restart the current search. The existing family ranking remains a pre-bundle branch-certificate proxy: it records frequency closure, active-root lineage, self-root parity proxy coverage, torque/wake diagnostics, and hinge evidence, but it does not yet report the phase-bundle data now required for a retained tri-binary lock. The next replay should add retained plane normals or angular-momentum bivectors and $D_{\mathrm{plane}}$, relative holonomy defect $\Theta=(\phi_{MO}-\phi_{MO}^\ast,\phi_{IO}-\phi_{IO}^\ast)$, middle-caustic impulse rows, caustic-weighted amplitude rows, and flat-moduli Floquet or recurrence diagnostics. Until those fields are populated, no frequency family is accepted or excluded as a retained dyadic or non-dyadic tri-binary branch solely from self-root parity proxy coverage. The practical condition is `schema_update_required`, not `start_over`: previous rows remain valid evidence for the frequency/root/hinge layer, while promotion now needs a bundle-readiness payload on the same retained row set.

Binary-dynamics impact 2026-06-21. The updated [binary-dynamics](../../../content/markdown/aaa/dynamics/binary-dynamics.md) chapter adds a signed root-complex burden to the same search. The next replay should not treat $N_s$, $M_p$, self-root parity, or hinge rows as raw counts alone; each retained root row should carry its fold status, $\operatorname{sign}(\partial_{t_0}F)$ contribution to the signed degree $D_{ij}$, signed-sheet label $\sigma$, negative-sheet eligibility, caustic-grazing / Conley-status flag when $J=0$ is touched, branch-escapement contribution $N_{\mathrm{esc}}$, and the binary handedness label when an MCB-derived chirality row is claimed. This is also `schema_update_required`, not `start_over`: the existing root replay, transition classifier, hinge candidates, and torque/wake diagnostics remain valid reduced evidence, but a retained branch promotion now needs the signed Morse ledger and negative-sheet closure data on the same row set.

Master-equation impact 2026-06-21. The updated [master-equation](../../../content/markdown/aaa/dynamics/master-equation.md) adds a causal-root gluing and continuation-cardinality burden to the same binary-frequency-search packet. The next replay should report whether the local retained branch charts glue on overlaps, the finite continuation-family cardinality $|\mathfrak S_{\Omega,W}^{\mathrm{ME},\eta}|$, singular-stratum labels for fold versus cusp or deeper events, and the starvation scale $\Lambda_{\mathrm{starv}}$ or $u_{\mathrm{crit}}$ for any forward structural partner row used by a translating candidate. This remains `schema_update_required`, not `start_over`: existing root, hinge, torque, wake, and endpoint-provider rows remain useful, but retained-branch promotion now needs a finite labeled causal-root section family on the same row set rather than only point-event or route-local evidence.

Assembly topological charge capture 2026-06-21. The new [assembly-topological-charge](../../../content/markdown/aaa/dynamics/assembly-topological-charge.md) chapter makes the combined label $[\mathfrak B]_{\mathrm{top}}=(N_s,M_p,c_1)$ first-class. This confirms the same conclusion for the binary-frequency-search packet: the active family ranking does not restart, but the next retained-promotion replay must emit the compact assembly topological charge fields on the same retained row set, with $N_s$ and $M_p$ backed by signed root-complex rows and $c_1=(m,n)$ backed by phase-bundle holonomy. Existing frequency, hinge, root-transition, and torque/wake rows remain valid proxy evidence; they are insufficient for a retained assembly-sector claim until the assembly topological charge report, gluing status, and stability gap are populated together.

Causal-action / degree bridge impact 2026-06-21. The updated [causal-action-functional](../../../content/markdown/aaa/dynamics/causal-action-functional.md) and [assembly-topological-charge](../../../content/markdown/aaa/dynamics/assembly-topological-charge.md) chapters sharpen the replay payload again without invalidating earlier rows. The next binary-frequency-search replay should report lifted-strip fiber-intersection degrees $\iota_a$ for retained causal-locus components, compute the signed degree refinement $(D_s,D_p,c_1)$ alongside the compact assembly topological charge $[\mathfrak B]_{\mathrm{top}}=(N_s,M_p,c_1)$, distinguish closed torus winding labels from diagonal-collar arc components, and show regulator-stable middle-caustic rows when the middle carrier is evaluated at the field-speed hinge. This remains `schema_update_required`, not `start_over`: current frequency, root, hinge, and wake rows remain useful proxy evidence, but retained promotion now needs degree and caustic-regularization stability on the same retained row set.

Non-minimal competitor update 2026-06-20. The frequency-triplet search summary now emits `nonMinimalCompetitorAudit`. It populates ten finite reduced competitor families across 140 sampled competitor cases: the symmetric control, dyadic control, and finite integer-lock controls. The audit does not exclude retained competitors; it changes the blocker from absence of a finite competitor set to absence of retained competitor rows. No competitor family has retained row-set identity, retained phase lock, torque consistency, tail-wake pullback, vector partition, energy routing, section stability, or `retainedBranchClaim=true`, so a later non-minimal competitor can still defeat the middle-hinge candidate if it populates the same retained branch-selection residuals with lower cost or an accepted route.

Vector-partition payload update 2026-06-20. The retained-vector-partition payload target now emits `aaa-tri-binary-retained-vector-partition-payload-target.v1` with status `retained_vector_partition_payload_unit_balance_and_carrier_assignment_populated_acceptance_blocked`. It populates two route-local unit transport/recoil balance rows on the same-source and same-receiver middle routes under route root key `2856731379702547500`; both rows pass with maximum unit-balance residual $0$. The nested `aaa-tri-binary-physical-partition-carrier-assignment-target.v1` row is now `physical_partition_carrier_assignment_candidate_populated_coefficients_missing`: two of two unit transport/recoil carrier-pair rows are populated, with maximum carrier-balance residual $0$. The nested `aaa-tri-binary-partition-transport-recoil-coefficient-source-target.v1` row now reports `partition_transport_recoil_coefficient_source_current_wake_coefficients_rejected`: the unit coefficient convention is exact but ineligible, the accepted route-local wake coefficient angular sums do not match the partition transport vectors, and the best real rejected source is the normalized boundary-charge vector with residual $0.0011593636323863898$. The nested `aaa-tri-binary-partition-transport-recoil-coefficient-law-search-target.v1` row now reports `partition_transport_recoil_coefficient_law_search_fitted_same_source_scale_formal_acceptance_blocked`: 12 current candidates were tested, four exact rows are target-derived/formal, including the route-geometry cross-product row $\Delta J_{\mathrm{partition}}^{\mathrm{geom}}=r_{\mathrm{mid}}\times\Delta p_{\mathrm{route}}^{\mathrm{unit}}$ and the fitted same-source scalar $0.09754069445241503$, eight independent route/wake scalar rows are rejected, and no candidate is accepted. The nested `aaa-tri-binary-partition-transport-recoil-coefficient-independent-source-exclusion-summary.v1` row now reports `partition_transport_recoil_coefficient_independent_source_current_search_exhausted_new_law_required`, with two rejected source families, one formal-only family, zero incomplete current families, and best rejected independent candidate `normalized_boundary_charge_same_source_gate` at residual $0.0011593636323863898$. The nested `aaa-tri-binary-partition-point-event-torque-coefficient-source-target.v1` row now reports `partition_point_event_torque_coefficient_source_rejected_not_partition_transport_law`: it tests $\Delta J_{\mathrm{partition}}^{\mathrm{point}}=J_{\mathrm{point}}^{\mathrm{out}}-J_{\mathrm{point}}^{\mathrm{in}}$, but the same-source row gives candidate transport $0$ against target $z=0.09754069445241503$ and residual $0.09754069445241503$; the same-receiver row is exact only as a formal zero row with residual $5.551115123125783\times10^{-17}$. This moves `partition_payload` from missing to partial in the compensated retained-chart inventory, discharges the route-local carrier-assignment blocker, and narrows the remaining coefficient blocker to a new independent partition transport/recoil coefficient law or full-event rule beyond point-torque difference. It is still not a retained vector-partition pass: `acceptedRetainedVectorPartitionPass=false` because that coefficient law, retained force / torque / wake rows, and accepted energy routing remain absent.

Retained-chart feasibility update. The same runner now adds a fail-closed zero-slack retained-chart test for the $\pi/2$ middle field-speed hinge. The result is `retained_chart_feasibility_zero_slack_fails_compensation_payload_missing`: one same-source middle route requires compensation, `zeroSlackRetainedChartNoGo=true`, maximum required endpoint compensation norm is $0.6280004778252143$, maximum required endpoint-pair residual is $0.2032833773475938$, maximum required clock retune is $0.2085874165895798$, and maximum required phase compensation is $0.4171748331791605$. The compensated retained-chart payload inventory is still blocked, but its row-set evidence is narrower and stronger: `row_set_identity` is now a candidate payload because all nine $\pi/2$ hinge-pair witnesses share one common root key, `2856731379702547500`, while the global retained row-set identity over $W$ remains missing. The root-payload interval enclosure is `hinge_common_root_interval_point_only_no_common_side`: all nine pairs have common-root intervals, but their intersection is exactly $t=\pi/2$ with maximum common width $0$, no common left-sided interval, and no common right-sided interval. The branch-route feasibility row `aaa-tri-binary-hinge-root-branch-transport-route-feasibility.v1` now sharpens that blocker: `candidateRoutePass=true`, `zeroSlackRoutePass=false`, and `compensationRequiredMatchCount=1`. Both middle branch-map routes stitch positive-width one-sided root intervals through the same root key; the same-receiver route is zero-slack, while the same-source route has one-sided width $0.09817477042468115$ on each side and remains endpoint / clock compensation blocked. The route-payload certificate `aaa-tri-binary-compensated-route-payload-certificate.v1` is now fail-closed as `compensated_route_payload_complete_formal_acceptance_blocked`: `bounded_undeclared_route_slack`, `transport_angular_momentum_increment`, `root_energy_increment`, and `recoil_channel_data` are populated as diagnostic route fields; no route fields remain missing. The transport rows report maximum unit angular-momentum norm $0.09754069445241503$ with upper bound $0.3080684096622172$; the root-energy rows report maximum unit-action increment $0.4171748331791596$; the recoil rows report maximum unit-balance angular-momentum norm $0.09754069445241503$; the bounded slack rows report maximum bounded geometric slack $0.6280004778252143$, maximum bounded clock retune $0.2085874165895798$, and maximum bounded phase slack $0.4171748331791605$. The candidate payloads are now `row_set_identity` and `route_payload`; partial payloads are `root_payload`, `phase_payload`, `wake_payload`, `torque_payload`, and `partition_payload`; no payloads remain missing; blocking payload is `stability_payload`; and wake completion is the next smallest closure target.

The wake target row now gives required boundary-charge norm $0.09870005808480142$, a unit action-kernel wake-charge candidate with candidate residual $0$, a route-authorized pullback-domain target over two route rows and the same three endpoint row IDs, an action-kernel normalization-convention candidate with `eta=0.024543692606170286`, `epsilonC=0.11995659448597733`, endpoint convention `endpoint_clear_receiver_gradient_spatial_charge`, accepted chart-restricted crossing-domain rows with 2/2 route rows accepted, minimum $\eta$ route margin $0.07363107781851086$, minimum lever-arm regularization margin $0.35986978345793197$, a finite endpoint-clear kernel-gradient candidate evaluation with residual $0$, `aaa-tri-binary-master-equation-characteristic-tail-pullback-candidate.v1`, `aaa-tri-binary-pair-radial-characteristic-tail-constrained-solve.v1`, `aaa-tri-binary-characteristic-tail-coefficient-quadrature-target.v1`, `aaa-tri-binary-characteristic-tail-single-coefficient-sign-pattern-solve.v1`, `aaa-tri-binary-layer-polarity-sign-feasibility-target.v1`, `aaa-tri-binary-source-receiver-polarity-row-binding-target.v1`, `aaa-tri-binary-route-derived-source-receiver-polarity-metadata-target.v1`, `aaa-tri-binary-route-local-polarity-acceptance-target.v1`, and `aaa-tri-binary-route-local-coefficient-acceptance-target.v1`.

The free finite gradient remains non-radial against pair geometry (`pairRadialAlignmentPass=false`, maximum pair-radial residual $2.084719821603678$), but the side-split radial-constrained solve reconstructs target charge $(0,0,0.09870005808480142)$ with residual $0$ by `axis_aligned_z_least_norm`. Its maximum absolute side coefficient is $4.104328256986956$, and its maximum absolute signed $\delta_\eta$ candidate is $1.1463938810547583$. The coefficient/quadrature target evaluates all four side rows on-characteristic with maximum $|g|=5.1958437552457326\times10^{-14}$ under the Gaussian $\delta_\eta(g)$ convention; the single-coefficient sign-pattern solve reconstructs the target charge with one positive common coefficient $0.19201858530431484$ and side sign pattern $(-1,-1,-1,-1)$ at residual $0$. The route-local polarity target accepts the canonical middle-positive assignment `outer=-1`, `middle=1`, `inner=-1` for the four route-local characteristic-tail wake rows: `middle->outer` and `middle->inner` are same-source middle rows with source sign $+1$ and receiver sign $-1$, while `outer->middle` and `inner->middle` are same-receiver middle rows with source sign $-1$ and receiver sign $+1$.

The coefficient target deliberately does not accept the stronger direct rowwise common-$\kappa\sigma$ claim: direct accepted row count is $0/4$ and maximum coupling residual is $0.2576215812415463$. The new solver path now accepts the route-local row-amplitude requirement target as `route_local_least_norm_boundary_charge_row_amplitude_law_accepted`: four of four required amplitudes are finite, and the least-norm boundary-charge amplitude law reconstructs them with target residual $0$ and maximum row residual $0$. The same-source compensation-required rows require amplitudes $0.30070373454018245$ and $0.3672995792422465$; the same-receiver zero-slack rows require $0.12884270880640172$ and $-0.3416491993900621$. The algebraic $\delta_\eta$ identity still reconstructs the same amplitudes, while 120 simple non-tautological single-scalar geometry candidates remain rejected; the best rejected candidate is `sqrtUnitAngularContributionNorm:angularOrientation` with maximum residual $0.0842840557121674$. The current wake payload is `wake_payload_boundary_charge_pullback_accepted_wake_energy_law_missing`: the normalized action-kernel wake charge is accepted, the route-authorized retained crossing-domain pullback is accepted, and the wake-energy increment target is `wake_energy_increment_target_action_boundary_derivative_evaluated_action_scale_missing`. That target rejects zero wake energy, rejects reusing the $0.4171748331791596$ root-energy diagnostic as retained wake energy, rejects treating the boundary-charge norm $0.09870005808480142$ as energy without a transaction frequency or accepted action-boundary derivative law, and evaluates the $\omega_\ast$-weighted boundary-charge candidate as $0.2714251597332039$ using $\omega_\ast=2.75$ while blocking it because $\omega_\ast$ is not an accepted $\omega_{\text{tx}}$ and the action scale is undeclared. The direct action-boundary derivative target now populates four local $\partial_{t_1}K_{\mathrm{eff}}$ boundary rows, applies `aaa-tri-binary-finite-gaussian-endpoint-clearance-gauge-repair.v1`, and evaluates the normalized history integral. It now reports `action_boundary_derivative_kernel_history_integral_evaluated_action_scale_missing`: maximum route-width endpoint leakage before gauge repair is $0.009460477799001017$, maximum endpoint-clearance residual after gauge repair is $0$, half weighted local boundary derivative sum is $-2.83881740534609$, and half weighted normalized $\partial_{t_1}K_{\mathrm{eff}}$ sum is $-2.6740124169676682$. The action-boundary wake-energy law candidate is now `action_boundary_wake_energy_law_candidate_omega_tx_and_scale_law_search_no_simple_candidate_accepted`: it compares that unit-action derivative against $\omega_\ast|\Delta J_{\mathrm{wake}}|=0.2714251597332039$, requires orientation $-1$ and positive action-scale factor $0.10150482399068296$ if the $\omega_\ast$ comparison were accepted as the transaction-energy target, and now accepts the required energy orientation through `aaa-tri-binary-action-boundary-energy-orientation-law-target.v1`: `route_local_common_sigma_sign` supplies candidate orientation $-1$ with residual $0$, while `negative_action_time_derivative_convention` and `unit_action_derivative_sign` are exact but ineligible because they are not independent route-local orientation evidence. `aaa-tri-binary-action-boundary-action-scale-law-search-target.v1` rejects 37 finite existing scalar candidates for the required positive action scale; the closest rejected candidate is `target_charge_norm` at $0.09870005808480142$ with residual magnitude $0.002804765905881537$. `aaa-tri-binary-action-boundary-action-scale-derivation-target.v1` now keeps the exact comparison-measured scale row diagnostic-only and reports `action_boundary_action_scale_derivation_target_measured_scale_populated_route_local_candidates_rejected`: it tests 14 finite candidates from the accepted route-local amplitude/coefficient rows plus the action-kernel margin ratio, accepts none, and has best eligible miss `least_norm_boundary_charge_coefficient_min_abs_over_action_kernel_margin` at $0.10150468399144119$ with residual magnitude $1.3999924176888445\times10^{-7}$. The new residual-correction diagnostic reports `action_scale_residual_correction_diagnostic_quadrature_stable_endpoint_candidates_rejected`: 8192-step quadrature leaves residual magnitude $1.3999855889845758\times10^{-7}$, while the tested endpoint-leakage correction scales do not match. Its boundary-charge gap diagnostic maps the miss to the `outer->middle` incoming row and a required wake-charge norm $0.09870019421579312$, gap $1.3613099170139975\times10^{-7}$. `aaa-tri-binary-omega-tx-law-search-target.v1` now reports `omega_tx_law_search_minimal_branch_frequency_certificate_formal_acceptance_blocked` over 15 finite transaction-frequency candidates. The clean weighted identity $\omega_\ast=(\omega_O+\omega_M+2\omega_I)/4=2.75$ remains ineligible because it is the comparison target, while `aaa-tri-binary-minimal-branch-transaction-frequency-certificate-target.v1` proves the reduced four-substep candidate $\omega_{\text{tx}}^{(4)}=2.75$ with residual $0$ but blocks acceptance on geometrically continuous branch transport, accepted retained branch claim, global retained point-event rule or positive-width common retained time domain, and same-event carrier population. The omega same-event dependency diagnostic reports `omega_same_event_dependency_carrier_populated_retained_event_blockers_remain`: both exact formal same-event carrier rows are populated, so only the retained-event geometry/domain blockers remain on the frequency-certificate side. Its retained-event geometry blocker is `omega_retained_event_geometry_same_source_compensation_required`: two route rows are evaluable, one same-receiver row is zero-slack, and the same-source middle row requires endpoint/clock/phase compensation. The same-source emission-clock transport diagnostic is now `same_source_emission_clock_transport_chart_exact_endpoint_residual_blocked`: the circular middle chart explains the clock/phase jump exactly, with chart transport residual $5.551115123125783\times10^{-17}$ and chart-chord residual $3.885780586188048\times10^{-16}$, but sampled endpoint transport still fails with endpoint advection residual $0.00022316007514149727$ and maximum endpoint-to-emission-chart residual $0.009607165803389037$. The closest eligible rejected frequency candidate remains `outer_inner_angular_velocity_mean` at $2.5$ with residual magnitude $0.25$. The next smallest closure target remains `wake_payload` before section stability can be evaluated, specifically an accepted $\omega_{\text{tx}}$ or energy target, a derived $\sigma\hbar$ action scale, and accepted wake-energy increment law. This falsifies immediate zero-slack chart promotion for the current packet; it does not exclude a later compensated retained chart that populates wake, partition, and stability payloads on the same retained event and retained interval.

The boundary-charge increment candidate audit is now explicit inside the residual-correction diagnostic. It reports `boundary_charge_increment_candidates_rejected_target_derived_comparison_only`: 10 candidates were tested, zero acceptance-eligible rows matched the $1.3613099170139975\times10^{-7}$ boundary-charge gap, and the only exact row is the ineligible remapping of the measured action-scale miss into boundary-charge units. The next action-scale proof route therefore cannot rely on current quadrature convergence, simple endpoint-leakage scales, or direct endpoint-leakage charge increments.

The alternate-normalization audit is also explicit inside the residual-correction diagnostic. It reports `alternate_normalization_shift_candidates_rejected_target_derived_comparison_only`: exactness for the best coefficient/margin law would require changing the action-kernel margin ratio from $4$ to $3.9999944830507057$, equivalently shifting $\eta$ by $3.385162359106908\times10^{-8}$ at fixed route width or shifting the minimum $h_+$ route width by $1.35406307597008\times10^{-7}$ at fixed $\eta$. Eleven candidate rows were tested, zero acceptance-eligible rows matched, and the only exact rows are the ineligible target-derived required-shift comparisons. The independent quadrature-shift row is too small, while the tested endpoint-width and compensated-normalization rows are either too large or too small. The next normalization route therefore needs a new retained derivation rather than a fitted adjustment of the current margin ratio.

The retained-work / near-field action-scale audit is now explicit inside the residual-correction diagnostic. It reports `retained_work_action_scale_candidates_rejected_target_derived_comparison_only`: 23 finite compensated route-payload, root-energy, transport, recoil, endpoint, and slack rows were tested, zero acceptance-eligible rows matched, and the only exact row is the ineligible target-derived comparison $\omega_\ast|\Delta J_{\mathrm{wake}}|/|\partial_t K_{\mathrm{eff}}|$. The closest independent row is `same_source_endpoint_pair_residual_half` at $0.1016416886737969$, with residual magnitude $1.3686468311394084\times10^{-4}$ against the required positive scale $0.10150482399068296$. The current route-payload rows therefore do not supply the tiny action-scale correction; a retained-work route would need a new accepted retained-work law rather than reuse of the present formal payload diagnostics.

The coefficient-provenance audit is now explicit inside the same residual-correction diagnostic. It reports `coefficient_provenance_candidates_rejected_target_derived_comparison_only`: the best action-scale miss is the `outer->middle` least-norm coefficient row, requiring coefficient magnitude $0.40601929596273184$ rather than $0.40601873596576477$, a gap of $5.599969670755378\times10^{-7}$. In equivalent units, that is signed-$\Delta\eta$ shift $5.546421561187014\times10^{-7}$, row-amplitude shift $1.777049179510411\times10^{-7}$, common-coupling shift $2.6483956494465465\times10^{-7}$, target-charge shift $1.3613099170139975\times10^{-7}$, inferred-column shift $1.5452591328908583\times10^{-9}$, or $N_{zz}$ shift $3.7564019174938026\times10^{-10}$. Eleven rows were tested, zero acceptance-eligible rows matched, and the best independent row-amplitude residual maps to only $9.470949326781743\times10^{-17}$ in coefficient units. The action-scale correction therefore does not come from current least-norm solve residuals, accepted row-amplitude residuals, or finite causal-gap Gaussian effects.

The same-event energy-routing target is now explicit rather than implicit. It reports `same_event_energy_routing_target_minimal_branch_boundary_candidate_formal_acceptance_blocked`: the accepted boundary charge and retained crossing-domain pullback share the route rows, and both `minimal_branch_frequency_boundary_charge_candidate` and `action_boundary_derivative_scaled_candidate` hit $\Delta E_{\mathrm{wake}}=0.2714251597332039$ with residual $0$. They are not acceptance sources until the same retained event also has accepted $\omega_{\text{tx}}$, a derived $\sigma\hbar$ action scale, and an accepted wake-energy increment law. The wake diagnostic now emits `nextWakePayloadAcceptanceTarget` with payload `wake_payload`, target `wake_energy_increment`, status `wake_energy_increment_target_action_boundary_derivative_evaluated_action_scale_missing`, and blockers `accepted_omega_tx_source`, `derived_sigma_hbar_action_scale`, and `accepted_wake_energy_increment_law`.

Current polarity-metadata disposition. The sampled phase metadata cross-check remains `source_receiver_polarity_phase_metadata_source_mismatch_and_receiver_missing`: it matches the two middle-source rows, mismatches the two outer/inner source rows, and supplies no receiver signs, so it is rejected as an acceptance source. The independent route-derived metadata target is `route_derived_source_receiver_polarity_metadata_candidate_matches_binding` over all four bound rows, and the route-local acceptance target now promotes that metadata to `route_local_source_receiver_polarity_metadata_accepted` for the characteristic-tail wake rows only. This does not accept the global retained branch, the normalized action-kernel charge, retained pullback, or wake energy.

## Row Verdicts

| Row family | Instance value | Verdict |
| --- | --- | --- |
| Candidate identity | One reduced core candidate $\mathfrak a_{\min}$ is named. | Populated. |
| Scalar partition | $\mathcal R_I^{B_{\min}}=0$ in the reduced certificate. | Conditional pass. |
| Vector partition | $\mathcal R_{\mathbf J}^{B_{\min}}=\mathbf 0$ in the reduced certificate. | Conditional pass. |
| Transverse bookkeeping | $\mathcal R_{\perp}^{B_{\min}}=\mathbf 0$ in the reduced certificate. | Conditional pass. |
| Root replay | A reduced circular-root proxy is now populated, but individual continued active-root rows are not supplied through the full retained chart. | Blocked. |
| Phase lock | Solver phase-at-hit rows are now populated for the selected reduced payload, but binary-to-binary retained receiver phase, geometric phase, wake-return delay, and branch-domain margins are not populated. | Blocked. |
| Torque consistency | The force, partition, torque, and wake diagnostic rows now share sampled row IDs. A 65-sample fixed-receiver torque stream gives outer residual $3.009907861145633\times10^{-14}$ but middle residual $0.23789379538011096$ and inner residual $0.13919373729527945$; the binary-to-binary path-history replay now has 606 active-root detail rows, 1,056 inactive-gap rows, 775 classified transitions, and 379 retained transitions. Both hinge candidates $7\pi/16$ and $\pi/2$ have all nine pair witnesses, a shared retained root key, boundary-only incidence, three populated diagonal identity witnesses, six populated off-diagonal force-bearing diagnostics, passing candidate point-event admissibility rows, and layer-balanced branch-transport incidence. The new retained torque payload acceptance target reports `retained_torque_payload_point_event_candidate_populated_acceptance_blocked`: six of six off-diagonal point-event torque rows pass candidate scope with maximum row torque norm $5.551115123125783\times10^{-17}$ against tolerance $10^{-12}$, but retained torque acceptance is false until retained transport, global row-set identity, a full point-event rule, and retained energy routing are accepted. The $\pi/2$ topology pair-map is populated, and its middle field-speed hinge-capture row is chart-continuous: the middle speed residual is $0$, both pair-map routes run through the middle layer, two of two chart rows pass, and the maximum chart residual is $0$. It is delayed-endpoint-geometry blocked: one same-receiver endpoint-continuity row passes with residual $0$, while the same-source middle row has source-point residual $0.2032833773475938$, middle emission-clock jump $0.2085874165895798$, wrapped middle phase jump $0.4171748331791605$, and maximum causal-endpoint-to-hinge-chart residual $0.6280004778252143$ against tolerance $10^{-12}$. The branch-route feasibility row now shows both middle pair-map routes stitch positive-width one-sided root intervals through the common root key, but one route still needs endpoint and clock compensation. They remain blocked because no global retained point-event rule, zero-slack geometrically continuous branch-transport pair-map, positive-width common retained time domain, accepted wake payload, accepted partition transport/recoil coefficient law, or section-stability payload exists. | Blocked. |
| Retained chart feasibility | The zero-slack retained-chart feasibility diagnostic fails for the $\pi/2$ middle field-speed hinge: one same-source middle route requires endpoint and phase compensation. The compensated payload inventory is blocked, with candidate `row_set_identity` at the hinge event, point-only `root_payload` interval evidence with max common width $0$ and no common one-sided interval, branch-route candidate evidence with `candidateRoutePass=true`, `zeroSlackRoutePass=false`, and one compensation-required match, a same-source emission-clock transport diagnostic whose nested path-history segment-bound row reports `same_source_path_history_segment_error_bound_contains_endpoint_residuals`, a complete route-payload certificate that now populates bounded route slack, unit transport angular momentum, unit-action root energy, and unit-balance recoil-channel data, partial `root_payload`, `phase_payload`, `wake_payload`, `torque_payload`, and `partition_payload`, no missing payloads, blocking `stability_payload`, and wake completion as the next smallest closure target. The partition payload remains only route-local: `aaa-tri-binary-retained-vector-partition-payload-target.v1` now populates two same-root unit transport/recoil balance rows and a nested physical-partition carrier assignment over both rows with maximum residual $0$; its coefficient-source audit rejects the current wake coefficient rows and normalized boundary charge as partition transport/recoil coefficient sources, with best real rejected residual $0.0011593636323863898$, and its coefficient-law search records the route-geometry cross-product row $\Delta J_{\mathrm{partition}}^{\mathrm{geom}}=r_{\mathrm{mid}}\times\Delta p_{\mathrm{route}}^{\mathrm{unit}}$ plus the fitted same-source scalar $0.09754069445241503$ as formal-only with no accepted independent candidate. The independent-source summary exhausts the current finite partition source family, and the point-event torque coefficient-source target rejects point-torque difference as the coefficient source because the same-source row needs $z=0.09754069445241503$ while its point-torque delta is $0$; retained vector-ledger acceptance remains false until a new independent partition transport/recoil coefficient law or full-event rule, retained force / torque / wake rows, and accepted energy routing are present. The bounded residual row uses the 32-segment middle-layer bound $0.009607359799384785$ and double bound $0.01921471959876957$ to contain the sampled endpoint residuals. The endpoint linear-segment replay row reports `same_source_endpoint_linear_segment_replay_identifies_linearization_error`: incoming and outgoing sampled source endpoints replay as 32-segment linear interpolation points with maximum replay residual $4.775249788392736\times10^{-16}$, while exact circular endpoint replay remains false. The nested convergence row reports `same_source_endpoint_linear_segment_convergence_certificate_populated`: segment counts $16,32,64,128,256$ reduce maximum linear-to-exact circular residual from $0.028600000511357693$ to $0.00015042504896734804$, match the 32-segment residual, decrease monotonically, and remain inside shrinking segment-error bounds. The exact circular endpoint replacement target reports `same_source_exact_circular_endpoint_replacement_target_populated_formal_acceptance_blocked`: exact circular replacement leaves advection residual $5.551115123125783\times10^{-17}$ and endpoint-pair residual versus chart chord $0$, but requires a physical endpoint provider or retained-domain transport law for corrections $0.009446954866495872$ incoming and $0.009607165803389037$ outgoing. Its nested reduced circular endpoint-provider law candidate reports `same_source_reduced_circular_endpoint_provider_law_candidate_populated_formal_acceptance_blocked`: point residual $0$, provider advection residual $5.551115123125783\times10^{-17}$, radius $0.5$, angular velocity $2$, and phase-at-epoch $0.39269908169872414$. The same-source emission-clock diagnostic now reports aggregate retained endpoint-provider acceptance as `same_source_retained_endpoint_provider_accepted`, and its retained endpoint-provider acceptance target reports `same_source_retained_endpoint_provider_route_authorized_point_event_accepted`: the provider is on the same route/root-key row set with `sameRetainedRowSetProviderPass=true`, route root key `2856731379702547500`, minimum one-sided route width $0.09817477042468115$, one of one retained-provider row accepted, nested route-authorized domain status `route_authorized_endpoint_provider_point_event_domain_accepted`, and row status `same_source_retained_endpoint_provider_route_authorized_point_event_accepted`. The new route-authorized endpoint-provider global-domain obstruction target reports `route_authorized_endpoint_provider_global_domain_blocked_point_only_common_root_and_compensation`: the local provider is accepted, but the all-pair common-root interval has max common width $0$ with no left or right common side interval, global retained row-set identity is missing, one of two route rows still requires compensation, and `retainedBranchClaim=false`. This acceptance is local to the endpoint-provider point-event scope; full retained-branch acceptance or a positive-width common retained-domain acceptance remains unaccepted. | Blocked. |
| Causal-wake pullback | A diagnostic wake torque sample is attached to each sampled row ID, and the older no-boundary work-integral wake reconstruction now gives $z=-0.09870005808480142$. The route-authorized wake target has required boundary-charge norm $0.09870005808480142$, a unit action-kernel wake-charge candidate with residual $0$, a pullback-domain target over two route rows and the same three endpoint row IDs, an action-kernel normalization-convention candidate with candidate scales `eta=0.024543692606170286` and `epsilonC=0.11995659448597733`, accepted chart-restricted crossing-domain rows with 2/2 route rows accepted, a finite endpoint-clear kernel-gradient candidate evaluation with residual $0$, a populated Master-Equation characteristic-tail pair-radial pullback target, a side-split pair-radial constrained solve that reconstructs the target charge with residual $0$, coefficient/quadrature rows that evaluate $\delta_\eta(g)$ on-characteristic with maximum $|g|=5.1958437552457326\times10^{-14}$, a single-coefficient sign-pattern solve with common coefficient $0.19201858530431484$, sign pattern $(-1,-1,-1,-1)$, and residual $0$, a route-local source/receiver polarity acceptance target with canonical assignment `outer=-1`, `middle=1`, `inner=-1`, a route-local row-amplitude requirement target that accepts the least-norm boundary-charge amplitude law with maximum row residual $0$, and a route-local coefficient acceptance target that is accepted through that amplitude law. The normalized action-kernel boundary charge and retained crossing-domain pullback are accepted; the finite-Gaussian endpoint-clearance gauge repair and normalized history integral are evaluated; the unit-action derivative wake-energy candidate is $-2.6740124169676682$; the energy-orientation law search accepts `route_local_common_sigma_sign=-1`; the action-scale law search rejects 37 finite scalar candidates, with `target_charge_norm` closest at residual magnitude $0.002804765905881537$; the action-scale derivation target keeps the measured scale row diagnostic-only and rejects 14 route-local derivation candidates, with `least_norm_boundary_charge_coefficient_min_abs_over_action_kernel_margin` closest among eligible rows at residual magnitude $1.3999924176888445\times10^{-7}$; the residual-correction diagnostic rejects quadrature convergence and the tested simple endpoint-leakage scales as the missing correction source, and maps the miss to required wake-charge norm $0.09870019421579312$ with gap $1.3613099170139975\times10^{-7}$; and the $\omega_{\text{tx}}$ search evaluates 15 finite candidates. The clean $\omega_\ast=2.75$ comparison row remains ineligible, while the reduced four-substep certificate supplies exact candidate $\omega_{\text{tx}}^{(4)}=2.75$ but is formal-acceptance blocked by missing geometrically continuous branch transport and accepted retained branch/event domain. The same-event energy-routing target is populated and has two exact formal carrier rows at $\Delta E_{\mathrm{wake}}=0.2714251597332039$, but accepted $\omega_{\text{tx}}$, derived action scale, and accepted wake-energy law are still missing for the same retained active rows. | Blocked. |
| Stability | Inactive-gap margins are populated for the replayed binary-to-binary detail rows, but section stability is not evaluated on a common retained row set. | Blocked. |
| Energy routing | The reduced energy-frequency residual is not paired with a declared route. | Blocked. |
| Non-minimal competitors | Ten finite reduced competitor families are now populated across 140 sampled competitor cases, but none has retained row-set identity, retained phase lock, torque, wake, partition, energy, stability, or `retainedBranchClaim=true`. | Blocked. |
| Local exclusion | No row proves contradiction or forbidden routing. | No exclusion. |

The positive-width lift audit now distinguishes route-local width from retained-domain lift. `aaa-tri-binary-positive-width-retained-domain-lift-target.v1` reports `positive_width_retained_domain_lift_blocked_all_pair_point_only_and_route_compensation`: both branch routes have route-restricted positive one-sided width, but the all-pair common retained interval remains point-only with max width $0$, `inner->inner` and `middle->middle` supply no positive side interval, four pairs are missing on each all-pair side, and the same-source middle route remains compensation-required. This keeps the minimal candidate live as a route-local hinge packet while blocking retained-branch promotion.

The full point-event lift audit now isolates the complementary route. `aaa-tri-binary-full-point-event-rule-lift-target.v1` reports `full_point_event_rule_lift_blocked_route_compensation_and_global_scope`: the $\pi/2$ hinge has passing candidate point-event admissibility, passing branch-transport incidence, topology pair-map support, off-diagonal point-torque norm $2.7755575615628914\times10^{-17}$, route root key `2856731379702547500`, and `routeAuthorizedPointEventDiagonalIdentityPass=true`, but `fullPointEventRuleLiftPass=false`. The blockers are endpoint-provider-assisted geometry not yet being global retained transport, missing zero-slack route transport, same-source route compensation, missing global retained row-set identity, missing positive-width common retained time domain, partial retained payload acceptance, missing retained energy routing, and an accepted full point-event rule.

The diagonal identity rule audit now gives the point-only rows their own fail-closed target. `aaa-tri-binary-full-point-event-diagonal-identity-rule-target.v1` reports `route_authorized_point_event_diagonal_identity_accepted_full_scope_blocked`: all three diagonal rows carry common-root identity witnesses, the point-only diagonal keys are `inner->inner` and `middle->middle`, `outer->outer` supplies positive side width on both sides, and the route-authorized acceptance blockers are empty. This accepts diagonal identity only at route-authorized point-event scope; full-scope acceptance remains blocked by endpoint-provider geometry not yet being global retained transport, missing zero-slack branch route, same-source compensation, missing global retained row-set identity, partial retained payload acceptance, missing retained energy routing, and missing accepted full point-event rule.

The full point-event payload coverage audit now separates populated same-event evidence from accepted retained payload closure. `aaa-tri-binary-full-point-event-payload-coverage-target.v1` reports `full_point_event_payload_coverage_partial_route_wake_energy_carriers_populated_acceptance_blocked`: the point-event force/torque row is candidate-populated, provider-assisted branch geometry passes locally, the route payload is complete, route-authorized wake pullback is populated, route-authorized diagonal identity is accepted, `aaa-tri-binary-retained-torque-payload-acceptance-target.v1` is `retained_torque_payload_point_event_candidate_populated_acceptance_blocked` with six of six off-diagonal torque rows passing candidate scope, the route-local clock-to-phase payload law is populated with residual $0$, the route-local vector-partition unit balance target is populated with residual $0$, the route-local physical-partition carrier assignment is populated with residual $0$, the partition coefficient-source audit rejects current wake coefficient reuse, the partition coefficient-law search is formal-only with no accepted independent candidate, the partition independent-source exclusion summary exhausts the current finite source family, the partition point-event torque coefficient-source target rejects point-torque difference as a transport/recoil coefficient law, and same-event energy carriers are formal-populated. The audit keeps `acceptedRetainedPayloadRowsPass=false` and `acceptedRetainedEnergyRoutingPass=false` because accepted $\omega_{\text{tx}}$, derived $\sigma\hbar$ action scale, accepted wake-energy increment law, accepted retained transport for the phase law, retained torque acceptance, accepted partition transport/recoil coefficient law, section stability, accepted retained energy routing, and the accepted full point-event rule remain missing.

The exact-endpoint action-scale audit now tests whether the accepted same-source endpoint provider explains the remaining action-scale gap. `aaa-tri-binary-exact-endpoint-provider-action-scale-diagnostic.v1` reports `exact_endpoint_provider_action_scale_candidates_rejected`: the provider row has exact replacement geometry with residual $5.551115123125783\times10^{-17}$, but the seven finite endpoint-correction candidates do not match the required action scale $0.10150482399068296$. The closest endpoint-provider row is the incoming-plus-outgoing correction norm $0.01905412066988491$, still short by residual magnitude $0.08245070332079805$, so the action-scale miss cannot be charged to the exact circular endpoint replacement.

The action-scale source exclusion summary now closes the current search family rather than the action-scale problem itself. `aaa-tri-binary-action-scale-independent-source-exclusion-summary.v1` reports `action_scale_independent_source_current_search_exhausted_full_event_rule_or_new_law_required`: eight current source families are excluded, zero families remain incomplete or untested inside this diagnostic, and no independent $\sigma\hbar$ source is accepted. The minimal candidate therefore remains live as a hinge packet, but its next executable fork is a global retained full point-event rule or positive-width common retained time domain that can carry the accepted route-authorized identity into full scope, or a new independent action-scale law outside the tested scalar, route-local, residual, boundary-charge, normalization, retained-work, coefficient-provenance, and exact-endpoint-provider families.

The endpoint-provider-assisted geometry audit now separates sampled-route compensation from route-authorized exact-provider geometry. `aaa-tri-binary-endpoint-provider-assisted-branch-transport-geometry-target.v1` reports `endpoint_provider_assisted_branch_transport_geometry_populated_sampled_route_compensation_remains`: one same-source route remains sampled compensation-required, but its exact circular endpoint-provider replacement has residual $5.551115123125783\times10^{-17}$, and the other route is already sampled zero-slack. The audit therefore upgrades the local branch-transport geometry evidence while keeping retained-branch promotion blocked by sampled `zeroSlackRoutePass=false`, route-authorized point-event-only scope, missing global retained row-set identity, missing full point-event or positive-width retained-domain acceptance, and missing retained payload acceptance.

The global retained-transport lift audit now rejects the current provider-substitution shortcut as retained transport. `aaa-tri-binary-endpoint-provider-global-retained-transport-lift-target.v1` reports `endpoint_provider_global_retained_transport_lift_candidate_populated_acceptance_blocked`: route substitution coverage is complete across the two route rows, with one sampled zero-slack row and one exact endpoint-provider substitution row, but `acceptedGlobalRetainedTransportLiftPass=false`. The blockers are the provider substitution not being sampled zero-slack transport, endpoint-provider scope `same_source_endpoint_provider_route_only`, missing sampled zero-slack branch route, missing global retained row-set identity, missing full point-event rule or positive-width retained domain, missing retained payload acceptance, missing retained energy routing, and missing accepted full point-event rule.

The physical provider-transport law audit now tests the only route by which that exact substitution could become retained transport. `aaa-tri-binary-physical-retained-provider-transport-law-target.v1` reports `physical_retained_provider_transport_law_candidate_geometry_populated_global_acceptance_blocked`: the exact reduced provider map is populated on the same-source substituted route with residual $5.551115123125783\times10^{-17}$, but `acceptedPhysicalRetainedProviderTransportLawPass=false`. The current row remains route-authorized point-event evidence, not a physical retained transport law, because no global retained row-set declaration, full point-event or positive-width common retained-domain support, retained payload rows, retained energy routing, or accepted full point-event rule is present.

The deterministic branch-selection residual is therefore not evaluated:

$$
\mathcal R_{\mathrm{sel}}(\mathfrak a_{\min})
\quad
\text{is blocked, not passing.}
$$

## Use In The Workstream

This instance is useful because it makes the next branch-selection job concrete. The next successful pass must replace

$$
\mathcal A_{N,\min}^{\mathrm{eval}}=\varnothing
$$

with a nonempty finite candidate set whose row-lineage maps, interval payloads, quotient witnesses, and retained wake/routing rows all feed the same residual vector $\mathcal R_{\mathrm{sel}}$. Until then, the minimal four-substep branch remains a reduced diagnostic candidate rather than a selected physical branch.
