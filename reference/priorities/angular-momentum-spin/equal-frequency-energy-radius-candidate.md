# Equal-Frequency Energy-Radius Candidate

Status. Priority-only investigation under `tri_binary_partition_rule` and `worked_three_layer_noether_transition`. This is not a theorem row, not a canonized tri-binary law, and not reader-facing corpus prose. Its purpose is to preserve and discipline the candidate that three retained binaries may share one frequency while occupying different energy, effective-lever-arm, speed, and phase-offset branches inside the general velocity-deforming tri-binary Noether swarm.

Claim level. Derivation-closure target. The candidate statement is:

$$
\text{binaries may have equal frequency yet have different radius/speed relations through branch energy.}
$$

Promotion decision. Defer with blocker. Promote only after a retained branch chart or reduced proof derives the energy-radius relation for the same retained row set and then shows that the equal-frequency family closes speed-regime, phase-offset, causal-root, total-momentum, angular-momentum axis-alignment, wake, energy-routing, and stability rows. Until then, treat it as a very high-priority search candidate and a possible correction to the current offset-first ranking.

Working label. `equal_frequency_energy_radius_candidate` is a working packet label only. It is not a terminology policy change.

## Candidate Statement

Use generic binary labels first. The proposed common-frequency family is

$$
(f_1,f_2,f_3)=(f,f,f),
\qquad
f\in\mathbb Z_{>0}.
$$

Equivalently,

$$
\omega_1=\omega_2=\omega_3=\omega_f,
$$

while the phase offsets remain free branch data in the input row order:

$$
\Theta_{123}
=
\left(
\phi_2-\phi_1,
\phi_3-\phi_2
\right).
$$

The first high-priority phase profile is the triadic spacing suggested by the equal-frequency clock:

$$
\left(\phi_1,\phi_2,\phi_3\right)
=
\left(0,\frac{2\pi}{3},\frac{4\pi}{3}\right),
\qquad
\Theta_{123}
=
\left(\frac{2\pi}{3},\frac{2\pi}{3}\right).
$$

Equivalently, the three binaries are separated by $120^\circ$ or one third of a turn in the displayed input order. All $S_3$ permutations of this profile are the same quotient-sector candidate but should remain valid repeated solver rows unless an explicit quotient-sector view is being used. Other phase offsets remain searchable, but this triadic profile is the first candidate because it uses the common clock without privileging one binary phase.

The speed rows are not frequency rows. In a role-assigned chart one targeted speed profile is

$$
s_I=\rho_I\omega_f>c_f,
\qquad
s_M=\rho_M\omega_f\approx c_f,
\qquad
s_O=\rho_O\omega_f<c_f.
$$

Here $\rho_\ell$ is the retained effective lever-arm projection entering the local binary speed row. Earlier shorthand $R_\ell$ in this packet should be read as this effective lever arm unless a retained branch radius or projection map has been derived. Thus the candidate requires an energy-radius law that admits distinct retained lever-arm projections

$$
\rho_I>\rho_M>\rho_O
$$

in the simple monotone speed-row model after the retained branch supplies the $I:M:O$ role map. This inequality is not a pre-search ordering of the three binaries. It is not automatically the global nested-shell coordinate or the radius of a common sphere. If the retained shell geometry uses a different radial ordering, the proof must exhibit the corresponding effective-lever-arm map rather than assuming the simple ordering. The middle-tagged binary remains on the $c_f$ edge by lever arm and energy placement, not by having a different frequency.

## General Tri-Binary Noether Swarm Geometry

The baseline object is the retained all-layer tri-binary Noether swarm branch state. It is not a fixed sphere by itself, not a flattened chart by itself, and not three independent orbits chosen before the branch is solved. The low-drift state is a spherical or near-spherical branch with three retained orbit rows; as the branch carries drift velocity, the same object deforms through an oblate spheroidal envelope toward a flattened limit. In the Lorentz/return-cycle bridge notation, a branch state has the form

$$
B_{3B}(q,v)
=
\left(
R_{\parallel},R_{\perp},\xi;\,
\omega_a,J_a,\phi_a,\mathbf{N}_a;\,
\rho_a,s_a;\,
\mathbf{P}_{\mathrm{branch}},\mathbf{J}_{\mathrm{branch}};\,
\mathcal{L}_{\mathrm{root}};\,
\mathcal{L}_{\mathrm{wake}};\,
E_{\mathrm{branch}};\,
\mathrm{role\ map};\,
\mathrm{coupling\ rows}
\right)_q,
$$

where $a\in\{1,2,3\}$ names the generic binaries before any $I:M:O$ role map is attached. Here $\mathbf{N}_a$ denotes the orbit-plane normal or bivector, $\rho_a$ is the retained effective lever-arm projection emitted by the branch state, and the coupling rows are whatever external assembly, field, or retained event interacts with the tri-binary Noether swarm. The branch-total momentum and angular-momentum ledgers include retained wake contribution. A return-map pass is not enough for promotion if the reported tri-binary plane normals fail to decompose $\mathbf{J}_{\mathrm{branch}}$ or if the moving-envelope axis is inconsistent with $\mathbf{P}_{\mathrm{branch}}$. The retained replay must solve a velocity-deformation map

$$
\mathcal D_v(B_{3B}):
\text{spherical or near-spherical support with three orbit rows}
\longrightarrow
\text{oblate spheroidal envelope}
\longrightarrow
\text{flattened or planar limit},
$$

and emit $\rho_\ell(v)$ only as a projection of that branch state. The velocity-dependent geometry must include the known deformation sequence:

1. low-drift or rest branch: spherical or near-spherical three-dimensional support with three retained orbit rows;
2. moving branch: oblate spheroidal envelope with shape channel $\xi=R_{\parallel}/R_{\perp}$;
3. field-speed alignment limit: flattened or planar branch support.

The current executable row is therefore a flattened-limit projection view of this general case, not the model itself. It is one member of a deformation projection-chart family that also contains low-drift spherical-orbit and moving oblate-incidence rows. The current view can populate causal roots, self-hit spans, phase rows, and speed-row projections. It proves only that the algebraic speed relation can be populated with a common $\omega_f$ and different effective lever-arm projections.

For a retained replay, the proof must choose how $\rho_\ell$ is produced by the tri-binary Noether swarm branch:

1. Low-drift spherical or near-spherical support, with small-circle or latitude lever arms:

$$
\rho_\ell=R_{\mathrm{sph}}\sin\theta_\ell,
\qquad
s_\ell=\rho_\ell\omega_f.
$$

In this case equal frequency can still give different speeds if the retained branch energy selects different $\theta_\ell$ or an equivalent orbital-plane incidence. A hinge row becomes $\rho_h\omega_f\approx c_f$ only after the retained branch identifies which binary plays that role.

2. Moving oblate spheroidal support, where the branch supplies $\rho_\ell$ through envelope axes, orbital-plane incidence, orbit-row continuation, and the shape/scale channels:

$$
R_{\parallel}(v,E,n)=\frac{\lambda(v,E,n)R_0}{\gamma_\star(v)},
\qquad
R_{\perp}(v,E,n)=\lambda(v,E,n)R_0.
$$

Here $\rho_\ell$ is a projected lever arm inside the deformed envelope, not a raw shell radius.

3. Common-sphere great-circle control:

$$
\rho_O=\rho_M=\rho_I=R_{\mathrm{sph}}.
$$

In this case equal frequency forces equal speed magnitude in the simple kinematics, so it cannot by itself realize the outer/middle/inner speed ordering. A proof using great circles would need another speed definition, projection, or causal-delay row.

4. Nested shell, branch-radius, or flattened-alignment support:

$$
\rho_\ell=R_\ell^{\mathrm{eff}}\left(J_\ell;\lambda_\ell\right),
$$

where the retained energy/radius law supplies different effective lever arms. This is closest to the reduced runner only if $\rho_\ell$ is treated as a branch projection, not as the nested shell radius itself. The sampled equal-frequency speed rows force $\rho_I>\rho_M>\rho_O$ only inside the displayed role map, so a direct nested-shell-radius reading with $\rho_I<\rho_M<\rho_O$ is incompatible unless the retained replay derives a projection, incidence, envelope-deformation, or retained branch map that separates lever arm from shell radius.

The working target is therefore not a completed flattened-radius, fixed-sphere, or single-chart geometry claim. It is: equal-frequency binaries may have different retained effective lever-arm projections inside the velocity-deforming tri-binary Noether swarm, and those projections may produce different speed relations while the same branch continues from spherical or near-spherical support through oblate support toward the flattened limit.

## Why This Is A Distinct Hypothesis

The current offset candidate

$$
(I,M,O)=(f+2,f,f-1)
$$

is one role-assigned subfamily of the unordered three-binary search. It tests whether the inner-tagged layer's doubled self-hit burden appears as a phase-lock or resonance-index offset. The equal-frequency candidate tests the opposite branch mechanism: the transaction may have a common clock, while any doubled-burden role appears through causal-root multiplicity, phase offset, branch incidence, action partition, wake routing, or energy/radius placement.

This matters because the present branch-certificate ranking privileges the index proxy

$$
n_I-n_M=2.
$$

For $(f_1,f_2,f_3)=(f,f,f)$, this proxy is zero by construction. A naive replay that scores the equal-frequency family only by self-root parity index coverage would reject the wrong question. The equal-frequency packet needs separate rows:

1. common-frequency closure;
2. phase-offset row-set identity;
3. energy-radius or lever-arm speed-regime placement;
4. branch-incidence or root-multiplicity support for the doubled inner burden;
5. retained angular-momentum and wake-energy routing on the same event or positive-width retained domain.

## Energy-Radius Proof Target

The branch energy/radius claim must be proved from the retained dynamics, not assigned. A clean proof target is:

$$
\omega_f
=
\frac{\partial H_a}{\partial J_a}
\left(J_a;\lambda_a\right),
\qquad
\rho_a
=
\rho_a\left(J_a;\lambda_a\right),
\qquad
a\in\{1,2,3\},
$$

where $H_a$ is the reduced branch Hamiltonian or energy functional, $J_a$ is the branch angular momentum, and $\lambda_a$ denotes retained layer data such as root sheet, wake charge, polarity assignment, or branch geometry. The proof must exhibit three retained branch states with the same $\omega_f$ but different $J_a$, $\rho_a$, and $s_a=\rho_a\omega_f$. Only after that row is retained should the solver attach a role map such as slow/hinge/fast or outer/middle/inner.

A minimal sanity check is the circular kinetic proxy

$$
J_a=\mu_a \rho_a^2\omega_f,
\qquad
E_{\mathrm{kin},a}
=
\frac{1}{2}\mu_a \rho_a^2\omega_f^2
=
\frac{1}{2}J_a\omega_f.
$$

This proxy does not prove the retained branch. It only shows why the idea is physically coherent: at fixed $\omega_f$, different effective lever arms give different speeds, angular momenta, and energies. The real proof must replace the proxy with the delayed branch action, wake-history terms, and root-ledger constraints.

## Relation To The Angular-Momentum Unit

This candidate may simplify the one-unit angular-momentum ledger. The existing packet often writes the unit as $\hbar$ in energy-frequency rows such as

$$
\Delta E=\omega_{\text{tx}}\hbar.
$$

For a four-substep transaction with weights $1:1:2$, the current weighted frequency target is

$$
\omega_\ast
=
\frac{\omega_O+\omega_M+2\omega_I}{4}.
$$

Under the equal-frequency family this collapses to

$$
\omega_\ast=\omega_f.
$$

That is the main attraction: the angular-momentum unit can remain one conserved action increment, while energy differences are carried by the common transaction frequency, tri-binary Noether swarm lever arms, phase offsets, wake routing, envelope deformation, and branch energy placement. The candidate therefore may reduce the need to explain a distinct weighted transaction frequency before the angular-momentum unit is accepted.

This is not yet an acceptance claim. The proof burden is to show that the same retained event can carry one angular-momentum unit through the common-frequency branch without hiding missing angular momentum in the wake, recoil, transport, or endpoint-provider rows.

## Sampled Runner Row Population

[tri-binary-offset-family-runner.mjs](../../../scripts/angular-momentum/tri-binary-offset-family-runner.mjs) now emits `aaa-tri-binary-frequency-candidate-solver-report.v11`. The v11 report is still role-assigned `I:M:O` in its current data order; under the newer configuration-space policy this should be read as a legacy candidate role map, not as the general search semantics. Outer-normalized `O:M:I` forms are retained only as `legacyOuterNormalized*` translation metadata. The runner includes an `equal-frequency` policy and an equal-frequency phase-profile catalog for the common-frequency relation `(f_1,f_2,f_3)=(f,f,f)`. The sampled replay

```sh
node scripts/angular-momentum/tri-binary-offset-family-runner.mjs --policy all --f-min 2 --f-max 8 --output .tmp/angular-momentum-spin/frequency-candidate-current-report-v11.json
```

emits `equalFrequencyEnergyRadiusAudit` with schema `aaa-equal-frequency-energy-radius-audit.v13` and status `equal_frequency_phase_catalog_populated_priority_triadic_pass_acceptance_blocked`. It now contains 28 equal-frequency cases: seven priority triadic-120 cases and 21 phase-control cases. The priority rows have `allAuditRowsPass=true`, all controls populate, and `triadic-120` ranks first by the triadic equal-spacing residual. For each sampled $f\in\{2,\ldots,8\}$, the priority triadic-120 rows satisfy

| Row | Current role-assigned value |
| --- | --- |
| Common angular frequency | $\omega_1=\omega_2=\omega_3=f$ |
| Retained effective lever-arm projections | $\rho_I=1.25/f$, $\rho_M=1/f$, $\rho_O=0.75/f$ |
| Speed ratios | $s_I/c_f=1.25$, $s_M/c_f=1$, $s_O/c_f=0.75$ |
| Phase turns | $\phi_I=0$, $\phi_M=1/3$, $\phi_O=2/3$ |
| Transaction-frequency collapse | $\omega_\ast=\omega_f=f$ |

The phase-profile catalog is:

| Phase profile | Role | Current role-assigned phase turns $(\phi_I,\phi_M,\phi_O)$ | Max triadic-spacing residual |
| --- | --- | --- | --- |
| `triadic-120` | priority | $(0,1/3,2/3)$ | $5.551115123125783\times10^{-17}$ |
| `quadrature-90` | control | $(0,1/4,1/2)$ | $1/6$ |
| `aligned-0` | control | $(0,0,0)$ | $1/3$ |
| `opposed-middle-180` | control | $(0,1/2,0)$ | $1/3$ |

The v13 audit also emits `aaa-equal-frequency-phase-response-discrimination.v2`. In the present fixed-receiver executable chart, the four phase profiles share the same root-count response signature: one root, one active root detail, and one phase diagnostic row per layer, with the inner self-hit span $2.262205171302801$ and zero middle/outer self-hit span. They are not identical as phase responses, however. The phase-delta means differ by phase profile; the middle-layer range across profiles is $0.21208071888628424$, and the outer-layer range is $0.11371534893878049$. The current status is therefore `phase_profiles_share_root_signature_but_phase_delta_means_differ`.

| Phase profile | Mean phase delta, inner | Mean phase delta, middle | Mean phase delta, outer |
| --- | --- | --- | --- |
| `triadic-120` | $-0.19334089091984827$ | $-0.17217460581083058$ | $-0.10598734898474163$ |
| `quadrature-90` | $-0.19334089091984827$ | $-0.25474400138533343$ | $-0.058883244932721866$ |
| `aligned-0` | $-0.19334089091984827$ | $-0.18442841937464652$ | $-0.17259859387150236$ |
| `opposed-middle-180` | $-0.19334089091984827$ | $-0.042663282499049195$ | $-0.17259859387150236$ |

This is the first executable warning that phase offsets matter even before retained binary-to-binary row-set identity is solved. The triadic profile remains the first target because it is the only equal-spacing profile and it produces a distinct phase-delta response, not because the current chart has already shown a root-count advantage.

The populated projection rows are `equal_frequency_common_clock`, `equal_frequency_lever_arm_speed_relation`, `triadic_120_phase_profile`, and `equal_frequency_transaction_frequency_collapse`. The triadic-120 priority rows pass in the reduced sampled replay, phase controls populate as controls, and the report keeps `retainedBranchClaim=false`.

The same report emits `aaa-tri-binary-frequency-runner-architecture.v6` with status `retained_branch_state_primary_velocity_deformation_projection_family_policy_family_layers`. The v13 equal-frequency audit includes an $S_3$ role-assignment audit, a deformation-regime audit, a deformation-continuation audit, a circular kinetic action-ledger proxy, and an effective-inertia law scan, while full raw-row preservation remains a retained replay requirement. Families declare search lanes, candidate class, optional phase profile, and optional role map. Policies declare compatible search lanes plus frequency, speed, phase, geometry, and audit-row models. The equal-frequency case is therefore not a one-off solver fork: it is the `equal-frequency-energy-radius` search lane paired with the `equal-frequency` policy. The primary branch model is `retained-tri-binary-noether-swarm-deformation-branch` under base geometry model `tri-binary-noether-swarm-velocity-deformation`; the current executable chart is the flattened-limit member of `tri-binary-noether-swarm-deformation-projection-chart-family`, not the retained geometry itself. The v11 report also emits `aaa-tri-binary-noether-swarm-branch-state-contract.v1`, which makes the retained branch state $B_{3B}(q,v)$ the object that all frequency candidates, phase profiles, deformation rows, and coupling rows must read from.

The search summary now emits `aaa-tri-binary-frequency-triplet-search-summary.v12` and `aaa-tri-binary-frequency-triplet-candidate-set-review.v8`. The review still reports the investigated candidate set in role-assigned `I:M:O` order:

| Candidate row | Role-assigned relation | Current disposition |
| --- | --- | --- |
| Equal-frequency priority | $(f_1,f_2,f_3)=(f,f,f)$ with optional role map | Separate energy-radius-phase candidate; old self-root parity proxy is not applicable. |
| Middle-hinge offset priority | $(I,M,O)=(f+2,f,f-1)$ | Top current offset-proxy family; retained branch still blocked. |
| Symmetric offset control | $(I,M,O)=(f+1,f,f-1)$ | Adjacent offset control for the middle-hinge family. |
| Dyadic integer-lock control | $(I,M,O)=(4f,2f,f)$ | Span-only / dyadic control under the current proxy. |
| General integer-lock controls | $(I,M,O)=(nf,mf,f)$ with $1<m<n$ | Finite $m:n$ competitor lattice; no retained competitor exclusion or acceptance. |

The same review records the old-order disposition explicitly: `O:M:I` is only outer-normalized translation metadata, not the primary solver order.

The v13 audit also emits `aaa-equal-frequency-role-assignment-s3-audit.v1`. It keeps the generic input labels $(B_1,B_2,B_3)$ separate from the role-assigned chart order and populates all six $S_3$ role maps for the triadic $120^\circ$ profile. These six maps collapse to one quotient-sector key, `equal_frequency_triadic_120_unordered_s3`, but the audit keeps the unquotiented rows as evidence. Three maps preserve the forward `I:M:O` $+120^\circ$ orientation, three maps reverse it to $-120^\circ$, and the current chart representative is `role-map-123`. The populated status is `s3_role_maps_populated_triadic_quotient_sector_one_plus_and_minus_orientations`.

The v13 audit also emits `aaa-equal-frequency-deformation-regime-audit.v2`. Across the seven priority triadic rows, low-drift spherical-orbit support passes in seven of seven rows, moving oblate-envelope incidence projection passes in seven of seven rows, and the flattened-limit projection chart passes in seven of seven rows. The common-sphere great-circle control passes in zero of seven rows: at common $\omega_f$, equal great-circle lever arms force equal speed magnitude, so that control cannot realize the current sub-field / field-speed / super-field ordering without an additional projection or causal-delay speed definition. For the $f=2$ row, the low-drift spherical-orbit support uses $R_{\mathrm{sph}}=0.625$ with $\sin\theta_I=1$, $\sin\theta_M=0.8$, and $\sin\theta_O=0.6$; the oblate incidence candidate uses $R_{\parallel}=0.375$, $R_{\perp}=0.625$, and $\xi=0.6$.

The v13 audit adds `aaa-equal-frequency-deformation-continuation-audit.v1`. This is the first reduced continuity witness that the spherical-orbit, oblate, and flattened-limit rows are projections of one branch-state deformation family rather than disconnected chart samples. The general retained target is

$$
\rho_a(\xi)
=
R_{\perp}
\sqrt{
\xi^2u_a^2+(1-\xi^2)q_a
},
\qquad
0\le \xi\le 1,
$$

where $u_a$ is the spherical-orbit lever-arm coordinate and $q_a$ is the flattened/incidence coordinate supplied by the retained branch state. The current audit only populates the restricted witness

$$
\rho_\ell(\xi)
=
R_{\perp}
\sqrt{
\xi^2+(1-\xi^2)q_\ell
},
\qquad
\xi=\frac{R_{\parallel}}{R_{\perp}},
$$

with $u_\ell=1$ and $q_\ell$ fixed by the current oblate slice. For the $f=2$ row, $\xi_{\mathrm{current}}=0.6$, the normalized current lever arms are

$$
\frac{\rho_I}{R_{\perp}}=1,
\qquad
\frac{\rho_M}{R_{\perp}}=0.8,
\qquad
\frac{\rho_O}{R_{\perp}}=0.6,
$$

and the incidence coordinates are

$$
q_I=1,
\qquad
q_M=\frac{7}{16},
\qquad
q_O=0.
$$

At the restricted equal-envelope-axis endpoint $\xi=1$, the same formula gives equal normalized lever arms $(1,1,1)$; at the current oblate slice $\xi=0.6$, it gives $(1,0.8,0.6)$; and the current flattened-limit chart agrees with that projected slice in seven of seven priority rows. The formal $\xi=0$ forecast gives $(1,\sqrt{7}/4,0)$ for the same $q_\ell$ values, but this is only a forecast row. The retained replay still must derive the velocity-deformation branch-continuation law from branch energy/action, including the general spherical-orbit coordinates $u_a$, rather than accept this kinematic witness as dynamics.

The v13 audit now also emits `aaa-equal-frequency-action-ledger-audit.v2`. This is only a circular kinetic proxy, but it is a useful one: with the current equal-frequency lever-arm projections, $\rho_I:\rho_M:\rho_O=5:4:3$, so the unit-inertia action ratios scale as $25:16:9$. The inner-tagged share is therefore

$$
\frac{25}{25+16+9}=\frac{1}{2}.
$$

Across the seven priority triadic rows, the proxy matches the inner half-action burden in seven of seven rows. It does not match the full four-substep $2:1:1$ partition: the proxy fractions are $J_I/J_{\mathrm{tot}}=0.5$, $J_M/J_{\mathrm{tot}}=0.32$, and $J_O/J_{\mathrm{tot}}=0.18$, so the middle and outer residuals against the $0.25:0.25$ target are $+0.07$ and $-0.07$. The audit reports zero of seven full unit-inertia four-substep matches. It also reports seven of seven positive algebraic effective-inertia solutions for the full $2:1:1$ partition, with the $f=2$ row requiring relative inertias normalized to the middle row

$$
\mu_I:\mu_M:\mu_O=1.28:1:1.7777777777777777.
$$

The v2 action audit adds `aaa-equal-frequency-effective-inertia-law-scan.v1`. The scan compares the required relative inertias against simple laws. Uniform branch inertia, root-weight-only inertia, direct-square lever-arm inertia, inverse-square lever-arm inertia, and inverse-square speed-ratio inertia all fail as complete independent laws. The useful near miss is inverse-square lever-arm inertia:

$$
\frac{\mu_a}{\mu_M}
=\left(\frac{\rho_M}{\rho_a}\right)^2.
$$

It matches the outer row exactly in seven of seven priority rows and misses only the inner row by the doubled factor: the mean inner residual is $-0.6399999999999998$. Multiplying the inverse-square lever-arm law by the four-substep/root-multiplicity weights gives an exact target-derived law in seven of seven rows:

$$
\frac{\mu_a}{\mu_M}
=
\frac{w_a}{w_M}
\left(\frac{\rho_M}{\rho_a}\right)^2
=
\frac{w_a}{w_M}
\left(\frac{s_M}{s_a}\right)^2.
$$

The status is `no_independent_simple_law_exact_target_weighted_inverse_square_law_exact`: no scanned independent kinematic law closes the retained inertia burden, but the scan has identified the precise proof route. The retained replay must derive both an inverse-square branch-inertia response and the inner doubled root/action weight, or replace them with an accepted wake/coupling action-partition law.

This is the current best reduced action result: equal frequency plus the sampled lever arms naturally carries the doubled inner burden under a unit-inertia proxy, while the full one-unit ledger still requires a retained effective-inertia law, wake/coupling angular momentum, or a revised action-partition law.

The v13 equal-frequency audit also emits `aaa-equal-frequency-retained-tri-binary-replay-target.v8`. This is the next mathematical object for the candidate. It asks the retained replay to close:

$$
\omega_1=\omega_2=\omega_3=\omega_f,
\qquad
s_a=\rho_a\omega_f,
\qquad
\rho_h\omega_f=c_f,
$$

plus the general velocity-deformation continuation target

$$
\rho_a(\xi)
=
R_{\perp}
\sqrt{\xi^2u_a^2+(1-\xi^2)q_a},
\qquad
0\le \xi \le 1,
$$

with $u_a$ supplied by the spherical-orbit branch rows and $q_a$ supplied by the flattened/incidence branch rows, plus an energy-radius stationarity row

$$
\partial_{\rho_a}
E_{\mathrm{branch}}
(q,v,\rho,\omega,\phi,L_{\mathrm{wake}})
=0,
\qquad
a\in\{1,2,3\},
$$

a one-unit angular-momentum ledger

$$
L_{\mathrm{retained}}
=
\sum_{a=1}^{3} \mu_a\rho_a^2\omega_f
+L_{\mathrm{wake}}
+L_{\mathrm{coupling}},
$$

and retained binary-to-binary phase row-set identity

$$
\Phi_{ij}^{\mathrm{hit}}(\phi_1,\phi_2,\phi_3;q,v)
$$

on the same retained row set. In the current chart, the common-clock, speed, hinge, deformation-support, circular kinetic proxy, and target-weighted effective-inertia law-scan rows are populated; the retained energy-radius law, same-event one-unit angular-momentum ledger, and retained phase row-set identity are still missing.
The angular-momentum row is the direct $h$-unit target: the retained replay must show whether the common-frequency branch supplies one accepted action unit without undeclared wake, recoil, endpoint-provider, or coupling slack.

Solver field contract. The v11 report intentionally removes the ambiguous public field `layers[].radius` and the misleading `geometryLiftTarget` / `retainedLiftTarget` geometry language. Physics-facing speed rows use `layers[].effectiveLeverArm`; the low-level circular-source parameter is `layers[].solverGeometry.circularSourceRadius`; the deformation-family object is named by `projectionChartFamily`; the general retained object is named by `retainedBranchModel`; and the complete object contract is named by `triBinaryNoetherSwarmBranchStateContract`. Old consumers that read removed fields must migrate rather than silently preserve the earlier ambiguity.

Important geometry verdict. The report explicitly distinguishes the current chart lever-arm field from a nested-shell radius interpretation:

$$
\rho_I>\rho_M>\rho_O
$$

passes as the speed-regime lever-arm order, but the nested-shell-radius order

$$
\rho_I<\rho_M<\rho_O
$$

fails. Therefore the current equal-frequency reduced row is not a proof that the inner shell has a larger physical shell radius. It says that the speed rows require a larger effective lever arm for the inner branch unless the retained tri-binary Noether swarm replay supplies another projection, envelope-deformation map, incidence map, or branch-radius map.

## Solver And Search Requirements

The runner now has a sampled equal-frequency mode, but it should not rank $(f_1,f_2,f_3)=(f,f,f)$ by the old self-root parity proxy alone. Required next search changes:

1. Replace the fixed speed rows with retained tri-binary Noether swarm branch-state effective lever arms, spherical-orbit rows, envelope-deformation maps, explicit shell-radius maps, coupling projection rows, or an explicit energy-radius candidate law.
2. Preserve the raw unordered labeled rows and all $S_3$-equivalent repeats; compute quotient-sector grouping only as an analysis view.
3. Lift the current phase-profile catalog beyond reduced initial conditions: triadic $120^\circ$ now outranks the aligned, quadrature, and opposed-middle controls by equal-spacing residual, and the v13 chart response shows profile-dependent phase-delta means plus the six $S_3$ role maps, deformation-regime support audit, and circular kinetic action proxy, but retained phase row-set identity, geometric phase, and wake-return delay are not solved.
4. Resolve whether the retained tri-binary Noether swarm branch uses latitude/small-circle lever arms, oblate-envelope incidence, branch-selected effective radii, planar-alignment support, or a separate speed projection.
5. Report whether the four-substep transaction-frequency certificate collapses to $\omega_{\text{tx}}=\omega_f$ without target-derived acceptance.
6. Report whether a field-speed hinge is captured by some $\rho_a\omega_f\approx c_f$ while the other two rows fall on opposite sides of $c_f$; do not assume the hinge row is known before the branch supplies it.
7. Add retained root-multiplicity or branch-incidence support for any doubled-burden role without assuming $n_I-n_M=2$.
8. Keep `retainedBranchClaim=false` until global retained row-set identity, phase, torque, wake, partition, action scale, energy routing, and stability rows close on the same retained row set.

The diagnostic `index-ratio` policy is not a valid rejection test for this candidate. That policy maps speed ratios to frequency-index ratios. The equal-frequency hypothesis says the speed ratios come from retained tri-binary Noether swarm lever arms, envelope deformation, and branch energy placement.

## Failure Conditions

Reject or demote the candidate if any of these fail under the retained branch rules:

1. The energy-radius law cannot produce the required effective lever arms and speed ordering at common $\omega_f$.
2. The retained tri-binary Noether swarm replay forces $\rho_\ell$ to equal nested shell radius while also requiring $\rho_I<\rho_M<\rho_O$; that would contradict the equal-frequency speed-row ordering unless another speed projection is derived.
3. Phase offsets cannot populate the hinge pair-map, row-set identity, or branch-incidence pattern.
4. Root multiplicity, wake routing, or action partition cannot replace the current $n_I-n_M=2$ proxy for the doubled inner burden.
5. The one-unit angular-momentum ledger requires undeclared wake, recoil, endpoint-provider, or root-energy slack.
6. A retained offset family closes the same rows with lower branch-selection cost while the equal-frequency family remains blocked.

## Priority Disposition

Priority: very high inside `angular-momentum-spin`.

Disposition: priority-only. The search now emits sampled equal-frequency rows, but promotion remains blocked until the energy-radius proof target has at least one retained or fail-closed candidate on the same retained row set.

Likely corpus destination if proved: [angular-momentum-and-spin](../../../content/markdown/aaa/philosophy-history/theory-bridges/angular-momentum-and-spin.md), with supporting derivation references in [causal-action-functional](../../../content/markdown/aaa/dynamics/causal-action-functional.md) or [master-equation](../../../content/markdown/aaa/dynamics/master-equation.md) if the energy-radius law is derived there first.
