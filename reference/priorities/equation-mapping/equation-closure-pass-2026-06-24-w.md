# Equation Closure Pass 2026-06-24 W

## Workstream Metadata

- Kind: `priority`
- Status: `draft`
- Parent: [Equation Mapping Internal Priority](priorities.md)
- Source inventory: [Equation Mapping Detail](equation.md)
- Claim level: score-neutral just-in-time review packet
- Promotion status: priority-only

## Scope

This pass creates the next narrowed entourage review packet after passes U and V. The retained-support side now rejects empty support shells, bare row bindings, one-point refinement, under-margin negative controls, missing per-step refinement evidence, and support-id drift across refinement steps. The next implementation risk is therefore not another support falsifier; it is the boundary between shared retained support and connection/holonomy no-retune evidence.

No equation scores change.

## Mathematical Focus

The new review packet asks for a Cartan-style attack on the proposed transport certificate:

$$
\mathfrak E_u
=
(e^A_u,\omega^A{}_{B,u},T^A_u,\Phi_{T^2}(u),W_{\mathrm{supp}},W_{\mathrm{hol}}),
$$

built over an already source-backed retained-domain certificate:

$$
\mathfrak D_N
=
(B_N,\Sigma_N,P_N,\mathcal K_{P_N},\rho_{\mathrm{ref}},\mathcal R_{\mathrm{rows}},\mathcal N_{\mathrm{neg}}).
$$

The packet explicitly asks whether $\mathfrak E_u$ should instead be formulated as a principal-bundle connection, associated coframe bundle, groupoid of row identifications, or flatness/curvature condition over the drift base.

The central question is the minimal definition of $W_{\mathrm{hol}}$: the witness that rejects separately tuned clock, envelope, momentum, and mass-shell rows even when their final residuals all vanish.

## Packet Created

The self-contained packet is:

```text
reference/entourage/review-packets/elie-cartan-eq02-04-connection-holonomy-transport-2026-06-24.md
```

It intentionally avoids repo-specific filenames in the body. It includes the current mathematical setup, the already-hardened support-side fake-evidence blockers, the proposed $\mathfrak D_N$ and $\mathfrak E_u$ split, candidate transport-step fields, and eight specific breakthrough questions.

The broader earlier source-contract split packet is now superseded by this narrower packet, because passes U and V changed the active question.

## Score Decision

No scores change.

- `EQ-02` remains `4`.
- `EQ-03` remains `4`.
- `EQ-04` remains `4`.
- `EQ-04A` remains `1`.

This pass creates a design-review object, not accepted retained support, accepted coframe extraction, or a holonomy witness.

## Next Action

Submit the narrowed Cartan packet. Do not add connection transport fields, holonomy-step checks, or connection-id stability checks until the response is integrated.
