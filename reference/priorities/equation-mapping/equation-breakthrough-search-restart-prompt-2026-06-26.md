# Equation Breakthrough Search Restart Prompt 2026-06-26

## Restart Boundary

This is the corrected restart prompt for the interrupted 2026-06-26 equation-mapping breakthrough search. The previous durable checkpoint claimed about 10h53 elapsed time, but that was not active runtime: the thread hit a token-limit interruption and did not automatically restart. Treat the earlier run as interrupted before the hard runtime requirement was satisfied.

Use the prior artifacts as score-neutral starting artifacts and search context, not accepted retained evidence. Do not redo broad first-pass work that already produced source-field maps, source-attempt fixtures, negative controls, or Equation Attack Cards unless a row needs a second substantive pass or a narrower blocker attack.

## Updated Prompt

```text
Closure goal:
Run a corrected nine-hour, multi-agent equation-mapping breakthrough restart that resumes from the interrupted 2026-06-26 run, starts with unfinished/unscored and low-score rows, preserves already landed score-neutral artifacts, and pushes first toward concrete accepted retained evidence. When accepted retained evidence does not land, record native-carrier candidates, checker-backed blocker diagnostics, source-contract boundaries, or cross-row eureka candidates as score-neutral search output only.

Use the AAA corpus advancement skill in team-agent mode.

Context:
- Work from the repository root (`$REPO_ROOT`).
- The previous overnight-style run did not actually satisfy the hard runtime requirement. It hit a token-limit interruption and did not automatically restart. Do not claim that run reached nine or ten hours.
- Treat [Equation Breakthrough Search 2026-06-26](equation-breakthrough-search-2026-06-26.md) as useful score-neutral checkpoint material, but correct its `Checkpoint 11` runtime claim as interrupted rather than completed.
- This restart is not a one-pass closure audit. It is a long-running research attack intended to run for up to 9 hours without operator babysitting.
- Start from the live equation inventory and score ladder:
  - `reference/priorities/equation-mapping/equation.md`
  - `reference/priorities/equation-mapping/equation-score-5-closure-ladder.md`
  - `reference/priorities/equation-mapping/equation-mapping.md`
  - `reference/priorities/equation-mapping/equation-common-architecture-2026-06-23-c.md`
  - `reference/priorities/equation-mapping/equation-breakthrough-search-2026-06-26.md`
- Prior work over-focused on high-value blockers and then falsely appeared to have run long after token interruption. This restart should deliberately continue from unfinished low-score work and second-pass implementation targets.
- Do not promote into `content/markdown/aaa`.
- Do not change scores unless accepted retained evidence actually lands.
- Do not stop after finding the first blocker. A blocker should trigger a narrower follow-up agent or a different angle on the same equation before moving on.

Previously landed score-neutral work to preserve:
- `EQ-07B` and `EQ-23A` now have priority-only packets; they remain absent from the main score table and score ladder. `EQ-28B` remains deferred unless a concrete high-energy propagation consumer appears.
- `EQ-04A` has a score-neutral Koide residual checker and direct-fit negative control; it still inherits the `EQ-02` through `EQ-04` retained-domain blocker.
- `EQ-12` has a `theta_gamma_packet` source shell and source-attempt fixture; it still blocks at `missing_accepted_theta_gamma_packet`.
- `EQ-17` has a `theta_transfer` source-field map, default attempt, and priority-source negative control; it still blocks at `missing_accepted_theta_transfer`, and numeric/path-quality/segment-energy passes remain score-neutral.
- `EQ-22A` has a `theta_bb` source-field map and source-attempt fixture; it still blocks behind `missing_accepted_theta_gamma_packet`.
- `EQ-26A` has a `theta_alpha` source-field map; it still needs scheme-pinned charge/exposure evidence and remains score-neutral.
- `EQ-28A` has a `Theta_nu-ex` source-field map and source-attempt fixture; it still blocks at `missing_accepted_path_frequency_exchange_carrier`.
- `EQ-13`/`EQ-28` Gate A has a source-attempt payload and coordination-source controls; it still blocks at `missing_accepted_photon_gate_A_input_output`.
- `EQ-29` has a radiation-source carrier map and source-attempt fixture; it still blocks at `missing_accepted_radiation_source_carrier`.
- `EQ-14`/`EQ-30`/`EQ-31` finite-window work now rejects missing, coordination, and self-referential sources; it still blocks at `missing_accepted_W`.
- `EQ-24`/Noether sea density-compression rejects coordination and authored-prose false positives; it still blocks at `missing_accepted_theta_sea_rho_NS`.
- `EQ-25` has CMB thermal source-window and coordination-source negative controls; it still blocks at `missing_accepted_theta_therm`.
- `EQ-26` has a two-line `theta_H_spec` identity checker, source-attempt fixture, carrier-shell boundary, and fail-closed observer-Rydberg, priority-source, and support-carrier substitution controls; it still blocks at `missing_accepted_theta_H_spec`, with a carrier-shell boundary only advancing to `missing_accepted_recovered_label_rows`. No score change follows.
- Recent findability clarifications landed with no score changes:
  - Malus law routes to `EQ-12` / Gate B analyzer evidence, not a standalone row.
  - MOND routes to `EQ-32` as observer-level RAR/BTFR comparison, not substrate ontology.
  - Planck energy $E=h\nu$ belongs to `EQ-12`/`EQ-12A`; Planck blackbody belongs to `EQ-22A`.
  - Blackbody ultraviolet catastrophe belongs to `EQ-22A`; QFT-style ultraviolet divergence/renormalization belongs to `EQ-26A` only as alpha/running/exposure evidence.

Hard runtime requirement:
- Target wall-clock runtime: 9 hours of active restart work.
- Do not finish before 8 hours 45 minutes if there is still meaningful work in the queue.
- “Meaningful work” includes any unprocessed equation in the current score band, any equation with only one pass completed, any unresolved existing checker/fixture/source packet, any plausible cross-row clue, or any safe priority/script artifact that would sharpen a concrete mathematical route.
- If an agent finishes quickly, recycle it into the next equation or a narrower blocker attack.
- If all rows through score `3` receive at least two substantive passes before 8 hours 45 minutes, begin a second sweep over the highest-potential discoveries rather than ending early.
- End early only if every row through score `3` has two substantive passes, no safe edits remain, no checker/source search remains, all agents agree the queue is exhausted, and the coordinator writes a precise exhaustion report.

Required start:
1. Run `git status --short --untracked-files=all`. Do not revert existing changes.
2. Read the AAA corpus advancement skill and canonical corpus-advancement prompt.
3. Read the equation inventory, score ladder, mapping overview, common architecture, and prior breakthrough checkpoint files listed above.
4. Build the live equation queue dynamically:
   - First: rows with blank/missing `6/23 b`, rows present in packets/scripts but missing from `equation.md`, or newly added suffix rows not fully integrated.
   - Then score `1`.
   - Then score `2`.
   - Then score `3`.
   - Then score `4` only if the lower-score queue is exhausted.
   - Skip `EQ-01` unless a low-score row directly needs the root law.
5. From the current table, expect:
   - Unscored/not fully integrated: `EQ-07B`, `EQ-23A`; `EQ-28B` deferred unless a concrete consumer appears.
   - Score `1`: `EQ-04A`.
   - Score `2`: `EQ-07A`, `EQ-11A`, `EQ-12A`, `EQ-15`, `EQ-16`, `EQ-22A`, `EQ-22B`, `EQ-26A`, `EQ-27`, `EQ-28A`, `EQ-30`, `EQ-31`.
   - Score `3`: `EQ-10`, `EQ-11`, `EQ-12`, `EQ-13`, `EQ-14`, `EQ-16A`, `EQ-18`, `EQ-19`, `EQ-20`, `EQ-21`, `EQ-22`, `EQ-23`, `EQ-24`, `EQ-25`, `EQ-26`, `EQ-28`, `EQ-29`, `EQ-32`.
   - Verify this list against live files before relying on it.

Team-agent structure:
- Keep 4-7 agents active when possible.
- Assign disjoint equation shards by score band and row id.
- For each equation, use at least two agent roles before declaring it exhausted:
  1. Carrier/evidence agent: identify the smallest native carrier or accepted evidence object that could move the row.
  2. Breakthrough scout: look for cross-row, inverse-equation, solved-wave, source-packet, or analogy clues that might unlock the row from a different direction.
- When a checker exists, run it or inspect its attempt fixture. If it fails, record the exact first blocker and spawn a follow-up agent asking whether the blocker can be narrowed or bypassed by a different accepted object.
- When no checker exists, the agent must propose the smallest concrete mathematical artifact: definition, lemma, invariant, residual equation, proof route, source-backed row, or simulation target.

Restart priorities for the first two coordinator cycles:
1. Correct the prior runtime record and create this restart prompt/checkpoint.
2. Re-attack the highest-value remaining evidence objects:
   - `theta_sea_rho_NS` retained-window evidence source for `EQ-24`, `EQ-20`, `EQ-11`, and `EQ-32`, with `EQ-06` consuming the same density/cadence projection route.
   - `theta_transfer` for signed-frequency / `EQ-17`, `theta_cos` for effective-FRW / `EQ-18` and `EQ-19`, `neutral_lepton_retained_branch` for `EQ-16A`, `theta_obs` for shared-observation rows, `branch_chart` for `EQ-05`, and `theta_gamma_packet` for photon rows; keep these as live first blockers, not accepted retained evidence.
   - finite-window `W` for `EQ-31`, especially the charged-pion route from guard-passing source paths.
   - compact-region `Theta_cs_07A` source-attempt fixture and source-evidence guard.
   - `EQ-12A` retained action-period source-attempt fixture and dedicated source-evidence guard.
   - `theta_gamma_packet` parent support, including Malus/Gate B analyzer evidence as a possible narrow benchmark.
   - `theta_alpha` source-attempt route for `EQ-26A`, keeping QFT ultraviolet divergence/renormalization separate from blackbody ultraviolet catastrophe.
   - `Theta_src`, `theta_therm_CMB`, `theta_H_spec`, `radiation_source_carrier`, and Gate A only when they can sharpen a first blocker or accepted-evidence contract.

Per-equation minimum:
- Spend at least 12 minutes of active agent/coordinator work per equation before moving on, unless an accepted evidence object lands or the equation is proven blocked by a source/theory decision.
- For score `1` and unscored rows, spend at least 25 minutes each unless exhausted.
- Each equation must produce or update an Equation Attack Card with:
  - current score and closure driver;
  - primary AAA carrier;
  - smallest accepted evidence object;
  - exact first blocker;
  - existing scripts/fixtures/packets found;
  - one candidate breakthrough angle;
  - one fail-closed negative control;
  - one next action smaller than “write a broad report.”
- Each Equation Attack Card must also include or point to a packet-level Direct Geometry Layer with these fields before the row is treated as having a complete blocker route. The Direct Geometry Layer is routing evidence only; a row is score-review-ready only after the named accepted evidence object actually lands with durable source evidence and checker support:
  - comparison term;
  - $\mathbb{A}\mathbb{A}\mathbb{A}$ geometric readout;
  - required carrier or row;
  - same-record binding;
  - fail-closed negative control;
  - smallest accepted evidence object.

Coordinator cycle:
Repeat until the 9-hour cap or true exhaustion:
1. Launch or recycle agents for the next unprocessed equation shard.
2. Wait for worker outputs; do not busy-poll.
3. Merge findings into the live queue.
4. Choose one safe implementation target per cycle:
   - priority packet refinement;
   - existing script/fixture improvement;
   - exact accepted-evidence object contract;
   - source-backed row template only if it directly targets a first blocker;
   - proof-route or residual equation if no code artifact is safe.
5. Apply safe edits only under:
   - `reference/priorities/equation-mapping/`
   - existing `scripts/equation-mapping/`
   - existing `scripts/spacetime/` equation-mapping Noether sea runners.
6. Do not add new gates, ledgers, or broad scaffolding unless they directly populate or sharpen one existing first blocker.
7. Every 60-90 minutes, write or update a compact checkpoint in `reference/priorities/equation-mapping/` that records:
   - elapsed active restart time;
   - equations processed;
   - agents active/completed;
   - current best breakthrough candidate;
   - files edited;
   - validation status if edits were made;
   - remaining queue.

Breakthrough search posture:
- Prefer rows that have not received serious effort or still lack second-pass implementation work over rows already hammered by prior closure passes.
- Look for “eureka elsewhere” clues:
  - a low-score row that defines a missing carrier needed by a high-score blocker;
  - a suffix equation that has a cleaner source-backed event than the main row;
  - a standard equation whose inverse form exposes a native retained variable;
  - a solved-wave family, conservation law, finite-window measure, or source-event ledger that can become a shared carrier;
  - a negative control that reveals the exact missing native object;
  - a score `2` row whose first accepted object is smaller than the known Bucket A/B/C blockers.
- Preserve candidate-level status. A promising synthesis is a breakthrough candidate, not canon, until evidence lands.

No-score-change rule:
- Scores may change only if accepted retained evidence actually lands and validation/checkers support score review.
- If only contracts, packets, attempt fixtures, toy fixtures, or closure cards are added, state `no score changes`.
- Priority prose, authored AAA prose, generated files/copies, mocks, toys, attempts, probes, source-evidence probes, negative controls, temporary paths, directory paths, self-source shells, and source-contract shells do not count as accepted retained evidence.

Validation:
Run at the end, and after any substantial edit batch:
- `git diff --check`
- `node scripts/validate-content.mjs --check --strict`
- `node scripts/build-scene-graph.mjs --check --strict`

Also run any relevant equation-specific checker touched by the cycle.

Expected final output:
- Actual active restart runtime and whether the 9-hour target was reached.
- Explicit correction that the prior run was interrupted and should not be cited as a completed 9+ hour run.
- Agent roster, equation shards, and time spent by score band.
- Dynamic queue used: unscored, score `1`, score `2`, score `3`, and any score `4` reached.
- Equation Attack Card summary for every processed equation.
- Best breakthrough candidates found, ranked by expected ability to unlock multiple rows.
- Smallest accepted evidence object proposed for each processed equation.
- Coordinator implementation targets chosen per cycle.
- Files edited.
- No-score-change statement unless accepted evidence truly landed.
- Validation results.
- Remaining queue and exact reason for any early stop.
- A ranked next-action list focused on accepted retained evidence and mathematical breakthroughs, not more scaffolding.

Final handoff must include:
Thread state: DONE or BLOCKED
Mode: team-agent
Authority used: edited files or report-only
Files changed: list
Validation: passed or failed
Closure goal: concise next objective
```

## Promotion Classification

Classification: `priority-only`.

Promote now: no.

Defer with blocker: this restart prompt corrects runtime state and directs priority/search work. It does not add accepted retained evidence and does not justify any score change.
