# Equation Breakthrough Search Prompt

```text
Closure goal:
Advance the least-closed equation-mapping rows toward their smallest accepted retained evidence objects while preserving score-neutral boundaries for every diagnostic, source contract, attempt, and negative control.

Use the compatibility-identifier `aaa-corpus-advancement` skill in team-agent mode.

Context:
- Resolve the live queue from:
  - `reference/priorities/equation-mapping/priorities.md`
  - `reference/priorities/equation-mapping/equation.md`
  - `reference/priorities/equation-mapping/equation-score-5-closure-ladder.md`
  - the focused packet that owns each selected row
- Treat the tracker, work log, priority prose, authored corpus prose, generated files, attempts, probes, toys, mocks, negative controls, and source contracts as coordination or diagnostic material unless the owning checker explicitly accepts an independent retained evidence source.
- Do not promote into `content/markdown/aaa` during this search.
- Do not change a score unless accepted retained evidence lands and the owning checker consumes it successfully.

Task:
1. Run `git status --short --untracked-files=all` and preserve unrelated worktree state.
2. Rank unfinished and low-score rows by the expected cross-row value of their smallest missing accepted object.
3. For each selected row, read the owning checker obligation before proposing work.
4. Attempt the smallest source-backed evidence object that could move the first blocker.
5. If accepted evidence cannot land, improve the native carrier contract, source-boundary diagnostic, independent negative control, or exact blocker statement without claiming score movement.
6. Record chronological status in `work-log.md`, provisional theory ideas in `brainstorming.md`, and durable proof/evidence structure in the owning focused packet. Keep `priorities.md` compact.
7. Continue through the ranked queue while safe, scoped work remains; do not stop merely because one row is blocked.

Constraints:
- Reason from $\mathbb{A}\mathbb{A}\mathbb{A}$ primitives at the native layer. Standard equations are recovery targets, observational constraints, or clearly labeled comparisons, never native premises.
- Grade each substantive claim as derived, measured, inferred, or guessed.
- Name the independent reference behind any correctness claim.
- Preserve TeX and canonical terminology.
- Leave equation scores unchanged unless the accepted-evidence condition is actually met.
- Do not create another dated checkpoint ledger. Use the compact tracker, focused packets, and work log.

Expected output:
- Rows attempted and their exact first blockers.
- Smallest accepted evidence object attempted for each row.
- Files edited and focused validation results.
- Explicit score decisions and promotion classification.
- Remaining ranked queue and exact next action.
- Thread-state handoff using the corpus-advancement protocol.
```

## Promotion Classification

Classification: `priority-only`.

Promote now: no. This reusable prompt routes proof search; it is not equation evidence.
