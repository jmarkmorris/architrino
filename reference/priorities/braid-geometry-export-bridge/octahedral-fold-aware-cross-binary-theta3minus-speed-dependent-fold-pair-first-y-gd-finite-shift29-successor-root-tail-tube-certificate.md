# Octahedral Fold-Aware Cross-Binary Theta3minus Speed-Dependent Fold-Pair First-Y G/D Finite Shift29 Successor Root-Tail Tube Certificate

Promotion status: `diagnostic-only; receiver-normal restart required`.

Receiver-normal quarantine. This packet is historical diagnostic material for root geometry or dependency provenance only. It is not force/action evidence and cannot support branch promotion until same-record   and ^{\mathrm{rec}} are regenerated and accepted.

This packet closes the finite positive-y successor root-tail tube exposed by the
twenty-fifth-order post-$U$ successor coefficient certificate. It certifies the
finite $Z_\varepsilon(y,\nu)$ tube used by

$$
h_\varepsilon(y,\nu)
=
h_{\varepsilon,\le24}(y,\nu)
+y^{25}Z_\varepsilon(y,\nu),
\qquad
Z_\varepsilon(0,\nu)=h_{25,\varepsilon}(\nu).
$$

It does not close the continuous quotient-tail bound, full first-y quotient
enclosure, scaled remainder, `I1`, quadrature, or retained branch status.

No fixed speed band is imposed. The certificate uses only the certified positive
speed-ratio zero enclosure

$$
\nu\in[3.02156,3.02157],
$$

subdivided into $128$ directed-rounded speed cells.

## Shifted Successor Residual

The finite successor residual is

$$
R_{\varepsilon,29}(y,Z,\nu)
=
\operatorname{Shift}_{29}\!\left(
F_\varepsilon(y,h_{\varepsilon,\le24}+y^{25}Z,\nu)
\right).
$$

The lower coefficients $y^0$ through $y^{28}$ are dropped only after interval
containment of zero is certified. The positive-y rows then certify endpoint
signs in the tube

$$
Z_\varepsilon
\in
[h_{25,\varepsilon}^{-}-10^{16},
h_{25,\varepsilon}^{+}+10^{16}],
$$

with the inherited derivative orientation

$$
\partial_ZR_{\varepsilon,29}=J_\varepsilon.
$$

The induced predecessor image is also certified:

$$
U_\varepsilon
=
h_{23,\varepsilon}
+yh_{24,\varepsilon}
+y^2Z_\varepsilon
$$

stays inside the already-certified finite $U_\varepsilon$ tube on every
positive-y subcell.

## Certified Result

Across the $128$ speed cells, two branch signs, and $15$ positive-y subcells:

| Row | Directed-rounded interval result |
| --- | ---: |
| speed cells | $128$ |
| positive-y row count | $3840$ |
| inherited $h_{25}$ artifact valid | `true` |
| all lower shift coefficients $y^0$ through $y^{28}$ contain zero | `true` |
| all $\operatorname{Shift}_{29}$ endpoint signs certified | `true` |
| all $\partial_ZR_{\varepsilon,29}$ signs certified | `true` |
| all induced $U_\varepsilon$ images stay inside predecessor tube | `true` |
| minimum endpoint clearance | $7.9188994295\times10^{15}$ |
| minimum $\partial_ZR_{\varepsilon,29}$ clearance | $0.791609030534$ |
| maximum $Z$-tube absolute upper endpoint | $1.01521961838\times10^{16}$ |
| maximum induced $U$-image absolute upper endpoint | $4.35162728309\times10^{12}$ |
| maximum lower-shift residual interval witness | $2.01275998755\times10^{16}$ |
| maximum shifted trigonometric remainder | $9.50819787001\times10^{-146}$ |
| left endpoint interval hull | $[-8176929956290000,8168025764310000]$ |
| right endpoint interval hull | $[-8168025761540000,8176930000550000]$ |

The status is

$$
\boxed{
\texttt{directed-rounded-theta3minus-fold-pair-first-y-GD-finite-shift29-successor-root-tail-tube-certified}.
}
$$

## Claim Boundary

This packet may claim:

$$
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_finite\_successor\_root\_tail\_tube=true}.
$$

It does not claim:

$$
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_continuous\_successor\_tail\_bound=false},
\qquad
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_finite\_remainder\_bound=false},
$$

$$
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_jet\_enclosure=false},
\qquad
\texttt{certifies\_directed\_rounded\_fold\_pair\_scaled\_remainder=false},
$$

$$
\texttt{certifies\_I1\_regular\_critical\_exhaustion=false},
\qquad
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_quadrature=false},
\qquad
\texttt{retained\_branch=false}.
$$

## Closure Burndown

| Row | Status |
| --- | --- |
| `theta3minus.fold-pair-first-y-GD-twenty-fifth-order-post-U-successor-coefficient` | directed-rounded interval certified |
| `theta3minus.fold-pair-first-y-GD-finite-shift29-successor-root-tail-tube` | directed-rounded positive-y certified |
| `theta3minus.fold-pair-first-y-GD-twenty-sixth-order-tail-bound` | directed-rounded open |
| `theta3minus.fold-pair-first-y-GD-directed-rounded-enclosure` | blocked by twenty-sixth-order tail bound |
| `theta3minus.fold-pair-scaled-remainder-continuous-collar` | blocked by directed-rounded first-y $G,D$ enclosure |

## Next Tail Target

The next quotient-tail successor is

$$
T_G^{(26)}
=
\operatorname{Shift}_{28}\!\left(
P-L-y^2A_{G,25}
\right),
\qquad
Q_G=A_{G,25}+y^{26}T_G^{(26)},
$$

and

$$
T_D^{(26)}
=
\operatorname{Shift}_{28}\!\left(
D_{\mathrm{pair}}-L-y^2A_{D,25}
\right),
\qquad
Q_D=A_{D,25}+y^{26}T_D^{(26)}.
$$

The correlated identity is

$$
T_D^{(26)}
=
-27T_G^{(26)}
-y\partial_yT_G^{(26)}.
$$

The inherited budget floors are

$$
B_{G,26}=2.07319466431\times10^{70},
\qquad
B_{D,26}=2.07305754145\times10^{70}.
$$

The remaining first-y blocker is a factored quotient/inverse Taylor-model bound
on this certified $Z_\varepsilon$ tube. The expected pressure is not the
root-tail endpoint sign row; it is the width growth in the quotient inverse and
in the $y\partial_yT_G^{(26)}$ term needed for the $D$ identity.

## Executable Artifact

The executable packet is
[octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift29-successor-root-tail-tube-certificate.mjs](../../../scripts/neutral-braid/octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift29-successor-root-tail-tube-certificate.mjs).
It emits:

- directed-rounded $\operatorname{Shift}_{29}$ endpoint signs on $3840$
  positive-y rows;
- $\partial_ZR_{\varepsilon,29}=J_\varepsilon$ sign certification;
- lower coefficient containment through $y^{28}$ before shifting;
- induced $U_\varepsilon$ image containment inside the predecessor tube;
- explicit open continuous tail, full quotient, scaled-remainder, `I1`,
  quadrature, and retention rows.

The companion test
[neutral-braid-octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift29-successor-root-tail-tube-certificate.test.js](../../../tests/neutral-braid-octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift29-successor-root-tail-tube-certificate.test.js)
validates schema, no-fixed-speed-window discipline, endpoint signs,
monotonicity, predecessor containment, overclaim rejection, and CLI
write/validate behavior.
