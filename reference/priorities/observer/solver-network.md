The purpose is to contemplate a full combinatorial search for solutions.
1. leverage the 5 lane model and combinatorials.
    `Pass Thru` is a direct op.
    `Dissociate` produces `Unbound Architrinos` and often a `Noether core`.
    Xyzzy may render that same participant as the four-tile `Unbound | Electrinos | Positrinos | Architrinos` group.


    
    TO DO: research this

    1. each lane 1 assembly can either go to `Pass Thru` or `Dissociate`
    2. each lane 3 assembly can either go to `Pass Thru` or `Associate`
