# Octahedral Fold-Aware Cross-Binary Theta3minus Speed-Dependent Fold-Pair First-Y G/D Continuous Post-Seed Tail-Bound Target

Promotion status: `priority-only`.

This target tracks the remaining zero-touching first-y blocker after the finite
$\operatorname{Shift}_{25}$ $E_\varepsilon$ root-tail tube, the positive
first-y $G,D$ quotient subcells, the twenty-third-order $U$-seed coefficient,
the finite $\operatorname{Shift}_{27}$ $U_\varepsilon$ root-tail tube, the
twenty-fourth-order post-$U$ coefficient, and the twenty-fifth-order successor
coefficient are certified. The finite $\operatorname{Shift}_{29}$ successor
root-tail tube is now also certified. A valid C1 attempt on that certified
$Z_\varepsilon$ tube produced a rigorous obstruction for the broad factored
positive-y interval evaluator, and the coefficient chain then advanced one
more row to the certified $h_{26}$, $Q_{G,26}$, and $Q_{D,26}$ row. The finite
$\operatorname{Shift}_{31}$ next-successor root-tail tube is now certified. The
root-tangent C1 attempt on that certified $W_\varepsilon$ tube proves that the
broad positive-y interval evaluator is still too wide. The lower numerator
coefficients through $y^{28}$ are certified to contain zero before any
$y^{29}$ division, and the successor coefficient rows now certify
$h_{27}$ through $h_{38}$ together with their induced $Q_G,Q_D$ rows. The
thirty-eighth-order successor coefficient packet certifies $h_{38}$,
$Q_{G,38}$, $Q_{D,38}$, and $Q_{D,38}+39Q_{G,38}\ni0$. The live constructive
problem is therefore the coefficient-preserving thirty-ninth-order $G,D$
successor tail, without dividing by a zero-touching $y$ interval.

The next certificate should not use the broad finite $E_\varepsilon$ tube as the
post-seed tail coordinate. The sharper zero-touching coordinate is

$$
E_\varepsilon(y,\nu)
=
h_{21,\varepsilon}(\nu)
+yh_{22,\varepsilon}(\nu)
+y^2U_\varepsilon(y,\nu),
$$

so that

$$
h_\varepsilon(y,\nu)
=
h_{\varepsilon,\le22}(y,\nu)
+y^{23}U_\varepsilon(y,\nu).
$$

The finite $U_\varepsilon$ certificate already verifies that the induced
$E_\varepsilon$ image stays inside the already-certified finite
$E_\varepsilon$ tube. The tail bound itself should therefore be stated in the
post-seed $U_\varepsilon$ coordinate.

## Original Twenty-Third-Order Tail Residuals

Let

$$
P=G_-+G_+,
\qquad
D_{\mathrm{pair}}=P-y\partial_yP,
$$

and let

$$
A_{G,22}(y,\nu)=\sum_{k=0}^{22}Q_{G,k}(\nu)y^k,
\qquad
A_{D,22}(y,\nu)=\sum_{k=0}^{22}Q_{D,k}(\nu)y^k.
$$

The zero-safe shifted tails are

$$
T_G^{(23)}
=
\operatorname{Shift}_{25}
\left(
P-L-y^2A_{G,22}
\right),
\qquad
Q_G=A_{G,22}+y^{23}T_G^{(23)},
$$

and

$$
T_D^{(23)}
=
\operatorname{Shift}_{25}
\left(
D_{\mathrm{pair}}-L-y^2A_{D,22}
\right),
\qquad
Q_D=A_{D,22}+y^{23}T_D^{(23)}.
$$

The certificate must preserve the correlated identity

$$
T_D^{(23)}
=
-24T_G^{(23)}
-y\partial_yT_G^{(23)}.
$$

## Budget Condition

On the first-y cell

$$
Y=\frac{0.115}{64}=0.001796875,
$$

the inherited twenty-third-order budget floors are

$$
B_{G,23}=1.20280111306\times10^{62},
\qquad
B_{D,23}=1.20272156491\times10^{62}.
$$

The successor closes only when the shifted, zero-touching rows prove

$$
\sup_{0\le y\le Y}|T_G^{(23)}|<B_{G,23},
\qquad
\sup_{0\le y\le Y}|T_D^{(23)}|<B_{D,23}.
$$

Equivalently, the $D$ row may be closed through the identity if

$$
\sup|T_G^{(23)}|<B_{G,23},
\qquad
24\sup|T_G^{(23)}|
+\sup|y\partial_yT_G^{(23)}|
<B_{D,23}.
$$

The $U$-seed packet consumes the coefficient part of this target and moves the
successor-tail chain one order higher.

## Closed Twenty-Fourth-Order Coefficient Row

Let

$$
A_{G,23}(y,\nu)=\sum_{k=0}^{23}Q_{G,k}(\nu)y^k,
\qquad
A_{D,23}(y,\nu)=\sum_{k=0}^{23}Q_{D,k}(\nu)y^k.
$$

The then-active zero-safe tails were

$$
T_G^{(24)}
=
\operatorname{Shift}_{26}
\left(
P-L-y^2A_{G,23}
\right),
\qquad
Q_G=A_{G,23}+y^{24}T_G^{(24)},
$$

and

$$
T_D^{(24)}
=
\operatorname{Shift}_{26}
\left(
D_{\mathrm{pair}}-L-y^2A_{D,23}
\right),
\qquad
Q_D=A_{D,23}+y^{24}T_D^{(24)}.
$$

The preserved correlated identity is

$$
T_D^{(24)}
=
-25T_G^{(24)}
-y\partial_yT_G^{(24)}.
$$

The updated budget floors are

$$
B_{G,24}=6.69384967266\times10^{64},
\qquad
B_{D,24}=6.69340696994\times10^{64}.
$$

The successor closes only when the shifted, zero-touching rows prove

$$
\sup_{0\le y\le Y}|T_G^{(24)}|<B_{G,24},
\qquad
\sup_{0\le y\le Y}|T_D^{(24)}|<B_{D,24}.
$$

Equivalently, the $D$ row may be closed through the identity if

$$
\sup|T_G^{(24)}|<B_{G,24},
\qquad
25\sup|T_G^{(24)}|
+\sup|y\partial_yT_G^{(24)}|
<B_{D,24}.
$$

The twenty-fourth-order post-$U$ coefficient packet consumes the leading
coefficient of this target. It certifies

$$
h_{24,\varepsilon}\in[-25502690592000,25502690592000],
$$

$$
Q_{G,24}\in[-21722122111200,21723283979000],
\qquad
Q_{D,24}\in[-543082099475000,543053052779000],
$$

with the correlated identity interval

$$
Q_{D,24}+25Q_{G,24}\ni0.
$$

This coefficient row moves the successor-tail chain one order higher.

## Closed Twenty-Fifth-Order Coefficient Row

Let

$$
A_{G,24}(y,\nu)=\sum_{k=0}^{24}Q_{G,k}(\nu)y^k,
\qquad
A_{D,24}(y,\nu)=\sum_{k=0}^{24}Q_{D,k}(\nu)y^k.
$$

The then-active zero-safe tails were

$$
T_G^{(25)}
=
\operatorname{Shift}_{27}
\left(
P-L-y^2A_{G,24}
\right),
\qquad
Q_G=A_{G,24}+y^{25}T_G^{(25)},
$$

and

$$
T_D^{(25)}
=
\operatorname{Shift}_{27}
\left(
D_{\mathrm{pair}}-L-y^2A_{D,24}
\right),
\qquad
Q_D=A_{D,24}+y^{25}T_D^{(25)}.
$$

The preserved correlated identity is

$$
T_D^{(25)}
=
-26T_G^{(25)}
-y\partial_yT_G^{(25)}.
$$

The updated budget floors are

$$
B_{G,25}=3.72527166308\times10^{67},
\qquad
B_{D,25}=3.72502527045\times10^{67}.
$$

The successor closes only when the shifted, zero-touching rows prove

$$
\sup_{0\le y\le Y}|T_G^{(25)}|<B_{G,25},
\qquad
\sup_{0\le y\le Y}|T_D^{(25)}|<B_{D,25}.
$$

Equivalently, the $D$ row may be closed through the identity if

$$
\sup|T_G^{(25)}|<B_{G,25},
\qquad
26\sup|T_G^{(25)}|
+\sup|y\partial_yT_G^{(25)}|
<B_{D,25}.
$$

The twenty-fifth-order post-$U$ successor coefficient packet consumes the
leading coefficient of this target. It certifies

$$
h_{25,\varepsilon}\in[-152196183760000,152188011264000],
$$

$$
Q_{G,25}\in[-129647700798000,129647700798000],
\qquad
Q_{D,25}\in[-3370840220760000,3370840220760000],
$$

with the correlated identity interval

$$
Q_{D,25}+26Q_{G,25}\ni0.
$$

At the $h_{25}$ stage, the next object was a finite successor tube before the
continuous quotient-tail evaluator could be attempted. That stage is now a
closed predecessor row and a source for the C1 obstruction below.

## Closed Shift29 Predecessor Root-Tail Coordinate

Define the successor root-tail coordinate by

$$
h_\varepsilon(y,\nu)
=
h_{\varepsilon,\le24}(y,\nu)
+y^{25}Z_\varepsilon(y,\nu),
\qquad
Z_\varepsilon(0,\nu)=h_{25,\varepsilon}(\nu).
$$

The finite successor tube certificate now certifies

$$
R_{\varepsilon,29}(y,Z,\nu)
=
\operatorname{Shift}_{29}\!\left(
F_\varepsilon(y,h_{\varepsilon,\le24}+y^{25}Z,\nu)
\right)
$$

with endpoint signs, inherited $y=0$ seed, and the derivative orientation

$$
\partial_ZR_{\varepsilon,29}=J_\varepsilon.
$$

The broad continuous quotient-tail evaluator targeted the one-order-higher
residuals on this certified $Z_\varepsilon$ tube; the directed-rounded C1
attempt below proves that broad evaluator too wide.

## Obstructed Twenty-Sixth-Order Predecessor Tail Residuals

Let

$$
A_{G,25}(y,\nu)=\sum_{k=0}^{25}Q_{G,k}(\nu)y^k,
\qquad
A_{D,25}(y,\nu)=\sum_{k=0}^{25}Q_{D,k}(\nu)y^k.
$$

The pre-$h_{26}$ zero-safe tails after the finite successor tube were

$$
T_G^{(26)}
=
\operatorname{Shift}_{28}
\left(
P-L-y^2A_{G,25}
\right),
\qquad
Q_G=A_{G,25}+y^{26}T_G^{(26)},
$$

and

$$
T_D^{(26)}
=
\operatorname{Shift}_{28}
\left(
D_{\mathrm{pair}}-L-y^2A_{D,25}
\right),
\qquad
Q_D=A_{D,25}+y^{26}T_D^{(26)}.
$$

The preserved correlated identity is

$$
T_D^{(26)}
=
-27T_G^{(26)}
-y\partial_yT_G^{(26)}.
$$

The updated budget floors are

$$
B_{G,26}=2.07319466431\times10^{70},
\qquad
B_{D,26}=2.07305754145\times10^{70}.
$$

The quotient-tail successor closes only when the shifted, zero-touching rows
prove

$$
\sup_{0\le y\le Y}|T_G^{(26)}|<B_{G,26},
\qquad
\sup_{0\le y\le Y}|T_D^{(26)}|<B_{D,26}.
$$

Equivalently, the $D$ row may be closed through the identity if

$$
\sup|T_G^{(26)}|<B_{G,26},
\qquad
27\sup|T_G^{(26)}|
+\sup|y\partial_yT_G^{(26)}|
<B_{D,26}.
$$

The exact denominator that must be protected in the $G$ tail is

$$
G_\varepsilon(y,Z,\nu)
=
\frac{4cB_\varepsilon}{W_\varepsilon},
\qquad
c=-1,
\qquad
B_\varepsilon=-\frac12(\cos\phi_\varepsilon+\cos\delta_\varepsilon),
$$

with

$$
W_\varepsilon
=
\nu\,\delta_\varepsilon^2J_{\varepsilon}^{\mathrm{abs}},
\qquad
J_{\varepsilon}^{\mathrm{abs}}=-\varepsilon J_\varepsilon,
\qquad
J_\varepsilon=\operatorname{Shift}_{1}(F_{\delta,\varepsilon}).
$$

A valid certificate cannot differentiate the $Z_\varepsilon$ tube as though
$Z_\varepsilon$ were a constant coefficient. The derivative in the correlated
$D$ identity must follow the certified root graph. The required root-tangent
operator is

$$
\Xi_\varepsilon
=
yZ_\varepsilon'
=
-\frac{y\,\partial_yR_{\varepsilon,29}}{J_\varepsilon},
\qquad
\mathcal D_y
=
y\partial_y+\Xi_\varepsilon\partial_Z.
$$

Thus the inverse model must be at least $C^1$ on the certified tube:

$$
\mathcal D_y(W_\varepsilon^{-1})
=
-\frac{\mathcal D_yW_\varepsilon}{W_\varepsilon^2}.
$$

The direct $D$-quotient inverse is not the safe zero-touching route because its
unfactored denominator contains $F_\delta=yJ_\varepsilon$. Raw
$F_\delta^{-1}$ touches the $y=0$ endpoint, so the $D$ row should close through
the correlated identity after the $C^1$ $G$-tail model is certified.

The practical ceiling from the identity is already stricter than the $G$ budget:

$$
\sup|T_G^{(26)}|
<
\frac{B_{D,26}}{27}
=
7.67799089426\times10^{68}
$$

before any derivative budget is spent.

## C1 Twenty-Sixth-Order Obstruction

The first valid C1 attempt on the certified $Z_\varepsilon$ tube used the
root-tangent operator above and rejected both the invalid constant-$Z$
derivative and the direct $D$ quotient inverse through the unfactored
$F_\delta$ denominator. It checked $1920$ positive first-y pair rows with
minimum $|J_\varepsilon|$ clearance $0.791609030534$ and found a rigorous
obstruction for that evaluator rather than a closure.

The worst certified row was

$$
\texttt{speed.0.first-y-positive.1.pair},
\qquad
y\in[0.0001123046875,0.000224609375],
$$

with

$$
\sup |T_G^{(26)}|
\le
4.275058839\times10^{106},
$$

and

$$
\sup |\mathcal D_yT_G^{(26)}|
\le
1.0598339923\times10^{124}.
$$

Thus the certified identity enclosure

$$
27\sup|T_G^{(26)}|+\sup|\mathcal D_yT_G^{(26)}|
\le
1.0598339923\times10^{124}
$$

exceeds the inherited $D$ budget by a positive interval margin. This does not
disprove the tail theorem; it proves that the broad factored positive-y C1
interval model is too wide.

## Closed Twenty-Sixth- And Twenty-Seventh-Order Coefficient Rows

The next coefficient row can still be certified at the $y=0$ endpoint. Let

$$
h_\varepsilon(y,\nu)
=
h_{\varepsilon,\le25}(y,\nu)
+y^{26}Z_\varepsilon(y,\nu),
\qquad
Z_\varepsilon(0,\nu)=h_{26,\varepsilon}(\nu).
$$

The twenty-sixth-order post-$U$ successor coefficient packet certifies

$$
h_{26,\varepsilon}\in[-908343826729000,908343826729000],
$$

$$
Q_{G,26}\in[-773778357003000,773775566334000],
\qquad
Q_{D,26}\in[-20891940291000000,20892015639100000],
$$

with the correlated identity interval

$$
Q_{D,26}+27Q_{G,26}\ni0.
$$

The remaining budgets become

$$
B_{G,27}=1.15377790014\times10^{73},
\qquad
B_{D,27}=1.15370158828\times10^{73}.
$$

The broad C1 $T_G^{(26)},T_D^{(26)}$ evaluator on the predecessor tube is not
the live route. The finite next-successor root-tail tube is now certified:

$$
R_{\varepsilon,31}(y,W,\nu)
=
\operatorname{Shift}_{31}\!\left(
F_\varepsilon(y,h_{\varepsilon,\le26}+y^{27}W,\nu)
\right),
\qquad
\partial_WR_{\varepsilon,31}=J_\varepsilon,
$$

The executable $\operatorname{Shift}_{31}$ packet certifies $3840$ positive-y
rows, minimum endpoint clearance $7.87633458815\times10^{17}$, minimum
$\partial_WR_{\varepsilon,31}$ clearance $0.791609030534$, induced predecessor
$Z_\varepsilon$ containment, and shifted trigonometric remainder at most
$9.14082344368\times10^{-158}$.

The twenty-seventh-order post-$U$ successor coefficient packet then certifies

$$
h_{27,\varepsilon}
\in[-5.42114282999\times10^{15},5.42121259771\times10^{15}],
$$

$$
Q_{G,27}
\in[-4.61813833997\times10^{15},4.61813833997\times10^{15}],
\qquad
Q_{D,27}
\in[-1.29307873519\times10^{17},1.29307873519\times10^{17}],
$$

with

$$
Q_{D,27}+28Q_{G,27}\ni0.
$$

The remaining budgets become

$$
B_{G,28}=6.42102483554\times10^{75},
\qquad
B_{D,28}=6.4206001435\times10^{75}.
$$

The twenty-eighth-order post-$U$ successor coefficient packet then certifies

$$
h_{28,\varepsilon}
\in[-3.23553241084\times10^{16},3.23553241084\times10^{16}],
$$

$$
Q_{G,28}
\in[-2.75624779878\times10^{16},2.75624540089\times10^{16}],
\qquad
Q_{D,28}
\in[-7.99311166258\times10^{17},7.99311861646\times10^{17}],
$$

with

$$
Q_{D,28}+29Q_{G,28}\ni0.
$$

The remaining budgets become

$$
B_{G,29}=3.57343990847\times10^{78},
\qquad
B_{D,29}=3.57320355812\times10^{78}.
$$

The twenty-ninth-order post-$U$ successor coefficient packet then certifies

$$
h_{29,\varepsilon}
\in[-1.93106192270\times10^{17},1.93105617963\times10^{17}],
$$

$$
Q_{G,29}
\in[-1.64501288268\times10^{17},1.64501288268\times10^{17}],
\qquad
Q_{D,29}
\in[-4.93503864803\times10^{18},4.93503864803\times10^{18}],
$$

with

$$
Q_{D,29}+30Q_{G,29}\ni0.
$$

The remaining budgets become

$$
B_{G,30}=1.98869699254\times10^{81},
\qquad
B_{D,30}=1.98856545843\times10^{81}.
$$

The thirtieth-order post-$U$ successor coefficient packet then certifies

$$
h_{30,\varepsilon}
\in[-1.15251558957\times10^{18},1.15251558957\times10^{18}],
$$

$$
Q_{G,30}
\in[-9.81794132784\times10^{17},9.81794749250\times10^{17}],
\qquad
Q_{D,30}
\in[-3.04356372268\times10^{19},3.04356181163\times10^{19}],
$$

with

$$
Q_{D,30}+31Q_{G,30}\ni0.
$$

The remaining budgets become

$$
B_{G,31}=1.10675310889\times10^{84},
\qquad
B_{D,31}=1.1066799073\times10^{84}.
$$

The thirty-first-order post-$U$ successor coefficient packet then certifies

$$
h_{31,\varepsilon}
\in[-6.87856199450\times10^{18},6.87856650661\times10^{18}],
$$

$$
Q_{G,31}
\in[-5.85965356972\times10^{18},5.85965356972\times10^{18}],
\qquad
Q_{D,31}
\in[-1.87508914231\times10^{20},1.87508914231\times10^{20}],
$$

with

$$
Q_{D,31}+32Q_{G,31}\ni0.
$$

The remaining budgets become

$$
B_{G,32}=6.15932164949\times10^{86},
\qquad
B_{D,32}=6.15891426672\times10^{86}.
$$

The thirty-second-order post-$U$ successor coefficient packet then certifies

$$
h_{32,\varepsilon}
\in[-4.10533911463\times10^{19},4.10533911463\times10^{19}],
$$

$$
Q_{G,32}
\in[-3.49722356695\times10^{19},3.49722268609\times10^{19}],
\qquad
Q_{D,32}
\in[-1.15408348641\times10^{21},1.15408377709\times10^{21}],
$$

with

$$
Q_{D,32}+33Q_{G,32}\ni0.
$$

The remaining budgets become

$$
B_{G,33}=3.42779639624\times10^{89},
\qquad
B_{D,33}=3.42756967887\times10^{89}.
$$

The thirty-third-order post-$U$ successor coefficient packet then certifies

$$
h_{33,\varepsilon}
\in[-2.45019316463\times10^{20},2.45019277793\times10^{20}],
$$

$$
Q_{G,33}
\in[-2.08725160467\times10^{20},2.08725160467\times10^{20}],
\qquad
Q_{D,33}
\in[-7.09665545588\times10^{21},7.09665545588\times10^{21}],
$$

with

$$
Q_{D,33}+34Q_{G,33}\ni0.
$$

The remaining budgets become

$$
B_{G,34}=1.90764321182\times10^{92},
\qquad
B_{D,34}=1.90751703868\times10^{92}.
$$

The thirty-fourth-order post-$U$ successor coefficient packet then certifies

$$
h_{34,\varepsilon}
\in[-1.4623509893\times10^{21},1.4623509893\times10^{21}],
$$

$$
Q_{G,34}
\in[-1.24573680421\times10^{21},1.24573692589\times10^{21}],
\qquad
Q_{D,34}
\in[-4.36007924061\times10^{22},4.36007881472\times10^{22}],
$$

with

$$
Q_{D,34}+35Q_{G,34}\ni0.
$$

The remaining budgets become

$$
B_{G,35}=1.06164491788\times10^{95},
\qquad
B_{D,35}=1.06157469978\times10^{95}.
$$

The thirty-fifth-order post-$U$ successor coefficient packet then certifies

$$
h_{35,\varepsilon}
\in[-8.72776427154\times10^{21},8.72776458435\times10^{21}],
$$

$$
Q_{G,35}
\in[-7.43494723105\times10^{21},7.43494723105\times10^{21}],
\qquad
Q_{D,35}
\in[-2.67658100318\times10^{23},2.67658100318\times10^{23}],
$$

with

$$
Q_{D,35}+36Q_{G,35}\ni0.
$$

The remaining budgets become

$$
B_{G,36}=5.90828476039\times10^{97},
\qquad
B_{D,36}=5.90789398141\times10^{97}.
$$

The thirty-sixth-order post-$U$ successor coefficient packet then certifies

$$
h_{36,\varepsilon}
\in[-5.20900125767\times10^{22},5.20900125767\times10^{22}],
$$

$$
Q_{G,36}
\in[-4.43740962363\times10^{22},4.43740947313\times10^{22}],
\qquad
Q_{D,36}
\in[-1.64184150506\times10^{24},1.64184156074\times10^{24}],
$$

with

$$
Q_{D,36}+37Q_{G,36}\ni0.
$$

The remaining budgets become

$$
B_{G,37}=3.28808891013\times10^{100},
\qquad
B_{D,37}=3.28787143313\times10^{100}.
$$

The thirty-seventh-order post-$U$ successor coefficient packet then certifies

$$
h_{37,\varepsilon}
\in[-3.10889437276\times10^{23},3.10889434346\times10^{23}],
$$

$$
Q_{G,37}
\in[-2.64838543511\times10^{23},2.64838543511\times10^{23}],
\qquad
Q_{D,37}
\in[-1.00638646534\times10^{25},1.00638646534\times10^{25}],
$$

with

$$
Q_{D,37}+38Q_{G,37}\ni0.
$$

The remaining budgets become

$$
B_{G,38}=1.82989295868\times10^{103},
\qquad
B_{D,38}=1.82977192800\times10^{103}.
$$

The thirty-eighth-order post-$U$ successor coefficient packet then certifies

$$
h_{38,\varepsilon}
\in[-1.85548531397\times10^{24},1.85548531397\times10^{24}],
$$

$$
Q_{G,38}
\in[-1.58063981796\times10^{24},1.58063984386\times10^{24}],
\qquad
Q_{D,38}
\in[-6.16449539105\times10^{25},6.16449529005\times10^{25}],
$$

with

$$
Q_{D,38}+39Q_{G,38}\ni0.
$$

The remaining budgets become

$$
B_{G,39}=1.01837521179\times10^{106},
\qquad
B_{D,39}=1.01830785559\times10^{106}.
$$

The live zero-safe tails are now

$$
T_G^{(39)}
=
\operatorname{Shift}_{41}
\left(
P-L-y^2A_{G,38}
\right),
\qquad
Q_G=A_{G,38}+y^{39}T_G^{(39)},
$$

and

$$
T_D^{(39)}
=
\operatorname{Shift}_{41}
\left(
D_{\mathrm{pair}}-L-y^2A_{D,38}
\right),
\qquad
Q_D=A_{D,38}+y^{39}T_D^{(39)}.
$$

The preserved correlated identity is

$$
T_D^{(39)}
=
-40T_G^{(39)}
-\mathcal D_y^{(X_{39})}T_G^{(39)},
\qquad
\mathcal D_y^{(X_{39})}
=
y\partial_y+\Xi_\varepsilon\partial_{X_{39}},
\qquad
\Xi_\varepsilon=-\frac{y\,\partial_yR_{\varepsilon,43}}{J_\varepsilon},
\qquad
R_{\varepsilon,43}
=
\operatorname{Shift}_{43}
\left(
F_\varepsilon(y,h_{\varepsilon,\le38}+y^{39}X,\nu)
\right),
\qquad
J_\varepsilon=\partial_XR_{\varepsilon,43}.
$$

The displayed $y\partial_y$ in the formal coefficient identity must therefore
be implemented as the total derivative along a future certified
$X_{39,\varepsilon}$ successor root graph, not as a constant-$X_{39}$
derivative. The $h_{38}$ packet certifies only the coefficient row; it does
not certify a finite $X_{39,\varepsilon}$ tube.

The current non-ladder route is the h39 Cauchy-majorant reduction. Let
$N_G=P-L-y^2A_{G,38}$ and choose a complex $y$ radius $\rho>Y$ and root-graph
radius $\rho_X$ with graph enclosure radius $r_X<\rho_X$ and Cauchy margin
$\sigma_X=\rho_X-r_X$. A directed-rounded certificate of
$\sup|N_G|\le M_G$ and $\sup|\Xi_\varepsilon|\le\Xi_*$ on that polydisc closes
both tails if the two h39 majorant inequalities are satisfied. For the concrete
diagnostic choice $\rho=4Y$, the $D$ row is the stricter budget; even before
including the root-tangent slope cost $\Xi_*/\sigma_X$, it requires

$$
M_G < 2.49441326560\times10^{16}.
$$

This is the present quantitative target for the first directed-rounded h39
polydisc majorant attempt.

The executable budget reducer is
[octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-root-tangent-cauchy-majorant-tail-budget.mjs](../../../scripts/neutral-swarm/octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-root-tangent-cauchy-majorant-tail-budget.mjs).
It also proves the radius monotonicity corollary: for fixed
$\Xi_*/\sigma_X\ge0$, both h39 $M_G$ thresholds strictly increase with
$s=\rho/Y>1$, and the correlated $D$ threshold is always stricter than the
$G$ threshold. The next certificate should therefore maximize the certified
analytic $y$ radius and $X$-Cauchy margin where doing so does not worsen the
certified $M_G$ or $\Xi_*$ bounds past the envelope, then test the single active
$D$-threshold inequality rather than tune an interior radius. The reducer can
consume a future candidate $M_G$ bound directly, invert the active inequality
to compute the maximum admissible $\Xi_*/\sigma_X$ slope ratio,

$$
\frac{\Xi_*}{\sigma_X}
<
(1-q)\frac{B_{D,39}\rho^{41}}{M_G}
-40-\frac{q}{1-q},
$$

and report whether the certified root-tangent slope margin closes the active
h39 majorant budget.

The root-tangent input can now be supplied in primitive certificate form. If a
future graph-centered polydisc backend certifies

$$
M_R\ge\sup |y\,\partial_yR_{\varepsilon,43}|,
\qquad
J_{\min}\le\inf |J_\varepsilon|,
\qquad
J_{\min}>0,
\qquad
\sigma_X=\rho_X-r_X>0,
$$

then the reducer derives

$$
\frac{\Xi_*}{\sigma_X}
\le
\frac{M_R}{J_{\min}\sigma_X}
$$

and tests the same active h39 scalar inequality. This is the current backend
contract: certify $M_G$, $M_R$, $J_{\min}$, and $\sigma_X$ on one shared
polydisc. Equivalently, once a candidate backend reports $M_G$, $J_{\min}$,
and $\sigma_X$, the h39 budget gives a direct residual-numerator ceiling

$$
M_R
<
J_{\min}\sigma_X
\left(
(1-q)\frac{B_{D,39}\rho^{41}}{M_G}
-40-\frac{q}{1-q}
\right),
$$

with a positive parenthesized term. The backend therefore has a scalar
pass/fail target for $M_R\ge\sup |y\,\partial_yR_{\varepsilon,43}|$, not an
open-ended slope-budget guess.

The graph tube itself can be certified by a Rouché lift. If the same backend
certifies the center residual $E_R$, center Jacobian floor $\nu_J$, and
Jacobian oscillation bound $L_J$ with

$$
J_{\min}=\nu_J-L_J\rho_X>0,
\qquad
E_R+\frac12L_Jr_X^2<\nu_Jr_X,
$$

for $0<r_X<\rho_X$, then $R_{\varepsilon,43}=0$ has a unique
$X_{39,\varepsilon}$ root graph inside $|X-X_c|<r_X$, the Cauchy margin is
$\sigma_X=\rho_X-r_X$, and the same primitive h39 scalar test applies. This
turns the remaining backend search into a shared-domain certificate for
$E_R$, $\nu_J$, $L_J$, $r_X$, $\rho_X$, $M_G$, and $M_R$.

For the $L_J$ entry, the preferred backend route is not a raw bivariate
curvature search. The h39 identity factors

$$
\partial_X^2R_{\varepsilon,43}
=
y^{41}K_\varepsilon,
\qquad
K_\varepsilon=
\frac{2}{\nu^2}-\sin\delta_\varepsilon-\sin\phi_\varepsilon.
$$

Thus a continuous shared-domain majorant $M_K\ge\sup|K_\varepsilon|$ gives
the reduced Lipschitz input $L_J^{\mathrm{red}}=\rho^{41}M_K$.

A conservative source for this one entry is already explicit: bound the branch
coordinates by outward-rounded seminorms
$|\delta_\varepsilon|\le D_\varepsilon$ and
$|\phi_\varepsilon|\le\Phi_\varepsilon$ on the same $y,X,\nu$ polydisc, then
use $|\sin z|\le\sinh(|z|)$ to take

$$
M_K^{\mathrm{coord}}
=
\max_\varepsilon
\left(
\frac{2}{\nu_-^2}
+\sinh(D_\varepsilon)
+\sinh(\Phi_\varepsilon)
\right),
\qquad
L_J^{\mathrm{red}}=\rho^{41}M_K^{\mathrm{coord}}.
$$

This closes only the conservative $L_J$ source for the $X$-curvature row. The
shared-domain certificate still needs $E_R$, $\nu_J$, $M_G$, $M_R$, and strict
$r_X$ slack on the same graph-centered domain.

The current finite-prefix replay shows that this remaining task is not blocked
by the scalar Rouché algebra. Feeding the aggregate finite-prefix candidates
for $E_R,\nu_J,M_G,$ and $M_R$ together with
$L_J^{\mathrm{red}}$ into the h39 reducer, and choosing
$\rho_X=2r_R^-$ and $r_X=r_R^-+\frac12(\rho_X-r_R^-)$, gives the candidate
ratio

$$
\Lambda_{39}^{\mathrm R}\approx3.796256065292518\times10^{-115}<1.
$$

This is not a directed-rounded primitive certificate, because the four
primitive values still come from finite-prefix candidates. It does, however,
turn the next backend obligation into a precise provenance problem: produce
continuous shared-domain $E_R,\nu_J,M_G,$ and $M_R$ values close enough to the
candidate tuple, then replay the same strict $\rho_X,r_X$ construction.

The non-ladder h39 route is now narrowed to a specific analytic bridge:
coefficient-series provenance, then shared-domain analytic remainders, then
primitive diagnostic replay. A raw Cauchy shifted-tail theorem is available:
if $F(y)=y^N\operatorname{Shift}_N F(y)$ on an outer first-y disc
$|y|\le R_y$ and $\rho<R_y$, then with $q=\rho/R_y$,

$$
\sup_{|y|\le\rho}|\operatorname{Shift}_N F|
\le
\frac{B_F}{R_y^N(1-q)},
\qquad
\sup_{|y|\le\rho}|y\partial_y\operatorname{Shift}_N F|
\le
\frac{B_F}{R_y^N}\frac{q}{(1-q)^2}.
$$

The theorem is useful as a tail interface, but the raw unshifted source bound
is not sharp enough on the present collar: at $\rho=0.001$ and
$R_y=0.001796875$ it gives $E_R\approx8.43\times10^{118}$ and
$M_R\approx1.06\times10^{119}$ before the graph lift. The backend therefore
cannot close by applying one outer-disc bound to the unshifted source alone.
It must retain the finite shifted coefficient prefix and add only the
remaining analytic tail through the same shared-domain
$\mathcal M_{\rho,\rho_X}$ and $\mathcal F_{\rho,\rho_X}$ functionals.
A full shifted-prefix diagnostic with \texttt{shifted\_order=20} already
validates all $128$ h38 cells and leaves the replay ratio essentially
unchanged at
$\Lambda_{39}^{\mathrm R}\approx3.796300532408038\times10^{-115}$. That
diagnostic confirms the direction of the next proof: deeper finite prefix
alone does not close the theorem, but it makes the analytic-tail remainder
the isolated target.

The $M_G$ part of that backend has now been separated from the shifted
$T_G^{(39)}$ scale. Since the h39 reducer consumes
$M_G\ge\sup|N_G|$ and

$$
N_G=y^{41}T_G^{(39)},
$$

a retained prefix for $T_G^{(39)}$ must be multiplied by the radius factor
$\rho^{41}$ before it is interpreted as an $N_G$ majorant. In the hybrid
notation, the candidate numerator input is

$$
M_G^{\mathrm{hyb}}(\rho,K)
=
\rho^{41}
\left(
\sum_{m=0}^{K}\max(|g_m^-|,|g_m^+|)\rho^m
+
\frac{B_{N_G}^{\mathrm{out}}}{R_y^{41}}\frac{(\rho/R_y)^{K+1}}{1-\rho/R_y}
\right),
$$

not the shifted $T_G^{(39)}$ tail itself. With
$\rho=0.001$, $R_y=0.001796875$, and target
$M_G^{\mathrm{fp}}\approx9.468681741438209\times10^{-99}$, the current
sensitivity diagnostic reports required retained prefix orders $K=266$ for
$B_{N_G}^{\mathrm{out}}=10^{-20}$, $K=345$ for
$B_{N_G}^{\mathrm{out}}=1$, and $K=424$ for
$B_{N_G}^{\mathrm{out}}=10^{20}$. This is still not closure: the actual
$B_{N_G}^{\mathrm{out}}$ has to come from the same directed-rounded complex
domain as the branch denominator clearance, root graph, $E_R,\nu_J,L_J,$ and
$M_R$ inputs.

That branch denominator clearance has a specific sufficient form. If each
outer-domain branch proves
$|B_\varepsilon|\le K_\varepsilon$,
$\nu\ge\nu_-$, $|\delta_\varepsilon|\ge d_\varepsilon>0$, and
$|J_\varepsilon^{\mathrm{abs}}|\ge j_\varepsilon>0$, then the branch $G$
contribution is bounded by

$$
\frac{4|c|K_\varepsilon}{\nu_-d_\varepsilon^2j_\varepsilon}.
$$

The corresponding candidate outer source is

$$
B_{N_G}^{\mathrm{out}}
=
\sum_{\varepsilon\in\{-,+\}}
\frac{4|c|K_\varepsilon}{\nu_-d_\varepsilon^2j_\varepsilon}
+
L_*
+
R_y^2
\sum_{k=0}^{38}
\max(|Q_{G,k}^-|,|Q_{G,k}^+|)R_y^k.
$$

This identifies the next continuous-backend calculation: certify
$K_\varepsilon$, $d_\varepsilon$, and $j_\varepsilon$ on the same outer
domain, rather than search for another coefficient successor.

The candidate values for those three branch quantities are now explicit:

$$
K_\varepsilon(\rho)
=
\frac12
\left(
\cosh\sum_{n\ge0}|\delta_{\varepsilon,n}|\rho^n
+
\cosh\sum_{n\ge0}|\phi_{\varepsilon,n}|\rho^n
\right),
$$

$$
d_\varepsilon(\rho)
=
\operatorname{dist}(0,\delta_{\varepsilon,0})
-
\sum_{n\ge1}|\delta_{\varepsilon,n}|\rho^n,
\qquad
j_\varepsilon(\rho)
=
\operatorname{dist}(0,J_{\varepsilon,0}^{\mathrm{abs}})
-
\sum_{n\ge1}|J_{\varepsilon,n}^{\mathrm{abs}}|\rho^n.
$$

For a retained prefix through order $K$, these infinite sums reduce to a
finite prefix plus Cauchy tail. If $a(y)=\sum_{n\ge0}a_ny^n$,
$B_a^{\mathrm{out}}\ge\sup_{|y|\le R_y}|a(y)|$, and $q=\rho/R_y<1$, then

$$
\mathcal M_{\rho,K}^{\mathrm C}(a)
=
\sum_{n=0}^{K}|a_n|\rho^n
+
B_a^{\mathrm{out}}\frac{q^{K+1}}{1-q},
$$

and

$$
\mathcal F_{\rho,K}^{\mathrm C}(a)
=
\operatorname{dist}(0,a_0)
-
\sum_{n=1}^{K}|a_n|\rho^n
-
B_a^{\mathrm{out}}\frac{q^{K+1}}{1-q}.
$$

Thus the next backend calculation is a concrete branch-coordinate and
Jacobian floor problem on the shared complex domain: bound
$\delta_\varepsilon$, $\phi_\varepsilon$, and
$J_\varepsilon^{\mathrm{abs}}$ on the same outer radius, feed their hybrid
$\mathcal M^{\mathrm C}$ and $\mathcal F^{\mathrm C}$ values into the branch
denominator inequality, and only then form $B_{N_G}^{\mathrm{out}}$.

After that substitution, the closure test is one strict scalar inequality.
For the resulting $B_{N_G,K}^{\mathrm{denC}}$, set

$$
M_{G,K}^{\mathrm{denC}}
=
\sum_{m=0}^{K}|g_m|\rho^{41+m}
+
B_{N_G,K}^{\mathrm{denC}}\frac{(\rho/R_y)^{41+K+1}}{1-\rho/R_y}.
$$

Then the h39 denominator-Cauchy route closes conditionally when

$$
\frac{
M_{G,K}^{\mathrm{denC}}
\left(
40+\frac{M_R}{(\nu_J-L_J\rho_X)(\rho_X-r_X)}+\frac{1}{\rho/Y-1}
\right)
}
{B_{D,39}Y^{41}(\rho/Y)^{40}(\rho/Y-1)}
<1,
$$

with $0<r_X<\rho_X$,
$\nu_J-L_J\rho_X>0$,
$\nu_Jr_X-E_R-\frac12L_Jr_X^2>0$, and all inputs certified on the same
graph-centered complex domain. This turns the live target from "find more
cancellations" into a precise denominator-clearance plus primitive-bound
certificate problem.

The same scalar inequality gives the backend a pass/fail ceiling before the
full branch enclosure is attempted. Let

$$
C_M
=
\frac{
B_{D,39}Y^{41}(\rho/Y)^{40}(\rho/Y-1)
}
{
40+\frac{M_R}{(\nu_J-L_J\rho_X)(\rho_X-r_X)}+\frac{1}{\rho/Y-1}
},
$$

$$
P_{G,K}=\sum_{m=0}^{K}|g_m|\rho^{41+m},
\qquad
\alpha_K=\frac{(\rho/R_y)^{41+K+1}}{1-\rho/R_y}.
$$

Then the branch-denominator backend must make

$$
B_{N_G}^{\mathrm{out}}
<
\frac{C_M-P_{G,K}}{\alpha_K},
$$

and the branch $G$ contribution itself must fit below

$$
\frac{C_M-P_{G,K}}{\alpha_K}
-
L_*
-
R_y^2A_*.
$$

This is the current quantitative closure target for the non-ladder h39 route.
It is deliberately a two-branch sum ceiling, because any per-branch allocation
is an additional backend policy, not part of the theorem.

For executable planning, the sum ceiling can still be translated into
branch-local target floors under an explicit allocation policy. With

$$
W_G=
\frac{C_M-P_{G,K}}{\alpha_K}
-
L_*
-
R_y^2A_*,
\qquad
W_\varepsilon=W_G\frac{a_\varepsilon}{\sum_\eta a_\eta},
\qquad
a_\varepsilon>0,
$$

each branch allocation is met if

$$
d_\varepsilon^2j_\varepsilon
>
\frac{4|c|K_\varepsilon}{\nu_-W_\varepsilon}.
$$

If $d_\varepsilon$ is already certified, this is a lower target for
$j_\varepsilon$; if $j_\varepsilon$ is already certified, it is a square-root
target for $d_\varepsilon$. This does not replace the theorem-level sum
ceiling and does not certify retained branch status. It gives the backend a
local denominator-clearance product to beat before attempting the full
directed-rounded shared-domain enclosure.

The least arbitrary default is the pressure-balanced allocation. With
$C_\varepsilon=4|c|K_\varepsilon/\nu_-$, choose
$W_\varepsilon=W_GC_\varepsilon/\sum_\eta C_\eta$ when all
$C_\varepsilon>0$. This equalizes the branch product targets at

$$
d_\varepsilon^2j_\varepsilon
>
\frac{\sum_\eta C_\eta}{W_G},
$$

and no other positive allocation can lower the maximum required product in the
candidate branch-majorant formula.

The next priority-only diagnostic now measures that retained-prefix order
directly. For each shifted prefix order $K$, it combines the exact finite
prefix with a Cauchy bound for the post-$K$ analytic tail, feeds the resulting
candidate $E_R,\nu_J,M_G,$ and $M_R$ values together with
$L_J^{\mathrm{red}}$ into the primitive replay, and reports the first order at
which the scalar margin would survive. On the raw source-bound
$R_{\varepsilon,43}$ side, matching the current finite-prefix $E_R$ and $M_R$
scales requires about $K=367$ and $K=388$, respectively. Passing this
diagnostic in the future would not close the theorem; it would identify the
order and tail-size target that the directed-rounded shared-domain backend
must certify.

The radius choice is also no longer arbitrary: if $L_J>0$, the backend must
choose $r_X$ strictly above

$$
r_R^-=\frac{2E_R}{\nu_J+\sqrt{\nu_J^2-2L_JE_R}},
$$

and below $\rho_X$; if $L_J=0$, it must choose $r_X>E_R/\nu_J$. The best
Cauchy margin is approached from above at that lower boundary, but is not
attained because the Rouché inequality is strict.

The h39 closure decision can now be written as one Rouché-primitive ratio:

$$
\Lambda_{39}^{\mathrm R}
=
\frac{
M_G
\left(
40+
\frac{M_R}{(\nu_J-L_J\rho_X)(\rho_X-r_X)}
+
\frac{1}{s-1}
\right)
}{
B_{D,39}Y^{41}s^{40}(s-1)
}.
$$

The directed-rounded h39 successor closes when the same shared-domain
certificate proves the strict Rouché inequalities and
$\Lambda_{39}^{\mathrm R}<1$. Equivalently, for $M_G>0$ the certificate must
beat the Rouché-form numerator ceiling

$$
M_R
<
(\nu_J-L_J\rho_X)(\rho_X-r_X)
\left(
(1-q)\frac{B_{D,39}\rho^{41}}{M_G}
-40-\frac{q}{1-q}
\right).
$$

Thus the backend no longer needs to report an abstract slope budget. It needs
the seven values $E_R,\nu_J,L_J,r_X,\rho_X,M_G,M_R$ on one graph-centered
polydisc and a strict margin below $\Lambda_{39}^{\mathrm R}=1$.

The $r_X$ choice can also be reduced before the backend commits to a concrete
strict radius. For $M_G>0$, set

$$
C_D
=
(1-q)\frac{B_{D,39}\rho^{41}}{M_G}
-40-\frac{q}{1-q}.
$$

If $C_D\le0$, no nonnegative $M_R$ can close this sufficient h39 majorant at
the chosen $M_G$. If $C_D>0$, the fixed-radius ceiling
$J_R(\rho_X-r_X)C_D$ is strictly decreasing in $r_X$, so the best admissible
ceiling is the unattained lower-boundary supremum

$$
M_{R,\sup}
=
(\nu_J-L_J\rho_X)(\rho_X-r_{\mathrm{low}})C_D,
$$

where

$$
r_{\mathrm{low}}=r_R^-
\quad(L_J>0),
\qquad
r_{\mathrm{low}}=\frac{E_R}{\nu_J}
\quad(L_J=0).
$$

The existential radius question is therefore reduced to $M_R<M_{R,\sup}$.
When that strict inequality holds, the backend may choose any
$r_X\in(r_{\mathrm{low}},\,\rho_X-M_R/(J_RC_D))$; equality at the lower
boundary is never allowed because the Rouché inequality is strict.

There is one more scalar reduction before a directed-rounded backend chooses a
specific $X$ polydisc. Holding $E_R,\nu_J,L_J,M_G$ fixed over a shared-domain
radius family, the $r_X$-optimized ceiling is
$C_D(\nu_J-L_J\rho_X)(\rho_X-r_{\mathrm{low}})$. For $L_J>0$, this is a
concave quadratic in $\rho_X$ with optimum

$$
\rho_X^\ast
=
\frac{\nu_J/L_J+r_{\mathrm{low}}}{2}
=
\frac{2\nu_J-\sqrt{\nu_J^2-2L_JE_R}}{2L_J},
$$

and uncapped ceiling

$$
M_{R,\sup}^{\rho_X,L_J>0}
=
C_D\frac{\nu_J^2-2L_JE_R}{4L_J}.
$$

If the backend has only $\rho_X\le\bar\rho_X$, it uses
$\min(\bar\rho_X,\rho_X^\ast)$, with the strict-window requirement
$\bar\rho_X>r_{\mathrm{low}}$. For $L_J=0$, a finite optimized ceiling requires
a finite cap and becomes

$$
M_{R,\sup}^{\rho_X,L_J=0}
=
C_D(\nu_J\bar\rho_X-E_R).
$$

This is still only a scalar budget optimization: the eventual interval
certificate must prove that the same $E_R,\nu_J,L_J,M_G,M_R$ bounds are valid
on the chosen shared graph-centered domain.

The $y$-radius choice reduces similarly, but only as a cap-aware scalar
statement. For $s=\rho/Y>1$ and $M_G>0$,

$$
C_D(s)
=
\frac{B_{D,39}Y^{41}}{M_G}s^{40}(s-1)
-40-\frac{1}{s-1}
$$

satisfies

$$
C_D'(s)
=
\frac{B_{D,39}Y^{41}}{M_G}s^{39}(41s-40)
+\frac{1}{(s-1)^2}>0.
$$

Thus, under fixed shared-domain constants, there is no interior $y$-radius
optimum. If the backend certifies a finite radius family
$1<s\le s_{\max}$, the best scalar h39 ceiling uses $s=s_{\max}$; without such
a finite cap, the scalar formula is unbounded under the fixed-$M_G$ hypothesis.
This does not mean a larger interval domain is automatically better, because
the actual certificate may worsen $M_G$, $E_R$, $\nu_J$, or $L_J$ as the
domain grows.

The h39 shared-domain primitive diagnostic now makes this contract executable
without overclaiming the missing backend. The diagnostic packet
[octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-h39-shared-domain-primitive-diagnostic](octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-h39-shared-domain-primitive-diagnostic.md)
consumes supplied
$E_R,\nu_J,L_J,\rho_X,r_X,M_G,M_R$ values, replays the h39 reducer, and refuses
to report a replay pass unless the values are explicitly labelled as coming
from an external directed-rounded shared-domain source. It still does not
certify those values. The live mathematical successor is therefore no longer a
generic "find the next check" task; it is the construction of the actual
shared-domain evaluator that computes $E_R$, $\nu_J$, $L_J$, $M_G$, and $M_R$
on one graph-centered domain and feeds this diagnostic.

## Certificate Requirements

A valid executable successor must certify:

- the imported finite $\operatorname{Shift}_{25}$ $E_\varepsilon$ root-tail tube;
- the imported $h_{22}$ post-seed coefficient and twenty-third-order budgets;
- the imported $h_{23}$ $U$ seed coefficient and twenty-fourth-order budgets;
- the imported finite $\operatorname{Shift}_{27}$ $U_\varepsilon$ root-tail
  tube, including its induced $E_\varepsilon$ image containment inside the
  certified $E_\varepsilon$ tube;
- the imported $h_{24}$ post-$U$ coefficient and twenty-fifth-order budgets;
- the imported $h_{25}$ successor coefficient and twenty-sixth-order budgets;
- the finite $\operatorname{Shift}_{29}$ $Z_\varepsilon$ successor root-tail
  tube and its induced $U_\varepsilon$ image containment inside the predecessor
  tube;
- the recorded C1 obstruction for the broad factored positive-y
  twenty-sixth-order evaluator, so that future work does not reuse the invalid
  constant-$Z$ derivative route or overclaim the wide C1 interval result;
- the imported $h_{26}$ successor coefficient and twenty-seventh-order budgets;
- the imported finite $\operatorname{Shift}_{31}$ next-successor
  $W_\varepsilon$ root-tail tube and its induced predecessor $Z_\varepsilon$
  tube containment;
- the recorded C1 obstruction for the broad factored positive-y
  twenty-seventh-order evaluator, so that future work preserves coefficient
  cancellations before dividing by $y^{29}$;
- the certified lower coefficient containment through $y^{28}$ before applying
  $\operatorname{Shift}_{29}$ to the $G,D$ tail numerators;
- the imported $h_{27}$ successor coefficient and twenty-eighth-order budgets;
- the imported $h_{28}$ successor coefficient and twenty-ninth-order budgets;
- the imported $h_{29}$ successor coefficient and thirtieth-order budgets;
- the imported $h_{30}$ successor coefficient and thirty-first-order budgets;
- the imported $h_{31}$ successor coefficient and thirty-second-order budgets;
- the imported $h_{32}$ successor coefficient and thirty-third-order budgets;
- the imported $h_{33}$ successor coefficient and thirty-fourth-order budgets;
- the imported $h_{34}$ successor coefficient and thirty-fifth-order budgets;
- the imported $h_{35}$ successor coefficient and thirty-sixth-order budgets;
- the imported $h_{36}$ successor coefficient and thirty-seventh-order budgets;
- the imported $h_{37}$ successor coefficient and thirty-eighth-order budgets;
- the imported $h_{38}$ successor coefficient and thirty-ninth-order budgets;
- the formal coefficient recurrence theorem as a structural guide, without
  treating it as analytic tail closure before the uniform expansion, chart
  slope, coefficient-growth, and root-tangent burdens are proven;
- a coefficient-preserving thirty-ninth-order successor-tail enclosure, or the
  equivalent Cauchy-majorant certificate, for $P-L-y^2A_{G,38}$ and the
  correlated $D_{\mathrm{pair}}-L-y^2A_{D,38}$ tail;
- the identity residual
  $T_D^{(39)}+40T_G^{(39)}+\mathcal D_y^{(X_{39})}T_G^{(39)}$ contains zero;
- a factored $C^1$ quotient/inverse Taylor-model remainder on a certified
  successor root graph, including the implicit root-tangent term
  $\Xi_\varepsilon=-y\,\partial_yR_{\varepsilon,43}/J_\varepsilon$, because the
  direct positive-y quotient rows do not provide a zero-endpoint proof and the
  broad predecessor tube does not preserve the post-$h_{38}$ cancellations;
- no raw division by a zero-touching $y$ interval.

The broad C1 attempt on the certified $W_\varepsilon$ tube gives a rigorous
obstruction, not closure: on the worst row
`speed.0.first-y-positive.1.pair`, the shifted numerator enclosure is already
about $1.1\times10^{-4}$ wide before the $y^{29}$ division, giving
$\sup|T_G^{(27)}|\le3.80666108795\times10^{110}$ and
$\sup|\mathcal D_y^{(W)}T_G^{(27)}|\le4.03517714144\times10^{128}$ against
$B_{D,27}=1.15370158828\times10^{73}$.

The coefficient-preserving lower-cancellation certificate closes the first half
of that successor route: all coefficients $y^0$ through $y^{28}$ of
$P-L-y^2A_{G,26}$ and $D_{\mathrm{pair}}-L-y^2A_{D,26}$ contain zero on all
$128$ speed cells, with no raw $y$ inverse and no $y^{29}$ division. A
twenty-seventh-order coefficient successor then certifies the $Q_{G,27}$ and
$Q_{D,27}$ constant term. The twenty-eighth-, twenty-ninth-, thirtieth-,
thirty-first-, thirty-second-, thirty-third-, thirty-fourth-, thirty-fifth-,
thirty-sixth-, thirty-seventh-, and thirty-eighth-order coefficient successors
now certify the $Q_{G,28},Q_{D,28}$, $Q_{G,29},Q_{D,29}$,
$Q_{G,30},Q_{D,30}$, $Q_{G,31},Q_{D,31}$, $Q_{G,32},Q_{D,32}$,
$Q_{G,33},Q_{D,33}$, $Q_{G,34},Q_{D,34}$, $Q_{G,35},Q_{D,35}$,
$Q_{G,36},Q_{D,36}$, $Q_{G,37},Q_{D,37}$, and $Q_{G,38},Q_{D,38}$ constant
terms. A successful successor must therefore enclose only the thirty-ninth-order
tail in a Taylor or Taylor-inverse quotient model while retaining the
root-tangent operator. It
must not claim full first-y quotient enclosure, scaled remainder closure, `I1`
closure, interval quadrature, or retained branch status by itself.
