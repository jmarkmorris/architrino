# Octahedral Fold-Aware Cross-Binary Theta3minus Speed-Dependent Fold-Pair First-Y G/D Finite Shift31 Next-Successor Root-Tail Tube Certificate

Promotion status: `priority-only`.

This packet closes the finite positive-y next-successor root-tail tube exposed
by the certified $h_{26}$ coefficient row. It certifies the finite
$W_\varepsilon(y,\nu)$ tube used by

$$
h_\varepsilon(y,\nu)
=
h_{\varepsilon,\le26}(y,\nu)
+y^{27}W_\varepsilon(y,\nu).
$$

This is a finite positive-y tube closure, not the continuous quotient-tail
bound. It does not claim full first-y quotient enclosure, scaled remainder,
`I1`, quadrature, or retained branch status.

No fixed speed band is imposed. The certificate uses only the certified
positive speed-ratio zero enclosure

$$
\nu\in[3.02156,3.02157],
$$

subdivided into $128$ directed-rounded speed cells.

## Shifted Next-Successor Residual

The finite next-successor residual is

$$
R_{\varepsilon,31}(y,W,\nu)
=
\operatorname{Shift}_{31}\!\left(
F_\varepsilon(y,h_{\varepsilon,\le26}+y^{27}W,\nu)
\right).
$$

The lower coefficients $y^0$ through $y^{30}$ are dropped only after interval
containment of zero is certified. The positive-y rows then certify endpoint
signs in the symmetric tube

$$
W_\varepsilon\in[-10^{18},10^{18}],
$$

with derivative orientation

$$
\partial_WR_{\varepsilon,31}=J_\varepsilon.
$$

The induced predecessor image is also certified:

$$
Z_\varepsilon
=
h_{25,\varepsilon}
+yh_{26,\varepsilon}
+y^2W_\varepsilon
$$

stays inside the already-certified finite $\operatorname{Shift}_{29}$
$Z_\varepsilon$ predecessor tube on every positive-y subcell.

## Certified Result

Across the $128$ speed cells, two branch signs, and $15$ positive-y subcells:

| Row | Directed-rounded interval result |
| --- | ---: |
| speed cells | $128$ |
| positive-y row count | $3840$ |
| imported $h_{26}$ artifact valid | `true` |
| imported finite $\operatorname{Shift}_{29}$ predecessor artifact valid | `true` |
| all lower shift coefficients $y^0$ through $y^{30}$ contain zero | `true` |
| all $\operatorname{Shift}_{31}$ endpoint signs certified | `true` |
| all $\partial_WR_{\varepsilon,31}$ signs certified | `true` |
| all induced $Z_\varepsilon$ images stay inside predecessor tube | `true` |
| minimum endpoint clearance | $7.87633458815\times10^{17}$ |
| minimum $\partial_WR_{\varepsilon,31}$ clearance | $0.791609030534$ |
| maximum $W$-tube absolute upper endpoint | $10^{18}$ |
| maximum induced $Z$-image absolute upper endpoint | $1.57057123839\times10^{14}$ |
| maximum lower-shift residual interval witness | $1.9784447947\times10^{18}$ |
| maximum shifted trigonometric remainder | $9.14082344368\times10^{-158}$ |
| left endpoint interval hull | $[-7.97871638326\times10^{17},7.97021457594\times10^{17}]$ |
| right endpoint interval hull | $[-7.9702140243\times10^{17},7.97871582835\times10^{17}]$ |

The status is

$$
\boxed{
\texttt{directed-rounded-theta3minus-fold-pair-first-y-GD-finite-shift31-next-successor-root-tail-tube-certified}.
}
$$

## Claim Boundary

This packet may claim:

$$
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_finite\_next\_successor\_root\_tail\_tube=true}.
$$

It does not claim:

$$
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_continuous\_next\_successor\_tail\_bound=false},
\qquad
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_finite\_remainder\_bound=false},
$$

$$
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_jet\_enclosure=false},
\qquad
\texttt{certifies\_directed\_rounded\_fold\_pair\_scaled\_remainder=false},
$$

$$
\texttt{certifies\_I1\_regular\_critical\_exhaustion=false},
\qquad
\texttt{certifies\_directed\_rounded\_first\_y\_GD\_quadrature=false},
\qquad
\texttt{retained\_branch=false}.
$$

## Closure Burndown

| Row | Status |
| --- | --- |
| `theta3minus.fold-pair-first-y-GD-finite-shift29-successor-root-tail-tube` | directed-rounded positive-y certified |
| `theta3minus.fold-pair-first-y-GD-twenty-sixth-order-post-U-successor-coefficient` | directed-rounded interval certified |
| `theta3minus.fold-pair-first-y-GD-finite-shift31-next-successor-root-tail-tube` | directed-rounded positive-y certified |
| `theta3minus.fold-pair-first-y-GD-twenty-seventh-order-lower-coefficient-cancellation` | coefficient-preserving cancellation certified |
| `theta3minus.fold-pair-first-y-GD-twenty-seventh-order-post-U-successor-coefficient` | directed-rounded interval certified |
| `theta3minus.fold-pair-first-y-GD-twenty-eighth-order-tail-bound` | blocked by coefficient-preserving successor tail bound |
| `theta3minus.fold-pair-first-y-GD-directed-rounded-enclosure` | blocked by twenty-seventh-order tail bound |
| `theta3minus.fold-pair-scaled-remainder-continuous-collar` | blocked by directed-rounded first-y $G,D$ enclosure |

## Next Tail Target

The next quotient-tail successor is

$$
T_G^{(27)}
=
\operatorname{Shift}_{29}\!\left(
P-L-y^2A_{G,26}
\right),
\qquad
Q_G=A_{G,26}+y^{27}T_G^{(27)},
$$

and

$$
T_D^{(27)}
=
\operatorname{Shift}_{29}\!\left(
D_{\mathrm{pair}}-L-y^2A_{D,26}
\right),
\qquad
Q_D=A_{D,26}+y^{27}T_D^{(27)}.
$$

The correlated identity is

$$
T_D^{(27)}
=
-28T_G^{(27)}
-\mathcal D_y^{(W)}T_G^{(27)},
\qquad
\mathcal D_y^{(W)}=y\partial_y+\Xi_\varepsilon\partial_W.
$$

The inherited budget floors are

$$
B_{G,27}=1.15377790014\times10^{73},
\qquad
B_{D,27}=1.15370158828\times10^{73}.
$$

The lower numerator cancellations through $y^{28}$ are now certified before
any $y^{29}$ division. The remaining first-y blocker is therefore the
coefficient-preserving $y^{29}$ quotient/inverse Taylor-model bound on the
certified $W_\varepsilon$ next-successor tube. The finite endpoint-sign row is
closed; the remaining pressure is the width growth in the quotient inverse and
in the root-tangent $\mathcal D_y^{(W)}T_G^{(27)}$ term needed for the $D$
identity.

## Executable Artifact

The executable packet is
[octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift31-next-successor-root-tail-tube-certificate.mjs](../../../scripts/neutral-braid/octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift31-next-successor-root-tail-tube-certificate.mjs).
It emits:

- directed-rounded $\operatorname{Shift}_{31}$ endpoint signs on $3840$
  positive-y rows;
- $\partial_WR_{\varepsilon,31}=J_\varepsilon$ sign certification;
- lower coefficient containment through $y^{30}$ before shifting;
- induced $Z_\varepsilon$ image containment inside the predecessor tube;
- explicit open continuous tail, full quotient, scaled-remainder, `I1`,
  quadrature, and retention rows.

The companion test
[neutral-braid-octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift31-next-successor-root-tail-tube-certificate.test.js](../../../tests/neutral-braid-octahedral-fold-aware-cross-binary-theta3minus-speed-dependent-fold-pair-first-y-gd-finite-shift31-next-successor-root-tail-tube-certificate.test.js)
validates schema, no-fixed-speed-window discipline, endpoint signs,
monotonicity, predecessor containment, overclaim rejection, and CLI validation
behavior.
