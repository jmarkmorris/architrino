# Swarm Migration Archive

| Source wording or notation | Current wording or notation |
| --- | --- |
| `Noether core` | `Noether swarm` |
| `Noether Swarm` in running text | `Noether swarm` |
| `neutral knot-cloud` / `knot-cloud` | `neutral swarm` |
| `nested tri-binary` | `nested shell swarm` |
| `exact nested tri-binary` | `nested shell swarm with exact binary assumptions` |
| `same-level tri-binary` | `shell swarm` |
| `branch` as swarm taxonomy | mathematical-use-only `branch` |
| `triply` | `three ordered radial support bands` |
| `cloud` as swarm taxonomy | the actual mathematical object, such as support distribution or occupancy measure |
| `neutral Noether swarm branch` | `neutral swarm`, with mathematical `branch` only if needed |
| `pair` / `pairing` as swarm taxonomy or binary replacement | `binary`; `partition` or $\mathcal{P}$ only for the grouping |
| `slot` / `binary slot` as swarm taxonomy | mathematical-use-only `slot` |
| `sector` as swarm taxonomy or broad migration label | mathematical-use-only `sector`; otherwise use explicit source/target wording |
| `variable radius` / `variable-radius` as swarm type | `nested shell swarm` or `shell swarm`, by context |
| `fixed radius` / `fixed-radius` as swarm type | idealized zero-variation case inside the relevant shell model |
| `tri-binary-to-swarm-sector` and similar broad migration labels | explicit source/target migration wording |
| `pro-core` | `pro-swarm` |
| `anti-core` | `anti-swarm` |
| `tri-core` | `nested shell swarm` |
| `quark core` | `Noether swarm` |
| `core shielding` | `Noether swarm shielding` |
| $\rho_{\text{core}}$, $\rho_{\mathrm{core}}$, and indexed or decorated variants | $\rho_{\text{NS}}$, $\rho_{\mathrm{NS}}$, and matching indexed or decorated variants |
| $\Lambda_{\text{NC}}$, $\Lambda_{\mathrm{NC}}$, and indexed variants | $\Lambda_{\text{NS}}$, $\Lambda_{\mathrm{NS}}$, and matching indexed variants |
| $\Lambda_{\text{core}}$ | $\Lambda_{\text{NS}}$ |
