# Swarm

## Workstream Metadata

- Kind: `priority`
- Rank: `5`
- Value: `28.34`
- Cost: `5.4`
- ROI: `5.25`
- Status: `active-proof-and-validation-work`

## Current Terminology

The active reader-facing swarm terms are:

| Term | Working meaning |
| --- | --- |
| `Noether swarm` | The overall class of Noether-constrained swarm models. Use `Noether Swarm` only in titles, headings, scene titles, and other title-case labels. |
| `neutral swarm` | The broad six-architrino neutral case before any required binary grouping or radial organization is assumed. |
| `shell swarm` | A neutral swarm whose six architrino paths remain in a controlled radial support band. |
| `nested shell swarm` | A shell swarm with three ordered radial support bands. |

Formula notation currently uses $\rho_{\text{NS}}$ for physical Noether swarm density and $\Lambda_{\text{NS}}$ for the reduced nested shell swarm closure label. Other formula symbols with legacy `core` subscripts, including $R_{\text{core}}$, $\omega_{\text{core}}$, $Q_{\mathrm{core}}$, and $\mathcal{F}_{\text{core}}$, remain compatibility symbols unless a separate approved row renames them.

The completed terminology migration table is archived in [swarm-migration-archive.md](swarm-migration-archive.md).

## Remaining Steps

1. `certificate_and_gate_repair` — Reconnect proof obligations, certificate rows, and migration-gate language to the accepted swarm triad as proof/gate packets are touched. Status: `active`. Notes: retained-branch claim discipline has been applied to the main proof/gate surfaces; continue only when another proof/gate packet is selected or when a retained-branch certificate surface is selected.
2. `validation_method_promotion` — Decide which certificate schemas, residual definitions, solver-test protocols, ledger requirements, negative-result interpretations, and reproducibility criteria should be promoted from `reference/priorities/swarm/` into reader-facing validation material. Status: `open`. Notes: promotion must be self-contained, must not link from corpus pages to priority files, and must not claim a retained branch.
3. `proof_surface_repair_and_closure` — Advance the surviving proof surfaces under the accepted Noether swarm / neutral swarm / shell swarm / nested shell swarm architecture. Status: `active`. Notes: priority targets include retained-branch promotion, bounded-speed branch rows, support-complete $M=3$ rows, Noether/action closure, event ledgers, stability handoff, observer exports, and strong-field/cosmology export rows.
4. `retained_branch_certificate_closure` — Close or reject retained-branch claims on one live ledger. Status: `blocked`. Notes: no branch is retained until root, dynamics, support, action, event, stability, inventory, and observer-export rows are explicitly closed or statused on the same branch convention.
5. `unapproved_formula_symbol_review` — Decide whether to rename remaining compatibility symbols such as $R_{\text{core}}$, $\omega_{\text{core}}$, $Q_{\mathrm{core}}$, and $\mathcal{F}_{\text{core}}$. Status: `operator-approval-needed`. Notes: do not rewrite these symbols without a separate approved mapping.
6. `assembly_swarm_fallback_investigation` — If closure stalls after the triad migration, study whether assembly swarms are needed. Status: `low-priority`. Notes: an assembly swarm would let all architrinos in an assembly participate in one swarm rather than being added externally as poles or kept separately in a nucleus.

## Operating Rules

- Use the accepted terminology above in new priority prose.
- Treat priority-only labels from older packets as history, not active taxonomy.
- Use `slot` and `sector` only in standard mathematical usage.
- Use `binary` for one binary in a nested shell swarm; in that context each shell contains a binary.
- Do not promote retained-branch claims, observer exports, or model-closure claims until the live ledger/certificate rows close.
