# Spindle Spin Scaffold Re-Derivation Packet

Status: `complete` (all five deliverables executed 2026-07-10; queue item 12 removed from the lane queue. Gate opened 2026-07-10, operator-directed; session 1 executed 2026-07-10: Deliverable 5 at plan grade, Deliverable 1 stated as definitions and lemma targets — see Session 1 Record below; session 2 executed 2026-07-10: Deliverable 1 drafted into the bridge and Deliverable 4 executed with the flutter verdict — see Session 2 Record below; session 3 executed 2026-07-10, operator-directed: Deliverables 2 and 3 drafted into the bridge and the Deliverable 5 placement plan executed — see Session 3 Record below. Remaining follow-ons are tracked as bridge closure targets, not packet deliverables: the spindle worked example and closure-route restatement are gated on a surviving native release, and target 17/L5 waits on braid-ideal queue item 23). Owner lane: braid-angular-momentum-spin, queue item `spindle_spin_scaffold_rederivation`. Corpus destination: [angular-momentum-and-spin.md](../../../content/markdown/aaa/philosophy-history/theory-bridges/angular-momentum-and-spin.md) — the heaviest nested-first file remaining in the corpus (40 mentions) — plus cross-links from [spindle-braid.md](../../../content/markdown/aaa/noether-braid/spindle-braid.md).

## Closure Goal

Re-derive the spin/angular-momentum bridge on the spindle braid's uniaxial structure, replacing or complementing the bridge's three-layer nested scaffolding (`Nested Shell Braid Spin Scaffold`, `Delayed Three-Layer Functional Scaffold`, the partition equations, and the worked transitions) with spindle-native counterparts, so the bridge's consumers (spinor closure, measurement response, Bell rebuild, orbital quantization) can generalize off the nested realization.

## Why the Spindle Substrate Is Stronger Than the Nested Frame Ever Was

- **One axis, exactly.** Rigid uniaxial co-rotation gives a single exact kinematic angular-momentum axis with time-constant pairwise alignment scalars — the nested frame's approximate rank-three construction is replaced by an exact rank-one spine plus cap structure.
- **The glove is measured.** The braid's chirality is the locked relative orientation of three oriented structures (spin axis, cap polarity dipole, cap azimuthal offset), with the helicity-polarity lock measured maximal under drift — the spinor program's ordered frame comes pre-locked.
- **Native nutation numbers exist.** The held-cage release produced the program's first native axis dynamics ($1.1°$ tilt by $t=0.1$, $9.5°$ by $t=1.0$, $35°$ by $t=1.6$ at the $dt/2$ witness) — real precession/nutation data where the nested frame had only the precession-as-symmetry-distance hypothesis.
- **The tilt stiffness block is physics-rich.** The measured tilt matrix is asymmetric because internal torques need not cancel instantaneously in a causal-delay theory (the field in flight carries angular momentum); global tilt is an exact null (isotropy); relative tilts are restoring at the self-equilibrated fixed point, with inner-tilt$\to$middle-torque ($+0.90$) the strongest coupling — the same pathway the native nutation followed.
- **The transacted ledger is quantified.** $J_z$ per click runs as $1\pm0.236\,|u|-1.28\,u^2$ (helicity-odd linear, quadratic run-down), and the $J_z$-conserving-trajectory versus closure-optimal-trajectory comparison (braid-ideal queue item 23) is the named route to derived $h$-constancy — the bridge's $h/\hbar$ convention section should consume it.
- **Soft modes are named.** The stiffness spectrum's soft coupled relative-tilt modes are the natural strain sinks and candidate $h$-click storage — a concrete substrate for the bridge's action-ledger partitions.

## Deliverables

1. **Spindle spin scaffold** for the bridge: the kinematic and wake angular-momentum ledger on the uniaxial family, with the quotient-spectrum discipline (deflate the exact global-tilt null before reading stability) stated as method.
2. **Partition-rule analog.** The nested partition targets ($\Delta I_{\text{inner}},\Delta I_{\text{middle}},\Delta I_{\text{outer}},\Delta I_{\text{wake}}$) re-derived for iso-frequency layers: allocation moves through tilts and radii at one common cadence rather than through per-layer frequencies — a different constrained map, not a rename (same lesson as the retuning map).
3. **Spinor hookup.** The ordered-frame spinor closure target consuming the spindle's locked frame — the pseudoscalar pairing of dipole and spin as the handedness label, with the C-degeneracy and CP structure inherited from the spindle scene's discrete-symmetry results.
4. **Gyroscopic-circulatory treatment.** The true linearized axis dynamics $\lambda^2 M+\lambda G+K$ with the measured asymmetric $K$: whirl/flutter analysis at seed grade, feeding the self-equilibrated candidate's native release (where flutter is the declared decisive question) and the spin-mapping lane simultaneously.
5. **Corpus placement plan.** Which bridge sections generalize in place, which get spindle-native counterparts, and which remain explicitly nested-comparison material — executed under the standing preservation discipline (near-verbatim relocation, slugs preserved, no content loss).

## Inputs and Provenance

Corpus: [spindle-braid.md](../../../content/markdown/aaa/noether-braid/spindle-braid.md) (discrete-symmetry structure, stiffness spectrum, motion ledgers, stability gate), [braid-mathematics.md](../../../content/markdown/aaa/noether-braid/braid-mathematics.md) (momentum screw and helicity, axial polarity dipole). Priority record: [fold-crossing-chart-spec.md](../braid-ideal/fold-crossing-chart-spec.md) Sections 23–24 (stiffness spectrum, drift lock), 28–29 (anisotropy decomposition), 31 ($h_{\mathrm{act}}(u)$ two-ledger split), 56 (native nutation), 57–59 (stability matrix, size pin, tilt block). Lane instruments: the planar tri-binary reduced chart and the core angular-momentum ledger packets in this directory.

## Discipline

Claim levels carry from the sources unchanged (measured on prescribed worldlines / native scoped findings / seed-grade instruments; nothing retained). Fail-closed: this packet authorizes derivation work and corpus drafting, not retention claims and not canon changes beyond the declared destination sections.

## Session 1 Record (2026-07-10, CSO)

Scope discipline: no corpus edits this session (other agents active in the area); all output lands in this packet. Bridge edits happen in a dedicated single-owner session against the placement plan below, slugs preserved.

### Deliverable 1 — Uniaxial Angular-Momentum Ledger: Definitions and Lemma Targets

Setup: the spindle core is the six-site drum (two staggered triangular rings, equal lever arms $\rho=R\sqrt{2/3}$, heights $\pm R/\sqrt3$), in rigid uniaxial co-rotation at common cadence $\omega$ about $\hat{\mathbf n}$.

Definitions (unit kinematic weights; architrinos carry no primitive mass; combined particle-plus-wake conservation per the momentum-screw section of braid-mathematics):

- Kinematic row: $\mathbf J_{\mathrm{kin}}(T)=\sum_a \mathbf X_a(T)\times\mathbf V_a(T)$ about the braid center.
- Wake row: $\mathbf J_{\mathrm{wake}}(T)$ = the in-flight assignment computed from the path-history record; $\mathbf J_{\mathrm{tot}}=\mathbf J_{\mathrm{kin}}+\mathbf J_{\mathrm{wake}}$ is the conserved slice total (assemblies-plus-record accounting).

Lemma targets:

- **L1 (exact rank-one spine).** Under rigid uniaxial co-rotation, $\mathbf J_{\mathrm{kin}}=\omega I_{\mathrm{axis}}\hat{\mathbf n}$ exactly, with $I_{\mathrm{axis}}=\sum_a\rho_a^2=6\cdot\tfrac{2}{3}R^2=4R^2$ time-constant, and all pairwise alignment scalars constant. Replaces the nested frame's approximate rank-three construction with an exact rank-one spine plus cap structure. Elementary; provable now.
- **L2 (axial wake row).** By the acting $2\pi/3$ rotation symmetry, the cycle-averaged $\mathbf J_{\mathrm{wake}}$ is axial; the instantaneous transverse content is the nutation channel — the same object as the measured native nutation numbers. Nonzero transverse wake row with zero transverse kinematic row is the causal-delay signature carried by the asymmetric tilt block.
- **L3 (quotient discipline, method statement).** Global tilt is an exact null of the isotropic law; every stability read deflates the null first; the claim-bearing spectrum is the quotient. Promoted from the spindle scene as bridge method.
- **L4 (gyroscopic-circulatory form).** True linearized axis dynamics $\lambda^2M+\lambda G+K$ with the measured asymmetric $K$; whirl/flutter at seed grade; co-feeds the braid-ideal native release where flutter is the decisive question.
- **L5 (transacted ledger consumption).** $J_z$ per click $=1\pm0.236\,|u|-1.28\,u^2$; the $h$-constancy route is the $J_z$-conserving versus closure-optimal trajectory comparison (braid-ideal queue item 23); the bridge's $h/\hbar$ section is the consumer.

### Deliverable 2 — Iso-Frequency Partition Analog (target statement)

The nested partition targets $(\Delta I_{\text{inner}},\Delta I_{\text{middle}},\Delta I_{\text{outer}},\Delta I_{\text{wake}})$ allocate over per-layer frequencies. On the iso-frequency spindle the analog is a constrained allocation map at one cadence: $\mathbf J=\mathbf J(\theta_{\mathrm{rel}},\mathbf r,\omega)$ with transfer moving through relative tilts and radii, $d\mathbf J=\sum_i\partial_{\theta_i}\mathbf J\,d\theta_i+\sum_i\partial_{r_i}\mathbf J\,dr_i+\partial_\omega\mathbf J\,d\omega$, the soft coupled relative-tilt modes serving as strain sinks and candidate $h$-click storage. A different constrained map, not a rename.

### Deliverable 5 — Corpus Placement Plan for angular-momentum-and-spin.md

| Bridge section (line) | Disposition |
| --- | --- |
| Primitive Status through Maximum-Curvature Binary (22–216) | Family-general; generalize in place, light terminology pass only. |
| Nested Shell Braid Spin Scaffold (217) | Retain as explicitly nested-comparison subsection; new preceding `Spindle Spin Scaffold` section (Deliverable 1) becomes primary. |
| Delayed Three-Layer Functional Scaffold (279) | Nested realization; retain as comparison; new spindle counterpart = single-cadence functional scaffold (rank-one spine + caps). |
| Partition Equations from the Master Ledger (537) | Retain nested per-frequency partition as comparison; iso-frequency allocation map (Deliverable 2) becomes primary. |
| Worked Outer-Coupled Transition + subsections (769–1367) | Retain as worked example explicitly scoped to the nested realization; spindle worked example gated on native-release data (flutter). |
| Ordered-Frame Spinor Target (1368) | Generalizes with upgrade: the spindle's measured locked frame (spin axis, cap polarity dipole, cap azimuthal offset; helicity-polarity lock maximal under drift) supplies the ordered frame; C-degeneracy and CP structure inherited from spindle discrete-symmetry results. |
| The $h$ and $\hbar$ Convention (1924) | Consumer of L5. |
| Foundation-Up Closure Route through Bell Handoff (2022–3638) | Consumer sections; retarget internal scaffold references to the spindle scaffold; no independent nested dependence. |
| Terminology Rules; Closure Targets (3639+) | Update to spindle-first with gate notes for retained nested-comparison sections. |

### Next Steps

1. Dedicated single-owner session: execute the placement plan on the bridge (slugs preserved, near-verbatim relocation for retained material).
2. Prove L1 (exact, elementary) and draft the L2 symmetry argument; both are corpus-ready lemma statements for the new Spindle Spin Scaffold section.
3. Dependency coordination: L5 waits on braid-ideal queue item 23 output; L4 co-feeds the native release.

## Session 2 Record (2026-07-10, operator thread: Deliverables 1 and 4)

Scope: packet deliverables 1 and 4, in that order, per the operator thread contract; deliverables 2, 3, and 5 untouched. Executed concurrently with Session 1 above; the two sessions are consistent — Session 1's L1–L3 lemma targets are realized below as drafted bridge prose, L4 is executed, and the drafted section's position defers to the Session 1 placement plan.

**Deliverable 1 — drafted into the bridge.** New section `Spindle Braid Spin Scaffold` in [angular-momentum-and-spin.md](../../../content/markdown/aaa/philosophy-history/theory-bridges/angular-momentum-and-spin.md#spindle-braid-spin-scaffold), currently placed after the `Delayed Three-Layer Functional Scaffold` (the Session 1 placement plan calls for it to become the primary scaffold preceding the nested-comparison material; relocation belongs to the placement session, slugs preserved). Content: (i) the exact kinematic ledger of the rigid uniaxial family — rank-one spine $\mathbf J_{\mathrm{kin}}=\sum_a 2\mu\rho_a^2\omega\,\hat{\mathbf z}$ exact at every instant (Session 1's L1, family-general per-layer form), per-layer spin/tilt-inertia split $J_a=2\mu\rho_a^2\omega$ versus $m_a=\mu(\rho_a^2+2z_a^2)$ as the cap structure, exact momentum screw and helicity label; (ii) the wake ledger with the causal-delay asymmetry stated as physics — tilt-block row sums null (isotropy), column sums nonzero (the field in flight carries angular momentum; the tilt-sector posting to the wake ledger; Session 1's L2 signature), baseline axial rows $\tau_I,\tau_O\approx0$ with the middle's rail pump as the standing wake entry, and the transacted $J_z(u)$ per-click law routed to the $h/\hbar$ section; (iii) the quotient-spectrum discipline stated as method (L3), with the dynamical-null refinement from Deliverable 4 below; (iv) the ordered frame anchored on the measured helicity-polarity lock (spin axis, cap polarity dipole, cap azimuthal offset; pseudoscalar pairing as handedness). Cross-link added from the spindle scene's stability-gate paragraph. Claim levels carried unchanged.

**Deliverable 4 — executed; the verdict is FLUTTER.** Instrument: `gyroscopicTiltAnalysis` extending the braid-ideal owner script [spindle-support-ratio-targeted-search.mjs](../../../scripts/braid-ideal/spindle-support-ratio-targeted-search.mjs) (tests in [braid-ideal-spindle-support-ratio-targeted-search.test.js](../../../tests/braid-ideal-spindle-support-ratio-targeted-search.test.js), 19 passing), Sections 57–59 conventions: same cycle-averaged $\kappa^*$-scaled exact-causal-root torque evaluator as the tilt block, both transverse tilt axes per layer. One correction to the packet's pencil was required by the physics: the honest linearization is $\lambda^2M+\lambda G+\Gamma-K$, where $\Gamma$ is the spin-transport block of the measured baseline axial torques — without it the global-tilt null is not exact (the rail pump reorients under a global tilt). Validation rows: baseline transverse torques $\sim10^{-16}$; cycle-averaged response exactly $z$-rotation covariant ($\sim10^{-15}$); $x$-$x$ block $=$ Section 59 tilt block to $10^{-9}$ with its global null at $10^{-15}$; cross-block row sums $=$ measured layer axial torques to $7\times10^{-4}$ (the pump entering the axis sector, measured both ways); exact global double zero root recovered and deflated. **Quotient verdict at V5, $N_t=8/16$ identical, $\eta$-step witnessed:** growing whirl pair $\lambda=+0.183\pm0.382\,i$ (e-fold $\approx0.9$ rotation, whirl period $\approx2.7$ rotations), mode shape middle-dominated with strong inner participation (the Section 59 inner-tilt$\to$middle-torque pathway) and caps near-spectator; second weak fast pair $+0.037\pm1.406\,i$; the pump-absorbed counterfactual ($\tau_M$ removed from $\Gamma$) still flutters at $+0.275\pm0.435\,i$. The restoring quasi-static tilt block does not survive the gyroscopic-circulatory coupling, and the instability is independent of the rail pump. Native corroboration: mode class and direction match the Row 7 nutation growth ($2.4°\to7.8°$ over $t=0.2\to1.0$, preceding shape loss); the seed-grade linear rate underpredicts the native mid-window rate (frozen-geometry pencil; the released braid was simultaneously expanding). Named-input statement recorded in [fold-crossing-chart-spec.md](../braid-ideal/fold-crossing-chart-spec.md) Section 61. Claim level: seed-grade instrument on prescribed worldlines, natively corroborated in direction; fail-closed — no retention claim, and nothing in this session authorizes a release or re-release.

## Session 3 Record (2026-07-10, operator thread: Deliverables 2, 3, and 5)

Scope: operator-directed continuation of Session 2 in the same thread; executes the Session 1 placement plan and the two remaining derivation deliverables. All bridge edits under the standing near-verbatim/slug-preserving discipline; claim levels carried from sources unchanged.

**Deliverable 2 — the iso-frequency partition map, drafted into the bridge** (new subsection of the spindle scaffold). The increment identities are exact kinematics: $\Delta J_z=\sum_a 4\mu\omega\rho_a\Delta\rho_a+2\mu(\sum_a\rho_a^2)\Delta\omega+\Delta L_{\text{wake},z}$ with $\Delta\rho_a=\cos\alpha_a\Delta R_a-R_a\sin\alpha_a\Delta\alpha_a$, and transverse $\Delta\mathbf J_\perp=\sum_a J_a\Delta\boldsymbol\eta_a+\Delta\mathbf L_{\text{wake},\perp}$ (tilt cost second order axially). Three structural constraints distinguish it from the nested per-frequency partition: one common $\Delta\omega$; the rail pin slaving the cadence to the middle's geometry ($\Delta\omega/\omega=-\Delta R_M/R_M+\tan\alpha_M\Delta\alpha_M$, inheriting the pin's claim level); and the single energy price $\Delta E_{\text{core}}=\omega\Delta I_{\text{core}}$ at first order regardless of allocation — $\Delta E=\hbar\omega$ as a ledger identity of the family, moving the partition question onto the constraint structure (support sum rule, stiffness spectrum, admissibility). Soft coupled relative-tilt modes named as strain sinks / candidate click storage. Allocation dynamics stated open.

**Deliverable 3 — the spinor hookup, drafted into the bridge** (new `The Spindle Locked Frame` subsection of the Ordered-Frame Spinor Target, with the nested chart retitled `The Nested Ordered-Frame Chart` and scoped as the nested-comparison realization). Key structural point: the non-coplanarity chart $\det[\hat{\mathbf n}_I,\hat{\mathbf n}_M,\hat{\mathbf n}_O]\ne0$ has no uniaxial image — the spindle's ordered frame is the measured locked triple $(\hat{\boldsymbol\jmath},\hat{\mathbf d},\theta_O;\mathcal R)$ with handedness the pseudoscalar $h=\hat{\mathbf d}\cdot\hat{\boldsymbol\jmath}$; C flips $\hat{\mathbf d}$, mirror flips $\hat{\boldsymbol\jmath}$, CP restores $h$ (inherited claim levels); the gauge quotient must not remove dipole reversal, rotation-sense reversal, or root-branch change; the $\epsilon_r^{2\pi}$ parity test carries over unchanged, posed on the locked frame's retained rows. Closure target 6 restated spindle-first.

**Deliverable 5 — placement plan executed** (conservative grade, no content loss, slugs preserved; the only retitle is the added `### The Nested Ordered-Frame Chart` subsection heading, which introduces a new anchor without removing any existing one). Spindle scaffold relocated to primary position preceding the nested scaffold; scoping sentences added to the four retained nested sections (nested spin scaffold, delayed three-layer functional scaffold, partition equations, worked outer-coupled transition — the worked example explicitly gated on native-release data); intro reading path spindle-first; Foundation-Up Closure Route gate-noted (the winding-triple/normal-triad route degenerates deliberately at one cadence; spindle-native restatement is an open re-derivation gate); realization-agnostic consumer references generalized to `Noether braid` (six instances in the Stern-Gerlach/Bell/apparatus material); Downstream Use spin row spindle-first; $h/\hbar$ section given the iso-frequency form and the L5 transacted-ledger consumption (the two-ledger split and the $J_z$-conserving versus closure-optimal coincidence question, stated at measurement/open claim levels); Terminology Rules given the spindle-primary/nested-comparison rule; Closure Targets updated (items 2, 5, 6 spindle-first; new targets 16 — the axis-sector absorber, from the Session 2 flutter verdict — and 17 — the item-23 $h$-constancy comparison). Sections 22–216 verified free of nested references (no terminology pass needed).

Verification: `validate-content --check --strict` reports zero errors attributable to this session's files; the 36 ambient errors at check time reference `noether-braid-doubling-frequency-resonance-lock.md`, deleted by a concurrent thread, with stale links in generated reading copies (that thread's regeneration burden). Scene-graph TOC drift from the new bridge headings is expected; regeneration deferred per the standing generated-artifacts policy.
