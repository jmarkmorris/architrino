# Target Ref/Value Object-Domain Membership-Predicate Signature Without Contract Link Target

## Verdict

Status: priority-only-fold-coordinate-endpoint-functional-target-ref-value-object-domain-membership-predicate-signature-without-contract-link-target-fail-closed-candidate-symbol-sort-convention-stated-proof-burdens-separated-no-row-consumption

This target follows the target ref/value object-domain
membership-predicate-signature proof attempt. It states a packet-local
candidate symbol and sort convention for the first exact blocker
`target_ref_value_object_domain_membership_predicate_signature_present`, while
keeping the proof-attempt fields absent.

The target is priority-only. It consumes 0 rows, constructs 0 branch-chart
artifacts, imports no `witness_object_has_contract_link` premise, accepts no
primitive signature, and promotes no predicate convention into
`content/markdown/aaa`.

## Source State

The latest proof attempt permits these fields to be reused as source scope:

| Premise field | Current count | How this target may use it |
| --- | ---: | --- |
| `parent_membership_predicate_proof_attempt_input_present` | 4 / 4 | Parent packet for each endpoint functional. |
| `parent_membership_predicate_source_scope_ready` | 4 / 4 | Source scope for stating the candidate signature target. |
| `definition_source_data_preserved` | 4 / 4 | Preserved source data for a later derivation attempt. |
| `target_ref_value_source_payload_preserved` | 4 / 4 | Target ref/value payload data to be typed by the signature. |
| `target_endpoint_ref_value_source_equations_present` | 4 / 4 | Source-equation sets that remain source facts, not predicate arguments. |
| `source_equation_only_guard_present` | 4 / 4 | Guard against accepting source-equation handles as target objects. |
| `endpoint_value_map_only_guard_present` | 4 / 4 | Guard against accepting value-map payloads as target objects. |
| `endpoint_value_binding_map_ref_values_certified` | 4 / 4 | Attachment evidence for endpoint values, not a predicate signature. |
| `value_map_ref_value_payload_matches_target_object` | 4 / 4 | Payload matching evidence, not a predicate argument. |
| `contract_link_premise_not_imported` | 4 / 4 | The signature target must exclude `witness_object_has_contract_link`. |
| `no_contract_link_independence_guard_declared` | 4 / 4 | No-link guard available as an exclusion premise. |
| `witness_object_has_contract_link_excluded` | 4 / 4 | Confirms the prohibited contract-link field remains excluded. |
| `primitive_domain_definition_acceptance_not_used` | 4 / 4 | Confirms this target cannot accept a domain definition as primitive. |
| `primitive_membership_predicate_acceptance_not_used` | 4 / 4 | Confirms this target cannot accept the predicate as primitive. |
| `primitive_signature_acceptance_not_used` | 4 / 4 | Confirms this target cannot accept the signature as primitive proof evidence. |
| `membership_predicate_signature_source_scope_ready` | 4 / 4 | Confirms the target has the right source scope for a signature attempt. |
| individual target ref/value source equations | 6 / 6 | Preserved equations to be guarded from direct predicate-argument promotion. |
| individual value-map bindings | 6 | Preserved bindings to be guarded from direct predicate-argument promotion. |
| row signature source-scope pairs | 3 / 3 | Confirms source and receiver signature scopes are paired at the row level. |

The target does not claim these fields:

| Missing field | Current count | Why it remains missing |
| --- | ---: | --- |
| `target_ref_value_object_domain_membership_predicate_signature_present` | 0 / 4 | The typed signature is stated here only as a target convention, not proved or accepted. |
| `membership_predicate_signature_symbol_declared` | 0 / 4 | The symbol below is target-note-declared only; it is not proof evidence. |
| `membership_predicate_signature_domain_sort_declared` | 0 / 4 | The domain sort below is target-note-declared only; it is not proof evidence. |
| `membership_predicate_signature_codomain_sort_declared` | 0 / 4 | The codomain sort below is target-note-declared only; it is not proof evidence. |
| `target_ref_value_object_argument_sort_present` | 0 / 4 | No proof yet shows target ref/value objects are legitimate arguments. |
| `source_equation_handle_non_argument_sort_present` | 0 / 4 | No proof yet excludes source-equation handles as arguments. |
| `target_note_non_argument_sort_present` | 0 / 4 | No proof yet excludes target notes as arguments. |
| `payload_match_non_argument_sort_present` | 0 / 4 | No proof yet excludes payload-match records as arguments. |
| `endpoint_value_map_certification_non_argument_sort_present` | 0 / 4 | No proof yet excludes endpoint value-map certifications as arguments. |
| `no_link_guard_non_argument_sort_present` | 0 / 4 | No proof yet excludes no-link guards as arguments. |
| `signature_no_contract_link_premise_absence_proven` | 0 / 4 | No proof yet shows the signature excludes `witness_object_has_contract_link`. |
| `membership_predicate_signature_derivation_from_definition_source_data_present` | 0 / 4 | No derivation from preserved definition source data exists. |
| `target_ref_value_object_positive_membership_clause_present` | 0 / 4 | Positive membership remains downstream of the signature. |
| `target_ref_value_object_domain_membership_predicate_present` | 0 / 4 | The full membership predicate remains downstream of this target. |
| `target_ref_value_object_domain_defined` | 0 / 4 | The object-domain definition remains downstream of the predicate and constructor stack. |
| `row_consumption_authorized` | 0 / 4 | No row may be consumed from this target alone. |
| `branch_chart_authorized` | 0 / 4 | No branch chart may be authorized from this target alone. |

## Candidate Convention

For each endpoint functional `e`, use the local predicate symbol
`is_target_ref_value_object_e`.

```text
is_target_ref_value_object_e : target_ref_value_object_argument_e -> truth_value_judgment
```

The argument sort `target_ref_value_object_argument_e` is reserved for target
ref/value boundary-binding objects only. Source-equation handles, target notes,
payload-match records, endpoint-value-map certifications, and no-link guards
are non-argument sorts for this predicate.

The codomain sort `truth_value_judgment` records a membership judgment only. It
is not a positive membership clause, a constructor, a target ref/value
object-domain definition, an interpretation rule, a definition bridge, or a
promotion rule.

## Proof Burden

A later proof packet can discharge this target only by supplying:

1. A proof-grade declaration of
   `target_ref_value_object_domain_membership_predicate_signature_present`.
2. Proof-grade declarations for
   `membership_predicate_signature_symbol_declared`,
   `membership_predicate_signature_domain_sort_declared`, and
   `membership_predicate_signature_codomain_sort_declared`.
3. A proof of `target_ref_value_object_argument_sort_present`.
4. Proofs of all five non-argument-sort exclusions:
   `source_equation_handle_non_argument_sort_present`,
   `target_note_non_argument_sort_present`,
   `payload_match_non_argument_sort_present`,
   `endpoint_value_map_certification_non_argument_sort_present`, and
   `no_link_guard_non_argument_sort_present`.
5. A proof of `signature_no_contract_link_premise_absence_proven`, excluding
   `witness_object_has_contract_link` directly and through inherited source
   scope.
6. A proof of
   `membership_predicate_signature_derivation_from_definition_source_data_present`
   from preserved definition source data.

Only after those obligations are met may a downstream packet attempt the
positive membership clause, nonmembership clauses, soundness proof, endpoint
application proof, derivation, and full membership predicate.

## Endpoint Targets

The signature proof must instantiate the candidate convention for each endpoint
functional:

| Endpoint | Role | Source scope ready | Current signature status |
| --- | --- | ---: | --- |
| `fc_sigma_source_lower` | source | true | target convention stated; not proved |
| `fc_rho_receiver_lower` | receiver | true | target convention stated; not proved |
| `fc_sigma_source_upper` | source | true | target convention stated; not proved |
| `fc_rho_receiver_upper` | receiver | true | target convention stated; not proved |

## Row Pair Targets

The three row signature source-scope pairs may be used only as application
targets after both the source and receiver endpoint functionals have
proof-grade target ref/value object-domain membership-predicate signatures.

| Row | Pair target status | Current row status |
| --- | --- | --- |
| `R_w_A04_A03` | source and receiver signatures required | not unblocked; not consumed |
| `R_u_A10_A09` | source and receiver signatures required | not unblocked; not consumed |
| `R_u_A07_A06` | source and receiver signatures required | not unblocked; not consumed |

This target does not create:

- a positive membership clause;
- source-handle, target-note, payload-match, endpoint-value-map certification,
  or no-link-guard nonmembership clauses;
- membership-predicate soundness proof;
- endpoint application proof;
- full membership predicate;
- object-domain constructor;
- target ref/value object-domain definition;
- interpretation rule;
- definition bridge;
- promotion rule;
- proof-grade target ref/value equation package;
- `row_consumption_authorized`;
- `row_unblocked`;
- `row_consumed`;
- live-ledger update;
- branch-chart authorization.

## Failure Modes

- If the proof treats a source-equation handle as an
  `is_target_ref_value_object_e` argument, it violates the
  source-equation-only guard.
- If the proof treats endpoint value-map certification as an object argument,
  it violates the endpoint-value-map-only guard.
- If the proof treats a payload match as an object argument, it confuses
  agreement evidence with membership-domain content.
- If the proof imports `witness_object_has_contract_link` directly or through
  inherited source scope, it fails the no-link target.
- If the proof skips the five non-argument-sort exclusions, it can only name a
  predicate symbol, not a well-scoped target object-domain signature.
- If the proof skips derivation from preserved definition source data, it
  leaves the signature as an unsupported target convention.
- If the proof consumes rows before the positive membership clause and full
  membership predicate are proved, it exceeds this target's authority.

## First Safe Proof Route

The low-risk next proof attempt is a derivation-from-definition-source-data
route: prove the candidate signature from preserved target ref/value payloads,
source-equation guards, endpoint value-map certifications, payload matches, and
no-link guards. That route remains fail-closed unless it supplies the complete
signature statement, symbol declaration, domain and codomain sort declarations,
argument-sort proof, five non-argument-sort exclusion proofs, no-link premise
absence proof, and derivation.

Accepting `is_target_ref_value_object_e` as a primitive signature would be a
theory decision. That decision is not made here.

## Capture Decision

Priority-only; records the candidate target ref/value object-domain
membership-predicate signature target without contract link and does not
promote to `content/markdown/aaa`. Promotion is deferred until the signature is
derived from existing source data or explicitly accepted after operator
discussion as a new primitive signature.
