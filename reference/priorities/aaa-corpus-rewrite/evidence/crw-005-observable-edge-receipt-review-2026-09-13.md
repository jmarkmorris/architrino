# CRW-005 The Observable Edge Receipt review — 2026-09-13

## Scope and disposition

Read the full five-line [The Observable Edge Receipt leaf](../../../../content/markdown/aaa/archie/comics/observable-edge-receipt.md) and visually inspected the original [artwork](../../../../content/assets/images/comics/observable-edge-receipt.png) with `view_image`. Unchanged pass, with no substantive finding. Only this evidence receipt was written; source, approved image, runtime, generated assets, shared records and Git/index state were not edited.

Baseline/final source SHA-256: `ad0e3aac9ce6ef8b021e5fcbd38a354f1cce9f7b6adab5e737062ba5eaf63eb4`.

Original/final artwork SHA-256: `688fcf3f206f822c04149ddf3bd71676e4870d534828d6a6c8e7cb7e695fb090`.

Scoped initial `git --no-optional-locks status --short` returned no chapter change. Final equality against HEAD confirms the unchanged source.

## Reasoning and visual evidence

The caption's receipt/transaction metaphor requires tracing an observed record through source, path, detector and interpretation. It does not claim that this reconstruction has already proved a particular global cosmology.

The artwork asks whether the map ends at the observed boundary and answers that the universe “need not.” Its proposed unbounded fixed-void universe is explicitly labeled “candidate.” A separate note says a boundary in a data product is not automatically an ontological boundary. These are appropriately scoped inference distinctions: restricting observations to an accessible region can leave multiple global extensions compatible with the same records. That underdetermination alone proves neither an unbounded universe nor a physical outer edge.

The humorous inequality between the edge of information and the edge of existence is read with those surrounding qualifiers, not as a theorem that the two sets can never coincide. The sketch is a conceptual illustration, not a metric calculation. No literal mathematical derivation or new empirical result requires correction. Approved lettering, characters, image and caption remain intact.

## Verification and limits

A marked-based nested image/link extractor first passed a known linked-image control, returning one link and one image reference in order, then checked the target. Both routes are identical to HEAD and resolve to the original existing image. Full byte equality preserves the title, caption and image syntax. There are no TeX spans or standalone mathematical displays in the source.

Node crypto and `shasum -a 256` verify the source and image hashes. Direct source/receipt whitespace and local-path checks and scoped `git diff --check HEAD` pass. Direct validation covers this new receipt, which ordinary Git diff omits.

This is a bounded comic source and visual-context review, not a whole-cosmology proof, collection-wide visual audit or new artwork approval. No chapter-local blocker or follow-up obligation was identified. Shared-record integration belongs to the coordinator.
