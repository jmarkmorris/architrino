# CRW-005 — Simulation README bounded review — 2026-09-13

## Scope and disposition

Board rank 3: [Simulation README](../../../../content/markdown/aaa/validation/simulations/README.md). The complete 52-line index chapter received a bounded review and safe repairs for SRM-01–SRM-05: one High and four Medium findings. High denotes a defect that can change a mathematical or evidentiary inference; Medium denotes a consequential definition, scope, attribution, terminology, or exposition defect. The chapter is a directory index with no display equations, no equation-viewer links, and no external citations, so the finding count is small by construction and has not been padded. Completion is document-level disposition, not evidence that any indexed protocol has been executed or passed, not EOM solver acceptance, not theory closure, and not publication.

Only the assigned chapter and this receipt were written by this worker, `crw-005 simulation-readme worker`. No supporting agent was launched; every check below was run by this worker, so the review is editor self-review plus the separate references named under each finding: the live H1 title of each indexed chapter, the retained Git history of those titles, the directory listing, and the terminology owners. Agent agreement is not independent evidence; the comparisons in the independent-checks section are the references a reader can rerun by hand.

Claim grade: measured. At dispatch, scoped `git --no-optional-locks status --short -- content/markdown/aaa/validation/simulations/README.md` was empty, `git rev-parse HEAD` returned `06fc05899fa972f5b43bf5e1d58e16e6b8f9ac95`, and the chapter's `sha256sum` matched both the coordinator-measured value and the bytes from `git show 06fc05899fa972f5b43bf5e1d58e16e6b8f9ac95:content/markdown/aaa/validation/simulations/README.md`:

    fce18d0c8b84e55fff0d3cf7d370398483a2053fbe86ee7a63e0ed2472c3a6e7

Final chapter SHA-256 from `sha256sum`, 52 lines by `wc -l`:

    b0c966201b99ce22cd73e46fea0fd4d2196b5fd2a83e8770e1321702e0d68494

Falsifier: different chapter bytes invalidate this byte-specific handoff until the difference is reviewed. No staging, commit, push, publication, reset, stash, restore, checkout, switch, rebase, or generator `--write` was performed, and `.git/index.lock` was neither created nor removed.

## Authority and source support

The live `CLAUDE.md`, `AGENTS.md` (Theory Layer Discipline, Evidence Independence, Claim Grading), the generated startup router, the corpus-reviewer and integrator-reviewer prompts, the operator explanation standard, theory orientation, the Done Criteria of the corpus-rewrite work queue, and the academic style guide (edition 1.1), mathematics style guide, mathematics terminology, terminology usage, comparative glossary, and the reference-selection and source-checking sections of About Architrino governed the review. The assignment supplied repair authority and restricted writes to two paths. The unified CRW-005 board listed this chapter at rank 3; this worker does not update the board.

Corpus owners read as evidence and left unedited: every chapter the index lists, opened to compare its H1 title and opening scope sentence with the index label — [Simulation Run Protocols](../../../../content/markdown/aaa/validation/simulations/run-protocols.md) (Master Simulation Protocol items 1–7 at lines 7–13, which own the fixed-Cartesian-grid coordinate anchor, the $\mathbb{U}_{\text{now}}$ sensor interface logging $\Phi$ and $\nabla\Phi$, and the scope-envelope inventory, and the $c_f=1$ normalization at line 339), [Convergence Tests for Non-Markovian Dynamics](../../../../content/markdown/aaa/validation/simulations/convergence-tests.md), [Architrino Simulation Tests](../../../../content/markdown/aaa/validation/simulations/architrino.md), [Simulation Perspective and Closure Targets](../../../../content/markdown/aaa/validation/simulations/perspective.md), [Synthetic Observables and $\mathbb{U}_{\text{now}}$ Logging](../../../../content/markdown/aaa/validation/simulations/synthetic-observables.md) (definition of $S(T)$ at line 12 and of the $\mathbb{U}_{\text{now}}$ perspective as a non-physical bookkeeping operator at line 286), [Bell-Family Record-Measure Harness](../../../../content/markdown/aaa/validation/simulations/bell-family-record-measure.md), [Thermodynamic Residual](../../../../content/markdown/aaa/validation/simulations/thermodynamic-residual.md), [$A_0$ Branch Certificate Protocol](../../../../content/markdown/aaa/validation/simulations/a0-branch-certificate-protocol.md), [$A_0$ Tier 0 Result Interpretation: Reduced Branch Search](../../../../content/markdown/aaa/validation/simulations/a0-tier0-result-interpretation.md), [Coincident-Midpoint Orthogonal-Axis Action-Increment Protocol](../../../../content/markdown/aaa/validation/simulations/coincident-midpoint-orthogonal-axis-action-increment-protocol.md), [Retuning-Map Toy Model](../../../../content/markdown/aaa/validation/simulations/retuning-map-toy-model.md), [Action Model Comparison](../../../../content/markdown/aaa/validation/simulations/action-energy/action-model.md) (its Sibling Derivation Notes section at line 424 links all fifteen sibling notes), [Cosmology Shared Residual Fit Protocol](../../../../content/markdown/aaa/validation/simulations/cosmology-shared-residual-fit.md), [Redshift-Budget Toy Model](../../../../content/markdown/aaa/validation/simulations/redshift-budget-toy-model.md), [Static Response Vector Toy Model](../../../../content/markdown/aaa/validation/simulations/static-response-vector-toy-model.md), and [Hydrogen $\Gamma_N$ Spectral Coefficient Row Toy Scan](../../../../content/markdown/aaa/validation/simulations/hydrogen-gamma-n-spectral-row-toy-scan.md). Foundation and terminology anchors read for the added clues: [Validation Protocols](../../../../content/markdown/aaa/validation/validation-protocols.md) (item 10 names this directory as the owner of packet schemas, convergence tests, negative controls, synthetic observables, and fixtures), [Master Equation](../../../../content/markdown/aaa/dynamics/master-equation.md), [Architrino](../../../../content/markdown/aaa/foundations/architrino.md), [Euclidean Void](../../../../content/markdown/aaa/foundations/euclidean-void.md), [Noether Sea](../../../../content/markdown/aaa/spacetime/noether-sea.md), and [Constructing the Absolute Frame](../../../../content/markdown/aaa/foundations/constructing-the-absolute-frame.md). The dated receipts for Action Model Comparison (2026-09-11), Convergence Tests (2026-09-13), and Architrino Simulation Tests (2026-09-13) were read for title changes; the title history of each listed chapter was checked with `git show <commit>:<path> | head -1` across its retained revisions.

Named artifacts. The chapter names no solver, script, test file, fixture, or machine identifier by path or name, so there is nothing to verify by `ls` or `rg`; the EOM solver is not mentioned. The chapter uses no `mass` field and no force language; `rg -n -i 'retard|\bforce\b|\bmass\b'` on the repaired chapter matches only the compound `mass-map`, which is the canonical closure-target name.

No external source was added. The chapter's content is an index plus definitions and scope statements that follow from canon; under the About Architrino selective-reference policy none requires an external citation, so no Sources section was added.

Pre-edit `rg -l -F 'content/markdown/aaa/validation/simulations/README.md' content scripts tests reference` found: the scene configuration `content/scenes/validation/sim_readme.json` (scene title `README`), the generated scene graph and textbook TOC under `content/graph/`, `content/markdown/markdown_index.json`, the generated reference-surface and source-index snapshots under `content/generated/`, the CRW-005 board, two earlier CRW-005 receipts, the dormant app-simulation work log, and two specialist role files. A relative-path search for `simulations/README.md` over the same roots plus `src/` additionally found Validation Protocols (the one authored corpus consumer), the conversion ledger, the generated textbook TOC, and two generated reading copies; no consumer under `scripts/`, `tests/`, or `src/` by either search. This bounds the search to those roots; it does not establish absence of every byte-sensitive consumer. All consumers remained read-only.

## Findings and repairs

Baseline lines refer to the hash-verified original chapter, not current line positions. Each disposition is implemented in the chapter; an open obligation is not counted as a demonstrated defect.

| ID | Severity | Baseline lines | Evidence, repair, grade, and falsifier |
| --- | --- | --- | --- |
| SRM-01 | High | 5 | The index labeled `architrino.md` as "Architrino Simulation Record", but that chapter's H1 is "Architrino Simulation Tests" in every retained revision from `626211d84` (2026-07-05) through HEAD, while the label itself dates from `8d477f41a` (2026-07-24); the two never matched. "Record" tells a reader that a record of executed simulations exists, whereas the chapter is a test specification for which no repository instrument has been identified (SIM-O1 in the Architrino Simulation Tests receipt), so the mislabel changes the evidentiary inference a reader draws from the index. Repaired to the live title; this discharges SIM-O2. Claim grade: measured against `head -1` of the live chapter and `git show` of each retained revision. Falsifier: a retained revision of `architrino.md` whose H1 reads "Architrino Simulation Record", or a repository record of executed simulations owned by that chapter. |
| SRM-02 | Medium | 5–7 | Six further labels differed from the live H1 titles. "$A_0$ Tier 0 Result-Schema Interpretation" was the title at `4c1860f86` and became "$A_0$ Tier 0 Result Interpretation: Reduced Branch Search" at `c973402b9` (2026-09-05) without the index following. "Action-Energy Model" names a chapter titled "Action Model Comparison" in every retained revision; the label implies a single model where the chapter compares three modeling options. "Bell-Family Record Measure" dropped the hyphen and the word "Harness", which is that chapter's own claim-level word ("It is not a closure proof. It is a probability-table harness"). "Convergence Tests", "Simulation Perspective", and "Synthetic Observables" are truncations that drop the subject qualifiers "for Non-Markovian Dynamics", "and Closure Targets", and "and $\mathbb{U}_{\text{now}}$ Logging". Repaired every label to the live title, and linked the phrase "sibling derivation notes" to the sub-index anchor `action-energy/action-model.md#sibling-derivation-notes` so the fifteen action-energy notes are reachable from this index through one named hop. All seventeen original link targets are retained verbatim. Claim grade: measured against `head -1` of each target and its Git title history. Falsifier: a live H1 that differs from the label now written. |
| SRM-03 | Medium | 7 | The group label "mass-map and action closure" presented its members as closures. Every member is a protocol, interpretation note, toy model, or model comparison: the $A_0$ protocol states "No computed $A_0$ branch is reported here", the Retuning-Map Toy Model states it "is not a delayed-dynamics proof", and the Action Model Comparison is a comparison of modeling options. An index must not promote a toy model or protocol to a validated result. Repaired to "protocols and toy models toward mass-map and action closure targets"; the other three group labels ("common execution and interpretation", "detector-facing and statistical outputs", "cosmology and response scaffolds") already carried their claim level and are unchanged. Claim grade: measured against the members' own scope sentences. Falsifier: a member chapter that reports a certified $A_0$ branch or a derived one-cycle action increment. |
| SRM-04 | Medium | 3, 12, 14, 16, 20, 21, 25, 26 | A reader arriving from search was not told what a simulation here is, what it validates, or against what; "the simulation protocols" had no antecedent; "All simulation tiers" used an undefined tier vocabulary that collides with the $A_0$ Tier 0/Tier 1 packets; and the Master Equation, architrino, Euclidean void, Noether sea, assembly, the complete state $S(T)$, and the clock readout $\tau$ were used without a clue or link at first use (Done Criteria 6–8). Repaired by adding an opening paragraph that names the directory as the simulation protocols of the validation program (link), defines a simulation as a numerical integration of the Master Equation (link) under the delayed acceleration law with a one-clause architrino and causal-wake clue (link), states that a result is evidence only against the independent reference its protocol names and that agreement with a run's own output or a replay establishes repeatability rather than correctness, and states that no index entry is itself a physical acceptance; by writing "All simulations are implemented in the absolute frame"; and by adding clues and links for the Euclidean void, the Noether sea, assemblies, $S(T)$ (position, velocity, polarity, and retained path history of every modeled architrino and assembly), and $\tau$ as derived clock time, the readout of a physical clock. All four headings and every original sentence's claim are retained. Claim grade: inferred exposition repair against the academic style guide, edition 1.1; the independence sentence restates the style guide's Claim Grades and Falsifiers rule and AGENTS Evidence Independence, not a new claim. Falsifier: baseline text supplying those definitions at first use, or a canon statement that replay agreement establishes correctness. |
| SRM-05 | Medium | 16, 21, 26 | "fields" named the substrate-level emitted influence the simulator computes and the detectors experience, and "potentials propagated at speed $c_f$" left the normalization unstated. The terminology owners reserve `field` for effective or comparative discussion and prefer `wake` for emitted substrate structure; the owning protocol logs the potential $\Phi$ and its gradient $\nabla\Phi$ at sensors (Run Protocols item 3) and declares $c_f=1$ (Run Protocols line 339); the Convergence Tests receipt (CT-13) made the same `field`-to-`potential` replacement in the sibling chapter. Repaired to "causal wakes, which carry the potential each architrino emits, propagate at the wake speed $c_f$, normalized to $c_f=1$ in every numerical run", "the superposed wake potential $\Phi$, its gradient $\nabla\Phi$", and "the wake potential and gradient they experience"; "master equation" is written "Master Equation" to match the new lead and the sibling chapters. The fixed Cartesian grid wording is retained because Run Protocols item 1 owns it, with its own clue that a grid address is a chart label, not a structure in the void. Claim grade: measured against the terminology owners and Run Protocols. Falsifier: a canon row establishing `field` as the substrate-level term for emitted architrino influence, or a run-protocol numerical instantiation with $c_f\neq1$. |

Index completeness, claim grade measured: `find content/markdown/aaa/validation/simulations -type f -name '*.md' | sort` lists 32 files and `find ... -type f ! -name '*.md'` lists none; the index links the 15 top-level chapters and `action-energy/action-model.md`, whose Sibling Derivation Notes section links each of the other 15 `action-energy/` files exactly once by `grep -c`, so every file is reachable from this index within one hop and no index entry lacks a file. Falsifier: a `.md` file under that directory that neither this index nor the action-model sub-index links. The loose subject grouping of Thermodynamic Residual and of the two spacetime-facing response fixtures is an optional improvement not applied (SRM-O1).

## Independent mathematical checks and boundaries

The chapter contains no display equation and no derivation, so no equation was checked by hand. The independent references for the findings are non-mathematical and are named so a reader can rerun them: for SRM-01 and SRM-02, the H1 of each target chapter by `head -1` and the title at each retained revision by `git show <commit>:<path> | head -1`; for SRM-03, the members' own scope sentences quoted above; for SRM-05, the terminology rows in the mathematics terminology (`Field`, `Wake`, `c_f`) and the Run Protocols items cited. The one physical sentence the repair adds — a receiver is accelerated only by the causal wakes that arrive at it at the current instant — is the canonical form's causal-set restriction, $T_t\in\mathcal C_{r\leftarrow t}(T_r)$ with $\|\mathbf X_r(T_r)-\mathbf X_t(T_t)\|=c_f(T_r-T_t)$, restated in words from the Master Equation and the mathematics style guide; no new numerical instantiation was made, and $c_f$ appears only symbolically or as the declared normalization $c_f=1$.

Boundaries. Nothing here certifies the EOM solver, executes or verifies any indexed protocol, or establishes that the fifteen action-energy notes are individually reader-ready; those chapters have their own CRW-005 rows. The lead paragraph's statement of what simulations validate is scope, not a claim that any recovery has been achieved.

## Validation record

Claim grade: measured. Instruments and limits are recorded here; the falsifier for each is a rerun on the same bytes that fails the stated check.

- Known-case-first Node instrument (`/tmp/srm/audit.mjs`, KaTeX 0.16.47 from the repository `node_modules`, `throwOnError: true`, `strict: 'error'`): on a synthetic input it extracted exactly five TeX expressions (four inline, one display), rejected `\badcmd{x}` and an unclosed subscript, excluded fenced and inline-code content including a fenced bad display and a fenced missing link, found exactly five links, resolved a valid path and a GitHub-style fragment slug, rejected a missing path and a missing fragment, found one heading and one viewer identity, detected trailing whitespace at the known line, and distinguished a double final newline from a single one. Two expectations in the hand-written known case were wrong on the first run: the trailing-whitespace line was miswritten as 16 (a recount of the synthetic input gave 14, which the instrument had reported), and the synthetic viewer link's `equation-mapping.html` did not exist in the scratch directory (the instrument correctly reported it missing; the file was created for the known case). Both corrections were to the known case, not the instrument, and the pass was recorded before the target run.
- Target run on the hash-verified baseline: 19 TeX expressions, 0 displays, 0 viewer identities, 4 headings, 17 links, all local, all resolving, 0 trailing-whitespace lines, single final newline, exit 0. Target run on the repaired chapter: 23 TeX expressions, 0 displays, 0 KaTeX errors, 23 local links with 0 unresolved paths or fragments (including the new `#sibling-derivation-notes` fragment against the live action-model H2), 0 trailing-whitespace lines, single final newline, exit 0.
- Preservation against the baseline by a separate Node comparison: all 4 baseline headings present verbatim, all 17 baseline link targets present verbatim (6 added: `../validation-protocols.md`, `../../dynamics/master-equation.md`, `../../foundations/architrino.md`, `action-energy/action-model.md#sibling-derivation-notes`, `../../foundations/euclidean-void.md`, `../../spacetime/noether-sea.md`), all 19 baseline inline TeX bodies present verbatim, 0 viewer identities before and after, 52 lines before and after. Changed viewer identities: none, because the chapter has no display equations and no `View →` links.
- `node scripts/validate-equation-mapping-links.mjs`: exit 0; 23 registered equation links resolve.
- `node scripts/validate-content.mjs --check --strict` (pre-receipt): exit 0; 391 scene files, 199 content Markdown files, 1735 repository Markdown files audited; 0 errors, 0 warnings, 30 notes (all `no-incoming-link` notes on element scenes, unrelated to this chapter).
- `node scripts/check-braid-taxonomy-terminology.mjs`: exit 0; 359 migrated-scope files scanned, no stragglers.
- `node scripts/build-equation-mapping-corpus.mjs --check`: exit 1; 199 Markdown files, 4685 displays, 23 promoted equations, 30481 symbol definitions; the sole reported defect is the stale `content/generated/equation-mapping/corpus-equations.json`. This chapter contributes no display, so the stale registry is expected drift from authored edits elsewhere in the shared tree, not from this task. The exact deferred command is `node scripts/build-equation-mapping-corpus.mjs --write` followed by its `--check`, only under separate regeneration or publication authority; it was not run.
- Scoped `git --no-optional-locks diff --check -- content/markdown/aaa/validation/simulations/README.md`: exit 0. Scoped `git diff --stat`: 11 insertions, 11 deletions. Scoped status ` M` (working tree modified, not staged) at the time of the chapter checks.
- `rg -n -i 'retard|\bforce\b|\bmass\b|\bfield'` on the repaired chapter: one match, the compound `mass-map` in the group label; no `field`, `force`, or causal-delay variant.
- Post-receipt runs are recorded in the handoff paragraph below with the final chapter hash.

## Unresolved obligations and handoff

- SRM-O1: The subject grouping is loose: Thermodynamic Residual (a local-horizon comparison scaffold) sits under "detector-facing and statistical outputs", and the Static Response Vector Toy Model and Hydrogen $\Gamma_N$ scan (spacetime clock-channel fixtures) sit under "cosmology and response scaffolds". Regrouping by subject owner is an optional editorial improvement that changes no claim and was not applied under the bounded rules; a reader misled about a chapter's subject by its group would be the falsifier of "optional".
- SRM-O2: The chapter's H1 "Simulation README" and scene title `README` are repository vocabulary rather than a reader-facing title; Validation Protocols item 10 calls the directory "Simulation Protocols". A retitle would change a heading and a scene configuration, both outside this bounded repair, and is left for the owner.
- SRM-O3: Index labels and scene titles for this directory are maintained by hand (the scene titles `Architrino` and `Perspective` also differ from their chapters' H1s), so label drift of the kind found in SRM-01 and SRM-02 can recur whenever a chapter is retitled. This is recorded as residual review risk, not as a request for a new checker; the falsifier for the repaired index is any future H1 change in a listed chapter without a matching label edit here.

Post-receipt checks. `node scripts/validate-content.mjs --check --strict`: exit 0; 391 scene files, 199 content Markdown files, 1736 repository Markdown files audited (this receipt is the added file); 0 errors, 0 warnings, 30 notes. Final two-file instrument run: 23 chapter and 35 receipt TeX expressions rendered under strict KaTeX with 0 errors; 23 chapter and 23 receipt local links resolved, fragments included; trailing-whitespace and single-final-newline checks passed on both files, which covers the untracked receipt that a tracked-file diff check does not see. Scoped `git --no-optional-locks diff --check` on both paths: exit 0. Final scoped status: chapter ` M` (working tree modified, not staged), receipt `??` (untracked); `git diff --cached --name-only` on both paths returned nothing, so neither file is staged; `.git/index.lock` absent by `ls`. The chapter hash matched the final value recorded above.

SIM-O2 from the Architrino Simulation Tests receipt is discharged by SRM-01; the coordinator can close it. The bounded implementation stops after this chapter and receipt. No next document is started, no shared board is updated, and nothing is staged. No physical acceptance, EOM solver acceptance, theory closure, or publication is claimed.
