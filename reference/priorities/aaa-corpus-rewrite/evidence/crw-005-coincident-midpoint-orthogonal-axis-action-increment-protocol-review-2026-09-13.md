# CRW-005 — Coincident-Midpoint Orthogonal-Axis Action-Increment Protocol bounded review — 2026-09-13

## Scope and disposition

Board rank 9: [Coincident-Midpoint Orthogonal-Axis Action-Increment Protocol](../../../../content/markdown/aaa/validation/simulations/coincident-midpoint-orthogonal-axis-action-increment-protocol.md). The complete chapter received a bounded review and safe repairs for CMP-01–CMP-12. These are three High and nine Medium findings. High denotes a defect that can change a mathematical or evidentiary inference; Medium denotes a consequential definition, scope, attribution, or exposition defect. Completion is document-level disposition, not physical acceptance, EOM solver acceptance, theory closure, or publication. No retained coincident-midpoint orthogonal-axis braid branch exists in the corpus, so the protocol remains a specification that no packet has yet run to promotion.

Only the assigned chapter and this receipt were written by this worker. Shared CRW-005 status, priorities, work log, and queue remain the coordinator's responsibility. No supporting agent was launched; the review and the repairs are the work of one agent and are therefore editorial self-review. Agent agreement is not independent mathematical evidence; the separate dimensional, vector-identity, finite-set, fold-law, and owner-comparison arguments below supply the bounded mathematical references, and each names the live owner or theorem it rests on.

Claim grade: measured. At dispatch, `git --no-optional-locks status --short` scoped to the two assigned paths returned nothing and `git --no-optional-locks diff --cached --name-only` scoped to the chapter returned no path. The chapter's initial SHA-256 from `sha256sum` matched both the coordinator-measured value and the bytes extracted by `git show 06fc05899fa972f5b43bf5e1d58e16e6b8f9ac95:content/markdown/aaa/validation/simulations/coincident-midpoint-orthogonal-axis-action-increment-protocol.md`, at 249 lines:

    2e2f701fae611af075af1496684edcefe61001547502b8929b7d57ce48b5994c

Final chapter SHA-256 from `sha256sum`, at 251 lines:

    cc41ac7013aa1af4f9fef5a6d9decfa4bd533c95fa4545bf4b38606d25503640

Falsifier: different chapter bytes invalidate this byte-specific handoff until the difference is reviewed. No staging, commit, push, publication, reset, stash, restore, checkout, or generator `--write` was performed, and `.git/index.lock` was neither created nor removed.

## Authority and source support

The live CLAUDE.md, AGENTS.md (Theory Layer Discipline, Evidence Independence, and Claim Grading), the generated startup router, the Corpus Reviewer and Integrator Reviewer prompts, the operator explanation standard, the theory orientation, and the five Archie style and terminology authorities with the reference-selection and source-checking sections of About Architrino governed the review. The explicit assignment supplied repair authority and restricted writes to two paths. The live CRW-005 board identified the chapter as rank 9; this worker does not update or infer new shared board counts.

Nearby owners inspected as source evidence and left unedited: [Master Equation](../../../../content/markdown/aaa/dynamics/master-equation.md) (parameter dimensions, the generic fold law in Caustic Transit and Finite Impulse, the quadratic kinetic proxy, the candidate action-level wake energy, and the acceleration-residual finite-window balance), [Effective Lagrangian](../../../../content/markdown/aaa/dynamics/effective-lagrangian.md) (the conditional integer action condition and the scale-only versus complete Euler-derivative distinction recorded by its 2026-09-11 bounded repair in the CRW-005 work queue), [Causal Action Functional](../../../../content/markdown/aaa/dynamics/causal-action-functional.md), [Delay Dynamics Energy](../../../../content/markdown/aaa/validation/simulations/action-energy/delay-dynamics-energy.md) (the statement that an Euler residual is a different object from the acceleration-residual power), [Action Model Comparison](../../../../content/markdown/aaa/validation/simulations/action-energy/action-model.md), [Analytic Baselines](../../../../content/markdown/aaa/validation/simulations/action-energy/analytic-baselines.md), [Angular Momentum and Spin](../../../../content/markdown/aaa/philosophy-history/theory-bridges/angular-momentum-and-spin.md) (the $h$ and $\hbar$ convention and the conditional action-cell theorem target), [Zero-Axial-Offset Three-Binary Dynamics and Interpretation](../../../../content/markdown/aaa/noether-braid/zero-axial-offset-three-binary-dynamics-and-interpretation.md) (retention open; $h_{\mathrm{act}}$ as the closed-cycle action unit), [Braid Mathematics](../../../../content/markdown/aaa/noether-braid/braid-mathematics.md) (the hinge-click hypothesis), [Braid Taxonomy](../../../../content/markdown/aaa/noether-braid/braid-taxonomy.md) (coordinate constraints of the coincident-midpoint member), [Coincident-Axis Three-Binary Symmetry](../../../../content/markdown/aaa/noether-braid/coincident-axis-three-binary-symmetry.md), [Coordinate-Axis Six-Point Symmetry and Return Response](../../../../content/markdown/aaa/noether-braid/coordinate-axis-six-point-symmetry-and-return-response.md), [Ideal Braid Guide](../../../../content/markdown/aaa/archie/ideal-braid-guide.md), [$A_0$ Branch Certificate Protocol](../../../../content/markdown/aaa/validation/simulations/a0-branch-certificate-protocol.md) (Tier 1 order: direct evolution and return map before the Floquet gap), and [Particle Masses](../../../../content/markdown/aaa/assemblies/particle-masses.md) (stability constraint: the full acceleration law on the retained cycle before any Floquet analysis). No external source was needed: the chapter quotes no benchmark value, and its claims rest on internal derivations and owners, so no Sources section was added.

Pre-edit `rg -l -F` for the exact chapter path under `content/`, `scripts/`, `tests/`, `reference/`, and `src/` found these consumer classes: the scene configuration `content/scenes/validation/sim_nested_shell_braid_action_increment_protocol.json` (path only, no section keys), the scene and textbook graphs, the Markdown index, the generated equation registry, textbook reading copies and TOC, the generated reference-surface and source-index snapshots, three authored corpus chapters (the simulations README, `architrino.md`, and the $A_0$ Branch Certificate Protocol), the simulation-protocol routing index, and CRW-005 records. A basename search additionally found the iOS generated textbook package. The only inbound fragment from authored corpus text is `#branch-chart-conservation-pullback`, whose heading is retained. This bounds the search; it does not establish absence of every possible byte-sensitive consumer. All generated targets and all consumers remained read-only.

## Findings and repairs

Baseline lines refer to the hash-verified original chapter, not current line positions. Each disposition below is implemented; an open scientific obligation is not counted as a demonstrated physical failure. All thirteen display equations and their viewer identities are byte-identical to the baseline; every repair is in prose, list items, or table cells.

| ID | Severity | Baseline lines | Evidence, repair, grade, and falsifier |
| --- | --- | --- | --- |
| CMP-01 | High | 33–41, 167–178, 217, 219 | Endpoint eligibility and the Floquet gap were admitted without requiring that each endpoint be a retained solution of the delayed dynamics; the chapter elsewhere calls the configuration prescribed. A stability spectrum is the linearization of the one-period return map about a cycle the dynamics occupies, so a gap about a non-solution has no referent. Added the return-map and acceleration-residual retention record to the eligibility list, the ordering statement, the definition of the multipliers, and gate 2; linked the $A_0$ Tier 1 order and the Particle Masses stability constraint. Claim grade: derived from the definition of a Floquet multiplier and measured against AGENTS.md and the two owners. Falsifier: a construction assigning meaning to multipliers about a configuration that does not close under the delayed dynamics, or a retained-branch record for the endpoints that already exists and was overlooked. |
| CMP-02 | High | 94–96, 140–149, 196, 216, 231 | The benchmark residual $\delta_h$ depends on the bookkeeping constant $\mu_{\text{arch}}$ and the unit map, neither of which the chapter required to be fixed independently of $h$, so a small $\delta_h$ could be manufactured by one free parameter. Dimensional analysis shows the run itself derives only the pure number $N_*=\Delta I_*^{\mathrm{spec}}c_f/(\kappa\epsilon^2)$. Added the $N_*$ statement with $c_f=1$, the independent-calibration requirement, the campaign-file row, gate 1, and the contamination trigger. Claim grade: derived by dimensions. Falsifier: a dimensionless action built from $\kappa$, $\epsilon$, and $c_f$ alone, or an independently derived $\mu_{\text{arch}}$ already fixed in the corpus. |
| CMP-03 | High | 121 | The pullback residuals subtracted "Euler-residual" terms, which the repaired Master Equation and Delay Dynamics Energy owners distinguish from the acceleration-residual terms that actually enter the finite-window balance; the substitution needs a derived conversion that does not exist. Named the acceleration-residual power, impulse, and moment terms with the boundary flux, linked the owner, and required the residual to be reported so a large residual is not hidden by subtraction. Claim grade: measured against the live owners. Falsifier: an owner passage deriving the Euler-to-acceleration conversion or defining the balance with the Euler residual. |
| CMP-04 | Medium | 66–78 | The reference point $\mathbf X_C(T)$ in the acceleration moment was never defined, and the moment equals the rate of change of angular momentum only for a fixed point or the equal-weight centroid. Defined the centroid, stated the identity and the vanishing extra term, and named the transaction axis and window. Claim grade: derived by the product rule. Falsifier: a reference point whose velocity is not parallel to the summed constituent velocity leaves the term $-\mathbf V_C\times\sum_i\mathbf V_i$ nonzero. |
| CMP-05 | Medium | 94 | The wake boundary term carrying a $\Delta$ was described as the value at the end of the window rather than the window change, and its superscript was unexplained. Defined it as the difference between the window endpoints and stated the units of both bracket terms. Claim grade: derived from the bookkeeping of the display. Falsifier: a convention under which the in-flight wake angular momentum vanishes identically at every accepted endpoint. |
| CMP-06 | Medium | 45–62 | The accepted-transition set used $\mathcal{R}_{\mathrm{phase}}$, $\mathcal{R}_{\mathrm{root}}$, $\Delta_{\mathbf{k}}$, and $\Delta N_{\mathrm{self}}$ before defining them, and the even-parity condition was unexplained. Named each and grounded the parity condition in the Master Equation's generic fold law. Claim grade: measured against Caustic Transit and Finite Impulse. Falsifier: a certified singular-stratum chart admitting an odd change in the active self-root count. |
| CMP-07 | Medium | 167–176 | The gap was labeled a basin-robustness gap, which a linearization cannot establish, and $\mu_i$, $G$, and $\mathbf{k}$ were unnamed. Renamed it the non-symmetry Floquet gap used by neighbouring owners, named the symbols, and stated that a positive gap gives linear attraction only. Claim grade: derived. Falsifier: a theorem bounding basin measure by the multiplier gap for this delayed system. |
| CMP-08 | Medium | 62, 196, 216, 245 | $h$ and $\hbar$ were wrapped in inline code, which renders as literal `$h$` text; `rg -F` over `content/markdown/aaa` finds this pattern only here and in style-guide examples. Replaced with inline math. Claim grade: measured by search and by the Markdown rule that code spans are literal. Falsifier: a renderer rule that evaluates TeX inside code spans. |
| CMP-09 | Medium | 5 | $\lambda_A$ was unnamed and the coordinate list invited nonzero axial half-separations, which Braid Taxonomy assigns to the axially separated member. Named the flattening coordinate, fixed $h_a=0$ and $\rho_a=R_a$, and linked the taxonomy. Claim grade: measured against Braid Taxonomy. Falsifier: a taxonomy row permitting nonzero $h_a$ in the coincident-midpoint member. |
| CMP-10 | Medium | 3, 20–29 | The opening gave a cold reader no clue what the braid or the action increment is, and $h_{\mathbb{A}\mathbb{A}\mathbb{A}}$ was not related to the $h_{\mathrm{act}}$ of the braid chapters or to the Effective Lagrangian's conditional integer-action statement. Added the clues, the name correspondence, and the action-side boundary. Claim grade: inferred exposition repair. Falsifier: a reader test showing the opening still requires the linked chapters to be understood. |
| CMP-11 | Medium | 123–138, 218, 232 | The candidate floor was written as an infimum, but a finite scanned family has a minimum, and a finite set of nonzero increments always has a positive minimum, so gate 3 was nearly vacuous. Added the finite-family statement and the stability-under-enlargement requirement to the prose and gate 3. Claim grade: derived. Falsifier: none for the arithmetic; for the repaired gate, a floor stable under enlargement that the cluster residual still rejects would show the two conditions are independent, which the chapter already requires. |
| CMP-12 | Medium | 100–121, 192–204 | $K_{\mu}$, $E_{\mathrm{wake}}^{(\eta)}$, the mechanical entries, $\eta$, $\epsilon_c$, and $\nu_J$ were never named in words. Named each on the Master Equation's conventions. Claim grade: measured against the owner's definitions. Falsifier: an owner definition differing from the stated one. |

Optional improvements not applied, to keep the thirteen viewer identities intact: terminal punctuation inside three displays (`corpus-equation-07210d95933edafb`, `corpus-equation-1afa3c3d67c9e6b8`, `corpus-equation-796980e1e5662ff5`), the vector norm bars around a scalar multiplier in `corpus-equation-e1bb97ae077631e6`, the six TeX thin-space commands `\ ` that end lines inside `corpus-equation-f12757a4496ccae3` and register as trailing whitespace, the `\text{arch}` versus canonical `\mathrm{arch}` subscript spelling that the chapter shares with the Master Equation and AGENTS.md, and the legacy `layer` vocabulary in table rows that the terminology instrument does not flag.

## Independent mathematical checks and boundaries

For CMP-02, the per-hit acceleration $\kappa|q_tq_r|W^{\mathrm{acc}}/r^2$ with $[\kappa]=L^3T^{-2}Q^{-2}$ and $[q]=Q$ from the Master Equation parameter table gives $[\kappa\epsilon^2]=L^3T^{-2}$. The acceleration moment has units $L^2T^{-2}$, its time integral $L^2T^{-1}$, and $[\kappa\epsilon^2/c_f]=L^3T^{-2}\cdot L^{-1}T=L^2T^{-1}$, so the specific increment is a pure number times $\kappa\epsilon^2/c_f$. Multiplying by $\mu_{\text{arch}}$ gives $ML^2T^{-1}$, an action, and $2\pi$ is dimensionless, so $h_{\mathbb{A}\mathbb{A}\mathbb{A}}$ has action units. The observer value of $\mu_{\text{arch}}\kappa\epsilon^2/c_f$ is a separate calibration; the check establishes only that the run cannot supply it.

For CMP-04, with $\mathbf X_C=\tfrac16\sum_i\mathbf X_i$ the derivative of $\sum_i(\mathbf X_i-\mathbf X_C)\times\mathbf V_i$ is $\sum_i(\mathbf V_i-\mathbf V_C)\times\mathbf V_i+\sum_i(\mathbf X_i-\mathbf X_C)\times\mathbf A_i=-\mathbf V_C\times\sum_i\mathbf V_i+\sum_a\boldsymbol{\tau}^{(A)}_a$, and $\sum_i\mathbf V_i=6\mathbf V_C$ makes the first term $-6\,\mathbf V_C\times\mathbf V_C=0$. The identity holds for the sum over the three binaries; a single binary's moment about the shared centroid is not by itself that binary's angular-momentum rate. Equal weights are the only choice consistent with a universal bookkeeping constant and massless architrinos.

For CMP-06, the Master Equation's fold analysis states the generic $\Delta N=\pm2$, $\Delta D=0$ law on the fold stratum and withholds certification from cusp and coincidence strata. The parity condition therefore restricts accepted transitions to fold-certified root changes; it does not prove that only such transitions occur. Braid Mathematics' hinge-click hypothesis proposes single same-transmitter roots opening or closing at the field-speed crossing, born on the coincidence stratum; a transaction whose net self-root change is odd is excluded from $\mathcal{T}_{\mathrm{acc}}$ by this chapter, and that tension is recorded as CMP-O4 rather than resolved here.

For CMP-11, any finite set of nonzero real numbers has a positive minimum, so $0<\min|\Delta I_{\mathrm{ME}}|$ over a scanned family carries no information beyond the absence of an exact zero. The infimum over the full accepted class can be zero only along an infinite sequence, which a finite scan detects only as a decreasing trend under enlargement and refinement.

For CMP-01 and CMP-07, a Floquet multiplier is an eigenvalue of the monodromy of the variational equation along a periodic solution; without a solution the variational equation has no base trajectory. A positive gap places every non-symmetry multiplier inside the unit circle, which is local linear attraction and says nothing about basin size. These are definitional checks; no return map was computed and no braid was evolved.

All checks are symbolic. No new numerical instantiation was written, and the one numerical convention introduced, $N_*$, is stated with $c_f=1$. Nothing here evolves a braid, certifies a branch, closes the delayed-action variation, or supplies the $\mu_{\text{arch}}$ calibration.

## Validation record

Claim grade: measured. The validation instruments and limits are recorded here; their falsifier is a rerun on these same bytes that fails the stated check.

- Known-case-first Node audit instrument (`cmp-audit.mjs`, session scratch, not committed) ran its `known` mode before any target run and printed its pass: on a fixture it returned two displays with one valid and one missing `View →` paragraph while ignoring a fenced decoy display, two inline TeX expressions while excluding an inline-code `$fake$`, two links while excluding a fenced bad link, two headings, the repository slug for a punctuated heading and for the math-bearing heading `The $h$ and $\hbar$ Convention` (`the-h-and-hbar-convention`, matching `normalizeMarkdownKey` in `scripts/validate-content.mjs`), KaTeX acceptance of two valid expressions and rejection of an unknown command and an unbalanced brace under `throwOnError: true` and `strict: 'error'` (katex 0.16.47 from `node_modules`), rejection of an unpaired dollar delimiter, trailing whitespace at the known line, three final-newline states classified, fragment resolution on a temporary target classified good and bad, and a preservation control that detected a changed display body and rejected heading loss and viewer-identity reorder. An earlier version of the slugger stripped TeX from headings and reported the chapter's `#the-h-and-hbar-convention` fragment as broken; it was corrected to the repository rule and the known case extended before the chapter was reaudited.
- Hash-verified baseline by that instrument: 249 lines, 13 displays each followed by its `View →` paragraph, 87 inline and 100 total TeX expressions all rendering, 24 links all local and all resolving including fragments, 11 headings, trailing whitespace on lines 50–55 (the TeX `\ ` commands inside the accepted-transition display, retained), single final newline.
- Repaired chapter by the same instrument: 251 lines, 13 displays each with its `View →` paragraph, 149 inline and 162 total TeX expressions all rendering under strict KaTeX, 33 links all local and all resolving including the nine new fragments and files, 11 headings, the same six baseline `\ ` lines, single final newline. Preservation comparison against the `git show` baseline: all 11 headings retained in order, all 13 viewer identities retained in order, zero changed display bodies. This is syntax, path, and preservation evidence, not visual-layout or physical acceptance.
- Changed viewer identities: none. No display was added, removed, or edited.
- `node scripts/validate-equation-mapping-links.mjs`: exit 0; 23 registered equation links resolve.
- `node scripts/check-braid-taxonomy-terminology.mjs`: exit 0; 359 migrated-scope files scanned, no terminology stragglers. This is the instrument's declared scan, not a universal vocabulary claim.
- Pre-receipt `node scripts/validate-content.mjs --check --strict`: exit 0; 391 scene files, 199 content Markdown files, 1739 repository Markdown files; 0 errors, 0 warnings, 30 notes. The gate checks the shared-tree snapshot and is not a scientific acceptance test.
- `node scripts/build-equation-mapping-corpus.mjs --check`: exit 1; 199 Markdown files, 4685 displays, 23 promoted equations, 30478 symbol definitions; the sole reported defect was stale `content/generated/equation-mapping/corpus-equations.json`. The chapter's new inline symbol prose is a fingerprinted source of that registry, so this drift is the expected consequence of the edit. The exact deferred command is `node scripts/build-equation-mapping-corpus.mjs --write`, followed by its `--check`, only under separate regeneration or publication authority; this receipt does not authorize that write and no generated target was written.
- Receipt audit by the same instrument, rerun after its last edit: 86 lines, no displays, 74 inline TeX expressions all rendering under strict KaTeX, 16 links all local and all resolving, 7 headings, no trailing whitespace, single final newline. A first draft of this receipt carried one mistyped character in the baseline hash; a shell comparison of the receipt's hash lines against `git show` and `sha256sum` caught it before the receipt was finished, and the corrected line now matches both.
- Post-receipt `node scripts/validate-content.mjs --check --strict`: exit 0; 391 scene files, 199 content Markdown files, 1741 repository Markdown files; 0 errors, 0 warnings, 30 notes. The repository count rose by two between the runs; this receipt accounts for one, and the other Markdown file was added concurrently by another agent in the shared tree.
- Final scoped `git --no-optional-locks diff --check -- content/markdown/aaa/validation/simulations/coincident-midpoint-orthogonal-axis-action-increment-protocol.md reference/priorities/aaa-corpus-rewrite/evidence/crw-005-coincident-midpoint-orthogonal-axis-action-increment-protocol-review-2026-09-13.md`: exit 0. Scoped status showed the chapter modified and unstaged and the receipt untracked; scoped `git diff --cached --name-only` returned no path, so the concurrent staging process had not staged this chapter at the time of measurement. The final chapter hash still matched the value above. The coordinator must use the completed working-tree receipt.

## Unresolved obligations and handoff

- CMP-O1: No retained coincident-midpoint orthogonal-axis braid endpoint exists; the owning chapter states that retention, same-record closure, and evolved convergence remain open. Until a branch certificate with return-map and acceleration-residual closure exists, every gate after eligibility is unreachable and no packet can promote `candidate_action_increment`. A retained-branch record would open the protocol; its absence leaves it a specification.
- CMP-O2: The conservation pullback rests on the candidate action-level charges whose complete delayed-action variation, boundary terms, and wake-history Noether identity remain open in Master Equation and Effective Lagrangian. Until that closes, $\mathcal{R}_E$, $\mathcal{R}_P$, and $\mathcal{R}_J$ test a candidate ledger's self-consistency, and the increment stays a diagnostic as the chapter states.
- CMP-O3: An independent calibration of $\mu_{\text{arch}}\kappa\epsilon^2/c_f$ in observer action units, through the mass map or the SI base-unit map, is required before $\delta_h$ is a test rather than a fit. The run's derived output is the pure number $N_*$; a benchmark match asserted without that calibration is `input-hbar-contamination`.
- CMP-O4: The even-parity condition on the active self-root count admits only fold-certified root changes, while Braid Mathematics' hinge-click hypothesis proposes single same-transmitter roots born on the coincidence locus. Either a certified regularized normal form for coincidence-stratum root birth extends the accepted class, or such transactions remain outside $\mathcal{T}_{\mathrm{acc}}$; the chapter now states which side it currently takes, and the reconciliation belongs to the Master Equation and Braid Mathematics owners.
- CMP-O5: Packet schema entries that the chapter names but does not define, the action-level $g$ and $u$ columns of the root ledger, the `history_horizon` and `memory_depth` conventions, and the reference point for the wake momentum and angular-momentum entries, must be fixed by the first implementing packet on the Master Equation's conventions; the chapter does not supply them.

The bounded implementation stops after this chapter and receipt. No next document is started. The coordinator can verify the final chapter hash, integrate CMP-01–CMP-12 as bounded dispositions into its shared records, and retain CMP-O1–CMP-O5 with the existing subject owners. No physical acceptance, EOM solver acceptance, theory closure, or publication is claimed.
