# CRW-005 — Academic Style Guide review

## Scope and disposition

Full review of [Academic Style Guide](../../../../content/markdown/aaa/archie/academic-style-guide.md), all 518 baseline lines. This is a controlled reference: the worker initially had report-only authority. The coordinator accepted one concrete factual conflict with the actual equation-viewer implementation and authorized its exact correction before the source was edited. No style policy, symbol-definition requirement, edition, or normative sentence was changed.

Baseline SHA-256: `f7e80e2b9436568f80503f4d86e1fca3b7f377e8d1bb0e30e372785250076b79`.

Reviewed chapter SHA-256: `d2a048e3902023dd4e56dbff7c07a657eafffc1cd2b5f9b37da2e2a5d0f7c8a5`.

Scoped `git --no-optional-locks status` showed no source edits before work. Only the source and this receipt are owned; shared records belong to the coordinator.

## Finding and accepted correction

**AS-01 — Medium severity, factual capability overstatement.** The paragraph beginning “Symbols are a solved case” said the equation viewer holds each symbol's full definition. The live generator can instead emit inferred shared-notation guidance, including a generic context-specific-quantity fallback. An actual generated record and its rendering consumer establish that this is reachable behavior, not just unused code.

The accepted first two sentences now read:

> **Symbol reference support.** Corpus prose carries `View →` links into the equation viewer, which provides source-context definitions or explicitly inferred notation guidance.

The final sentence governing symbol naming and repetition remains byte-for-byte unchanged. This correction describes current functionality; it neither relaxes nor expands the canonical style policy.

No further concrete policy or factual conflict was established by the full guide read. The scope/audience distinctions, evidence independence, claim grades, historical preservation, local explanation, current terminology, and source-policy delegation are preserved.

## Implementation and generated-record evidence

Direct reads establish the following chain:

1. [Generator](../../../../scripts/build-equation-mapping-corpus.mjs), `createSymbolDefinition` around lines 319–329, first calls `findContextDefinition`. When that returns no definition, it uses a shared base-symbol description or the generic phrase “a context-specific mathematical quantity or operator.” The generated text explicitly calls its role inferred and tells the reader to consult the source excerpt. The output is tagged `definitionSource: shared-context`.
2. The same generator builds each symbol's definition and source tag into the output record around lines 615–625.
3. The existing generated `content/generated/equation-mapping/corpus-equations.json` includes record `corpus-equation-ef6d57164647837d`, from `content/markdown/aaa/archie/ideal-braid-guide.md`, heading “What the App Shows.” Its hat-n symbol has the generic fallback and the explicit inferred-role sentence. This is a measured property of that generated record, not a claim that the current source has no definition anywhere.
4. [Corpus loader](../../../../src/apps/equation-mapping/EquationMappingCorpusLoader.js), lines 14–23, validates that symbols and nonempty definition strings exist. It does not certify semantic completeness.
5. [Runtime](../../../../src/apps/equation-mapping/EquationMappingRuntime.js), line 1283, binds `symbol.definition` to the tooltip; around lines 1375–1387 it displays the same definition and distinguishes “local source context” from “shared corpus notation.” [Tooltip](../../../../src/apps/equation-mapping/EquationMappingSymbolTooltip.js), lines 23 and 69, renders the supplied definition text.

A small record selector was tested first against a known synthetic shared-context generic record, then used on the generated registry to identify the concrete example. It was not used to count supposedly missing definitions across the corpus. The generator, registry, and renderer were not modified or regenerated.

The evidence chain is sufficient to refute a universal full-definition capability claim. It does not diagnose every inferred description as wrong, nor prove that the chapter containing the example lacks an adequate local explanation. Current generator code and existing generated bytes need not be perfectly fresh with one another for this narrower claim: the current generator admits the fallback and the consumer displays it.

## Mathematical and source review

The guide's illustrative function f(x)=x−1 has the single root x=1 on [0,2]; solving the linear equation gives uniqueness, and derivative one gives simplicity. This is elementary independent algebra, not a simulation result. The c_f=1 worked-example rule agrees with the repository's normalization requirement. Literal TeX syntax examples are preserved as code examples, not mistaken for equations.

The guide routes source selection and AI-assistance disclosure to About Architrino. Those policy links remain unchanged. No external factual claim requiring a fresh scientific-source search was introduced by the correction; the disputed capability is settled by its local implementation and generated consumer record.

## Focused checks and limitations

By `node .tmp/crw-005-academic-review/check.mjs`, following known-case math extraction, fenced-code exclusion, link extraction, and valid/invalid strict-KaTeX cases, all 22 TeX spans render. There are no display equations or viewer links in this guide. All five local link target paths and every original heading remain; path existence does not validate fragment semantics. Scoped `git diff --check` passes.

No browser interaction, corpus-wide definition completeness audit, generated freshness check, generator write, broad repository validator, neighboring edit, shared-record write, or Git mutation occurred. No policy edition increment is warranted because the accepted change is a factual capability correction rather than a substantive standard change.

## Remaining obligations and falsifier

There is no remaining chapter-specific repair obligation from this bounded review. A future guarantee of complete symbol definitions would need semantic coverage evidence beyond nonempty strings, a generated record set satisfying that guarantee, and a matching consumer. Such evidence could justify a stronger capability description. A working viewer, inferred fallback, or same-generator replay alone would not.

Coordinator review of the exact final source and shared integration remains separate.
