# CRW-005 — $A_0$ Branch Certificate Protocol bounded review — 2026-09-13

## Scope and disposition

Board rank 5: [$A_0$ Branch Certificate Protocol](../../../../content/markdown/aaa/validation/simulations/a0-branch-certificate-protocol.md). The complete chapter received a bounded review and safe repairs for ABC-01–ABC-14. These are three High and eleven Medium findings. High denotes a defect that can change a mathematical or evidentiary inference; Medium denotes a consequential definition, scope, attribution, or exposition defect. Completion is document-level disposition, not physical acceptance, EOM solver acceptance, theory closure, or publication.

Only the assigned chapter and this receipt were written by this worker. Shared CRW-005 status, priorities, work log, and queue remain the coordinator's responsibility. No supporting agent was launched; the review is editor self-review. Agent agreement would not be independent mathematical evidence in any case; the separate algebraic, dimensional, trace, and history-segment arguments below supply the bounded mathematical references, each with a known case run before its target.

Claim grade: measured. Initial scoped `git --no-optional-locks status --short` on the chapter was empty at HEAD `06fc05899fa972f5b43bf5e1d58e16e6b8f9ac95`. The chapter's initial SHA-256 from `sha256sum` matched the coordinator-measured value and the bytes extracted by `git show 06fc05899fa972f5b43bf5e1d58e16e6b8f9ac95:content/markdown/aaa/validation/simulations/a0-branch-certificate-protocol.md`, 265 lines:

    b01943b8d9ff00f94c1d717b777994d22cdba78e2df1f334da0f2a4f87fcd59e

Final chapter SHA-256 from `sha256sum`, 270 lines:

    f96a809a93cf0b3245e06048c8e52802af0c88539ef56614f445a15d953925d5

Falsifier: different chapter bytes at that path invalidate this byte-specific handoff until the difference is reviewed. No staging, commit, push, publication, reset, stash, generator `--write`, or worktree operation was performed.

## Authority and source support

The live AGENTS.md, generated startup router, corpus-reviewer and integrator-reviewer procedures, theory orientation, operator explanation standard, and the academic, mathematics-style, mathematics-terminology, terminology-usage, comparative-glossary, and About Architrino authorities governed the review. The explicit assignment supplied repair authority and restricted writes to two paths. The unified CRW-005 board lists the chapter at rank 5; this worker does not update or infer new shared board counts.

Nearby owners inspected as evidence and not edited: the [Master Equation](../../../../content/markdown/aaa/dynamics/master-equation.md) sections on the transversality floor, the branch-chart closure object, the separator taxonomy, and the auxiliary dual-mollified regulator; the repaired [Binary Dynamics](../../../../content/markdown/aaa/dynamics/binary-dynamics.md) sections on the Hessian bridge and the two-body closure packet; [Particle Masses](../../../../content/markdown/aaa/assemblies/particle-masses.md#reference-attractor-gate) after PL-13; [Energy](../../../../content/markdown/aaa/dynamics/energy.md) on shielding and inertial response; [Zero-Axial-Offset Three-Binary Dynamics and Interpretation](../../../../content/markdown/aaa/noether-braid/zero-axial-offset-three-binary-dynamics-and-interpretation.md) and [Spatial (3D) Braid Assemblies](../../../../content/markdown/aaa/noether-braid/3d-braid-assemblies.md) for the configuration definition and persistent indices; the companion [$A_0$ Tier 0 Result Interpretation](../../../../content/markdown/aaa/validation/simulations/a0-tier0-result-interpretation.md), read only because another worker holds it; [Simulation Run Protocols](../../../../content/markdown/aaa/validation/simulations/run-protocols.md); the repaired [Convergence Tests](../../../../content/markdown/aaa/validation/simulations/convergence-tests.md) and its [receipt](crw-005-convergence-tests-review-2026-09-13.md); [Well-posedness and Regularization](../../../../content/markdown/aaa/validation/simulations/action-energy/well-posedness-and-regularization.md); the [Coincident-Midpoint Orthogonal-Axis Action-Increment Protocol](../../../../content/markdown/aaa/validation/simulations/coincident-midpoint-orthogonal-axis-action-increment-protocol.md), which owns the definition of $\Delta_{\mathbf{k}}$; [Action Model](../../../../content/markdown/aaa/validation/simulations/action-energy/action-model.md) for the receiver label $o'$; and the [Parameter Ledger receipt](crw-005-parameter-ledger-review-2026-09-13.md), whose PL-13 disposition (retained dynamics first, $A_0$ one candidate, chart rejection domain-specific) this chapter must match. Where the chapter and these owners disagreed, the owner's convention was applied and the disagreement recorded as a finding.

No external source was added and no Sources section was needed: every repaired statement is supported by an in-corpus owner or by the explicit reasoning recorded below. The imported apparatus the chapter names, Floquet multipliers and the monodromy operator, is now explained in place at the level the argument needs, as the academic style guide requires; the About Architrino policy does not require a citation for an inline explanation of a mature method whose formulation is not in dispute. Every such object remains at effective or comparison grade; no standard-physics law enters as a substrate premise, the chapter speaks of acceleration throughout, and its one new numerical statement is the normalized-units declaration $c_f=1$, with symbolic $c_f$ retained in the equations.

Pre-edit `rg -l -F` for the exact chapter path under `content/`, `scripts/`, `tests/`, and `reference/` found thirteen consumers in five classes: scene and textbook graph (`content/graph/textbook_toc.json`, `content/graph/scene_graph.json`, `content/scenes/validation/sim_a0_branch_certificate_protocol.json`), Markdown index (`content/markdown/markdown_index.json`), generated registries (`content/generated/equation-mapping/corpus-equations.json`, `content/generated/reference/reference-surface.v1.json`, `content/generated/source-index/local-full-corpus-snapshot.v1.json`), authored priority and operator references (`corpus-review-status.md`, the Particle Masses receipt, `aaa-work-threads/analysis/notation.md`, `source-mining/analysis/source-mining-history.md`, `app-simulation/work-log.md`, `app-simulation/analysis/simulations.md`), and no file under `scripts/` or `tests/`. A basename search additionally found five sibling corpus chapters linking the file (`parameter-ledger.md`, `simulations/README.md`, `a0-tier0-result-interpretation.md`, `run-protocols.md`, `particle-masses.md`), the generated textbook reading copies, and the conversion ledger. This bounds the search to those directories and patterns; it does not establish absence of every byte-sensitive consumer. All consumers remained read-only and every heading and original link target is retained.

Named artifacts: the chapter names no solver, script, test, fixture, or file path. Its symbols $\mathfrak{B}(\Gamma,\mathcal{S};h,\eta,\epsilon_c)$, $\Delta_{\mathbf{k}}$, $\zeta(A_0)$, $\mathcal{L}_{\text{aniso}}$, $\mathcal{M}_{\text{sea}}^{ab}$, $E_0$, and $R_{\mathrm{df}}$ were each located in their corpus owner by `rg -n -F` before the chapter was made to cite that owner; the basis terms $\mathbf A_{a,B}$, coefficients $\alpha_B$, and the label $\mathcal{R}_{\mathrm{1per}}$ have no definition anywhere else in `content/` or `reference/priorities/` by the same search, which is why ABC-02 defines them in place and ABC-O2 remains open.

## Findings and repairs

Baseline lines refer to the hash-verified original chapter, not current line positions. Each disposition below is implemented; an open scientific obligation is not counted as a demonstrated physical failure.

| ID | Severity | Baseline lines | Evidence, repair, grade, and falsifier |
| --- | --- | --- | --- |
| ABC-01 | High | 29–30, 164–178, 188, 265 | The Tier 1 check list went from direct evolution to a monodromy estimate and a positive Floquet gap without requiring that the evolved history satisfies the acceleration law and returns to itself on the same retained cycle, and the pass condition treated a numerical gap as stability. AGENTS.md forbids linearizing about a configuration that is not an equilibrium; the repaired Binary Dynamics packet states that a matrix evaluated at an approximate orbit is a diagnostic until existence and error bounds exist and that the full history-space operator must carry the bound; Particle Masses after PL-13 requires the full acceleration law on the same retained cycle before any Floquet analysis. Added check 5 (balance and return within the declared $\mathcal{R}_{\text{state}}$ and $\mathcal{R}_{\text{root}}$ tolerances, before linearization), stated in check 1 that the compared history segment must be evolved rather than prescribed, added the diagnostic-versus-certificate sentence after the list, extended the Tier 1 pass sentence, and added the balance entry to the Stability row. Claim grade: derived for the referent argument and the segment-length condition; measured against the named owners. Falsifier: a stability theorem for delayed dynamics under which a spectrum computed at a non-solution history controls nearby true solutions, or a run-protocol definition of the return map that compares against the prescribed segment by design. |
| ABC-02 | High | 190–192, 209–243 | The one-period residual compared the Master Equation acceleration on the corrected carrier against a fitted scalar combination of undefined basis terms $\mathbf A_{a,B}$, so as displayed it tests whether the declared basis represents that acceleration and cannot detect that the carrier fails the acceleration law: the carrier's kinematic acceleration never enters. Well-posedness and Regularization already requires the acceleration-balance residual for the period before monodromy. Named every symbol in the residual ($a$, $\mathbf A^{\mathrm{ME}}_a$, $\mathbf A_{a,B}$, $\alpha_B$, the norm, $\varepsilon_0$), stated what the residual establishes, and required the balance residual (kinematic acceleration against $\mathbf A^{\mathrm{ME}}_a$ on the same window within its declared tolerance) before monodromy, citing that owner. Display unchanged. Claim grade: derived (with exact class contributions and unit coefficients the numerator vanishes identically, so the residual carries no balance information); measured for the owner's requirement. Falsifier: a corpus definition of $\mathbf A_{a,B}$ under which the displayed residual is equivalent to the balance residual. |
| ABC-03 | High | 192, 194–207, 243 | The corrected carrier's correction $\mathbf D_\ell(T)$ carried only the binary index, so both members of a binary received the same displacement; the separation $\mathbf X_{+,\ell}-\mathbf X_{-,\ell}$ and hence the relative orbit stayed circular, and a no-go inside that class would not be a non-circular-carrier no-go. The inline symbol $\mathbf d_\ell$ also disagreed with the displayed $\mathbf D_\ell$. Changed the correction index to $\mathbf D_{a,\ell}$ in the display, the inline references, and the declaration sentence; named $T_0$, $\mathbf X^{(0)}_{a,\ell}$, and the zero-mean condition in words; stated why a member-independent correction cannot serve. Viewer identity `corpus-equation-ed6d2de7bf72ebb0` changed. Claim grade: derived (a common additive displacement cancels in the difference), with a numerical witness below. Falsifier: a corpus declaration that the corrected-carrier class is intended as center wobble only, or that $\ell$ already enumerates individual architrinos. |
| ABC-04 | Medium | 63–76, 194 | The transversality-floor display used $T_0$ for the emission time while line 194 used $T_0$ as the window origin, the mathematics style guide reserves $T_0$ for an epoch and $T_t$ for an emission time, and the terminology guide requires renaming a local collision rather than disclaiming it; the inline receiver factor was written dimensionless, $D_r=1-\mathbf V\cdot\hat{\mathbf r}/c_f$, in the same sentence as the canonical dimensional $W^{\mathrm{acc}}=c_f/\lvert D_t\rvert$; and no symbol in the display was named in words. Rewrote the display in the canonical event-role notation $(T_r;T_t)$, wrote $D_r=c_f-\mathbf V_{o'}(T_r)\cdot\hat{\mathbf r}_{o'j}(T_r;T_t)$, and added the prose naming every symbol and identifying the bracketed quantity as $\lvert D_t\rvert/c_f$. Viewer identity `corpus-equation-e80a6a39747b59e2` changed. Claim grade: measured against the two canon guides; derived for the dimension of $D_r$. Falsifier: canon admitting $T_0$ as an emission-time symbol, or a canonical dimensionless $D_r$. |
| ABC-05 | Medium | 22, 39–59 | The reduced-coordinate row wrote the period ratios as $T_1/T_2$ and $T_2/T_3$ with the glyph reserved for absolute time, while the companion chapter and the style guide use the $P$ family; the quotient-row symbols ($\delta_2$, $G_{\ell m}$, $\chi_N$, $H_\ell$, $\Phi_{\text{rel}}$, the removed gauges, $[\Lambda]$) and the ten residual components were never named in words in this chapter. Changed the ratios to $P_1/P_2$ and $P_2/P_3$, named each quotient-row symbol in the table cell, and added the component-by-component reading after the residual display with a link to the companion's residual semantics. Display unchanged. Claim grade: measured against the mathematics style guide and the companion. Falsifier: a canon row assigning $T_\ell$ to a cycle period. |
| ABC-06 | Medium | 3, 5 | The opening called $A_0$ "the $A_0$ reference attractor" and "the first neutral rest-branch mass-map candidate", contradicting the chapter's own acceptance boundary and Particle Masses after PL-13, and defined neither $A_0$, the braid configuration, nor the term branch certificate for an arriving reader. Wrote "reference-attractor candidate", defined $A_0$ as one proposed neutral rest branch of a Noether braid in the coincident-midpoint orthogonal-axis three-binary configuration with links to both owners, defined a branch certificate, made retained dynamics the first requirement with $A_0$ one candidate, and stated that the chart is not the unique route. Claim grade: measured against Particle Masses and the Parameter Ledger disposition. Falsifier: a live retained-history record establishing the attractor, or an owner stating that this chart is the unique first gate. |
| ABC-07 | Medium | 9, 11 | The master-equation branch-chart object was cited without a link, with the horizon symbol $H_{\mathrm{hist}}$ where the owner writes $h$ and without stating the map, and with $\Gamma$, $\mathcal{S}$, $\eta$, $\epsilon_c$, $W^{\mathrm{acc}}$, $D_r$, and $D_r/D_t$ unnamed; the downstream symbols $\zeta(A_0)$, $\mathcal{L}_{\text{aniso}}$, $\mathcal{M}_{\text{sea}}^{ab}$ were used before definition; and the refinement test read "changes", which every finite-precision quantity does. Linked the owner, stated the $H_{\mathrm{hist}}\leftrightarrow h$ map and its reason, named each symbol, clued and linked the three downstream quantities to their Energy sections, and made the test "changes by more than its declared tolerance". Claim grade: measured against the Master Equation and Energy owners; inferred for the tolerance repair. Falsifier: a definition of the consistency test elsewhere that intends any change, however small, to fail it. |
| ABC-08 | Medium | 156–160 | The near-zero self-root exclusion was attributed to the convention $H(0)=0$, which removes only the exact zero-delay endpoint; the positive-delay neighborhood below the threshold is the self-coincidence locus the Master Equation quarantines by a declared separation floor, as the numerical-recipe review NR-3 recorded. "Fold-layer" and "locked self-root keys" were unexplained, and "the current fold-layer row" was process-history phrasing. Separated the endpoint convention from the numerical threshold policy, kept the companion's conservative reading, defined a fold-layer diagnostic from the separator taxonomy, and wrote "entries". Claim grade: measured against the Master Equation regulator section and the companion. Falsifier: a Master Equation statement that $H(0)=0$ alone excludes a positive-delay neighborhood. |
| ABC-09 | Medium | 5, 120, 124–129 | A numerical protocol with declared thresholds carried no normalized-units declaration; "carrier chart", $R_\ell$, and $s_\ell$ were unnamed; and the speed ordering $s_1>c_f$, $s_2\approx c_f$, $s_3<c_f$ was listed as a bare requirement although Particle Masses grades it as the specialized hypothesis's declared constraint with speed above $c_f$ not establishing a self-hit. Added the $c_f=1$ declaration at first numerical use with symbolic $c_f$ retained, set $c_f=1$ in the required-inputs cell, defined the chart and both symbols, and attributed the ordering to the hypothesis with its self-hit caveat. Claim grade: measured against AGENTS.md's units rule and Particle Masses. Falsifier: an owner deriving the ordering rather than declaring it. |
| ABC-10 | Medium | 29–30, 145, 175, 177 | Floquet gap, Floquet multipliers, and monodromy were used throughout without explanation, although $\Delta_{\mathbf{k}}=1-\max_{i\notin G}\lvert\mu_i(\mathbf{k})\rvert$ has a corpus owner, and the state-dependent delay derivative term in check 8 was unexplained. Added the definition with its owner link and a plain reading of a multiplier, named $G$, stated that a positive gap is the attractor criterion only for a history that is already a solution, and explained the delay-derivative term and why omitting it changes the multipliers. Claim grade: measured against the action-increment protocol; derived for the omitted-term statement (the linearized operator of a state-dependent delay carries the root-time variation). Falsifier: a corpus definition of $\Delta_{\mathbf{k}}$ that differs from the cited one. |
| ABC-11 | Medium | 35, 252, 257 | The mass-comparison row divided by an undefined $E_0$; the Tier 2 naive constituent sum did not state that its reference norm is the sum of constituent norms, although Energy states that the signed sum vanishes for a neutral assembly and leaves $\zeta$ undefined; and $\alpha$ was unnamed. Defined $E_0$ as in the repaired Parameter Ledger, added the norm clause, and named the fine-structure constant. Claim grade: measured against Energy and the Parameter Ledger. Falsifier: an Energy definition of $\mathcal{L}_{\text{naive}}$ as a signed sum. |
| ABC-12 | Medium | 184 | The overdetermination clause offered $N_{\mathrm{eq}}>N_{\mathrm{coef}}$ or $R_{\mathrm{df}}>0$ as alternatives with both symbols undefined; the two are equivalent, as CT-11 already recorded for Convergence Tests. Wrote "equivalently", displayed $R_{\mathrm{df}}$ inline, and defined both counts as that chapter does. Claim grade: derived (for $N_{\mathrm{eq}}>0$ the sign of $R_{\mathrm{df}}$ is the sign of the difference). Falsifier: none possible for the equivalence; a different corpus definition of $R_{\mathrm{df}}$ would reopen the wording. |
| ABC-13 | Medium | 243, 265 | The no-go scope sentence could be read as promoting exhaustion of the declared search class into an attractor failure for every $A_0$ branch, contrary to Particle Masses and PL-13, which restrict a rejection to the tested chart and domain. Split the sentence and stated that even a class-wide negative applies to the tested class and domain. Claim grade: measured against Particle Masses. Falsifier: an owner proving that the declared search class exhausts all admissible $A_0$ candidates. |
| ABC-14 | Medium | 20, 63, 82, 84–116, 261 | Exposition defects: separators and the even-count rule were stated without the separator taxonomy, whose memory-boundary events carry odd jumps; "derived $h$" named Planck's constant without explaining it; the anisotropy displays left $\mathbf{C}_{A_0}$, the cycle average, $\mathbf{V}_{\text{cm}}$, and the trace normalization unexplained, and the subscript could be read as a mass-weighted center; the Noether sea environment symbols were unnamed; and the Tier 3 first-order equality was not connected to the recovery target it corresponds to. Added the taxonomy link and odd-jump attribution, explained $h$ as an observer-level closure target, named every anisotropy symbol with the centroid stated as equal-weight because architrinos carry no mass, stated the traceless property and its meaning, named the environment symbols, and added the inertial–gravitational equality clue. Displays unchanged. Claim grade: derived for the traceless property, with a numerical witness below; measured against the Master Equation taxonomy for the parity statement; inferred exposition repair otherwise. Falsifier: a nonzero trace of $\mathcal{A}_{\mathrm{gv}}^{ij}$ for any six-point configuration with nonzero second moment. |

## Independent mathematical checks and boundaries

For ABC-14, $h_{ij}Q^{ij}=h_{ij}D^{ij}/(h_{mn}D^{mn})=1$ and $h_{ij}h^{ij}=3$, so $h_{ij}\mathcal{A}_{\mathrm{gv}}^{ij}=1-1=0$ for every configuration with nonzero trace, and an isotropic second moment $D^{ij}\propto h^{ij}$ gives $\mathcal{A}_{\mathrm{gv}}^{ij}=0$. A Node calculation first returned the known answers, trace of the identity equal to $3$ and a zero tensor for the six points $\pm\mathbf e_x,\pm\mathbf e_y,\pm\mathbf e_z$, then returned trace $0.00$ with maximum entry $0.42$ for six pseudorandom points, and for the same six axis points stretched threefold along $x$ returned $\mathcal{A}^{xx}=0.4848$ and $\mathcal{A}^{yy}=-0.2424$, the hand values $18/22-1/3$ and $2/22-1/3$. This checks the definition's algebra, not any braid.

For ABC-03, $(\mathbf X^{(0)}_{+,\ell}+\mathbf D_\ell)-(\mathbf X^{(0)}_{-,\ell}+\mathbf D_\ell)=\mathbf X^{(0)}_{+,\ell}-\mathbf X^{(0)}_{-,\ell}$ identically, so a member-independent correction leaves the relative orbit of the binary unchanged; the same Node run confirmed that a common displacement $(0.3,-0.2,0.1)$ leaves the separation $(2,0,0)$ unchanged while opposite per-member displacements change it to $(2.6,-0.4,0.2)$. The corrected class with $\mathbf D_{a,\ell}$ contains the common-translation case as $\mathbf D_{+,\ell}=\mathbf D_{-,\ell}$, so no admissible correction was removed.

For ABC-02, if the basis terms were the exact self, partner, and inter-binary contributions of the Master Equation on the carrier, then with $\alpha_B=1$ the numerator of $\mathcal{R}_{\mathrm{1per}}$ is $\int_W\sum_a\|\mathbf A^{\mathrm{ME}}_a-\mathbf A^{\mathrm{ME}}_a\|^2dT=0$; the residual is therefore either trivial or a template-adequacy measure, and in neither reading does it contain $d^2\mathbf X^\star_{a,\ell}/dT^2$. A carrier can pass it while failing the acceleration law. This is a structural observation about the displayed formula, not a claim about any run.

For ABC-04, $1-\mathbf V_j(T_t)\cdot\hat{\mathbf r}_{o'j}/c_f=(c_f-\mathbf V_j\cdot\hat{\mathbf r}_{o'j})/c_f=D_t/c_f$, the dimensionless transmitter-side Jacobian $J^t$ of the terminology table, so the display is unchanged in value and only its time labels changed; the baseline inline $D_r$ equaled the canonical $D_r$ divided by $c_f$, which is numerically identical at $c_f=1$ but a different symbol.

For ABC-01, a return map on history space compares segments of length $H_{\mathrm{hist}}$; with the prescribed carrier occupying $[T_0-H_{\mathrm{hist}},T_0]$, the first pair of fully evolved segments one period apart is $[T_0,T_0+H_{\mathrm{hist}}]$ and $[T_0+P_{\mathbf{k}},T_0+H_{\mathrm{hist}}+P_{\mathbf{k}}]$, which requires evolution over $H_{\mathrm{hist}}+P_{\mathbf{k}}$. The referent argument is AGENTS.md's Evidence Independence rule applied to a periodic history: a linearized return operator is a derivative at a point of the map, and the map's fixed point is the periodic solution, so a spectrum computed at a history that is not a fixed point within tolerance is the derivative at an arbitrary point.

For ABC-12, with $N_{\mathrm{eq}}>0$, $R_{\mathrm{df}}=(N_{\mathrm{eq}}-N_{\mathrm{coef}})/N_{\mathrm{eq}}>0$ if and only if $N_{\mathrm{eq}}>N_{\mathrm{coef}}$.

Boundaries: no EOM solver run, evolved delayed history, root census, Floquet spectrum, or regulator limit was computed. The witnesses are exact identities and prescribed samples chosen to test definitions; they certify no simulation, threshold, or physical claim. The numerical constant $0.02$ in the one-period boundary and the near-zero delay threshold are declared protocol constants; the review preserved them and grades them as guessed conventions whose calibration is an open obligation below. The lens checks were bounded: Poincaré (return map and balance before stability, ABC-01, ABC-02), Tao (what a finite projection and an unbounded fit can establish, ABC-01, ABC-02, ABC-12), Noether (symmetry quotient and centroid bookkeeping, ABC-10, ABC-14), and Evidence Independence (no certificate step in the repaired chapter treats self-agreement as evidence; the balance residual compares kinematics against the acceleration law on the same record, which is a consistency test, not an independent reference, and the chapter now says so through the two-body packet link).

## Validation record

Claim grade: measured. The validation instruments and limits are recorded here; their falsifier is a rerun on these same bytes that fails the stated check. All commands ran in the Linux sandbox from `/sessions/awesome-cool-rubin/mnt/architrino`, the same files as the checkout; no Python was used for any check.

- Known-case-first Node text/TeX/link audit, using the repository's `parseCorpusDisplayEquations` for displays, a fence- and inline-code-aware extractor for inline TeX, and `katex` 0.16.47 from the repository's `node_modules` with `throwOnError: true` and `strict: 'error'`: before either target read, a fixture returned exactly one display and two inline expressions with zero display errors, rejected the one undefined command outside the fence, excluded a fenced malformed expression and a fenced link, detected the unpaired dollar at its known line, found the two fixture headings, resolved the good link, flagged the missing file, extracted the fixture's viewer identity, located trailing whitespace at its known line, and passed the single-final-newline check. A first draft of the instrument replaced display blocks with same-length spaces, which collapsed newlines and shifted later line numbers by two; the known case exposed this and it was corrected before the target run. A second fixture confirmed missing-fragment detection, a changed display body with unchanged identity count, a removed heading, a removed link, and a `View →` line that was not the sole content of its paragraph.
- Hash-verified baseline: 265 lines, 8 displays, 141 inline TeX expressions, 17 links, 13 headings, no trailing whitespace, single final newline. Repaired chapter: 270 lines, 8 displays, 228 inline TeX expressions, 29 links, all 29 local destinations and fragments resolving, 13 headings, no trailing whitespace, single final newline. All display and inline TeX in both versions passed strict KaTeX rendering. This is syntax and path evidence, not layout or physical acceptance.
- All 8 viewer identities remain in the original order; every `View →` line is the sole content of the paragraph immediately after its equation block; 6 display bodies are byte-identical and two changed deliberately: `corpus-equation-e80a6a39747b59e2` (transversality floor, event-role time labels, ABC-04) and `corpus-equation-ed6d2de7bf72ebb0` (corrected carrier, member index on the correction, ABC-03). All 13 original headings and all 17 original link targets are retained; twelve link occurrences were added, to `noether-braid/noether-braid.md`, `3d-braid-assemblies.md#coincident-midpoint-orthogonal-axis-three-binary-configuration`, `dynamics/master-equation.md` (bare, `#branch-chart-closure-object`, `#separator-taxonomy`), `dynamics/energy.md` (`#apparent-energy-and-shielding`, `#operational-definition-of-inertial-mass`), `a0-tier0-result-interpretation.md#residual-semantics`, `coincident-midpoint-orthogonal-axis-action-increment-protocol.md`, `assemblies/particle-masses.md#reference-attractor-gate`, `dynamics/binary-dynamics.md#two-body-closure-packet-theorem-target`, and `action-energy/well-posedness-and-regularization.md`. Every fragment was confirmed against the owner's headings by the audit's slug rule, which reproduces `scripts/archie-service/build-source-index.mjs`.
- Terminology scan of the repaired chapter by `rg -n -i` over the disallowed causal-delay variants, `force`, and the retired plain-language tag: no matches. `particle` and `mass` occur only in comparison or effective-level uses (particle label, particle benchmarks, mass map, mass extraction) present in the baseline.
- `node scripts/validate-equation-mapping-links.mjs`: exit 0; 23 registered equation links resolve from canonical corpus sources.
- `node scripts/validate-content.mjs --check --strict`, run after the chapter edit and before this receipt: exit 0; 391 scene files, 199 content Markdown files, 1737 repository Markdown files; 0 errors, 0 warnings, 30 notes. The gate checks the shared-tree snapshot and is not a scientific acceptance test. The post-receipt rerun is recorded at the end of this list.
- `node scripts/check-braid-taxonomy-terminology.mjs`: exit 0; 359 migrated-scope files scanned, no terminology stragglers found.
- `node scripts/build-equation-mapping-corpus.mjs --check`: exit 1; 199 Markdown files, 4685 displays, 23 promoted equations, 30481 symbol definitions; the sole reported defect was stale `content/generated/equation-mapping/corpus-equations.json`. The two changed display bodies and the added context in this chapter contribute to that expected drift, which the shared tree also carries from concurrent work; no attribution to a single edit is claimed, and no generated target was written. The exact deferred command is `node scripts/build-equation-mapping-corpus.mjs --write`, followed by its `--check`, only under separate regeneration or publication authority.
- Scoped `git --no-optional-locks diff --check -- content/markdown/aaa/validation/simulations/a0-branch-certificate-protocol.md`: exit 0. Scoped `git --no-optional-locks diff --numstat` on the chapter: 43 insertions, 38 deletions. Scoped status showed the chapter modified and unstaged; `git --no-optional-locks diff --cached --name-only` listed 43 paths staged by a concurrent process, none of them this chapter and none staged by this worker, including nine sibling CRW-005 receipts. HEAD remained `06fc05899fa972f5b43bf5e1d58e16e6b8f9ac95` and no `.git/index.lock` was present.
- Final two-file Node audit and post-receipt checks: recorded in the closing paragraph of this section after the receipt was written.

Post-receipt measurements: the same known-case-first audit ran on both files after the receipt was written. Chapter: 270 lines, 8 displays, 228 inline TeX expressions, 29 local links resolving, 13 headings, no trailing whitespace, single final newline, hash unchanged from the value above. Receipt: 0 displays, 157 inline TeX expressions passing strict KaTeX, 15 local links resolving, 7 headings, no trailing whitespace, single final newline; these counts were measured on the receipt before this sentence was completed, and a rerun after it reports the same values except the inline count, which this sentence does not change. `node scripts/validate-content.mjs --check --strict` rerun with the receipt present: exit 0; 391 scene files, 199 content Markdown files, 1740 repository Markdown files, the repository count having grown by concurrent workers' receipts; 0 errors, 0 warnings, 30 notes. Scoped `git --no-optional-locks diff --check` on both files: exit 0.

## Unresolved obligations and handoff

- ABC-O1: Declare the Tier 1 acceleration-balance and state-return tolerances as predeclared protocol constants and supply the existence-enclosure route, an a posteriori fixed-point argument on the declared history space with nonlinear error bounds and a validated return operator, that converts a numerical Floquet gap into a stability certificate. Owner: the two-body closure packet and this protocol. A candidate whose numerical gap is positive but whose balance residual exceeds its tolerance is not advanced; a proved enclosure at that history closes the obligation for that candidate.
- ABC-O2: Define the branch-native basis terms $\mathbf A_{a,B}$ and the coefficient rule for $\alpha_B$ in a corpus owner, and calibrate the $0.02$ threshold and the floor $\varepsilon_0$ against a known case, so that the one-period residual has a referent independent of the run that uses it. Until then the threshold is a guessed protocol constant; a known case that a correct basis fails at $0.02$, or a wrong basis passes, rejects the calibration.
- ABC-O3: Tie the near-zero self-root delay threshold to the Master Equation's separation floor and core scale with a regulator-limit statement. A positive-delay self-root branch found inside the excluded neighborhood under refinement rejects the policy's claim to be conservative for that chart.
- ABC-O4: Two structural items were left for a terminology or scene-graph decision rather than edited: the group-velocity anisotropy passage sits under the self-hit heading, and the chapter mixes `layer` and `binary` for the same persistent index in the way the companion's alias map tolerates. A heading split changes the scene and textbook graphs; a vocabulary alignment is a canon decision.
- ABC-O5: The Tier 2 and Tier 3 objects, the far-field extraction basis for $\mathcal{L}(\hat{\mathbf{R}})$, the isotropic projector $\Pi_0$, and the probe protocol for $\mathcal{M}_{\text{sea}}^{ab}$, are defined only by reference to Energy; a same-record extraction procedure remains an owner obligation shared with PL-O2. Prescribed geometry does not establish those objects.

The bounded implementation stops after this chapter and receipt. No next document is started. The coordinator can verify the final chapter hash, integrate ABC-01–ABC-14 as bounded dispositions into its shared records, and retain ABC-O1–ABC-O5 with the existing subject owners. No physical acceptance, EOM solver acceptance, theory closure, or publication is claimed.
