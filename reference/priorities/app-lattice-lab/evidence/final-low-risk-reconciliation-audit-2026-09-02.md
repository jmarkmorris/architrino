# Lattice Lab Final Low-Risk Reconciliation Audit

Date: 2026-09-02

## Scope and authority

This audit closes LAT-008, LAT-014, LAT-064, LAT-065, LAT-072, LAT-073, LAT-074, LAT-076, LAT-082, and LAT-097 from the existing canonical implementation and closes LAT-015, LAT-094, and LAT-100 by supersession. No app source, coordinate, polarity assignment, relationship identity, camera, control, calculation, certificate, or evidence-authority path changed in this pass.

The audit establishes implementation state, exact display inventories, state transitions, and bounded presentation behavior. It does not establish motion, stability, retention, conservation, energy, force, or any broader physical result.

## Closure matrix

| Row | Disposition | Basis |
| --- | --- | --- |
| LAT-008 | Verified | Random is the final gallery item; its reproducible exact-50/50 assignment, finite 87-row Ledger, case-only recalculation control, no-repeat state, and changed seed/fingerprint on recalculation are implemented and tested. |
| LAT-014 | Verified | Every repeat-capable case owns exactly two sites; FCC's additional 18 markers are continuation context outside ownership. |
| LAT-064 | Verified | The shared semantic-X transform is identical, but HCP alone requires a case-specific β>0 certificate-boundary sentence. Exact full-paragraph parity would therefore be mathematically false. |
| LAT-065 | Verified | FCC exposes two owned sites, 18 continuation sites, 24 relationships, a 12-edge owned-cell boundary, and an accessible ownership/context description with uniform marker and ordinary-edge styling. |
| LAT-072 | Verified | HCP uses its periodic certificate only at β=0; every β>0 state is fail-closed as not established and exposes no zero residual or calculation disclosure. |
| LAT-073 | Verified | The endpoint map is reconstructed from transformed HCP geometry and the live marker-diameter threshold; source selection, Ledger target, rendered aggregate, and selection cue remain separate linked records. |
| LAT-074 | Verified | Exact β=1 hides all source markers in both HCP views, renders every group in the purple aggregate material, and uses one purple non-polarity selection circle; β<1 restores polarity markers. |
| LAT-076 | Verified | At exact β=1, the highlight maps the 63 canonical repeat links to 19 distinct rendered group-pair links, suppresses the same 19 ordinary under-strokes, and has zero ordinary/highlight overlap. No circle, halo, radial stub, or invented direction is used. |
| LAT-082 | Verified | Exact 2.75d constants, complete before/after inventories, proportional orthographic framing, unchanged apparent diameter, and the rebuilt Random finite case are all covered. |
| LAT-097 | Verified | Every ordinary main and repeat marker measured 16.0000px; every ordinary edge used `#c6b6ff`, opacity `0.64`, and width `1`. Owned/context style did not diverge. |
| LAT-015 | Superseded | LAT-029's accepted coordinate is β with $s_x=1-0.99\beta$, endpoints 1 and 0.01. Replacing it with the older $\sqrt{1-\beta^2}$ proposal would undo accepted behavior and import an unwanted relativity association. |
| LAT-094 | Superseded | The accepted concise deformation panel contains no zero-result sentence for any case. Evidence outcomes remain in the Ledger; HCP alone adds its β>0 not-established explanation. Reintroducing a second outcome surface would undo that separation. |
| LAT-100 | Superseded | The accepted companion is `Unpolarized Repeat Pattern`, includes honest represented-endpoint nearest-neighbor relationships, and remains available for Random's conventional simple-cubic geometry. Tests deliberately reject the earlier title and edge-free/excluded-Random contract. |

Plainly: ten rows describe behavior the app already has and the focused checks can verify. Three rows describe older designs that conflict with the subsequently accepted app, so they are closed without reverting the current design.

## Deformation comparison and certificate boundary

All five deterministic cases consume the same one-time model-boundary map $s_x=1-0.99\beta$, with $s_x(0)=1$, $s_x(1)=0.01$, monotone decrease, and unchanged semantic Y and Z coordinates. BCC, FCC, Simple Cubic Alternating Planes, and Diamond have validated periodic certificates at all supported factors. HCP's current certificate is valid only at the undeformed factor 1 because its proof uses threefold rotational symmetry that the semantic-X deformation does not preserve. The correct copy structure is therefore shared transform/endpoints plus one HCP-only β>0 sentence; forcing exact whole-paragraph parity would erase a real evidence distinction.

Plainly: the slider changes geometry the same way in every case, but the proof coverage is not the same. HCP must say why its deformed result is not established while the other cases need no such warning.

The browser state matrix was:

| HCP β | X factor | Ledger authority | Main source markers | Repeat source markers | Selection cue |
| ---: | ---: | --- | ---: | ---: | --- |
| 0 | 1.000000 | certified periodic zero; magnitude/vector zero; calculation available | 116 | 20 | blue polarity circle for selected electrino |
| 0.5 | 0.505000 | periodic acceleration not established; no residual or disclosure | 116 | 20 | blue polarity circle for selected electrino |
| 1 | 0.010000 | periodic acceleration not established; no residual or disclosure | 0 | 0 | one purple aggregate circle |

## HCP endpoint identity and relationship partition

At the `1280 × 720` operator viewport, the main canvas height was `720px`. The exact `2.75d` framing gives orthographic half-height `3.723076923...`; a `16px` rendered marker therefore gives the live grouping threshold `0.165470086...` world units after the `+1e-9` guard. Reconstructing the 116 transformed HCP display sites at β=1 produced 30 groups with no singleton source-marker presentation. Group sizes were four groups of 2, six groups of 3, ten groups of 4, and ten groups of 5, accounting for all 116 source identities exactly once.

The main 509-edge set partitioned into 86 internal-group edges, 71 distinct external group-pair representatives, and 352 redundant external identities. The repeat view's 63 canonical links partitioned into 10 internal-group links, 19 distinct external group-pair representatives, and 34 redundant external identities. Its 20 source sites produced 10 purple rendered groups: eight collapsed groups and two singleton groups, all using the same purple endpoint material. Every group's full source IDs remain in renderer metadata; aggregation changes presentation, not source identity or polarity.

Plainly: the endpoint display does not create or delete architrinos. It bundles visually overlapping source markers and duplicate-looking links while preserving the exact source records underneath.

With `Highlight repeat cell` enabled at β=1, all 19 actual external group-pair representatives rendered as thick violet links, the corresponding 19 ordinary under-strokes were suppressed, and the ordinary/highlight overlap counter was zero. The single purple selection circle was unchanged. Visual inspection found no duplicate circle, halo, radial stub, detached line, or red/blue endpoint marker in either HCP view.

## Crop, marker, edge, ownership, and companion inventories

The independent display-crop verifier records these exact prechange/postchange inventories:

| Case | Sites at prior 2.5d | Sites at 2.75d | Edges at prior 2.5d | Edges at 2.75d |
| --- | ---: | ---: | ---: | ---: |
| Simple Cubic | 56 | 88 | 120 | 192 |
| Body-Centered Cubic | 90 | 108 | 251 | 311 |
| Face-Centered Cubic | 98 | 118 | 411 | 515 |
| Hexagonal Close-Packed | 96 | 116 | 407 | 509 |
| Simple Cubic Alternating Planes | 56 | 88 | 120 | 192 |
| Diamond Cubic | 38 | 56 | 55 | 79 |
| Simple Cubic Random 50/50 | 56 | 88 | 120 | 192 |

The site/edge inventory comparison is against the prior `2.5d` membership radius. Framing uses the separately established `3.25d` camera-reference radius and `4.4` half-height: the current half-height is `4.4 × 2.75 / 3.25 = 3.723076923...`, so the envelope ratio remains exactly `3.25/4.4`. The live main canvas reported a `531.8182px` display-crop diameter for every case. This is direct numeric parity from the renderer state, not a screenshot estimate. Deterministic certificate validation remains independent of the display crop.

Plainly: the world-space crop changed, and the camera scale changed by the same ratio, so the dotted sphere stayed the same size on screen. That viewing change supplies no calculation evidence.

Fresh all-case browser measurements found the following repeat and unpolarized inventories:

| Case | Repeat owned/context | Repeat relationships | Unpolarized sites/frame/relationships |
| --- | ---: | ---: | ---: |
| Simple Cubic | 2 / 10 | 12 | 8 / 12 / 0 |
| Body-Centered Cubic | 2 / 14 | 16 | 9 / 12 / 8 |
| Face-Centered Cubic | 2 / 18 | 24 | 14 / 12 / 36 |
| Hexagonal Close-Packed | 2 / 18 | 24 | 17 / 18 / 45 |
| Simple Cubic Alternating Planes | 2 / 10 | 12 | 8 / 12 / 0 |
| Diamond Cubic | 2 / 6 | 8 | 18 / 12 / 16 |
| Simple Cubic Random 50/50 | not applicable | not applicable | 8 / 12 / 12 |

Every main and repeat ordinary marker measured `16.0000px`. Every ordinary edge reported color `#c6b6ff`, opacity `0.64`, and width `1`. FCC additionally reported its 12-edge owned-cell boundary and an accessible label naming exactly 2 owned markers and 18 equally styled continuation markers. The boundary and structure carry ownership; marker or ordinary-edge styling does not.

The unpolarized conventional-cell inventories and nearest-neighbor networks are independently checked against their coordinates. The current title and scope are intentionally `Unpolarized Repeat Pattern`; neutral purple is presentation-only and the relationship lines report represented-endpoint geometry, not calculation, binding, or physical interaction.

## Random finite configuration

Random is the final selector item after Diamond. Its canonical finite configuration contains 88 sites with exactly 44 electrinos and 44 positrinos. The finite Ledger includes all 87 other sites for the selected target and derives its residual from those included rows rather than a stale aggregate. The repeat panel is not applicable. The case-only button is exposed as `Recalculate random configuration`.

In the fresh browser pass, recalculation advanced the recorded seed from `20260802` to `20260803` and changed the assignment fingerprint from `21490819` to `a7b550cd`. The selected-target polarity and finite residual also changed while the scope remained `finite-nonperiodic`.

Plainly: the button generated a genuinely different balanced assignment and recalculated that finite configuration. Equal red/blue counts did not predetermine the nonzero result.

## Validation receipt

- `node --test tests/lattice-lab-ledger-presentation.test.js tests/lattice-lab-runtime.test.js tests/lattice-lab-bcc-neighbor-graph.test.js tests/lattice-lab-random-finite.test.js tests/lattice-lab-periodic-gallery.test.js` passed `76/76`.
- Focused tests cover exact crop inventories and framing, unpolarized conventional-cell geometry, ordinary marker/edge parity, FCC ownership/context counts, HCP certificate fail-closed behavior, endpoint grouping/relationship partition logic, canonical highlight mapping, Random assignment reproducibility, exact 50/50 counts, complete finite rows, and genuinely new recalculation.
- Fresh browser checks covered all seven cases, β values `0`, `0.5`, and `1`, HCP endpoint highlight off/on, FCC ownership/accessibility, all-case marker/edge/companion inventories, Random recalculation, visual layout, and rendered state associations.
- The fresh browser warning/error log was empty.
- `git diff --check` passed after queue and work-log reconciliation.
