# Mirror Close-Approach Causal-Root Boundary

## Status

- Kind: `priority`
- Queue item: `MEC-007`
- Priority object: `mirror_close_approach_causal_root_boundary`
- Claim level: `priority-only theorem, obstruction, and proof program`
- Workstream: [master-equation-closure](priorities.md)
- Current lifecycle status: `Awaiting verification`
- Promotion status: not promoted
- Separate mathematical owner: [MEC-006 receiver wake-gradient closure](receiver-wake-gradient-closure.md) owns the fixed-reception receiver-position derivative of one admitted acceleration row, not this encounter-boundary program.
- Routed evidence: [close-approach synthesis](../../research-office/research-history/review-packets/close-approach-causal-delay-seven-council-research-findings-2026-07-29.md), [arrival-bunching analysis](../../research-office/research-history/review-packets/master-equation-arrival-bunching-strict-subfield-research-findings-2026-07-29.md), [stationary first-boundary and self-root topology](../../research-office/research-history/review-packets/master-equation-field-speed-first-boundary-self-root-topology-2026-07-29.md), [admissible-history first-exit theorem](../../research-office/research-history/review-packets/master-equation-admissible-history-pre-coincidence-first-exit-theorem-2026-07-29.md), and [unchanged-law post-threshold obstruction](../../research-office/research-history/review-packets/master-equation-post-field-speed-existing-law-continuation-obstruction-2026-07-29.md).

## Scope

MEC-007 owns the persistent-label, mirror-symmetric collinear close-approach program at normalized wake speed $c_f=1$. Its declared objects are:

- the complete partner-and-self causal-root census on the incoming regular chart;
- the first regular-chart boundary reached by the stationary mirror release and by the stated admissible history class;
- the bounded pre-boundary root-and-singularity ledger;
- exact same-event root admission and the limiting all-root acceleration measure at coordinate coincidence or coincident same-transmitter birth; and
- the local post-field-speed root topology under the unchanged canonical sharp-root acceleration operator.

This owner does not derive the receiver-position gradient, select a continuation law, or supply a boundary value.

Plainly: MEC-007 follows which delayed roots exist and where the mirror encounter stops being a regular evolution. MEC-006 separately checks how one already admitted regular acceleration row changes when the receiver position is varied.

## Established Priority Results

### Stationary mirror first boundary

On the canonical monotone stationary-history, one-partner-root incoming chart, the independently rederived identities

$$
R=\frac{2q}{1-\bar u},
\qquad
\frac{\mathrm du}{\mathrm dR}
=
-\frac{K}{R^2(u_r+u_t)}
$$

and the complete incoming root census imply that the first regular-chart exit is

$$
u(T_\ast)=1
$$

while $R_\ast>0$ and $q(T_\ast)>0$. At that event the partner emission remains strictly earlier than reception, $D_t^{\mathrm{partner}}=1-u(T_t)>0$, and $D_r^{\mathrm{partner}}=2$. The partner root is therefore simple and the event is not a transmitter-side fold. The exact self-root integral is negative for every earlier candidate emission, so no positive-delay self root exists at the event.

Claim grade: `derived priority result` on the declared stationary mirror chart. The result is falsified by an in-chart certified root census or exact identity that contradicts the stated first-event ordering.

Plainly: the two receivers reach the primitive wake-speed threshold while their present and delayed separations are still positive. The incoming theorem does not say how to evolve through that threshold.

### Admissible-history first-exit ordering

For the class $\mathcal H_{\mathrm{att}}^{\mathrm{reg}}$ defined in the [admissible-history proof packet](../../research-office/research-history/review-packets/master-equation-admissible-history-pre-coincidence-first-exit-theorem-2026-07-29.md), same-event coordinate coincidence cannot be the first event. Before $q=0$, either a current receiver speed reaches $c_f=1$, or the declared regular sector has already exited through a certified root-topology, retained-history, or other regularity boundary.

Claim grade: `derived theorem` only on the packet's maximal admitted regular interval. Initially superfield, nonmonotone, initially multi-root, incomplete-history, zero-root, and nonattractive data remain outside the theorem. An in-class counterexample with every required ledger field would falsify it.

Plainly: the ordering theorem covers a precise regular incoming family, not every symmetric history and not anything after the first boundary.

### Unchanged sharp-law post-threshold obstruction

For the declared stationary mirror encounter, the existing partner root persists as one regular inward row immediately beyond the field-speed threshold. Any continuous regular extension entering $u>1$ creates one positive-delay inward self root per label from the excluded diagonal. The exact self-delay measure is not locally finite at birth. Therefore the unchanged canonical sharp-root row sum supplies no regular Carathéodory or locally finite measure continuation for this branch.

Disposition: `Not advanced`. This is obstruction evidence, not an alternative evolution. It is falsified only by a complete derivation showing that the same unchanged admitted-root sum is locally finite on the identical stationary mirror record without deleting or reweighting a root.

Plainly: the existing acceleration operator reaches a singular self-root birth whose accumulated contribution diverges. Repeating that unchanged-law continuation calculation cannot produce passage or rebound.

### Conditional immediate post-threshold ledger

Fix a prescribed continuous one-sided extension on $[T_\ast,T_\ast+\varepsilon]$ that enters $u>1$, remains regular away from the birth, preserves the complete monotone strict-subfield retained history before $T_\ast$, keeps $q(T_\ast)>0$, and retains the simple partner-root tube. These are history hypotheses for a root-topology calculation, not a continuation selected by the Master Equation.

For every sufficiently close reception $T_r>T_\ast$, the complete local four-bundle ledger is:

| Ordered bundle | Local root count | Root factors and playback | Acceleration sign |
| --- | ---: | --- | --- |
| $-\leftarrow+$ | one persistent partner root | $R_{\mathrm p}>0$, $D_{t,\mathrm p}=1-u(T_{t,\mathrm p})>0$, $D_{r,\mathrm p}=1+u(T_r)>0$ | inward for the left receiver |
| $+\leftarrow-$ | one mirror partner root | same scalar bounds under mirror symmetry | inward for the right receiver |
| $-\leftarrow-$ | one newborn positive-delay self root | $\rho>0$, $D_{t,\mathrm s}=1-u(T_s)>0$, $D_{r,\mathrm s}=1-u(T_r)<0$, so $\mathrm dT_s/\mathrm dT_r<0$ | inward for the left receiver |
| $+\leftarrow+$ | one mirror newborn self root | same scalar bounds under mirror symmetry | inward for the right receiver |

The partner root function

$$
F_{T_r}(s)=q(T_r)+q(s)-(T_r-s)
$$

has $\partial_sF_{T_r}=1-u(s)>0$ on the complete pre-threshold retained interval. The old partner root therefore remains unique there, while $q(T_\ast)>0$ supplies a positive gap on the new near-diagonal interval and excludes an additional local partner root.

For either self bundle, let $x$ increase inward and set $h(T)=x(T)-T$. The function $h$ decreases strictly before $T_\ast$ and increases on the prescribed superfield side. Thus the unique newborn root satisfies

$$
h(T_s)=h(T_r),
\qquad
\int_{T_s}^{T_r}(u(\tau)-1)\,\mathrm d\tau=0,
\qquad
T_s<T_\ast<T_r.
$$

No positive-delay self root exists at $T_\ast$, no additional older-history self root lies outside this unique pre-threshold match, and no post-threshold self match exists between $T_\ast$ and $T_r$ apart from the excluded $T_s=T_r$ diagonal. The two mirror self incidences remain separate ordered bundle rows; this packet assigns no MEC-005 event ownership or boundary semantics to their common reception time.

Plainly: immediately after the threshold, each receiver has exactly two local rows under the stated prescribed-history hypotheses: its old partner row and one new self row. The negative self playback reverses root traversal, not the direction of acceleration.

The partner rows remain locally bounded. Writing

$$
w_-=1-u(T_s)>0,
\qquad
w_+=u(T_r)-1>0,
\qquad
\rho=T_r-T_s,
$$

the self-root playback identity gives

$$
\frac{\mathrm dT_s}{\mathrm dT_r}
=-\frac{w_+}{w_-},
\qquad
A_{\mathrm s}\,\mathrm dT_r
=
\frac{K_{\mathrm s}}
{\rho^2(w_-+w_+)}
\,\mathrm d\rho.
$$

Because $\rho\to0$ and $w_-+w_+\to0$ at birth, the self-row measure has infinite total variation; its fixed inward sign also makes the signed inward integral diverge. Mirror cancellation of the two receivers' coordinate-vector sum would not cancel either receiver's local row or the signed relative acceleration, and supplies no account or conservation statement.

Plainly: every admitted row is pointwise defined at positive delay, but the new self rows accumulate an infinite update next to the excluded diagonal. The ledger therefore confirms the existing `Not advanced` obstruction and does not reopen continuation as an unchanged-law question.

## Active Proof Program

### Bounded pre-boundary root-and-singularity ledger

Execute the regular approach only while every required certificate remains strictly inside the regular chart. Preserve persistent labels and the complete causal-root census. Record:

1. signed coordinate-crossing status;
2. minimum delayed range;
3. minimum absolute transmitter Jacobian;
4. retained-history margin;
5. signed relative-acceleration integral;
6. total variation;
7. refinement across time step, history representation, arithmetic precision, root-isolation tolerance, quadrature, and section ladder; and
8. evidence from an independently authored or analytically closed root oracle wherever that oracle has declared reach.

Stop before any uncertified root, zero-containing delayed-range or transmitter-Jacobian interval, retained-history loss, or oracle disagreement. A missing root, label swap, hidden branch deletion, same-implementation parity presented as independence, or refinement-dependent integral returns `Not advanced`.

Plainly: the ledger must show exactly which regular margin fails first. It may not fill a missing singular value with zero or a fitted finite part.

### Same-event admission and limiting measure

On shrinking certified positive-separation sections, distinguish three questions:

1. whether the canonical causal-root admission rule excludes an exact same-time, co-located partner row or merely leaves it unassigned;
2. whether the signed all-root acceleration integral and its total variation have finite section limits; and
3. whether any claimed limit is independent of section sequence and admitted approaching-history family.

Omission or nonassignment of one exact same-event row does not establish a finite accumulated update or a unique outgoing retained history. Divergence, sequence dependence, family dependence, or absence of a canonical exclusion returns `Not advanced`.

Plainly: removing one undefined point cannot cure a divergent accumulation in the open intervals beside it.

### Conditional sandbox fallback

This investigation remains deferred and must run only if the continuous wake construction cannot resolve the finite-width or regular-boundary question. At diagnostic authority, compare:

- symmetric radial/core smoothing that recovers the regular inverse-square acceleration outside a width scale; and
- a separate smooth fold/transversality regularization.

Every width, fold scale, and profile is a sandbox parameter rather than a selected physical constant. Regular self interactions remain admitted; no self root may be removed. Acceptance requires a finite symmetric collinear two-body acceleration impulse, regular-law recovery outside the smoothing region and in a controlled zero-width limit, and comparisons across multiple smooth profiles that expose parameter and profile dependence. Hidden root deletion, failed regular-law recovery, a divergent or regulator-dependent claimed limit, or missing MEC-005 ownership returns `Not advanced`.

No result advances beyond diagnostic authority unless a scale and rule are independently derived from a same-record wake construction.

Plainly: the sandbox may compare candidate smoothings, but it cannot choose a new law or turn a convenient scale into a derived constant.

## Current Execution Blocker

The current-source [`eom_stationary_joint_frontier_fixture_cli`](../../../src/eom/native/eom_stationary_joint_frontier_fixture_cli.cpp) certifies the regular partner-root frontier only through $T=1.394999999999996$. Its next probe at $T=1.3999999999999959$ rejects atomically because the joint root-time enclosure width $1.0205062366854167\times10^{-5}$ exceeds the declared $10^{-5}$ tolerance even at 512-bit arithmetic. The limiting term is the retained-history remainder radius $3.8146835796385637\times10^{-6}$, not arithmetic roundoff.

The fixture is also a root-frontier instrument rather than the complete MEC-007 ledger: it does not emit signed coordinate-crossing status, minimum delayed range, retained-history margin, signed relative-acceleration integral, or total variation. Its traversal/direct-route parity is same-implementation evidence and cannot serve as the independent trajectory oracle.

Claim grade: `measured implementation blocker`. A tighter retained-history representation that certifies the next probe under the unchanged tolerance, or an independent full-trajectory instrument that reaches the terminal regular event, would falsify the blocker.

Plainly: the present fixture stops because its retained-history enclosure is too wide and because its output schema is not the requested ledger. Increasing arithmetic precision alone does not solve either problem.

## Dependency Boundaries

| Owner | Relationship to MEC-007 |
| --- | --- |
| MEC-002 — causal wake update law | May eventually supply a same-record representation or boundary rule. MEC-007 cannot infer that rule from the encounter or from a desired outcome. |
| MEC-003 — finite coincident same-transmitter transition | Retains ownership of any finite, unique, regulator-independent transition law. MEC-007 supplies this mirror encounter's theorem, root/measure tests, and unchanged-law obstruction as bounded evidence only. |
| MEC-004 — same-update conserved accounts | Receives no signed account value or conservation result from MEC-007. Any accepted boundary update would need separately derived maps on the identical update. |
| MEC-005 — pairwise causal-root ledger closure | Owns exhaustive root, event, and boundary provenance with no duplicate booking. MEC-007 must preserve every incident root and one unresolved boundary owner, but its mirror calculation cannot close MEC-005 or consume unverified provenance as accepted. |
| MEC-006 — receiver wake-gradient closure | Owns the regular fixed-reception derivative and its singular derivative boundary. It is a dependency only if an MEC-007 calculation actually consumes a receiver/self acceleration-gradient row. |

Plainly: MEC-007 owns one encounter-boundary proof program. It neither replaces the general transition owner nor supplies provenance, derivative, or account closure for the neighboring MEC items.

## Nonclaims And Completion

MEC-007 establishes no continuation, passage, rebound, coordinate-crossing outcome, outgoing retained history, boundary value, conservation result, physical realization, stability, MEC closure, or EOM solver acceptance.

MEC-007 remains `Awaiting verification`. Completion requires an independently checked complete pre-boundary ledger through the terminal regular event and an exact disposition of same-event root admission and the limiting all-root measure. The unchanged sharp-law post-threshold branch is already exhausted with `Not advanced`; it must not be presented as an open route. Any proposed new continuation or boundary prescription remains a separately authorized MEC-002/MEC-003 candidate and is outside this packet's current authority.

Plainly: this slot can finish by proving exactly where the regular mirror program stops and why. It cannot finish by inventing what happens afterward.
