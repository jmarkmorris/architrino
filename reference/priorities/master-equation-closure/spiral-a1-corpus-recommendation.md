# Spiral A1 Corpus Recommendation

Status. Reset recommendation packet for `spiral_branch_chart_test`.

Claim level. Corpus promotion record, not a theorem-grade branch certificate.

Recommended corpus state:

- Retain A1 only as a receiver-normal restart target.
- Preserve active-root, inactive-gap, finite-memory, source-normal floor, and
  root-transport rows as conditional topology inputs.
- Do not promote radial-turn, tangential-drive, tangential-compatibility,
  action, power, or finite-window conservation verdicts until the same retained
  boxes carry $D_T/D_s$ branch-strength intervals.

Smallest promotion-worthy artifact:

$$
\left\{
D_{s,\alpha},\,
D_{T,\alpha},\,
W_{\alpha}^{\mathrm{rec}},\,
B_{r,\alpha}^{\mathrm{rec}},\,
T_{\alpha}^{\mathrm{rec}}
\right\}_{\alpha\in\{P_1,P_2,P_3,S_1\}}.
$$

Until that table exists, there is no A1 reader-facing pass/fail update to
promote.
