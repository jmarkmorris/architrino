# Spiral A1 Receiver-Normal Force-Ratio Decision Target

Status. Receiver-normal decision target for the A1 radial force-ratio row. This file
does not supply a pass, failure, or $\Gamma$ interval.

Claim level. Restart target, not closure evidence.

The retained A1 chart can use
$$
\Gamma=\frac{r_\ast^3\Omega^2}{\kappa q_1^2}
$$
only after the branch side is recomputed with receiver-normal branch strength.
The required branch sum is
$$
B_r^{\mathrm{rec}}(C_{\mathrm{A1}};0)
=
\sum_{\alpha\in\{P_1,P_2,P_3,S_1\}}
B_{r,\alpha}^{\mathrm{rec}}(0),
$$
where each summand is computed from the same-box factor
$$
W_{\alpha}^{\mathrm{rec}}=\left|D_{T,\alpha}/D_{s,\alpha}\right|.
$$

Promotion rule. No A1 force-ratio row may pass or fail until a receiver-normal
$B_r^{\mathrm{rec}}$ interval and an independently justified $\Gamma$ interval
are declared on the same retained record.
