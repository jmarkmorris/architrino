# Spiral VP-1 Transmitter-Side Rebuild Target

## Status

- Claim grade: priority-only branch rebuild target.
- Preserved evidence: retained-root topology, inactive gaps, finite memory,
  transmitter-side floors, and root transport.
- Withdrawn evidence: every radial, tangential, action, power, sensitivity, or
  pass/fail conclusion computed with receiver playback as acceleration strength.
- Current blocker: A1 is the selected first transmitter-side rebuild; VP-1 waits
  for the same accepted causal wake-state accounts.

## Retained candidate

$$
a=\frac{1}{10},
\qquad
b_\ast=\frac72,
\qquad
I_\ast=\left[-\frac{\pi}{6},\frac{\pi}{6}\right],
\qquad
D_{\mathrm{cert}}=\left[\frac12,4\pi\right],
$$

with retained labels $P_1,P_2,P_3,S_1$.

The active-root, inactive-memory, interval root-gap, and root-transport packets
remain topology inputs. A future physical calculation must reproduce their
identities and bounds on the same retained record.

## Required causal-hit records

For every retained root $\alpha$, the same retained record must contain

$$
D_{t,\alpha},
\qquad
D_{r,\alpha},
\qquad
W_\alpha^{\mathrm{acc}}=\frac{c_f}{|D_{t,\alpha}|},
\qquad
\frac{dT_{t,\alpha}}{dT_r}=\frac{D_{r,\alpha}}{D_{t,\alpha}}.
$$

The first three physical calculations are the outward radial aggregate, the
outward tangential aggregate, and their negative controls. Removing or changing
$D_r$ may change playback but must not change the instantaneous acceleration at
fixed causal geometry. Removing or changing $D_t$ must make the acceleration
calculation is not advanced.

## Promotion boundary

VP-1 cannot inherit any physical verdict from the removed receiver-weighted
law. Promotion requires one reproducible retained state containing transmitter-
side acceleration, separate signed playback, outward radial and tangential
aggregates, the accepted causal wake state, and energy, momentum, and angular-
momentum accounts from the same update.
