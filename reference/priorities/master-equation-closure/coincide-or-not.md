# Coincide Or Not: Adjudicating Architrino Coordinate Coincidence In A Populated Universe

## Status

- Kind: `priority`
- Queue item: `MEC-008`
- Priority object: `same_transmitter_coincidence_domain_reachability`
- Claim level: `derived positive secular sign on the symmetric pair-center chart, derived sign-indefinite general absolute-frame correction, conditional populated invariant tube, derived uniform-sub-field radial first-exit obstruction, derived locally affine root-existence classification, derived exact-symmetry populated EOM reachability of first coordinate coincidence, derived local codimension-two nongenericity, derived bounded-history self-root and regular-chart DDE lemmas, derived local self-chord expansion, derived inverse-square regularization obstruction and one-sided causal-shadow exceptional-sphere flow, bounded EOM prefixes and failed boundary diagnostics, and unresolved formal boundary-certificate acceptance; no continuation law claimed`
- Workstream: [master-equation-closure](priorities.md)
- Current lifecycle status: `In progress`
- Promotion status: not promoted
- Normalization: $c_f=1$ throughout; symbolic $c_f$ is retained only where its dependence is the point of the identity.
- Separate mathematical owner: [MEC-007 mirror close-approach causal-root boundary](mirror-close-approach-causal-root-boundary.md) owns the persistent-label mirror-symmetric collinear encounter. This packet does not reopen, re-derive, or contradict it.
- Consumed as settled prior: MEC-007's first-boundary ordering and unchanged-law divergence; [MEC-006 receiver wake-gradient closure](receiver-wake-gradient-closure.md) for the regular fixed-reception gradient; [GD-5 vanishing-delay hazard](../field-speed-ceiling/germund-dahlquist-review-2026-08-02.md); and the [conditional speed-bound rigidity and self-root exclusion theorem](../field-speed-ceiling/jack-k-hale-second-review-2026-08-02.md), without importing a global velocity ceiling.

## Scope

This packet is the analytical owner of MEC-008. It asks one question that the completed collinear programs deliberately do not answer.

> In a populated universe of many architrinos in general position, with no imposed symmetry, can two architrinos reach the same point of the Euclidean void at the same absolute time?

The declared objects are: a reframing of coincidence as delay vanishing rather than as a kernel pole; an assessment of the current null-action-at-coincidence convention; a survey of the mathematical frameworks that apply natively to the Master Equation as a delay system in $\mathbb{R}^3$ under absolute time; the strongest available case for ruling coincidence in; the strongest available case for ruling it out; and the identification of the single quantity whose sign decides between them.

The collinear head-on case is already settled and owned elsewhere. This document is about the ordinary case, where two architrinos among many drift near each other with no special alignment, and it exists to decide whether that case can ever end in exact contact.

This packet derives no continuation law, selects no boundary value, assigns no event semantics, and closes no conserved account. It derives a conditional local invariant tube around nonzero areal rate, a uniform-sub-field radial first-exit obstruction, a locally affine root-existence classification, and a same-transmitter chord expansion. It also corrects the secular-sign scope: the positive sign is derived on the symmetric pair-center chart, while a general absolute-frame expansion and a periodic prescribed-path control prove that the complete pair contribution is not universally one-signed. The root classification identifies a two-sided super-field inbound causal shadow in which both recent partner roots are absent. An explicit noncollinear four-Architrino retained history then supplies fourteen simple older roots, a bounded acceleration bootstrap, and a unique EOM continuation to first coordinate coincidence. This proves mathematical reachability on an exact reflection-symmetric set without making coincidence a dynamical attractor; an unchanged independent oracle verifies the complete pre-contact evolution and topology bracket, while formal certificate review and continuation remain open.

## Three Distinct Mathematical Questions

The word `possible` separates into three questions that must not be merged.

1. **Kinematic compatibility:** can two freely prescribed point paths be assigned the same coordinate at the same absolute time? Yes, but this says nothing about the Master Equation because arbitrary prescribed paths need not be its solutions.
2. **Dynamical reachability:** can an admissible EOM-evolved retained history attain $d(T_\ast)=0$? This is MEC-008's question. The open approach interval is not a substitute for reachability; it is where reachability is decided, because the accumulated acceleration before $T_\ast$ determines whether the path can arrive.
3. **Boundary definition and continuation:** if an admissible history reaches $d=0$, what event object is defined there and which outgoing retained history follows? These are downstream of reachability. Assigning a value at the single endpoint neither proves arrival nor selects a continuation.

Thus the run-up and reachability are the same analytical subproblem viewed from evolution and endpoint perspectives, while reachability and continuation are different problems. A domain-exclusion theorem closes MEC-008 without an event rule; a boundary-reaching counterexample makes a derived event rule and continuation theorem unavoidable.

## MEC-008 acceptance conditions

MEC-008 is stated in the [work queue](work-queue.md#mec-008--same-transmitter-coincidence-domain-reachability) and its acceptance conditions govern this packet. They are restated here because they bound what any result in this document is allowed to claim.

A **domain exclusion** — a verdict that coincidence cannot occur — is accepted only if a theorem derived from the existing update proves an invariant admissible region that cannot reach the boundary. Singularity of the formula at the boundary is not itself an exclusion proof. That distinction is the reason the codimension argument in Framework 4 below is recorded as insufficient rather than as a verdict, and the reason the transverse barrier is posed as a scaling question with a named decider rather than asserted.

**Reachability** — a verdict that coincidence does occur — is accepted by one independently certified EOM-evolved counterexample carrying complete retained history and causal-root provenance. Prescribed isolated collinear histories are local analytical controls and cannot alone decide physical-domain membership, which is why MEC-007's mirror result is consumed here as a bounded prior rather than as an answer.

An assertion that the Master Equation simply stops before the boundary is classified as an incomplete-domain convention, not a solution, until one of the two verdicts above is supplied or a separately accepted terminal-event ontology exists. The null-action convention assessed below falls under exactly this classification.

MEC-008's required controls are: (1) the existing isolated collinear birth as a singular analytical control; (2) a minimally noncollinear three-or-more-architrino EOM-evolved control with complete root census; (3) perturbations of population, geometry, and retained prehistory testing whether the boundary is avoided robustly or only by fine tuning; and (4) a local chord expansion separating the necessary $\|\mathbf V_i\|=c_f$ contact condition from curvature-dependent higher-order terms. Every numerical instantiation uses $c_f=1$.

This packet supplies the analytical frame for controls (2) and (3), derives the signed pair contribution first on the symmetric pair-center chart and then on a general locally affine absolute-frame chart, proves the inverse-square blow-up obstruction and the causal-shadow one-sided exceptional-sphere flow, records bounded EOM-evolved three- and four-Architrino controls, and completes control (4) at the local $C^4$ chord-expansion level. The turning arc does not complete a turn, so the full-turn numerical check remains open. The exact noncollinear four-Architrino construction proves EOM reachability to first contact and proves that nearby collision histories have local codimension two on an explicit retained-history perturbation slice. Control (3) therefore has a precise local answer on that chart—generic transverse perturbations in the slice miss, but exact symmetric populated data reach—without yet classifying every arbitrary populated history.

Neither the near-diagonal self-root stratum nor a chance pair encounter is privileged here. MEC-008 covers both the same-transmitter case, in which an architrino's own path history produces a vanishing-delay root, and the two-body case, in which two distinct architrinos approach coincidence. The transverse analysis below addresses the two-body case directly; its conditional bearing on the self-root case runs through the additional speed-bound rigidity hypothesis recorded as O6.

## Why The Collinear Result Does Not Settle The General Case

MEC-007 proves, on its declared admissible class, that same-event coordinate coincidence cannot be the first event: the receiver speed reaches the primitive wake speed $u=c_f$ while both the present separation $q(T_\ast)$ and the delayed range $R_\ast$ remain strictly positive. It further proves that any continuous regular extension past that threshold births a positive-delay self root whose exact delay measure has infinite total variation, so the unchanged sharp-root row sum supplies no locally finite continuation.

Both results are load-bearing and both are consumed here as settled. Neither transfers to the populated case, for two structural reasons.

First, MEC-007's protection is a *speed* threshold, not a *separation* threshold. It says the encounter exits the regular chart before contact because a receiver runs out of admissible speed. A populated encounter with an arbitrary approach geometry has no reason to saturate speed first; the exit ordering is a property of the mirror chart's one-dimensional forced kinematics, and reordering it is exactly what breaking the symmetry does.

Second, the mirror configuration is invariant under the dynamics. Persistent-label mirror symmetry is preserved by the law, so the relative separation vector is confined to a line for all time. That confinement is the whole reason the encounter is forced. In general position nothing confines it, and the two transverse directions become live degrees of freedom that the collinear analysis has integrated away by hypothesis.

Plainly: the finished proof says two architrinos sliding toward each other on the same wire hit a speed limit before they touch. It says nothing about two architrinos free to move in all three directions, because the wire was doing most of the work.

## Reframing: Coincidence Is Vanishing Delay, Not A Kernel Pole

The causal-root condition is $r_{ij}=c_f(T_r-T_t)$, so on any admitted root

$$r=0 \iff \Delta = T_r-T_t = 0 .$$

Coincidence is therefore not primarily an event at which an inverse-square kernel becomes large. It is the event at which the delay itself vanishes and the Master Equation ceases to be a delay system at all, attempting to degenerate into an instantaneous law.

This reframing has an immediate consequence already recorded in the lane. GD-5 observes that the method-of-steps causality horizon — the forward interval over which the ledger is fully determined by already-computed history — equals the minimum active delay, and collapses to zero precisely on approach to coincidence. Explicit causal stepping is impossible there, not through numerical weakness but because no forward window is causally closed.

Plainly: the equation only knows how to move forward in time by less than the shortest lag it is currently using. Coincidence is where the shortest lag is zero, so the equation has no forward step available at all. That is a statement about the law, not about any particular integrator.

The corresponding solver behavior is the halt code `minimum_step_exhausted` in [`src/eom/src/CoupledEvolution.cpp`](../../../src/eom/src/CoupledEvolution.cpp). Under this reframing that halt should be read as a faithful report of a structural fact rather than as an instrument limitation to be engineered away.

Claim grade: the equivalence $r=0\iff\Delta=0$ is `derived` from the root condition. The causality-horizon collapse is `derived` and already owned by GD-5. The reclassification of `minimum_step_exhausted` as structural is `inferred`; it is falsified by an explicit causal stepping construction that remains well defined with $\inf r=0$, which the delay identity appears to forbid.

### Same-time coincidence is not the same problem as different-time path crossing

Two distinct questions are easy to run together, and only one of them is hard.

**Same-time coincidence** asks whether $\mathbf X_i(T)=\mathbf X_j(T)$ for some absolute time $T$ — two architrinos at one point on one slice. That is the subject of this packet.

**Different-time crossing** asks whether one architrino later occupies a point another once occupied, at a different time. That is not a hazard at all. It is the theory's **normal mode of interaction.**

The reason is the root condition itself. Every admitted contribution is a receiver meeting a surface emitted from a transmitter's past position, so architrinos are continuously arriving at places other architrinos have been. Ordinary interaction *is* different-time crossing, and it happens at every root of every pair at every instant.

The two questions therefore differ in what they impose. Same-time coincidence is three scalar conditions $\mathbf X_i(T)=\mathbf X_j(T)$ against one free parameter $T$, hence codimension two, and it degenerates the delay as recorded above. Different-time crossing imposes only that the root condition hold, which is what the acceleration row requires anyway, and it degenerates nothing.

Stating the distinction matters because an intuition that "architrinos are constantly running into each other's positions" is correct, and it does not bear on coincidence at all. What makes coincidence hard is simultaneity, not proximity.

Claim grade: `derived` from the root structure. Falsified by exhibiting an admitted root whose contribution requires the transmitter and receiver to occupy the same point at the same absolute time, which the $H(0)=0$ endpoint convention excludes.

### Conjecture: three-body coincidence should be more strongly excluded than two-body

Simultaneous coincidence of **three** architrinos imposes six scalar conditions — $\mathbf X_i(T)=\mathbf X_j(T)$ and $\mathbf X_j(T)=\mathbf X_k(T)$ — against the same single free parameter $T$. That is codimension five, against codimension two for a pair.

If codimension counting has any force here, a triple coincidence is far more strongly excluded than a pair coincidence, and an $n$-fold coincidence more strongly still, giving a hierarchy rather than a single threshold.

Two cautions keep this a conjecture rather than a result, and both are the same caution recorded in Framework 4 below. Codimension counting establishes non-genericity within a family of systems, and $\mathbb{A}\mathbb{A}\mathbb{A}$ is one specific system whose flow is free to live on a thin set. More seriously, the count assumes nothing is driving the surplus conditions toward zero — and a bound three-architrino assembly is precisely a mechanism that holds several separations small and correlated. Symmetric configurations may collapse the effective count exactly as the collinear case collapses it for a pair.

Claim grade: `guessed`. Falsifier: a symmetric three-architrino configuration whose retained dynamics drives all three separations to zero together, which would show the effective codimension is lower than the naive count and that the hierarchy does not hold.

The useful form of this conjecture is not the ordering itself but what it predicts for the transverse analysis below: if the barrier argument survives for pairs, it should survive *more easily* for triples, because more transverse conditions must be held simultaneously. A result showing the opposite would indicate that the pair analysis has missed a cooperative channel.

## Assessment Of The Current Null-Action Convention

The working model treats coordinate coincidence as contributing null action: the acceleration row admitted at $r=0$ is assigned the zero vector, and the coincidence event is booked as carrying no update.

The finding of this packet is that the convention is not incorrect but is **inert with respect to the questions it is being asked to settle**. A trajectory's arrival at coincidence is decided by the accumulated update $\int\|\mathbf A\|\,dT$ over an approach interval, and by the existence of an admissible continuation on an open neighborhood of the endpoint. Neither functional is sensitive to the value assigned on a single absolute-time instant, which carries zero measure in that integration. Assigning null action removes a formal ambiguity in how the symbol is evaluated at one point while leaving reachability and continuation entirely undetermined.

This is consistent with the workstream's own acceptance criterion, which demands $\int_{T_0}^{T_0+\epsilon}\|\mathbf A_{ii}(T)\|\,dT<\infty$ on an open neighborhood rather than a value at the event.

Plainly: choosing what the rule says at the exact instant of contact is like choosing what a sign says at the bottom of a cliff. It settles no question about whether you go over, and none about what happens after.

Claim grade: `derived`. Falsified by exhibiting any reachability or continuation conclusion that changes when the assigned value at the single coincidence instant is changed, with the approach neighborhood held fixed.

## Near-Field Magnitude Lemma

Let $d=\|\mathbf X_i(T_r)-\mathbf X_j(T_r)\|$ be the present separation and consider the first partner root. Define

$$g(\Delta)=\|\mathbf X_i(T_r)-\mathbf X_j(T_r-\Delta)\|-c_f\Delta ,
\qquad g(0)=d>0 .$$

Writing $\hat{\mathbf s}$ for the unit vector from the transmitter's delayed position to the receiver's present position, differentiation gives

$$g'(\Delta)=\hat{\mathbf s}\cdot\mathbf v_j(T_r-\Delta)-c_f=-c_f D_t ,
\qquad D_t=1-\frac{\hat{\mathbf s}\cdot\mathbf v_j}{c_f} .$$

So the transmitter factor is exactly the normalized root-condition derivative at the root. That endpoint derivative alone does **not** equal the average slope of $g$ from $0$ to $\Delta_\ast$: when the transmitter has transverse velocity, the line-of-sight direction changes by an $O(1)$ angle across an $O(d)$ delay. The earlier formula $r_\ast\approx d/D_t$ therefore omitted a transverse geometric factor.

The corrected leading relation follows from the locally affine root itself. Resolve the reception-time transmitter velocity as

$$
\mathbf v_j=a\hat{\mathbf n}+\mathbf p,
\qquad
\mathbf p\mathbin{\cdot}\hat{\mathbf n}=0,
\qquad
\chi=(c_f^2-\lVert\mathbf p\rVert^2)^{1/2},
$$

and take the first-root branch $a<\chi$ with $\chi>0$. Exact affine geometry gives

$$
\Delta_\ast=\frac{d}{\chi-a},
\qquad
r_\ast=\frac{c_fd}{\chi-a},
\qquad
D_t=\frac{\chi(\chi-a)}{c_f^2}.
$$

Equivalently, if $\alpha$ is the angle between the delayed row direction and the present baseline, then $\cos\alpha=\chi/c_f$ and

$$
\boxed{
r_\ast
=
\frac{\chi}{c_f}\frac{d}{D_t}
=
\frac{d\cos\alpha}{D_t}.
}
$$

For a $C^2$ transmitter history with bounded acceleration, these formulas carry relative $O(d)$ corrections while the first-root and factor margins remain uniform. Substitution into the canonical transmitter-side row gives the corrected leading magnitude in the **present** separation:

$$
\boxed{
\lVert\mathbf A\rVert
=
\frac{\kappa\lvert q_iq_j\rvert c_f^3\lvert D_t\rvert}{\chi^2d^2}
\left[1+O(d)\right]
=
\frac{\kappa\lvert q_iq_j\rvert c_f\lvert D_t\rvert\sec^2\alpha}{d^2}
\left[1+O(d)\right].
}
$$

Plainly: converting delayed range to present separation requires both the root derivative and the fixed delay-lead angle. The omitted $\sec^2\alpha$ factor is positive, so it changes the magnitude but not the inverse-square scaling or the sign conclusions below.

### Power accounting

The sign of the transmitter factor's effect must be stated explicitly, because the naive reading inverts it. In the raw row $|D_t|$ sits in the denominator, which suggests that a transmitter moving toward the receiver amplifies the contribution. After the root substitution it appears linearly in the numerator instead. The two effects act in opposition and the geometric one wins two powers to one:

| Effect | Origin | Power of $D_t$ |
| --- | --- | ---: |
| Range: small $D_t$ pushes the admitted emission point further away | $1/r_\ast^{2}=c_f^2D_t^{2}/(\chi^2d^{2})$ | $+2$ |
| Bunching: small $D_t$ compresses arriving wake fronts and raises the row amplitude | $1/\lvert D_t\rvert$ | $-1$ |
| Net near-field dependence | | $+1$ |

The bunching enhancement is therefore real but outvoted at fixed nonzero $\chi$, because range enters squared while bunching enters linearly. In the limit $D_t\to0^{+}$ with $\chi$ bounded away from zero, the admitted root recedes without bound and the contribution vanishes rather than diverging. The separate transverse limit $\chi\to0$ is not controlled by that statement; the $\sec^2\alpha$ factor must then be retained.

Plainly: ask where the other architrino was standing when it emitted the wake now arriving. A transmitter moving toward the receiver was further away then, so what arrives is fainter; a transmitter moving away was closer then, so what arrives is louder. Compression of the arriving fronts partly offsets this, but distance costs twice over while compression pays back once.

### Exact case check

For a transmitter in constant radial motion the lemma is exact rather than leading order, which supplies an internal closed-form consistency check on the algebra, not independent evidence. Set $c_f=1$ and let the transmitter move along the line of sight with signed speed $v$, positive toward the receiver, so $D_t=1-v$ exactly. At delay $\Delta$ the transmitter stood at range $d+v\Delta$, so the root condition $d+v\Delta=\Delta$ gives

$$\Delta_\ast=\frac{d}{1-v}=\frac{d}{D_t},\qquad r_\ast=\frac{d}{D_t},$$

with no expansion and no discarded terms. Evaluating at $d=0.1$ with $\kappa|q_iq_j|$ factored out:

| Transmitter motion | $v$ | $D_t$ | $r_\ast$ | $\|\mathbf A\|$ | Relative to static |
| --- | ---: | ---: | ---: | ---: | ---: |
| Approaching at half field speed | $+0.5$ | $0.5$ | $0.200$ | $50$ | $0.5\times$ |
| Static | $0$ | $1.0$ | $0.100$ | $100$ | $1\times$ |
| Receding at half field speed | $-0.5$ | $1.5$ | $0.0667$ | $150$ | $1.5\times$ |

Both the direct evaluation $c_f/(r_\ast^{2}|D_t|)$ and the boxed form $|D_t|/d^{2}$ return the same three magnitudes. The receding row is the sharpest illustration: its admitted emission range $r_\ast=0.0667$ is strictly smaller than the present separation $d=0.1$, so the receiver is acted on by an emission made from nearer than the partner now stands.

Plainly: at the same present separation, an approaching partner delivers half the acceleration a stationary one would, and a receding partner delivers half again more. Nothing is being hidden in an approximation here; the constant-speed radial case gives these numbers exactly.

The transverse case exposes the factor that the radial check cannot see. Still with $c_f=1$ and $d=0.1$, take $a=0$ and $\lVert\mathbf p\rVert=0.6$, so $\chi=0.8$. Exact affine geometry gives

$$
\Delta_\ast=r_\ast=0.125,
\qquad
D_t=0.64.
$$

After factoring out $\kappa\lvert q_iq_j\rvert$, direct evaluation gives

$$
\frac{1}{r_\ast^2D_t}=100,
\qquad
\frac{D_t}{\chi^2d^2}=100,
\qquad
\frac{D_t}{d^2}=64.
$$

Thus the corrected formula agrees exactly, while the omitted-$\chi$ formula understates this transverse row by $36\%$. This is an algebraic instance of the affine formula, not an independent numerical certificate.

### Scope of the correction

Three boundaries on what this lemma does and does not establish.

The power is untouched. On every uniformly sub-field chart, both $D_t$ and $c_f/\chi$ are bounded, so the corrected coefficient rescales the near-field constant and never regularizes the $1/d^{2}$ growth. No softening of coincidence follows from it.

Only the transmitter's velocity enters. The receiver's own motion does not appear in the acceleration magnitude at all, per the workstream's transmitter-side convention and its revocation boundary against receiver-weighted $|D_r/D_t|$ as acceleration strength. There is accordingly no symmetric mutual-enhancement mechanism during a close approach: each architrino's coefficient is set by its partner's line-of-sight velocity, independently.

On a fixed-$\alpha$ family, the direction of the transmitter-factor effect still runs against approach: lowering $D_t$ lowers the coefficient linearly. The complete coefficient is $D_t\sec^2\alpha$, however, so an independently changing transverse lead angle can strengthen it. Any argument that close approach drives its own runaway through the transmitter factor alone remains unavailable.

The correction does not alter the populated shelf-history reachability theorem below. Every active emission in that construction comes from a stationary transmitter point, so $\mathbf p=\mathbf0$, $\chi=c_f$, $\alpha=0$, and $D_t=1$ exactly; the two collapsing partner rows are absent on the incoming chart. Its acceleration bounds and independently replayed prefix are therefore numerically unchanged.

Plainly: the transmitter factor alone still weakens an inbound row, while transverse lead supplies a separate positive geometric multiplier. Neither changes the inverse-square power, and neither turns the single-row coefficient into a universal collision verdict.

Claim grade: the affine formulas are `derived exactly`; the $C^2$ extension is `derived asymptotically` under bounded acceleration and uniform first-root, $D_t$, and $\chi$ margins. Falsifier: direct affine substitution yielding a range other than $d\chi/(c_fD_t)$ or a leading row magnitude other than $\kappa\lvert q_iq_j\rvert c_f^3\lvert D_t\rvert/(\chi^2d^2)$. The lemma says nothing about a limit in which either $D_t$ or $\chi$ loses its margin.

## Applicable Mathematical Frameworks

Four frameworks apply natively to the Master Equation in $\mathbb{R}^3$ under absolute time. None imports a law, constant, or mechanism from an observer-level theory; each supplies a method for asking the reachability question rigorously.

### Framework 1 — State-dependent delay systems

The Master Equation is a state-dependent delay differential system: it prescribes a second derivative, the input is drawn from earlier absolute times, and the size of the lag is itself determined by the solution through $r=c_f\Delta$. This is the correct classification, and it is why results proved for instantaneous systems do not transfer without argument.

The relevant structural fact concerns the behavior of such systems as the delay shrinks. For a linear scalar delay system with constant lag $\tau$, seeking solutions of the form $e^{\lambda T}$ yields a transcendental characteristic equation rather than a polynomial one, and hence an infinite family of characteristic roots. One root tracks the instantaneous-system answer as $\tau\to0$; the remainder have magnitudes growing like $|\lambda|\sim 2\pi k/\tau$ and escape to unbounded frequency. They do not merge into the instantaneous limit; the small-delay limit is singular.

Plainly: any system with a lag carries a hidden family of ringing modes whose pitch is set by one over the lag. Shrink the lag toward zero and those modes ring arbitrarily fast. Coincidence is the point where the lag is zero, so the system becomes unboundedly stiff exactly there.

Claim grade: the constant-lag spectral escape is a standard `derived` fact about linear delay systems. Its transfer to the nonlinear, vector, state-dependent Master Equation is `inferred` and unproven; the honest status is a candidate obstruction to any finite-regularity continuation through coincidence, not a theorem. It is falsified by constructing a continuation with bounded local regularity across a coincidence event.

### Framework 2 — Blowing up the diagonal

The diagonal is the set $\{\mathbf X_i=\mathbf X_j\}$ in configuration space. Asking whether the flow reaches it is awkward, because the acceleration row is undefined there, so the question concerns a flow arriving where it has no definition.

The standard geometric repair is to blow up the diagonal: substitute $\mathbf r=\rho\,\hat u$ with $\rho\ge0$ a scalar separation and $\hat u$ a unit direction, and rescale the time parameter so the rescaled vector field extends continuously to $\rho=0$. The single undefined point is replaced by a two-dimensional exceptional set carrying the directions of approach, and the flow becomes an ordinary flow on that set.

Plainly: instead of asking whether the motion hits a bad point, inflate the point into a sphere labelled by which direction you arrived from, and ask which spots on that sphere the motion can actually land on. A question about a singularity becomes a question about an ordinary flow on a surface.

What this buys is decisive rather than descriptive. The rescaled flow on the exceptional set has fixed points, and their stability in the $\rho$ direction answers the reachability question directly: if every fixed point repels in $\rho$, coincidence is unreachable and the ruling-out case is proved; if some attract, coincidence is reachable and the attracting $\hat u$ enumerate the only admissible approach directions, which is exactly the data a continuation law would need.

Claim grade: this is a `method`, not a claim. The admissibility check below rules out its nondegenerate application on the declared bounded-velocity regular chart: every acceleration-regularizing time scaling freezes the geometric sphere and sends the root's rescaled memory span to infinity.

### Framework 3 — Regularization and its obstruction

Separately from reachability, a singularity may be *removable*: a change of dependent variables together with a change of the evolution parameter can sometimes convert an apparent singularity into a regular passage with a unique continuation.

There is a specific structural reason to doubt removability here, and it is native to $\mathbb{A}\mathbb{A}\mathbb{A}$ rather than borrowed. Regularization schemes operate by finding a new evolution parameter in which the motion is smooth. But the *content* of the Master Equation — which past emission event acts on a given reception — is fixed by absolute time through $r=c_f\Delta$. An integrator's parameter may be rescaled freely; the causal-root condition may not be reparameterized without changing which events are admitted. The standard technique therefore has strictly less freedom than it does for an instantaneous law.

Plainly: the usual trick for surviving a singularity is to change clocks until the motion looks smooth. Here the clock is not a convenience — it is what decides who hears whom. You cannot change it without changing the physics.

Claim grade: the universal reparameterization result is `derived` below: with fixed $c_f$, only absolute-time translations preserve the canonical admission equation over the declared history class, and a translation cannot regularize vanishing delay. Existing corroborating evidence sits on the same side: the straight-through mirror trial in [the continuation scratchpad](../field-speed-ceiling/coincidence-continuation-scratchpad.md) yields a $\delta^{-2}$ obstruction rather than a clean passage, and [the endpoint-residue result](../field-speed-ceiling/coincidence-open-interval-convergence-and-endpoint-residue.md) establishes that ordinary receiver measures cannot converge to a finite vector-Radon measure on any neighborhood containing the endpoint.

### Framework 4 — Codimension and transversality, with its limits

Coincidence imposes three scalar conditions $\mathbf X_i(T)=\mathbf X_j(T)$ against one free parameter $T$, hence codimension two. A trajectory in general position misses.

This packet records the argument together with an explicit statement of why it cannot carry the verdict alone, because it is the argument most likely to be over-trusted. Codimension counting establishes non-genericity within a family of systems; $\mathbb{A}\mathbb{A}\mathbb{A}$ is one specific system, and a specific flow is free to live on a codimension-two set. More importantly, the count silently assumes the two surplus conditions are not being actively driven toward zero. If the dynamics contains a channel that shrinks the miss, coincidence is an attractor rather than an accident and the count is irrelevant.

Plainly: counting says you will not land on the target by chance. It says nothing at all if something is steering you onto it.

Claim grade: the codimension count is `derived`. Its adequacy as a ruling-out argument is explicitly `not claimed`. This is precisely the gap that the next section's transverse analysis is built to close.

## The Transverse Barrier And The Decisive Quantity

This section contains the packet's principal new content. Nothing in the lane currently analyzes the transverse degrees of freedom that the collinear programs remove by hypothesis, and they turn out to carry the verdict.

Let $\mathbf d=\mathbf X_i-\mathbf X_j$ be the present relative separation, $d=\|\mathbf d\|$, and let $\theta$ parameterize the direction of $\mathbf d$. Define the areal rate

$$h=d^{2}\dot\theta ,$$

a purely kinematic quantity built from positions and velocities alone, with no mass, no force, and no assembly-level bookkeeping.

If the relative acceleration were directed exactly along $\mathbf d$ with magnitude $a(d)$, then $h$ would be constant and the radial evolution would read

$$\ddot d=\frac{h^{2}}{d^{3}}-a(d) .$$

With the near-field magnitude lemma supplying $a(d)\approx C/d^{2}$, the geometric term $h^{2}/d^{3}$ dominates as $d\to0$ for any $h\neq0$. Radial approach therefore reverses at the finite separation $d\approx h^{2}/C$, and coincidence requires $h=0$ exactly.

Plainly: two things that are not aimed exactly at each other sweep around each other faster and faster as they close, and that sweeping wins over the pull. They turn instead of touching. Only a perfectly aimed approach has nothing to turn.

The Master Equation's row is not exactly central, and this is where the delay enters decisively. The admitted direction $\hat{\mathbf r}_{ij}$ points from the transmitter's *delayed* position, not its present one, so the row is misaligned from $\hat{\mathbf d}$ by an angle $\alpha$. The areal rate is then not conserved but obeys $\dot h=d\,a_\perp$ with $a_\perp$ the transverse component.

The size of that misalignment is the first surprise. On the corrected affine first-root geometry above,

$$
\sin\alpha=\frac{\lVert\mathbf p_j\rVert}{c_f},
\qquad
\cos\alpha=\frac{\chi_j}{c_f},
\qquad
\tan\alpha=\frac{\lVert\mathbf p_j\rVert}{\chi_j},
$$

which is **independent of $d$**. The delay-induced misalignment does not shrink on approach; it persists at fixed angle all the way in. The earlier estimate $\alpha\approx v_{j\perp}/(c_fD_t)$ incorrectly replaced the root interval's average slope by its endpoint slope; only its conclusion about $d$-independence survives.

Plainly: as two architrinos close in, the direction each one is pushed stays tilted away from the straight line between them by the same angle no matter how close they get. Getting closer does not straighten the aim.

Now the competition can be posed exactly. The barrier survives while $h^{2}>Cd$, so the threshold that $h$ must clear falls to zero like $\sqrt{d}$, a power law. Meanwhile the transverse row drains or feeds $h$ at rate $|\dot h|\sim\alpha C/d$, and integrating along an approach at radial speed $u$ gives an accumulated change

$$|\Delta h|\;\sim\;\frac{\alpha C}{u}\,\ln\frac{1}{d},$$

which is logarithmic. These scalings alone do not decide whether the barrier survives: the threshold shrinks toward zero while the accumulated transverse change grows without bound, so the sign and state dependence of that change are decisive.

That comparison is necessary but not sufficient, and the reason is the crux of the entire question. A logarithm that accumulates with a fixed sign still reaches any finite magnitude eventually, so a **secular** one-signed drain drives $h$ through zero at the finite separation $d\sim\exp(-h_0u/\alpha C)$, and at that separation the barrier condition $h^{2}>Cd$ has already failed. A drain that merely oscillates — reversing each half turn as the transverse geometry reverses — leaves $h$ bounded and the barrier intact.

Therefore the verdict reduces to a single quantity:

> **Does the delay-induced transverse row carry a secular component in the areal rate $h$, and if so, what is its sign?**

A secular drain removing areal rate makes $h=0$ an attractor and would make coincidence generic for bound opposite-polarity pairs on this local model. A secular gain repels from $h=0$, while a vanishing secular part leaves the central barrier unchanged. Either non-drain outcome supports the exact-aim case locally, but only an invariant-region theorem can establish populated-domain exclusion.

Plainly: the local question has three answers. The delay can bleed away the sideways swing, rock it back and forth with no net change, or steadily enlarge it. Only the first drives the pair toward exact aim.

Claim grade: the central-case barrier $\ddot d=h^{2}/d^{3}-a(d)$ and the reversal at $d\approx h^{2}/C$ are `derived` for exactly central rows. The $d$-independence and displayed trigonometric form of $\alpha$ are `derived` on the affine first-root chart and hold asymptotically under the corrected near-field hypotheses. The power-versus-logarithm comparison is `derived` given those scalings. Reduction of the local pair verdict to the secular part of $\dot h$ is `derived`; extension from that local verdict to the full populated domain remains `inferred` and is not claimed. A populated approach in which coincidence is reached while the leading pair coefficient remains positive would falsify the sufficiency of the local reduction for the global domain question.

The earlier consistency expectation that a characteristic separation required the secular term to vanish or reverse was too narrow. A positive secular pair term is also compatible with non-collapse because it strengthens the transverse barrier; whether a retained bound branch exists still requires its own complete-ledger stability certificate.

## Signed Secular Calculation

The turn average does not vanish on the declared symmetric pair-center control. On that regular first-root chart, the leading mutual opposite-polarity contribution has the same sign as the areal rate itself. It therefore increases $\lvert h\rvert$ rather than draining it.

Write the relative position and velocity as

$$
\mathbf d=d\hat{\mathbf n},
\qquad
\mathbf w=\mathbf v_i-\mathbf v_j,
\qquad
\mathbf h=\mathbf d\times\mathbf w.
$$

The vector $\mathbf h$ is the three-dimensional form of the signed planar areal rate $h=d^2\dot\theta$. Begin with the declared symmetric absolute-frame control $\mathbf v_i=\mathbf w/2$ and $\mathbf v_j=-\mathbf w/2$; this is a physical restriction, not a change of frame. Impose the same local hypothesis used by the near-field magnitude lemma: each transmitter velocity is constant to leading order across the first-root delay. The two ordered roots then have the same delay $\Delta$, delayed range $r=c_f\Delta$, and positive transmitter factor $D_t$. Their delayed line-of-action vectors are opposite. For the row received by $i$,

$$
\widehat{\mathbf r}_{ij}
=
\frac{d\hat{\mathbf n}-\mathbf w\Delta/2}{c_f\Delta},
\qquad
K
=
\frac{\kappa\lvert q_iq_j\rvert c_f}{r^2D_t}.
$$

Opposite polarity gives $\mathbf A_i=-K\widehat{\mathbf r}_{ij}$ and $\mathbf A_j=+K\widehat{\mathbf r}_{ij}$. The mutual relative acceleration is therefore $\mathbf a_{ij}^{\mathrm{rel}}=-2K\widehat{\mathbf r}_{ij}$, and its contribution to the areal-rate derivative is

$$
\begin{aligned}
\dot{\mathbf h}_{ij}
&=
\mathbf d\times\mathbf a_{ij}^{\mathrm{rel}}\\
&=
-2Kd\hat{\mathbf n}\times
\frac{d\hat{\mathbf n}-\mathbf w\Delta/2}{c_f\Delta}\\
&=
\frac{K}{c_f}\mathbf h\\
&=
\frac{\kappa\lvert q_iq_j\rvert}{r^2D_t}\mathbf h.
\end{aligned}
$$

Every scalar multiplying $\mathbf h$ is positive on the declared regular chart. The result is not an oscillatory torque whose two half-turns cancel: reversing the direction of the pair's turn reverses both $h$ and $\dot h$, so the magnitude grows in either orientation. Put $u_r=\hat{\mathbf n}\mathbin{\cdot}\mathbf w$, $\mathbf w_\perp=\mathbf w-u_r\hat{\mathbf n}$, and

$$
\chi=\left(c_f^2-\frac{\lVert\mathbf w_\perp\rVert^2}{4}\right)^{1/2}.
$$

The symmetric affine root has $D_t=\chi(\chi+u_r/2)/c_f^2$ and $r=d\chi/(c_fD_t)$. The exact affine present-separation form, and the leading $C^2$ form, are therefore

$$
\boxed{
\dot h_{ij}^{\mathrm{sec}}
=
+\frac{\kappa\lvert q_iq_j\rvert c_f^2D_t}{\chi^2d^2}\,h
=
+\frac{\kappa\lvert q_iq_j\rvert}{d^2}
\left(1+\frac{u_r}{2\chi}\right)h
}
$$

for affine histories, with a relative $O(d)$ correction under the bounded-acceleration near-field hypotheses. The formerly displayed coefficient $D_t$ is recovered only when the transverse lead is negligible, $\chi/c_f\to1$; the missing factor $c_f^2/\chi^2=\sec^2\alpha$ is positive and cannot reverse the sign.

Hence

$$
\operatorname{sgn}\!\left(\dot h_{ij}^{\mathrm{sec}}\right)
=
\operatorname{sgn}(h),
\qquad
\frac{d\lvert h\rvert}{dT}
=
\frac{\kappa\lvert q_iq_j\rvert c_f^2D_t}{\chi^2d^2}\lvert h\rvert
$$

on the exact affine chart. The leading coefficient is independent of $D_r$. Receiver-side playback never enters the magnitude or sign.

### Why this is repelling rather than attractive

`Transverse` means perpendicular to the instantaneous line joining the pair. Split the relative velocity into a radial part, which changes the separation $d$, and a transverse part, which changes the direction of the separation vector:

$$
\mathbf w
=
\mathbf w_{\parallel}+\mathbf w_{\perp},
\qquad
\mathbf w_{\parallel}=(\mathbf w\mathbin{\cdot}\hat{\mathbf n})\hat{\mathbf n},
\qquad
\mathbf w_{\perp}\mathbin{\cdot}\hat{\mathbf n}=0.
$$

Head-on motion has $\mathbf w_{\perp}=0$ and therefore $h=0$. Any sideways motion has $\mathbf w_{\perp}\ne0$ and

$$
\lvert h\rvert
=
\lvert\mathbf d\times\mathbf w\rvert
=
d\lVert\mathbf w_{\perp}\rVert.
$$

Plainly: radial motion is motion directly toward or away from the other architrino; transverse motion is the sideways miss that makes the joining line rotate.

Here `repelling` describes the behavior of the exact-aim state $h=0$ in the pair's position-velocity state space; it does not mean that the opposite-polarity radial acceleration has become spatially repulsive. To leading order, write

$$
\dot h=\lambda(T)h,
\qquad
\lambda(T)=\frac{\kappa\lvert q_iq_j\rvert}{r^2D_t}>0.
$$

On any interval that remains in the declared regular chart, this scalar equation has the solution

$$
h(T)
=
h(T_0)
\exp\!\left(\int_{T_0}^{T}\lambda(S)\,dS\right).
$$

The exponential factor is greater than one for every forward interval of positive duration. If $h(T_0)=+\varepsilon$, then $h$ becomes more positive. If $h(T_0)=-\varepsilon$, then $h$ becomes more negative. In both cases $\lvert h\rvert$ grows. The exactly aimed state $h=0$ remains a solution of this isolated leading equation, but any arbitrarily small nonzero transverse perturbation moves away from it. That is the precise local meaning of a repelling invariant set here.

The geometry fixes the sign. In the symmetric pair control, the delayed line from transmitter $j$ to receiver $i$ is proportional to

$$
d\hat{\mathbf n}-\frac{\Delta}{2}\mathbf w.
$$

Opposite polarity reverses this line to give the attractive acceleration received by $i$. Its radial part points inward, while its transverse part is $+\mathbf w$-directed. The other ordered row supplies the matching contribution, so the relative transverse acceleration is aligned with the existing relative transverse velocity. A counterclockwise turn is accelerated counterclockwise; a clockwise turn is accelerated clockwise. The causal delay therefore feeds the existing turn rather than damping it.

The radial attraction and phase-space repulsion from exact aim coexist. The inward term tries to reduce $d$, while the transverse term enlarges $\lvert h\rvert$. Since the kinematic barrier scales as $h^2/d^3$, enlarging $\lvert h\rvert$ strengthens the term that reverses radial approach. The derived statement is local and pairwise: it establishes instability of $h=0$ under the leading mutual row on the regular chart, not a global theorem that every populated trajectory moves monotonically away from the full exact-aim manifold.

Claim grade: `derived exactly` for affine symmetric first-root histories with $D_t>0$, and `derived asymptotically` on the corresponding bounded-acceleration $C^2$ chart. Falsifier: a compliant chart with $\lambda>0$ in which a nonzero solution of the displayed leading scalar equation decreases in magnitude, or an exact evaluation of the same two ordered transmitter-side rows yielding a relative transverse acceleration opposite to $\mathbf w_\perp$.

### Does every transverse perturbation prevent coincidence?

For the symmetric pair-center leading equation, yes, within the regular near-field chart: any nonzero $h$ at a finite separation is multiplied by a positive growth factor and is driven away from $h=0$. Exact collinearity of both the relative velocity and the mutual acceleration leaves $h=0$ invariant, but an arbitrarily small transverse velocity perturbation gives $h\ne0$ and activates the gain. The general absolute-frame calculation below corrects the broader reading: center motion can add a term of either sign, so an arbitrary isolated pair need not obey this scalar gain law.

For a populated history, that statement needs one qualification. Collinearity at one instant is not enough: every subsequent relative acceleration contribution must also remain collinear, or its transverse component immediately creates nonzero $h$. Conversely, a transverse perturbation at one instant is not by itself a global exclusion proof, because later third-party rows could be correlated so as to cancel it. The conditional populated-barrier theorem below proves the sharper available result: if the population remainder is bounded as stated there, every monotone approach whose transverse relative velocity eventually lies outside

$$
\lVert\mathbf w_\perp\rVert=O(d^2)
$$

is captured by the protected region and cannot reach coincidence while the hypotheses remain valid. A still-unexcluded approach must make the sideways velocity vanish at least quadratically with separation and must maintain that cancellation throughout the remaining approach.

The phrase “as $h\to0$” requires care because $h=d\lVert\mathbf w_\perp\rVert$ in magnitude. The factor $d\to0$ can make $h\to0$ even when the sideways velocity does not straighten. What matters for reachability is the rate of that decrease. The central barrier threshold is $h^2\sim Cd$, whereas the populated capture result leaves only $\lVert\mathbf w_\perp\rVert=O(d^2)$, equivalently $h=O(d^3)$, unexcluded. Merely having $h\to0$ therefore does not establish exact aim or coincidence.

Plainly: a pair on the symmetric pair-center chart must be perfectly aimed and stay perfectly aimed. In a populated universe, center-motion correlations can either create or drain the sideways miss. The conditional theorem identifies a protected region only when the positive pair-center-like coefficient dominates those correlations; the radial theorem below supplies a separate exclusion route that does not assume that sign.

Claim grade: the symmetric pair-center perturbation statement is `derived` under the signed-secular hypotheses; the $O(d^2)$ populated threshold is `derived` under the conditional theorem's explicit additional bounds. A universal isolated-pair or populated repeller is `not claimed`. Falsifier: a compliant symmetric pair-center solution with nonzero $h$ that reaches coincidence while retaining the positive pair coefficient, or a populated solution satisfying the conditional bounds that reaches coincidence after leaving the stated cone.

### Turn average and magnitude

On a planar turn, $\dot\theta=h/d^2$. Division of the affine instantaneous formula by this kinematic identity gives, at leading near-field order along a slowly varying $C^2$ turn,

$$
\frac{dh}{d\theta}
\approx
\kappa\lvert q_iq_j\rvert\frac{c_f^2D_t}{\chi^2}
.
$$

Consequently one oriented turn contributes

$$
\Delta h_{ij}^{\mathrm{sec}}
\approx
\kappa\lvert q_iq_j\rvert
\oint \frac{c_f^2D_t}{\chi^2}\,d\theta
.
$$

For nearly constant $c_f^2D_t/\chi^2$, the magnitude is $\lvert\Delta h_{ij}^{\mathrm{sec}}\rvert\approx2\pi\kappa\lvert q_iq_j\rvert c_f^2D_t/\chi^2$, with the sign of the incoming $h$. On the lowest-order slow, near-circular symmetric chart, $c_f^2D_t/\chi^2=1+O(\lVert\mathbf v\rVert^2/c_f^2)$, so the leading increment is $2\pi\kappa\lvert q_iq_j\rvert$ per turn. The time average over a period $P$ has the equivalent form

$$
\left\langle\dot h_{ij}\right\rangle_P
\approx
\frac{\kappa\lvert q_iq_j\rvert}{P}
\int_0^P\frac{c_f^2D_t h}{\chi^2d^2}\,dT,
$$

which has the sign of $h$ whenever $h$ does not reverse on the turn.

This also corrects the earlier interpretation of the logarithmic estimate. If the misalignment angle is frozen as an externally prescribed constant, integrating $\lvert\dot h\rvert\sim\alpha C/d$ along a constant-speed radial approach gives the stated logarithm. For the mutual pair, however, the exact affine relation is $\sin\alpha=\lvert h\rvert/(2c_fd)$, with no $D_t$ in that angle. Substitution gives the multiplicative law above. If $\dot d\approx-u<0$ and $c_f^2D_t/\chi^2$ is nearly constant over a radial subinterval, then

$$
\ln\!\frac{\lvert h(d)\rvert}{\lvert h(d_0)\rvert}
\approx
\frac{\kappa\lvert q_iq_j\rvert c_f^2D_t}{u\chi^2}
\left(\frac{1}{d}-\frac{1}{d_0}\right).
$$

The secular term is therefore parametrically stronger than the fixed-angle logarithm on that prescribed approach, but it has the protective sign: positive feedback for $\lvert h\rvert$. The approximation ceases to describe the trajectory once the growing transverse term reverses the radial approach.

The common absolute-translation velocity cancels from the term linear in velocity. The general calculation below shows that quadratic absolute-frame terms survive and can reverse the instantaneous sign outside the pair-center chart. Acceleration rows from other architrinos remain bounded relative to the pair's $d^{-2}$ row so long as no third-party separation or transmitter factor becomes singular, but they can correlate the pair's common motion with its turn and thereby change the full-turn sign.

Plainly: the delayed attractive direction points slightly ahead in the sense of the pair's existing turn. Each architrino is accelerated along its own sideways motion, so the turn grows rather than being bled away. The target $h=0$ is a repelling set for the leading pair contribution, not an attractor.

Claim grade: `derived exactly` on the affine pair-center, first-root, positive-$D_t$ chart and `derived asymptotically` for bounded-acceleration $C^2$ histories with uniform root and $\chi$ margins. Falsifier: a compliant opposite-polarity chart on which the complete two-row mutual contribution has $\mathbf h\cdot\dot{\mathbf h}<0$, or a full turn with fixed sign of $h$ and $D_t>0$ whose mutual contribution satisfies $\Delta\lvert h\rvert\le0$.

### General absolute-frame correction

The pair-center restriction cannot be removed by calling it a frame choice, because absolute time and the fixed wake speed distinguish the substrate frame. The leading pair torque must therefore be recomputed with arbitrary present velocities.

Let

$$
\mathbf v_i=a_i\hat{\mathbf n}+\mathbf p_i,
\qquad
\mathbf v_j=a_j\hat{\mathbf n}+\mathbf p_j,
\qquad
\mathbf p_i\mathbin{\cdot}\hat{\mathbf n}
=
\mathbf p_j\mathbin{\cdot}\hat{\mathbf n}=0,
$$

and define $\chi_i=(c_f^2-\lVert\mathbf p_i\rVert^2)^{1/2}$ and $\chi_j=(c_f^2-\lVert\mathbf p_j\rVert^2)^{1/2}$. On locally affine sub-field transmitter histories, the two first-root delays and directions are

$$
\Delta_{j\to i}=\frac{d}{\chi_j-a_j},
\qquad
\hat{\mathbf r}_{ij}=\frac{\chi_j\hat{\mathbf n}+\mathbf p_j}{c_f},
$$

$$
\Delta_{i\to j}=\frac{d}{\chi_i+a_i},
\qquad
\hat{\mathbf r}_{ji}=\frac{-\chi_i\hat{\mathbf n}+\mathbf p_i}{c_f}.
$$

The associated normalized transmitter factors are

$$
D_{t,j\to i}=\frac{\chi_j(\chi_j-a_j)}{c_f^2},
\qquad
D_{t,i\to j}=\frac{\chi_i(\chi_i+a_i)}{c_f^2}.
$$

Substitution into the two transmitter-side rows gives the complete leading mutual contribution

$$
\boxed{
\dot{\mathbf h}_{ij}
=
\frac{\kappa\lvert q_iq_j\rvert}{d}
\hat{\mathbf n}\times
\left[
\left(1+\frac{a_i}{\chi_i}\right)\mathbf p_i
-
\left(1-\frac{a_j}{\chi_j}\right)\mathbf p_j
\right]
}
$$

for affine histories. For $C^2$ histories with bounded acceleration and uniform root margins, the displayed term is the $d^{-1}$ leading part and the omitted areal-rate derivative is $O(1)$. Writing $\mathbf w_\perp=\mathbf p_i-\mathbf p_j$ separates the pair-center term from the absolute-frame correction:

$$
\dot{\mathbf h}_{ij}
=
\frac{\kappa\lvert q_iq_j\rvert}{d}
\hat{\mathbf n}\times
\left[
\mathbf w_\perp
+\frac{a_i}{\chi_i}\mathbf p_i
+\frac{a_j}{\chi_j}\mathbf p_j
\right].
$$

Specializing this formula to $a_i=u_r/2$, $a_j=-u_r/2$, $\mathbf p_i=\mathbf w_\perp/2$, and $\mathbf p_j=-\mathbf w_\perp/2$ reduces its bracket to $(1+u_r/(2\chi))\mathbf w_\perp$, exactly reproducing the corrected symmetric coefficient. This agreement is an internal algebraic consistency check, not independent evidence.

The first term is the positive feedback derived above. The other two terms have no fixed sign. In particular, at exact relative aim $\mathbf p_i=\mathbf p_j=\mathbf P$, let $A=\hat{\mathbf n}\mathbin{\cdot}(\mathbf v_i+\mathbf v_j)/2$. Then $\chi_i=\chi_j=\chi$ and

$$
\left.\dot{\mathbf h}_{ij}\right|_{\mathbf h=0}
=
\frac{2\kappa\lvert q_iq_j\rvert A}{d\chi}
\hat{\mathbf n}\times\mathbf P.
$$

Thus $\mathbf h=0$ is not even invariant for a generic absolute-frame pair: common motion having both radial and transverse components immediately creates areal rate. It remains invariant at this order only on the union $A=0$ or $\mathbf P=\mathbf0$, absent cancellation by other rows.

On a planar segment write $\mathbf w_\perp=s\hat{\mathbf t}$, where the signed $s=d\dot\theta$ is nonzero, and let

$$
b_c=\hat{\mathbf t}\mathbin{\cdot}\left(
\frac{a_i}{\chi_i}\mathbf p_i+
\frac{a_j}{\chi_j}\mathbf p_j
\right).
$$

Then the leading contribution per unit turn angle is

$$
\boxed{
\frac{dh_{ij}}{d\theta}
=
\kappa\lvert q_iq_j\rvert
\left(1+\frac{b_c}{s}\right).
}
$$

This displays both the sign and the magnitude obstruction to a universal turn average. The pair-center term is the constant positive $1$; the center-motion correction is the uncontrolled ratio $b_c/s$. It can dominate when the relative turn rate is small, so no history-independent full-turn magnitude follows from the canonical row alone.

The pointwise sign is also not universal. In normalized units set $\hat{\mathbf n}=\mathbf e_x$, $\mathbf p_i=0.1\mathbf e_y$, $\mathbf p_j=0.2\mathbf e_y$, $a_i=0.8\sqrt{0.99}$, and $a_j=0.95\sqrt{0.96}$. The two speeds are $0.8022468448$ and $0.9520504188$, the radial relative velocity is $-0.1348161526$, and the transverse bracket in the boxed equation is $+0.17\mathbf e_y$. Therefore

$$
\mathbf h=-0.1d\,\mathbf e_z,
\qquad
\dot{\mathbf h}_{ij}
=
+\frac{0.17\kappa\lvert q_iq_j\rvert}{d}\mathbf e_z,
\qquad
\mathbf h\mathbin{\cdot}\dot{\mathbf h}_{ij}
=
-0.017\kappa\lvert q_iq_j\rvert<0.
$$

This is an approaching, strictly sub-field, locally affine counterexample to any unqualified pointwise gain claim. It does not decide the turn average because it is only a local prescribed-history control.

A full-turn prescribed control shows that the correction can also be secular. Set $c_f=1$, let $d$ be constant, let $\hat{\mathbf n}$ and its planar tangent $\hat{\mathbf t}$ rotate with relative transverse speed $s=k\varepsilon^2$, and prescribe the pair-center velocity

$$
\mathbf V=-a\varepsilon\hat{\mathbf n}+p\varepsilon\hat{\mathbf t},
\qquad
0<k<2ap.
$$

The two velocities are $\mathbf v_i=\mathbf V+(s/2)\hat{\mathbf t}$ and $\mathbf v_j=\mathbf V-(s/2)\hat{\mathbf t}$. They are strictly sub-field for sufficiently small $\varepsilon$, and the center path closes after one turn because the integrals of $\hat{\mathbf n}$ and $\hat{\mathbf t}$ over that turn vanish. The causal delay is $O(d)$ while the direction changes only by $O(\varepsilon^2)$ across it. Expanding the exact two roots therefore gives

$$
\dot h_{ij}
=
\frac{\kappa\lvert q_iq_j\rvert}{d}
\left[(k-2ap)\varepsilon^2+O(\varepsilon^3)\right],
$$

and, since $\dot\theta=k\varepsilon^2/d$,

$$
\boxed{
\Delta h_{ij}^{\mathrm{turn}}
=
2\pi\kappa\lvert q_iq_j\rvert
\left(1-\frac{2ap}{k}+O(\varepsilon)\right)<0
}
$$

for sufficiently small $\varepsilon$. The complete pair contribution can therefore supply a secular drain on a non-pair-center prescribed turn. This does not prove that the populated EOM realizes the prescribed center path, but it disproves a universal positive-sign theorem based on polarity alone.

The corrected transverse range factor does not alter this negative leading term. The periodic calculation was obtained from the exact arbitrary-velocity root formula rather than from $r=d/D_t$, and its $\chi_i$ and $\chi_j$ satisfy $c_f^2/\chi_{i,j}^2=1+O(\varepsilon^2)$. The first correction from that factor is therefore beyond the displayed $O(\varepsilon^2)$ torque coefficient and is absorbed by the stated $O(\varepsilon)$ remainder in the normalized turn increment.

For fixed $a$, $p$, and $k$, this negative term is $O(\kappa\lvert q_iq_j\rvert)$ per turn and is not suppressed by a positive power of $d$. It accumulates linearly with the number of turns rather than averaging to zero, so it cannot be discarded as smaller than the earlier logarithmic estimate merely from near-field scaling. If an EOM-evolved bound family retained the displayed negative coefficient, it could cross $h=0$ after finitely many turns. No such EOM family is established here; the exact reachability construction instead begins and remains at $h=0$ inside a recent-partner-root causal shadow.

Claim grade: the arbitrary-velocity affine formula and its local numerical instance are `derived`; the periodic small-$\varepsilon$ turn asymptotic is `derived` for the displayed prescribed family. Their EOM reachability is `not claimed`. Falsifier: direct exact-root evaluation of the displayed affine data producing a nonnegative $\mathbf h\mathbin{\cdot}\dot{\mathbf h}_{ij}$, or an exact-root sequence on the periodic family whose normalized one-turn contribution fails to approach $2\pi\kappa\lvert q_iq_j\rvert(1-2ap/k)$ as $\varepsilon\to0$.

### Decider verdict

The positive secular result is valid on the symmetric pair-center chart and is measured with the same sign on the bounded EOM arc below. It is not the universal decider originally sought. The general absolute-frame formula contains a sign-indefinite drift term, and the periodic prescribed family makes that term a negative full-turn secular contribution. Polarity alone therefore does not determine whether the mutual pair row drains or amplifies $h$ on an arbitrary populated turn.

The magnitude is therefore $O(\!\left(\kappa\lvert q_iq_j\rvert c_f^2D_t\lvert h\rvert/(\chi^2d^2)\right)$ on the symmetric near-field chart and $O(\kappa\lvert q_iq_j\rvert)$ per turn at its lowest slow, near-circular symmetric order. The general locally affine torque is $O(\kappa\lvert q_iq_j\rvert V/d)$ for a declared velocity scale $V$, but its per-angle magnitude contains the uncontrolled ratio $b_c/s$ and has no universal bound. In the displayed periodic family with fixed $a$, $p$, and $k$, the full-turn increment is $O(\kappa\lvert q_iq_j\rvert)$ and can have either sign. This correction changes the closure burden. A universal exact-aim repeller or attractor cannot be inferred from polarity. The radial first-exit theorem below supplies a sign-independent exclusion on uniformly sub-field-speed regular charts, while the populated causal-shadow theorem supplies an exact EOM reachability example outside that class. The global outcome is consequently neither branch of the original dichotomy: coincidence is mathematically reachable but locally nongeneric on the exhibited super-field chart, and no derived secular mechanism attracts an open set of populated histories to $h=0$.

## Conditional Populated-Barrier Theorem

The statement that a populated universe continually perturbs every pair can be made quantitative without treating the perturbations as random. Let $H=\lVert\mathbf h\rVert$, and suppose an approaching opposite-polarity pair remains on a regular near-field interval $0<d\le d_\ast$ on which its exact areal-rate equation can be decomposed as

$$
\dot{\mathbf h}
=
\lambda(T)\mathbf h+\boldsymbol\eta(T),
\qquad
\lambda(T)\ge\frac{L}{d^2},
\qquad
\lVert\boldsymbol\eta(T)\rVert\le Bd,
$$

for constants $L>0$ and $B\ge0$. The first term is an additional pair-center-like dominance hypothesis, not a consequence of polarity alone: the general absolute-frame correction above shows that arbitrary center motion need not admit it. The remainder $\boldsymbol\eta$ contains third-party transverse contributions and any controlled higher-order pair remainder. A finite population with uniformly bounded charge products and root count, whose third-party root ranges and transmitter factors have positive lower bounds, supplies a bounded relative third-party acceleration; crossing that bounded acceleration with $\mathbf d$ gives precisely the $Bd$ torque bound. Assume also the finite relative-speed bound $\lVert\mathbf w\rVert\le V$.

For any $\beta>0$, define

$$
Q_\beta=H^2-\beta d.
$$

On the boundary $Q_\beta=0$, where $H=\sqrt{\beta d}$,

$$
\begin{aligned}
\dot Q_\beta
&=
2\mathbf h\mathbin{\cdot}\dot{\mathbf h}-\beta\dot d\\
&\ge
\frac{2L\beta}{d}
-2B\sqrt{\beta}\,d^{3/2}
-\beta V.
\end{aligned}
$$

Choose $d_\ast>0$ small enough that

$$
\frac{2L\beta}{d_\ast}
>
2B\sqrt{\beta}\,d_\ast^{3/2}+\beta V.
$$

Then $\dot Q_\beta>0$ everywhere on $Q_\beta=0$ with $0<d\le d_\ast$. A first-crossing argument proves that the region

$$
\mathcal R_{\beta,d_\ast}
=
\left\{0<d\le d_\ast:\ H^2\ge\beta d\right\}
$$

is forward invariant across its $Q_\beta=0$ boundary while the declared bounds remain valid. Moreover, $H=\lVert\mathbf d\times\mathbf w\rVert\le dV$, so every state in this region obeys

$$
d\ge\frac{\beta}{V^2}>0.
$$

This is a genuine local domain-exclusion theorem: a pair inside $\mathcal R_{\beta,d_\ast}$ cannot reach coordinate coincidence before leaving the regular chart or violating one of the stated population bounds. It is conditional rather than MEC-008-complete because the current corpus has not yet proved the decomposition and constants uniformly over every admissible populated history.

### Capture outside the shrinking exact-aim cone

The theorem also identifies how fine the unprotected aim must become. On a monotone approach with $\dot d\le0$, the norm inequality gives

$$
\dot H
\ge
\frac{L}{d^2}H-Bd.
$$

For $B>0$, set $\Gamma=2B/L$. At the boundary $H=\Gamma d^3$,

$$
\dot H\ge Bd,
\qquad
\frac{d}{dT}\left(\Gamma d^3\right)=3\Gamma d^2\dot d\le0.
$$

Therefore an approaching solution that satisfies $H\ge\Gamma d^3$ at one time cannot cross back below that threshold. Within it,

$$
\dot H\ge\frac{L}{2d^2}H.
$$

Using $-\dot d\le V$ along the monotone approach gives

$$
H(d)
\ge
H(d_0)
\exp\!\left[
\frac{L}{2V}
\left(\frac{1}{d}-\frac{1}{d_0}\right)
\right].
$$

Thus $H^2/d$ must cross any prescribed positive $\beta$ before $d$ can vanish, after which the invariant tube supplies the positive separation floor. When $B=0$, the same growth conclusion holds from any $H>0$, leaving only exact aim $H=0$ unprotected. For $B>0$, any still-unexcluded approach must remain inside

$$
H<\Gamma d^3,
\qquad
\lVert\mathbf w_\perp\rVert=\frac{H}{d}<\Gamma d^2.
$$

Plainly: on a populated trajectory satisfying the displayed positive-coefficient decomposition, a small transverse miss is not enough to reach coincidence. The miss must be cancelled until the transverse relative velocity shrinks at least quadratically with separation. Any perturbation asymptotically larger than that scale is captured by the stipulated positive pair-center-like term and moved into the protected region.

This result gives rigorous content to the populated-universe intuition but does not convert it into probability. No probability measure on retained histories has been defined, so `almost surely` is not an available claim. A deterministic population could in principle correlate its rows so that $\boldsymbol\eta$ holds the pair inside the $O(d^2)$ cone, or another root range or transmitter factor could lose its positive margin. Those cases are now the entire remaining pairwise reachability target rather than an unstructured appeal to perturbations.

Claim grade: the invariant-tube and capture statements are `derived` from the displayed quantitative hypotheses. Their application to every populated Master Equation history is `not claimed`; the general correction proves that no universal positive $L$ follows from polarity alone. The remaining use of this theorem is to identify and certify EOM subclasses on which the decomposition actually holds. Falsifier: a solution satisfying the displayed decomposition, bounds, and monotone-approach hypotheses that crosses from $Q_\beta\ge0$ to $Q_\beta<0$, or that begins with $H\ge\Gamma d^3$ and reaches $d=0$ without a declared chart or margin failure.

## Uniform Sub-Field Radial First-Exit Theorem

The shrinking exact-aim cone can be excluded on a broader regular chart without assuming any sign for $\dot{\mathbf h}$. The argument uses the radial acceleration and the finite relative-speed bound directly.

Fix $0\le\nu<1$. Suppose that, on a candidate close-approach interval, each ordered newest partner root exists and every transmitter velocity on its emission-to-reception interval satisfies

$$
\lVert\mathbf v\rVert\le\nu c_f.
$$

For the row from $j$ to $i$, define the mean transmitter velocity over its delay by

$$
\bar{\mathbf v}_j
=
\frac{1}{\Delta_j}
\int_{T-\Delta_j}^{T}\mathbf v_j(S)\,dS
=
a_j\hat{\mathbf n}+\mathbf p_j,
\qquad
\mathbf p_j\mathbin{\cdot}\hat{\mathbf n}=0,
$$

and let $\chi_j=(c_f^2-\lVert\mathbf p_j\rVert^2)^{1/2}$. The root equation itself, with no constant-velocity approximation, gives

$$
\Delta_j=\frac{d}{\chi_j-a_j},
\qquad
r_j=\frac{c_fd}{\chi_j-a_j},
\qquad
\hat{\mathbf r}_{ij}\mathbin{\cdot}\hat{\mathbf n}
=
\frac{\chi_j}{c_f}>0.
$$

The uniform speed margin implies

$$
r_j\le\frac{d}{1-\nu},
\qquad
\hat{\mathbf r}_{ij}\mathbin{\cdot}\hat{\mathbf n}
\ge\sqrt{1-\nu^2},
\qquad
D_{t,j\to i}\le1+\nu.
$$

The same bounds hold for the opposite ordered row relative to its reversed baseline. Opposite polarity makes both radial projections inward in the relative acceleration. Their sum therefore satisfies

$$
\hat{\mathbf n}\mathbin{\cdot}\mathbf a_{ij}^{\mathrm{near}}
\le
-\frac{C_\nu}{d^2},
$$

where

$$
C_\nu
=
2\kappa\lvert q_iq_j\rvert c_f
\frac{(1-\nu)^2\sqrt{1-\nu^2}}{1+\nu}
>0.
$$

This lower magnitude bound uses only the exact root geometry, the transmitter-side row, and the uniform sub-field margin. It does not use the pair-center restriction or the sign of the transverse contribution.

Put every other retained row, including older partner roots, into a relative-acceleration remainder $\mathbf a_{\mathrm{rem}}$ and assume the complete root census gives

$$
\lVert\mathbf a_{\mathrm{rem}}\rVert\le M,
\qquad
\lVert\mathbf w\rVert\le V.
$$

With radial relative speed $u=\dot d=\hat{\mathbf n}\mathbin{\cdot}\mathbf w$, the exact kinematic identity is

$$
\dot u
=
\frac{\lVert\mathbf w_\perp\rVert^2}{d}
+\hat{\mathbf n}\mathbin{\cdot}\dot{\mathbf w}.
$$

Consequently

$$
\dot u
\le
\frac{V^2}{d}
-\frac{C_\nu}{d^2}
+M.
$$

Choose $d_\nu>0$ so that

$$
V^2d_\nu+Md_\nu^2\le\frac{C_\nu}{2}.
$$

Then every state with $0<d\le d_\nu$ obeys

$$
\dot u\le-\frac{C_\nu}{2d^2}.
$$

On any inward segment beginning at $(d_0,u_0)$ with $d_0\le d_\nu$ and $u_0\le0$, the radial speed becomes strictly more negative. Since $d(u^2)/dd=2\dot u$ while $u<0$, integration toward smaller $d$ gives

$$
u(d)^2
\ge
u_0^2
+C_\nu
\left(
\frac{1}{d}-\frac{1}{d_0}
\right).
$$

The assumed speed bound $u^2\le V^2$ therefore enforces the explicit floor

$$
\boxed{
d
\ge
\left[
\frac{1}{d_0}
+\frac{V^2-u_0^2}{C_\nu}
\right]^{-1}
>0.
}
$$

If the trajectory enters the small-$d$ region with $u\ge0$, it is not approaching coincidence. To reach a smaller separation it must later begin an inward segment there, after which the same estimate applies. Thus no solution can reach $d=0$ while all the displayed chart hypotheses remain valid.

This is a first-exit result, not a rebound law. The inward inverse-square contribution demands a radial-speed change larger than the chart's finite bound before contact. A candidate boundary-reaching history must therefore lose at least one hypothesis first: the newest-root transmitter histories lose every uniform sub-field margin, the complete root chart loses a range or transmitter-factor margin, another retained row becomes unbounded through a simultaneous close approach or singular root, or the relative speed ceases to be bounded. In particular, if both path velocities extend continuously to a proposed pair-coincidence event with values strictly below $c_f$ and every other row remains regular, compactness supplies some $\nu<1$ and the event is impossible.

Plainly: on an ordinary regular sub-field approach, the inward pair acceleration accumulates too strongly for a finite-speed path to arrive at zero separation. The path must encounter a speed, root, or multi-body singular boundary first. This extends MEC-007's one-dimensional first-exit mechanism to noncollinear populated motion, but it does not say what happens after that earlier boundary.

Claim grade: `derived` on the displayed uniform-sub-field, complete-root, bounded-remainder chart. It excludes coordinate coincidence inside that chart and closes the previously remaining $O(d^2)$ cone there, but it is not a global MEC-008 domain exclusion because individual Architrino histories can contain field-speed or super-field-speed sectors and simultaneous singular rows. Falsifier: an exact EOM solution satisfying all displayed bounds that crosses below the boxed floor, or a direct causal-root evaluation under the stated speed margin whose newest opposite-polarity pair rows fail the radial $C_\nu/d^2$ bound.

### Locally affine root-existence classification

The complement of the radial theorem is not merely an unspecified high-speed region. On an affine transmitter segment it can be classified exactly. For a receiver-transmitter present baseline $d\hat{\mathbf n}$, decompose the transmitter velocity as

$$
\mathbf v=a\hat{\mathbf n}+\mathbf p,
\qquad
\mathbf p\mathbin{\cdot}\hat{\mathbf n}=0.
$$

The positive-delay root equation is

$$
\left\lVert d\hat{\mathbf n}+\Delta\mathbf v\right\rVert^2
=c_f^2\Delta^2,
$$

or

$$
d^2+2da\Delta+
\left(a^2+\lVert\mathbf p\rVert^2-c_f^2\right)\Delta^2=0.
$$

When $\lVert\mathbf p\rVert<c_f$, put $\chi=(c_f^2-\lVert\mathbf p\rVert^2)^{1/2}$. The polynomial factors as

$$
\left[d+(a-\chi)\Delta\right]
\left[d+(a+\chi)\Delta\right]=0.
$$

The number of positive local roots is therefore:

| Transmitter radial component | Positive roots | Local meaning |
| --- | ---: | --- |
| $a>\chi$ | $0$ | super-field motion toward the receiver outruns its recent wake |
| $-\chi<a<\chi$ | $1$ | sub-field transmitter |
| $a<-\chi$ | $2$ | super-field motion away from the receiver leaves two recent intersections |
| $a=\chi$ | $0$ | field-speed toward boundary; the sub-field root has receded to infinite delay |
| $a=-\chi$ | $1$ | field-speed away boundary; one finite root remains and the other is at infinite delay |

For $a<-\chi$, both positive roots must be retained. Their directions and transmitter factors have opposite radial signs,

$$
\hat{\mathbf r}_{+}=\frac{\chi\hat{\mathbf n}+\mathbf p}{c_f},
\qquad
D_{t,+}=\frac{\chi(\chi-a)}{c_f^2}>0,
$$

$$
\hat{\mathbf r}_{-}=\frac{-\chi\hat{\mathbf n}+\mathbf p}{c_f},
\qquad
D_{t,-}=\frac{\chi(\chi+a)}{c_f^2}<0.
$$

Although one opposite-polarity row points radially outward, the complete two-root radial sum is still inward:

$$
\hat{\mathbf n}\mathbin{\cdot}
\left(\mathbf A_{+}+\mathbf A_{-}\right)
=-\frac{2\kappa\lvert q_iq_j\rvert\chi}{d^2}.
$$

Thus a regular super-field receding branch does not by itself cancel the inverse-square first-exit mechanism. The coefficient can nevertheless lose a uniform lower bound as $\chi\to0$, and any incomplete census that moves the older of these two roots into a bounded remainder is invalid because that root also scales as $d^{-2}$.

If $\lVert\mathbf p\rVert=c_f$ and $a<0$, the two algebraic roots merge at $\Delta=-d/a$ and $D_t=0$; this is a singular fold, not a regular passage. If $\lVert\mathbf p\rVert>c_f$, the discriminant is negative and there is no local root. The root-free sector also occurs for $\lVert\mathbf p\rVert<c_f$ with $a>\chi$.

This last sector gives an explicit local coincidence channel not covered by the radial theorem. Take $c_f=1$ and prescribe two affine paths with separation $d(T)>0$ along $\hat{\mathbf n}$ and velocities

$$
\mathbf v_i=-U\hat{\mathbf n},
\qquad
\mathbf v_j=+U\hat{\mathbf n},
\qquad
U>1.
$$

They reach $d=0$ at finite absolute time. For the $j\to i$ row, $a_j=U>1=\chi_j$; relative to the reversed baseline, the $i\to j$ row has the same inequality. Neither ordered pair has a positive-delay root on the affine approach segment, so the mutual acceleration kernel supplies no near-field barrier there. This is a prescribed root-free local control, not an EOM-evolved reachability certificate: older retained roots and population rows can still alter the paths before contact.

The classification narrows the regular pairwise escape route. Subject to bounded accelerations, a coincidence candidate with a continuously limiting affine germ must either have no recent partner root in both ordered directions, approach a zero-coefficient or $D_t=0$ boundary, or acquire another singular row capable of cancelling the inward sum. A symmetric super-field head-on approach lies in the first category. The next EOM control should therefore target that causal-shadow geometry rather than repeat a sub-field near-field approach already excluded by the theorem.

Claim grade: the affine root count, the complete two-root radial sum, and the displayed root-free collision germ are `derived`. EOM realization in a populated retained history is `not claimed`. Falsifier: direct factorization or exact root enumeration of the displayed affine histories producing a different positive-root count, a complete two-root opposite-polarity radial sum other than $-2\kappa\lvert q_iq_j\rvert\chi/d^2$, or a positive-delay partner root on the displayed $U>1$ inbound germ.

### A bounded super-field history necessarily carries a self root

The recent partner-root causal shadow does not make the complete ledger empty. Let one path be differentiable at reception time $T$ with $\lVert\mathbf V(T)\rVert>c_f$, and define the same-transmitter chord residual

$$
F(\tau)
=
\left\lVert\mathbf X(T)-\mathbf X(T-\tau)\right\rVert-c_f\tau.
$$

Differentiability gives

$$
\lim_{\tau\downarrow0}\frac{F(\tau)}{\tau}
=
\lVert\mathbf V(T)\rVert-c_f>0.
$$

If the retained past contains some $L>0$ for which

$$
\left\lVert\mathbf X(T)-\mathbf X(T-L)\right\rVert<c_fL,
$$

then $F(L)<0$. Continuity therefore supplies at least one positive-delay self root in $(0,L)$. In particular, a path whose retained spatial image has diameter at most $B$ has a self root whenever its super-field reception state is accompanied by retained depth $L>B/c_f$.

The result is only existence; it does not say the self root is singular. A constant-speed circle gives a regular example. For radius $R$ and speed $\beta c_f$ with $1<\beta<\pi$, put $x=\beta c_f\tau/(2R)$. The nonzero self-root equation is

$$
\sin x=\frac{x}{\beta},
\qquad
0<x<\pi.
$$

It has one root in that interval, and its transmitter factor is

$$
D_t=1-\beta\cos x=1-x\cot x>0,
$$

because $\sin x-x\cos x>0$ for $0<x<\pi$. Thus bounded super-field motion can lie on a regular self-root chart even though it cannot be root-free as a complete bounded history.

This closes one false shortcut. A bi-infinite affine super-field path has no self root, but it escapes every bounded spatial envelope. A bounded retained history can support the inbound mutual causal shadow, but its self rows must be included and may affect the collision. Neither observation by itself excludes reachability.

Claim grade: the bounded-history self-root existence result and the circular regular-root example are `derived`. Falsifier: a differentiable path satisfying the displayed strict endpoint inequalities with no zero of $F$ in $(0,L)$, or a circle in the stated $1<\beta<\pi$ range whose displayed nonzero root has $D_t\le0$.

### Regular-chart DDE local-existence lemma

The delay character does not obstruct local evolution before a root or range margin fails. Take a finite retained interval and suppose its complete ordered root census has finitely many roots, every active root has $r\ge r_0>0$ and $\lvert D_t\rvert\ge\delta_0>0$, every root emission time has a positive margin from the retained-history boundary, every inactive root-search cell has a positive residual gap, and the retained paths are $C^1$ with a locally bounded derivative. A **simple root** means precisely that the derivative of the scalar root residual with respect to emission time is nonzero; here that derivative is proportional to $D_t$.

The implicit-function theorem then makes every active emission time a locally unique continuous function of the nearby receiver state and retained path. The inactive residual gap prevents an undeclared root from appearing under a sufficiently small perturbation. Because the row denominators stay above $r_0^2\delta_0$, each transmitter-side acceleration contribution is locally Lipschitz in that neighborhood, and so is their finite sum. Rewriting the second-order update as a first-order history system gives the usual short method-of-steps interval: the already retained past determines one unique local extension until one of the declared margins is first lost.

This lemma does not cross coincidence, a fold, a memory boundary, or a root birth. Its role is narrower: regular self roots on a bounded super-field prehistory are not by themselves a local-existence obstruction. They contribute bounded rows to the $M$ used in the crossing estimate below as long as their margins persist.

Claim grade: `derived` by the implicit-function and local-contraction arguments under the displayed finite-census and uniform-margin hypotheses. Falsifier: a retained history satisfying every displayed margin for which an active root is not locally unique, an inactive root appears without closing its residual gap, or the resulting finite acceleration functional admits no unique short extension before any margin loss.

### Symmetric populated embedding target

The root-free germ can be embedded in a minimally noncollinear symmetry class without assuming that environmental acceleration vanishes. Let $S(x,y,z)=(-x,y,z)$ and let $\pi$ exchange labels in opposite-polarity pairs. Prescribe retained histories satisfying

$$
\mathbf X_{\pi(k)}(T)=S\mathbf X_k(T),
\qquad
q_{\pi(k)}=-q_k.
$$

Spatial reflection preserves every distance and dot product in the causal-root condition and in $D_t$, while flipping every polarity preserves each pair product $q_iq_j$. The transmitter-side update is therefore equivariant under the combined map. On any unique regular evolution chart, the symmetry persists. For the target pair this gives

$$
\mathbf X_2(T)=S\mathbf X_1(T).
$$

Write $\mathbf X_1=(x,y,z)$, so the pair separation is $(2x,0,0)$ even though additional paired architrinos may occupy off-axis positions. Suppose at the evolution cut

$$
x(0)=x_0>0,
\qquad
\dot x(0)=-U,
\qquad
U>c_f,
$$

and suppose the complete regular EOM evolution exists through $0\le T\le2x_0/U$ with $\lvert\ddot x\rvert\le M$. Then

$$
x\!\left(\frac{2x_0}{U}\right)
\le
-x_0+\frac{2Mx_0^2}{U^2}.
$$

For $M=0$, or for $x_0<U^2/(2M)$ when $M>0$, the right side is negative. Continuity then forces some $T_\ast\in(0,2x_0/U)$ with $x(T_\ast)=0$, and the preserved symmetry gives $\mathbf X_1(T_\ast)=\mathbf X_2(T_\ast)$.

The recent partner-root exclusion is exact on this evolving symmetry chart, not merely affine. If $-\dot x(S)>c_f$ throughout the approach, the $x$ component of the delayed displacement for the $2\to1$ row is

$$
2x(T)+\int_{T-\Delta}^{T}-\dot x(S)\,dS
>
c_f\Delta
$$

for every $\Delta>0$ whose transmitter interval lies inside the approach chart. Its Euclidean norm is therefore strictly greater than $c_f\Delta$, so no such delay is a root. Reflection gives the same result for the $1\to2$ row. All acceleration entering the bound then comes from older roots and the off-axis population rather than from a collapsing recent partner row.

The conditional premises can be realized by an explicit complete retained history. Set $c_f=1$, give every Architrino unit charge magnitude, put the target pair at $(\pm 0.01,0,0)$ with charges $\pm1$, and put an environmental opposite-polarity pair at $(\pm1,1.5,0)$ with the same right-positive, left-negative assignment. The environmental pair is stationary on $[-5.5,0]$. For the right target, write $s=T+1$ and prescribe

$$
\mathbf X_+(T)=
\begin{cases}
(2,0,0), & -5.5\le T\le-1,\\
\bigl(f(s),0,0\bigr), & -1\le T\le0,
\end{cases}
\qquad
f(s)=2-3.97s^2+1.98s^3,
$$

with the left target obtained by reflection and polarity reversal. The stationary shelves are declared to extend for all $T\le-1$; the represented retained suffix begins at $-5.5$. This history is $C^1$, $f(1)=0.01$, $f'(1)=-2$, and every point lies inside the radius-$2.5$ ball. Its retained depth $5.5$ exceeds the diameter-$5$ causal bound by $0.5$, so no earlier emission from the declared bounded prehistory can reach a receiver event in the stated forward window.

At $T=0$, every nonempty positive-delay root emits on a stationary shelf. The complete ordered census has fourteen simple roots and two empty stationary self channels:

| Receiver | transmitter | root delay |
| --- | --- | ---: |
| target $+$ | target $+$ | $1.99$ |
| target $+$ | target $-$ | $2.01$ |
| target $+$ | environment $+$ | $\sqrt{3.2301}$ |
| target $+$ | environment $-$ | $\sqrt{3.2701}$ |
| target $-$ | reflected sources | the reflected four values above |
| environment $+$ | target $+$ | $\sqrt{3.25}$ |
| environment $+$ | target $-$ | $\sqrt{11.25}$ |
| environment $+$ | environment $+$ | no positive root |
| environment $+$ | environment $-$ | $2$ |
| environment $-$ | reflected sources | the reflected four values above |

All listed emission times lie strictly inside $[-5.5,-1]$, all their transmitter velocities vanish, and hence every active root has $D_t=1$. There are no omitted roots on the joining cubic. For an environmental receiver and a target transmitter, the spatial distance is at least $1.5$ while the joining-segment delay is at most $1$. For a target receiver and its own joining segment, the residual is

$$
g_{\mathrm{self}}(s)=f(s)-0.01-(1-s)=(s-1)(1.98s^2-1.99s-0.99)>0,
\qquad 0\le s<1,
$$

because the quadratic factor is negative on $[0,1]$. For the opposite target transmitter the residual is $g_{\mathrm{cross}}(s)=f(s)+0.01-(1-s)$; its only interior critical point is a maximum, and its endpoint values are $1.01$ and $0.02$, so it too is positive. Stationary environmental transmitters have exactly the single distance-equals-delay root displayed in the table. This exhausts all sixteen ordered channels.

Choose $\kappa=10^{-6}$. On the positive-separation bootstrap tube $0\le T\le0.005$ in which every receiver remains within $0.1$ of its initial position, every positive-delay emission remains on a stationary shelf, every active $D_t$ remains $1$, and every active range exceeds $1.6$. With at most four rows per receiver, the transmitter-side update therefore gives the deliberately loose bound

$$
\lVert\mathbf A_i\rVert
\le
\frac{4\times10^{-6}}{1.6^2}
<2\times10^{-6}.
$$

The resulting velocity change is below $10^{-8}$ and the acceleration-induced position change is below $2.5\times10^{-11}$ on that interval, closing the assumed tube. The joining-segment exclusions also persist. While $x>0$, the target cross residual is at least $0.02-0.01-0.005=0.005$; the target self residual across the join gains at least $(1-10^{-8})T$ from the inward motion; and an environmental receiver remains at least $1.4$ from a target joining segment while its delay is at most $1.005$.

The two same-transmitter channels also stay root-free on their newly evolved segments. For a target, the $x$ displacement over every future subinterval of length $\delta$ exceeds $(2-10^{-8})\delta>\delta$, so its chord cannot satisfy the unit-field-speed root equation. For an environmental Architrino, the speed is below $10^{-8}$, so every future chord has length below $10^{-8}\delta<\delta$; across $T=0$, its displacement from the stationary prehistory is at most $MT^2/2<T$, while the delay is at least $T$, with the $T=0$ equality excluded as the diagonal. Thus neither target nor environmental self channel acquires an omitted positive-delay root. The complete recent-root exclusions and old-root margins prevent every chart exit other than target coincidence, so the regular-chart local-existence lemma repeatedly extends the solution. In fact, because every active emission lies on a stationary shelf with $D_t=1$, the delay system reduces on this tube to the explicit smooth ordinary field

$$
\mathbf A_i(\mathbf X_i)
=
\kappa\sum_{j\in\mathcal S_i}
\sigma_{ij}\lvert q_iq_j\rvert
\frac{\mathbf X_i-\mathbf P_j}{\lVert\mathbf X_i-\mathbf P_j\rVert^3},
$$

where $\mathbf P_j$ is transmitter $j$'s stationary shelf position and $\mathcal S_i$ is the already enumerated active shelf-source set for receiver $i$. The delayed root census still supplies the provenance of each row, but no unknown future value occurs on the right-hand side before contact. Reflection-plus-polarity equivariance keeps the target positions $(x(T),y(T),z(T))$ and $(-x(T),y(T),z(T))$.

Every active row for the right target has a strictly negative $x$ component while $x>0$: self repulsion points away from the shelf at $x=2$, attraction to the opposite target shelf points toward $x=-2$, environmental repulsion points away from $x=1$, and environmental attraction points toward $x=-1$. Hence $\ddot x<0$, $\dot x<-2$, and $x(T)<0.01-2T$ for every $T>0$ before contact. If no contact occurred through $T=0.005$, this would give

$$
x(0.005)<0.01-2(0.005)=0.
$$

This contradicts $x>0$. The maximal positive-separation solution therefore has some $T_\ast\in(0,0.005)$ with exact coordinate coincidence. Until that first contact the two recent ordered partner-root sets are empty by the super-field inequality already proved, while the fourteen older or environmental root branches remain separated from zero delay. This is an EOM solution, not a prescribed future: only the retained history through $T=0$ is prescribed, and the Master Equation uniquely determines $T>0$ up to the contact boundary.

The acceleration norm bound also supplies an almost sharp derived lower bracket. Since $\ddot x>-M$ with $M=2\times10^{-6}$,

$$
x(T)>x_0-UT-\frac12MT^2.
$$

The positive zero of the right side is

$$
T_-
=
\frac{2x_0}{U+\sqrt{U^2+2Mx_0}}
=
0.0049999999875\ldots .
$$

Consequently

$$
\boxed{0.0049999999875\ldots<T_\ast<0.005.}
$$

The independently accepted oracle prefix through $T=0.0048$ checks the regular evolution and complete root census before this analytically bracketed boundary; the narrow contact bracket itself is derived from the EOM acceleration inequalities.

**Populated perturbations do not turn this event into an attractor.** On the displayed causal-shadow chart the pair's collapsing mutual rows are absent and every retained row is smooth and bounded. Because all active emissions lie on stationary shelves, the future acceleration on this window is an ordinary smooth field of the receiver position. The derivative of one row $\kappa(\mathbf X-\mathbf P)/\lVert\mathbf X-\mathbf P\rVert^3$ has operator norm at most $2\kappa/r^3$; hence the complete position derivative is below $8\times10^{-6}/1.6^3<2\times10^{-6}$. Integrating the linearized equation $\delta\ddot{\mathbf X}=D\mathbf A\,\delta\mathbf X$ twice shows that, over $T<0.005$, the position-flow derivative differs from its free-evolution value by less than $2.5\times10^{-11}$.

Let $z$ denote perturbation coordinates in the retained-history phase space and define $G(z,T)=\mathbf X_+(z,T)-\mathbf X_-(z,T)$. Two admissible coordinates are obtained by adding independently scaled smooth transverse bumps to one target history on $(-1,0]$, with value and first derivative zero at $T=-1$, unit value and zero first derivative at $T=0$, and directions $\mathbf e_y$ and $\mathbf e_z$. Sufficiently small bumps preserve every strict root and inactive-gap margin above, leave all stationary-shelf emission points unchanged, and vary the two transverse endpoint positions independently without varying their endpoint velocities. At the constructed event, $\partial_TG$ is the nonzero relative velocity. The two bump derivatives remain within $2.5\times10^{-11}$ of the independent free-evolution transverse directions, so the derivative of $G$ has rank three. The implicit-function theorem on this smooth finite-dimensional slice of retained-history space makes $G^{-1}(0)$ a set of codimension three in joint $(z,T)$ space; after allowing the one collision time to vary, collision histories have local codimension two in that slice. Here **codimension two** means that two independent scalar transverse tunings remain necessary. Arbitrarily small generic transverse perturbations in this slice therefore produce a miss, even though no inverse-square transverse barrier repels the pair on this chart. A deterministic populated universe can remain on the symmetry manifold, but the current update supplies no mechanism that attracts an open set of perturbed histories back to it.

This separates population from probability. `Populated` means that the other two Architrinos are present and EOM-evolved; it does not install random noise. If an ensemble of retained histories has a probability density with respect to ordinary area in the two transverse bump coordinates—that is, it assigns zero probability to every zero-area subset—then the local codimension-two collision set has probability zero in that ensemble. A measure concentrated on the exact reflection-symmetry manifold can instead assign the collision set nonzero or even unit probability. The existing deterministic update selects neither ensemble. Thus “perturbations are essentially always present” supports a conditional almost-sure miss statement only after a population measure is postulated; it cannot erase the exact admitted collision history or remove the need for boundary semantics.

Claim grade: the update equivariance, exact fourteen-root census, bounded-acceleration bootstrap, EOM reachability of first coordinate coincidence, and local codimension-two collision classification on the displayed retained-history slice are `derived`. This establishes mathematical reachability on an exact populated symmetry set and is independently corroborated before contact by the unchanged oracle; whether that hybrid record satisfies the queue's formal independence wording remains open for review. The theorem proves neither continuation through contact nor a measure statement on the full retained-history phase space. Falsifier: an additional positive-delay root of the displayed history, any displayed root with $D_t\ne1$, failure of the $2\times10^{-6}$ acceleration bound inside the stated tube, a unique EOM continuation satisfying those bounds with no zero of $x$, or a rank loss in the displayed transverse bump derivatives.

Claim grade: the zero-probability statement is `derived conditionally` for ensemble measures absolutely continuous in the two transverse coordinates; no physical ensemble or actual-universe probability is claimed. Falsifier: positive collision probability under such an absolutely continuous local measure, or proof that the collision set has positive two-dimensional area in the displayed slice.

## Blow-Up Admissibility

The answer is chart-dependent. A surviving inverse-square partner row has no nondegenerate bounded-velocity blow-up, while the incoming causal-shadow chart has a one-sided continuous blow-up because its relative acceleration remains bounded. Neither construction supplies a two-sided continuation across the change in root admission.

### Surviving inverse-square row

Let the leading relative acceleration have the near-field form $\dot{\mathbf w}=\rho^{-2}\mathbf F+o(\rho^{-2})$, with $\rho=d$, bounded physical relative velocity $\mathbf w$, and nonzero bounded $\mathbf F$ on a positive-$D_t$ chart. Rescale absolute time by $dT/ds=\rho^p$. The blown-up kinematic equations are

$$
\rho'
=
\rho^p\hat{\mathbf u}\cdot\mathbf w,
\qquad
\hat{\mathbf u}'
=
\rho^{p-1}
\left(\mathbf I-\hat{\mathbf u}\hat{\mathbf u}^{\mathsf T}\right)\mathbf w,
\qquad
\mathbf w'
=
\rho^{p-2}\mathbf F+o(\rho^{p-2}),
$$

where a prime means differentiation in $s$. Continuity of the acceleration equation with bounded physical velocity requires $p\ge2$. For every such $p$, both geometric equations vanish at $\rho=0$, and the radial linearization also vanishes. The exceptional sphere is frozen rather than carrying fixed points with attracting or repelling $\rho$ directions. It therefore supplies no $\rho$-stability classification.

The alternative nondegenerate inverse-square scaling introduces $\mathbf z=\rho^{1/2}\mathbf w$ and $dT/ds=\rho^{3/2}$. A nonzero boundary value of $\mathbf z$ then requires $\lVert\mathbf w\rVert\asymp\rho^{-1/2}$, which exits every finite-speed, transmitter-factor-bounded chart before $\rho=0$. It is not an admitted blow-up of the Master Equation regime being analyzed.

The state-dependent delay adds a second obstruction. Since $\Delta T\asymp\rho$ while $dT/ds=\rho^p$, the same root spans

$$
\Delta s\asymp\rho^{1-p}.
$$

For the only bounded-velocity scalings that regularize the acceleration, $p\ge2$, the required source point recedes to unbounded $s$-history as $\rho\to0$. The boundary value therefore cannot be a closed continuous vector field on $(\rho,\hat{\mathbf u},\mathbf w)$ or on any finite-memory extension of those variables. Retaining the complete absolute-time history preserves the original delayed problem but does not construct an ordinary flow on the exceptional sphere.

Claim grade: `derived` for power-law time rescalings on the declared bounded-velocity, positive-$D_t$, first-root near-field chart. Falsifier: a blow-up with bounded physical velocities whose rescaled field is continuous and nondegenerate at $\rho=0$, retains a finite rescaled memory horizon, and preserves the original absolute-time causal-root set.

### Incoming causal-shadow row

On the reachable shelf-history chart, write the relative equation as

$$
\dot{\mathbf d}=\mathbf w,
\qquad
\dot{\mathbf w}=\mathbf B(\mathbf d,\mathcal H),
$$

where $\mathcal H$ is the retained absolute-time history and $\mathbf B$ is bounded and continuous because every admitted root stays on a stationary shelf. Put $\mathbf d=\rho\hat{\mathbf u}$ and rescale time by $dT/ds=\rho$. The equations become

$$
\rho'=\rho\hat{\mathbf u}\mathbin{\cdot}\mathbf w,
\qquad
\hat{\mathbf u}'=
(\mathbf I-\hat{\mathbf u}\hat{\mathbf u}^{\mathsf T})\mathbf w,
\qquad
\mathbf w'=\rho\mathbf B.
$$

For each fixed incoming history state these equations extend continuously to the exceptional sphere $\rho=0$. Its boundary flow is

$$
\hat{\mathbf u}'=
(\mathbf I-\hat{\mathbf u}\hat{\mathbf u}^{\mathsf T})\mathbf w,
\qquad
\mathbf w'=0.
$$

When $\mathbf w\ne0$, the fixed directions are $\hat{\mathbf u}_\pm=\pm\mathbf w/\lVert\mathbf w\rVert$. The radial linearization is

$$
\delta\rho'
=
(\hat{\mathbf u}_\pm\mathbin{\cdot}\mathbf w)\delta\rho
=
\pm\lVert\mathbf w\rVert\delta\rho.
$$

Thus $\hat{\mathbf u}_-=-\mathbf w/\lVert\mathbf w\rVert$, the incoming collision direction, is attracting in $\rho$, whereas $\hat{\mathbf u}_+$ is repelling in $\rho$. The angular stability is the opposite: $d(\hat{\mathbf u}\mathbin{\cdot}\mathbf w/\lVert\mathbf w\rVert)/ds=\lVert\mathbf w\rVert[1-(\hat{\mathbf u}\mathbin{\cdot}\mathbf w/\lVert\mathbf w\rVert)^2]$, so $\hat{\mathbf u}_-$ is angularly repelling and $\hat{\mathbf u}_+$ is angularly attracting. The incoming fixed point therefore attracts only in the radial direction and repels transverse aim errors, matching the codimension-two result. If $\mathbf w=0$, the entire exceptional sphere is fixed and nonhyperbolic, meaning its linearization has zero eigenvalues; that degenerate case is not the shelf-history crossing.

This is only a one-sided history-parametrized extension. It is a passive relabeling: absolute time $T$ remains a carried state variable, and every root is still admitted from the original absolute-time condition rather than from an $s$-delay condition. Every old positive-delay root spans an unbounded interval in $s$ as $\rho\to0$, so the boundary field is not closed on $(\rho,\hat{\mathbf u},\mathbf w)$ without the incoming absolute-time history. More decisively, after a transverse-free passage reverses the separation direction, the target transmitter changes from the inbound no-recent-root sector to the receding two-recent-root sector. Those newborn rows have ranges $O(\rho)$ and accelerations $O(\rho^{-2})$, for which the causal-shadow scaling gives $\mathbf w'=O(\rho^{-1})$. The incoming extension therefore cannot be continued continuously onto the outgoing root-admission chart. The blow-up classifies approach but selects no passage, rebound, or outgoing history.

For the explicit crossing, the obstruction can be stated without the blow-up coordinates. Assume a $C^1$ post-contact continuation preserves the finite inherited relative velocity and retains both newborn roots on each ordered partner channel. With $\tau=T-T_\ast$ and $W_\ast=\lVert\mathbf w(T_\ast^-)\rVert=4+O(10^{-8})$, continuity gives $d(\tau)=W_\ast\tau+o(\tau)$. Relative to the reversed post-crossing baseline, each transmitter remains strictly inside the $a<-\chi$ receding sector; its transverse component is $O(10^{-8})$ and $\chi=(c_f^2-\lVert\mathbf p\rVert^2)^{1/2}$ stays bounded away from zero. The exact two-root formula therefore gives radial acceleration $-2\kappa\lvert q_iq_j\rvert\chi/d^2$ per receiver toward its partner; the two ordered channels give a relative radial term bounded below in magnitude by a positive constant times $d^{-2}$, while every older and environmental row remains bounded. Its absolute-time integral diverges as

$$
\int_0^\epsilon\frac{d\tau}{d(\tau)^2}
\asymp
\int_0^\epsilon\frac{d\tau}{\tau^2}
=\infty,
$$

contradicting the assumed finite continuous relative velocity. Thus the unchanged row admits no $C^1$ finite-velocity passage that inherits the incoming velocity and keeps the complete newborn-root census. A rebound would require a boundary-supplied velocity change that the bounded incoming acceleration does not produce; termination would likewise be a new event disposition. Neither is selected here.

Claim grade: the one-sided exceptional-sphere flow, its two fixed directions, radial and angular stability, the outgoing divergence, and the no-$C^1$ finite-velocity passage result are `derived` on the bounded-acceleration causal-shadow chart with nonzero relative velocity and complete newborn-root retention. Falsifier: failure of the displayed coordinate substitution, a different linearized radial sign, an outgoing two-root branch whose rescaled $\mathbf w'$ stays finite under $dT/ds=\rho$, or a complete-row $C^1$ continuation with finite inherited velocity and finite acceleration integral.

## Reparameterization Invariance

Only translations of absolute time preserve the canonical causal-root admission set for the full history class. Let $T=\phi(s)$ be a strictly increasing change of parameter. Passive relabeling preserves the physical roots only if the root condition continues to use the stored absolute-time difference,

$$
\left\lVert
\mathbf X_i(\phi(s_r))-\mathbf X_j(\phi(s_t))
\right\rVert
=
c_f\left[\phi(s_r)-\phi(s_t)\right].
$$

To recover the same canonical equation in the new parameter with the same fixed $c_f$, one would need

$$
\phi(s_r)-\phi(s_t)=s_r-s_t
$$

for every admitted pair. Stationary separated histories realize every positive delay, so the identity must hold for every $s_t$ and every positive increment. Hence $\phi(s)=s+T_0$. A constant translation neither slows the coincidence approach nor removes the vanishing delay. A scaling would change the numerical wake speed, and any state-dependent scaling would change which emission and reception events satisfy the root equation.

A passive nonuniform relabeling can carry absolute time as an additional state and continue evaluating roots with $\phi(s_r)-\phi(s_t)$. That leaves the admission set intact, but it leaves the singular absolute-time equation intact as well; it is bookkeeping, not regularization.

Claim grade: `derived` for invariance over the declared history class with fixed $c_f$. Falsifier: a strictly increasing non-translation $\phi$ that preserves the admitted emission-reception pairs for every stationary separated control and every moving retained history while keeping the canonical root equation and the same $c_f$.

## Same-Transmitter Local Chord Expansion

Required control (4) can be completed directly from the path geometry. Let one path be $C^4$ from the past at reception time $T$, and write its velocity, acceleration, and jerk as $\mathbf V$, $\mathbf A$, and $\mathbf J$ at $T$. For a prospective self root of delay $\tau>0$,

$$
\mathbf X(T)-\mathbf X(T-\tau)
=
\mathbf V\tau
-\frac12\mathbf A\tau^2
+\frac16\mathbf J\tau^3
+O(\tau^4).
$$

Writing $v=\lVert\mathbf V\rVert>0$, the self-root residual $G(\tau)=\lVert\mathbf X(T)-\mathbf X(T-\tau)\rVert-c_f\tau$ has the expansion

$$
\begin{aligned}
G(\tau)
&=
(v-c_f)\tau
-\frac{\mathbf V\mathbin{\cdot}\mathbf A}{2v}\tau^2\\
&\quad+
\left[
\frac{\lVert\mathbf A\rVert^2}{8v}
+\frac{\mathbf V\mathbin{\cdot}\mathbf J}{6v}
-\frac{(\mathbf V\mathbin{\cdot}\mathbf A)^2}{8v^3}
\right]\tau^3
+O(\tau^4).
\end{aligned}
$$

This expansion first gives an open-velocity-domain result. If $v\ne c_f$, the nonzero linear coefficient has a fixed sign for all sufficiently small $\tau>0$, so no self roots can accumulate at the diagonal from either the sub-field or super-field side. Hence a near-diagonal self-root sequence necessarily has $v=c_f$. At $v=c_f$, any $\mathbf V\mathbin{\cdot}\mathbf A\ne0$ gives a fixed-sign quadratic leading term and again excludes sufficiently small roots. If also $\mathbf V\mathbin{\cdot}\mathbf A=0$, define

$$
C_3
=
\frac{\lVert\mathbf A\rVert^2}{8c_f}
+
\frac{\mathbf V\mathbin{\cdot}\mathbf J}{6c_f}.
$$

Any $C_3\ne0$ gives the same exclusion at cubic order. Thus the general $C^4$ unresolved set already requires $v=c_f$, $\mathbf V\mathbin{\cdot}\mathbf A=0$, and $C_3=0$; this conclusion uses no speed ceiling.

Three local cases follow under the **additional one-sided speed-bound hypothesis** $\lVert\mathbf V(S)\rVert\le c_f$ on a left neighborhood of $T$. The current canonical Architrino velocity domain is open and does not impose this bound globally; the calculation classifies only histories that happen to satisfy it near the reception event. When $v=c_f$, expansion of $\lVert\mathbf V(T-\tau)\rVert^2\le c_f^2$ first forces $\mathbf V\mathbin{\cdot}\mathbf A\ge0$, so the last two cases exhaust field-speed equality.

1. If $v<c_f$, then $G(\tau)<0$ for all sufficiently small positive $\tau$; there is no near-diagonal self root.
2. If $v=c_f$ and $\mathbf V\mathbin{\cdot}\mathbf A>0$, then the quadratic term is negative. This is one-sided arrival at field-speed equality from lower speed, and again no sufficiently small positive self root exists.
3. If $v=c_f$ and $\mathbf V\mathbin{\cdot}\mathbf A=0$, the one-sided speed maximum gives $\mathbf V\mathbin{\cdot}\mathbf J\le-\lVert\mathbf A\rVert^2$. Hence

$$
G(\tau)
\le
-\frac{\lVert\mathbf A\rVert^2}{24c_f}\tau^3
+O(\tau^4).
$$

Any nonzero curvature acceleration at field-speed equality therefore opens a cubic root-free gap. If $\mathbf A=0$ but $\mathbf V\mathbin{\cdot}\mathbf J<0$, the cubic coefficient is still strictly negative. The cubic-order unresolved case must have $v=c_f$, $\mathbf V\mathbin{\cdot}\mathbf A=0$, $\mathbf A=0$, and $\mathbf V\mathbin{\cdot}\mathbf J=0$, with higher jets likewise required not to bend or slow the chord. Under the same speed-bound hypothesis, the triangle-equality rigidity theorem supplies the nonlocal completion: equality for a positive delay requires a straight, exactly field-speed segment throughout that delay interval.

An exact circular control checks the cubic coefficient without using the expansion itself. A path moving at field speed on a circle of radius $R$ has chord length

$$
2R\sin\!\left(\frac{c_f\tau}{2R}\right)
=
c_f\tau-\frac{c_f^3}{24R^2}\tau^3+O(\tau^5).
$$

Since $\lVert\mathbf A\rVert=c_f^2/R$, the cubic deficit is exactly $-\lVert\mathbf A\rVert^2\tau^3/(24c_f)$, matching the general coefficient. This is a same-author closed-form consistency check, not independent evidence for the Master Equation's path regularity.

Plainly: within a history segment that stays at or below field speed, reaching field-speed equality is necessary but not sufficient for a newborn self root. Arriving from below misses at quadratic order, and turning there misses at cubic order. This is a conditional local protection result, not a velocity-domain premise; super-field histories remain admitted elsewhere in the packet.

Claim grade: the expansion, the necessity $v=c_f$ for near-diagonal accumulation, and the general linear/quadratic/cubic nonzero-coefficient exclusions are `derived` for $C^4$ paths on the open velocity domain. The signed curvature bound is `derived conditionally` under the displayed one-sided speed bound. This completes MEC-008 control (4) at local order but supplies no global Architrino speed ceiling. It does not prove that every populated EOM path has nonzero curvature at every field-speed contact. Falsifier: a $C^4$ path whose residual violates the displayed coefficients, a near-diagonal root sequence with $v\ne c_f$, or a path satisfying the one-sided bound with $\mathbf A\ne0$ and positive self roots accumulating at $\tau=0$.

## MEC-008 Control (2): Bounded EOM-Evolved Arc

The existing EOM solver and its pre-existing root-accounting output were used without modifying either instrument. The control initialized an opposite-polarity pair at $\mathbf X_a=(0.05,0,0)$ and $\mathbf X_b=(-0.05,0,0)$ with velocities $(0,0.2,0)$ and $(0,-0.2,0)$, plus a third positive architrino at $(0.3,0.2,0.7)$ with zero velocity. The retained inertial prehistory covered $[-2,0]$, $c_f=1$, $\kappa\lvert q_aq_b\rvert=0.008$, and the EOM solver evolved the complete three-path state from $T=0$ to $T=0.2$ at fixed step $0.001$.

All 200 steps were accepted with no rejected step. Each step certified all nine ordered receiver-transmitter rows, including root-free complements, no unresolved traversal row, and no memory-boundary contact: 1,800 ordered root-accounting rows in total. The pair's areal-rate vector changed from

$$
\mathbf h(0)=(0,0,0.04)
$$

to

$$
\mathbf h(0.2)
=
(-0.0001861421810,\ 0.0004307294674,\ 0.0465138089981).
$$

The component along the initial turn normal therefore increased by $0.0065138089981$, or $16.2845\%$, while the separation direction advanced by $0.8493798015$ radians, or $0.135183$ turns. The sign agrees with the derived secular gain over this certified arc.

Claim grade: `measured` by the pre-existing EOM solver's coupled-evolution, complete-root-accounting, and published-history outputs over the declared $0\le T\le0.2$ control only. The cross product used to read $\mathbf h$ is its defining kinematic expression, not an acceleration oracle. Falsifier: replay of the declared request yielding a non-complete accepted-step root census or a nonpositive change in the initial-normal component of $\mathbf h$.

This does not complete required control (2). The arc covers only $13.5183\%$ of a turn, and the initial retained history is prescribed rather than EOM-produced. Earlier fixed-step attempts to extend a comparable three-path request through a full turn exceeded their bounded process time without returning a retained final record. Two later adaptive requests did return fail-closed records: the original-scale control halted at $0.366036$ turns and a faster-turning sub-field control halted at $0.294148$ turns. In both cases the next candidate step exhausted the root-completeness precision budget at an evolving published-history joint. Their accepted prefixes continued to increase the initial-normal component of $\mathbf h$, but the overall responses were failed and cannot supply a turn average. The required full-turn EOM measurement and an independently retained successful replay artifact therefore remain open.

The repository's pre-existing independent Python EOM oracle was also applied unchanged to the exact four-Architrino shelf history. With 80-decimal arithmetic, sharp roots, root tolerance $10^{-12}$, coupling $10^{-6}$, and fixed step $0.0005$, it completed nine accepted steps through $T=0.0045$ with no rejection. The three corrected substeps and two endpoint snapshots per accepted step produced $9\times3\times2\times16=864$ ordered root-accounting rows; every row was `certified_complete` and none touched the memory boundary. The request digest was `82453e066cb33bc5397e65a24b0777515f8605f79fb945046a2860c822b3034a`. At the accepted endpoint the target positions and velocities were

$$
\mathbf X_+=(0.0009999999914812126,-3.3498191\times10^{-14},0),
\qquad
\mathbf X_- =(-0.0009999999914812126,-3.3498191\times10^{-14},0),
$$

$$
\mathbf V_+=(-2.000000003786117,-1.1697809\times10^{-11},0),
\qquad
\mathbf V_-=(2.000000003786117,-1.1697809\times10^{-11},0).
$$

Thus the separation was $0.0019999999829624252$, the inward relative speed was $4.000000007572234$, and the measured areal-rate vector remained exactly $\mathbf h=\mathbf0$ under the reflection symmetry. Every one of the 108 target-partner snapshot rows retained exactly one old root.

An additional unchanged-oracle request used fixed step $0.0004$ and completed twelve accepted steps through $T=0.0048$ with no rejection. Its $12\times3\times2\times16=1152$ ordered root rows were all `certified_complete`, none touched the memory boundary, and all 144 target-partner rows retained exactly one old root. At that endpoint,

$$
\mathbf X_+=(0.0003999999903075166,-3.7024549\times10^{-14},0),
\qquad
\mathbf V_+=(-2.000000004038523,-1.1797065\times10^{-11},0),
$$

with the target-minus state reflected in $x$ and equal in $y,z$. The separation was $0.0007999999806150332$, the inward relative speed was $4.000000008077046$, and $\mathbf h=\mathbf0$ exactly. The request digest was `1885654daa7f7775630e9979319996c1b6998e1d660d9c03f9dcfcdd5a8e6222`. This moves the accepted numerical prefix to within $0.0002$ of the analytically bracketed contact while preserving the complete one-old-root topology.

Extending the fixed-step-$0.0005$ request through $T=0.005$ returned a failed certificate with accepted end $0.0045$, nine accepted steps, and one rejected step. All 896 root rows in that response were `certified_complete` and none touched the memory boundary, but the candidate endpoint changed each target-partner channel from one old root to three roots. The oracle therefore rejected the atomic step as `root_event_requires_subdivision`; the fixed minimum step produced top-level `minimum_step_exhausted`. The failed-request digest was `0417a4bc5bd0963b828f76e500c8abc8908c7f0658007c9beda45b6c24314f59`. This independently locates the contact/topology boundary in $(0.0048,0.005)$ when combined with the accepted $0.0048$ prefix, consistently with the much narrower derived bracket, but a rejected candidate step is not a published EOM history at contact.

Claim grade: the accepted prefixes, including the 1152-row census through $T=0.0048$, their endpoint states, exact zero $\mathbf h$, and the failed boundary bracket are `measured` by the unchanged independent oracle. They establish a minimally noncollinear EOM-evolved control with complete root provenance up to $T=0.0048$ and independently test the theorem's pre-contact regime. By themselves they do not prove the zero-separation limit or the still-missing full-turn nonzero-$h$ average. Falsifier: replay with any rejected pre-$0.0048$ step, incomplete root row, memory-boundary contact, broken reflection equality, nonzero relative areal rate, or a different boundary topology transition.

### What a boundary certificate can require

First-contact reachability is a statement about the maximal regular solution and its limit, not about an update after the limit. Define

$$
T_\ast=\inf\{T>0:d(T)=0\}.
$$

A boundary-reaching EOM counterexample therefore needs: a unique admitted solution on every compact subinterval of $[0,T_\ast)$; complete retained history and causal-root provenance there; a finite $T_\ast$; and the proven limit $d(T)\to0$ as $T\uparrow T_\ast$. It does not need a post-contact state. Requiring a fixed-step solver to accept an endpoint exactly equal to an a priori unknown $T_\ast$ would not be invariant under step choice, and requiring it to cross $T_\ast$ would presuppose the event rule whose necessity MEC-008 is deciding.

The exact shelf-history theorem supplies the regular solution, complete fourteen-root census, and finite-time zero-separation limit. The unchanged oracle independently supplies an accepted EOM prefix through $T=0.0048$ with complete root provenance and then detects the predicted root-topology change on a step that straddles the boundary. Together they form a candidate hybrid reachability certificate. This packet does not self-award the queue's word `independently`: formal completion requires a separate review confirming that the theorem-plus-unchanged-oracle record satisfies the acceptance sentence. The remaining review question is therefore certificate independence, not whether a solver must invent a post-boundary history.

| Acceptance element | Present evidence | Status |
| --- | --- | --- |
| Admitted path domain | The canonical model has an open velocity domain; the separate [field-speed compatibility decision](../field-speed-ceiling/field-speed-ceiling-compatibility-decision.md) explicitly records that no primitive $c_f$ path-speed ceiling is adopted | satisfied by current canon |
| Populated and noncollinear | Four Architrinos, with the environmental pair at $y=1.5$ | satisfied |
| Complete retained history | Exact piecewise-cubic target histories, stationary environmental histories, declared all-earlier shelves, retained depth $5.5$, and radius $2.5$ | satisfied analytically |
| Complete causal-root provenance | Fourteen named simple $D_t=1$ roots and two proved-empty self channels; persistence is proved on $[0,T_\ast)$ | satisfied analytically |
| EOM rather than prescribed future | Only $T\le0$ is prescribed; the smooth fixed-source acceleration field uniquely evolves $T>0$ | satisfied analytically |
| Boundary reach | $0.0049999999875\ldots<T_\ast<0.005$ and $d(T)\to0$ | satisfied analytically |
| Independent operational check | Unchanged oracle accepts through $0.0048$ with 1152 complete rows and detects the one-to-three partner-root change on a straddling step | satisfied as pre-contact corroboration |
| Formal independence disposition | No separate reviewer has adjudicated the combined theorem and oracle record | open |

Claim grade: the distinction between a first-contact limit certificate and post-contact evolution is `derived`; classification of the current hybrid record as formally independent is `open for review`. Falsifier: an acceptance interpretation that both avoids assuming a boundary update and proves that one of the four displayed first-contact certificate elements is absent from the construction.

### Causal-shadow EOM diagnostics

The unchanged EOM solver was also probed with the four-path reflection-plus-polarity geometry derived above. The first request used affine target histories over $[-2.2,0]$, target positions $x=\pm0.001$, target velocities $\dot x=\mp20$, an off-axis opposite-polarity pair at $(\pm0.2,0.3,0)$, $c_f=1$, and coupling $10^{-4}$. Two incremental steps were accepted through $T=0.000049$. At that cut the target positions were $x=\pm0.0000199999989574$, the inward speeds were $20.0000000422$, all 32 ordered root-accounting rows over the two accepted steps were returned, and both target-partner root sets were empty. A third request to $T=0.00005$ was rejected with `caustic_exit_not_certified` and top-level halt `caustic_transit_uncertified`; its attempted partner rows still contained no roots.

That affine run is a diagnostic, not evidence. Its super-field target positions escape the declared radius-one spatial envelope over the retained interval, so the finite history does not satisfy the request's own geometric-completeness provenance even though the row enumerator returned `certified_complete` on the supplied segments. The endpoint rejection is likewise not a dynamical repulsion: it is the EOM solver refusing to certify the unresolved caustic exit.

A bounded-history replacement used reflection-related radius-$0.05$ circular target prehistories at speed $1.2$, retained depth $0.55$, spatial radius $0.25$, an off-axis pair at $(\pm0.1,-0.15,0)$, and coupling $10^{-6}$. It accepted no evolution step: target self rows routed to `caustic_state_reconstruction_failed` at the initial cut. This failure is instrument-level and cannot establish that the regular circular self-root chart derived above lacks an EOM solution. It does establish the immediate operational blocker for the reachability control: a valid bounded super-field prehistory necessarily carries self roots, and the present EOM request must certify their state reconstruction before it can test target-pair contact.

The exact shelf history used by the populated reachability theorem was then sent to the same unchanged solver. Its first attempted $0.0001$ step returned all sixteen ordered root-accounting rows as `certified_complete`, with exactly the theorem's fourteen roots, two empty environmental self channels, no memory-boundary contact, and $D_t$ intervals enclosing $1$ on every active row. The step was nevertheless rejected. Under the ratified `sharp_with_finite_width_fallback` chart, the solver automatically routed the two super-field target self channels to finite width and returned `caustic_state_reconstruction_failed` because no common-domain chart overlap was certified. A diagnostic sharp-only allocation also rejected the step with `root_completeness_not_certified`; its correction snapshot reported `numeric_transmitter_factor_sign_uncertified` for the same two self channels even though the start snapshot had certified their shelf roots with $D_t=1$. These are solver-enclosure failures, not counterexamples to the exact root calculation.

Claim grade: the affine prefix values, shelf-root census, and halt codes are `measured diagnostics` from the unchanged EOM solver, but none is an accepted numerical reachability control. The affine request fails its declared spatial-envelope provenance, while both bounded requests have no accepted step. Falsifier: replay of the stated requests returning different status, endpoint values, target-partner root counts, or shelf-root factors. These diagnostics establish instrument reach and the next failure boundary only; physical reachability is established separately by the exact theorem, not by these failed runs, and no run establishes continuation.

## Steel-Man: Ruling Coincidence In

Stated at full strength, without hedging, so that it can be defeated on its merits.

**R1. Nothing at the destination opposes arrival.** Architrinos are true geometric points with no volume and no exclusion, as [the foundations](../../../content/markdown/aaa/foundations/architrino.md) state directly. Whatever resistance exists is accumulated during approach and never at the endpoint. This is an unusual situation and it is the primary argument: there is no contact term to appeal to.

**R2. For opposite polarity the accumulated update points inward.** By the near-field lemma the magnitude grows like $|D_t|/d^{2}$, and for opposite polarity its radial component is directed along the closing direction. Approach reduces $D_t$ and therefore suppresses the coefficient relative to the static case, but it does not remove the inverse-square growth. On the radial row alone, no repulsive contact barrier exists to invoke.

**R3. A populated universe destroys every isolation-based protection.** Barrier and conservation arguments constructed for an isolated pair have no referent when each architrino receives contributions from an enormous number of transmitters. There is no closed subsystem for which a relative account can be conserved, so the transverse barrier of the previous section must be defended as a scaling statement rather than as a conservation statement.

**R4. $\mathbb{A}\mathbb{A}\mathbb{A}$ contains structures that deliberately revisit small separation.** Bound opposite-polarity pairs are basic constituents, not rare accidents. Each is a configuration that returns to small separation repeatedly over cosmological durations. Measure-zero-for-a-random-draw is weak protection against a structure whose function is to revisit the neighborhood.

**R5. Self-action is steerable rather than accidental.** The coincident same-transmitter case needs no second object to be found. An architrino's own path history can be arranged by its own evolution, so the relevant configuration is reachable by design rather than by coincidence in the colloquial sense.

**R6. The convention currently in force concedes the point.** Modeling $r=0$ as null action presupposes that the configuration occurs and needs a value. A model that had ruled coincidence out would not need the assignment.

## Steel-Man: Ruling Coincidence Out

**O1. Vanishing delay degrades the solution concept.** By Framework 1 the delay spectrum escapes to unbounded frequency as $\Delta\to0$, and by GD-5 no causally closed forward window exists at the endpoint. This argument does not assert that something pushes back; it asserts that there is no admissible object to arrive with. It is polarity-independent, which the ordinary repulsion intuition is not.

**O2. Divergent accumulated update is a barrier in the strict sense.** MEC-007's exact result that the newborn self-root measure has infinite total variation is the collinear instance of a general form of argument: a functional that is monotone along admissible histories and unbounded at the target makes the target unreachable. This form is indifferent to genericity, which is what makes it stronger than codimension counting.

**O3. Exact aim is two conditions and a populated environment never holds them.** Coincidence requires $h=0$ exactly at exactly the right instant. A populated environment perturbs every pair transversally and continuously. Failing to hold an exact transverse condition is the one thing an environment can be relied upon to do.

**O4. Magnitude asymmetry means the environment cannot defeat a barrier.** At small $d$ the pair row scales like $1/d^{2}$ while the aggregate environmental contribution remains $O(1)$. The environment therefore cannot push a pair through a divergent barrier. Combined with O3 this is a pincer rather than a tension: the environment destroys exact aim while being unable to remove whatever protection exists.

**O5. Regularization has less freedom here than for instantaneous laws.** By Framework 3, the causal-root condition is tied to absolute time and cannot be reparameterized away, so the standard route to a removable singularity is structurally narrowed. The independent $\delta^{-2}$ and endpoint-measure results corroborate.

**O6. Conditional speed-bound rigidity excludes the self-root route for curved paths in that class.** If a retained segment satisfies $\lVert\mathbf V\rVert\le c_f$ throughout, then $\|\mathbf X(T)-\mathbf X(S)\|\le c_f(T-S)$ with equality only on a straight exact-aim field-speed chord, so every non-straight history in that bounded class has no positive-delay self root. The canonical velocity domain is open, so this does not exclude R5's route on super-field retained histories and is not a global MEC-008 argument.

## Adjudication

The symmetric signed calculation establishes a secular gain with $\mathbf h\mathbin{\cdot}\dot{\mathbf h}>0$ on the pair-center chart. The general absolute-frame calculation corrects the attempted extension of that result: center-motion terms have no fixed sign, and a strictly sub-field periodic prescribed family has a negative full-turn pair contribution. The pair row therefore does not make exact aim a universal attractor or a universal repeller. The bounded EOM arc measures gain on one populated prefix, but no completed EOM turn yet selects the physical sign class.

The conditional populated-barrier theorem remains valid for any EOM subclass on which a positive pair-center-like coefficient and bounded remainder are certified; it cannot be promoted to arbitrary center motion. The radial first-exit theorem supplies a sign-independent replacement on every uniformly sub-field-speed regular chart: the inward $d^{-2}$ pair contribution forces the bounded radial-speed chart to end at positive separation. The self-chord expansion separately proves on the open velocity domain that a same-transmitter near-diagonal root sequence requires field-speed contact and vanishing quadratic and cubic coefficients; the additional one-sided speed bound makes nonzero curvature a strict cubic exclusion.

The adjudication does not establish global domain exclusion, and that branch is now false because the explicit four-Architrino construction reaches coordinate coincidence. The radial theorem still excludes every regular approach whose two newest-root transmitter histories retain a uniform sub-field margin and whose other rows remain bounded. The reachability construction lies outside that theorem exactly as the affine classification predicts: the inbound target pair is super-field, its recent mutual roots are absent, and its fourteen older self, partner, and population roots stay simple with $D_t=1$. Their bounded acceleration cannot prevent first contact. Thus coincidence is mathematically possible under the existing EOM in a populated noncollinear system.

This reachability does not validate the proposed dichotomy between a universal transverse repeller and a universal $h=0$ attractor. The full-turn secular sign is history-dependent in the general absolute frame. On the constructed super-field causal-shadow chart, the singular mutual row is absent altogether, the remaining acceleration is smooth, and collision data have local codimension two. Generic transverse perturbations miss rather than being driven back to exact aim. Exact reflection symmetry is nevertheless an invariant EOM set, so “a populated universe never holds exact aim” is not a mathematical exclusion premise: population alone does not break an exact symmetry already present in the complete retained history.

The blow-up and reparameterization results independently strengthen O1 and O5 by showing why an ordinary exceptional-sphere regularization cannot be constructed without leaving the declared causal-root problem.

Plainly: the transverse sign by itself does not settle coincidence. Ordinary regular sub-field approaches cannot arrive, but the explicit populated super-field history does. Its exact symmetry makes contact possible, while nearby transverse perturbations normally miss. Because the existing update admits an exact EOM history that reaches the boundary, a mathematically complete theory still owes a terminal or continuation rule there even if later evidence shows that physical realizations almost always avoid the codimension-two set.

## Deliverable Disposition

1. **Secular transverse row — derived with corrected scope and coefficient.** The symmetric pair-center affine chart has $\dot h_{ij}^{\mathrm{sec}}=+\kappa\lvert q_iq_j\rvert c_f^2D_t h/(\chi^2d^2)$ and repels from $h=0$. The positive factor $c_f^2/\chi^2=\sec^2\alpha$ corrects the earlier transverse-range omission without changing the sign. The general absolute-frame leading row contains an additional sign-indefinite center-motion term. A periodic prescribed family has a negative full-turn contribution, so polarity alone does not select the general sign.
2. **Blow-up admissibility — chart-dependent and derived.** A surviving inverse-square row needs $dT/ds=O(\rho^p)$ with $p\ge2$, which freezes the exceptional sphere and makes the rescaled root-memory span diverge. On the bounded causal-shadow approach, $dT/ds=\rho$ gives a one-sided continuous sphere flow. Its incoming fixed direction is radially attracting but angularly repelling; the outgoing two-root branch makes $\mathbf w'=O(\rho^{-1})$. Equivalently, its absolute-time acceleration has a nonintegrable $\tau^{-2}$ lower magnitude, so no $C^1$ finite-velocity passage retaining the complete newborn-root census exists.
3. **Reparameterization invariance — derived negatively.** Only $T=s+T_0$ preserves the canonical root equation for the full history class with fixed $c_f$, and this translation does not regularize coincidence.
4. **Minimally noncollinear EOM controls — executed before contact; formal hybrid adjudication open.** The unchanged production instrumentation gives a complete nine-row census and positive $h$ increment over a certified $0.135183$-turn arc. The unchanged independent oracle gives a 1152-row complete accepted census through $T=0.0048$ on the shelf-history approach, with $\mathbf h=0$ exactly; a straddling fixed step fails closed after its complete candidate census detects the topology change. No full-turn retained result was obtained, and the theorem-plus-oracle boundary-limit certificate has not received separate acceptance review.
5. **Conditional populated invariant tube — derived.** Under the additional positive pair-center-like coefficient floor, bounded population remainder, and finite relative speed, $H^2\ge\beta d$ is forward invariant at sufficiently small $d$ and enforces $d\ge\beta/V^2$. Every monotone approach outside this region is captured unless it remains in the shrinking cone $H=O(d^3)$, equivalently $\lVert\mathbf w_\perp\rVert=O(d^2)$. The general absolute-frame calculation proves that the positive coefficient is not universal.
6. **Same-transmitter chord expansion — derived, with a conditional rigidity strengthening.** On the open velocity domain, near-diagonal root accumulation requires $v=c_f$ and vanishing quadratic and cubic residual coefficients; both sub-field and super-field $v\ne c_f$ states have a fixed-sign linear gap. Under the additional one-sided speed bound, arrival from below has a quadratic deficit and nonzero equality-speed curvature has a cubic deficit. The straight exact-field-speed chord is the bounded-class equality case; no global speed ceiling is imposed.
7. **Uniform sub-field radial first exit — derived.** With newest-root transmitter speeds bounded by $\nu c_f$ for some $\nu<1$, bounded remaining rows, and bounded relative speed, the exact two-row radial contribution is at least $C_\nu/d^2$ inward and enforces an explicit positive separation floor before the chart can continue. Any candidate coincidence must lose a speed, root, remainder, or finite-velocity bound first.
8. **Locally affine root-existence classification — derived.** A sub-field transmitter has one recent root; a super-field transmitter moving away from the receiver has two, whose complete opposite-polarity radial sum remains inward; and a sufficiently super-field transmitter moving toward the receiver has none. A two-sided inbound $U>c_f$ affine pair therefore supplies a prescribed root-free collision germ, but not the independently certified populated EOM history required for reachability.
9. **Bounded super-field self-root and regular-chart DDE lemmas — derived.** A super-field reception state with a sufficiently deep bounded retained past necessarily has a positive-delay self root. Such a root can be regular, as the constant-speed circular example proves. A finite simple-root census with positive range, transmitter-factor, inactive-gap, and history margins gives a unique short method-of-steps extension; it does not cross a margin loss.
10. **Populated causal-shadow reachability — derived.** The explicit radius-$2.5$, depth-$5.5$ four-Architrino history has fourteen simple $D_t=1$ roots, two empty stationary self channels, no joining-segment roots, and a unique bounded-acceleration EOM future whose first target coincidence obeys $0.0049999999875\ldots<T_\ast<0.005$. The collision set has local codimension two on the displayed retained-history perturbation slice and is not an attractor there.
11. **Causal-shadow EOM probes — mixed prefix evidence, no accepted endpoint.** The independent oracle accepted the bounded shelf history through separation $0.0007999999806150332$ at $T=0.0048$ with 1152 complete rows; a straddling request rejected after a complete candidate census detected the predicted partner-root topology change. The production solver's affine request advanced to separation $3.9999997915\times10^{-5}$ but violated its declared spatial envelope, while its bounded circular and shelf replacements accepted no step. These boundaries do not negate the exact reachability theorem.

The mathematical domain question is answered by existence: coordinate coincidence is reachable, although it is locally nongeneric rather than attractive on the exhibited chart. The remaining MEC-008 work is evidentiary and numerical: independently adjudicate whether the theorem plus unchanged-oracle prefix/topology record meets the formal reachability certificate, and obtain the requested full-turn areal-rate measurement without changing the analytical instrument in the same change. Downstream MEC-002/MEC-003 work must derive what the theory does at or after the reached boundary.

## Claim Register

| Claim | Grade | Falsifier |
| --- | --- | --- |
| $r=0\iff\Delta=0$; coincidence is vanishing delay | `derived` | a root admitted with $r=0$ and $\Delta\neq0$ |
| Null-action-at-$r=0$ is inert for reachability and continuation | `derived` | any such conclusion that changes when only the single-instant value changes |
| Near-field magnitude $\approx\kappa\lvert q_iq_j\rvert c_f\lvert D_t\rvert\sec^2\alpha/d^{2}=\kappa\lvert q_iq_j\rvert c_f^3\lvert D_t\rvert/(\chi^2d^2)$ | `derived` under stated hypotheses | exact root computation on a compliant chart departing from this leading order |
| At fixed nonzero $\chi$, the transmitter factor suppresses rather than amplifies after root substitution | `derived` under the same hypotheses | an exact fixed-$\chi$ family whose coefficient increases as $D_t$ decreases |
| Delay misalignment satisfies $\sin\alpha=\lVert\mathbf p\rVert/c_f$ and is independent of $d$ | `derived` under the same hypotheses | a compliant affine first root with another angle, or a bounded-acceleration near-field family whose limiting angle depends on $d$ |
| Exactly central rows give a transverse turning point at $d\approx h^{2}/C$ for $h\neq0$ | `derived` | an exactly central case with $h\neq0$ reaching $d=0$ |
| Barrier threshold falls as $\sqrt{d}$ while a prescribed fixed-angle transverse magnitude accumulates as $\ln(1/d)$ | `derived` given the above scalings | a compliant approach violating either rate |
| Symmetric pair-center affine mutual row gives $\dot h_{ij}^{\mathrm{sec}}=+\kappa\lvert q_iq_j\rvert c_f^2D_t h/(\chi^2d^2)$ | `derived exactly` on that declared chart | a compliant symmetric complete two-row calculation with $\mathbf h\mathbin{\cdot}\dot{\mathbf h}\le0$ |
| Symmetric pair-center one-turn increment is approximately $\kappa\lvert q_iq_j\rvert\oint(c_f^2D_t/\chi^2)\,d\theta$ | `derived asymptotically` on that declared chart | a compliant symmetric full turn with fixed-sign $h$ and positive $D_t$ but nonpositive $\Delta\lvert h\rvert$ |
| $h=0$ is repelling under the symmetric pair-center leading contribution | `derived` locally | a negative linear coefficient of $h$ on that declared chart |
| General locally affine pair torque contains sign-indefinite absolute-frame terms | `derived` | exact-root evaluation of the displayed local control yielding nonnegative $\mathbf h\mathbin{\cdot}\dot{\mathbf h}_{ij}$ |
| The displayed periodic prescribed family has a negative full-turn pair contribution for $0<k<2ap$ and sufficiently small $\varepsilon$ | `derived` for that family; EOM reachability not claimed | exact-root refinement whose normalized turn integral does not approach the displayed negative limit |
| $H^2\ge\beta d$ is a local invariant coincidence-exclusion region under the displayed $L$, $B$, and $V$ bounds | `derived` conditionally | a compliant first crossing into $H^2<\beta d$ or boundary reach without a hypothesis failure |
| Every monotone approach with $B>0$ and $H\ge(2B/L)d^3$, or with $B=0$ and $H>0$, is captured by an invariant tube before coincidence | `derived` conditionally | a compliant approach remaining uncaptured through $d=0$ |
| A still-unexcluded pair approach must satisfy $\lVert\mathbf w_\perp\rVert=O(d^2)$ | `derived` under the same bounds | a compliant boundary-reaching approach with transverse relative velocity asymptotically larger than $d^2$ |
| Uniformly sub-field newest partner roots plus bounded remaining rows and relative speed force a positive first-exit separation | `derived` | an exact solution satisfying all displayed bounds that crosses the explicit floor |
| Locally affine root count is $0$, $1$, or $2$ according to the displayed radial/transverse velocity sectors | `derived` | exact positive-root enumeration that disagrees with the factored quadratic |
| The complete two-root super-field receding radial contribution is $-2\kappa\lvert q_iq_j\rvert\chi/d^2$ | `derived` | exact evaluation of both retained roots giving another radial sum |
| A symmetric two-sided inbound $U>c_f$ affine pair has no recent partner roots and reaches prescribed coincidence | `derived` as a prescribed local control; EOM reachability not claimed | a positive-delay root on either displayed affine partner row, or failure of the prescribed paths to meet |
| A bounded retained super-field history of sufficient depth has a positive-delay self root | `derived` | a path satisfying the displayed endpoint signs whose chord residual has no zero |
| Finite simple roots with uniform range, $D_t$, inactive-gap, and history margins admit a unique short DDE extension | `derived` conditionally | loss of local root uniqueness or solution uniqueness before any displayed margin closes |
| The explicit bounded four-Architrino shelf history has fourteen simple roots, and its unique EOM future has first target coincidence in $0.0049999999875\ldots<T_\ast<0.005$ | `derived` | an additional root, a listed root with $D_t\ne1$, failure of either acceleration comparison bound, or a compliant EOM continuation with no first contact in the bracket |
| Collision histories near the shelf-history event have local codimension two on the displayed transverse-bump slice and are not an attracting open set there | `derived` on the regular pre-contact flow and stated retained-history slice | failure of rank three for the relative-position map in joint history/time space, or an open neighborhood in that slice attracted to contact while the same smooth chart remains active |
| Collision has zero local probability for an ensemble absolutely continuous in the two transverse bump coordinates | `derived conditionally`; no physical ensemble selected | positive collision probability under such a measure or positive two-dimensional area of the local collision set |
| Four-body affine causal-shadow probe reaches separation $3.9999997915\times10^{-5}$ with empty target-partner root sets before the endpoint request halts | `measured diagnostic`; invalid as reachability evidence because its declared spatial envelope is violated | replay with a different prefix, partner-root count, or halt code |
| The unchanged solver certifies all sixteen initial shelf-history channels and fourteen $D_t=1$ roots but rejects the first step on the target self channels | `measured diagnostic`; not reachability evidence | replay with another root census, factor enclosure, or halt route |
| The unchanged independent oracle evolves the shelf history through $T=0.0048$ with 1152 complete accepted rows, separation $0.0007999999806150332$, and $\mathbf h=0$; a straddling fixed step fails closed after a complete candidate census changes each target-partner count from one to three | `measured` accepted-prefix evidence; boundary step failed | replay with an incomplete pre-boundary row, different endpoint state, nonzero $\mathbf h$, or no topology change in the failed boundary step |
| A bounded-velocity blow-up of a surviving inverse-square row does not yield a nondegenerate exceptional-sphere flow | `derived` for power-law time rescalings on that declared chart | a continuous nondegenerate boundary field preserving bounded velocity, finite rescaled memory, and the original roots |
| The bounded causal-shadow approach has a one-sided exceptional-sphere flow whose incoming fixed direction is radially attracting and angularly repelling | `derived` | a different boundary flow or stability sign under $dT/ds=\rho$ |
| The one-sided causal-shadow blow-up does not extend continuously to the outgoing two-root chart | `derived` | finite outgoing $\mathbf w'$ under the same scaling while preserving both newborn rows |
| The explicit crossing has no $C^1$ finite-velocity continuation that inherits the incoming velocity and retains all newborn partner roots | `derived` | a complete-row continuation with finite inherited velocity and finite acceleration integral |
| Vanishing-delay spectral escape obstructs finite-regularity continuation | `inferred` — transfer from constant-lag systems unproven | a continuation with bounded local regularity across a coincidence event |
| Only absolute-time translations preserve canonical root admission for the full history class at fixed $c_f$ | `derived` | a non-translation preserving every stationary and moving retained-history root pair |
| A same-transmitter near-diagonal root sequence requires $v=c_f$ and vanishing quadratic and cubic residual coefficients; under a one-sided $\lVert\mathbf V\rVert\le c_f$ bound, nonzero equality-speed curvature gives a strict cubic deficit | `derived` for the general $C^4$ coefficient test and `derived conditionally` for the bounded-class curvature sign; no global ceiling claimed | a root sequence with $v\ne c_f$, a nonzero leading residual coefficient with accumulating roots, or a curved equality-speed bounded path with roots accumulating at zero delay |
| Three-architrino EOM control increases the initial-normal $h$ component by $0.0065138089981$ over $0.135183$ turns | `measured` on the declared bounded arc | replay with incomplete accepted-step census or nonpositive increment |
| Codimension two makes coincidence locally nongeneric but does not exclude invariant exact-symmetry histories | `derived` on the displayed smooth chart | a rank-deficient collision map or a proof that codimension alone excludes an admitted invariant history |
| Different-time path crossing is the normal interaction mode, not a coincidence hazard | `derived` | an admitted root requiring transmitter and receiver at the same point at the same absolute time |
| Three-body coincidence is codimension five, so more strongly excluded than pairwise | `guessed` | a symmetric three-architrino configuration whose retained dynamics drives all three separations to zero together |
| Coincidence is unreachable in a populated universe | **not claimed** | — |
| Coordinate coincidence is reached by at least one populated EOM history | `derived` by the four-Architrino construction and independently corroborated on the pre-contact chart; formal hybrid-certificate review open | failure of any displayed root, bound, continuation, symmetry, crossing step, or accepted-prefix replay |

## Nonclaims

This packet establishes no continuation, passage, rebound, coordinate-crossing outcome after first contact, outgoing history, boundary value, event semantics, conserved account, global stability verdict, physical-frequency estimate, or production EOM solver acceptance of the contact boundary. It does not reopen MEC-006 or MEC-007, does not modify the canonical transmitter-side acceleration row, and treats every receiver-side factor strictly as signed playback rather than as acceleration strength, per the workstream's revocation boundary. It establishes first-contact reachability on one exact populated symmetry set, not generic attraction or positive-measure physical incidence. It also establishes a symmetric pair-center repeller, disproves a universal pair-sign claim, preserves a conditional invariant region on positive-coefficient subclasses, and proves radial first exit on uniformly sub-field regular charts.

The derived construction answers MEC-008's mathematical domain question in favor of reachability and rules out domain exclusion as the global resolution. MEC-008 remains `In progress` only because the theorem-plus-unchanged-oracle hybrid certificate has not received a separate independence adjudication. The independent oracle now accepts twelve fixed steps through $T=0.0048$ with 1152 complete rows and fails closed on a separate request whose next fixed step straddles the predicted topology boundary; the production solver's separate shelf request still rejects before its first step. MEC-002 and MEC-003 may cite reachability as a derived theorem target input, but may not claim formal MEC-008 acceptance, continuation, or a selected boundary rule.

This document answers that two architrinos can touch on an admissible populated EOM history. It simultaneously proves that the event is locally codimension two on the exhibited smooth chart, so a generic transverse perturbation causes a miss rather than attraction. The secular sign is history-dependent outside the symmetric chart, and uniformly sub-field regular approaches remain excluded.

Closure goal: independently adjudicate the exact first-contact theorem together with the unchanged-oracle accepted prefix and topology-change record; if accepted, close MEC-008 and hand reachability to MEC-002/MEC-003 for a terminal or continuation rule. Separately complete the full-turn areal-rate control without co-authoring its analytical and numerical instruments.
