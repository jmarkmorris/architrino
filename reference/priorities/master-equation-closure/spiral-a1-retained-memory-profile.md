# Spiral A1 Receiver-Normal Retained-Memory Profile Target

Status. Current-law restart target for the A1 finite-memory inverse-rate
profile. This packet does not certify a nonconstant A1 orbit and does not carry
active radial, tangential, force, action, or pass/fail evidence.

Claim level. Priority-only restart target.

## Retained Topology Use

The retained A1 root topology may still seed a current-law search:
$$
P_1,\ P_2,\ P_3,\ S_1.
$$

Root windows, inactive gaps, finite-memory bounds, and source-normal Jacobian
floors remain useful as branch-chart diagnostics. They do not determine branch
strength.

## Current-Law Profile Requirement

A retained-memory profile can re-enter the proof stack only after the same
finite-memory record emits, for every retained branch $\alpha$,
$$
D_{s,\alpha},
\qquad
D_{t,\alpha},
\qquad
W_{\alpha}^{\mathrm{rec}}
=
\left|D_{t,\alpha}/D_{s,\alpha}\right|.
$$

The profile packet must then recompute radial and tangential branch sums from
those receiver-normal factors on the same boxes used by the retained-root
topology rows.

## Promotion Rule

No polynomial, sampled inverse-rate, local-slope, radial-jet, or turn-center
memory witness may be promoted as A1 force/action evidence until it carries the
same-record receiver-normal branch-strength rows above.
