# A polynomial proposal for the next common-height minimum

The [numerical constructor](smooth-two-particle-next-minimum-approximant.py) continues the same supplied two-target past at $g=16$, $c_f=1$. Its output is an exactly defined piecewise polynomial candidate for independent checking. Numerical integration alone supplies no actual-solution sign or turn theorem. The infinite stationary eight-source block sum is omitted from the numerical center and must be enclosed, unchanged, in the independent residual.

## History construction

The candidate first extends all 76 environmental histories already present in the accepted source archive from $33/32$ to $41/32$. It also evolves the 24 newly required environmental labels whose first old-pulse source has squared lattice distance six. These 100 environmental histories cover the full old-excited population at the intermediate prefix. Each new environmental acceleration includes the two supplied old pulses and the generated contributions from the earlier 76 environmental sources at squared lattice distances one or two. Those generated terms sample only the earlier accepted source archive through $33/32$. The original targets have no generated motion at those earlier emission times, so their generated contribution to this intermediate extension is exactly zero. Completeness of this source selection is a separate mathematical census obligation.

The second stage extends the right target from $259/128$ to $9/4$, using these newly evolved source prefixes. The source table also contains the other original target as label 76: its entire needed forward history is the exact reflection of the directly certified right-target polynomial. Multiplication of the displacement, velocity, and acceleration $x$ components by $-1$, with the other components unchanged, implements reflection in the plane halfway between the two lattice anchors. Thus the reception of the other target's generated motion is included. A self row is excluded by identity. The additional 24 environmental labels occupy indices 77 through 100, leaving all original source indices and the partner index fixed.

The first 76 source labels retain their previous ordering. Every corresponding position, velocity, and acceleration node through $33/32$ is copied bit for bit. Every right-target node through $259/128$ is likewise copied bit for bit. For the added left-target source, the directly accepted right-target prefix is reflected exactly. A common nodal triple defines both adjacent cells, so the joins preserve position, velocity, and acceleration exactly when the binary64 node values are interpreted as dyadic rationals. The exact quintic reconstruction is the frozen `exact_coeff` function in the prior approximant constructor.

All paths use grid spacing $1/1024$. No history evaluation beyond a declared positive-time endpoint is allowed; the helper asserts its domain before evaluation. Negative-time generated displacement and velocity are zero. The original supplied pulse is evaluated separately by its compact polynomial, with its original timing and transmitter-side density factor.

## Validation order and evidence limits

Before the first target use, `known` returned `PASS` for a degree-five polynomial history, the zero generated past, rejection of future extrapolation, an analytically known ordered nearest-neighbor graph, and exact rational endpoint conditions. After adding the exact partner reflection, the repeated known-case pass also verified the reflection and its involution before the new target run. The target run additionally checks exact preservation of the inherited nodes and equality of the partner nodes to the exact reflected right-target prefix. Neither these implementation controls nor root residual diagnostics establish completeness of a physical root sum or the distance of this candidate from an actual solution.

The durable machine outputs are under `.local-data/master-equation-closure/next-minimum/approximant/`. `candidate.npz` holds the shared dyadic nodes and graph labels; `candidate.json` records their hash, producer hash, prefix hashes, graph sizes, history-time diagnostics, measured turns, and wall time. The independent full-law certificate and continuation argument own any subsequent actual-solution conclusion.

The completed constructor measures one next candidate minimum at $t=2.221894879065627$, with height $3.762178562461883\times10^{-7}\ell$. This is a numerical proposal for the independent sign proof. The final interval is $[0,9/4]$. The 101 source paths have 1313 shared nodes each; the right target has 2305 shared nodes. The 988 stored environmental generated rows and 80 stored target rows are conservative candidate lists that include inactive rows, not counts of entered physical responses.

The largest source emission time sampled while constructing the intermediate paths was $0.2812531660051387$, within the retained earlier archive. The largest source emission time sampled while constructing the final target was $1.2500010014386795$, within $41/32$. The maximum numerical causal-root residual was $4.440892098500626\times10^{-16}$; this is an implementation diagnostic, not a root enclosure. The final known-case-controlled construction completed in 9.4064 wall seconds. The machine receipt binds these values to the producer and array hashes.

The subsequent operator-authorized removal of historical script-hash enforcement deleted only the constructor's assertion that its current bytes equal those recorded by the known-control receipt. Source hashes remain provenance, and archive/data hash verification remains active. Fresh known controls passed after this single-assertion removal. The numerical constructor was not rerun: the candidate array and its generating receipt retain their original bytes and original producer hash, while `known.json` records the current source hash. This change affects the launch guard and does not alter the equations, numerical arithmetic, or candidate data.

## Numerical illustration

The local figure is `.local-data/master-equation-closure/next-minimum/approximant/figure/next-minimum-candidate.png`. It samples the frozen candidate quintics. Shading marks the interval added after $259/128$. The marked minimum is numerical and carries no actual-solution error band. The local plotting script, `.local-data/master-equation-closure/next-minimum/approximant/figure/plot.py`, passed a known quadratic sampling control before reading the candidate archive; its receipt records the input and figure hashes. Visual inspection verified legible labels and no clipping. These ignored local artifacts are identified by literal paths so that the tracked document does not create links that fail in a fresh checkout.

The sibling figure `.local-data/master-equation-closure/next-minimum/approximant/figure/next-minimum-certified.png` preserves the numerical curve and adds the separate interval instrument's height error band and fourth-turn window from `.local-data/master-equation-closure/next-minimum/check/turns.json`. Its lower panel enlarges the minimum. The figure labels the certificate bounds as conditional on independent review while that assessment is pending. Fresh known controls verified quadratic sampling, the conversion to millionths of a lattice spacing, and a symmetric error band before this rendering. The source archive and residual-receipt hashes were checked; visual inspection found legible labels and no clipping. The candidate array was not rerun or changed.

## Reproduction

```bash
"${AAA_VENV:-../.venv}/bin/python" reference/priorities/master-equation-closure/analysis/smooth-two-particle-next-minimum-approximant.py known
"${AAA_VENV:-../.venv}/bin/python" reference/priorities/master-equation-closure/analysis/smooth-two-particle-next-minimum-approximant.py target
```

An extrapolated source future, a missing generated source identity, a failed prefix-equality assertion, a failed independent residual enclosure, or a continuation bound that does not cover the required histories overturns the corresponding use of this candidate. The numerical center alone cannot establish an actual fourth turn, a smaller actual excursion, damping, or typical behavior under another preparation.
