# Independent final current F5 run review

Measured by `review-final-current-run.mjs`, after its known SHA-256 positive and changed-input rejection controls, the exact current run completes the declared 8, 32, and 128 sample ladder. Six recorded producer/checker stages exited zero with closed process groups, no timeout, no interruption, and no recorded remaining descendants. The unchanged final ledger reducer accepts 24,192 rows and the two genuine prefix checks accept their supplied rungs. All recorded run dependencies, rung artifacts, prefix source/implementation bindings, final raw-input bindings, and stage-log sizes were independently rechecked against live bytes. The measurement result and exact artifact hashes are in `final-run-review.json`.

This closes the bounded current F5 enclosed-root execution and independent build/API applicability review. The original build-admission artifacts remain byte-identical. The actual current executable is the byte-identical admitted copy, and the independent unchanged nominal/API certificates bind the exact fresh-build manifest. The result preserves the reducer's census, certificate serialization, source-byte, and history-token claim boundary. This is evidence for the declared prescribed-history execution only: H3 eligibility remains false, and there is no ordinary evolution, retention, stability, binding, score, particle identity, physical realization, overall test health, or circular-execution acceptance.

The raw run retains `prescribed-root-ladder-passed-pending-review` as historical producer output; this separate review supplies the independent disposition without rewriting the produced evidence. A changed dependency, mismatched raw artifact, failed prefix/final predicate, or nonzero/unclosed actual owner result overturns the disposition. The owned execution record is `efd61ef6-af27-47c9-874c-058a3b7fdbb7`; its full owner closure is retained by the execution worker in the current acceptance evidence.
