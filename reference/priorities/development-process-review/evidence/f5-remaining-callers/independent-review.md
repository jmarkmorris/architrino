# Independent original-input transport review

This reviewer did not author the transport or the display/interpolation test changes. `shasum -a 256` measured the reviewed materializer `scripts/eom/prepare-f5-original-input-tree.mjs` at `881ffd557e8459f259f948613a44e1c36e3b27804919c0e9468a541e25f09373`; its four-control test at `1f06d70304d02079d07d65bea992e5116607630696ec64b6e3864190937168eb`; interpolation test at `41e05c8e3873f89ebdc5b9f6c575bfcb07a798e479c375298d10692366d7f711`; and display test at `275f872fcaaad0e23e406bd3e47542a73d246e697b0d6d9179fdf4efdeaa489d`.

Source inspection finds no blocking issue for the transport's declared scope. Five required original digests are fixed independently in the materializer rather than supplied solely by routing metadata. Routing requires an exact unique logical census, correct expected hashes, canonical relative physical paths, and matching actual bytes and sizes. Regular-file capture rejects symlink aliases and parents through canonical-path comparison, opens without following leaf symlinks and without blocking on a FIFO, and checks file metadata and byte length across the read. Captured current sources are copied byte-for-byte. The materializer rechecks source bytes, original data, destination bytes, routing metadata and source census before its last record is written.

`--check-root` reconstructs the expected source census and original-input records from the origin; it does not trust the stored record to choose files or provide its own accepted hashes. It compares the required identity/authority fields, route binding, source census and full file-record array against that expected profile and checks each copied file. Missing final records, omitted record entries and changed copied source cannot satisfy it. This is path-based consistency at the checked boundaries, not atomic execution authentication or a closed-world directory/import policy: extra output files are permitted, interpreters and dynamic-loader dependencies are not authenticated, and later mutation remains outside a completed check. Existing consumers keep those execution and scientific obligations. The documentation states these limits and does not use the materialization record's `accepted: false` as scientific admission.

The display test verifies the archived pilot's original SHA-256, retains the current real version-three schema validator, and compares all seven declared scientific fields after an explicit identifier map. Its transformation changes matching string names and preserves numerical values. The interpolation test calls the real unchanged production instrument from the explicit root, asserts the current instrument digest and logical config name, and privately projects only those two metadata fields before comparing the complete serialized result digest with the preserved original. The produced receipt is not modified. This remains regression reproduction, not an independent new mathematical proof.

`git diff --name-only` over the preparation, prefix, ledger CLI/reducer, interpolation instrument and four named Python oracle files returns no paths: this batch does not modify those subjects or references. That command's scope is those named files, not a claim about every repository file. The independently retained original report and route objects are read as inputs.

Independent owned run `ea0dac59-bbca-4a37-8c42-0f4490a26b6b` executes the three reviewed test files with `node --test --test-concurrency=1 --test-reporter=tap`. Native TAP reports eight passes, zero failures, zero skips and zero cancellations. The lease reports exit zero, 13.313 wall seconds and process-group closure. Its native log is retained alongside this review. The transport's known `abc` digest and unsafe-file controls precede its real route controls; the earlier pre-materialization control is separately recorded in [known-control.md](known-control.md).

Verdict: the exact reviewed implementation is suitable for the explicitly named original-input filesystem transport and the disclosed display/interpolation regressions. A changed copied/original byte accepted by the checks, a numerical difference concealed by a metadata projection, or a missing required consumer dependency would falsify the corresponding scoped claim. No nominal-history certificate, reviewed enclosed-root build, accepted real prefix/rung result, complete test-health verdict or wider scientific acceptance is supplied by this review.
