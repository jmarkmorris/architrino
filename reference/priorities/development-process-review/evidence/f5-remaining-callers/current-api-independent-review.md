# Independent current F5 API applicability review

This review did not edit the adapter, builder, API reference, nominal proof or acceptance runner. Its subject is the applicability of the original expanded-domain API proof to the actual current manifest and the recorded current build. It does not cover root completeness, final ledger admission, or the complete execution lifetime.

## Source transition

`git show 0fb575921783188ce528a45c671090e9ecc00464:src/eom/native/eom_f5_enclosed_root_cli.cpp | shasum -a 256` returns the original required `9f7661f4000174d631d4c60f7078e124d77ae9b2ddba6af36197f13096095f81`. The exact diff from that generation to `897fe1aa79be7ae1e77144d52ef396d209645323` changes one configuration-path literal from `f5-phase-varying-campaign.v2.json` to `phase-varying-prescribed-display-history.v3.json`; its expected configuration-byte digest remains `e92e450c8ea83086b60184d31ff5b07fe8a470b1e20088ea312592f2b38800fb`. No adapter computation changes in that diff. The current adapter digest by `shasum -a 256` is `16a067847d77348b4c4d809dbaece8a4a80b91a6e9568198603b0338407d38f6`.

The CMake diff from the same original generation changes only the Borg protocol-version test regular expression from V10 to V11; current digest is `dc78fe2643e6d7f76cf7787b02133e9815226ff7248aff4c6fec790a528d53f4`. The four original History/Interval header/implementation digests still match by `shasum -a 256`. This measured transition leaves the arithmetic/API semantics examined by the original proof unchanged conditional on the required configuration bytes being supplied at the current adapter's declared path. It is not an equivalence claim for arbitrary source substitutions or all linked solver behavior.

## Reference execution boundary

Reading `f5_api_domain_conformance.py` shows that `_verify_snapshot` authenticates the six original API source identities before and after `verify_files`. The latter independently validates the actual manifest against the exact configuration and nominal certificate, calls unchanged `certify_api_segment` for every segment, checks all 12,384 member segments, rechecks its inputs and reports its original subject-binding census. The local arithmetic helper explicitly does not return an accepted manifest receipt.

A reference-only tree containing those exact original source bytes can therefore execute the unchanged complete API proof against the actual current manifest and nominal certificate. It must remain honestly labeled as the original API proof. A separate applicability record must bind that complete proof receipt, actual manifest and nominal certificate, original/current API source pairs, the configuration identity, current build identity, and this narrow source-transition review. It must not replace the original receipt's `subjectApiBindings`, mutate the oracle's global source map, compile the originals as if they were the current producer, or infer root/ledger acceptance. This is the reviewed transport design; no completed current API receipt is asserted here.

## Recorded build inspection

The [read-only review instrument](review-current-api-build.mjs) passes known SHA and mismatch-rejection controls before inspecting the actual tree. Its [result](current-api-recorded-build-review.json) checks all 48 recorded repository source bindings, two built artifacts, the compiler shim, two external library identities, cache and compile-command hashes, and both successful closed build stages. It rechecks stage log sizes, records their observed hashes, and verifies that the built artifacts postdate the copied source files. The toolchain bytes are `6786f9c0ce07f63fd7b41395e22baa1defa3d361d3d91d47e606c18efbb68685` (10,625 bytes); the actual manifest is `bbfb55e2c789b5e6f034745c8e7e1b3a7d502b97082862558fb8eb4c6a707749` (11,837,478 bytes).

This record does not supply a before-build external-toolchain census, CMake/linker identities, the actual Xcode compiler behind `/usr/bin/c++`, or external headers. Consequently it supports its recorded build identity, not the stronger complete before/after source/tool/header/runtime-capture contract used by the newer build owners. A later census cannot retroactively establish those before-build values. Complete current-build provenance requires a fresh captured build or an explicit narrower trusted-toolchain boundary; this review supplies neither a silent waiver nor a blanket substitution for the previously reviewed F5 inspector build. Any changed recorded source, cache, compiler, library, output, failed stage, changed source-transition diff, or mismatched actual manifest overturns the corresponding bounded finding.

## New captured builder source review

The separately authored `scripts/eom/prepare-f5-enclosed-root-build.mjs` at SHA-256 `4df9d54cf3627fa5cb0b3acd47bfb3f941bf8b0f82db7800c7d4fa0e3f3df14e` addresses that missing execution capture. `diff -u` against the previously reviewed handoff builder shows a focused adaptation: it removes the handoff-specific Python/inspector owners, selects the exact enclosed-root adapter and its original four API dependencies, changes the receipt/output/dependency identifiers, and retains the complete repository source snapshot, actual compiler/tool resolution, compiler-reported header census, external library checks, actual dependency comparison and final byte rechecks. Its only built-executable invocation is `--help`; it loads no configuration and makes no root request. The unchanged shared helper pin is `3d372147742b1036bb3101b7a8b39fcfeb1b5cb7176c3c1f7edeaee5635b9802` by `shasum -a 256`.

Source-review verdict: this exact builder may proceed to a bounded fresh build. Actual receipt/output inspection remains required before build admission. The macOS shared-cache and non-atomic file-path recheck boundaries remain explicit. This source verdict neither proves an actual build passed nor supplies API/root/ledger acceptance.

A follow-up trace of `cmake --build` identifies an inherited tool-census gap: the configured `CMAKE_MAKE_PROGRAM` executes the generated build, normally through `/bin/sh`, while the initial tool list does not bind either executable. The cache digest binds a pathname rather than its executable bytes. Final captured-build admission therefore also requires both identities before compilation and their after-build rechecks; the worker was notified before final acceptance. A build performed without them retains the narrower recorded scope until rerun with the corrected census.

## Final captured build and applicability admission, 2026-09-08

Independent `review-current-captured-build.mjs` first passed the standard SHA-256 `abc` answer and changed-input rejection, then rehashed the final `acceptance-20260908-c` receipt and its live inputs. The receipt binds 50 sources, 12 tools, 2,018 compiler-reported headers, 23 dependency units with before and actual dependency files, external libraries, outputs, and 42 successful closed stages. The final builder `eeb342b80ab855930d58d30a23eaa88ee8d8376ec3117d1c4f0e7d2cceb25ee6` includes the explicit Unix Makefiles driver, typed `CMAKE_MAKE_PROGRAM:FILEPATH`, and captured `/bin/sh`; this resolves the earlier build-driver omission. The explicit macOS shared-cache boundary remains platform trust, not a file-hash claim.

The same instrument measured exact byte equality between the fresh captured executable's manifest and the manifest used by both unchanged certificates. It checked the full API receipt's 12,384 segment passes and the original instrument identities. Equality here transfers these exact manifest bytes to the fresh build; it is not an independent numerical reference. The separately authored unchanged nominal and API proofs supply the bounded enclosure reference described above. Original API source bindings remain original, with applicability transferred only across the reviewed configuration-path literal and CTest-selection changes.

Source review of the current wrapper confirms that admission adds exact proof-input identities, complete build census, before and actual dependency-file bindings, and byte-identical executable/library copies inside the explicit input root. Both original outputs and execution copies remain bound. The wrapper rejects runtime injection variables, requires macOS and the captured Node runtime, and feeds its evidence into the shared runner's repeated binding checks. The numerical rungs and final reducer predicates remain unchanged. The admission binds the actual snapshot copies of both executing scripts, including wrapper `d4288208fff939ac96d7554bdd78094db6044c0dd8512cdf65e8621ed3489c43` and runner `5c117182438600f336f4bf5c4047ae0ba61a6af86952653f53db29f3434bcd23`.

This review admits the exact captured build and bounded API applicability for attempting the declared rungs. It does not accept root completeness, any rung outcome, circular execution, overall test health, evolution, H3, or a scientific result. Any changed bound byte, failed original certificate, unequal fresh manifest, changed arithmetic source, unclosed build stage, or unresolved runtime dependency overturns admission. The machine admission record binds this review and the independent measurement result.
