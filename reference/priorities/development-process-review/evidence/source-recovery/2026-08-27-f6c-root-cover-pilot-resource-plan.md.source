# F6c One-Cell Root-Cover Resource Plan

Status: technical launch plan awaiting separate consumer and watcher review, 2026-08-27. This document does not authorize a full 160-cell run. The [fixed mathematical predeclaration](2026-08-27-f6c-continuous-reception-root-cover-predeclaration.md) owns the scientific subject, original history allowances and exact root-cover contract. The immediate consumer is its first closed reception cell, followed by the unchanged independent rational comparison. No EOM evolution, acceleration, residual, quadrature, metric, score or physical claim is included.

Plainly: this first attempt checks whether the reviewed continuous-time calculation can handle one actual cell and whether the independently written checker agrees. It is not the full measurement campaign.

## Fixed Scope And Limits

| Control | First-attempt requirement |
| --- | --- |
| Scope | `pilot-cell-0` only: closed reception interval `[0,0.001]`, 64 pair-cell records, 56 ordinary nonself rows and eight self exclusions |
| Prescribed data | The unchanged eight-member, 14,080-piece export and its accepted reconstructed-family and uniform-guard receipts; all original scalar allowances retained |
| Arithmetic | Frozen 90-digit outward library followed by the separately frozen exact-rational comparison; no tolerance change, root refinement or automatic precision escalation |
| Emission proposal | The already declared first-cell interval `[-8,-0.05]`; no fitting to a sampled midpoint |
| Concurrency | One consumer, then one comparison process; no overlapping F5 response pilot or second F6c numerical attempt |
| Inclusive deadline | 1,800 seconds from external launcher start through capture, consumer closure, independent comparison, final durable publication, cleanup and successful final completion |
| Heartbeat | External flushed stage, elapsed time and observed completed-row progress every 15 seconds; internal progress is supplemental |
| Memory monitoring | External aggregate resident-memory samples of the active owned computation group no more than one second apart; stop at 2 GiB or loss of valid monitoring |
| Host admission | At least 40 percent reported free memory and 64 GiB available disk before launch; stop below 20 percent free memory or 16 GiB available disk during the attempt |
| Host observation | Every 15 seconds and after each process closes; each memory-pressure observation has its own two-second timeout |
| Scientific input bound | Each fixed scientific JSON at most 64 MiB; no discovered scientific input paths or substituted histories |
| Output bound | Each of the three raw subject files at most 64 MiB, comparison receipt at most 64 MiB, and at most 16 MiB combined operational logs/rejection diagnostics |
| Retry | None automatically; retain a rejected attempt and its exact failure before any separately reviewed revision |

Plainly: these are stopping limits, not predictions of actual runtime or memory use. Sampled resident memory is an observation with gaps between samples, not a hard allocation ceiling. The first attempt cannot silently turn into the full campaign.

## Execution And Publication Obligations

Before launch, freeze a machine-readable plan binding the reviewed consumer, its controls, the unchanged independent comparison and controls, the mathematical declaration and three captured library modules. Its closed `comparisonContract` must name `pilot-cell-0`, exact source byte counts/hashes, and the resolved shared-venv interpreter, resolved Git binary, and loaded standard-runtime source/cache bindings needed by the isolated Python process. No source or runtime hash is inferred from an untrusted output. Loaded runtime-file binding is not a hermetic operating-system or dynamic-loader attestation; those platform premises remain explicit.

Plainly: the launch plan must identify the program and data that actually run. A cached or substituted implementation cannot acquire authority merely by printing the expected filename.

The consumer must execute captured project-library bytes in a private package, retain complete original closed-piece coverage, and publish only `accepted:false` conditional records. Its three files are `rows.ndjson`, `pieces.ndjson` and `cover-manifest.json`, under a new exclusive child of `.local-data/braid-analysis/f6c-continuous-reception-root-cover-20260827/`. The external watcher authenticates fresh consumer completion and process closure, then independently rehashes all three files before invoking the unchanged comparison. The comparison gets the exact manifest and launch-plan hashes and the remaining time from the original inclusive deadline. It must not import the subject or its root-cover library.

Plainly: a complete-looking output from the first program is only a candidate. The second program checks its mathematical enclosures independently, after the first program has finished successfully.

The watcher must survive synchronous computation and publication stalls, retain control of every owned process group, and reject on lost monitoring, source drift, incomplete rows, changed output, timeout, interruption or failed cleanup. A process may not start numerical work before the supervisor has registered its identity. A temporary passing comparison file is not admission: the external process must close successfully, its final output hash must be checked, and the complete attempt must remain within the original deadline. The final operational receipt records measured elapsed time, maximum sampled group RSS, sampling gaps, log/output byte counts, source bindings, both child completions and complete owned-process closure. No field may present a forecast of later publication or closure as an observed fact.

Plainly: success belongs to the complete, checked attempt, not to an intermediate file. Late or interrupted output remains useful failure evidence but cannot count as a passing pilot.

## Decision After The Pilot

A complete passing pilot establishes source-bound conditional root coverage for that one closed reception cell only, under the accepted reconstructed-family premises. It does not establish the identity of a historical trajectory or the full 160-cell result. A separate full-run resource plan must bind the actual pilot and comparison hashes and use their measured costs before any wider execution. Failure names the failed mathematical or operational obligation without changing the source geometry, enlarging allowances, replacing histories, or advancing a score.

Plainly: one good cell would remove a practical blocker and provide measured planning evidence. One failed cell would identify the next exact repair or unresolved bound; neither outcome decides whether F6c is a retained braid.
