# Refined acceleration transport recoverability

**Current disposition, September 11: ✓ Done — independently reviewed admission decision; ◐ Partial — I4 transport implementation and validation remain uncompleted.** The [refined-specific decision](#refined-admission-decision--september-11) permits a separately versioned current transport to distinguish retained-record verification from incomplete original-environment availability. It resolves the missing-utilities decision prerequisite; it does not recover the utilities, make the existing strict verifier pass, or complete I4. The earlier recovery investigations below retain their measured search limits and original conclusions. The candidate-producing numerical function `project_cell`, its range reference, and the separately authored `compare_candidate` path remain unchanged. No partial replacement transport was added.

## Availability measured against the actual contract

`check-original-input-availability.mjs` reads the entrypoint's `FIXED`, `REFINED`, and `PRIOR_OPERATIONS` inventories, authenticates both original admission files by their declared SHA-256 digests, and inventories their source bindings and stage stdout/stderr bindings. It normalizes paths before counting path-plus-hash identities. Known SHA-256, exact bytes, substituted bytes, and wrong-size controls pass before the target scan. The resulting [availability record](original-input-availability.json) reports 235 distinct obligations, of which 34 no longer match their live paths. Retained archives supply 31 of those 34. An initial exploratory count of 247 did not normalize relative/absolute aliases; 235 is the corrected canonical count.

Archive search is explicitly bounded to files beneath `.local-data` and `reference/priorities` with a `.source` suffix or the basename `ps`, `git`, or `memory_pressure`, and at most 64 MiB. It does not establish absence elsewhere on the host, in system backups, or in external archives. The remaining three originals were not found in that search:

| Original input | Required SHA-256 | Required bytes |
| --- | --- | ---: |
| `/usr/bin/memory_pressure` | `a1668e28505400a9e09ab9b2bd2558f04d038152dfdb05826576a0a0aa27fe56` | 135248 |
| `/usr/bin/git` | `179301dcb41ea78accc3fa0048a7e6f6710d891945a751a34addd622020c1818` | 118928 |
| `/bin/ps` | `472992c470606d28f577590decfecd7f4a20f832fd92c671bebc6d44790b5d02` | 170816 |

The one missing repository control, `tests/f6c-emission-refinement-pilot.test.js`, was recovered from commit `0fb575921783188ce528a45c671090e9ecc00464`. Its new data-only `.source` copy has exactly the admitted digest `b7b91ae569b20a1a53e7c37a63de3edc65e844019ee5a8bae03bc84d2d9dd277` and 31,325 bytes; [recovery provenance](unit-control-recovery.json) records the source. It was not executed.

The two original theorem archives are already present and hash to `f20e4bdaaff8b6f0012fdc6135b15d568a817832fb55d5c42f80d8421a117f68` and `6abbbbacc1671052bdd881790094dbd71ebb03d54904ac1f937edae1f3c9f936` by `shasum -a 256`. The current refined declaration itself matches the refined wrapper's `a9d871a35e6e9f00e96ba07182798cb87f546eabe0664e7f170b67c820bb43fc` pin by the same command. The older archived refined declaration has a different, preserved identity; it is not interchangeable with that current named binding.

## Why fresh execution bindings do not close this gap

The preserved refined verifier's `authenticate_refinement` requires the full 202-source admitted refinement map, checks every original source through its `read_binding` callback, and validates original stage output logs. The producer's `authenticate_refined` independently enforces the corresponding obligation. Thus these three system executables are required as original evidence bytes even if a future run correctly binds today's system executables for new process observation. Updating new-run operational hashes cannot establish the old byte identities. Passing current process tests cannot establish them either.

The preserved verifier also compares original manifest ancestry with the caller-supplied ancestry map. The [recorded ancestry comparison](recorded-ancestry-differences.json), authenticated against the pinned original manifest and preceded by known equal/changed metadata controls, identifies five differences between the current wrapper's expected documentary generations and the original manifest: prior closure owner, acceleration reference proof, member predeclaration, root theorem, and reconstruction theorem. Their original archives are available. A future transport must route these original logical identities consistently through the original evidence chain and retain the current named execution inputs separately. It must not make a mixed-generation source map appear to be the original admitted map.

## Supported next step and falsifier

Recover the three exact original system binaries into explicitly bound data-only archives, then implement and test the current transport against the complete original source census. Finding files with all three required SHA-256 digests and sizes overturns the present availability blocker. If recovery is impossible, a separately reviewed change to historical evidence-admission obligations is required; this review neither proposes that missing bytes be silently waived nor authorizes rewriting original records. Source availability does not itself accept the scientific result or authorize a full numerical campaign.

## Wider local recovery follow-through

After rollover the operator requested continued original-binary recovery. The named-candidate [comparison instrument](check-host-binary-candidates.mjs) first passed exact digest, substituted-byte and wrong-size controls, then compared the three installed utilities and the Git executables bundled with Command Line Tools and Xcode. None matches any required original identity; [the comparison](host-binary-candidates.json) records their exact current sizes and hashes. Candidates were read as data, never executed. The two developer Git executables are different objects from the much smaller `/usr/bin/git` system launcher, so their availability does not satisfy that binding.

Read-only backup inspection with `tmutil destinationinfo` reports no configured destination; `tmutil listlocalsnapshots /` lists no Time Machine snapshots. `tmutil listbackups` reports no machine directory and an operation-not-permitted error, so that command does not prove absence of every possible backup. `diskutil apfs listSnapshots /` lists one snapshot; `diskutil info /` identifies that same snapshot as the currently mounted root. It therefore supplies the current bytes already rejected by the comparison, not an older generation. `ls /Volumes` lists only Macintosh HD. No historical snapshot was mounted or modified.

The exact-name `find` search under `/System/Volumes/Update`, `/Library/Updates`, `/Library/Developer/CommandLineTools` and `/Applications/Xcode.app` found the two developer Git executables and their shell-completion text. The update Controller, Hardware and `.TemporaryItems` directories denied access, and the search exited one. Thus the search establishes no matching accessible executable candidate in those locations, not exhaustive absence from protected update storage. Spotlight's exact-name query returned no candidates and is not relied on as an exhaustive filesystem search. The accessible update `mnt1` directory is empty by `ls`; `lastOTA` lists update logs rather than a retained binary tree.

The original-byte blocker remains. A backup, installer image or another host may supply the required objects, but no such source was available in this bounded local inspection. Recovery requires matching both the table's digest and byte size before the files enter data-only evidence storage. Recompiling public source, substituting a current executable, or changing the expected digest would establish a different object and cannot close original-input authentication.

## Disposition in the approved items 1–8 continuation

The renewed continuation supplies no additional backup, installer or host location. The retained search and access limits above therefore remain the evidence for item 2; repeating the same searches would not broaden that evidence. Item 3 remains blocked by the actual original-source authentication contract, and no partial refined transport or weakened historical verifier is substituted. The next actionable input is an accessible candidate source for the three table entries. If exact originals remain unavailable, the alternative is a separate reviewed admission-contract decision that explicitly distinguishes unavailable historical authentication from any new current result. This is an unresolved prerequisite, not a completed refined implementation.

## Refined admission decision — September 11

The operator requested resolution of the OPS-024 refined I4 prerequisite by exact recovery or a bounded independently reviewed admission decision, with changes confined to owners, queues and the work log. Under that authority, this decision accepts the narrower admission boundary specified below. An independent read-only contract reviewer inspected the producer, verifier, wrapper, frozen projection declaration and prior parent contract, and accepted this boundary while rejecting a claim that documentation alone completes I4. A separate evidence reviewer rechecked the retained bindings; a third reviewer investigated new local recovery leads. These reviews supply source inspection and file-identity evidence, not an independent mathematical result or a fresh observation of the historical process.

### Evidence and reasoning

Claim grade: measured. At `2026-09-11T04:13:26.479Z`, `node .tmp/ops-024-i4/recheck-retained-bindings.mjs --target` checked exactly the candidate paths already recorded in [original-input-availability.json](original-input-availability.json), after `--controls` passed known SHA-256, changed-byte, wrong-size and path-alias controls at `04:13:16.540Z`. The instrument independently rebuilt the normalized obligation set from the live wrapper and both hash-checked original admissions: 235 obligations, 34 current-path mismatches, 31 supplied by exact recorded archives, and only the three utility tuples in the table above unavailable. No previously available nonutility binding became unavailable, and all five original ancestry routes in [recorded-ancestry-differences.json](recorded-ancestry-differences.json) matched their recorded bytes. The broad cached-pilot admission has 189 source bindings; the refined admission has 202. The 198-member full-run inventory discussed in the parent migration is a different record. Scratch instrument, controls and detailed result remain under `.tmp/ops-024-i4/`; the durable findings and source references are recorded here. This is an availability snapshot over those exact paths, not a complete admission-validator pass. A failed digest/size check at one of those paths overturns the corresponding availability finding.

Independent `shasum -a 256` and `jq` checks authenticate the broad admission to `1a814c90279eed456546b2c4959a8504657213ffc2d25c063060831814e930ee`, refined admission to `51f0b3774bfb489bbab4fddd7f7612c6d4132f2654a36aa4091e5445eca9b51c`, refined manifest to `d4ec0d60631dd46cf2872ace941677dfd980af6f8cdbe540347b5252d336ebb6`, and original refined plan to `295d1d8a8366942c4aa3f0c586e028faae08e2ac64517c837f0fe73a6b8b8a88`. The plan classifies `git` as runtime and `ps`/`memory_pressure` as operational dependencies. Those classifications and the [earlier source-role inspection](../../analysis/archive-contract-and-coordinator-review.md#what-the-old-host-binaries-are-doing) distinguish implementation-availability evidence from the retained scientific artifacts. Utility bytes would permit further inspection of the old observers; possessing them today would not independently observe the old process table, memory state or exit.

Claim grade: inferred. Separating these verification statements is defensible because all nonutility obligations remain exact-byte requirements and missing utilities remain explicitly unverified. The [approved parent contract](../../contracts/historical-evidence-and-external-closure-v1.md) demonstrates the same distinction for parent version three, but does not authorize refined execution by itself. That contract is pinned by `scripts/eom/prepare-f6c-parent-refinement-batch.mjs` and remains unchanged. This decision establishes the refined-specific authority; it does not reinterpret old receipts or claim that the strict original-source requirement was already satisfied.

### Accepted boundary

1. **Preserve complete original membership.** Authenticate the pinned admissions, original plans, source-role relationships, all scientific inputs, proof/reference sources, stage stdout/stderr and prior-operation logs at their original path/hash/size identities. Only the three exact historical utility tuples in the table above may be recorded as unavailable. They remain members of the original inventories and remain excluded from the physically verified set. A missing scientific source, different utility generation or requested byte payload receives no exception.
2. **Keep current execution physical and separate.** A new transport must explicitly select a separately versioned admission/result contract. Capture its actual current sources, interpreter, runtime and observers independently of historical routes. An unavailable historical tuple supplies neither bytes nor executable authority. Data-only archives must preserve original hashes/sizes, declared route membership, bounded regular-file capture, unique physical identities and final rechecks; current executable capture must bypass historical resolution.
3. **Report the limitation in the result.** Distinguish retained scientific-byte verification, recorded-provenance verification and full original-environment verification. The last remains false while any original utility is unavailable. A new result must not reuse the strict old full-authentication success label or make a frozen `read_binding` callback appear to have physically verified bytes that it did not read. Historical closure remains an authenticated recorded observation at its original conditional scope. Each future attempt needs its own complete stage and outer-process observations under unchanged resource limits.
4. **Preserve original ancestry and independent mathematics.** Route `priorClosureOwner`, `referenceProof`, `memberPredeclaration`, `rootTheorem` and `reconstructionTheorem` through their exact original generations from the authenticated manifest. Preserve all 16 ancestry roles and keep current named inputs separately bound; conflicting generations must never be flattened into one path-keyed source map. Preserve the frozen producer/verifier APIs and predicates, `project_cell`, `compare_candidate`, the independent range reference and all scientific flags. Any bridge-owned structural projection must be explicit, independently reviewed and confined to the declared transport boundary; it cannot change historical records or relax mathematical checks.
5. **Establish the new transport before using it.** Independent literal controls must accept the narrower claim while rejecting wrong or missing scientific bytes, forged tuples, current-byte substitution, unknown/duplicate/unused archive routes, changed physical captures, executable archive requests, mixed ancestry generations and false full-environment claims. Preserve existing source, parser, numerical and cleanup controls. After those controls pass, run exact retained-metadata stages with numerical evaluation trapped and separately verify new process closure. No new numerical run, broader campaign or promotion follows from this decision; any such run retains its own admission requirements.

These conditions define the accepted design, not a claim that an implementation meets it. The independent review locates the remaining work in `prepare-f6c-refined-acceleration.py:630–637`, which checks every member of the 202-source set; `verify-f6c-refined-acceleration.py:319–337`, whose callback contract requires original bytes; and `run-f6c-refined-acceleration-pilot.mjs:163–192`, which combines historical and current bindings by path. Reading these code paths with `sed` establishes that a new current bridge and its validation are still necessary. The frozen [projection declaration](../../../braid-program/evidence/2026-08-27-f6c-refined-cover-acceleration-projection.md#independent-reconstruction-and-source-closure) remains the mathematical and scientific-claim authority.

**Disposition:** the independently reviewed historical-utility admission prerequisite is resolved. I1–I3 exact recovery remains blocked, and full original-environment verification remains unavailable. I4 current transport and its five ancestry routes remain uncompleted with the [Development Process Review queue, item 2](../../work-queue.md#blocked-prerequisites), linked from the Braid shared prerequisites and OPS-024's work log. A future implementation that omits a nonutility binding, hides an unavailable utility, alters a frozen numerical predicate or claims old full authentication would falsify compliance with this decision. Owner edits alone cannot close I4 or make its existing strict historical authentication pass.

### New exact-recovery lead

Claim grade: measured. Targeted `rg -n '25F84|25G83' /var/log/install.log` finds macOS 26.5.2 build 25F84 on July 17 at lines 89229–89241 and August 27 at lines 122225–122237; line 130994 records its replacement by 26.6.2 build 25G83 on August 30. `plutil -p /Library/Receipts/InstallHistory.plist` independently corroborates the installation dates. Inference: an accessible 26.5.2/25F84 installer or host is a specific future recovery candidate. These logs neither identify the missing binary bytes nor attribute their mismatch to an operating-system update.

The independent recovery review's first `find` covered `/System/Volumes/Preboot`, `/System/Volumes/Recovery`, `/Library/InstallerSandboxes` and `/System/Library/AssetsV2/com_apple_MobileAsset_MacSoftwareUpdate` with `-maxdepth 5`, selecting regular files named `git`, `ps`, `memory_pressure`, `SystemVersion.plist`, `Info.plist`, `*.dmg`, `*.ipsw` or `*.pkg`. It exited one because `/System/Volumes/Preboot/.TemporaryItems` and `/System/Volumes/Preboot/com.apple.security.cryptexd` denied access. A second `find` covered `/System/Volumes/Preboot/Cryptexes`, `/System/Volumes/Preboot/D90281D7-5767-426A-851D-480E63F02556/restore/usr` and `/Library/InstallerSandboxes` with `-maxdepth 7`, selecting regular files named `git`, `ps`, `memory_pressure`, `SystemVersion.plist`, `*.dmg` or `*.aea`; it exited zero. Neither search returned a named utility file. Image contents were not mounted or extracted, so this is not an absence claim about their payloads.

`plutil -p` reports 26.6.2/25G83 in the first search's `D90281D7-5767-426A-851D-480E63F02556/cryptex1/current/SystemVersion.plist` and `restore/SystemVersion.plist`, and the second search's `Cryptexes/OS/System/Library/CoreServices/SystemVersion.plist` and `Cryptexes/Incoming/OS/System/Library/CoreServices/SystemVersion.plist`, all beneath `/System/Volumes/Preboot`. The same command reports an empty `Assets` array in `/System/Library/AssetsV2/com_apple_MobileAsset_MacSoftwareUpdate/com_apple_MobileAsset_MacSoftwareUpdate.xml`. These metadata observations identify the accessible current platform, not individual image payload hashes. No mount, download or executable substitution was performed. Resume exact recovery only with a new accessible candidate; require all three original digest-and-size matches before claiming recovery.
