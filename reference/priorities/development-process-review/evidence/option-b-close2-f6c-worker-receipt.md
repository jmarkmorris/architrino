# B-CLOSE-2 F6c candidate preparation receipt

Measured candidate preparation, 2026-09-13: `.tmp/option-b-close2/f6c/check-preservation.py` reverses every declared edit for the worker-owned 32 production candidates and 35 Python test candidates to their captured originals and parses the candidate Python sources. This establishes declared representation and host-adapter changes, not scientific equivalence across arbitrary historical generations or connected launch acceptance.

The complete 37-path F6c family inventory is `.tmp/option-b-close2/f6c/inventory.json`; its known-controlled literal extractor recorded 736 digest values and incoming binders before edits. Root owns five remaining Node composition candidates; this worker owns 26 Python candidates and six Node stage entries. The current handoff manifest is `.tmp/option-b-close2/f6c/final-candidates.json`, including explicit candidate paths, sizes and digests, 35 Python test adapters, 21 Node test adapters, and three shared helpers. The exact selected test lists are `selected-python-tests.json` and `selected-node-tests.json` in that directory.

The historical full-entry record preserves the exact original 35 pins and independently reconstructs the original 198-entry union. `.tmp/option-b-close2/f6c/historical-table.py` first passed a synthetic 35-pin parser control, then compared the actual derived union to the retained original admission. Original source versions are enumerated with Git provenance in `historical-versions.json`; the raw sources remain independently protected archives, not current-source replacements.

Candidate hosts execute current bytes only when captured bytes equal the authenticated default pre-Batch-2 original or its selected current equivalent. An explicitly older original is authenticated by its own digest and retains original execution. Self-code checks reject an older generation rather than relabeling it as current. Pure/reference modules receive immutable values from admitted hosts; standalone host routes authenticate their bridge before executing captured bridge bytes. First-read identity checks retain inode, device, size, nanosecond modification time and nanosecond change time.

Original logical bindings and physical archive reads remain distinct. Local capture adapters retain actual archive descriptors and stat records; original metadata is an explicit logical view. The variable adapter records physical archives in its observer and census. Parent producer/verifier final recapture identities use actual physical archive bindings. Existing source-union, numerical, reference, rejection, finite-resource and historical membership predicates remain present; connected controls must verify these paths together before application is called complete.

Focused known-case checks passed with the shared repository venv: `check-variable-loaders.py` checks default/current substitution, older execution, unknown-original rejection and actual physical observer/census accounting; `check-physical-capture.py` checks original/current/unknown routing and distinct logical versus physical bindings; `check-captured-exec.py` checks injected current execution, preserved older execution and rejection of older self-code substitution. `node --check scripts/eom/f6c-production-admission.mjs` passed. These focused checks do not establish full launch or repository closure.

Next action: root applies the coordinated candidate closure and protected records, runs the explicit selected test lists, and returns concrete F6c failures to this worker. In particular, the bridge's actual Node runtime must satisfy existing runtime/operational admission before scientific work; no runtime census, self-source, old-hash or rejection check is waived. Direct CLI and complete replay validation remain pending. No scientific production or Git publication was performed by this worker.

Resource-census finding: the known-controlled `measure-source-census.py` reads the disposable selected production manifest, full-original membership and five selection anchors. At its second snapshot the graph contained 369 sources (99 production/control, 80 tests, 190 archives); the union with the 198-member historical obligation contained 563 paths, 51 above the existing 512 bound. Routing the seven available indexed historical sources to their physical archives did not reduce this union because their current paths and archive paths were already selected. This is a concrete graph-capture design blocker, not permission to raise a resource limit or hide production-reader reads. Root was notified to select genuine consumer-specific runtime dependencies and account all retained captures.

The same census identified two missing protected historical test versions. `recover-full-test-versions.py` authenticated both against the original membership digests and recovered exact Git bytes from commit `0fb575921783188ce528a45c671090e9ecc00464`; the source paths, lengths and provenance are in `.tmp/option-b-close2/f6c/full-original-test-versions.json`. These two versions must be indexed before original-full replay. The fixture can change while root integrates it, so the source-count numbers describe the recorded measurement snapshot rather than a final live repository result.

Independent reduction projection: keeping the 99 non-test current production/control sources, actual test helper hosts, current control-test targets required by protected source-pair APIs, non-test predecessor archives, and all archive digests referenced by the production identity values or full-original membership leaves 239 graph sources and a known combined union of 435 paths. The 77-path headroom is conditional on that recorded snapshot and does not include every later operation source. This permits root to retain the existing capture-all engine and 512-file limit; irrelevant test clients and test archives need not enter the production runtime graph. `source-census-projection.json` lists the exact projected removals. The two test helpers now identify themselves as the admitted reader consumer while preserving their public signatures, and the known Python helper control passes. Actual production graph captures and selection anchors must still be included in the host's resource and lifetime accounting; a projected count alone does not supply that implementation.

Resource accounting integration: root explicitly delegated the helper/coordinator patch and approved one narrow read-only accessor exception to the unchanged-engine scope. `captureSet.snapshot()` reports frozen copies of its already-retained physical binding and five-field identity records without reading files or changing capture/check/selection behavior. The production reader exposes these actual captures, including the selection anchors. The F6c helper merges them with operational bindings under the existing 512-file/1-GiB limits; the coordinator includes them in its existing capture union, retained lifetime source map and final identity rechecks. The original operational plan membership check still applies to the same operational set, followed by combined physical accounting of the admitted transitive sources.

Before connected application, three known controls passed: `check-capture-snapshot.mjs` proves no recapture, immutable output and unchanged same-byte replacement rejection; `check-resource-accounting.mjs` proves duplicate-path overlap accounting, exact 512 acceptance, 513 and aggregate-byte rejection, immutable outputs, conflicting identity/digest rejection and hardlink rejection; `check-coordinator-resource.mjs` runs the actual coordinator candidate with a test-only injected production admission and proves production-only capture inclusion, combined 513 rejection and same-byte replacement rejection. Exact before/after receipts are `resource-accounting-api-changes.json`, `resource-accounting-helper-changes.json` and `resource-accounting-coordinator-changes.json`. Their after states match the live helper/readers and coordinator candidate. Root's latest disposable fixture measurement was 255 selected sources and a routed known union of 448, leaving 64 paths for additional operation inputs; connected acceptance remains pending.

Connected regression preparation: after root applied the 162-source candidate generation, the disposable coordinator's actual `initializeSourceBindings` passed with 31 operational bindings, 269 production captures and 283 distinct combined physical sources totaling 11,646,232 bytes; all 283 had retained first-read identities. `final-coordinator-admission.json` records that instrument result. The first broad Node run cancelled most files before execution; the replacement run `96b69116-b382-4f4c-aa0f-27a63f34a3b3` produced actual F6c failures. Known causes include fixtures omitting production closure, stale fixed operational counts, synthetic Python wrappers lacking their admitted envelope, original scientific-pin checks reading migrated current bytes, and source-section markers spanning the newly separated operational checker wrapper.

The worker's first regression chunk is `.tmp/option-b-close2/f6c/regressions/manifest.json` (11 exact candidate paths), with individual before/after receipts alongside it. It is scratch-only pending root's coordinated application and graph rebind. This chunk provides complete fixture closure copying, explicit disposable synthetic-entry selection rebinding, unchanged historical pin assertions against authenticated original source, real stage-spec bootstrap envelopes, exact physical-path set comparisons, and a coordinator correction retaining operational-only handles in metadata-worker capture unions. No original 35-pin or 198-membership obligation was changed.

The new fixture helper first passed a known six-path source/anchor copy and a known explicit fixture-only source rebinding control. The shared production-test helper adds selected-runtime `originalProductionTestSource(target, expectedOriginalSha)` and separate unexecuted-data `originalProductionTestData(target)`. The latter authenticates its original index through the selected production reader, then retains exact archive capture identities without granting runtime eligibility to unselected test archives. Its known control passed original bytes, absent-target null, production rechecks and same-byte archive replacement rejection. All 11 candidate JavaScript sources pass syntax checks; this is preparation evidence, not a passing connected regression suite.

### Second connected regression preparation

The per-file Python runner `b5a7bbeb-52a7-4395-aa70-56507452ba11` completed all 35 selected modules; 20 returned nonzero. This is a measured regression inventory, not scientific failure attribution. The source admission migration exposed distinct original-source reads, legacy test-data import barriers, virtual fixture roots, and production capture adapter defects. Root owns the production signature/lifetime fixes and coordinated rebind. F5 owns the remaining coordinator/paired/family/parent-batch Node controls; this worker retains Python and emission/refined process fixes.

Frozen candidate batches are `.tmp/option-b-close2/f6c/regressions2/manifest.json` (14 files) and `.tmp/option-b-close2/f6c/regressions3/manifest.json` (5 files, including three overlays). Both retain exact before/after source evidence. They add read-only captured-source reporting to the Python bridge, retained original test-data capture outside runtime eligibility, unique private test module names, explicit finite synthetic admission and capture fixtures, original-reference byte assertions, complete isolated CLI fixture closure, and the serialized child process fixture imports. Known controls `check-python-test-data.py`, `check-synthetic-production.py`, and `check-virtual-capture3.py` passed before candidate target integration; every Python candidate compiles. These facts do not establish that the connected regressions pass. The next action is root's coordinated application and selected re-run, then resolution of any legacy-control replay or current-plan identity failures remaining after the first import/capture barriers are removed.

### Follow-through on the next connected Python run

Run `00d2101c-b025-4a9c-aa01-913ef7012bfd` advances past the initial shared-helper barriers and the parent verifier's 32 controls pass. Fresh candidate-only batches `regressions4` through `regressions8` preserve exact before/after sources and explicit live-before digests. They address actual logical-file FD delegation, original scientific bindings in current execution test plans, original comparator bytes and exact older dependency archive sizes, complete finite fixture input/output path censuses, complete isolated CLI production closure, and the distinction between an authenticated B1 current test successor and its original data bytes. No original hash expectation or original 35/198 membership assertion was substituted.

The refined verifier candidate's three MainFlow methods, including its late-failure subcases, passed in 1.612 seconds after the finite declared path census was completed. Six acceleration/refined preparation MainWiring methods passed in 3.054 seconds after adding their exact `range.json` output paths. These runs compile scratch test candidates against the unchanged live selected production generation; they are bounded plumbing controls, not a connected-generation or scientific closure claim. Root separately owns the ordinary external-path canonicalization and exact existing error-message repairs. F5 owns both cached legacy test modules and their historical AST/replay obligations. Root must apply each fresh batch and rebind before the final connected selection is run.

### Complete current execution source obligation audit

The two current acceleration/emission entrypoints' actual named plan sources, operational/runtime bindings, subject lists, fixed checker dependencies, theorem archive routes and pinned support/producer/verifier roles were enumerated together by `.tmp/option-b-close2/f6c/regressions9/audit-execution-sources.py`. Its known indexed-original/current/missing-version and archive-selection controls passed before the live inventory. The resulting `execution-source-audit.json` records 440 role occurrences and identifies eight distinct runtime source obligations whose current paths must be selected. Seven corresponding default original archives already exist and need selection; the eighth requires the exact pre-B1 acceleration preparation control generation, recovered via the existing admitted B1 original-test source accessor as 30,600 bytes. No additional missing source version was found within this enumerated execution scope. The exact sixteen current/archive paths are in `execution-required-selected-inputs.json`; root owns their graph integration and the final combined 512-source census.

Provider-specific streamed Node repairs are an eight-edit fragment at `.tmp/option-b-close2/f6c/stream-provider/edits.json`, merged only by Borg into its test candidate. The genuine two-refined-parent synthetic provider passes with separately checked original literal identities and admitted current control definitions, reporting two pairs, eight evaluations and sixteen residuals in 8.71 seconds. The four focused package/descriptor/ancestry validator tests pass in 28.18 seconds. Their original 28 package routes and eight archived relations remain asserted; the obsolete operational count was replaced by an exact expected physical path-set comparison that includes the admitted operational sources. These are scoped representation/fixture checks, not scientific acceptance.

### Pass-4 four-module connected run

Supervised run `d4082080-dd97-4391-86b8-86405e24b302`, owned by the stable root task, completed in 257.397 seconds with `processGroupClosed: true`. It used the shared venv, a 600-second outer hard deadline and 15-second heartbeat. Per-module outcomes are retained in `.tmp/option-b-close2/f6c/pass4-four-python-results.json`: acceleration execution passed 7 tests (157.818 seconds); parent emission preparation passed 40 (47.887 seconds); refined acceleration passed 36 (6.296 seconds); emission current execution ran 13 with one error (41.132 seconds). Overall this bounded selection has 95 passes and one error, not closure.

The sole remaining emission error is the unchanged 30-second synthetic invocation deadline, reached during operational binding capture while `production_original_source_binding(optional=True)` launched another authenticated Node request. No missing source selection or historical version remains in this run's failure stack. The bridge currently recaptures the full selected production graph in a new Node process for each query, including absent optional original-source lookups. Root owns the decision on a retained-identity-preserving request cache or admitted-index absence lookup; no timing, scientific, source-count or byte limit was increased. Complete stdout/stderr and terminal lease remain under `.local-data/owned-compute/` for this run ID.

### Pass 5 bridge admission reuse candidate (2026-09-14)

Prepared and froze `.tmp/option-b-close2/f6c/bridge-cache-pass5/manifest.json`, covering the Python bridge and its focused bootstrap controls; selected live sources remain unchanged pending root integration. Exact successful immutable JSON responses are cached by consumer and all request fields (operation, target, expected original generation), bounded to 4096 responses and 64 MiB. Every reuse checks all retained source, bootstrap and runtime identities without renewing them. A successful optional-original operation may retain that consumer's original-index key census authenticated against the Node admission's actual captured index digest; only canonical absent paths use that census, while indexed targets and versions continue through exact cached admission or the original Node engine. Failed operations do not create cached results or absence authority. The Node executable retains its first captured digest and five-field identity, with identity checks before and after subprocess execution. Filesystem path strings are converted using Node's builtin `pathToFileURL` within the captured module transport, avoiding Python's lazy `urllib` import.

Measured with `node --test --test-concurrency=1 .tmp/option-b-close2/f6c/bridge-cache-pass5/check.test.mjs`: all nine focused tests pass in 2.642 seconds; `.tmp/option-b-close2/f6c/bridge-cache-pass5/controls.log` records the result. The known tuple/sourcepair/version case passed first before additional controls. Controls include spaces, `#` and Unicode paths; no `urllib.parse` import; immutable caller responses; source modification and same-byte replacement; runtime modification and same-byte replacement; invalid canonical paths; required absent targets; indexed unselected targets; unavailable original generations; admitted consumer lacking index dependency; repeated failed requests invoking the engine again; and existing bootstrap/PATH/postexecution guards. Bridge candidate digest and exact before bytes are retained in the manifest. These are admission controls, not mathematical acceptance or a deadline result. The remaining latency falsifier is the current emission execution control exceeding its unchanged 30-second deadline after coherent integration; root and F5 own the combined timing sequence.

### Integrated pass 5 Python regression closeout (2026-09-14)

Measured with supervised shared-venv `unittest` through `.tmp/option-b-close2/f6c/run-pass5-python.mjs emission`: all 13 emission-current-execution tests pass, 40.158 seconds unittest time, 41.473 seconds supervisor wall time; run `f95a083a-ef48-4972-8ff2-e5d4de843fbe` is completed with exit 0 and process-group closure. The production invocation reported final-cover at 18.698 seconds and succeeded under its unchanged 30-second deadline. This resolves the preceding deadline rejection for that measured invocation, not an unrestricted latency guarantee.

The subsequent `final44` selection completed all 35 F6c modules and nine F5 modules with exit 0: 1,226 tests total, comprising 1,056 F6c and 170 F5 tests. Supervisor run `e6ddbf68-d3b7-4934-8ec6-0dbde06f92f9` completed in 512.111 seconds with `processGroupClosed: true`. Exact per-module stdout, stderr, exit status and timings are retained under `.tmp/option-b-close2/f6c/pass5-final44/`, with `results.json` and `summary.json`; standard supervisor logs retain the same run identifier. A known seven-test unittest-summary fixture passed before extracting target totals; each target log contained exactly one unittest summary. No skip, failure, error or traceback markers were found by `rg` across the 44 individual stderr logs. The additional focused 13-test run is not counted twice in the 1,226 total. All selected sources remained frozen throughout both runs.

Pass 6 assessment: inspection of the streamed-session and leaf-continuation Python loaders shows they execute the Python session/codec and captured adapter controls, not the Node streamed diagnostic launcher. The actual full-closure dependency still matters: `production_source_pair`/`production_recheck` retain the entire selected production graph, and accepted-frame reconstruction plus refined-acceleration tests copy that graph into disposable fixtures. Therefore this full result belongs to the integrated pass 5 generation. A later stream-only source repair should receive Node launcher controls plus Python streamed-session, leaf-continuation, accepted-frame and refined-acceleration integration smoke; a claim of whole-campaign acceptance on the later generation still requires its stable final acceptance run. Root owns that distinction and final campaign closure.

### Frozen pass 6 full Python selection (2026-09-14)

Repeated the complete 35 F6c plus nine F5 module selection after the two stream source repairs and coherent pass 6 rebinding, preserving every pass 5 log. Measured by `.tmp/option-b-close2/f6c/run-pass6-python.mjs final44` using the shared venv under supervisor run `6196264d-15cc-4b0a-8f69-fb4da925e908`: all 44 modules exit 0, all 1,226 tests pass (1,056 F6c and 170 F5), in 581.024 seconds supervisor wall time. The supervisor returned completed, exit 0, no error or signal, and `processGroupClosed: true`. No further job was launched by this worker.

Exact individual stdout/stderr, exit codes and wall times are retained in `.tmp/option-b-close2/f6c/pass6-final44/`; `results.json` is the runner record and `summary.json` adds exact unittest counts and reported unittest durations. `.tmp/option-b-close2/f6c/summarize-pass6-python.mjs` passed its known seven-test fixture before reading target summaries, then required exactly one unittest summary in each of the 44 logs. No skip, FAILED, ERROR or Traceback markers were found by `rg` across those individual stderr logs. The source freeze was maintained throughout; the existing 30-second production deadline was unchanged and emission execution passed on this same generation. This is the scoped B-CLOSE-2 Python regression receipt; root retains responsibility for the separate Node, build, integrity and whole-campaign acceptance gates.

### Pass 7 Python delta assessment (2026-09-14)

Inspected the exact coordinator diff against `.tmp/option-b-close2/stream-startup-profile/f6c-bounded-operation-v2.before` and the two-path pass 7 source manifest. The production change moves authenticated stream caller/spec/observer capture earlier and includes those original identities in the initial lifetime union, taking a real resource sample sooner; plan mode passes an empty early set. Python source and mathematical implementation bytes are unchanged. Inspection of Python loaders/subprocess entrypoints and the shared production helper identifies indirect full-graph admission and fixture copying as the Python integration seam; the reviewed Python routes do not execute the changed Node `wholeAttemptMain` streamed branch.

Recommended bounded follow-up is `test_f6c_accepted_frame_reconstruction`, `test_f6c_refined_acceleration`, and `test_f6c_emission_current_execution` (25 + 36 + 13 tests): copied-current-closure rejection controls and positive current execution/deadline admission. The coordinator's actual changed branch belongs to the Node stream tests. A further full 44-module mathematical replay is not justified solely by this startup-order delta. Preserve the 1,226-test pass 6 receipt at its actual generation; any pass 7 checkpoint must label separately the current Node stream, current binding audit, bounded Python follow-up, and reused prior regression evidence. This assessment authorizes no acceptance claim or test launch; root coordinates CPU and the checkpoint decision.

### Pass 7 bounded Python integration result (2026-09-14)

After root and stream-worker CPU clearance, ran the approved three-module selection with `.tmp/option-b-close2/f6c/run-pass7-python.mjs bounded74` under the stable root supervisor owner. Run `5160aee5-2a6d-4b1c-b421-404b8a9e75e1` completed exit 0 with `processGroupClosed: true` in 63.565 seconds. All 74 tests pass: accepted-frame reconstruction 25 (6.048 seconds module wall), refined acceleration 36 (7.041 seconds), emission current execution 13 (50.376 seconds). The emission module's full unittest duration includes multiple tests; its actual production invocation retained and passed the unchanged 30-second deadline, with a capture-stage heartbeat at 18.705 seconds. No deadline or assertion was widened.

Separate individual logs, runner results and exact parsed counts are retained in `.tmp/option-b-close2/f6c/pass7-bounded74/summary.json` and its sibling files. The summary parser passed a known seven-test fixture before target extraction. This is the pass 7 bounded integration evidence; the full 1,226-test result remains labeled pass 6. No source changes or further jobs were made by this worker.

### Pass 8 cadence review and competitor diagnosis (2026-09-14)

Static review of the two exact diffs in `.tmp/option-b-close2/stream-cadence-pass8/manifest.json` found five additive sampling calls and no clock, numeric, source-identity, limit or rejection-rule edits. Prelayout sampling is valid on this route: the exact observer executable is already bound in the early lifetime union; captured H/outer/D helpers are initialized; `lifetimeCensus` explicitly supports absent layout; samples and probe records remain retained before file descriptors exist. Original `s.live`, `lifetimeTable`, original executable identity checks and sticky failures remain active. This does not prove empirical cadence: a single synchronous executable read, source admission or union recapture longer than the unchanged one-second limit still rejects even when phase boundaries receive additional samples.

Root separately requested diagnosis of the broad Node run's emission competitor rejection. A scratch copy of the original `assertNoCompetitor` added only reporting immediately before its unchanged assertion. It first passed a known ordinary-row acceptance and a known competitor rejection, then read the actual process table at 2026-09-14T06:21:23.530Z. The actual rejected row was stopped Python PID 12576, parent/group 12571, recorded birth Mon Sep 14 02:15:58 2026, from fixture `f6c-stream-Odf83K`. The matching command substrings were source-envelope references to `launch-prescribed-response-pilot.mjs` and `launch-f6c-emission-refinement-pilot.mjs`. Exact original row and command remain in `.tmp/option-b-close2/f6c/competitor-review/current.json`; the scratch known-case instrument is `check.mjs`. This establishes a current stopped fixture process and the guard's matching basis, not its original run ownership. Root/F5 were asked to reconcile retained birth/group identities. No process was signaled by this worker and no guard was bypassed.

### Multioccupant physical-source census review (2026-09-14)

The retained two-advance fixture `f6c-stream-t6FyMW` reports 510 distinct Python `sourceIdentities` paths totaling 18,883,127 bytes. `.tmp/option-b-close2/f6c/census-review/check.mjs` first passed the known duplicate-path census `a,b,a → a,b`, then measured the actual completion. The evidence stream plus process stdout/stderr require three additional paths, reaching 513; eventual operation publication adds another. The coordinator already deduplicates exact canonical paths and rejects conflicting generations, while physical captures reject inode aliases. No duplicate-path defect or reason to raise the 512-file limit was found.

All seven `option-b-production-regression-pass1.json` through `option-b-production-regression-pass7.json` records are physically captured members. Review of `.tmp/option-b-close2/author-production.mjs` shows their runtime membership comes from the unconditional `additional-production-inputs.json` insertion as scientific-control sources; generic graph edges then make them dependencies of the reader. The executed reader and transition engine retain the selected `option-b-production-transfer.json` as opaque historical proof, and that proof already contains the exact path/SHA references to the seven review records. A literal-reference search over `scripts/`, `tests/`, and non-JSON/non-JSON-LD development-process-review files found no executable consumer of these regression-record paths; its sole documentation match was the handoff. JSON index checks, after known present/absent controls, confirmed all seven are absent as original-index targets/archives, identity-record consumers/values, and original full historical membership, while all seven exact proof references are present. Those checks are recorded in `census-review/metadata-obligations.json`; actual captured paths and sizes are in `census-review/physical.json`.

Root approved correcting only these seven records from runtime inputs to retained review evidence: preserve immutable files and selected proof references, remove the exact seven runtime Source memberships, omit future regression review records from this runtime graph, and authenticate their references in an offline closure/retention check with a known bad-proof control. This intentionally changes review-metadata runtime eligibility; it does not omit required source, archive, original-membership, runtime or executable obligations. The resulting 503 Python paths plus three stream/process artifacts and one operation publication project to 507 final files, leaving five slots under 512. This is a projection for the measured fixture, not a post-change acceptance result. Root owns graph application and the measured rerun; no fixture or live source change was made by this worker.

### Pass 8 affected process suites after residual closure (2026-09-14)

Following exact residual-process closure and coordinated CPU isolation, reran all tests in `tests/f6c-emission-refinement-pilot-process.test.js`, `tests/f6c-refined-acceleration-pilot-process.test.js`, and `tests/option-b-f6c-coordinator-process.test.mjs` on pass 8. Measured by serial `node --test --test-concurrency=1` through `.tmp/option-b-close2/f6c/run-pass8-process.mjs`: 12 + 12 + 2 tests pass, no failures, skips or cancellations. The independent coordinator/observer positive control completed in 10.887 seconds; both complete pilot suites reached their intended negative fault stages and passed cleanup checks rather than stopping at competitor admission.

Supervisor run `36e64dcf-72de-4e1b-a6e9-c954c90639fa` returned completed, exit 0, and `processGroupClosed: true` in 89.612 seconds. Exact per-file logs and runner exit/timing records are retained in `.tmp/option-b-close2/f6c/pass8-process/results.json` and sibling stdout/stderr files. Current independent-observer operational records are retained under `.local-data/braid-analysis/option-b-current-closure-V88WYl`. No source edits or further jobs were launched; Borg received the next build window, followed by F5's full stream suite. This closes only the three previously competitor-blocked process-suite reruns.
