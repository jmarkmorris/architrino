# Independent current circular context review

Reviewed `src/prescribed-path-analysis/SubfieldCircularRootLedgerReducer.mjs` at SHA-256 `72935e3439b27cc27f2efcc1db5393cf0c8ce1eb0d2e9dc777755d4263d685b1` and `tests/subfield-circular-current-context.test.js` at `e93cd0727b85c5bc93b5782150a4ededa0a808275a6820c67a10fc0e204a574e`, measured with `shasum -a 256`. This reviewer did not author either change or edit the adapter, reference, or numerical predicates.

The `git diff` and surrounding `verifyBuild` source show an explicit admission of the exact `c80526d097c81627186cbbfcea7e0005d9d73288e331f4535f07982cc2bef944` preparation through the hash-bound retained independent build review. A caller-supplied preparation hash alone cannot select a new generation: the added branch also verifies review bytes, the full reviewed binding census, actual file hashes and sizes, linked executable, successful closed stages, discovery tools, file-backed runtime dependencies, and the declared platform boundary. The original profile and API pins remain in force outside the explicit reviewed source substitutions. The file context rechecks the captured inputs before consumption completes.

Reading the unchanged `verify-subfield-circular-history.mjs` producer confirms its proof binding order is six frozen references, the candidate source, and the verifier itself. The reducer previously indexed a nonexistent eighth reference; selecting the actual named verifier restores the producer's declared order without changing that producer or its analytic predicates. The exact current-context controls reject an incorrect preparation hash and a substituted proof missing a verifier source binding.

Independent run `5ce2deca-dae0-449a-84eb-ec82e7aacf2e` executed `AAA_CIRCULAR_CURRENT_CONTEXT=1 node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 90 -- node --test --test-concurrency=1 --test-reporter=tap tests/subfield-circular-current-context.test.js`. The native TAP result reports two tests passed, zero failures and zero skips, after the known SHA-256 control. The lease reports exit zero, 1.833 wall seconds, and process-group closure. Logs remain under `.local-data/owned-compute/logs/5ce2deca-dae0-449a-84eb-ec82e7aacf2e.*.log`.

Verdict: the recorded exact current build may enter the production ledger context. This review supplies no root-row, pilot, resource-ceiling, ladder, H3, or broader scientific acceptance. A changed reviewed source, reference, build/review object, omitted dependency, or a failing actual phase/pilot contract would overturn the applicable admission or prevent downstream acceptance. The platform and non-atomic path-recheck limits remain those declared by the retained build review.

## Operational caller composition

The subsequent `git diff` of the pilot, outer launcher, rung and dispatcher contains only selection of the exact reviewed current build and transitive runtime hashes. `shasum -a 256` measures pilot `18479b39de068129423ecdffd103feb9c06a3f870648d55cbd7b0a3264926774`, outer `2037b77009f26b7bf51e599dee222961c80820d0e16f9d50e0cdfbf152ba4170`, rung `d3b1094fdc5cf9e9b3849101778aef784d2610e3dad0502e2ea92ab08cd253bc`, and phase helper `15a844adc1731a6ea47f0636f86d9e0d7196d6b15dd963006a278c129cc328f1`. The dispatcher selects that outer launcher and preserves its original `memory_pressure` binding; a mismatching host utility continues to block ladder execution.

The dispatcher SHA-256 is `375c556695e81e0c222e4e95a465d94c3db130f968d0af7eab957948b9f72fb9` and revised pilot-test SHA-256 is `a25bedaf8ee51aded6f38ddb5cab7ef53e2b3c68dcf7e65d304efdc7421cc9f6`, by the same command. Reading the pilot's build selection, current context invocation and existing per-phase reduction shows no added bypass of source, history proof, row, resource or final-process predicates. The revised pilot test replaces an assumption that the selected build must fail with a concrete reviewed-build positive and substituted-receipt negative. This tracks the changed selected profile without weakening the actual build identity check. These are source-review findings; the integrating runner owns the final caller-suite result and subsequent actual execution receipts.

This reviewer finds no new blocking issue in that bounded caller composition. The already rebuilt and independently reviewed executable can be used if its complete declared source/tool/reference identities remain exact and its recorded build postdates the compiled sources; a launcher-only change does not itself require another numerical rebuild. The context control independently rechecked the recorded build inputs. Any later compiled-source or build-identity change requires a fresh build and review, and every actual pilot still must earn its separate execution and resource acceptance. No ladder or scientific acceptance is inferred from these pin changes.

## Actual launch exposes an unresolved caller contract

The preceding pin-composition review was insufficient to establish that the top-level launcher could execute. The integrating runner's actual attempt `d0a580d2-a601-40f3-9d61-8075acec4655` rejects before a target spawn because `launchReviewedPilot` does not supply `inspectProcesses`. Reading that call and `superviseRegisteredPilot` confirms the latter explicitly rejects its default `processTable`: its callback must carry independently reviewed bounded probe lifetime and complete resource accounting. Pin consistency and fixture callbacks do not supply that production obligation. The context admission verdict above stands; the inference that caller composition was ready for actual pilot execution is withdrawn.

The top-level launcher currently has no probe registry or resource observer owner to supply the missing contract. Its `runtimeBindings` records `/bin/ps` bytes, but this alone records neither each probe's lifetime nor all its resource costs. The simple exported `processTable` uses `execFile` timeout/max-buffer behavior and returns only parsed rows. Wrapping that function in an anonymous callback would satisfy a JavaScript identity comparison while leaving the same obligation unmet, so no such change is made.

The existing F6c `lifetimeTable` is part of a larger lifetime owner, not a standalone utility. Its `lifetimeProbe` requires an exact source/runtime union, original executable identity, a tracked actual close promise, retained raw-byte accounting, abort/cleanup phase budgets, aggregate RSS sampling, and bounded output publication. It can only be reused by making those dependencies explicit and reviewing their integration; importing it does not itself grant a circular launcher those responsibilities.

The next implementable object is a production circular observation owner with these acceptance requirements:

1. Bind the actual probe executable and any wrapper/runtime dependencies before the first probe; recheck the original identities through final publication. Define the observer's trusted platform boundary.
2. Give each actual probe a recorded start, deadline derived from the existing work/completion budgets, process identity, byte bounds, exit/stream status, and observed close. Track pending probes through cleanup. A timeout must not be reported as closure, and a callback that resolves after its allowance cannot revive admission.
3. Include observer activity in the declared resource accounting. Define precisely how CPU, peak or sampled RSS, wall time, and output storage enter shared overhead and any resource ceiling. Process rows without observer accounting do not meet the explicit callback contract.
4. Authorize cancellation through the live owned child or an independently authenticated current process identity. Preserve ambiguous groups and unresolved callbacks as failed admission, without numeric historical-PID signaling or substituting later absence for the failed run's result.
5. Expose the inspected table and a terminal observer receipt to the outer owner; join probe closure, bounded receipt publication, source rechecks and original deadline before the outer guard is released. Test normal observation, blocked/failed observation, output growth, interruption, late completion, source replacement and final cleanup failure before running the actual pilot.

This is a missing lifecycle and resource-accounting interface. It is not resolved by the startup fix in the generic owned-compute supervisor, by a green process-control subset, or by the independently accepted circular build. The top-level pilot and dependent ladder remain blocked on this interface; no circular launcher source was changed in this follow-through.
