# Current circular memory observation source review

Legend: ✓ Done, ◐ Partial, ○ Not done. A completed source review supplies operational applicability only; it does not accept a numerical rung or a scientific claim.

**✓ Done — Independent source review.** The integration reviewer inspected the memory helper and owner separately from their implementation author. `shasum -a 256` binds `scripts/eom/observe-subfield-circular-memory.py` to `abe24baaf063ba5fe159a8c475816816ff812c912d8faab67156cc7f318cc220` and `src/prescribed-path-analysis/SubfieldCircularMemoryOwner.mjs` to `721c75a7fa1e6d62618f48e982432e941cefe77a44a785a7a57f68d5322e6f9b`. These sources preserve the previously accepted process-observation helper and owner. No mathematical evaluator, numerical predicate, or independent numerical reference changes in this transport.

The Python helper executes the fixed `/usr/bin/memory_pressure` utility as its direct child, drains bounded output, and uses `wait4` to record the actual query's exit and resource use. Its signal handler addresses only the unwaited direct child. A two-second timer applies to the helper operation, including its runtime inventory and output construction. The JavaScript owner retains the live child handle through exit and stream close, enforces its output and source limits, and escalates cancellation from TERM to KILL. The enclosing entry must retain its independent whole-entry guard: an unresolved close never becomes an accepted observation.

The initial helper invocation inventories its shared-venv executable, loaded module files, and non-system dynamic libraries without launching the query. The query invocation must reproduce that exact inventory. Source and runtime digests are rechecked after each observation and at final closure. The macOS kernel and system shared cache remain explicit trusted platform dependencies. An individual process's maximum resident memory is not an aggregate peak. The full helper's wall time multiplied by the logical CPU count is an upper bound, distinct from the query's measured `wait4` CPU use and the helper's usage sampled before serialization.

The parser requires exactly one whole-line system-free percentage and checks the unchanged memory and disk thresholds. Failure, truncation, malformed output, a changed runtime, an incomplete query, or a missed deadline rejects the observation. For actual rung execution the entry must sample on the declared cadence, stop work on failed resource observation, finish both observation owners, and condition any published result on the exact external owner's zero exit and closed group.

**◐ Partial — Execution acceptance remains separate.** The source inspection supports this transport design. The actual preflight, independent resource-plan review, failure controls, and each requested rung's external closure and complete admission remain separate evidence. A changed reviewed digest, an unclosed observed query, a runtime mismatch, or a resource observation that is accepted despite a failed threshold would overturn the corresponding operational conclusion.
