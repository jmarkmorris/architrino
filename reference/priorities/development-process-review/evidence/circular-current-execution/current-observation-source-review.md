# Current circular observation source review

This independent source review concerns `SubfieldCircularObservationOwner.mjs`, `observe-subfield-circular-processes.py`, and the separate current entry. It grants no circular pilot or scientific acceptance. Final executing byte identities and actual lifecycle controls remain required before the pilot.

The first review identified four concrete gaps: runtime inventory invoked the process probe before establishing its runtime census; Python `-B` did not prevent loading an existing bytecode cache; cancellation could wait indefinitely after a single TERM; and empty resource objects passed the field validation. The revised owner/helper inspected on 2026-09-08 separates inventory from process observations, uses a fresh empty private bytecode-cache prefix with writes disabled, rechecks inventoried runtime files before each probe, escalates through the live child handle only before observed exit, keeps the helper timer through serialization, and requires all three named resource fields. Python blocks the handled signals around `wait4` and return-code update, preventing a reaped PID from remaining a signaling authority during that interval.

The parent-observed wall interval multiplied by logical processor count is a conservative upper bound for total probe CPU, including serialization and exit tails, on the recorded fixed host CPU-count boundary. It is not a measurement of actual consumed CPU. The helper and ps measurements name their different completion points; sampled aggregate RSS is explicitly not a continuous peak guarantee. These distinctions must survive the final receipt and operator report.

The current entry starts an independent deadline guard before profile reads and captures reviewed modules before import. It intends to retain the guard through observer closure, final source rechecks, admission publication, and stdout closure. Its admission remains conditional on observing the exact owner exit zero before the original deadline. Source review identified startup failures before the inner try block that could leave the guard active until its full deadline, and an absent current Node executable binding. These findings were sent to the implementer before any pilot. Initial inventory resource usage also needs the same declared ceiling as later probes. Final review must verify those corrections and the late-serialization, stubborn-cancellation, and cleanup-failure controls.

Any unbound executing source/runtime, unclosed probe, identity ambiguity, deadline breach, resource-limit breach, failed external exact-owner termination, or failed joint final admission overturns execution acceptance. No regression subset substitutes for this owner-specific acceptance.

## Updated source readiness boundary

The revised entry closes safe prework failures promptly, captures Node and its loaded non-system shared libraries, rejects runtime injection variables, and includes a conservative full-entry CPU upper bound through publication. The owner now enforces the initialization RSS ceiling. The independent review read the owned 13-control pass `390c0edb-2c52-4495-8376-514ae5ab989d` and confirmed its zero exit and closed process group. It also rehashed profile `adafb201734ecc9d3db2d08aed6a233864086f93e16aad75c174c36d7bda7842` and its 32 source/runtime records after known positive and negative SHA controls.

Before launching, the implementation owner identified one remaining runtime boundary: the Python executable is a small loader, and module-file inventory alone does not bind its linked non-system libpython and other loaded libraries. The pilot remains pending loaded Python image capture before the first process-table probe, with the same explicit system shared-cache trust boundary as Node. This finding supersedes the provisional source-ready message; the final profile must bind the corrected helper bytes and pass the focused controls before execution.

The corrected helper `d2bc66a813507d37d3d9ff8312cabc76f179d75bdbd13c7e82c68f7983f074b3` now inventories dyld images through ctypes, includes non-system loaded libraries in the same hash census, and rejects missing or oversized image inventories. The module inventory includes ctypes itself. Inspection confirms that the process-table path compares this runtime census against initialization and rechecks its bound bytes before the probe. Independent hashing verified refreshed profile `b3e945c7bdd602da7d6790773376510f6c5f32687717aa452f48b8ce3ede99db` and all 32 declared source/Node runtime records. This resolves the identified source/runtime capture gap; actual final control results and target admission remain separate evidence.

Final control readiness: the independent reviewer read lease `37155579-74f1-4c30-8587-f5de31cf5d2e` and its TAP log, observing 14 passes, zero failures, exit zero, and closed process group in 3.987 seconds. The controls include an actual Python framework image assertion and a changed inventoried runtime dependency rejection before the next process-table probe. Source readiness for the exact refreshed profile is approved for attempting the bounded current pilot. Acceptance still requires the actual joined process/observer receipt and exact terminal closure; the controls alone do not close that execution obligation.
