# Independent current circular prior transport review

**✓ Done — source and immutable packet review for the next 32-phase invocation. ✓ Done — exact final profile/index linkage inspected. ○ Not done — actual 32-phase acceptance.** This review creates no new numerical or closure result and changes no resource cap.

Full source reads of `prepare-current-subfield-circular-prior.mjs`, `run-current-subfield-circular-root-rung.mjs` and `authenticateSubfieldCircularPriorPhases` establish the actual consumer contract. The frozen entry authenticates the entire plan-review byte object but semantically compares only its accepted review, plan, candidate/rung and entry/memory identities. The frozen prior consumer reads an accepted prior object's `processesClosed` and `admission` fields, matches the original plan and preceding rung, and authenticates the complete phase chain. Neither module generically compares `profile.planReview` with the final `prior.rungAdmissions` entry. That structural limitation is explicit; this review concerns one independently authenticated profile and CLI digest, not arbitrary caller-created profiles.

The implementing worker's preparer, SHA-256 `46aa0a4c79597882a9cf6279fe66b1c9c857b9f035ffaaa577e336655045c206` by `shasum -a 256`, carries original joint admission, independent review, stdout and prior-prefix bytes losslessly through bounded gzip/base64 records. Each decoded payload retains its original digest and byte count; both decoded objects and the final packet are bounded by the existing two-MiB reader limit. The original lease stays private. Only its run ID, exit code, group-closure flag and elapsed wall time are embedded, explicitly labeled as a public field projection and independently corroborated by the original review. The preparer emits pending bytes and never accepts its own packet.

The combined packet has two distinct scopes. Top-level review scope is the same candidate's next rung, 32. Its `admission` and `processesClosed` fields are copied exactly from the actual eight-phase joint process object, preserving that admission's original rung-eight scope. The accepted combined packet must be selected both as the new profile's plan review and as the final prior-rung admission; the independently inspected profile digest fixes that linkage for this invocation. This is the existing consumer's accepted-prior interface, with the original observations retained, not a newly observed process closure.

Five producer controls passed in `c3383936-f6f4-4c5c-8876-45685a7bea90` before preparation, covering lossless known bytes, complete prior chains, pending-finalization rejection, changed evidence/closure/scope/projection, unexpected private terminal fields and the size bound. Actual preparation `a655f84d-d951-4ffa-a523-0e0e292ae277` exits zero with closed group and creates the 96,296-byte pending packet. The independently authored [packet-review instrument](review-current-prior32.mjs) first passed known digest/lossless payload and substituted digest/size controls in `0623223b-ebe0-446f-b5e5-3c8342abc40c`, then checked the target under `67638b74-400a-45da-9261-84b86fb858c0`, again exiting zero with group closure.

The [linkage result](current-prior32-linkage-review.json) records exact original payload equality, the accepted eight-phase review and external closure, the two pilot plus eight preceding phase bindings, original plan and unchanged executing source identities. The separate accepted packet changes only `accepted` and `independentReviewStatus`; its SHA-256 is `1361d54a6d5aaf3e3de41cf579f0dd0b2a45f018e26e5b2b8c3ce44a9a8725e8`. The producer's pending packet and every earlier admitted source, producer and review object remain unchanged. An incorrect final profile/index route, altered original bytes, a different candidate/rung or a missing external closure would overturn this scoped acceptance.

## ✓ Done — exact next32 invocation linkage

The [final profile review](current-profile32-review.json) passes under `3981e92f-cede-4b12-9062-730ac4c22b31`. Its instrument first passes a known matching profile/prior fixture and rejects changed scope and a changed admission digest. It then authenticates profile SHA-256 `14d487f42533d74154fc53b153e6a514b75a398013a6ca9db864b6d521f99b37`, verifies that only `rung`, `prior` and `planReview` differ from the original eight-phase profile, checks every unchanged source/runtime and plan binding, and proves that the combined review is the sole prior-rung admission with the same path and digest. All ten original phase receipts retain their recorded order and identity; the unchanged `authenticateSubfieldCircularPriorPhases` accepts the exact chain. The permitted CLI must select this exact profile digest. Actual execution remains a separate measured result.
