# Independent current circular 128-phase prior review

**✓ Done — immutable 8/32 prior packet and unchanged resource-envelope review. ✓ Done — final exact profile/index linkage inspected. ○ Not done — actual 128-phase and complete-candidate acceptance.** This permits only a bounded attempt for the same candidate, without changing any cap or scientific authority.

The frozen preparer reviewed for the [32-phase handoff](current-prior32-review.md) also produces the next 128-phase packet after actual 32-phase acceptance. Preparation `c4599bc2-75da-4ff1-824d-2f6dd7da94a0` closes with exit zero and produces 324,612 pending bytes. The independent [128-packet reviewer](review-current-prior128.mjs) passed known digest/lossless-decoder and substitution controls before target inspection, then passed the target under `d4a2cf67-c917-4408-83f6-fe0e9e2b48aa`. Its [linkage record](current-prior128-linkage-review.json) authenticates the original 32-phase joint admission, independent review, stdout and public terminal projection, together with the prior ten-phase prefix and unchanged preceding eight-phase review packet. The next scope is rung 128; the copied preceding `admission` correctly retains rung 32. The final index must contain 42 phases and two preceding admissions.

The separate accepted packet has SHA-256 `1c4c18b62eb1a96ca359d1b18521142c5c4d04c27738fcb81e3dc8cb9979253b`; only its review acceptance flags differ from the pending producer packet. Every embedded original object is losslessly decoded and byte-authenticated. The same exact-invocation limitation applies: final profile review must establish that this packet is both `planReview` and the last prior admission. The frozen entry does not generically compare those fields for arbitrary caller-created profiles.

## ◐ Partial — tight observation-storage projection

Source inspection of the rung's final summary branch and `checkSubfieldCircularRungGateCensus` identifies 514 gates for rung 128: four per phase plus the candidate-rung and complete candidate-ladder summaries. Using the normal seven-probe overhead observed in the completed lower rungs gives a nominal 521 total probes, including 520 process-table probes and one inventory. This corrects an earlier preliminary estimate that omitted the final ladder-summary gate.

A known arithmetic control (`[10,20]` averaged over four probes returns 60) passed before reading the completed 32-phase observation record. That actual record contains 135 process-table probes with mean 125,224.4889 bytes and observed maximum 126,030 bytes, plus 13,760 inventory bytes. Applying those measured costs to 520 table probes projects approximately 65,130,494 bytes at the observed mean, or 65,549,360 bytes at the observed maximum, against the unchanged 67,108,864-byte cap. The latter leaves 1,559,504 bytes, about 2.32 percent.

These are sample-based projections, not a bound on future table size, cleanup work or successful completion. Table growth or additional cleanup probes can consume that margin. The bounded attempt retains the original cap and must stop safely with its resource failure preserved if it reaches the limit; no source or cap increase is approved here. The separately fixed 1,800-second deadline, system free-memory and disk predicates remain unchanged. Actual acceptance still requires every declared phase/gate, owner closure and exact external zero exit.

## ✓ Done — exact 128-phase invocation

Independent profile review `c5e38322-668b-4aed-9454-01181bd44f44` passes after its known matching-link and changed-scope/digest controls. The [result](current-profile128-review.json) binds profile `2f8de6267c7e897ee1516f6068917f462adef62f87243541f1b5bbdf556f8ad4`, all 42 prior phase receipts, both prior admissions and the exact common review/admission packet. Only rung, prior and plan-review selection differ from the accepted 32-phase profile; executing source/runtime and resource-plan bytes remain identical. The unchanged prior validator accepts that complete chain. Actual 128-phase acceptance remains conditional on the measured run and full closure review.
