# Independent current circular rung entry and resource-plan review

**✓ Done — source and resource-envelope review for the first candidate's eight-phase rung. ○ Not done — actual rung acceptance pending its complete receipt and exact external zero exit.** H3 eligibility and authorization for later rungs remain false.

Full source inspection of `run-current-subfield-circular-root-rung.mjs`, its captured dispatcher admission operation, source-capture boundaries and owner finalization paths supports the selected operational entry at SHA-256 `91218e1e4bd85fc614c5e270086bd62d766266cd02a51c06a047cef192984c81`. The entry binds every declared source role, Node executable and non-system shared library census, the absolute shared venv, and the exact plan, review and prior-phase inputs. It enforces one concurrent candidate and the existing 1,800-second cap. Root separately reviewed the current memory owner/helper in [the memory source review](current-memory-source-review.md).

The entry's periodic sampler uses a fixed 15-second cadence target, stops rescheduling before awaiting pending work, and converts a failed observation into owned cancellation. Completion requires the captured dispatcher to authenticate the complete phase/summary/evidence/gate census, followed by memory and process observer closure and post-publication source checks. The independent guard covers final serialization and publication; the conditional JSON is accepted only with the exact external owner exiting zero before the original deadline. Sampled memory is not represented as a continuous peak guarantee, and CPU upper bounds are labeled as bounds.

Review found that the first version authenticated an accepted plan/entry review without limiting that review to a particular candidate/rung. The implementing root added exact equality between review and profile candidate/rung and explicit plan/review/prior digest-token checks. The final machine [resource review](current-resource-plan-review-b.json) therefore permits only `coincident-midpoint-common-frequency`, rung eight. It is insufficient for another candidate or rung, even though the resource envelope retains all ten eligible candidates and the six explicit resource returns.

The [independent entry controls](current-rung-entry-review.test.mjs) check complete argument selection, missing/duplicate/unknown options, output traversal, digest changes, changed wall cap and missing source roles, including guard closure before work. Run `43d7c04e-b297-4a7e-a3ef-3c3e5905b2c8` first exposed a review-fixture error: macOS temporary-directory spelling was a symlink, so the intended cap rejection was correctly preceded by canonical-path rejection. Canonicalizing that fixture alone yielded two passes in `9a9a6a8c-2084-4e7e-b8fc-ee4bf6774041`; the final frozen source repeats both passes in `1a2440a1-e9e4-404f-86b0-24f91f8aa2cb`, exit zero with group closure. These are bounded rejection controls, not numerical execution.

The actual preflight B follows the explicit known memory-threshold control and closes under `57c5451f-0814-4924-b0eb-0d2a315c54ea`. The [preflight review record](current-ladder-preflight-review-b.json), run with argument `b` under `5b17ecd9-711f-4ebd-a84c-84c11655037a`, authenticates all recorded source/runtime bindings, four zero-exit closed probes, the unchanged pending-plan bytes, ten/six disposition census, exact prior phase receipts and external closure before 60 seconds. Preflight A and its earlier review remain preserved, and B is the selected handoff.

A separate accepted resource-plan copy changes only its review flags and execution-status explanation, preserving the pending original, all candidate estimates, caps and scientific flags. It records resource-envelope applicability; the exact candidate/rung machine review and actual entry obligations remain necessary. The [final-rung review record](current-rung128-independent-review.md) retains the bounded target review under the same evidence boundary. Dispatcher re-execution is deterministic transport checking; the unchanged independently authored numerical references retain their own evidential role.
