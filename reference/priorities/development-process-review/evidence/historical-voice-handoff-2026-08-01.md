# Fresh Voice Chat Handoff

I am continuing a long Architrino collaboration in:

`/Users/markmorris/vibe/architrino`

Treat the live repository as the source of truth. This prompt is a handoff, not authority if the files have advanced.

Before making edits or current-status claims:

1. Read `AGENTS.md`.
2. Read `reference/op/agent-startup-orientation.generated.md`.
3. For field-speed work, read:
   - `reference/priorities/field-speed-ceiling/mathematics-geometry-dynamical-system.md`
   - `reference/priorities/field-speed-ceiling/work-queue.md`
   - `reference/priorities/field-speed-ceiling/work-log.md`
4. For Topo work, read:
   - `reference/priorities/app-topo/priorities.md`
   - `reference/priorities/app-topo/work-queue.md`
   - `reference/priorities/app-topo/requirements-and-design.md`
   - `reference/priorities/app-topo/topo-observable-and-reference-geometry-v1.md`
5. Inspect the live working tree before editing. A dirty tree is normal because multiple agents may be active. Preserve unrelated changes and never overwrite or revert work you do not own.
6. Do not commit, push, regenerate generated artifacts, or broaden the write scope unless I ask. Prefer check-only validation during ordinary editing.
7. Do not propose a git worktree unless I explicitly request one.

## General theory discipline

- Reason from Euclidean void, absolute time, architrino paths, causal wakes, polarity, delayed history, the Master Equation, and the proposed response.
- Do not import standard-physics laws as architrino-level premises.
- Use acceleration language, not force language.
- Keep symbolic $c_f$ where useful, but use $c_f=1$ in numerical examples.
- $c_f$ is wake speed. $c_a$ is the investigated path-speed ceiling.
- Write `architrino`, `electrino`, and `positrino` lowercase in ordinary prose.
- Use `coincidence` for the same event in absolute time and Euclidean position.
- Use `tangency` for root grazing.
- Use `nonordinary event` as the broader category.

## Active lane 1: field-speed ceiling mathematics

The primary document is a provisional partial model. It does not adopt a field-speed ceiling or establish a complete dynamical system.

Established or conditionally derived results include:

- The regular Master Equation remains unchanged as the unbounded reference.
- The proposed ceiling response is applied once, after the complete ordinary wake ledger has been summed.
- It removes only the net acceleration component that would carry a path outside $\|\mathbf V\|\le c_a$.
- Ordinary positive-delay channel geometry has been classified under $c_a\le c_f$.
- A channel may have no root, one isolated root, one degenerate isolated root, or one rigid characteristic interval.
- Root monotonicity and frozen-interval and characteristic-interval rigidity results have been derived under the proposed closed velocity domain.
- A receiver-side frozen interval is not merely an instant with $D_r=0$. It is an interval during which the received-emission clock is constant and the receiver rides one wakefront on a straight $c_f$-speed ray.
- An isolated tangency with $D_r=0$ must remain distinct from a frozen interval.

### Coincidence, frozen roots, and swept-source reception

For the ideal mirror straight-through trace after coincidence:

$$
S(T)=T_c,\qquad D_t=2c_f,\qquad D_r=0.
$$

The receiver rides the partner's coincidence-time front. It does not repeatedly receive new source history from that front.

The Swept-Source Reception Law is proposed foundational data, not adopted canon. It agrees with the canonical ordinary row wherever $D_t,D_r>0$, records a nondegenerate frozen interval as inactive, and does not silence an isolated tangency.

It does not by itself define event atoms, select an outgoing history, or solve every characteristic interval.

The incoming partner cap at exact mirror coincidence is a whole nonordinary family. Its right-hand incidence depends on the selected trace:

- the prescribed straight-through trace retains only the frozen endpoint;
- exact maximum-speed rebound retains the full $D_t=D_r=0$ interval;
- a generic right trace may retain neither.

That incidence theorem is complete, but it does not select the right trace.

### Common zero-impulse event superposition

The exact-mirror proposal defines a common impulse-event carrier for the two labeled, equal-magnitude, opposite-polarity cap histories.

Their finite raw event measures are aggregated at the same event in absolute time and Euclidean position before a common event response is assigned.

Under the exact matching assumptions:

$$
\mathbf J_{i,-}=-\mathbf J_{i,+},\qquad \mathbf J_i^{\mathrm{imp}}=\mathbf0.
$$

Interpret this carefully:

- wakes do not collide with or alter other wakes;
- both labeled wake histories and provenance records remain;
- only their matched impulse-event contributions cancel in the event aggregate;
- the continuous universe-wide wake sum remains;
- unmatched event components remain;
- ordinary wake rows remain;
- every other wake in the universe remains part of the complete superposition.

This aggregation law is still proposed. The cancellation theorem currently requires the exact matching hypotheses stated in the document.

Do not generalize it to every angled or asymmetric coincidence merely from opposite polarity. First prove that the event carriers and unsigned weights match.

The exact-mirror event restart is also proposed. It books the matched family once; preserves position, velocity, both labeled histories, received clocks, ownership, and typed event records; and restarts from the complete remainder ledger.

The owned-family permanence lemma proves that an already-owned emission cannot later return as an isolated ordinary root.

Straight passage is exactly compatible, and immediate rebound is excluded, but compatibility is not uniqueness.

### Current rank-one problem: trailing-front activation dichotomy

The decisive live issue is no longer the coincidence instant. It is the post-event continuation.

Along the straight trace, every later partner wake remains behind. Its inactive margin is:

$$
g(T,s)=2c_fs.
$$

The infimum of this margin is zero as $s\downarrow0$. Therefore, no uniform inactive-gap neighborhood surrounds the straight chart.

A candidate continuation can remain straight until an arbitrary $u_*>T_c$ and then begin to slow slightly. That slowdown may allow a previously trailing, unowned post-event partner front to become an ordinary root and supply further backward acceleration.

The reduced mirror calculation gives:

$$
2c_fs=E(T)+E(s),\qquad E(T)=\int_{T_c}^{T}[c_f-v(u)]\,du.
$$

Its formal onset coefficient is:

$$
A_0=\frac{K}{2c_f^3(u_*-T_c)^2}.
$$

The third Jack K. Hale review numerically reproduced this reduced delayed-ignition branch. That is measured evidence for the reduced system, not a full-system theorem.

The current rank-one obligation is to resolve one of two outcomes:

1. Prove that the delayed-ignition branch exists in the full delayed system. This would make the restart multivalued unless a new selection law is adopted.
2. Find and prove the complete-ledger estimate that excludes the delayed-ignition branch.

Immediate sustained ignition at $T_c$ is excluded in the declared absolutely continuous class because its row is nonintegrable. Delayed ignition remains unresolved.

Do not describe it simply as "spontaneous braking," as though an unexplained velocity change has been established. The exact question is whether the delayed equation admits a branch in which a root detaches and then supplies the slowdown self-consistently.

The current queue head is:

`mirror_event_family_completion_and_right_trace_selection`

It is owned by the trailing-front activation dichotomy, together with the remaining measure-typing and event-adjacent no-cascade checks.

### Circular binary program

The prescribed all-past two-label circular certificate is complete for every $0<c_a\le c_f$.

It certifies:

- exactly one ordinary partner root per receiver;
- no positive-delay self root;
- positive $D_t$ and $D_r$;
- a complete two-label ordinary ledger;
- zero compatibility residual at the selected radius and frequency under the proposed post-sum response.

At $c_a=c_f$, the delayed half-angle is the Dottie number:

$$
D=\cos D.
$$

The compatible radius is:

$$
R_*=\frac{K}{4c_f^2D(1+\sin D)}.
$$

This is a prescribed-chart compatibility certificate only. It does not prove delayed-system existence, capture, retention, stability, conservation, action transfer, or physical realization.

The next constructive binary objects are:

1. Local regular history-to-ledger existence and uniqueness in an atom-free finite-delay $W^{2,\infty}$ setting with a fixed census and positive range, delay, $D_t$, and $D_r$ floors.
2. A quantified census-stability neighborhood around the circular chart.
3. Only after those two steps, a local circular-binary contraction theorem.
4. Afterward, the complete interval-numerical contract.

The higher-ceiling case $c_a>c_f$ reintroduces returned self and partner roots. It does not inherit the at-or-below-wake binary derivation.

Sections 12–14—including the braid inverse problem, action interface, cycle diagnostics, energy interface, persistence and stability program, and Noether-sea subsection—still require their own independent review before being described as reconciled.

## Active lane 2: Topo and Applications organization

A separate Codex background task titled **Topo UI and Applications** has already been created.

Task ID:

`019fc5bf-9399-7140-b6b5-5a92899ef310`

Do not duplicate its implementation blindly. Read its status or inspect the live Topo packet before editing overlapping files.

We can discuss, review, or redirect that task from this voice chat.

### TOPO-001 is complete

Topo is a two-dimensional, display-only viewer around one prescribed uniformly translating architrino.

Its accepted first raw product is **Signed ordinary wake intensity**. It is not called scalar potential, and it is not receiver acceleration.

For $0\le\beta<1$, every off-source sample has one regular causal root. TOPO-001 supplies exact reference geometry, the static radial control, equal-distance leading and trailing controls, polarity reversal, and the result-state contract.

At $\beta=1$:

- the strict trailing half-plane retains one ordinary root;
- the leading half-plane has no positive root;
- the off-source transverse line has no positive root;
- the source point has a nonisolated degenerate root family.

Topo must display ordinary, singular, unavailable, nonordinary, unresolved, and display-clipped states distinctly.

It must never manufacture a scientific value through masking, clipping, color choice, or interpolation.

### TOPO-002 is next

TOPO-002 is the interaction and color contract. It must freeze:

- the scenario menu;
- fixed-source camera;
- beta slider;
- contour-count control;
- raw-value probe;
- legend;
- unavailable-state presentation;
- comparison of Linear, Signed log2, and Asinh transforms using the same synthetic signed surface;
- accessibility and keyboard behavior;
- focus states;
- reduced-motion behavior;
- desktop and mobile behavior.

Shared UI decisions already accepted:

- Reuse the shared dark neutral-purple semantic shell. Do not introduce another hard-coded purple palette.
- Reuse the established top-right Architrino application control bar.
- Do not change the existing Home behavior.
- Reuse the shared `PanelCollapseIcons.js` left-panel open and closed icons.
- Preserve the narrow reopen rail.
- Preserve keyboard access, `aria-expanded` behavior, inert closed content, and reduced-motion behavior.
- Reuse the established Architrino beta-slider interaction pattern.
- Keep scenario, beta, contours, transform, legend, and raw-value inspection in the local left panel.

### Accepted Applications grouping

Do not continue growing one flat grid containing every application.

The accepted scalable organization uses four category scenes:

#### Learn & Reference

- Atom
- Causal Delay Feedback
- It's Greek to Me!
- Hyde Periodic Table
- Periodic Table
- Standard Model

#### Explore Models

- Lattice Lab
- Lorentz Geometry
- Molecule
- Photon
- Topo

#### Analyze Evidence

- Braid Search
- Equation Mapping

#### Build & Simulate

- Animator
- Borg

Each category opens a child scene containing its applications.

Preserve direct application routes, global search access to individual applications, existing scene identities, and the existing global Home icon behavior.

## Voice-collaboration preferences

- Speak a little more slowly than normal.
- Let me finish my question before beginning a long explanation.
- Lead with the plain-language answer, then provide the equation or technical distinction.
- When I ask "say that again," shorten and simplify it instead of repeating the same wording.
- Define unfamiliar terms immediately.
- Clearly distinguish derived result, proposed law, numerical or diagnostic evidence, open theorem target, and speculation.
- Do not agree merely because an intuition sounds attractive. Check it against the wake geometry and complete ledger.
- Do not lose the global superposition principle by analyzing one row as though it were the entire universe.
- If I am brainstorming, explore the idea before editing unless I explicitly authorize a change.
- If I authorize a document change, make the smallest coherent edit, update necessary terminology and references, and verify it.
- Give concise progress updates during longer work.
- Do not redo completed reviews or derivations unless live evidence exposes a contradiction.
- Capture any substantial new derivation or corrected claim boundary in the owning priority packet rather than leaving it only in chat.

Begin by briefly confirming that you understand the two active lanes and the current field-speed rank-one problem.

Then ask me only:

"Would you like to continue the field-speed mathematics, check the Topo background task, or discuss the Applications organization?"
