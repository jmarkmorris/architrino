# Reviewed connected owner repair proposal

Legend: ✓ Done — supported bounded work complete; ◐ Partial — preparation complete with stated condition remaining; ○ Not done — not executed. All generated writes below are **Blocked awaiting explicit generated-drift authorization**. No scientific source, independent oracle, acceptance threshold, historical receipt, or remote/public authorization is included.

| Status | Target | Reviewed change and execution |
| --- | --- | --- |
| ✓ Done | Archie source-provenance negative control | `tests/archie-service-source-index-snapshot.test.js` now validates a fresh baseline before mutating its source hash, so reading-copy drift cannot mask the intended negative. Owned `392dd281-7f69-4a4f-b015-9d64d1401327` passes both selected provenance/authority controls. The separate fixture freshness assertion remains intact. |
| ◐ Partial — Blocked | Archie six-source generated fixture | Run upstream `node scripts/check-content-integrity.mjs` first; resolve only separately authorized upstream drift. Then `node scripts/archie-service/build-source-index.mjs --write` and `node scripts/archie-service/build-source-index.mjs --check`, updating only `tests/archie-service/fixtures/source-index/source-index-snapshot.v1.json`. |
| ◐ Partial — Blocked | Archie generated query and stdio expected responses | Reviewed proposed comparison has 18 leaf changes across eight cases: enclosing snapshot identity, ontology section identities, exact read text and search teaser. Ranking, requests, cursors, limits, graph paths, privacy and authority fields remain unchanged. The source-index and tool owners explicitly classify these same-engine fixtures as deterministic contract evidence. Run `node scripts/archie-service/validate-mcp-tool-contracts.mjs --write`, then `--check`; run `node scripts/archie-service/check-fixture-mcp-server.mjs --write`, then `--check`. Exact targets are `tests/archie-service/fixtures/mcp/mcp-tool-contract.v1.json` and `tests/archie-service/fixtures/mcp/mcp-stdio-smoke.v1.json`. |
| ◐ Partial — Blocked | Archie authored deployment candidate identity | After the full-corpus artifact is stable and its owning check passes, update only `snapshotPublication.candidateSnapshotId`, `candidateSnapshotSha256`, and `candidateRepositoryRef` in `tests/archie-service/fixtures/mcp/mcp-remote-deployment-contract.v1.json` to the actual retained candidate. Preserve `fixture_only`, `remoteReady:false`, `publicDeploymentAuthorized:false`, `candidateIsAcceptedMain:false`, visibility, capabilities and authentication/rollback requirements. Run `node scripts/archie-service/validate-mcp-remote-deployment-contract.mjs`, the complete Archie source/tool/stdio/HTTP/deployment test selection, and the separate actual SDK conformance owner. Current candidate is `source_snapshot_full_corpus_v1_96a7ddf5ba18a44daf43`, hash `fd75267ad9c80fb85c78d1868704de409fa2ccd4fbbf5393479adbdcada03e82`; remeasure at execution rather than freezing this proposal as future authority. |
| ◐ Partial — Blocked | Borg adjudication projection | Read-only review checks all 28 projected adjudication rows against the current owner and all five requirement cells. Exactly one source-row spelling changes: `../analysis/f6c-geometry.md` to `f6c-geometry.md`; both resolve to the same evidence file from the owner directory. All evidence paths exist, 25 exact catalog bindings plus one preserved off-catalog row remain, and the three H4 measured passes/zero H5 passes remain unchanged. The proposed update is only `sourceSha256` and that one `sourceRow` in `reference/priorities/braid-program/contracts/braid-candidate-adjudication-projection.v1.json`. No projection generator was found among its scripts/src/tests consumers; this is an explicitly disclosed manual machine-projection maintenance step, not a fabricated `--write` command. Then run `node scripts/check-borg-scientific-status-projection.mjs` and serial `tests/borg-library.test.js tests/borg-scientific-status.test.js`; if registry output drifts, its separately named owning command is `node scripts/borg/build-assembly-registry.mjs --write` then `--check`. No adjudication grade or source evidence changes. |
| ◐ Partial — Blocked | PDG glyph inventory | Current shared catalogs require 147 SVG names; 141 exist, six are missing and none are extra, recorded in `pdg-inventory-review.json`. Run `VIRTUAL_ENV="${AAA_VENV:-../.venv}" "${AAA_VENV:-../.venv}/bin/python" scripts/glyphs/glyph.py --output-dir scripts/glyphs`. This generator has no `--write` mode: the explicit output command performs regeneration. It writes catalog-declared `scripts/glyphs/pdgedit-*.svg` only; no authored catalog or renderer changes are proposed. Verify with serial `node --test --test-concurrency=1 tests/pdgedit-generator-drift.test.js tests/pdgedit-renderer-drift.test.js` under the owned supervisor. |
| ○ Not done — Deferred | Reference surface and machine receipt regeneration | Existing campaign-owned targets remain with their owner: `node scripts/build-reference-surface.mjs --write` and `node scripts/build-machine-artifact-receipts.mjs --write`, each followed by `--check`. Do not infer scientific acceptance from refreshed metadata. |

The PDG Python entry now delegates to the same JavaScript reference renderer. Therefore regenerated SVG parity establishes reproducibility and catalog consistency, not independent rendering correctness. The older recovery report's proposed “independent JS renderer” comparison is superseded by this live source inspection. Canonical sample checks and separate authored rendering controls remain useful within their stated bounds.

Evidence: `before-bindings.json`, `archie-proposed-response-changes.json`, `borg-projection-review.json`, and `pdg-inventory-review.json`. The current Borg live-model binding remains a separate semantic App Solver decision; it is excluded from this mechanical refresh authorization.
