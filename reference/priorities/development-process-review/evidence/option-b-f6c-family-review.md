# B-REM-1 family independent review

## Scope and present disposition

Assignment: `b-rem-1 family review`. This record covers the parent, packaging, and streamed operational binding composition and their immediate preparation/registered-entry paths. The main implementation owner handles these changes; a separate implementation worker owns the emission/refined entry and launcher pairs. This reviewer owns only this evidence file. The [earlier checker and coordinator review](option-b-f6c-closure-review.md) remains unchanged and retains its bounded acceptance and original-map provenance.

This is a design/dependency review, not final merged-implementation acceptance. Read-only inspection of the named source sections and `git diff HEAD` supports reusing the existing coordinator operational manifest rather than introducing duplicate parent/packaging/streamed graphs. No conflict with the frozen serial closure checker has been identified in that design. Streamed v6 continuation-version handling needs an explicit disposition before this migration is accepted; the exact dependency is recorded below. No scientific or historical-current-transport acceptance follows.

`git rev-parse HEAD` returned `ab3ed93a59e69a5807397c24cefcfcb3a9642487`. Ambient staged and unstaged work is preserved; comparison is against HEAD, not merely the index. `shasum -a 256` reconfirmed coordinator `f0a310b0902b66b60b98ab2f6b2109f352a1263ad23cc1bd5b53763c197bcd7c` and checker `9887632e2900efb1feecf0dfe24ae5f138e46ba7ab870c56eede66a522e84609` during this audit. Implementation is concurrent, so line references below identify inspected sections, not an immutable final candidate. No scientific module was imported or executed, no process operation was launched, and no bespoke extractor was used for these findings.

## Minimal operational migration coverage

| Consumer | Operational selection to migrate | Required admission paths | Preserved boundary |
| --- | --- | --- | --- |
| Parent composition | Its three helper paths: prescribed-response launcher, circular-root launcher, and emission-refinement launcher, selected from the admitted common coordinator map | Pure `validatePlan` receives explicit `sourceAdmission`; generic-plan runtime admission supplies it to `parentContext`; registered producer/comparison and file-worker preflight/admit/final retain that path | Keep the existing eight per-parent operational bindings and closed scientific plan schema; the complete map/reader/coordinator/control union remains in the outer generic plan, not a newly invented scientific plan field |
| Parent preparation | Existing explicit coordinator and source-map selection | `prepare` admits the map; `validateConfiguration` checks full selection and original identity coverage; `derivePlans` passes that admission into every `validatePlan` | Keep historical `EXPECTATIONS`, original source census, document archive routes, independent closure checker selection, and outer invocation digests |
| Packaging | Existing generic coordinator admission; no packaging pin deletion is warranted | Both `fileOperation` and standalone `registered` call the plan-bound coordinator path; producer budget/source union includes all admitted coordinator sources | Retain `PINS` and `GENERIC_PINS`: fixed packaging/inventory contracts and modules, controls, independent readers/decoders, and externally selected inventory are not generic helper pins |
| Streamed caller | Five roles: coordinator, coordinator controls, and the same three helpers; replace their digest literals with path-only role declarations selected through the common map | Current v6 spec includes map and reader as two additional `bindings`; asynchronous admission precedes preflight validation and standalone registered target launch; coordinate binds the admitted selection to the existing lifetime | Keep all adapter, diagnostic, stream, continuation, codec, storage, refined-acceleration transport, readiness, package, fresh-evidence, mathematical, and historical acceptance selections at their existing authority |
| Streamed preparation | Explicit current schema/map/history inputs, or an explicit unsupported disposition before runtime discovery/publication | Do not let the old v4 constructor silently become a purported valid current preparer after helper pin removal | Candidate preparation remains `independentlyAdmitted:false`; no inference of scientific execution readiness |

The shared-map design preserves roles: the coordinator remains the map's single `admission` source, the captured reader remains `manifest-reader`, and controls/helpers remain `current-source`. Parent and streamed caller identities are selected separately by the outer plan or spec/caller digest; they need not be falsely relabeled as extra admission sources. The frozen coordinator's initializer at lines 120–134 requires its exact six-source composition. Adding parent/streamed sources to that manifest would violate that contract; reusing its existing roles does not.

The parent helper selection does not require expanding its scientific plan's operational inventory. `validatePlan` can compare its three operational helper tuples to the externally admitted common selection, while outer-plan coverage captures the full seven physical map/source bindings. This avoids altering closed Python schemas merely to carry operational admission metadata. Parent `NAMED` includes four separately selected current wrapper/control roles, but its reference instruments and `DEPENDENCIES` remain separately fixed. In particular, the role named `transport` denotes the refined-acceleration scientific verifier, not an operational helper eligible for this migration.

## Blocking dependencies and required controls

### FAM-1: v6 must not bypass retained evidence/header equality

At `run-f6c-streamed-leaf-diagnostic.mjs:422–424`, continuation admission compares the frozen header's source bindings and runtime bindings with the retained invocation, but its additional evidence checks are conditional on v4/v5. The accepted-parent/package equality predicate recognizes only v4/v5, and the historical-evidence equality predicate recognizes only v5. Inspection of the preceding continuation function shows no earlier closed retained-invocation version check. Consequently a newly produced v6 predecessor would skip those two predicates if it reached this route.

This is a source-level control-flow finding, not an executed forged acceptance. `git show HEAD:scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs` confirms that unknown-version fall-through already exists at HEAD. The migration must not convert that pre-existing gap into an untested route for its new supported output version. Minimal disposition: explicitly reject v6 historical continuation until separately reviewed, or extend the same retained-evidence equality predicates to v6 and add negative controls for each mismatch. Do not reinterpret a v6 operational completion as independent scientific segment acceptance. A control demonstrating rejection before these predicates, or the corresponding exact equality controls for v6, would discharge this dependency.

### FAM-2: preserve worker and canonical lifetime entry semantics

The shared prescribed helper at lines 75–88 automatically calls an exported `initializeSourceBindings(job.root, job.sourceMapSha256)` before `fileOperation`. The frozen coordinator at lines 673–683 supplies those fields only in serial-plan mode. Streamed preflight passes spec path/hash/self hash, while recheck passes sources and original identities. Adding that generic initializer export to the streamed caller would therefore cause missing-context initialization before the caller can read its spec. This is a prospective integration hazard, not a defect already introduced in the inspected streamed source. Use a differently named explicit asynchronous admission function reached through preflight/registered paths.

The streamed `coordinate` path at lines 905–916 must retain the canonical file-C instance for `assertLifetime`; a captured data-URL import has a distinct private lifetime registry. A captured coordinator import may perform source-map admission, but must not replace that canonical registry owner. Bootstrap coordinator bytes must agree with the externally selected lifetime identity, not an unauthenticated ambient import. Required controls include missing/wrong map digest, removed reader/helper/control, role/path substitution, spec-versus-lifetime map mismatch, and same-byte original-identity replacement. A fabricated plain `sourceAdmission` object is not authority at a public execution boundary; only the pure validator may rely on its explicitly trusted caller to provide admission.

### FAM-3: shared-map dependency convergence follows the paired worker

The emission-refinement launcher is one of the frozen coordinator map's three helper sources and is being edited by the separate implementation worker. The final common map must select the final merged launcher bytes and current control bytes. Selecting a changed helper under the old map should fail. This is an operational manifest dependency, not permission to refresh scientific references or retained historical tuples. Re-run admission and exact-selection process controls after that map settles; prior retained receipts remain evidence for their original selections only. The coordinator and checker themselves need not change.

## Streamed v6 schema and closure assessment

The proposed two extra fields belong inside the existing `bindings` object, not an uncaptured top-level inventory. The embedded Python body at lines 569–590 captures every `bindings.values()` member, verifies exact binding shape and size, rejects physical hardlink aliases, retains original identities, and rechecks them. It writes `sourceBindings.files=bindings` into stream metadata at lines 705–708. Thus map and reader capture/identity reporting follow the existing path without changing the Python body. Node completion admission at lines 879–884 requires exact identity keys for the complete source union and equality with preflight originals. This supports the design by code inspection, not by a new process test.

The codec's `HEADER_KEYS` and `StreamEncoder` enforce the outer metadata shape, not a fixed enumeration of the nested `sourceBindings.files` role names. Existing historical continuation header comparison must continue comparing each old header against its own original invocation; it must not require old headers to acquire current map/reader roles. The FAM-1 version condition is separate from this nested-shape compatibility.

Both live `validateSpec` and the lighter coordinate precheck contain closed binding enumerations and current schema checks. Update both for v6, preserving self/control path checks and original clock, process, lock, resource, output-layout, and false-authority predicates. Admission must require all seven physical bindings of the selected common manifest to equal the five operational roles plus map/reader in the spec; a helper-only comparison is insufficient. Lifetime `bindSources` at coordinator lines 422–428 merges against prior source selections, checks conflicting original identities, and recaptures the union. Bind the spec-selected admission into that lifetime before launching its registered target so a different CLI-selected map cannot be silently substituted.

The frozen coordinator's streamed wire predicate at lines 777–783 does not enumerate invocation versions or nested binding keys. The frozen closure checker admits serial-plan closure only; the migration must not broaden it to streamed mode or claim that it accepts a streamed receipt. No independent current streamed scientific acceptance instrument has been established by this review.

## Hidden paths and pre-existing boundaries

Parent `parentContext`, registered batch execution, and file-worker phases must all retain admitted helper selection. The preparer's pure `derivePlans` is a distinct caller of `validatePlan` and needs explicit admission forwarding; runtime-only initialization would leave it uncovered. Packaging `registered` is independently callable and must retain the same coordinator admission as its hook entry. Its exported `runtimeInventoryCommand` constructs the captured Python inventory command; it is not an authority to admit arbitrary current package modules.

`git show HEAD` confirms the streamed preparer already constructs v4 without `historicalEvidence`, while the caller already requires v5 with that field. This is pre-existing, not a v6 regression. An explicit unsupported result before Python runtime discovery is a bounded disposition if current preparation remains deferred; do not silently manufacture history or widen accepted evidence. Restoring valid current preparation requires an explicit source-map selection and required history/schema inputs and controls.

By inspection of parent Python validators at `prepare-f6c-parent-emission-refinement.py:214–227` and `verify-f6c-parent-emission-refinement.py:139–151`, scientific runtime capability entries are path-only, while the JavaScript composition accepts full bound runtime entries. The operational map migration does not resolve that separate transport mismatch or establish successful scientific production. Closed parent plan keys also bar adding a new arbitrary top-level map field. Historical adapter consumers in `f6c_variable_cell_adapter.py` preserve old parent entry/control/archive generations and legacy source/operational census expectations; they must not be rewritten to current wrappers just because current operational callers change.

## Final merged-review gate

After implementation owners notify that the candidate has settled, review `git diff HEAD` for the three callers, both preparers, common map, paired-worker helper changes, and affected controls. Check role preservation, no generic streamed initializer export, complete admission forwarding, exact spec/map/lifetime identity agreement, FAM-1 disposition, and original scientific/history declarations. Run known literal controls before any newly authored probe and inspect retained supervised process evidence for the final map. No final merged test totals are claimed in this initial audit.

Claim grade: measured for the named source/history/hash inspections; inferred for schema compatibility and minimal migration recommendations. Falsifiers are an omitted public/preparation path, a map role used as authority beyond its operational scope, an accepted spec with incomplete or conflicting map identities, bypass of retained evidence equality, or changed scientific/historical selections. Those observations reopen the corresponding assessment. Only this evidence file was authored by this review; implementation, the earlier review, manifests, queues, and the ambient index were not edited by the reviewer.

## Alternative review: v5 with externally transported map digest

The implementation owner proposed preserving the exact v5 specification fields and five operational binding roles, passing the externally selected map digest from the coordinator into `coordinate`, through worker jobs and the registered CLI, and returning all admitted map sources in `validateSpec`'s physical union. This avoids creating a v6 historical predecessor, so FAM-1's new-version exposure would not arise. It does not, as proposed, preserve the existing end-to-end source-identity contract without further changes.

### FAM-4: out-of-spec map sources are absent from Python's exact identity inventory

Live source inspection reconfirmed that the embedded Python capture loop at streamed caller line 579 reads the invocation and sources listed in its bindings, runtime, package, continuation, accepted-parent evidence, and historical evidence. It receives no separate operational map binding list. Therefore, for an ordinary v5 spec without map/reader in those inventories, merely adding the admitted seven-source set to Node preflight adds two physical paths that Python never captures. Completion admission at lines 881–884 requires `done.sourceIdentities` keys to equal the entire combined preflight/historical union and each preflight original identity to match. The proposed completion therefore fails that existing predicate. Passing a digest only to the Node registered entry does not change Python capture.

This is a source-level deterministic mismatch, not a launched process repro. A known-case-controlled target inventory demonstration showing both missing paths captured and reported by Python, without weakening exact identity equality, would falsify it. Do not hide the mismatch by placing admission data in scientific history fields, labeling map/reader as runtime capabilities, dropping those paths from closure coverage, or synthesizing Python-reported identities from Node observations.

Two coherent resolutions exist. One is explicit bounded binding transport into the embedded Python target, retaining capture/recheck and the exact completion inventory; this changes the Python body/argument identity contract but need not change scientific modules or mathematical references. The other is a deliberately separate Node-owned operational identity inventory with independently checked coverage, retained originals, complete outer closure accounting, and truthful receipt attribution. The latter is a receipt/ownership contract change, not an unchanged full-union Python proof, and requires its own downstream historical-consumer assessment. Neither follows automatically from keeping the v5 JSON field names.

### Frozen coordinator and checker consequences

The inspected coordinator still hashes to `f0a310b0902b66b60b98ab2f6b2109f352a1263ad23cc1bd5b53763c197bcd7c`. Forwarding `options.sourceMapSha256` at its `coordinate` call near line 959 is a narrow possible interface change, but adding a registered CLI argument also requires changing `lifetimeRegistered` near line 695: it currently accepts exactly `['--registered', specPath, specSha, callerSha, deadline]`. Bind the additional expected digest to the coordinator's retained `s.sourceAdmission.sourceMapSha256`, not a new caller-supplied expectation. Update the streamed call site, exact CLI argument count, missing/wrong digest controls, and any Python target argument checks if target transport changes. Preserve the original deadline and private canonical lifetime registry.

Explicit forwarding from the already parsed and admitted external digest is preferable to adding a generally exposed lifetime property for this one use. Nevertheless, it changes the formerly frozen coordinator subject. The new self hash and common map require fresh bounded coordinator/interface review and final-selection tests; the earlier acceptance remains evidence for its recorded old subject. This reviewer has not changed that subject or authorized implementation by making this design comparison.

The checker remains `9887632e2900efb1feecf0dfe24ae5f138e46ba7ab870c56eede66a522e84609` by `shasum -a 256`. It does not need modification to select a new coordinator/map for serial validation, and it must still reject streamed closure. Keeping v5 preserves the inspected v5 historical header predicates, but does not establish compatibility for any newly split source-identity receipt. Historical continuation's exact inventory comparison at streamed lines 409–415 remains relevant if those receipt semantics change.

Disposition: do not accept the v5 proposal as a minimal forwarding-only alternative. It is viable only after choosing and testing a complete capture/identity transport. The earlier v6 proposal, with explicit rejection of v6 historical continuation for this bounded scope, preserves the existing Python all-source capture mechanism and avoids coordinator changes; that remains the smaller reviewed implementation surface. This is an inferred design tradeoff from the named live code paths, not a process-test result or scientific acceptance. No implementation was edited and no process was launched for this alternative review.

## Candidate source review: implemented v6 and shared-map consumers

The owner selected v6 with map/reader inside `bindings`, restored the coordinator, and requested source review while the supervised streamed suite runs. This subsection records a source-only candidate disposition; the streamed preparer, new admission/continuation negative controls, and final test evidence remain pending. No reviewer process or test operation was launched during the owner's run.

### Streamed map coverage and original identity chain

Inspection of `git diff HEAD -- scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs` and its live entry paths finds no new blocking source defect in the implemented operational selection. `admitOperationalSources` at lines 472–483 captures the spec-selected coordinator, invokes its captured initializer using the spec-selected common map digest, compares the map size and reader binding, compares all five operational role bindings, and rechecks the admitted originals. The frozen coordinator initializer supplies the canonical manifest validation and exact six-source role composition. The spec's five operational bindings plus map/reader therefore cover its complete seven-binding admission set; missing or substituted roles cannot be supplied only through unrelated runtime or historical fields.

The pure validator at lines 485–507 requires v6, the exact closed binding keys, explicit matching admission, each operational role's selected binding, and the unchanged scientific bindings and limits. It includes the admission set in the bounded physical union. Preflight independently calls admission before validation at lines 853–859. Registered entry independently admits before target launch and rechecks admitted original identities after target closure at lines 764–777. There is no new generic `initializeSourceBindings` export in this caller, so the shared worker does not trigger the missing-root/map hook described in FAM-2.

Coordinate now bootstraps the canonical file-C owner from the lifetime's existing coordinator binding at lines 916–922, retaining `assertLifetime` and the original identity/clock checks. At lines 938–941 it admits the spec map and binds admitted sources and identities into that lifetime before starting the streamed operation. Frozen coordinator `bindLifetimeSources` checks conflicting bindings and original identities against the already CLI-selected common map. Consequently a spec-selected different map cannot become the operation's selection merely because it independently passes manifest admission.

The diff leaves the embedded Python body unchanged. Its capture loop includes map and reader through `bindings.values()`, and its metadata continues recording exactly those bindings. The unchanged Node completion predicate requires the exact complete physical identity inventory and equality with preflight originals. Thus the out-of-spec capture mismatch in FAM-4 does not apply to this implementation; no Python identity is fabricated from a Node observation. This conclusion is inferred from the inspected capture and equality paths, pending the owner's process evidence and missing/wrong selection controls.

### Continuation rejection placement and version scope

The new check at streamed line 409 explicitly rejects a retained invocation whose schema is v6. It executes after earlier bounded receipt/source verification but before frozen-header admission, continuation result construction, or target launch. That placement is sufficient to prevent v6 falling through the v4/v5 evidence predicates. Moving it immediately after decoding the retained invocation would reject earlier, but is not required to close the identified acceptance path. A negative control should reach this particular rejection with otherwise valid retained metadata, rather than pass solely because an unrelated earlier binding is invalid.

No all-version historical allowlist was introduced. Unknown versions other than v6 retain the pre-existing historical fall-through gap recorded in FAM-1. The current execution validator accepts v6 only, and this patch explicitly rejects that newly produced version as historical continuation. The absence of a general allowlist is therefore not a new blocker for this bounded migration, nor is it acceptance of unknown historical versions. It should not be silently broadened or represented as resolved by this patch. FAM-1 is resolved at source level for v6, pending its targeted negative control; existing v4/v5 equality predicates remain unchanged.

### Parent, preparation, and packaging composition

Parent `validatePlan` at lines 92–111 retains the exact eight operational bindings and closed scientific plan, comparing the three helper tuples to explicitly supplied common admission. Its exported `admitOperationalSources` at lines 251–256 admits the outer generic plan's complete source selection. Both registered execution and file-worker phases obtain that admission and pass it through `parentContext`; preflight, per-stage admission, and final re-admission therefore reach the helper check. Parent preparation's `derivePlans` forwards the admission selected by its explicit map/coordinator inputs. The inspected diff leaves the fixed parent scientific/reference/history declarations unchanged.

Packaging now exports `admitOperationalSources(plan,live)` at lines 50–51. It checks the original deadline callback before and after captured coordinator import and through `C.admitPlanSourceBindings`. Both file-operation and standalone registered paths call it before fixed scientific/inventory reads. This provides a bounded metadata admission seam for direct controls without executing those scientific owners. The producer budget retains the complete admitted map/source union. `PINS` and `GENERIC_PINS` remain unchanged in the inspected diff; their fixed packaging and independent-reader authority is preserved. This review does not require deleting or migrating those scientific pins.

### Checked candidate identities and outstanding evidence

`shasum -a 256` measured the following source identities, and direct inspection of the common manifest confirmed that its six source rows name the measured coordinator, control, reader, and three helpers with their original roles:

| Source | SHA-256 |
| --- | --- |
| Frozen coordinator | `f0a310b0902b66b60b98ab2f6b2109f352a1263ad23cc1bd5b53763c197bcd7c` |
| Frozen closure checker | `9887632e2900efb1feecf0dfe24ae5f138e46ba7ab870c56eede66a522e84609` |
| Common coordinator map | `bac429e22c115975ec905e9cbec2167ca416765ae2394b87a698ffc2e925b812` |
| Streamed caller | `9a8b0493c0c3400eccde7c60db6031bf04fe6f044e1bf1ddf0dd316c75871c40` |
| Parent composition | `999153c0e7eb51e90d0929230f8da2d9c4413f60a64acf8be0ff2eb45e5d494a` |
| Parent preparation | `a69b09a9278629680c17cd66fa151b515f1754e06f12a8d86f9934b1c8dd964c` |
| Packaging composition | `06ccff0984bafa40eeb2a9decff2283e0edccf939c68f94bb4730395d1ead2aa` |
| Selected emission diagnostics launcher | `37fc8aab3e33229878ba1bd50182d7b6eb75b41cdbe171817ece7fcf7b0309e7` |

Other measured common-map source hashes remain prescribed helper `fc758e8ffc1feaa566cc8e1849f73a0c8907f5ad7567e43fffd151eeceac30a1`, outer helper `71974054ddce7fc29b8464b9a7a63f8fbb04ee5b425dc997df4d40b2804341aa`, manifest reader `9097ef3fd3fb2040b48c601d81c184188e8a992aa4464694860d05d0bf76a56e`, and coordinator controls `a71ef095189f0bae802db6286868c4b99f4a756382c111e7980e9fa38ad4504b`. The selected diagnostics guard export remains independently callable without initializing the launcher's entry map, by inspection of its unchanged guard body; this does not accept the paired launcher's broader implementation.

The owner reports 21 parent controls and 11 packaging controls passing and a supervised 69-test streamed run in progress. Those counts are owner-reported, not reviewer-run or yet checked retained results in this subsection. Streamed fixtures now construct their own manifest for their transformed source set and include map/reader in v6; that is appropriate fixture selection, not evidence that a production invocation independently chose its expectation. Final disposition still requires the targeted admission/identity and v6-continuation negatives, the pending preparer review, and retained terminal evidence tied to the settled selection. No change to the serial checker or its authority is required.

Claim grade: measured for the named source/diff/hash inspections; inferred for bounded source-level suitability and closure of the identified design paths. A mismatching selected map, omitted map/reader Python identity, accepted v6 predecessor, or public path bypassing admission would overturn the corresponding conclusion. The candidate is not yet given final family acceptance.

## Extended source review: emission and refined pairs

The owner extended this review to the four paired production files, both operational maps, the 13-control admission suite, associated unit/process fixture changes, and the [paired preservation record](option-b-f6c-paired-transfer.md). Reviewer inspection of those sources and `git diff HEAD` finds no new blocking defect in the bounded operational-map transfer. This is not yet host-process acceptance for the pairs and does not resolve their deferred scientific/current-history transport.

Each entry captures its explicitly selected profile map and captured reader, invokes the unchanged reader's graph/schema/scope validation, requires exactly seven source roles, checks executing data-URL entry bytes against the selected entry, and rechecks the selected bytes before publishing admitted state. Roles are entry `admission`, launcher `launcher`, reader `manifest-reader`, and four `current-source` records for two Node control files and the two operational helpers. Each map carries the corresponding twelve check/dependency relationships. The physical closure has eight bindings when the map itself is counted; the initializer returns seven source records and a separate map binding. These counts differ intentionally from the common coordinator map's six source records/seven physical bindings.

Both launchers require the external map digest in their closed CLI. They check entry/launcher selections against admitted state before importing selected helper bytes, supply root/map digest after caller job fields to every worker, and forward the same digest to registered stage arguments. The worker initializer and subsequent `fileOperation` require matching admitted map/root. `planBindings` includes the map and all seven sources; preflight, registered execution, rechecks, and final publication use that complete operational closure. Scientific plan schemas and their twelve-entry emission/nine-entry refined operational lists are unchanged. Runtime inventory remains an explicit pre-existing branch that runs the embedded metadata inventory without loading a scientific plan; it is not a map-authorized scientific launch bypass. Diagnostic guard exports remain usable without launch admission, as required by the common coordinator's diagnostic-helper import.

### Preservation independently checked against retained bytes

Reviewer `shasum -a 256` authenticated all four retained `.local-data/development-process-review/b-rem-1-paired-baseline/<basename>.source` files against the preservation record. Standard `diff -u` between the two authenticated retained entry files and current entries shows only operational helper literal removal, new map admission/coverage, and its invocation wiring. Their scientific/history declarations, Python strings, limits, and scientific admission bodies are unchanged by that paired transition. This independently checks source preservation without importing or executing their scientific producers or writing a custom extractor.

The apparent emission bridge pin change in `git diff HEAD` is already present in the authenticated pre-pair entry snapshot (`efb67f6f0b2aae98b88978be71a80566fe5cc5e13e63e61788d177ae4c7f7d30`). It must not be attributed to the paired worker or called a new paired scientific repin. Refined's historical helper hashes remain explicitly asserted against the retained replay in its unit test; selecting different current operational helpers does not alter the old logical identities. Any resulting historical/current same-path conflict remains a scientific transport dependency, not something these maps silently repair.

An important inherited boundary remains: the pair entries' unchanged `readBound` checks identity during each individual read, but returns path/hash/size without a retained original-inode identity. Later `checkBindings` performs byte/size checks rather than the coordinator's lifetime-wide original-identity comparison. The new maps receive those same per-read and later hash checks. This is not the stronger closure contract accepted for the frozen coordinator; equal-byte replacement between reads is not proven absent by the paired implementation. The source diff establishes that this distinction predates the map transfer. This review does not silently upgrade the pair's identity claim or demand an unrelated closure-contract migration.

### Controls and selected hashes

The 13-test paired admission file begins with an inert seven-source fixture before repository-target checks. Inspection verifies tests for omitted/wrong external selection, source-byte changes, reader/map changes during traversal, wrong scope/roles, missing coverage, mismatched entry/launcher selection, early stage CLI rejection, and actual captured worker admission/executing-byte rejection. The two process fixtures now select maps for their synthetic entries and forward the digest. They exercise launcher/process plumbing, not real scientific execution or independent mathematical conformance. Their earlier deliberately different on-disk entry is replaced by the selected fixture bytes because current source admission now requires that on-disk binding; the separate captured-worker changed-entry negative supplies a distinct executable-byte check.

The seven family admission tests were also read. Their known inert closure precedes current admission; they cover all three consumer admission functions, fixed declaration/Python-string preservation, omitted and substituted streamed roles, parent/packaging map/helper/reader omissions, malformed selected graph coverage/roles/scope, early public-entry rejection, and expired-deadline rejection. Their reported pass counts are not independent reviewer reruns. A targeted otherwise-valid v6 historical-continuation rejection control is still a separate outstanding item at this checkpoint.

Reviewer `shasum -a 256` confirmed all four production and both map identities against the paired record:

| Artifact | SHA-256 |
| --- | --- |
| Emission entry | `4d0af8529a6f11587dfa5bf5fbe58b460c19cbbcaed30065ff8b454c2be980f8` |
| Emission launcher | `37fc8aab3e33229878ba1bd50182d7b6eb75b41cdbe171817ece7fcf7b0309e7` |
| Refined entry | `c3b5a8a230ef691996686cd13c84193bf95808bda5a1d51809ffa42640ad0992` |
| Refined launcher | `eb08281ebf7ee7dabe803b0abd66a5d175c9e74bda85bb8aa07750e6869a61bd` |
| Emission map | `643bbe03f23263e5e8fb37527d236e07cf7dd38a3d4c107484f9903818ec4656` |
| Refined map | `526b93e18bc12a279842e23c24dadc657c93ef3ca6f344df5147665e9e9a79a7` |

The four mapped unit/process controls also match their map rows by individual `shasum` inspection: emission unit `39fd667860c29c53570541f0b6319117b71d5c1f2118bcce659ac723cfbd24e8`, emission process `c43df866f414f73ba31dc40a5e846f6c07d837bc440a041f0171020cba4866db`, refined unit `bf9a9f4878350c6516580eeed42f2690a05d187df4aa7ce7dfc28966485d9d40`, refined process `bd38591c8b98f9c752832a4a2e6f2cd75711cd58d9e712424594793f4dc31fd0`. Paired admission controls hash to `f86e5b41470e41dde9a0810e781d611eb2404e98b7309477ff04ddf9c49c45c2`; family admission controls hash to `689aa3e30ea09becb53e99bb1c427a84a9ef840605b36c16feb46e60dd77838c`.

The paired preservation record reports 74 focused passes (61 existing plus 13 new) and the main owner reports seven family admission passes. Reviewer verification here is source/diff/hash inspection only. Paired host-process tests, streamed preparer, and final negative/control evidence remain pending. A missing selected binding, executable import outside the admitted selection, altered scientific declaration relative to the authenticated baseline, or failure of the scoped process controls would reopen this source-level disposition.

## Retained streamed completion for the v6 candidate

Reviewer `tail` inspection of `.local-data/owned-compute/logs/3f28cf51-5cf8-41cd-becc-a86858094dc0.stdout.log` confirms 69 tests passed, zero failures/cancellations/skips/TODOs, and 211886.950458 milliseconds. Direct inspection of the corresponding lease confirms the command `node --test --test-concurrency=1 tests/f6c-streamed-leaf-diagnostic.test.js`, completed status, exit zero, null signal/error/stop reason, and closed process group; elapsed wall time is 211.956 seconds. This is retained operational test evidence, not a new reviewer process observation or scientific acceptance.

Measured SHA-256 values are stdout `9fdd36211e207c11f770fb7277b1b8c6aa58e0046e05569dd7e871c86c02fbcf`, lease `03658cec0d293de44ad6d74635ab333c116d5f99879c38ce97ba4476bc2b3b0d`, and current streamed controls `7a63cddd4812553e25d6dcf4dc666adc5ace1bd5e52aa7d2e16ac2e0720c8431`. The lease does not itself attest a source hash for the test file; the current hash and the owner's run selection are recorded separately rather than inferred from the command string. The streamed source and common-map candidate identities are recorded above. No process was launched by this reviewer. This closes the previously outstanding supervised streamed terminal-result inspection, not the remaining family review items.

## Streamed preparer identity finding and completion criterion

### FAM-5: retain the new preparation's original admission identities

The reviewed v6 preparer captures and imports the externally selected streamed module, admits the explicitly selected common map, preserves scientific pins, constructs explicit historical metadata, runs only the unchanged Python runtime inventory, and produces a closed v6 spec with `independentlyAdmitted:false`. Its nine controls were inspected, including a known source-replacement fixture before current target use, positive metadata-only preparation, missing/wrong selection, wrong roles, scientific/history rejection, source-byte change during discovery, and write-once CLI publication. Those are appropriate preparation controls; they do not run scientific targets.

At the inspected candidate, however, `capture` checks the streamed file identity during reading but discards it in its return at line 24. `loadPreparationSources` at lines 38–49 cleans the map and source captures and calls `C.checkBindings` without the originals supplied by `admitted.identities`. `prepare` likewise calls `C.checkBindings(sources)` at line 80 without originals, and CLI lines 106–108 perform new admission and rehashing before publication. Equal-byte file replacement during runtime inventory therefore passes these checks by source-level reasoning: size/hash are unchanged, and the old inode identity is either discarded or not supplied to the final comparator. The inspected negative test changes file contents, so it does not exercise this case.

The original v4 preparer and general B byte-verification contract did not promise lifetime-wide inode retention. This finding must not be attributed as a regression of an old identity guarantee or confused with a scientific authenticity bypass: a later launch captures a new invocation with its own originals. It does limit the new preparer's claim to hash-only preparation closure. For the owner's requested stronger actual preparation closure, preserve the captured streamed identity and common admission originals across discovery, final validation, and CLI publication; reject conflicting identity merges and do not replace the old baseline with fresh admission. Identity metadata can remain private because `C.clean` already strips it from the closed spec binding fields.

Recommended narrow control: after a known successful preparation, replace the streamed source, map, reader, or selected operational source with equal bytes in a new inode during the observed runtime-inventory seam. Preparation must reject for original-identity mismatch and publish no success. Include the final CLI path, so reacquiring admission there cannot silently renew the preparation identity baseline. This is an open condition for the stronger preparation disposition requested by the owner, not authority to edit scientific schemas or inputs. No reviewer probe or process was launched to demonstrate the counterexample while owner tests were active.

### What B-REM-1 completion does and does not require

The live B-REM-1 row in [the remaining migration plan](../analysis/option-b-remaining-migration-plan.md) explicitly requires externally selected manifests/captured readers on every supported path, actual preflight/worker/stage/completion checks, source/graph substitution rejection, unchanged numerical/reference sources, and host-process controls for shared helpers **and all dependent callers**. It explicitly routes distinct scientific roles into B-REM-4. It does not require a new successful scientific parent or package computation.

The common coordinator's successful synthetic serial hook and the paired/streamed host controls establish common machinery, but do not by themselves exercise the actual parent/packaging hook import and admission path inside the frozen worker's `pureHook` restrictions. Direct metadata-admission positives and direct early-rejection API tests are useful, distinct evidence. They are not host-process controls for those two actual hook consumers. Closing the package without either covering those paths or explicitly amending its stated criterion would overstate the present evidence.

Minimum remaining operational coverage: route each actual parent and packaging hook through the frozen coordinator's captured worker/host path after a known literal worker/hook control. Show the externally selected common map is admitted under that route; show missing/wrong selection is rejected; and stop at an explicit unchanged scientific-input boundary or use separately authored inert metadata controls. Do not execute scientific producers, refresh fixed scientific pins, fabricate accepted scientific records, or weaken hook restrictions merely to obtain a positive. Retain the expected stopping boundary and actual worker/process closure. Existing generic serial evidence supplies terminal machinery coverage, while this additional test supplies the currently missing consumer-specific routing coverage.

With that bounded routed coverage, FAM-5 resolved at its declared strength, the pending v6-continuation control checked, and final owner test receipts inspected, an honest disposition can be: **B-REM-1 operational source-binding transfer complete for the enumerated family; current scientific parent/package success and deferred historical/scientific transport were not tested or accepted.** Without the routed parent/packaging controls, retain a partial disposition under the unchanged live criterion. No broader numerical/data launch is needed to resolve this evidence gap.

The owner subsequently reports 216 short controls, a repaired 26-test paired/serial run, and ten additional streamed controls passing, with a final consolidated 75-test streamed run underway. These are owner-reported at this checkpoint; retained results and successor map identities will be inspected before final disposition. The initially reported pair failure concerned the test runner's serialized `Admission` reference; source inspection shows the worker has since added that import to the serialized fixture. This does not retroactively turn the failed run into a pass or attribute that failure to production.

### Required-check wiring and repaired process evidence

`git diff HEAD -- scripts/check-content-integrity.mjs` shows the existing required Option B test step, retaining `--test-concurrency=1`, now includes coordinator admission, paired admission, family admission, and frozen current-closure controls alongside the original manifest/root-cover controls. Inspection of the paired/family admission tests shows that they use Node metadata, inert files, early-rejection Node CLI calls, and captured Node workers, not Python scientific or host-observation jobs. Their inclusion in that required step is appropriate. Streamed preparation's shared-macOS-venv tests and host-process suites are not added to this CI command. This is source-level wiring verification, not a full Content Integrity or Linux execution result.

Reviewer inspection of the repaired `c0f31571-c24a-4dc4-9c88-5ca8a82b5b72` stdout and lease confirms 26 passes, zero failures/cancellations/skips, 22470.325834 ms test duration, exit zero, and closed process group (22.537 s lease wall time). Its command selects both paired process files and the serial coordinator process file. The retained log names the successor serial record directory `.local-data/braid-analysis/option-b-current-closure-xcgOW2`; that record must retain its own selection, distinct from the earlier `txgLmh` run. The two earlier `bafd6885-416b-4528-ad71-5e9ac45bb65c` failures are visibly `ReferenceError: Admission is not defined` inside serialized `runFixture` at the EPIPE tests. The added explicit import in each serialized program and subsequent passing EPIPE controls support test-fixture attribution, not a production failure or erasure of the failed result.

Reviewer inspection of `cdcdba93-feef-4ab0-b89b-9460c64d5846` stdout/lease confirms ten passes, zero failures/cancellations/skips, 11206.978042 ms, exit zero, and closed process group (11.272 s wall time). This is the selected `operational map|actual captured Python` subset, including map/reader original-identity replacement negatives and four positive capture cases. It does not substitute for the owner's pending consolidated final 75-test run. No reviewer test or process launch was used for these retained-result inspections.

### Historical predicate factoring, full streamed result, and routed-test correction

Source inspection confirms `historicalInvocationVersion(schema)` contains exactly the prior explicit v6 rejection predicate, and `inspectContinuation` calls it at the same prior check position. The historical-input test adds a known literal v5 positive followed by v6 rejection against that actual function; the preceding three historical-input controls are unchanged. This meets the targeted predicate-control design without creating forged accepted scientific history. The factored streamed source hashes to `53997c14b8326402cd4d7f383a7f1b0702ddcd638555b1c78f74f206c7f35de0`; historical controls hash to `455fda5ea3e63bcffc3287541e307a10b65ce62f106734b5ccaf72e262061f82`. Post-factor owner controls remain to be checked as retained evidence.

Direct stdout/lease inspection of `902faa72-42c0-4ba3-8f32-70d6e47cc1fc` confirms the complete pre-factor 75-test streamed suite passed with zero failures/cancellations/skips, 215220.11575 ms test duration, 215.32 s lease wall time, exit zero, and closed process group. This result retains its pre-factor provenance; unchanged predicate semantics do not make the earlier process run a run of the later source bytes.

The seven routed host controls use the actual frozen coordinator, actual parent/packaging hook bytes, a complete externally selected operational map, and a deliberately empty configuration that should stop before science. The literal control traverses the captured coordinator worker and `pureHook` before the six target cases. Missing-map and conflicting-map cases have distinct expected errors, avoiding a claim that a coordinator-level conflict reached the hook. This is the requested bounded design.

The first run, `a906474c-cefd-44c5-af1e-df835ce8eed2`, has one literal pass and six failures, measured by retained stdout; its lease records exit one and a closed process group. All six failures are test-side `TypeError: The "path" argument must be of type string. Received undefined` at test line 42. Inspection identifies `path.join(root,C.LOCK)` there, while the coordinator defines a private `LOCK_PATH` at line 313 and exports no `LOCK`. Therefore these failures do not yet adjudicate the expected consumer rejection boundaries: the test errors before inspecting retained stderr. Correct the test's lock selector using the documented exact path or an existing correct export, leaving the coordinator unchanged. Retaining stdout/stderr before subsequent assertions would preserve diagnostics if another boundary fails. At this checkpoint the routed suite has not passed and cannot close its coverage condition.

The preparer still hashes to `e21c921691961413a01c5f619e9d9808f5b3651d542577ff128f93d687df4969`; inspection still shows the identity omission in FAM-5. The remaining substantive implementation condition is preparation original-identity retention; the remaining routed coverage condition is the corrected host test and inspected terminal evidence. No new production blocker was found in the paired migration or historical predicate factoring.

## Final candidate source disposition and actual consumer-worker coverage

### FAM-5 resolved in the successor preparer

Reviewer full-file inspection of the successor preparer and its controls confirms that `capture` now returns the streamed module's original identity. `loadPreparationSources` retains the original streamed/map/reader/operational identities, rejects conflicting identity merges, merges the captured admission originals, and checks that same baseline. The exported preparation path retains these identities across Python runtime inventory. The CLI reuses its first captured state rather than reacquiring admission, checks the original baseline before publication, and checks it again after write-once publication before emitting a success receipt. These private identities do not enter the closed spec. This resolves FAM-5 at the requested operational preparation strength; it does not assert lifetime identity retention for every scientific/runtime input.

The inspected 11-test file establishes its replacement utility against literal bytes and verifies unchanged digest with changed inode before target controls. Both map and streamed-module equal-byte replacements are exercised at direct inventory, CLI inventory, and CLI publication seams. Each expects the actual original-identity error; CLI stdout must remain empty. Publication-seam failure deliberately leaves rejected metadata but no success receipt, and no scientific output is created. The owner reports all eleven passing; the consolidated current-candidate terminal result is recorded separately below when available. Reviewer `shasum -a 256` measured preparer `c898f585066dc4b26ffce64cbb23f4e39912835dd597ef184f7abc73aaa3a53e` and controls `71c68602b06781fbdfc82134fc5ae7724fd3ffc224c1c934282566bd143e038d`.

### Failed whole-coordinator fixture retained; worker-only minimum accepted

Direct inspection of `.local-data/braid-analysis/option-b-family-parent-EO0xVL/operation/rejection.json` establishes an actual parent hook rejection at `closed fields`, with `ordinaryProcessesClosed:false`, `lockReleased:false`, `workerClosureFailures:[{kind:"hook",message:"closed fields"}]`, and the original 120000 ms guard. Frozen coordinator source inspection at `lifetimeFileWorker`, final closure checks, and cleanup shows that any rejected file-worker promise is conservatively recorded as a closure failure. This is not a successful whole-coordinator rejection/cleanup receipt. The owner reports the shorter outer deadline terminated that test and later recovered only its verified owned stale lock; this reviewer performed no recovery or process action. Earlier direct-`execFile` silent failures retain unresolved attribution: a process-group cause was not proven, and canonical serialization also failed. Neither those attempts nor subsequent lock-contaminated cases prove a production hook defect. The retained evidence supersedes speculative harness attribution without erasing failed runs or changing the frozen coordinator.

The replacement `tests/option-b-f6c-family-process.test.mjs` is acceptable as the minimum additional consumer-worker coverage. Source inspection confirms the real shared helper creates a real worker importing captured frozen coordinator bytes, invokes its `initializeSourceBindings`, then its `fileOperation` and `pureHook`, which imports actual captured parent/package hook bytes. The wrapper extends the actual Worker constructor and observes its exit before the helper settles; it also checks terminated `threadId === -1`. It does not mock the worker, bypass the hook guard, run the whole fatal lifetime, or claim successful science. A known literal successful hook precedes all six actual consumer cases. Selected maps reach the unchanged scientific configuration boundaries; missing-map and wrong-map cases reject with distinct exact admission errors. Parent/package source inspection confirms those configuration boundaries follow operational admission.

Reviewer stdout/lease inspection of `56922118-6d6e-4083-b738-94402f4bdd56` confirms seven passes, zero failures/cancellations/skips, 268.799291 ms test duration, exit zero and closed process group. Actual selected routing records at `option-b-family-parent-xhU5l6/routing.json` and `option-b-family-packaging-BAhXtP/routing.json` retain the expected errors, worker exit observations, actual hook/coordinator/map bindings, and explicit scientific nonacceptance. A worker's termination code is not asserted as successful scientific execution: rejection is the intended bounded result. The source test hashes to `8c4714de26b1a92c04d73621334643a6c9a2f5c6322d44a1819dfd95a7198d27`.

This supplies the previously missing actual parent/packaging worker-import/admission evidence. The independently checked whole-coordinator serial positive supplies separate lifecycle evidence; paired and streamed process controls supply their own stage/completion coverage. Repeating the deliberately fatal whole-coordinator path or running deferred parent/package science is not required for this operational-transfer criterion. No change to frozen closure or lock predicates is authorized or needed.

### Current identities and remaining terminal check

Reviewer `shasum -a 256` reconfirms frozen coordinator `f0a310b0902b66b60b98ab2f6b2109f352a1263ad23cc1bd5b53763c197bcd7c`, frozen checker `9887632e2900efb1feecf0dfe24ae5f138e46ba7ab870c56eede66a522e84609`, factored streamer `53997c14b8326402cd4d7f383a7f1b0702ddcd638555b1c78f74f206c7f35de0`, and common map `bac429e22c115975ec905e9cbec2167ca416765ae2394b87a698ffc2e925b812`. The historical function and its actual continuation call remain the exact explicit v6 rejection, tested with literal v5 positive/v6 negative; no general historical allowlist gap is claimed repaired. The required Content Integrity change remains one existing Node-only test-argument line by `git diff HEAD`.

The successor paired maps hash to emission `f82ae1b1b99625449167d8a98ece7f2b6face8181b72f38ad349b7f792077dba` and refined `9cd0a38eab35ff81cde21b00129ba8156f9c61523989dbb44b418c39ccc63c8f`. Their process-control rows match separately hashed current controls `c37575eaf55bc40aa1bfb2481e6ef4a73c2ab95595f92f158241624ab1e2a367` and `3c13fc3b1c13b44e3eea0cc0261a5287d0574ad4e99da7ece031ea5e27354b04`. Earlier map identities above remain prior selections, not current ones. Direct inspection of `option-b-current-closure-xcgOW2/observer/closure.json` confirms accepted external closure v2 selecting the frozen coordinator, common map `bac429...`, and checker `988763...`; it is distinct from prior serial records.

At this checkpoint there is no remaining implementation blocker in the reviewed enumerated family. Source acceptance, FAM-5 resolution, and minimum consumer-worker coverage are accepted within the operational scope. Consolidated lease `beb8d12e-dd29-4b8d-bc13-e6277197d48a` was still running when inspected, so its final pass count and closure are not yet accepted or inferred. Final combined-candidate disposition remains contingent only on its terminal result and unchanged selected bytes. Scientific parent/package success, general scientific current transport, historical reacceptance, and stronger paired lifetime-inode guarantees remain outside this acceptance. All reviewer changes are confined to this evidence file; no reviewer test, scientific process, staging, generator, or publication action was performed.

### Code-review conditions closed; terminal run only

Subsequent `git diff HEAD` inspection of the new remaining-F6c-family section in the source-admission contract confirms the common/pair map distinction, captured streamed map/reader inventory, preparation original-identity retention, separate serial closure authority, scientific/history exclusions, and Node-only required admission wiring agree with this bounded disposition. The addition does not change the manifest schema or frozen checker. A fresh `shasum -a 256` check reconfirms the six coordinator/checker/streamer/preparer/routed-control/preparation-control identities recorded immediately above. No additional implementation or review-condition changes are requested. The main owner may prepare queue/document updates with the precise status **code review accepted; consolidated terminal validation pending**. Do not mark the consolidated 327-test selection passed or B-REM-1 finally validated until its retained terminal count, exit, process-group closure and selected-byte stability have been checked. Lease `beb8d12e-dd29-4b8d-bc13-e6277197d48a` remained running at this inspection.

## Final bounded acceptance — B-REM-1

Accepted for the enumerated F6c operational source-binding family. Reviewer terminal stdout and lease inspection of `beb8d12e-dd29-4b8d-bc13-e6277197d48a` confirms the consolidated 20-file selection completed with **327 passes, zero failures, cancellations, skips or TODOs**, 248530.41 ms test duration, 248.595 s lease wall time, exit zero, null signal/error/stop reason, and `processGroupClosed:true`. The lease finished at `2026-09-12T18:54:02.855Z`. This closes the final pending validation condition; no implementation or review blockers remain for B-REM-1's stated operational transfer.

Reviewer `shasum -a 256` measured terminal stdout `db0ae8805dc44156df78103afbddcead440200ea64c8441c1cfd7c5a0de21b11` and completed lease `7bf6b6b9636d34bec58a2f9b52594320bef849772911c61c2aa2549e8255dbe7`. The final inspection reconfirmed the recorded coordinator/checker/streamer/preparer and two new host-control hashes. The immediately preceding final-run inspection also reconfirmed all parent/preparation/packaging and paired production hashes plus all three selected maps, with no differences from the accepted successor identities above. The owner reports no intervening production/test changes. These are current byte measurements plus retained test receipts, not an assertion that the supervisor's command string alone cryptographically attests every tested file.

The consolidated selection includes the actual historical predicate control, FAM-5 direct/CLI identity-replacement controls, actual parent/package captured-worker routes, paired process controls, frozen serial closure controls and full streamed controls. Their distinct evidence boundaries remain as recorded: consumer worker rejection is not whole-coordinator safe exit, unchanged scientific boundaries are not successful scientific runs, and the serial checker does not accept streamed science. Earlier failed runs and their selection/attribution distinctions remain preserved. The final acceptance does not reopen scientific or historical transport, strengthen the inherited paired inode guarantee, accept mathematical results, authorize publication, or close the four remaining migration items. The main owner may now record B-REM-1 complete within this scope. Reviewer writes remain confined to this evidence file.
