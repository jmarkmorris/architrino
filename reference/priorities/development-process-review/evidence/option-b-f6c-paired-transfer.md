# B-REM-1 paired operational source transfer

## Scope and current boundary

The emission-refinement and refined-acceleration entry/launcher pairs use externally selected operational source maps. This assignment changes only these two pairs, their existing Node unit/process controls, the new paired admission controls and the two authored manifests. Coordinator, parent, packaging, streamed callers, the captured manifest reader, the frozen independent closure checker and the original five-profile transfer checker remain outside this assignment's write scope. No scientific run or Git publication was performed.

The resume procedure preserved the original B-REM-1 family boundary. This paired transfer is an implementation substep, not closure of the whole family. The [final family integration record](../analysis/option-b-f6c-family-cutover.md) owns successor selection, independent family review and combined validation. Main completed the downstream diagnostic-helper integration and the paired host-process rerun recorded below; whole-family acceptance remains with that owner.

## Pre-edit identity and consumers

The pre-edit inventory used `shasum -a 256`, inert JavaScript export inspection and `rg` for the four paired basenames under `scripts/`, `tests/` and the development-process contract directory. The SHA-256 instrument passed the independent known `abc` vector before target hashing. The checkout anchor is commit `ab3ed93a59e69a5807397c24cefcfcb3a9642487`; it is not a claim that every selected working-tree byte equals that commit. In particular, `git show` hashes the emission entry at that commit as `55a236229a89ba6865871f7e6eed92c61d0e1f7a675b516ec6424ec82f7df889`, different from the measured pre-edit working-tree generation below. Existing staged and unstaged edits were preserved.

| File | Pre-edit SHA-256 |
| --- | --- |
| `scripts/eom/run-f6c-emission-refinement-pilot.mjs` | `efb67f6f0b2aae98b88978be71a80566fe5cc5e13e63e61788d177ae4c7f7d30` |
| `scripts/eom/launch-f6c-emission-refinement-pilot.mjs` | `8a7759d803c49470f471ef4e58171bbe010466aaf0d7abe92f6f9897881bbbb0` |
| `scripts/eom/run-f6c-refined-acceleration-pilot.mjs` | `f491081e3a0fe711d21fa4dcce6f5cfc482f4783768e22df83c55d33a9dfb922` |
| `scripts/eom/launch-f6c-refined-acceleration-pilot.mjs` | `3b85c950cd9be576a0ef41e501736649e6c6837c1e8b993c7791fffabd910e73` |

Exact pre-edit source bytes are retained as ignored local evidence at `.local-data/development-process-review/b-rem-1-paired-baseline/<basename>.source`. These are not execution inputs or generated targets. The identity table above is the durable baseline selector.

The basename search found the emission launcher used as a diagnostic helper by the coordinator, parent and streamed families. It also found scientific preparation, verification and variable-cell historical transport references to these pairs. Current diagnostic successor integration is recorded in the final family owner; historical scientific transport references must not be blindly repinned. Python consumer/reference owners were not edited.

## Role disposition

Each new graph has seven source records and twelve relationships: entry (`admission`), launcher (`launcher`), captured reader (`manifest-reader`), paired Node unit and process controls, prescribed-response helper and circular-root supervisor (four `current-source` records). Exact path-role census is enforced by each entry in addition to the existing reader's schema, graph and byte checks. No Python source, scientific control, historical input, numerical receipt or theorem enters these new operational maps.

| Binding group | Emission disposition | Refined disposition |
| --- | --- | --- |
| Prescribed-response operational helper | Transfer current `fc758e8ffc1feaa566cc8e1849f73a0c8907f5ad7567e43fffd151eeceac30a1` unchanged from the pre-edit entry and launcher | Select the same current helper already selected by the pre-edit launcher; entry's old operational `f178c5d393ca741a0e82aa9865fa796d5901f1751be954183735db1f4a3f6a31` retires only from current operational admission |
| Circular-root operational supervisor | Transfer current `71974054ddce7fc29b8464b9a7a63f8fbb04ee5b425dc997df4d40b2804341aa` unchanged from the pre-edit entry and launcher | Select the same current helper already selected by the pre-edit launcher; entry's old operational `35f00bb0b97a045447f3053ed2705bddceaa62d1ebdd522e9f6eb44943215826` retires only from current operational admission |
| `FIXED` | Preserve all 16 rows exactly | Preserve all 16 rows exactly |
| `HISTORICAL` | Preserve both archive-route source identities exactly | No new route introduced |
| `REFINED` and `PRIOR_OPERATIONS` | Not applicable | Preserve seven refined input rows and both original operational-log rows exactly |
| Remaining `PINS` | Preserve 35 scientific/historical bindings exactly, including execution bridge/support, producer, independent verifier and controls | Preserve 34 scientific/historical bindings exactly, including consumer, independent verifier, comparison core and controls |
| Python bootstraps and inventories; resource limits and claim/census constants | Byte/string and value equality checked against retained pre-edit source | Byte/string and value equality checked against retained pre-edit source |

The retained refined source replay still carries its original helpers as historical evidence. The unit control explicitly verifies both old helper digests in the retained replay, and distinguishes that historical source obligation from the externally selected current helper generation. This does not implement the deferred refined scientific transport route, discharge conflicting historical/current path requirements, or authorize a numerical run.

The preservation check authenticated each retained pre-edit entry using the identity table, loaded its inert exports, removed only `HELPERS` and `OUTER` from the prior `PINS` object and used Node strict deep equality against the new `PINS`. It also compared `FIXED`, `HISTORICAL`, `REFINED`, `PRIOR_OPERATIONS`, both Python strings, `LIMITS`, `CENSUS`, `PURE_FLAGS` and `ANALYSIS`. All comparisons passed. SHA-256 of the resulting `JSON.stringify(PINS)` is `64685e0ddfdd27343406dba16a3494a0e5c757ab0ff4871a040a6cdb97c8cd33` for emission and `9599ff5cdd46375f8d6d4b8ca184a1c16cacc503b0fbc4faec4a9219068efcf7` for refined; these summarize value preservation, not scientific correctness.

The emission Python bridge's current `expected` helper table was inspected with `sed` and `rg` in `scripts/eom/execute-f6c-emission-refinement.py`: it binds the prescribed helper at `fc758e8ffc1feaa566cc8e1849f73a0c8907f5ad7567e43fffd151eeceac30a1` and the circular helper at `71974054ddce7fc29b8464b9a7a63f8fbb04ee5b425dc997df4d40b2804341aa`. Both equal the new emission map selections. It does not pin the emission diagnostic launcher there. No bridge edit or expectation refresh is required for this paired transfer; its twelve-binding operational census remains unchanged.

## Invocation and lifetime

The caller supplies `--source-map-sha256` alongside existing `--plan`, `--plan-sha256`, `--launcher-sha256`, `--entry-sha256`, `--out`, `--python` and `--git-binary`. The map path is fixed per profile; the executable does not discover or accept its own map digest. There is no legacy launch fallback.

The launcher admits the selected graph before importing operational helper bytes, checks the captured entry and launcher against the graph, forwards the same digest to registered stage CLI arguments, and supplies `root` plus `sourceMapSha256` to every file-worker job. Workers use the existing prescribed helper's captured-reader initialization. A captured entry whose executing bytes differ from the selected entry is rejected. Each worker also checks its job selection against its admitted state. The selected map and all seven source bindings join preflight and final source checks. Scientific plan schemas and exact Python operational-list contracts are unchanged; map metadata is an additional operational source closure, not an invented Python scientific field.

Importing the diagnostic helper remains inert. Pure diagnostics need no launch admission. `reviewedHelpers(bytes, expected)` requires an explicit captured helper digest when requested; the diagnostic functions themselves retain their pre-edit algorithms.

## Validation

The final focused command was:

```bash
node --test --test-concurrency=1 tests/option-b-f6c-paired-admission.test.mjs tests/f6c-emission-refinement-pilot.test.js tests/f6c-refined-acceleration-pilot.test.js
```

Measured by Node's test runner: **74 tests passed, zero failed, cancelled or skipped**, in 8,969.567666 ms. This is 61 existing paired controls and 13 new admission controls. The new suite admits a known inert seven-source fixture before current repository targets, then checks omissions, substituted bytes, wrong roles and scope, missing graph coverage, map/reader changes during admission, wrong entry/launcher identity, actual early-reject stage CLI calls, and real captured file-worker selection and executing-byte rejection. The existing unit controls use the shared project venv for their bounded synthetic Python wrappers; no scientific target or retained numerical history is run.

Both updated host-process files pass `node --check`. Main ran their host-process suites under the owned-compute procedure, together with the two serial closure controls. The first run (`bafd6885-416b-4528-ad71-5e9ac45bb65c`) reported 24 passes and two EPIPE fixture failures: `runFixture` was serialized into a child without its new `Admission` import. This assignment added only that missing import to each child harness; production targets and EPIPE expectations remained unchanged. Main selected the revised process-control bytes in the two maps and reran the selection. The log `.local-data/owned-compute/logs/c0f31571-c24a-4dc4-9c88-5ca8a82b5b72.stdout.log`, inspected with `tail`, reports **26 passes, zero failures/skips/cancellations**, including both active-target EPIPE controls and both serial controls, in 22,470.325834 ms. The family record reports supervisor exit zero and closed group at 22.537 seconds. The paired portion is reproduced with:

```bash
node --test --test-concurrency=1 tests/f6c-emission-refinement-pilot-process.test.js tests/f6c-refined-acceleration-pilot-process.test.js
```

The process fixtures now construct their own selected operational maps and pass their digest through launcher, workers and synthetic stages. Their synthetic entry and scientific target are explicitly test-only and do not certify mathematical admission. Focused `git diff --check` passed for the eight edited paired production/control files. No full Content Integrity or repository-wide health result is claimed.

## Integration identities

Production hashes were measured with `shasum -a 256` after focused validation. Map hashes were refreshed by the same instrument after main integrated the EPIPE child-harness fix; earlier test records retain their preceding map selections. Any further source edit invalidates its corresponding selected manifest.

| Artifact | SHA-256 |
| --- | --- |
| Emission entry | `4d0af8529a6f11587dfa5bf5fbe58b460c19cbbcaed30065ff8b454c2be980f8` |
| Emission launcher / shared diagnostic helper | `37fc8aab3e33229878ba1bd50182d7b6eb75b41cdbe171817ece7fcf7b0309e7` |
| Refined entry | `c3b5a8a230ef691996686cd13c84193bf95808bda5a1d51809ffa42640ad0992` |
| Refined launcher | `eb08281ebf7ee7dabe803b0abd66a5d175c9e74bda85bb8aa07750e6869a61bd` |
| [Emission operational map](../contracts/option-b-f6c-emission-operational-sources.jsonld) | `f82ae1b1b99625449167d8a98ece7f2b6face8181b72f38ad349b7f792077dba` |
| [Refined operational map](../contracts/option-b-f6c-refined-operational-sources.jsonld) | `9cd0a38eab35ff81cde21b00129ba8156f9c61523989dbb44b418c39ccc63c8f` |

The frozen closure checker still hashes to `9887632e2900efb1feecf0dfe24ae5f138e46ba7ab870c56eede66a522e84609`; the shared reader still hashes to `9097ef3fd3fb2040b48c601d81c184188e8a992aa4464694860d05d0bf76a56e`. Neither was edited by this assignment.

Claim grade: measured for the scoped byte preservation, admitted operational identities and the 74 selected control results; measured also for the later 26-test main-owned paired/serial process rerun; inferred for suitability within the final family owner’s pending independent review. Falsifier: admission of changed source bytes under the preceding map digest, loss of a frozen scientific identity, an unselected worker/stage reaching execution, or failure of the coordinated host-process controls would overturn the corresponding scoped result. No scientific acceptance, full B-REM-1 completion or publication follows from these checks.
