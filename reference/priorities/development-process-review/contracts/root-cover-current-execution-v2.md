# One-cell root-cover current execution

The Monday-am continuation authorizes restoring the supported one-cell root-cover and cached-root-cover launchers. This contract defines their current operational generation. It grants no full-run or scientific execution approval. The existing one-cell resource plan remains binding: 1,800 seconds inclusive, sequential consumer and independent comparison, 2 GiB sampled aggregate RSS, one-second maximum observation gap, and the existing host-memory, disk, byte and cleanup limits.

## Execution and evidence identities

Each new machine plan uses its profile's `pilot-launch.v2` schema and selects the actual current launcher, entry, registered supervisor, Node, process observer and memory observer by exact bytes. The old `v1` machine plans, original source tuples, mathematical subjects, independent comparisons, and historical receipts remain unchanged. Current entries reject a `v1` plan before dispatch. A new plan records a new execution selection; it does not change what produced an old result.

Source inspection of `validate_launch_contract` in the baseline Python consumer and the comparison's plan-reading path establishes that their closed `comparisonContract` is independent of the outer machine-plan schema. The cached counterparts have the same separation. Their scientific source lists, runtime lists, one-cell bounds and rejection conditions remain enforced. No Python subject or comparison source changes in this migration, and operational controls establish no mathematical result.

## Controls and historical construction tests

Current behavior controls exercise both supported one-cell compositions. The baseline-to-cached source transformation control checks their current operational equivalence with separately selected scientific sources. It is a construction check, not independent scientific evidence.

The unmigrated full-run construction controls require their previous cached-pilot generation. They read hash-checked, nonexecuting source snapshots preserved from `d08cbad4aa8fbbed8a734adfe7affdf15ac4f322`, rather than requiring the current pilot to remain frozen. Their expected transformations and original digests remain unchanged. This preserves their existing historical purpose without making the full-run executable current.

Before admitting the current host utility, verify its actual output against the existing parser and run the threshold, malformed-output, memory-stop and observation-gap controls. Process tests must remain externally deadline-bounded, preserve failures, and check actual child absence. Successful synthetic execution validates process handling only. A numerical pilot requires its own reviewed plan and acceptance evidence; a full run additionally requires actual pilot evidence under its separate contract.

## Review boundary

The pre-edit caller inventory is the [shared-helper audit](../analysis/shared-helper-caller-audit.md) and its complete source/reference census. A live `git grep` of the five changing source identities under `scripts/` and `tests/` finds the cached current-construction test and the full-run historical-construction test as their direct digest consumers. Historical adapter routes already select preserved original pilot bytes and must not advance. New changes to common helpers would require another caller review.

Current machine-plan checks and bounded controls pass within the [recorded operational boundary](../analysis/root-cover-current-migration.md); numerical launch acceptance remains separate. A changed mathematical source digest, accepted mixed-generation plan, failure of preserved-source equality, surviving owned process, or admitted resource violation overturns the corresponding migration claim. Current metadata capture is not a scientific launch or complete host-environment attestation.
