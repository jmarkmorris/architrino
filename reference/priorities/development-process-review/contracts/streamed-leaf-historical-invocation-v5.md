# Streamed-leaf historical invocation, version five

The current streamed caller accepts `braid-program/f6c-streamed-leaf-invocation.v5`. It retains the prior invocation fields and adds the required `historicalEvidence` field. This is a current transport contract; preserved version-four records keep their original schema, source bindings, and bytes.

`historicalEvidence` is either `null`, retaining the adapter's strict direct-file behavior, or a closed object with `selection` and `sourceBindings`. `selection` is the adapter's `braid-program/variable-cell-historical-evidence.v1` object: exact original-to-physical archive routes and explicit unavailable historical environment tuples. `sourceBindings` is the complete physical provenance union the adapter will consume, including its current modules and controls. The invocation does not discover new authority or replace a historical digest with the current file's digest.

Before process admission, the caller validates the finite transport shape, identical archive bytes, distinct `.source` destinations, duplicate/conflicting selections, source-count and aggregate-byte limits, and separation from current executable/runtime declarations. The file worker captures the selected physical sources. The captured Python body forwards `selection` to `open_adapter`, compares the declared and returned physical provenance sets exactly, and rejects unused or omitted sources before the first provider call. The adapter independently enforces its finite historical tuple catalogue, consumed-route checks, unchanged scientific predicates, and final file identities.

Stream metadata records the authored historical selection and the adapter's retained-evidence verification result. Retained scientific bytes and recorded provenance are verified from repository artifacts; current host utilities and runtime packages are execution capabilities, not historical inputs. The frozen codec remains unchanged. Continuation inspection requires version-five metadata to match the original version-five invocation, while its version-four path retains the original version-four checks.

An accepted transport shape does not authorize a scientific launch. Conditional coordinator stdout remains unaccepted and incomplete until external exit and process/lock closure have been observed. The synthetic process controls exercise this distinction; they are not mathematical or whole-history evidence. A new-parent scientific acceptance contract remains a separate prerequisite for consuming new results as scientific evidence.
