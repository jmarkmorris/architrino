# Development-process campaign: rollout and completion checklist

This document tracks remaining work, completed decisions, and supporting recovery context. The explanatory chapters now live under the operational Git documentation:

- [Git-backed knowledge architecture](../../op/git/git-backed-knowledge-architecture.md) — former section 1, including options A/B/C and the deferred migration to B.
- [Git and GitHub operating guide](../../op/git/git-github-operating-guide.md) — former section 2, including credential routing and current versus deferred workflows.

Start with section 3 below. The appendix retains supporting historical context; it is not an additional task list. Detailed migration rows remain beside their subject explanations and are linked from this checklist.

## 3. Rollout and integration plan

This section is the overall migration and rollout hub. The detailed plans below specify the work and evidence needed for each accepted direction. Operational work takes priority over migration to B; no implementation dates have been set. Section 2.2.1 maps the existing guidance. For each change, compare the accepted direction with its live procedure, the executable checks that enforce it, and the explanations read by contributors. A difference in audience is not automatically a contradiction. Record concrete conflicting passages or behavior before proposing a repair.

### Campaign checklist

**Status key used throughout the detailed plans:** ✓ Done = stated scope completed; ◐ Partial = progress made with remaining work named in that row; □ Open/Queued = work remains; ○ Deferred = deliberately not being done now; ↻ Ongoing = a standing method, not another task to finish. Table status is text rather than an interactive checkbox so it remains visible in Markdown tables.

This is the campaign’s completion view. A checked item means only the stated scope is complete, with its evidence linked below. Unchecked items remain partial, next to do, or deliberately deferred; an accepted design is not an implemented feature. Status refreshed September 8, 2026 from the recorded procedure checks, Git/API inspections, and explicitly attributed publication reports.

**Completed**

- [x] Close the credential setup migration for the current workflow. All four dedicated routes have publication evidence; the operator confirmed ninety-day lifetimes for all four and previously reported the five read/write permission selections. Renewal and first-use checks of additional operations are ongoing responsibilities, not unfinished installation. [Closure and evidence limits](#34-credential-migration-and-completion-checks).

- [x] Correct the immediate publication guidance: accumulated-work staging, combined PRs, guarded cleanup, diagnostic permission counts, and the selected GitHub connection policy. [Resolved findings](#312-conflicts-and-integration-work).
- [x] Pause ordinary editing through PR review and rollover; retain scoped review fixes. [Accepted operating decision](../../op/git/git-github-operating-guide.md#29-accepted-directions-and-remaining-implementation-questions).
- [x] Adopt ordinary merge commits and verify a complete merge, cleanup, and successor cycle. [Settings and lifecycle evidence](../../op/git/git-github-operating-guide.md#284-merge-method-ordinary-merge-commits).
- [x] Install the four dedicated PAT contexts and exercise real publication. Architrino includes PR lifecycle evidence; MyLists publication is task/operator-reported, and MyLists PR permissions are untested. [Per-context status and evidence limits](../../op/git/git-github-operating-guide.md#241-agreed-design-one-fine-grained-pat-per-tool-and-repository).
- [x] Correct the circular branch-registry ordering rule. The first branch may carry the new registry’s initial commit; validation and verified push precede rollover completion. The six existing process-conformance tests passed after the guidance edit; a future series transition will exercise this revised rule. [Live bootstrap procedure](../../op/git/pr-lifecycle.md#new-series-registry-bootstrap).

**Partially complete**

- [ ] Finish the remaining integration review. The four-guide documentation layout and immediate contradictions are resolved; future worktree and B integration remain separate decisions. [Findings](#312-conflicts-and-integration-work) and [proposed destinations](#32-proposed-documentation-destination-and-consolidation).

**Next to do**

- [ ] Evaluate Option B’s authorized one-chain pilot, using the [gradual-adoption design](analysis/option-b-gradual-adoption-design.md) and [approval mechanism and proposed first pilot](analysis/option-b-approval-and-pilot-review.md). On September 8, the operator authorized mapping the real moving-single-root scalar-gradient chain and testing controlled changes in isolated copies. Existing production checks remain authoritative; adoption, enforcement transfer, and broader migration require a subsequent decision. The [completed exploratory pilot report](analysis/option-b-real-chain-pilot.md) records eight expected outcomes matched, including changed assumptions, deleted relationships and retained check obligations. The scientific checks do not establish prose or relationship completeness. ◐ Partial — pilot and [source-based dependency-map review](analysis/option-b-dependency-map-review.md) complete. The review identifies two unsupported/misleading edges and missing proof, numerical-coverage, and provenance distinctions. ✓ Done — [corrected candidate and isolated verification](analysis/option-b-corrected-candidate-review.md), with nine reported matching controls and explicit source/coverage limits. ◐ Partial — Option B is preparing the concrete one-chain integration plan from actual consumers, binders, reader/environment requirements, baseline authority and receipt integration points. This preparation is inspection and design only; the next decision is the resulting scoped implementation proposal. Existing enforcement remains authoritative; production integration remains unapproved.


- [ ] Refine the knowledge-integrity requirements and trace one representative equation through dependencies, selected tests, receipts, and publication. [Objectives](../../op/git/git-backed-knowledge-architecture.md#12-proposed-integrity-objectives-to-discuss) and [remaining questions](../../op/git/git-github-operating-guide.md#29-accepted-directions-and-remaining-implementation-questions).
- [ ] Complete the queued [public participation guidance review](../aaa-operations/work-queue.md#ops-026--github-presence-and-community-guidance-review) and [internal reference numbering work](../aaa-operations/work-queue.md#ops-025--internal-reference-section-numbering). These remain separately owned queue items.

**Deferred**

- [ ] Production migration to option B. The fictitious prototype is authorized; the operator is still evaluating the production approach. [Migration plan](#36-migration-to-b--accepted-deferred).

- [ ] Automate coordinated publication across active editors. [Agreed future workflow](../../op/git/git-github-operating-guide.md#261-agreed-future-workflow-coordinated-publication-while-agents-are-active).
- [ ] Evaluate worktrees when the operator is ready; no architecture selected. [Evaluation plan](../../op/git/git-github-operating-guide.md#283-how-we-would-evaluate-a-worktree-workflow).
- [ ] Delegate PR approval and merging after an explicit scope decision and verification. The operator remains the merge gate. [Delegation plan](#33-future-delegation-of-pr-review-and-merge).
- [x] Consolidate the current guides: architecture, operating guidance, PR lifecycle, and issue resolution. Migration checklists remain in this campaign; obsolete procedures were retired. [Proposed documentation destination](#32-proposed-documentation-destination-and-consolidation).

The tables below retain the detailed plan and decision boundaries; use the checklist above to find the current status.

| Detailed plan | Current disposition |
| --- | --- |
| [1.4.4. Migration to B](#36-migration-to-b--accepted-deferred) | Accepted future direction; production implementation not authorized. |
| [2.4.2. Credential migration](#34-credential-migration-and-completion-checks) | Codex–Architrino explicit PAT route verified through publication and cleanup; Codex–MyLists publication reported successful by its task. Claude–Architrino has a reported PAT-backed PR lifecycle with independently inspected merge and cleanup outcomes; Claude–MyLists has reported setup and an operator-reported real push. Explicit command routing, desktop limitations, ownership, and acceptance of the shared-account limitation are recorded. Setup migration closed for the current workflow by operator confirmation on September 8, 2026. Renewal and first-use checks remain ongoing, with the evidence limits retained in section 3.7. |
| [2.6.1.3. Coordinated publication migration](#35-coordinated-publication-migration) | Agreed future workflow; cross-vendor automation remains to be implemented and verified. |
| [2.8.3. Worktree evaluation](../../op/git/git-github-operating-guide.md#283-how-we-would-evaluate-a-worktree-workflow) | Open decision, deferred by the operator. Blanket restrictions remain removed, but neither a shared integration branch nor separate PRs per task has been selected. Revisit when the operator is ready. |
| [2.8.4. Ordinary merge commits](../../op/git/git-github-operating-guide.md#284-merge-method-ordinary-merge-commits) | Settings and effective branch rule verified; first complete ordinary merge, cleanup, and successor rollover verified. |

| Status | Stage | Work and decision boundary |
| --- | --- | --- |
| □ Open — knowledge requirements | Refine requirements from the accepted motivation | Review sections 1.1.1–1.2 and 2.1–2.2, select the needed protections and retrieval obligations, then reassess the provisional scores. |
| □ Open — coverage trace | Establish current coverage | Trace a representative equation through source records, dependencies, actual test selection, validation receipts, and publication. Distinguish observed behavior from intended policy and unverified coverage. |
| ✓ Done — initial owner review; dispositions in 3.1.2 | Review existing owners | Compare the standard Git process, experimental review-time development, startup routing, hooks, and hosting/retention policy against the accepted outcomes. Identify which owner needs each change and which references consume it. |
| □ Queued — separate owner | Review public participation guidance | Complete the definite [GitHub Presence and Community review task](../aaa-operations/work-queue.md#ops-026--github-presence-and-community-guidance-review). Examine contribution guidance and checkpoint examples in their public-contributor context; distinguish them from agent authority in a shared checkout. |
| ○ Deferred | Migrate to B — production decision pending | Follow section 3.6: map existing records, verify JSON-LD/Python/RDFLib, prove one complete chain, integrate publication checks, then expand and retire replaced machinery. |
| ✓ Done — current workflow setup closed | Separate GitHub authorization | Four dedicated contexts are operational with attributed publication evidence and operator-confirmed ninety-day lifetimes. Renewal and first-use checks remain ongoing; see section 3.4. |
| ○ Deferred | Introduce coordinated publication — agreed, deferred | Follow section 2.6.1: establish cross-vendor readiness and pause acknowledgments, one publisher, validated candidate protection, explicit release, and failure recovery. Integrate the protocol into the existing publication owner before routine automation. |
| ○ Deferred — operator still merges | Delegate PR review, merge, and lifecycle completion — planned, not enabled | Follow section 3.3. Decide whether Codex, Claude, or both may perform the operator’s current middle handoff, define the permitted scope and review requirements, and explicitly activate that authority after verification. |
| ✓ Done — first complete lifecycle verified | Adopt ordinary merge commits — first lifecycle verified | The recovery/publication PR preserved the reviewed commits and completed guarded cleanup and rollover. Continue this procedure; agent merge authority remains a separate explicit decision. |
| ○ Deferred — operator decision | Evaluate worktrees — permitted, outcome open | Follow section 2.8.3. Verify task directories, shared runtime state, integration, validation, and cleanup during a bounded trial. Permission to use worktrees is not evidence that every handoff path is reliable. |
| ↻ Ongoing method — not a separate deliverable | Prepare bounded changes | Propose the smallest coherent change addressing the verified gap, with its data model, affected consumers, preservation needs, and validation identified. Integrate accepted rules into existing owners; keep this guide as the explanatory map. |
| ↻ Ongoing method — not a separate deliverable | Validate and adopt | Apply agreed changes within their authorized scope, verify relevant links and behavior, and record the decision and its limits. Changes to executable gates require the checks appropriate to those gates; prose review alone cannot certify them. |
| ↻ Ongoing method — not a separate deliverable | Follow through | Update the existing queue and routing references. Evaluate the adopted practice during actual work and revise it when observed problems contradict its expected benefit. |

The public-community review is queued, not completed. Internal reference numbering has its own [queued rollout task](../aaa-operations/work-queue.md#ops-025--internal-reference-section-numbering), including promotion into the existing reader-facing format. These tasks preserve the separation between exploring an improvement, accepting it, and changing the operational or reader-facing system.

### 3.1. Review of existing Git procedures

Reading the [Git Lifecycle index](../../op/git/README.md), [standard publication procedure](../../op/git/pr-lifecycle.md), [issue-resolution procedure](../../op/git/github-issue-resolution.md), then-current review-time development experiment (subsequently removed; retained in Git history), then-current closed unattended-verification record (subsequently removed; retained in Git history), and [branch-series index](../../op/git/branch-series/registry.md) on September 8, 2026 establishes the following documentation findings. This is a source review, not a trial of the commands, a GitHub settings audit, or a certification that every linked procedure agrees. The row statuses below include the subsequent accepted corrections to those live owners. “Done” applies to the specific correction, not to deferred integration work.

#### 3.1.1. Established practices to preserve

**Standing requirements, not unfinished tasks.** These protections already govern the workflow; preserve them when implementing the open changes.

| Existing practice | Why it belongs in the migration requirements |
| --- | --- |
| Explicit invocation, one designated publication runner, and separate operator merge authority | Keep routine development from triggering publication. Preserve the healthy-path authorization without repeated confirmations; the future coordination protocol supplies editor readiness, not new merge authority. |
| Exact local, remote, and PR-head identities | A successful push message alone is insufficient. Confirm the intended candidate reached the intended branch and is the candidate under review. B must extend this association to network records without weakening it. |
| Final-staged-state validation and truthful check coverage | Preserve receipt invalidation when relevant state changes, supported-environment requirements, and the distinction between local validation, remote checks, and mergeability. |
| Protection of unrelated ambient edits and coherent combined workstreams | Do not require a globally clean checkout or split a coherent PR merely because several agents contributed. Worktree integration still needs a reviewable combined result. |
| Reviewed-head comparison before cleanup | Detect post-review commits and divergence before deleting any branch. Preserve unfinished work; synchronize and verify local main before ordinary rollover. Retain these protections with merge commits and worktrees. |
| Explicit issue-closure evidence | A local edit does not close an issue. Preserve verified merge-based closure, partial-resolution distinctions, and separate authorization for external comments. |
| Committed branch-token registries and successor reservation | Keep unique names and avoid creating a second successor when one already exists. Define how parallel task branches relate to the canonical publication series before automating their allocation. |

#### 3.1.2. Conflicts and integration work

| Status | Finding in the existing owner | Classification and action for the plan |
| --- | --- | --- |
| ○ Deferred — worktrees | The standard lifecycle says to keep one canonical working branch; its rollover assumes the runner can switch the checkout to main. | Integration gap with parallel worktrees. Define a canonical integration/publication branch separately from task branches, verify the runner's directory, and account for branches checked out elsewhere before switching or cleanup. Removing the blanket worktree restriction did not implement this lifecycle. |
| ✓ Done — experiment inactive | The experimental successor procedure assumes squash merging. | Removed from the active workflow by the accepted decision to pause development through PR review and rollover. Its document has been removed; the lifecycle retains protection for any existing successor work. Revisit its integration details only if the operator later chooses to reactivate it. Ordinary-merge settings and guidance are adopted; the first complete lifecycle verification passed. |
| ✓ Done | Permission-prompt bookkeeping. | Resolved in the live procedure, ledger's current operating disposition, and conformance test: retain counts as diagnostics in handoff receipts; remove run qualification and routine ledger-update obligations. Unexpected prompts warrant investigation, not automatic campaign reopening. Git outcomes and validation determine completion; historical closure evidence remains intact. |
| ✓ Done | Accumulated-work publication after editing stops. | Resolved in the publication owner and AGENTS.md: the operator can request all accumulated changes without listing files. The runner checks readiness and exclusions, derives explicit paths from the reviewed inventory, and validates the combined result. The conflicting unscoped-staging example has been removed. |
| ✓ Done | Guarded cleanup after merge. | Resolved in the publication owner: deletion examples reference the complete second-handoff authorization, the fallback is limited to the ancestry condition, and remote cleanup verifies the current remote tip. Scoped cleanliness preserves unrelated edits instead of requiring an empty checkout. The existing operator merge handoff and guarded rollover remain in place. |
| ✓ Done — connection policy only | GitHub access route for issue handling. | Resolved in the issue-handling owner: use local Git and GitHub CLI with verified PAT or OAuth authentication; connectors are excluded from the selected workflow. The connection-policy correction is complete. Credential setup subsequently progressed as recorded in section 2.4.2; remaining verification there is a separate item. |
| ✓ Done | Issue batching says mixed-domain work must be split, while the publication owner allows coherent combined workstreams. | Resolved by operator decision: use a combined PR for accumulated work after editing stops, when the changes are ready, understandable, and validated together. The issue-handling owner now preserves individual issue scope and closure evidence without requiring splits by subject area. Separate publication requires a concrete scope decision, not domain counting. |
| ○ Deferred — future implementations | The future editor pause-and-acknowledgment protocol and JSON-LD records do not yet appear as implemented obligations in the lifecycle. | Deferred integration, not an existing feature. Add the protocol and B coverage only with their implementations and verified failure behavior. |

These findings come from the named procedure passages and, for the unattended wording dependency, inspection of `tests/pr-branch-process-conformance.test.js`. A newer owner revision or a more specific governing clause can change the disposition. Before changing a procedure, re-read it and its consumers rather than applying this table mechanically.

### 3.2. Proposed documentation destination and consolidation

The current division has useful boundaries. The recommended destination is one operational Git entry point with focused live procedures, supported by separate explanations and decision records. The final file layout remains a proposal; no file moves or deletions follow from this review.

| Status | Material | Proposed disposition |
| --- | --- | --- |
| ○ Deferred — consolidation proposal | Git Lifecycle index | Keep as the short navigation home. Link live procedures, supporting explanations, and clearly labeled experiments; avoid copying their rules. |
| ○ Deferred — consolidation proposal | Standard publication and rollover procedure | Keep one canonical lifecycle owner. Integrate accepted coordination, credential routing references, merge method, and worktree behavior here. Consolidate repeated conditions within it before considering a split into smaller files. |
| ○ Deferred — consolidation proposal | Issue resolution | Keep its distinct intake, investigation, and closure responsibilities. Link the canonical lifecycle for publication mechanics and reconcile its routing and batching language. |
| ✓ Done — removed | Experimental review-time development | Removed by operator direction. The PR lifecycle records the decision to pause ordinary development and preserves recovery safeguards for existing work. |
| ✓ Done — removed | Closed unattended-verification record | Removed from the current tree by operator direction. Git retains its history; current diagnostic rules remain in the PR lifecycle procedure. |
| ○ Deferred — consolidation proposal | Branch-series registries | Keep as naming data, not prose merged into the lifecycle. Resolve parallel allocation without duplicating token lists. |
| ○ Deferred — consolidation proposal | This guide's operational explanations | Promote settled explanations into the operational documentation, near the Git index or the relevant setup owner. Retain enough explanation for someone learning the workflow; do not reduce everything to command recipes. |
| ○ Deferred — consolidation proposal | Knowledge-network architecture and option B | Keep its architecture, model, provenance, and retention explanations with the knowledge-system design. The Git lifecycle should reference the implemented publication contract, not absorb the whole theory-development design. |
| ○ Deferred — consolidation proposal | Recovery reports and this migration discussion | Keep evidence and unfinished migration work in the development-process review until resolved. Preserve the final decision record without making incident chronology part of ordinary instructions. |
| ○ Deferred — consolidation proposal | Hooks, validators, generated router, hosting/retention policy, and public contributor guidance | Keep their separate responsibilities. Update relevant consumers when a procedure changes; public participation guidance must not silently become agent execution authority. |

Before consolidation, inventory incoming links, startup routing, tests that inspect document text, and generated fingerprints for each proposed move or rewrite. The conformance test reads the publication procedure by path; the startup generator fingerprints the publication procedure. A filename-only cleanup can therefore break executable consumers. Migrate content to its chosen owner, update those consumers in the same scoped change, verify the new routing and behavior, and remove redundant live instructions only after their replacement is established. Retain a single current source for each rule; use links rather than permanent duplicate copies.


### 3.3. Future delegation of PR review and merge

- [ ] **Transfer the operator’s middle handoff to designated agents when ready.** The operator requested a migration item to allow Codex, Claude, or both to review and approve eligible PRs, merge them, and complete the existing post-merge verification, cleanup, and successor rollover. This records a future delegation decision; no agent gains merge authority from this checklist item.

**○ Deferred — define delegation boundaries.** Before enabling it, define which agents and repositories are eligible, whether authorization covers individual PRs or a bounded recurring class, which changes still require operator judgment, and how delegation can be withdrawn. Distinguish an agent’s substantive review from a GitHub approval event and the permission to merge. Separate tokens belonging to one account do not create independent reviewer identities; verify the actual account, required-review rules, and supported operations without bypassing them.

**○ Deferred — implement delegation.** Integrate the accepted delegation into the canonical publication procedure and its startup references. Preserve review of the exact candidate, required checks and mergeability, reconsideration after new commits, one designated runner, protection of concurrent work, verified merge outcome, and checks for unmerged work before branch cleanup. If an independent reviewer is required, define and verify that role separately from the author and publisher.

**○ Deferred — verify and activate delegation.** Demonstrate the complete delegated lifecycle on a bounded PR under explicit operator authorization, including refusal or escalation when a required check, review, identity, or scope condition is missing. Completion means the operator has accepted the delegation boundaries, the live owners reflect them, the authorized route has been verified, and the final report establishes merge and rollover outcomes. Until then, the existing operator merge gate remains in force.

### 3.4. Credential migration and completion checks

**Setup migration closed for the current workflow on September 8, 2026.** The operator confirmed that all four tokens use ninety-day lifetimes and that each working tool has its own PAT. Earlier token-form reports specify Contents, Issues, Pull requests, Actions, and Workflows read/write, with Metadata read-only and only the assigned repository selected. These settings are operator-reported, not independently enumerated by an API. Publication evidence is retained below, including the task/operator attribution for MyLists. Exact calendar expiry dates remain in GitHub’s token management records; no dates have been inferred here from the ninety-day lifetime. The operator owns renewal and revocation under the operating guide. Additional API write operations remain untested until their next legitimate authorized use; this is ongoing operation, not a claim that every permission has been exercised. The accepted shared-Mac limitation and mandatory own-token rule remain unchanged.

**Completed pilot: Codex on Architrino.** The initial pilot preceded the other tool/repository pairs. Local CLI status inspection identified an OAuth login, and Git configuration inspection identified the macOS Keychain helper. A known-case offline test using dummy credentials and isolated Git configuration demonstrated command-specific helper selection and failure with no helper rather than fallback to saved credentials. This establishes the selection mechanism only, not an installed secure credential route, actual token permissions, or isolation between applications sharing the same macOS account. GitHub CLI documents a process-specific token override. The Codex–Architrino token is now installed; actual Git and CLI operations have succeeded through its explicit route. The shared local OAuth login was removed and the operator revoked its GitHub authorization. These results do not establish hard isolation between applications sharing the same macOS account.

| Status | Step | Work | Completion evidence |
| --- | --- | --- | --- |
| ✓ Done — four-context map in 2.4.3 | 1. Inventory actual channels | Identify each client's local Git helper, API authentication, connector, session environment, and any broader fallback credentials without exposing secrets. | A non-secret map of the four contexts, supported mechanisms, and unresolved access paths. |
| ✓ Done — settings operator-reported; first-use checks ongoing | 2. Define required permissions | List the operations each context needs, including repository reads, ordinary pushes, PR operations, and any workflow-file changes. Check fine-grained PAT support for the actual endpoints. | A reviewed permission list per pair; unsupported operations are explicit rather than covered by an unrestricted token. |
| ✓ Done — explicit routes; shared-account limitation accepted | 3. Prepare secure routing | Establish how each session receives only its assigned authorization for Git and API use. Include worktrees and prevent fallback to an unintended global credential. | Routing demonstrated with a controlled example; secret values remain outside logs and repository files. Any remaining cross-context credential access is documented. |
| ✓ Done — installed; ninety-day lifetimes operator-confirmed | 4. Issue and configure credentials | The operator creates the four scoped authorizations and places them through the agreed secure mechanism. The runner configures the corresponding routes without changing repository destinations. | Each pair has a named context, assigned repository, operator-reported permission selections and ninety-day lifetime, and a working intended route. Consult GitHub’s token entry for the exact calendar expiry when renewing. |
| ✓ Done — current workflow; additional writes checked at first use | 5. Verify scope and behavior | Check identity, repository destination, and granted permissions through each real client context. Verify absence of unintended fallback. Test denied access with a non-mutating authorization check where reliable, or in a disposable test repository; do not attempt a cross-repository write against valuable work. | Expected access succeeds and the intended restrictions are evidenced. A public-repository read is not a valid negative test of write permissions. A dry run does not count as verified publication. |
| ✓ Done — Architrino inspected; MyLists task/operator-reported | 6. Verify a legitimate publication | At the next explicitly authorized publication through each required publishing route, validate, commit, push, and compare the server branch tip with the intended commit. | The exact route completes the intended publication. A route not exercised remains marked pending rather than inferred from another tool's success. |
| ✓ Done — logout inspected; revocation operator-confirmed | 7. Retire superseded access | After replacements work, identify and revoke or disconnect only the superseded authorizations with operator approval. Verify that designated sessions cannot fall back to them. | Retired access no longer serves those contexts, remaining legitimate uses are preserved, and recovery instructions identify the supported current route. |
| ✓ Done — setup closed; renewal and first-use checks ongoing | 8. Close the migration | Update the four status cells above with non-secret evidence references and document renewal and revocation ownership. | All current publishing contexts have evidence with its attribution retained. Unsupported desktop paths are excluded; additional operations are checked at first legitimate use and are not described as tested. |

Closure covers the current supported workflow: dedicated command routes, successful publication, retired superseded access, and assigned renewal ownership. It does not certify unexercised endpoint permissions or hard isolation between applications. Credential boundaries do not replace publication authorization, coordinated editing, or scientific validation.

### 3.5. Coordinated publication migration

| Status | Phase | Work | Evidence needed before advancing |
| --- | --- | --- | --- |
| ◐ Partial — manual coordination observed; automation deferred | Inspect existing coordination | Map live ownership records, task messaging, background writers, receipt scope, and publication/rollover rules. Identify what Codex and Claude can actually read, acknowledge, and enforce. | A concrete account of supported communication and the remaining gaps; no assumed cross-vendor channel. |
| ◐ Partial — manual publication evidence; full protocol trial deferred | Run a supervised checkpoint | Use one designated runner and explicit editor acknowledgments for a bounded real publication when the operator invokes it. Use operator relay only for an identified communication gap. | A coherent candidate, no conflicting staging or writes, verified publication, and explicit release of every participant. |
| ○ Deferred — not implemented | Implement the shared protocol | Integrate requests, acknowledgments, exclusive publisher ownership, cancellation, and release with existing owners and both vendors’ startup guidance. | Known-case checks for missing or stale acknowledgments, a write during validation, competing runners, a runner failure, and uncertain push completion. |
| ○ Deferred — not verified | Verify cross-vendor operation | Exercise the protocol with supported Codex and Claude sessions, including a background writer and an unreachable participant. | Publication fails closed when coordination is incomplete, and recovery leaves neither duplicate publication nor stranded editors. |
| ○ Deferred — not adopted | Adopt and maintain | Update the canonical publication procedure and linked guidance together. Make the coordinated path the routine response to the operator’s publication instruction. | Observed successful use and documented recovery; development pauses remain understandable and proportionate. |

This plan authorizes documenting the future workflow. Building the protocol, changing hooks or permissions, starting a publication, and delegating merge authority are separate implementation or execution steps.

<a id="36-migration-to-b--accepted-deferred"></a>

### 3.6. Migration to B — accepted direction, production deferred

**Status:** production migration remains deferred. The operator authorized fictitious prototypes and comparisons, not production implementation. An agent briefly recorded and dispatched production authorization in error; the operator corrected that interpretation and the instruction was withdrawn. The prototype informs the decision but does not establish production readiness. A remains operational. The phases below are a proposed migration plan, not execution authority.

| Status | Phase | Work | Completion evidence |
| --- | --- | --- | --- |
| ○ Deferred — B migration | Inventory and map | Identify existing records, stable identifiers, pins, receipts, consumers, and retention obligations. Map a selected dependency chain into B's model. | Each selected relationship has an identified source and purpose; existing adequate records are reused. |
| ○ Deferred — B migration | Establish the supported stack | Verify JSON-LD and RDFLib in the supported Python environment; define a local, version-controlled context and the minimum model. | Known-case parsing and queries pass, including offline operation and readable record changes. |
| ○ Deferred — B migration | Implement one complete chain | Connect an equation, assumption, derivation, calculation, result, and actual check record. | Current and historical queries return independently checked answers; retained source content can be retrieved. |
| ○ Deferred — B migration | Integrate review and publication | Connect source-change detection, affected-check selection, and publication receipts to the network records. | A stale source/record association is rejected; valid revisions pass with accurate coverage. Independent scientific checks remain effective. |
| ○ Deferred — B migration | Expand and retire redundant machinery | Migrate bounded groups of current consumers; remove replaced custom representation and query paths once parity of required capabilities is verified. | One authoritative representation per responsibility, preserved historical evidence, and no silent weakening of enforcement. Temporary transition paths have an explicit owner and retirement condition. |
| ○ Deferred — B migration | Close and document | Update canonical guidance, record retrieval and retention procedures, and assess measured maintenance effort. | Agreed migration scope is complete, current consumers use B, and remaining exclusions or optional extensions are stated explicitly. |

The implementation review can refine B's schema and staging, or surface a material blocker requiring a new decision. It must not silently substitute C, broaden into a product for external users, or treat the architectural decision as permission to rewrite historical evidence.

### 3.7. Credential verification evidence

| Tool context | Repository | Credential boundary | Migration status |
| --- | --- | --- | --- |
| Codex working on Architrino | Architrino | Its own unique, repository-scoped, independently revocable authorization | Dedicated Keychain PAT and explicit client route verified by API reads, real pushes, PR creation and inspection, and guarded remote branch cleanup. The explicit route is the adopted command path; additional permissions are checked at first legitimate use. |
| Claude working on Architrino | Architrino | Its own unique, repository-scoped, independently revocable authorization | Operator reports PAT created and saved. Claude Code Local reports Mac Git and CLI available. Shared helper extended and compiled with a distinct Claude service; offline selection and rejection tests pass. Operator reports hidden-prompt installation succeeded. Host-run identity and repository API queries through the explicit Claude context authenticated as the intended owner and selected Architrino. Claude Code Local subsequently reports both identity and repository queries succeeded through the explicit Claude context. Authenticated API reads are verified. The preserved Codex default also passed a post-update identity query after operator Keychain approval. Claude runner reports its first validated commit and push through this context at `170f0c63934c6563290fe135d75c522dab34271f`, with matching server tip and tree; local HEAD independently matches that commit. Git push access is exercised. Claude subsequently reports PR creation, check monitoring, merge verification, and guarded cleanup through the same context for [the F5 recovery and credential-route PR](https://github.com/jmarkmorris/architrino/pull/262). Independent Git/API inspection confirmed merged state, the reviewed head as the merge commit’s second parent, synchronized main, and retirement of Tanzanite. Credential attribution for those lifecycle operations is runner-reported. Explicit command routing is adopted; desktop PR-status controls are outside the supported authenticated path. |
| Codex working on MyLists | MyLists | Its own unique, repository-scoped, independently revocable authorization | MyLists task reports a successful dedicated-PAT test push, credential-free remote, ordinary Git routing, and a separate CLI route without shared-login fallback. Operator confirmed the older exposed token was revoked. |
| Claude working on MyLists | MyLists | Its own unique, repository-scoped, independently revocable authorization | Operator reports token installation completed. Claude Code Local reports dedicated Git and CLI wrappers, authenticated identity and private MyLists access, synthetic no-fallback checks, and preservation of Codex routing. The operator subsequently reports a successful real Claude commit and push; this task has not independently inspected that push or its commit identity. PR operations remain untested and are not required for the operator’s usual MyLists commit-and-push workflow. |

**Verification on September 8, 2026:** fresh `gh api user --jq .login` calls through all four routes returned the intended account with exit zero. The existing synthetic test `node --test tests/github-auth-routing.test.js` passed for Architrino's default and Claude contexts, including helper failure without fallback. Source inspection of MyLists' local wrappers confirmed explicit Claude selection and preserved Codex routing; this review did not rerun their synthetic fixture.

Both Architrino contexts returned HTTP 404 for a read of the private MyLists repository, while both MyLists contexts returned that exact repository with `private: true`. Together these checks establish denial of that specific private read through the Architrino tokens, not a universal authorization boundary. MyLists tokens can read public Architrino without proving write permission there. No cross-repository write was attempted. All four checks ran from this Codex process: the result demonstrates route availability, and also confirms these routes do not isolate credentials from another authorized process under the same Mac account.

| Required capability | Evidence and remaining limit |
| --- | --- |
| Identity and intended repository access | Fresh identity checks on all four routes; MyLists private access and Architrino-to-MyLists denial verified as above. |
| Contents write | Existing real publication evidence in section 2.4.1; MyLists publication remains task/operator-reported. No new commit or push was performed for this audit. |
| Architrino PR creation and cleanup | Existing lifecycle evidence in section 2.4.1. Operator merge authority is unchanged. |
| MyLists PR operations | Not part of the operator's routine workflow; verify when first required rather than manufacturing a PR. |
| Issues write, Actions management, workflow-file write | Operator-reported token form selections exist, but these specific write capabilities have not all been exercised per context. A successful push or public read does not verify them. The operator reported the selected settings; verify each capability during its next legitimate authorized use. |
| Isolation between local tools | Operator accepted the shared-Mac limitation on September 8, 2026. Separate PATs provide independent revocation and GitHub-side scope; local process isolation is not implemented. The mandatory caller rule prohibits requesting another tool’s token. |

GitHub's [fine-grained permission reference](https://docs.github.com/en/rest/authentication/permissions-required-for-fine-grained-personal-access-tokens) explains that the accepted-permissions response header describes an endpoint's requirements; it is not a listing of all permissions granted to a token. Public endpoints can also succeed without those permissions. Do not use either as a substitute for the missing write evidence.

**Closure boundary:** setup is closed on operator-confirmed settings and ninety-day lifetimes plus the recorded routing/publication evidence. Exact calendar dates were not independently inspected; consult the token management entries during renewal. Additional writes remain untested until exercised during legitimate work. Desktop controls remain outside the supported authenticated path, and the accepted shared-account limitation remains subject to the mandatory own-token rule.


### 3.8. Ordinary-merge adoption evidence

Settings and publication guidance are adopted. The live publication owner requires verification of the actual merge commit, its reviewed-head parent, and ancestry in synchronized main before guarded cleanup. The first complete cycle was verified on September 8, 2026 in [the recovery and publication PR](https://github.com/jmarkmorris/architrino/pull/261): Git inspection verified two merge parents with the exact reviewed head as the second parent, synchronized main, guarded retirement of Peridot, and publication of Tanzanite. The effective protect-main rule initially allowed squash only; the operator corrected it to allow merge, and the branch-rules API verified that correction. Repository-level merge settings and effective branch rules both require inspection. The continuous-development experiment remains inactive. The operator performed the merge; agent merge authority remains a separate future decision.

## 4. Appendix — Background on checks, Git history, and provenance

### 4.1. Why add checks when Git already keeps history?

The immediate motivation for a dependency pin is to prevent silent substitution during current work. Git answers “what was committed at that point?” A pin-enforcing consumer answers “may I run using these particular inputs?” These are different questions. If a calculation was assessed against one mathematical specification, silently changing its input specification could leave the old assessment attached to a different calculation. Refusing changed input can expose that inconsistency before a result is accepted.

For this concrete family, `git log --reverse -S test_frozen_mathematical_dependencies_remain_identical -- tests/test_eom_continuous_reception_roots.py` locates the test's first appearance in retained history at commit `0fb575921`. Reading that commit's test with `git show` establishes that it required exact identities for two helper modules and the enclosure theorem document. Reading the oracle's opening docstring at the same commit establishes that it described an unchanged reference generation and identified the theorem supporting it. These are measured statements about the retained source. The inferred design motivation is to keep that checker tied to a particular mathematical dependency set; the source does not establish the author's full deliberation, the operator's approval of the strategy, or why the practice later expanded. An earlier retained implementation or an explicit design discussion with a different purpose would revise this account.

Dependency pinning is an established practice for selecting external software. For example, GitHub recommends immutable commit identities for third-party Actions in its [secure-use guidance](https://docs.github.com/en/actions/reference/security/secure-use#using-third-party-actions). That addresses code selection and supply-chain risk. It does not establish that whole-file pins on evolving internal theory documents are a standard default, or that our chain of such checks is the right design.

For ordinary software tests, a contrasting established principle is to test required behavior without unnecessarily coupling the test to implementation details. Google's [testing guidance on behavior and implementation](https://testing.googleblog.com/2013/08/testing-on-toilet-test-behavior-not.html) explains why an implementation change that preserves behavior should generally leave behavioral expectations intact, while recognizing exceptions. A file-identity check can be a legitimate additional control when exact identity is itself the requirement. Its failure is not evidence that behavior changed.

The local tradeoff follows directly. A whole-file pin detects an equation change, but also a comma change, and it cannot distinguish them. It may be useful to guard a bounded comparison that deliberately holds its reference fixed. Applying that same restriction to continually evolving theory documents can create maintenance that Git history and ordinary review would avoid. The existence of a legitimate use does not establish that every use in this repository is justified.

For each binding, establish the selected input, the dependent claim, the event that runs the check, and the response to a mismatch. Then judge whether fixed identity protects a requirement that ordinary history, dependency review, or behavioral testing does not adequately cover. This evaluates a concrete mechanism against the motivation without requiring a complete reconstruction of every past design decision.

### 4.2. How Git history and scientific integrity became connected here

The recovery history illustrates why identity and meaning need separate treatment. Provenance connects a statement or result to its source version, derivation, assumptions, and relevant review or calculation. That connection helps determine what remains useful after a theory revision; it does not require the earlier version to remain current. The evidence below concerns how a particular binding behaved, rather than the drafting history of this guide.

The relationship can be understood in layers. Git retains committed versions so an earlier equation can be retrieved. Review examines whether an equation change is intentional and justified. A dependency check can alert a calculation that a selected supporting file no longer matches the version it expected. A validation receipt can connect checks to the candidate being published. These functions can cooperate, but each additional function needs to address a gap that matters to current work. They are not all alternative ways of storing the same history.

For the particular enclosure family, the available history supports this limited sequence:

| Stage | What the retained evidence establishes | What it does not establish |
| --- | --- | --- |
| A checker was connected to exact mathematical inputs | The original test and docstring inspected with `git show`, as described above, identify the theorem and helper versions selected by that checker. | The complete discussion that led to the design, or a general operator decision to freeze evolving documents. |
| The theorem document changed while an expectation stayed old | The [cross-report assessment](analysis/cross-report-findings.md#independently-inspected-findings) records `git log --follow`, source digest comparisons, and `git diff` attributing one mismatch to terminology replacements. | That the mathematical content became wrong, or that every later failure had this same cause. |
| Repairs encountered several kinds of binding | The [source recovery analysis](analysis/source-recovery-and-binding-repair.md) records source reconstruction and dependency comparisons distinguishing current execution selections from historical evidence identities. | That all pin updates were appropriate, or that every original source state had been committed. |
| The present review separates purpose from implementation | This guide distinguishes source history, equation integrity, dependency selection, and checked publication state. | A completed redesign, approval to add more controls, or authorization to remove existing evidence. |

One plausible explanation is that a useful local precaution grew into a more elaborate dependency system as additional consumers were connected. That remains an inference, not a recovered design history. The current evidence shows a specific fixed-input check and later repair interactions; it does not yet show every decision by which the wider system developed. Establishing that broader chronology would require a separately scoped investigation of actual changes and decision records.

The implementation must support two outcomes together: a revised equation must not silently inherit unsupported confidence from its predecessor, and a deliberate improvement must remain practical to make. An exact-content alarm can contribute to the first outcome but cannot classify the change or guarantee the second. The alternatives must therefore be compared as working arrangements, including their review and maintenance requirements.

### 4.3. Optional calculation execution with DVC

DVC remains a possible execution layer for calculation families under A, B, or C. Its [pipeline model](https://doc.dvc.org/user-guide/pipelines/defining-pipelines) describes executable stages, declared file dependencies, and outputs as an acyclic graph. That is narrower than the changing network of equations, terminology, assumptions, derivations, and unresolved ideas. DVC can manage declared reruns but does not provide that conceptual model by installation.


A **pipeline** describes a calculation as connected steps: prepare inputs, run a calculation, check its output, and produce a report. A **stage** is one such step. A **dependency graph** records which stages rely on which inputs or earlier outputs. These names describe the workflow, not a new scientific method.

DVC stores stage definitions and records the state of their declared dependencies. A **lock file** records particular commands, parameters, and content identities for that state; “lock” here does not mean that it prevents another agent from editing files. A **cache** stores reusable content or previous stage results. Retrieving a cached result means reusing an earlier output; it is not evidence that the calculation was freshly executed. The official [pipeline execution guide](https://doc.dvc.org/user-guide/pipelines/running-pipelines) explains changed-stage selection and reuse.

In our example, if the theory document is explicitly declared as an input to a stage, a comma edit can still make that stage out of date. The tool can manage downstream consequences, but it still does not know that the comma is irrelevant to the mathematics. If the document is not declared as a dependency, editing it will not by itself alert that stage. Choosing complete and useful dependencies remains an engineering responsibility.

For an equation change that affects the calculation, the relevant implementation and checks must be updated through review. A pipeline can then identify affected declared steps and organize execution or reuse. It cannot derive the changed equation, determine whether a verifier is independent, or ensure that the declared dependency list is complete. A successful run means the selected commands completed under their checks, not that the theory is proved.

An **execution environment** includes the interpreter or compiler, libraries, and operating-system capabilities needed to run the steps. Recording a pipeline does not supply missing software or reproduce every host characteristic. Likewise, declaring stored data does not guarantee permanent availability: storage and retention remain explicit responsibilities.

The decision about adding DVC to any arrangement is whether managing connected calculations is difficult enough to justify this additional tool. It can be useful for repeatable current workflows even when retaining every past result is unnecessary. It does not require turning ordinary theory prose into a permanently frozen dependency, and adopting it solely because it is capable of extensive tracking would repeat the mistake of choosing machinery before its purpose.
