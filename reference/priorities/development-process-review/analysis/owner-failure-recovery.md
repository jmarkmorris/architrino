# Owner-specific failure recovery

This September 8 pass implements the operational repairs supported by the actual failing paths and separates them from unavailable original inputs, stale generated artifacts, and unresolved scientific review. The user authorized remaining items one through eight. This record covers item six; F5, circular, refined, process supervision, and final-candidate GitHub validation remain with their separate owners.

## Current status after publication

Legend: ✓ Done — the stated bounded task is complete; ◐ Partial — useful work is complete and the stated condition remains; ○ Not done — execution has not occurred. **Blocked** names a required input, owner decision, or authorization; **Deferred** identifies work assigned to another owner. Historical measurements below retain their original dates and are not silently treated as current test results.

The resumed owned selection `1d2ea474-a6f6-4e9c-89a4-a6bceee023df` reports 31 tests, 27 passes and four failures in 0.759 seconds, with process-group closure: Archie response/deployment fixture identity and two source-index assertions. The corrected source-hash negative now builds and validates a current baseline before changing only its source hash, so generated reading-copy drift cannot mask the intended rejection. Run `392dd281-7f69-4a4f-b015-9d64d1401327` passes its two selected source/authority controls in 0.239 seconds; the independent fixture freshness assertion remains. A separate Borg selection `f3a325c8-232a-4d93-ae3d-4795e2e41eb6` reports 64 tests, 61 passes and three failures in 4.856 seconds, with closure: the master binding and two active-finding library assertions. These subsets do not establish overall health.

✓ Done — The exact connected regeneration proposal is reviewable in [repair proposal](../evidence/owner-failure-recovery/resumed-owner-review/repair-proposal.md), with byte-binding inventory, proposed Archie response differences, all 28 Borg adjudication-row comparisons, and the PDG catalog inventory. ◐ Partial — **Blocked:** execution of the listed generated repairs awaits explicit generated-drift authority; no generator write was performed in this resumed batch.

## Resumed authored deployment and actual SDK HTTP repair

**✓ Done — Authored candidate identity and loopback acceptance.** Full-corpus check-only run `e1c4729d-362e-4904-b9c9-9e22113457ba` verifies the stored snapshot against its current canonical inputs in 5.854 seconds, with exit zero and group closure. The authored deployment fixture changes only its three candidate identity fields to snapshot `source_snapshot_full_corpus_v1_96a7ddf5ba18a44daf43`, digest `fd75267ad9c80fb85c78d1868704de409fa2ccd4fbbf5393479adbdcada03e82`, and its matching local source-state ref. The diff preserves every permission, remote-readiness, accepted-main, authentication, rate and rollback field. Owned run `86f9094a-0ebb-4b51-89d9-a6124c4cc1f4` passes all 12 deployment/HTTP tests with no skips and group closure in 2.163 seconds.

**✓ Done — Actual SDK HTTP selection.** After identity repair, run `7ceddcc8-ea2e-412a-adc1-5b93b68cc453` reaches the server and rejects with HTTP 429 rather than the previous snapshot mismatch. The checker emitted its initialization/notification/GET probe and tools too quickly for the unchanged ten-request burst allowance. Its client calls now wait at least 1100 ms between semantic operations, also respecting the declared per-minute interval; the server clock, limits, limiter, protocol and query implementation are unchanged. The real SDK 1.30.0 rerun `71f1372c-0c87-44e8-abb0-098b8ee17b92` passes all five tools, missing-read error, ping, readiness and safe-log checks in 10.586 seconds with group closure. This checks paced SDK interoperability, not overload throughput or remote deployment. The existing rate-limit and concurrency negatives remain among the 12 passing controls. A failed named SDK assertion or altered server limit would overturn this scoped result.

Native stdout remains under `.local-data/owned-compute/logs/` named by these run IDs. `shasum -a 256` records SDK stdout `99cc8f6ed85042e699c42a10267041527be66ec755738862050e3285e877fb5e` and deployment test stdout `04e4ccb41b36fa6cf28c0b40110a072f5fb4fe4e9df4f6fa992a85480ef88019`. No new machine files were added to the collection whose retention exception covers its existing census.

**◐ Partial — Generated fixture/projection work remains.** The fresh six-file owner selection `aef76488-d4c9-4407-9418-bfad7d75b1a2` measured 42 tests, 36 passes and six failures before the authored repair. Its other failures concern generated Archie responses/source-index and Borg active findings. Their proposed regeneration remains explicitly pending; the passing authored repair does not disposition those failures.

## Historical reproduced scope and completed repairs

The owned Node selection `0239a2f9-b3ec-459c-af66-528d936aeb8f` ran fourteen selected Archie, Borg, diagnostic and renderer files serially and reported 111 tests, 91 passes, twenty failures, and zero skips or cancellations. Its terminal record reports exit one and process-group closure. The [original output](../evidence/owner-failure-recovery/baseline.stdout.log) preserves every failure; the [terminal collection](../evidence/owner-failure-recovery/runs.json) records commands, times and log hashes. These are measured results for that selection, not a complete test baseline. An earlier sandbox launch was refused with `spawn EPERM` before target spawn; the actual reproduction used the same supervisor outside that restriction.

### ✓ Done — Diagnostic input identities

Both F6b and F6c constructed exact source records without the identity pair required by `validateExactPrescribedSourceRecord`. F6b therefore threw before evaluation; F6c caught the same input rejection for each row and returned an empty best-row list. The shared [diagnostic source identity helper](../../../../scripts/mapping-electromagnetism/diagnostic-source-identity.mjs) now hashes the canonical declared source payload: schema, engine, normalized field speed, history and the entire ordered trajectory/source collection. It retains the preimage and the consistent assembly/hash pair. Display labels do not determine the identity, and changed history or polarity changes it. The identity labels these diagnostic paths; it does not admit a catalog assembly, certify the prescribed geometry, or establish that it solves the evolution equation.

Before applying the helper to the targets, run `d2da367a-00a0-4dc9-9790-ca552c21182a` passed its separately literalized canonical preimage and content-change controls. The actual unchanged F6b/F6c regressions then passed with that helper in run `34f5e730-7328-4a12-924b-8b58d44f46f5`: three tests, zero failures, exit zero and closed group. The [output](../evidence/owner-failure-recovery/diagnostics.stdout.log) records the result. Numerical evaluator, independent references, recorded scientific outputs and regression thresholds remain unchanged. Falsifier: either unchanged diagnostic regression fails with the repaired inputs, or a changed declared path can retain the same preimage/digest.

### ✓ Done — Borg geometry and summary delivery

The geometry failures came from comparing quantized display-record coordinates at a tighter tolerance than their declared grid. `canonicalizePrescribedBraidRecord` rounds each initial cubic coefficient to the nearest multiple of `positionQuantum = 2e-11`. At a segment's initial endpoint, each coordinate error is at most half that quantum. By the triangle inequality, one pair distance can move by at most `sqrt(3) * positionQuantum`; comparison of two recorded pair distances requires twice that bound. The repaired assertions use those independently derived bounds and retain the exact tetrahedral length and all five independently known vertex/edge/degree inventories. They also require the recorded quantum to equal `2e-11`, so a coarser generator cannot silently weaken the test. No geometry record, generator or playback implementation changed.

The home-entry failure was a source-text assertion requiring an obsolete inline call. Live `bootBorgApp` already computes the summary in the selected-record branch and supplies it through the `librarySummary` variable. Its test now calls the actual boot path and checks the selected assembly and model revision delivered to `mountApp`, retaining the existing renderer assertions. Run `7a210014-9d8b-4587-a550-a28e0bb54402` passes the initial eleven geometry/home tests; see [output](../evidence/owner-failure-recovery/borg.stdout.log). Review then added a negative control that displaces one actual tetrahedral endpoint by `1e-6` along its edge and verifies that the same bound rejects it. The tests explicitly check an initial segment boundary at zero and unit-scale vertex coordinates; the additional `1e-14` allowance covers binary64 subtraction/norm arithmetic in this scope, not arbitrary-scale or interior interpolation errors. Final run `0a4403cd-2283-4119-b152-199dd7eceb75` passes all twelve tests with zero skips, exit zero and closed group; see [final output](../evidence/owner-failure-recovery/borg-final.stdout.log). Falsifier: the deliberately distorted geometry passes the bound or the boot path delivers a different selected identity.

### ✓ Done — Original fold-certificate input recovery

The unchanged fold suite requires master-equation bytes with SHA-256 `0e8219c7a3cc750747c43030d5b998ddbf2de525a5469d32fa5e52fbae5ec63f`. `git show bfbb3ea3e7175e70e6d0707fef9b0a200f9fe845:content/markdown/aaa/dynamics/master-equation.md | shasum -a 256` returns that exact digest. The [retained driver](../evidence/owner-failure-recovery/run-original-fold-regression.mjs) first passes a known SHA control, authenticates all four declared original inputs, copies the unchanged suite/protocol/certificate/receipt into a disposable tree, and runs it using the shared venv. Run `74f235aa-7e24-45e9-8398-00da06491b70` passes all seven tests in 4.144 seconds by native unittest, including receipt-byte reproduction, then removes the disposable tree. Its [input record](../evidence/owner-failure-recovery/original-fold.stdout.log) and [unittest output](../evidence/owner-failure-recovery/original-fold.stderr.log) establish original-input availability and reproducibility only. The live checkout's historical assertion still expects different master-equation bytes; this does not make that ordinary live selection pass or establish independent scientific correctness from reproduction.

## Remaining family dispositions

| Status | Family | Actual obligation and measured finding | Remaining work |
| --- | --- | --- | --- |
| ◐ Partial — impact review complete; separate calibration composition retained | Analytical coverage | [Statement-level review](semantic-closeout-review.md) reviews all 29 measures and repairs dated current admission. The four-file run passes 26/27; its remaining frozen calibration composition mismatch is identical at start-state HEAD. | Calibration/Braid Program owns the uninstantiated receipt/implementation prerequisite. Preserve its frozen expectation until that calibration is selected; no numerical campaign is rerun. |
| ○ Not done — Deferred for authored scene review | Markdown heading icons | Check-only audit reports authored scene override/heading mismatches and index badge mismatches; [output](../evidence/owner-failure-recovery/standalone-5.stdout.log). | Review each authored scene against its current rendered heading structure before fixing the override or badge. The audit's text parser is not a substitute for that review. |


The check-only [standalone rerun](../evidence/owner-failure-recovery/standalone-results.json) executes all eight selected previously failed commands and records eight failures, each with exit one and a closed group. This is the original bounded observation, not a new post-publication standalone rerun; earlier pass counts are not subtracted or treated as a final combined certificate. Generated writes were not authorized in this delegated batch and were not performed. Source changes, reference-surface drift and receipt maintenance remain visible to the final publication owner.

Independent review of the resumed HTTP diff confirms that the production limiter retains its actual 1000-ms burst and 60000-ms principal windows, every existing SDK assertion remains, and the fixture diff contains only the three reviewed identity fields. The accepted claim is compatibility of this actual SDK/contract selection; arbitrary stricter future rate contracts require their own review. Standalone deployment validation `8af88854-8275-4e9c-b44a-05bdbf5ee854` also passes its positive and named negative cases with group closure.

## Completed historical execution selection

The [canonical F5/fold selectors](final-test-coverage.md) now have recorded passes: F5 run `5f3dcba7-9751-424c-a77c-7f48736cae7c` executes 41 Node and 32 Python tests; fold run `596c9a81-f88b-45fe-b06f-70251cac2895` executes seven Python tests. Original inputs are authenticated before and after the F5 selection. These completed substeps leave the remaining-family table; the ordinary discovery failures remain historical evidence, and final selected-candidate validation remains open.

## Authorized mechanical closeout checkpoint — September 8

**✓ Done — G1–G4 selected consistency repairs.** The operator-delegated coordinator explicitly selected the generators and narrow manual Borg maintenance in the reviewed proposal. The upstream integrity run `a8d86d52-8a17-4445-a161-4e555f3d3a31` closed exit one in 32.220 seconds: 33 required checks passed, only generated startup orientation failed, and the optional broad sweep was skipped. The unrelated router remains with the process owner; this batch does not claim upstream publishability.

The source fixture changes exactly its reading-copy digest and enclosing snapshot hash by `git diff`. Generator/check runs `87fc0205-7627-419f-8f7d-4b6f211a5a63` and `b36746f5-2f58-49ae-8f06-589088f82e16` pass. Tool generation/check `4217651b-9bb8-4929-a13b-f2942289d9c3` / `da3f47a0-af6a-4d43-94a5-afb41a2640e2` and stdio generation/check `3618e466-e069-4937-8ae3-77d6d644d1ae` / `53595cb3-c4d5-4864-b03e-79ec384c0f39` pass with closed groups. A recursive JSON difference instrument passed a known nested-change case first, then confirms the tool fixture's 18 changed leaves match the reviewed snapshot/ontology/teaser paths; stdio changes only the equivalent structured responses and their serialized text. Requests, access scopes, limits, ranking/privacy/pagination controls and the already-correct deployment fixture remain unchanged. Same-engine fixture agreement supplies deterministic contract consistency, not an independent scientific oracle.

The complete selected seven-file source/full-corpus/tool/stdio/HTTP/remote-contract/secure-tunnel regression run `39a45411-f6e4-493c-b05b-7b31bc25a889` passes all 41 tests, without skips or cancellations, and closes in 16.732 seconds. This is the declared Archie selection, not all repository tests or remote deployment.

Borg owner hashing matches the previously reviewed `9c68329d…` source before the change; all 28 adjudication rows are present after the one equivalent evidence-link correction. The manual projection change preserves original serialization and changes exactly two lines: owner digest and that source-row link. Checker `76459e2b-5ab0-4f1a-a63e-fbf8854267a9` passes; library/scientific-status run `f7d5dccd-8483-4636-a750-f1df30d7818f` passes 20 tests without skips/cancellations and closes in 2.887 seconds. The 25 current exact bindings, one retained off-catalog row, three H4 measured passes and zero H5 passes remain. G10's distinct semantic binding is not advanced.

**✓ Done — G5 catalog generation and canonical sample controls.** The shared venv executes Python 3.13.2. Glyph generation `18200470-d0a5-46ca-a5c8-6c603cc45ae7` closes successfully in 17.475 seconds. It adds the six previously missing catalog SVGs and refreshes two existing Noether-pair SVGs to the current catalog label “Pro/anti-orientation Noether-braid composite”; the tile also uses the current three label lines and positions. The authored catalogs and renderer are unchanged. Generator/renderer run `41b14a4d-b0ae-4b95-8d26-e61e042a8dc0` passes four tests, including exact 147-file catalog inventory and eight canonical regenerated samples, without skips/cancellations, closing in 18.177 seconds. The shared renderer limits parity to catalog/reproducibility evidence.

Native stdout/stderr remain in ignored `.local-data/owned-compute/logs/` under these run IDs. No new tracked machine evidence collection was created. A failed named control, a different source/request/scientific-grade diff, or a nonclosed owned group would overturn the corresponding bounded completion. Final stable-state reconciliation and G9/G10 semantic review remain open.

The existing browser exporter run `f3c8547c-f69a-4d7d-9103-99f3a8c11050` closes successfully and produces the ignored `.local-data/recovery-mechanical-20260908/glyph-review.png`. Visual inspection of all eight affected SVGs at the displayed 200-pixel sample size finds readable labels, complete borders and the intended opposing glyph orientations; this is a bounded rendering observation, not scientific validation or exhaustive screen-size QA. The two existing asset changes match the current catalog fields by source inspection.

## Semantic and freshness closeout checkpoint

The [statement-level review](semantic-closeout-review.md) records every one of the 29 methodology measures, every Master EOM binding statement, independent algebra, source transitions and C1–C3 supported-interface boundaries. G9 current admission is reviewed and repaired; its four-file selection passes 26/27, retaining a separately frozen uninstantiated-calibration mismatch already present at batch start. G10 semantic synchronization and 262 Borg/causal-wake controls pass. G8 remains blocked on explicit historical-versus-current provenance mapping; raw evidence and pre-batch receipt bytes are preserved. These replace the older hash-only blocker descriptions without claiming universal health.

**✓ Done — G10:** source-statement review, 262 Borg/causal-wake controls, all 34 required content checks and strict scene validation pass in the [semantic record](semantic-closeout-review.md). G7 local navigation write/check also passes. G8 remains a provenance-mapping obligation, not a routine hash refresh.

**✓ Done — G8 projection resolution:** the operator-selected [mapping review](semantic-closeout-review.md#g8-receipt-projection-resolution) now resolves historical/current receipt semantics; all four receipt generation checks and 16 affected Node tests pass. This supersedes the earlier G8 mapping blocker, preserving raw evidence, numerical fields and original scientific boundaries.
