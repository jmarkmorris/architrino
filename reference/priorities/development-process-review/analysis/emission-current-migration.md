# Current emission execution transport

The current emission entrypoint now uses an explicit version-two plan and a captured Python bridge. The bridge routes the original declaration and two theorem records to exact retained data archives, records current operational dependencies, and authenticates the original prior-cover generation. The original emission producer, independent verifier, and numerical comparison implementation remain unchanged. The refined verifier's historical transport API also remains unchanged.

The bridge delegates query proposal and cover construction to the original producer, and original mapping, stream parsing, query replay, restriction checks and conditional cover predicates to the independent comparator. Current execution bindings are preserved in the emitted manifest and report. Historical data paths are represented by their original logical identities; executable archives are never imported. This distinction does not authorize replaying a historical host binary under a current identity.

Input captures and loaded modules remain held through publication. Completion is emitted after capture/module cleanup, with the deadline still active. The initial metadata early-return finding is repaired: metadata completion now traverses the same cleanup and final deadline boundary. Output publication is retracted on a later failure. The bridge caps collected scientific/source data at 64 MiB and separately permits larger non-collected runtime bindings.

## Measured boundary

The [current plan inventory](../evidence/emission-current-migration/prepare-current-plan.mjs) first checks the independent SHA-256 `abc` answer, then captures actual source/runtime identities. Its final owned run `887b58a0-e146-4b7d-92d9-0f8cc90f0828` validates 204 distinct bindings and both isolated Python metadata stages, each reporting zero scientific calls; the supervisor records exit 0 and process-group closure. The [plan](../evidence/emission-current-migration/emission-launch.v2.json) and [validation](../evidence/emission-current-migration/current-plan-validation.json) retain the exact current census.

Shared-venv unittest discovery for `test_f6c_emission_current_execution.py` passes 11 controls in owned run `aad104a3-7e3d-431a-b2a2-194df094f8f3`, exit 0 with process-group closure. Ten independent controls cover plan routing, exact operational census, authentication order, scientific-scope rejection, original stream forwarding and negative comparator results. The additional lifecycle test keeps captures, original prior authentication, history construction, manifest construction and output writers real while substituting explicitly artificial numerical callbacks. It exercises both publication stages and retracts a comparison after an injected post-write failure. An initial test-layout failure in run `9509f745-92a8-401d-a7ff-df1c4d5307d1` was corrected to use the unchanged verifier's required sibling output directory; the production layout predicate was preserved.

The final serialized Node entry/process selection passes 42 tests in owned run `5ebc97df-cb44-49df-b6d6-43a5f6c0a482`, exit 0 with process-group closure. These are transport controls and metadata authentication, not acceptance of a new numerical campaign. A failed listed control, changed bound bytes, or an unclosed owned process group overturns the corresponding bounded claim.

## Remaining execution boundary

A complete emission numerical pilot still requires the ordinary external resource admission, full producer/comparator computation, and inclusive process closure. No new scientific receipt is granted here. The [refined original-input review](../evidence/refined-current-migration/recoverability-review.md) separately determines whether its required historical admitted source set is available; emission transport success does not waive that verifier's stronger predicate. The [independent F5/circular review](f5-circular-build-review.md) owns build-only advancement and its separate downstream restrictions.
