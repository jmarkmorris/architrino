# Variable-cell historical evidence migration

Date: 2026-09-07. The operator authorized the first remaining item in the development-process review: repair the variable-cell scientific reader's historical bindings and predicates under the accepted archive contract. The starting checkout was `7937a8733ef93e4a8e4109f9ce5180535c775120` on `codex/peridot`, with no changes reported by `git --no-optional-locks status -sb`. This work changes the reader and its focused controls; it does not authorize a scientific batch or Git publication.

## Result and meaning

The adapter can now construct its original 160-parent metadata view from retained scientific records. The [bounded retained-data instrument](../evidence/variable-cell-migration/check-retained-adapter.py) verifies the original full run's 198-member source list, then constructs the complete adapter with its earlier pilot and refined-cell ancestry. The [full-chain result](../evidence/variable-cell-migration/retained-full-chain.json) records 207 physical captures; the [complete adapter result](../evidence/variable-cell-migration/retained-adapter-construction.json) records 265 physical captures. These counts describe different scopes. The latter includes current executable references, both historical cover chains, their records, and additional document archives.

Construction makes zero root, acceleration, residual, refinement, or geometry-restriction evaluations by the adapter's returned counters. Its 2,608 coverage-cache entries are metadata about retained intervals, not numerical kernel calls. Final file checks pass before the context closes. The result verifies retained bytes and recorded provenance; full original-environment verification remains false because the three exact old host-tool versions remain unavailable. No claim about a fresh historical process observation, a replay, or new mathematical acceptance follows.

## Why the old reader failed

A hash is a fingerprint of file contents. A pin records the fingerprint a reader expects. For a current dependency, the expected fingerprint identifies the code that will execute now. For historical evidence, it identifies the version named by the original record. Advancing the latter to a current fingerprint changes the question being checked and can make an intact old record impossible to authenticate.

The fourth full-entry pin in the adapter named a later generation instead of the original 27,166-byte entry with fingerprint `1398a005…73352b`. Its full-plan predicate also demanded a later verifier and declaration. The repaired predicate checks the original verifier `19c57e9b…2d9132` and declaration `7c2a8b0b…28f9e4`, matching the already preserved full admission and entry. The original 198-member membership comparison and scientific claim checks remain in place.

Constructing the entire adapter exposed the same distinction in earlier ancestry, beyond the full chain. The pinned refined manifest names the original ancestry tuples; those tuples now supply the logical ancestry instead of the current mapping module's later document fingerprints. The current mapping module's numerical and structural checks remain unchanged. Before calling it, the adapter validates the original verifier/declaration identities and supplies a local view containing the two generation tokens that its structural predicate expects. Original records and their hashes are never rewritten. Likewise, the original recorded gate schema is projected locally from `abc-pilot-outer-heartbeat.v1` to the current structural reader's `subfield-circular-pilot-outer-heartbeat.v1`; resource values, exits, time bounds, and scientific flags are preserved. A substituted current schema in the old log is rejected.

Four additional document versions were recovered exactly from Git: the integral proof, correlated-residual proof, Gauss–Kronrod protocol, and refined-cover projection. The [document archive record](../evidence/variable-cell-migration/adapter-document-archives.json) records each original tuple and Git source. Thirteen additional historical source generations needed by the pinned pilot/refined receipts were recovered with a known-case-tested Git object reader; the [source recovery record](../evidence/variable-cell-migration/refined-source-archives.json) identifies every blob. These 17 additional files supplement the existing 19 archives. All 36 are data, with `.source` suffixes, and are not executed. The original mathematical references, old closure checker, old receipts, and historical source files are preserved.

## Interface and protections

The caller explicitly passes `historical_evidence` to `open_adapter`, selecting `braid-program/variable-cell-historical-evidence.v1`. The [retained selection](../evidence/variable-cell-migration/historical-evidence-selection.v1.json) supplies exact original/physical binding pairs and the three unavailable original host tuples. A route must preserve the digest and byte count, target a distinct `.source` file, and belong to the finite historical catalogue. Two different historical versions at one logical path remain distinct. Duplicate, unknown, unused, substituted, changed, replaced, or hardlinked sources reject.

Current executable captures bypass the historical resolver. An unavailable host can contribute only its explicitly reported original membership tuple; it cannot supply bytes or exempt a different version at the same path. Same-descriptor reads, aggregate limits, canonical paths, file identities, and final rechecks remain enforced. The resulting `historical_evidence_verification` is immutable and distinguishes recorded evidence from full original-environment availability.

Both exact owner headings—the original `F6c` spelling and the current descriptive spelling—retain the same original run/session/output checks. A document containing both sections rejects. This preserves old owner evidence without accepting an ambiguous current declaration.

The old independent parent closure instrument remains pinned to `3eefbb87…ba223`; the new external operational receipt is not a scientific acceptance input. No new-parent scientific interface was added. Such consumption must first have its own independently validated acceptance contract. The explicit archive selection here is a metadata interface, not a launch authorization.

## Validation and retained failures

The [validation records](../evidence/variable-cell-migration/validation-runs.json) retain complete stdout/stderr and bounded lease results, including unsuccessful attempts. The original adapter controls passed 127 tests before edits. The final combined Python run covers the adapter, single-leaf diagnostic, and streamed-session suites: 212 tests pass, including 136 adapter tests. Controls cover literal full-chain identities, missing-host limitations, archive substitution and replacement, multiple generations at one path, current-executable isolation, old gate and owner spellings, exact old-parent bindings, and unchanged mathematical fixture results. Literal controls preceded the retained target. The final retained-data result identifies the exact adapter and test-file fingerprints it checked.

The attempted Node diagnostic suite fails before its test bodies because the launcher's `diagnostics` pin expects `7a1f5571…9d65e71`, while the current module hashes to `042684de…8c62d47`. `git show 7937a8733:scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs` contains that same old pin, establishing that this particular mismatch precedes the present edits. The source at `tests/f6c-streamed-leaf-diagnostic.test.js` also explicitly sets `WHOLE_PROCESS_FIXTURES_READY=false` and asserts readiness before whole-process cases. The failed Node run establishes the import-stage mismatch; it does not establish results for those later cases. Neither the stale pin nor the readiness guard was weakened. This is an open defect, not a skipped success or an irrelevant failure.

Other retained failures concern integration during this repair: an initial sandboxed owned-process launch timed out before the tests started; an early historical full-chain read tried to request bytes for an explicitly unavailable host; whole-adapter attempts exposed the document bindings, old prior-generation predicate, additional pilot source generations, and old gate schema described above. One synthetic generic-parent fixture still used a one-byte placeholder for the now explicit 27,166-byte historical entry; correcting that literal fixture restored its pass. None of these failures is counted as a passing test. A transient source-inventory attempt using `ast.literal_eval` could not read expressions containing path-prefix constants and produced no inventory; the replacement used the current module's declared tuple and known matching/nonmatching hash controls.

Current dependency propagation is limited to the adapter/test fingerprints in the single-leaf controls, streamed-session controls, and streamed-leaf launcher's current source table. The [scoped propagation record](../evidence/variable-cell-migration/current-binding-propagation.json) names those edges. Historical fixtures and receipts are not repinned. This does not complete the separate current-binding audit or migrate the streamed-leaf launch plan to carry the new archive selection.

## Remaining work and falsifiers

The adapter migration is complete within retained full/pilot/refined metadata construction. The next operational task is to review the streamed-leaf launcher's current dependencies, forward the explicit archive selection and its physical source union through a versioned launch contract, and resolve its intentionally unready whole-process tests with bounded closure controls. A new operational receipt must remain insufficient for scientific acceptance.

The completion claim would be overturned by a failing final Python control, a retained-data construction that fails file rechecks, a missing scientific source being accepted, an archive supplying executable code, promotion of any scientific authority flag, or a substituted historical tuple passing. The linked controls, selection, hashes, and full logs make these observations reproducible. Three missing historical host executables, unrun new scientific batches, and the failed Node diagnostic suite remain explicit limits.

The [final source check](../evidence/variable-cell-migration/final-source-check.json) verifies that the retained result names the current adapter and controls, the four explicitly propagated current dependency edges match their files, all 36 selected archive routes retain their exact bytes (893,726 bytes in total), and all 19 prior archives and three frozen closure-reader files remain unchanged. `git diff --name-only HEAD` over the mathematical reference paths listed in that instrument reports no changes. `validate-priority-ranking.mjs`, `validate-machine-artifact-retention.mjs`, and the startup router's `--check` pass; `git diff --check` reports no whitespace errors in tracked edits. The [owner closeout](../evidence/variable-cell-migration/owner-closeout.json) reports this task clear after inspecting 317 leases. The [initial launch classification](../evidence/variable-cell-migration/startup-timeout-status.json) is `stale_closed`, with no recorded control endpoint; its old lease is preserved rather than relabeled as a completed test.
