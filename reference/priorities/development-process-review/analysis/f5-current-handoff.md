# Current F5 data handoff

Measured by the [joined admission and stage receipts](../evidence/f5-current-handoff/admission.json), the current F5 handoff completes production and independent data verification against the original prefix and restriction. The [independent source review](../evidence/f5-current-handoff/independent-review.md) assesses the transport separately. This acceptance is confined to the declared current build/input/runtime profile and exact history-data conformance. It grants no root execution, evolution or H3 authority.

The new transport [executes the unchanged producer and reference](../../../../scripts/eom/execute-f5-prehistory-handoff.py) from captured bytes. The [runner](../../../../scripts/eom/run-f5-current-handoff.mjs) launches them in separate owned processes and joins their identical handoff identity only after successful process-group closure. Historical scripts, inspector code, scientific references and original prerequisite bytes remain unchanged. The current F5 builder and build launcher bind the reviewed circular builder generation after its independently reviewed schema correction.

## Measured result

The [verifier stage](../evidence/f5-current-handoff/verifier-stage.json) reports twelve members, 612 segments, 12,240 binary64 token comparisons, 3,600 exact join comparisons, 288 analytic interpretation box comparisons and 72 release scalars, with no failures. The original independent reference returns `accepted:false` and `dataChecksPassed:true`; fresh external execution admission supplies the separate process/provenance conclusion. Both source-decimal and frozen-binary64 interpretations remain inherited from the original accepted restriction.

The [fresh build review](../evidence/f5-current-handoff/current-build-review.json) records 41 closed build stages and 54 captured sources. The fresh preparation digest is `200d6787f9ec64ec1fc140c512e58a7f15d30edb2e484c1f05ad7eccfb0dce3a`; the inspector digest is `8bee4717038b5475b856d7167ddd651e98a63e0e87f9c0cd176b752b9c546a99`. Source snapshots, tool/header/library before-and-after inventories and scoped `nm -C` inspection support build-only acceptance. The inspector contains retained-history functionality; coupled acceleration/evolution symbols are absent. The actual data handoff has digest `f13bc13ce4b276f628fe8f66280664629f0e28243576b1bd960134905c6682a7` and 908,905 bytes.

The [run record](../evidence/f5-current-handoff/runs.json) retains seven owned process receipts and raw native output. The build took 21.998 seconds and joined execution 1.227 seconds by supervisor elapsed-wall measurements. These are observed runs, not general performance estimates.

## Independence and provenance

The original version-one reference checks an original source-owner map. The new version-two handoff records current source owners. The bridge first verifies actual current provenance, then projects only the schema and source-owner map into a private copy for the unchanged numerical reference. Those two projected equality predicates are vacuous as provenance checks; they supply no historical executable authentication. Every other `analyze_data` predicate stays active. Current source ownership, exact complete source census, builder receipt, independent review, executable identity and successful producer process are checked separately.

The [current plan](../evidence/f5-current-handoff/current-plan.json) binds the exact original prefix/restriction, fresh build receipt and review, closed build admission, executable and 135 Python runtime files. Runtime capture includes the interpreter, shared-venv configuration and loaded Python/cache/extension module files; both stages reject a loaded file outside the census. File-backed inspector dependencies are captured with digest and byte count. Explicit macOS shared-cache dependencies remain platform trusted, as recorded in the authenticated build; current compiler/header bytes need not remain unchanged merely to execute the recorded binary.

The runner binds its local imported JavaScript graph, Node executable and `/bin/ps`, and rechecks both stages' entire input set after verification. Node builtin modules are embedded in Node. The operating system and macOS shared-cache runtime remain platform trusted. Before/after file checks are conservative path-reopen checks, not atomic executable identity guarantees against adversarial transient replacement. The required owned runner supplies an inclusive process deadline; standalone Python elapsed-time predicates do not constitute a persistent hard-stop alarm through stdout.

## Controls and falsifiers

Nine Python controls pass by native unittest output, including the pre-existing independent host-IEEE fixture, wrong current provenance, changed token/binary64/endpoint/fingerprint/future fields, same-byte inode replacement, consistent source-census omission, late deadline and inode-specific retraction. One Node test exercises known SHA identity, substituted bridge, failed/unclosed process and prior input mutation. A changed numerical datum accepted by the unchanged reference, a consistently omitted source accepted by transport, or a mismatched prior-stage input accepted at final admission would overturn the corresponding claim.

The [post-write failure control](../evidence/f5-current-handoff/post-write-failure-control.py) injects a capture failure only after a private stage packet has been written during actual production. Run `77874f1d-59d2-4583-9583-b09cff52fa3e` verifies rejection, preservation of private diagnostics and absence of a public stage receipt. It changes neither numerical subject nor reference. A public stage receipt surviving this injected failure would falsify the lifecycle conclusion.

The runtime inventory was rerun after its known-file/interpreter controls passed; only that later inventory supplied the current plan. Earlier exploratory inventory output was not used as acceptance evidence. Scoped `git diff --check` passed for the changed transport, builders and tests. This packet does not claim that unrelated repository tests or GitHub checks are globally green, nor does it reconstruct the unavailable historical binary.
