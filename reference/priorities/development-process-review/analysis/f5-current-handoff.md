# Current F5 data handoff

Measured by the [independent source review](../evidence/f5-current-handoff/independent-review.md), the current F5 handoff completes production and independent data verification against the original prefix and restriction. This acceptance is confined to the declared current build/input/runtime profile and exact history-data conformance. It grants no root execution, evolution or H3 authority.

The new transport [executes the unchanged producer and reference](../../../../scripts/eom/execute-f5-prehistory-handoff.py) from captured bytes. The [runner](../../../../scripts/eom/run-f5-current-handoff.mjs) launches them in separate owned processes and joins their identical handoff identity only after successful process-group closure. Historical scripts, inspector code, scientific references and original prerequisite bytes remain unchanged. The current F5 builder and build launcher bind the reviewed circular builder generation after its independently reviewed schema correction.

## Measured result

The original independent reference returns `accepted:false` and `dataChecksPassed:true`; current process admission remains a separate operational observation. Both source-decimal and frozen-binary64 interpretations remain inherited from the original accepted restriction.

The fresh build records closed stages, authored source bindings, and the data-only inspector boundary. Host compilers, SDKs, packages, and runtime libraries are current capabilities rather than historical build identities. The inspector contains retained-history functionality; coupled acceleration/evolution symbols are absent. The actual data handoff remains bounded by its transport receipt.

Owned process receipts remain local operational observations rather than repository-level toolchain identity records. Their wall-time measurements are scoped to those runs and are not general performance estimates.

## Independence and provenance

The original version-one reference checks an original source-owner map. The new version-two handoff records current source owners. The bridge first verifies actual current provenance, then projects only the schema and source-owner map into a private copy for the unchanged numerical reference. Those two projected equality predicates are vacuous as provenance checks; they supply no historical executable authentication. Every other `analyze_data` predicate stays active. Current source ownership, exact complete source census, builder receipt, independent review, executable identity and successful producer process are checked separately.

The current transport binds the exact original prefix/restriction, authored source owners, closed build admission, executable, and data handoff. Runtime tools and packages are selected as live capabilities and are not admitted through a historical byte census. Authored source and scientific input bytes remain independently bound.

The runner observes owned process closure and resource behavior live. Node builtin modules, the operating system, and package/runtime libraries remain current execution capabilities. Authored source and scientific input rechecks are conservative path-reopen checks, not atomic executable identity guarantees against adversarial transient replacement. The required owned runner supplies an inclusive process deadline; standalone Python elapsed-time predicates do not constitute a persistent hard-stop alarm through stdout.

## Controls and falsifiers

Nine Python controls pass by native unittest output, including the pre-existing independent host-IEEE fixture, wrong current provenance, changed token/binary64/endpoint/fingerprint/future fields, same-byte inode replacement, consistent source-census omission, late deadline and inode-specific retraction. One Node test exercises known SHA identity, substituted bridge, failed/unclosed process and prior input mutation. A changed numerical datum accepted by the unchanged reference, a consistently omitted source accepted by transport, or a mismatched prior-stage input accepted at final admission would overturn the corresponding claim.

Scoped `git diff --check` passed for the changed transport, builders and tests. This packet does not claim that unrelated repository tests or GitHub checks are globally green, nor does it reconstruct the unavailable historical binary.
