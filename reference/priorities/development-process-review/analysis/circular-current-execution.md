# Circular current pilot acceptance and remaining ladder requirements

Status legend: **✓ Done** = the named bounded requirement has its recorded evidence; **◐ Partial** = implemented or measured in part, with the remaining obligation stated; **○ Not done** = no accepted result. **Blocked** names a required decision or missing contract; **Deferred** identifies intentionally excluded execution. Historical sections below preserve the state at that stage and do not reopen subsequently completed requirements.

| Current actionable requirement | Status | Remaining obligation |
| --- | --- | --- |
| Current adapter/build and independent manifest check | ✓ Done | Exact v3 build review below; no broader numerical claim |
| Current production context and independent review | ✓ Done | Original numerical references retained |
| Complete observation owner and 32-phase pilot | ✓ Done | Accepted independent pilot review below |
| Current rung build/pilot/phase bindings and current joint-receipt transport | ✓ Done | Independent first-rung caller review accepted; later prior-rung transport remains separate |
| All sixteen candidate resource dispositions | ✓ Done | Independent resource review retains ten within-cap candidates and six returns; executable acceptance covers the first candidate across 8/32/128 |
| Bounded current memory-pressure observer | ✓ Done | Independent review, known controls and two bounded actual preflights accepted |
| Current guarded ladder execution entry | ✓ Done for the selected candidate | All 8/32/128 scopes independently accepted; arbitrary-profile generality is not established |
| Ten within-cap 8/32/128 candidate ladders | ◐ Partial — Paused | First candidate complete; nine deferred pending separately reviewed selection and transport |
| Six over-cap candidate ladders | ○ Not done — Blocked | Existing 1800-second cap retained; explicit resource-return decision required before enabling these candidates |
| H3/scientific promotion or overall repository test health | ○ Not done — Deferred | Outside this bounded operational campaign result |


The current circular pilot is accepted for all 16 candidates, 32 phases and 2,448 rows by the [independent final review](../evidence/circular-current-execution/current-pilot-independent-review.md). Its instrument rechecks 430 unique bindings, 129 successful retired execution gates, 136 closed observation probes and the exact external owner exit. The owned run completes in 412.981 seconds. This closes the pilot's current-build, phase and process-observation admission; it supplies no H3 or later-ladder authorization. The current memory observer and scoped resource plan are independently reviewed, and the first candidate's eight-phase rung is independently accepted. The same candidate now completes its independently reviewed 32/128 rungs using the immutable-prior transport described below; its full ladder covers 168 phases and 6,048 rows. The other nine within-cap candidate ladders are deferred; six larger candidates remain resource-return-not-run because their 128-phase projections exceed 1,800 seconds. The sections below retain the adapter/context repair evidence and the execution follow-through.

## Repair and independent checks

The first fresh build exposed a concrete mismatch: `source_members` in `src/eom/native/eom_subfield_circular_root_cli.cpp` required `prescribed-assembly-spec.v2`, while the adapter's pinned root-reference selects v3 configuration files. The unchanged independent checker `scripts/eom/verify-subfield-circular-history.mjs` already requires v3. The repair changes that one adapter schema literal and the builder's corresponding subject digest. All history construction, interval calculations, exact-pair execution, and independent checker code remain unchanged. The [exact diff](../evidence/circular-current-execution/schema-repair.diff) and [consumer inventory](../evidence/circular-current-execution/adapter-consumers.txt) retain the edit and its binders. The F5 worker was notified before the builder changed and received the final builder digest for its transitive build bindings.

The retained [run records and native logs](../evidence/circular-current-execution/runs.json) show the before and after controls:

| Instrument and scope | Measured result | Boundary |
| --- | --- | --- |
| Initial enabled adapter/preparation/process/rung selection, `703835fb-83a4-479d-8b11-93e9d0b777bf` | 44 tests: 37 passed, 7 failed, zero skipped | Six adapter failures originate from the schema mismatch or missing preceding manifest; one rung test reports its historical outer-supervisor pin |
| Repaired adapter/preparation/process selection, `9f1aa65f-d108-40b7-a16b-8dabd23d5a59` | 33 passed, zero failed/skipped, 13.827 seconds | Includes all seven optional adapter controls with `AAA_SUBFIELD_CIRCULAR_ADAPTER_BIN` selecting the fresh binary; excludes the still-open rung selection |
| Current manifest, original analytic checker, EOF-only serve, `0ee54313-b31d-423b-8783-396501e81e6d` | Completed in 3.396 seconds; three subprocesses closed successfully | One six-member candidate at rung 2, phase 0; no pair command sent, empty row file, `prepared` and `stopped` events present |

The seven adapter controls cover fresh binary selection, six-member and twelve-member manifests, repeated-reception byte identity, output non-overwrite, rejection of unaccepted conformance, and FIFO rejection. The analytic proof is independently authored and checks the actual current whole manifest. The empty serve transcript checks protocol preparation and closure only; it is not a phase certificate. The [data-path receipt](../evidence/circular-current-execution/data-path-review.json) states that boundary explicitly. All five recorded owned runs are terminal with process-group closure; no review-owned background job remains.

## Concrete build acceptance

The [v3 build review](../evidence/circular-current-execution/v3-build-review.json), produced by the retained [review instrument](../evidence/circular-current-execution/review-build.cjs), verifies 49 source bindings, 23 reference bindings, matching before/after sources, references, tools, headers, and external libraries, and all 40 successful closed build stages. It also rechecks the built outputs and stage logs. Known SHA and positive/negative symbol controls precede the target. `nm -C` on the resulting binary finds `certify_exact_pair` and none of the three named coupled acceleration/step/evolution entrypoints; this is a named-symbol check, not a complete call-graph proof.

The current preparation digest is `c80526d097c81627186cbbfcea7e0005d9d73288e331f4535f07982cc2bef944`; the linked executable digest is `b15799584810810aff587e64ce0e6e6c32c07f12c53f34df8614b6012fd219f6`. Their paths and sizes are in the review. Build preparation itself loads no histories and performs no root calls. The subsequent data-path probe deliberately prepares and independently checks one history manifest, while still sending no root requests. The initial pre-repair build review is preserved as historical evidence and does not describe the current edited adapter.

The source/library checks retain the builder's explicit macOS shared-cache boundary: system libraries unavailable as individual files are identified through the platform record rather than invented hashes. A changed captured dependency, failed subprocess closure, different executable digest, or failed independent proof overturns the corresponding bounded acceptance above.

## Historical current-build context requirements and their current disposition

The remaining obstacle is an executable contract, not a missing permission to run an otherwise compatible pilot. `prepareSubfieldCircularPhaseLedgerContext` in the unchanged `SubfieldCircularRootLedgerReducer.mjs` calls its private `verifyBuild`. That function hard-pins the old adapter (`42dc7e…`) and old CMake generation (`e4b3a8…`), in addition to unchanged History and Interval API dependencies. The new concrete build therefore cannot enter that context. The existing pilot and rung wrappers also select the original build receipt `be6f2e…`; the rung's outer-supervisor pin remains old. None of those original records was overwritten or relabeled.

A future independently reviewed current-build context interface needs the following explicit inputs and obligations:

1. **✓ Done — Current build identity.** Accept the original bytes and expected digest of the current build receipt, plus its separately recorded review. Check every declared source, header, tool, library, executable, and build-stage log against its recorded identity; require before/after equality, successful closed stages, the accepted adapter schema-only generation, reviewed current CMake, and unchanged History/Interval/ExactPair dependencies. Keep the existing platform-library boundary explicit.
2. **✓ Done — Current mathematical inputs.** Accept the exact manifest and independent conformance bytes and hashes, rung and phase, and original seven reference bindings. Preserve source membership/order, the complete segment grid, numeric carrier bits, history fingerprints, independent inflated-speed bounds, and proof identity checks currently enforced by the reducer. Build review cannot replace any of these predicates.
3. **✓ Done — Unchanged row predicates.** Delegate the existing ordered-pair, root/domain, cited-segment coverage, endpoint-self, cache-cell, precision, and authority predicates without changing the numerical reference in the adapter repair. A production context must retain the current stop-after-first-invalid-row behavior, exact row census, source rechecks, and original-row hashes. The exported abstract `inspectSubfieldCircularRowForTests` explicitly lacks source/file authority and cannot be promoted to an accepted phase.
4. **◐ Partial — Current process and resource plan.** Bind the resulting context API, current phase-process helper, pilot/rung/dispatcher, and outer supervisor. Require fresh closed phase records and a complete accepted 32-phase, 2,448-row pilot before using the existing ladder prerequisites. Its resource plan must account for all sixteen candidates, preserve the declared memory/disk limits, and carry the reviewed per-cohort wall limit. The zero-row handshake supplies neither that pilot census nor candidate cost estimates.

This was the prerequisite at the end of the adapter repair. The subsequent context implementation below advances that obligation separately from the unchanged adapter and analytic reference. Full pilot/ledger acceptance remains open until the complete execution and accepted resource chain exist; successful current adapter and handshake checks do not close it.

## Current context implementation, September 8

The production reducer now recognizes the exact previously reviewed preparation through the original bytes of `v3-build-review.json`. It verifies the entire reviewed source/reference/tool/header/library census, discovery tools, file-backed runtime dependencies, platform-library boundary, build outputs and closed stages. The original receipt still selects its original source contract. An arbitrary caller-supplied receipt hash cannot authorize a new source generation. Both the retained review and all consumed files participate in the final recheck.

Inspection also found an unrelated proof-binding defect: the seven-element reference array was indexed at position seven and sliced as if it had eight elements. The unchanged whole-manifest producer emits six frozen references, the candidate and its own verifier binding. The reducer now selects the verifier by its declared identifier and checks that exact independently produced order. No interval, root, row-census or mathematical predicate changes, and neither the adapter nor the whole-manifest verifier changes in this batch.

Owned run `8538f308-65f9-49ce-a4b3-bf5391e34137` passes all 30 existing ledger and phase-worker controls, with exit zero and process-group closure. The first sandbox launch was rejected before target spawn with `EPERM`; the successful host run is the reported test result. After the known SHA control, enabled `tests/subfield-circular-current-context.test.js` in run `eefdb14c-b515-4175-9a63-d82688097833` passes two tests without skips: the actual reviewed build and actual retained manifest/proof enter the production context and all six member speed bounds pass; a wrong build hash and a proof missing an executing dependency reject. That run closes successfully in 2.082 seconds. Logs remain under `.local-data/owned-compute/logs/<run-id>.stdout.log` and terminal ownership records under `.local-data/owned-compute/`.

The context pass does not certify any ordered-pair rows or a complete pilot. Independent implementation review and propagation into the pilot/resource/ladder composition remain required. A substituted source, omitted proof instrument, changed reviewed census, failed speed bound or accepted unreviewed build would overturn the corresponding context claim.

Independent review subsequently accepts this exact context generation and independently repeats both enabled controls in run `5ce2deca-dae0-449a-84eb-ec82e7aacf2e`; the [review record](../evidence/circular-current-execution/current-context-independent-review.md) retains its source identity and limits.

### Historical pilot attempt before process-owner completion

The attempted current pilot/outer/rung/dispatcher pin integration passes 80 selected process and caller controls in run `7f4bd857-be17-4466-a0d4-e2fee6bbc372`, after replacing a test whose original purpose was to assert rejection of the old selected build with current-build acceptance and substituted-build rejection. The earlier selection `3169d78b-af4a-42fa-9209-ce8ddedc51bf` retains 79 passes and that one obsolete expectation failure. These are results for the attempted source generation, not the final restored caller files.

Actual top-level pilot run `d0a580d2-a601-40f3-9d61-8075acec4655` exits one with closed process group before launching the scientific workload. Its first error is `explicit independently reviewed and accounted process inspection required`. Reading `launchReviewedPilot` and `superviseRegisteredPilot` identifies the gap: the former omits the required explicit inspection callback, while the latter deliberately refuses its own default process table because probe lifetime and resource accounting require an external owner. A wrapper that merely renames the default function would bypass the obligation.

The complete owner must bind the observer source/runtime, enforce probe deadlines through work and cleanup, retain probe birth identities and terminal closure, account for inspection resources, and include all owned probes in final admission. The existing F6c lifetime implementation couples these duties to a complete lifetime state and source union; it cannot be imported as an isolated callback. The [withdrawn integration diff](../evidence/circular-current-execution/withdrawn-pilot-integration.diff) preserves the attempted caller changes. Those five caller/test files were restored to their pre-attempt bytes, leaving the reviewed context repair and its tests as this batch's circular implementation. This avoids invalidating unrelated live consumers of the shared outer source while the complete top-level lifetime owner is still absent.

The exact remaining requirement is that lifetime integration followed by the complete accepted 32-phase pilot and measured resource admission. The ladder additionally still selects its original memory-observer digest; the unavailable utility must receive an explicit current operational contract before a new ladder execution. No callback waiver, scientific acceptance or old-receipt replacement was performed.

## Process-owner continuation

The operator selected completion of circular process ownership and the F5 acceptance chain. The current circular implementation separates the observation owner and current composition entry from the shared registered supervisor. This preserves the shared supervisor's bytes and existing consumers while supplying its missing caller obligation explicitly. The owner must retain bounded observation lifetime, source/runtime identity, measurement scope and probe closure through final publication; the current entry must keep its own deadline guard until its completion record is durably checked.

Before integration, run `a55f7930-d0a3-46fa-a1d0-57d91ce0443e` repeats the two enabled current-context controls with exit zero and group closure. The pilot runner then selects the exact reviewed current build, current phase-process helper and independently reviewed ledger context. Its SHA-256 is `18479b39de068129423ecdffd103feb9c06a3f870648d55cbd7b0a3264926774` by `shasum -a 256`. The old selected-build rejection fixture is replaced by current-build binding and substituted-build rejection. Run `cac2d680-f1d3-4437-b408-c2667cbb5915` passes all 40 pilot, phase-process and phase-ledger-worker controls with zero failures or skips and closed group. These controls do not supply the observation owner's independent review or an actual accepted pilot; those outcomes are recorded only after execution below.

The current launch profile captures five reviewed source roles and the executing Node binary, file-backed shared libraries and shared Python environment. Its creation command passed the known SHA-256 `abc` control before hashing the target files. The profile records an 1,800-second original deadline and retains false H3 eligibility. The observer records actual probe closure, measured helper/ps resource fields and a separately labeled conservative CPU upper bound; sampled resident memory is not reported as a continuous peak guarantee. Final source review and actual pilot execution follow this profile preparation.

## Actual current pilot execution

Owned run `765515fb-a7ff-4a20-93a9-8b7662f6c71c` executes the current entry against the reviewed launch profile. The observer captures the Node and Python loaders, their non-system loaded libraries, helper modules, process-table utility and five source roles before process observations. macOS system shared-cache libraries remain an explicit platform trust boundary. The owner retains each probe's birth identity, actual wait and stream closure, finite resource fields, output limits and failure state. A separate deadline guard remains active through final source rechecks and publication. Fourteen focused lifecycle controls pass in owned run `37155579-74f1-4c30-8587-f5de31cf5d2e`, including blocked and stubborn children, late serialization, unexpected cache artifacts, changed runtime dependencies, interruption and startup failure.

The actual pilot's native terminal reports accepted in 410.433 seconds, conditional on its external completion check. The current entry publishes `current-admission.json` at digest `b5118997f6956029ef98f4fee7daaf43f78961a54b37ab6bea935ebdad040231`. Its outer owned lease then records exit zero, a closed process group and 412.981 seconds, satisfying that conditional exit requirement within the original 1,800-second entry deadline. Independent final phase/resource review is recorded separately; the producer receipts remain unchanged.

The resource evidence distinguishes actual consumption from bounds. Gate and helper records carry their measured CPU and resident-memory fields; the observer's full probe wall interval multiplied by logical CPU count supplies a conservative CPU bound through probe exit. The full-entry deadline supplies a separate conservative ceiling that covers the publication tail. These are upper bounds, not measured CPU consumption. Resident-memory samples do not prove a continuous peak bound. The later circular 8/32/128 ladder still requires its own current memory-observer transport and independently reviewed cost plan; the accepted pilot does not authorize those runs or H3 claims.

The [independent final pilot review](../evidence/circular-current-execution/current-pilot-independent-review.md) accepts this bounded run after rechecking 430 unique file bindings, all 32 phases and 2,448 rows, all 129 retired zero-exit execution gates, all 136 closed probes and all 16 candidate cost records. It also verifies the external owner closure and exact published admission hash. Maximum sampled aggregate resident memory is 957,480,960 bytes; no continuous peak claim follows. The six larger candidates' recorded 128-phase projections are approximately 2,287–2,381 seconds, exceeding the existing 1,800-second per-candidate limit. These estimates derive from measured pilot costs and are not measurements of later rungs. The complete cost table and its accounting boundaries are retained in the review; a separate current cohort/resource and memory-observer contract remains the next ladder obligation.

## Current resource preparation and sixteen candidate dispositions

**✓ Done — Bounded preparation and current memory transport controls.** Owned serial run `48919643-e1e5-4982-beeb-7454230712b6` passes all 36 current resource, memory-owner, rung and dispatcher controls with zero skips and process-group closure in 3.022 seconds. The explicit 65% memory control accepts the equal threshold and rejects 66% before the actual utility. The known 1800/1800.01-second cost boundary and complete-census negatives run before actual cost input. The memory helper captures Python loaded libraries before the utility, measures the owned memory-pressure child through wait4 and stream closure, escalates stubborn children, rejects output growth and resource shortfall, and retains final source/runtime checks. Its [independent memory-source review](../evidence/circular-current-execution/current-memory-source-review.md) and [preparation review](../evidence/circular-current-execution/current-ladder-preparation-review.md) are separately recorded. [Control log](../evidence/circular-current-execution/current-ladder-controls.log).

**✓ Done — Actual current resource preflight.** Owned run `df6de16d-5d85-45eb-a1c8-6d187ea0ac67` exits zero with observed group closure in 1.502 seconds. Following the explicit known-percentage control, repeat run `57c5451f-0814-4924-b0eb-0d2a315c54ea` also exits zero with group closure in 1.579 seconds and writes the separate `current-ladder-preflight-20260908-b` output. Both pending plans have the same recorded bytes. The measured memory utility reports 56% free and `statfsSync` reports 934160949248 available bytes, above the unchanged 20% and 160-GiB launch floors. Both process probes and both memory probes close. These are instantaneous resource observations, not continuous peak or future availability guarantees. The original 60-second guard includes source capture, measurement and publication.

**◐ Partial — Resource plan and first-rung transport.** The preparation writes `resource-plan.pending.json` (digest `66b98be09a07f9fd9b8926de928f36a162ab9f6e4855f6597407c18762b0e875`) and `first-rung-prior.json` under `.local-data/braid-analysis/subfield-circular-root-pilot-20260827-v1/current-ladder-preflight-20260908-a/`. The plan retains `resourceBudgetReviewed:false`, independent status pending, one serialized EOM worker and the 1800-second cap. Its first ten candidates are proposed for execution; six have explicit resource-return-not-run dispositions. Independent resource acceptance and the complete current rung entry review must precede execution.

| Candidate | Projected 128-phase seconds | Current disposition |
| --- | ---: | --- |
| coincident-midpoint-common-frequency | 1216.667 | ✓ Done — independently accepted 8/32/128 execution |
| coincident-midpoint-equal-radius-common-frequency | 1264.522 | ◐ Partial — eight phases accepted; 32/128 prior transport and execution remain |
| coincident-midpoint-3-2-1-frequency | 1260.882 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| phase-compensated-equal-geometry | 1274.227 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| axially-separated-common-frequency | 1176.376 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| axially-separated-equal-radius-common-frequency | 1210.804 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| axially-separated-3-2-1-frequency | 1235.413 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| axial-transverse-coincident-axis-interior | 1309.446 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| high-axial-coincident-axis-interior | 1227.272 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| planar-common-center-three-binary | 1175.310 | ○ Not done — Deferred — separately reviewed candidate selection and prior transport required |
| coincident-center-two-component-circular-co-rotating | 2290.165 | ○ Not done — Blocked; resource-return-not-run |
| coincident-center-two-component-circular-counter-rotating | 2286.568 | ○ Not done — Blocked; resource-return-not-run |
| coaxial-separated-two-component-circular-co-rotating | 2381.034 | ○ Not done — Blocked; resource-return-not-run |
| coaxial-separated-two-component-circular-counter-rotating | 2300.965 | ○ Not done — Blocked; resource-return-not-run |
| coaxial-separated-two-planar-braid-co-rotating | 2301.388 | ○ Not done — Blocked; resource-return-not-run |
| coaxial-separated-two-planar-braid-counter-rotating | 2299.210 | ○ Not done — Blocked; resource-return-not-run |

The projections come from the independently reviewed two-phase pilot and remain estimates. No cap increase, concurrent multi-candidate run or six-candidate exclusion from the disposition census is implied. Changed pilot/review bytes, a failed probe closure, resource-floor contact or a changed code/runtime binding invalidates this preparation for execution.

## Reviewed first-rung launch preparation

**✓ Done — Independent resource-plan acceptance for the first bounded execution.** The [B resource review](../evidence/circular-current-execution/current-resource-plan-review-b.json) binds accepted plan `340961b3c8e134fee9548442c439b2c32a11e61dddf9b774e8db5c788bdfc39b`, current entry `91218e1e4bd85fc614c5e270086bd62d766266cd02a51c06a047cef192984c81`, and the exact memory-owner/helper generation. The accepted plan is a new copy; both original pending plans remain unchanged. Review authorizes only `coincident-midpoint-common-frequency`, rung 8. Remaining candidates and rungs retain their explicit not-run dispositions until their own entry and prior-chain requirements are met. The B preflight measures 54% free and 934039764992 available disk bytes.

**✓ Done — Exact launch profile.** Following a known SHA-256 `abc` control, profile construction verifies the exact accepted plan and review, then captures eight source roles, the Node executable and all 26 non-system Node images. `first-rung-profile.json` under the B preflight directory has digest `3a2eab4bcf45efc987862f814a43f3030fbba09e4396f84249a1fb3d7afd7e90`. Its original deadline is 1800 seconds. The current entry retains process and memory ownership through final publication and requires independent final review plus exact external zero exit.

**✓ Done — Actual first-rung acceptance.** Owned run `887ff7f4-1684-44be-b0ad-5651d34d3731` exits zero with observed process-group closure in 79.209 seconds. The [independent rung review](../evidence/circular-current-execution/current-rung-independent-review.json) accepts the eight phases and 288 rows at the recorded bounded grade. Subsequent 32/128 prior-chain transport must authenticate the current joint rung receipt and its exact external review before consuming it; original outer receipt shapes must not be assumed for this new joint record.

## Historical later-rung transport design, followed by current implementation

**◐ Partial — Concrete design; no later-rung transport implemented.** `authenticateSubfieldCircularPriorPhases` reads an outer-compatible admission (`accepted`, `processesClosed`, `admission`) from every `prior.rungAdmissions` binding. The new current entry publishes a joint record whose equivalent fields are under `process`. Feeding that joint record directly to the existing reader fails its declared contract. The first accepted rung's source bytes must remain stable while its receipts continue to bind them.

A new preparer can read the exact joint bytes, independently reviewed final rung record, and terminal external owner evidence; verify matching candidate/rung/plan, original deadline, zero exit, group closure, closed process and memory probes, and the complete accepted phase chain; then emit a **new** outer-compatible transport record carrying the unchanged inner `process` fields. It must name its original joint and review inputs and must not rewrite the joint, original inner record, old receipts, mathematical reference, or first-rung entry/rung/dispatcher. Known mismatch, missing-review, incomplete external closure, changed source, and wrong prior-rung controls must pass before preparing an actual later-rung input.

**○ Not done — Provenance binding through final publication.** The current entry's source role census is fixed and its Node image census must match actual loaded libraries. Its additional owner bindings contain only the plan, plan-review file, and prior-index file. Arbitrary extra path/hash objects in those JSON files are not recursively followed by the current entry. The rung itself rechecks the admission files named in `prior.rungAdmissions` during its shared checks, but the dispatcher's final admission does not independently rehash that entire prior-admission list after rung exit. Therefore merely appending external joint/review paths to an ignored JSON field does not supply whole-entry provenance rechecks and must not be presented as completed transport.

Two concrete implementation routes remain. A new, separately reviewed current entry can explicitly capture and finally recheck the normalized record and original joint/review/owner inputs while leaving the accepted first-rung entry unchanged. Alternatively, an independently reviewed self-contained prior-review packet can embed the exact original joint, final review and terminal owner/stdout bytes, with their original digests, and bind the normalized transport record's digest. Placing that complete packet in the existing `profile.planReview` file makes the actual consumed provenance bytes participate in the current owners' existing before-probe and final hash checks. The reader's 2-MiB file ceiling must hold; the first-rung joint alone is 271843 bytes by `wc -c`, before encoding and the additional review/owner records. The current entry does not interpret such embedded evidence fields: a separately checked preparer and independent review must establish the complete linkage, and no claim of runtime recursive path validation is permitted. Original external paths remain provenance labels; the captured embedded bytes would be the declared consumed records.

**○ Not done — Decision and acceptance of the selected route.** Neither route is supplied by the first-rung resource review, which authorizes only the named rung-8 run. The next concrete work is to choose and implement one reviewed prior transport, verify its known cases and original evidence bindings, obtain the separate rung-32 review, and create its exact profile. Rung 128 likewise requires both earlier accepted rung chains. The six candidates returned for exceeding 1800 seconds remain blocked independently of this transport issue.

## Current reusable prior preparation

**✓ Done — Reusable 32/128 transport implementation and known controls.** `prepare-current-subfield-circular-prior.mjs` prepares the same candidate's next scope from an independently accepted preceding rung. It supports rung 8 → 32 and rung 32 → 128, carrying the existing pilot/earlier phase order and preceding admission list. Five controls in owned run `c3383936-f6f4-4c5c-8876-45685a7bea90` pass before actual preparation, including known SHA and lossless compression, both schedule transitions, pending rejection, wrong closure and evidence digest, private-field projection rejection, changed terminal and phase evidence, changed projected admission, and the 2-MiB limit.

**✓ Done — Immutable current packet preparation.** Actual bounded run `a655f84d-d951-4ffa-a523-0e0e292ae277` closes with exit zero in 0.399 seconds and writes `current-prior32-preparation-20260908-a/next32-review.pending.json` at digest `2a4bf8a35641def34dcd5d80fa5f281c321234f649bff5e7e2655b8e76cc820b`, 96296 bytes. Original joint, independent review, stdout and earlier prior-index bytes are embedded using lossless gzip, with original size and digest verified on decode and a bounded 2-MiB decoded limit per item. The terminal record is explicitly a selected public projection corroborated by the independent review; the raw lease and private control token are not embedded or published.

**◐ Partial — Exact next-rung reviewed invocation.** The scoped review packet also carries the unchanged preceding `joint.process.admission` and `processesClosed` fields. Following independent acceptance, the finalizer makes that exact accepted packet the last `prior.rungAdmissions` entry, while the same path and hash occupy `profile.planReview`. Thus the frozen rung consumes its ordinary admission fields from the packet whose complete immutable evidence bytes the current owners recheck. The packet's top-level rung identifies the next execution scope; the projected `admission.rung` continues to identify the previous observed rung. The frozen entry does not structurally compare `profile.prior` against the review packet: independent verification of the exact immutable final profile and its CLI digest must establish that linkage for each actual invocation. This is not a generic validation claim for arbitrary profiles.

**○ Not done — Next rung execution.** The pending packet cannot pass the existing accepted-review checks. Independent packet acceptance, final prior-index preparation, exact profile verification and actual bounded execution remain sequential prerequisites. No existing accepted entry, rung, dispatcher, mathematical reference or original receipt was modified to create this transport.

**✓ Done — Next-32 accepted packet and exact profile preparation.** Independent packet review accepts a separate copy `next32-review.accepted.json` at digest `1361d54a6d5aaf3e3de41cf579f0dd0b2a45f018e26e5b2b8c3ce44a9a8725e8`; the pending packet is retained. Finalizer run `ce75fe4d-f79a-473e-b1ab-3ee1702092d6` closes in 0.082 seconds and emits the ten-phase prior index `6930989d57738a7cd13bcc546465dcfbe3efa1fe2473694eb8fd59e295c31966`. Profile `current-prior32-final-20260908-a/rung32-profile.json` has digest `14d487f42533d74154fc53b153e6a514b75a398013a6ca9db864b6d521f99b37`; its source/runtime bindings are the unchanged first-rung generation. Its last prior admission and its plan review select the same exact accepted packet. Independent exact-invocation linkage review precedes launch; completion cannot be inferred from these preparation outputs.

**✓ Done — Explicit host-fixture portability.** Actual Python/ps/memory observation tests now declare their reviewed macOS/shared-venv requirement and skip those specific fixtures on other operating systems. Portable parser, prior and resource controls remain unconditional. Owned serial run `655a3890-fcac-468c-86e0-743a6492d088` passes all 25 selected controls on this macOS host with zero skips and process-group closure in 6.249 seconds; [retained log](../evidence/circular-current-execution/current-portable-control-selection.log). No EOM fixture ran in this selection, no admitted source bytes changed, and this is not evidence that GitHub CI ran or that another platform passed.

**✓ Done — Actual rung-32 acceptance.** Owned run `7ccedb27-5e94-4d75-b3d2-104f191b403c` exits zero with a closed group in 307.203 seconds. The [independent rung-32 review](../evidence/circular-current-execution/current-rung32-independent-review.json) accepts its 32 phases and 1152 rows, 129 registered gates, 136 closed process probes and 23 closed memory probes. The original joint digest is `f325501de27237181f86633f2c37a07159a6af79eb38dc5dc6af95a2fc2805d1`; the independent review digest is `f0cdee75dccb913f16e327129b864f8deb9dfc2ea63d91e2df9b21ff5db58ad2`. These bytes and all admitted source generations remain unchanged.

**◐ Partial — Final rung-128 preparation and resource review.** The same frozen prior preparer consumes the accepted rung-32 chain in run `c4599bc2-75da-4ff1-824d-2f6dd7da94a0`, which exits zero with group closure in 0.337 seconds. Its pending 128 review packet is 324612 bytes, below the existing 2-MiB reader ceiling, and retains both earlier admission scopes. Independent packet/resource acceptance and the final exact profile are still required.

The process-observation byte budget needs explicit attention before the final run. Rung 32 records 16919066 observation bytes across 135 table probes and one 13760-byte inventory probe. Following known mean/max arithmetic controls, direct inspection of those recorded probe fields gives table observations from 124530 to 126030 bytes, averaging 125224.49 bytes. A nominal 128-rung gate census includes 512 phase gates and two summary gates, giving an estimated 521 total probes under the observed gate/probe relationship. Applying the largest observed table size to 520 table probes gives 65549360 bytes, below the unchanged 67108864-byte limit by 1559504 bytes. This is an estimate from observed samples, not a guaranteed bound on a future host process census or cleanup. Limit contact must reject as an operational resource return; neither the 64-MiB observation allowance nor the 1800-second deadline is increased by this projection.

**✓ Done — Final-128 accepted packet and exact profile preparation.** Independent packet review accepts `next128-review.accepted.json` at digest `1c4c18b62eb1a96ca359d1b18521142c5c4d04c27738fcb81e3dc8cb9979253b`, retaining the original pending packet. Finalizer run `3203f11d-1cbd-48a6-964e-6581e81e24d2` closes in 0.099 seconds and emits the 42-phase/two-admission prior index `f91cb2b262474a3cdfaecfcfb16d30c460a067fa9a7c06974290d90972dc8dfa`. The unchanged entry and runtime generation are selected by `current-prior128-final-20260908-a/rung128-profile.json`, digest `2f8de6267c7e897ee1516f6068917f462adef62f87243541f1b5bbdf556f8ad4`. Independent exact-invocation review checks that the final prior admission and `profile.planReview` select the same accepted immutable packet. The resource review permits a bounded attempt with the unchanged observation and time limits, preserving the narrow observed byte-budget margin and mandatory rejection on limit contact.

**✓ Done — Final-128 execution acceptance.** The [independent final review](../evidence/circular-current-execution/current-rung128-independent-review.json) accepts all 128 phases and 4,608 rows, and the complete 8/32/128 summary of 168 phases and 6,048 rows. Owned run `e155fb9a-fc62-4374-a8b7-77133a1928f5` exits zero with group closure in 1206.518 seconds; independent review run `df28da71-96fc-4230-ba6c-6a37e9bba853` also exits zero with group closure. The other nine within-cap candidates remain deferred, and the six over-cap candidates remain blocked resource returns. No full sixteen-candidate ladder, H3 result or overall test-health conclusion follows from completing the transport.

## Paused checkpoint and restart

**✓ Done — First candidate checkpoint preserved.** The independent final review checks 113 direct bindings, 1,298 dispatcher evidence bindings, 514 gates, 521 closed process probes and 83 closed memory probes. Actual process-observation storage is 65,418,366 bytes against the unchanged 67,108,864-byte cap. These measured records establish the selected execution and closure contract. They do not establish H3 eligibility, every candidate, overall test health or GitHub acceptance. Changed bound bytes or a failed unchanged acceptance instrument would overturn this scoped acceptance.

The operator instructed the current 128-phase run to finish, followed only by necessary review, validation and durable recording, then a pause of all recovery writers. No further rung, retry, expanded recovery or publication is part of this closeout. The source and original evidence generations remain frozen.

The completed output is `.local-data/braid-analysis/subfield-circular-root-pilot-20260827-v1/current-owned-rung128-20260908-a/`; its `current-admission.json` digest is `c980f9e308b39489a873789aac63229c1023c6807e540225d848b79e93151c7f`. The exact final profile is `current-prior128-final-20260908-a/rung128-profile.json` under the same pilot root, digest `2f8de6267c7e897ee1516f6068917f462adef62f87243541f1b5bbdf556f8ad4`. Preserve these ignored local outputs and earlier 8/32 profiles, admissions, and original reference inputs; committing documentation alone does not preserve those runtime files.

**○ Not done — Deferred restart.** After an operator instruction to resume, inspect this checkpoint and the current queue. This candidate needs no further rung or recomputation. The runner cannot resume an intermediate phase: rerunning a rung starts its complete loop. For another candidate, prepare and independently review its exact resource/profile selection and prior transport; the present prior preparer selects this first candidate and its 36-row-per-phase shape. Do not infer support for arbitrary candidates from this completed invocation. Nine within-cap candidates remain deferred. Six over-cap candidates remain blocked pending an explicit resource-return decision; keep the 1800-second and observation-storage limits unchanged.

**◐ Partial — Publication readiness.** Recovery implementation and this bounded checkpoint are ready for the designated runner's combined inventory and required publication validation after all participating writers have stopped. This closeout performs no Git writes and supplies no final publication-gate or overall-health receipt. Owner-specific generated repairs, missing originals, omitted input-dependent checks and the historical supervisor-cause investigation retain their explicit queue dispositions.

**✓ Done — Retention disposition; combined publication validation remains separate.** Final `validate-machine-artifact-retention.mjs` run `28f4e468-af14-4fe8-82ea-5a493b5cc5b7` rejects the evidence collection at 189 files, 100,152 lines and 5,148,110 bytes. The execution acceptance remains scoped as recorded; publication readiness requires a separately authorized retention disposition. Preserve current evidence during the operator-requested pause; no cap change or evidence rewrite was attempted.

The operator approved a narrow collection exception on 2026-09-08 after this failure. The [retention registry](../../../op/machine-artifact-retention-registry.v1.json) admits the measured 100,152 lines with an exclusive 100,153-line ceiling, preserves the existing byte limit, and identifies the current consumers and reason to retain their evidence. A fresh `node scripts/validate-machine-artifact-retention.mjs` passes index and working-tree file, collection, branch and generated-output checks; `git diff --check` also passes. No evidence was deleted or rewritten. This resolves the recorded retention blocker, not the required combined publication gate, and does not resume recovery writers.

## Resumed successor selection

**◐ Partial — Recovery resumed by operator instruction after rollover.** The earlier pause and retention failure above are historical checkpoints. The first candidate remains complete and is not rerun. The [next-candidate review](circular-next-candidate-review.md) confirms that the unchanged current entry can select the second candidate with its own two pilot phases and a separately reviewed eight-phase profile. Later 32/128 preparation still requires candidate-aware admission and independent review; no broader execution is inferred from the first candidate. The approved retention exception preserves the existing evidence collection; new local execution records remain subject to their current storage and retention contracts.

**✓ Done — Second candidate eight-phase execution accepted.** Independent review `d348ba5b-71df-4605-86ec-ba9148d1fefc` accepts eight phases and 288 rows from owned run `f05f74e2-900b-4e31-9df3-47fedc745545`, which exits zero with group closure in 81.157 seconds. The review checks 111 direct bindings, 40 closed process probes, eight closed memory probes and seven accepted resource observations. Full hashes and ignored local paths are retained in the [next-candidate owner](circular-next-candidate-review.md). The second candidate remains partial pending separately reviewed 32/128 preparation and execution; eight other within-cap candidates are unstarted and six remain blocked resource returns.
