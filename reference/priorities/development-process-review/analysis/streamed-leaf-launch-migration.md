# Streamed-leaf launch migration

Date: 2026-09-07. Completed at the streamed and active-parent operational boundary under the operator's instruction to continue the recommended diagnostic repair. Baseline: 21771de11164f95f129c42855a57dd699f4d0e4d on codex/peridot, with no changes by git --no-optional-locks status -sb.

## Findings and repair boundary

The current-binding inventory identifies two missed current selections in the streamed caller: the diagnostic helper and the coordinator generation preceding its diagnostic-helper update. Git diff 713c53a0a 7937a8733 over the coordinator establishes that the latter difference is a dependency selection, not a different completion protocol. A mistaken attempt to use a SHA-256 content digest as a Git revision failed and produced no attribution evidence; the commit diff supplies the attribution.

With those selections repaired, a bounded synthetic normal-return fixture rejects at the registered supervisor's ownership-record cap. Its command contains the captured caller source, encoded as a bootstrap argument. The supervisor shortened every individual string to 65,536 characters and marked the record incomplete even when it fit the existing 900 KiB complete-record cap. Preserving exact strings under the unchanged complete-record and 8 MiB aggregate caps makes the normal streamed fixture pass. This repairs evidence capture rather than raising the total allowance or accepting truncated evidence.

The historical invocation migration explicitly selects version five. It forwards the adapter's version-one retained-evidence selection and a declared complete physical source union, verifies the consumed union exactly, and records the selection and historical-verification limitation in stream metadata. Original version-four invocations and frozen scientific records remain unchanged. This interface passes the scoped transport and process controls; it is not an accepted scientific launch.

## Process test containment

The old readiness Boolean blocked every whole-process fixture. The validated replacement uses a copied, unchanged owned-compute supervisor in each private synthetic checkout, with a 15-second hard deadline and one-second termination grace. A wrapper preserves actual stdout, stderr, cancellation and broken-pipe behavior. The tests require external process-group closure and absence of the coordinator and registered target before cleanup; unresolved directories are retained. A timeout remains a rejection, never a successful completion. The independent observer also checks every observed descendant and process group before cleanup, as described below.

## Validation in progress

Initial owned runs 1ded2fb9-8313-4929-89c7-e321ba4a3852 and 5c45ee5c-61af-45c4-95b8-3a5d455fd3f9 failed at the ownership-record cap. Run 2df622ff-f53d-4db8-99f7-a1aa0fb8826a reproduced that rejection after the invocation migration. Run 4d36fc21-ca74-4cb7-ab19-cb77319e5081 passed dependency identity and the normal streamed fixture; its additional long-command control failed because its default process observer used executable names rather than full arguments. That control now requests full arguments. These were intermediate results; the final combined result is recorded below.

No numerical reference, independent mathematical oracle, historical receipt, Git publication or scientific batch is changed or activated by this work. Current dependency propagation and final source checks remain required before completion.

## Subsequent controls and integration findings

The first complete streamed suite ran 67 tests: 62 passed and five failed. Four tests expected the prior coordinator's terminal stderr message, which the current protected final boundary does not emit. A fixture-only witness now records the unchanged host parser's actual rejection at the point it throws; tests verify that record and whether work had started. The fifth expected lock release after an oversized private stream, although the current quota rule forbids further publication and release from that invalid layout. The revised test requires a rejected exit, a retained lock, the actual over-quota private layout, no public stream or new rejection publication, and external target/group absence. It does not count a timeout as successful completion.

The complete source transport uses the [version-five contract](../contracts/streamed-leaf-historical-invocation-v5.md). Four literal transport controls passed before the retained target. The unchanged adapter constructs 160 parents with 265 physical source bindings and zero numerical calls, then closes with final file checks; the JavaScript transport accepts the same 265 bindings and 36 archive routes. The complete original environment remains unverified. A separate captured-Python fixture proves that the selection reaches the adapter and frozen stream metadata; another rejects an unconsumed physical source before any provider call. An initial header assertion looked for a decoded claim in the encoded envelope; using the unchanged codec's semantic decoder corrected that test.

An independent test observer now tracks actual coordinator descendants and observed groups from bounded host process snapshots. Literal graph, reparenting, and reused-birth controls run before host observation. Each fixture requires the external supervisor's group closure and actual absence of every observed process/group before cleanup. Real host memory and disk admission precedes each fixture; the coordinator continues to measure real RSS while its synthetic host inputs exercise exact threshold boundaries. The separate streamed run passed 69 tests with no skips or cancellations, in 204.082 seconds by its owned lease. All observations concern synthetic processes and operational behavior.

The combined test command initially passed 119 of 158 tests and rejected the 39 streamed process fixtures before their intended boundaries. Its parent command line named f6c-bounded-operation.test.js, which matched the coordinator's unqualified competitor-name expression. The corrected expression distinguishes that test filename from the actual coordinator command and retains rejection controls for the bare coordinator name and its real .mjs entry. After that precise correction, the combined run passes all 158 tests.

## Dependency scope and remaining work

Current dependency changes cover the registered supervisor, its shared helper, diagnostic helper, generic coordinator, streamed caller, and active parent composition/preparer/external-checker selections. The external checker algorithm is unchanged; only its externally selected coordinator digest advances. Mathematical modules, the scientific adapter and its references, and preserved historical source/receipt bytes are unchanged. The [binding inventory](../evidence/streamed-leaf-migration/binding-inventory.json) records the starting current selections.

Other generation-bound launchers and the frozen operational declarations in old Python readers still name earlier helper generations. They must be classified against their original contract before any new use; they are not asserted current or passing, and their historical expectations must not be rewritten as routine pin maintenance. The shared-helper edit makes the remaining current-caller audit necessary before relying on another launcher. This task validates the streamed and active parent boundary, not every older launch protocol or the complete test collection. The broader review must decide which old protocols have current consumers, migrate those that do, and preserve historical readers independently.

A failed final source check, acceptance of an undeclared executable or unused source, an observed process surviving fixture cleanup, a timeout accepted as successful completion, or a promoted scientific flag would overturn the relevant completion claim. The targeted controls and retained run logs provide the checks. No scientific batch, broad sweep, commit, or push is authorized by this migration.

## Final verification and completion boundary

The [retained validation records](../evidence/streamed-leaf-migration/validation-runs.json) preserve eleven owned runs, including failures. Final run `483015ff-0495-4acf-8868-1edc9cd5a347` passes 158/158 tests, with zero skipped/cancelled, in 228.193 seconds by its terminal lease. It covers the streamed diagnostic, literal historical transport, registered supervisor, bounded coordinator, external closure checker, parent composition, and batch preparer. The [external fixture observations](../evidence/streamed-leaf-migration/fixture-closures.json) retain the closure verdict and observed process identities for all 39 whole-process fixtures.

The [final source check](../evidence/streamed-leaf-migration/final-source-check.json) verifies 24 active dependency edges and coordinator-selection agreement. Scoped `git diff --exit-code HEAD` verifies eight preserved source/evidence scopes, including mathematical references, the scientific adapter, codec and previous archives. The [retained transport result](../evidence/streamed-leaf-migration/retained-transport-result.json) and [transport admission](../evidence/streamed-leaf-migration/retained-transport-admission.json) retain the metadata-only 265-source/36-route result.

The source searches in [remaining generation occurrences](../evidence/streamed-leaf-migration/remaining-generation-occurrences.txt) cover the three replaced helper digests under scripts/ and tests/. These are occurrences to classify, not a count of defects: frozen historical declarations and current executable selectors have different obligations. The remaining queue item is that broader current-caller audit. No failed test was skipped or relabeled irrelevant, and no whole-repository pass is claimed. A mistaken housekeeping command named a nonexistent validate-owned-compute-launch-policy.mjs; the actual check-owned-compute-launch-policy.mjs subsequently passed.

Final housekeeping: strict content validation reports zero errors and zero warnings; priority ranking, machine-artifact retention, startup-router freshness, owned-launch policy and tracked whitespace checks pass. The [owner closeout](../evidence/streamed-leaf-migration/owner-closeout.json) returns clear after the final owned run. Final active source identities were rechecked after documentation capture; the preserved mathematical/evidence scopes remain unchanged.
