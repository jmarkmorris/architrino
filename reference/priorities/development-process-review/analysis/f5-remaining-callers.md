# F5 original-input caller execution

The current-input problem has two distinct consumers. The display uses the current version-three assembly schema and canonical member names. The frozen F5 preparation, prefix, ledger and Python conformance instruments require the original version-two scientific input bytes at their declared logical names. Replacing the current display file with those originals would break the current schema, while advancing the frozen input digests would change the question the independent instruments check.

## Explicit execution root

[The input-tree materializer](../../../../scripts/eom/prepare-f5-original-input-tree.mjs) provides an explicit production execution root. It copies the current consumer/reference source files without alteration and places the five authenticated original data objects under their required logical names inside ignored storage. The existing production CLIs then execute normally from that root; no test-only validator entrypoint, injected reader, schema projection, reference-code modification or historical digest substitution supplies their authority. This is a filesystem transport, not a second implementation of any F5 calculation.

The materializer checks each original against its independently fixed SHA-256 identity and declared byte count. It rejects duplicate, missing or altered route identities, noncanonical route names, symlinked files/parents, nonregular files and an existing output root. File opening is nonblocking so a FIFO is rejected by `fstat` instead of hanging before that check. It verifies source census and captured bytes again before writing its last materialization record. `--check-root` compares the complete record and copied files with the current source census and exact originals. Partial directories without that record are unusable. The record explicitly has `accepted: false` and `scientificAcceptance: false`.

These checks establish path-based byte consistency at the measured boundaries. They are not atomic runtime authentication and do not bind an interpreter, dynamic loader or compiler execution. A source change in the origin invalidates the current check and requires a fresh root. Each consumer retains its own identity, build, proof and output-acceptance obligations. The copied source profile covers the named F5 preparation, prefix, ledger, interpolation and two Python conformance interfaces; it is not a claim of complete repository dependency capture.

From the repository root, the production interpolation path is:

```bash
node scripts/eom/prepare-f5-original-input-tree.mjs --out-root .local-data/f5-original-input-trees/NEW
node scripts/eom/prepare-f5-original-input-tree.mjs --check-root .local-data/f5-original-input-trees/NEW
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 60 --cwd .local-data/f5-original-input-trees/NEW -- node scripts/eom/derive-f5-independent-interpolation-enclosure.mjs --out interpolation.json
node scripts/eom/prepare-f5-original-input-tree.mjs --check-root .local-data/f5-original-input-trees/NEW
```

The same `--cwd` root supports the unchanged `prepare-f5-enclosed-root.mjs`, `verify-f5-enclosed-root-prefix.mjs` and `reduce-f5-enclosed-root-ledger.mjs` CLIs with their documented arguments. Pass that execution root as `--repo-root` where requested, and supply the shared venv as an absolute `AAA_VENV` for preparation; the root's adjacent directory is not the shared venv. A full preparation requires its own fresh build, complete nominal proof and independent build/API-domain review. The prefix and final reducer require actual accepted history and rung inputs. Materialization supplies none of those results.

## Measured results

The [recorded runs](../evidence/f5-remaining-callers/runs.json) retain exact commands, timing and terminal process closure; adjacent logs contain the runner's native test summaries. The [known controls](../evidence/f5-remaining-callers/known-control.md) preceded actual materialization. Later controls additionally reject a FIFO, original-byte substitution, false size, path traversal, an omitted materialization census and changed copied source. The final four transport controls pass in run `8ee2039d-15c6-4423-8d34-459c820f30b4`.

The first combined run `1de57d47-c0be-4d78-8b98-343c5b1a902d` produced 45 Node tests: 42 passed and three failed, with no skips or cancellations. It demonstrated why display validation must remain on current inputs: the two display cases rejected original version-two data through the current version-three validator. It also exposed a receipt comparison that demanded an old instrument digest and config name from a current instrument. These failures were retained rather than reclassified as passes.

The final [execution-root record](../evidence/f5-remaining-callers/execution-tree.json) lists 84 captured files. Native Node output in run `7003641d-643a-435a-b24a-b4cdf7142c2d` reports all 41 unchanged preparation, prefix and ledger controls passing from that root, with no skips or cancellations. Native unittest output in run `49e84cb7-59c5-465d-917e-f5d88b6acda3` reports all 32 unchanged API-domain and manifest controls passing under the shared Python 3.13.2 environment. A final `--check-root` passes after both selections. These results cover the explicitly routed commands; the ordinary live-path copies of those tests remain unsuitable for the historical inputs.

The actual production interpolation CLI executed in run `6d133a31-d782-4298-8acb-a1ec218d1a6d`, exited zero with process-group closure after 1.77 seconds and reported `independent-enclosure-passed`, `accepted: true`, and all six falsifiers false. Its [recorded result binding](../evidence/f5-remaining-callers/interpolation-result.json) has SHA-256 `b466091d28642c51bf108ed15c589c594e869c50b9a4fe76411fe64ec71a3bec`. `diff -u` against the retained accepted original report finds only two metadata differences: the executing instrument's digest and the config's renamed logical path. It does not reproduce the original receipt bytes. The interpolation regression now runs the actual production CLI through explicit original-input routing, checks those two current metadata identities, and compares every remaining serialized byte with the original by a disclosed private metadata projection. The produced receipt is never rewritten. This comparison is regression reproduction, not an independent second mathematical proof.

The current display regression now reads the authenticated archived pilot and compares all seven complete scientific fields after an explicit mapping of member, worldline, relationship and validator names to the current schema. Numerical values are not projected or omitted. The current display still passes its real schema validator, speed bound and labeled-return controls. These two display controls and the two interpolation controls pass in run `47393bd9-03a6-447a-8266-c18094ecce0a`, alongside the then-three transport controls. The later fourth transport control is recorded separately above.

## Remaining obligations

The explicit input transport and current display/interpolation regressions are implemented. The follow-through below now supplies actual complete nominal and API-domain certificates, an independently admitted captured enclosed-root build, both accepted genuine prefixes and an accepted three-rung ledger reduction. The [separate final receipt review](../evidence/f5-remaining-callers/final-run-review.md) accepts the bounded execution; the original runner receipt preserves its recorded `prescribed-root-ladder-passed-pending-review` status without being rewritten after review. The existing preparation/prefix/ledger and Python test commands must be scheduled against the explicit original-input root when reconciling final coverage; their ordinary live-path failures have not been erased by these scoped runs. These measurements establish neither overall repository health nor H3, EOM evolution, retention, stability, binding or new-parent consumption acceptance.

The falsifiers are direct: altered original bytes accepted by the materializer, a changed copied source accepted by `--check-root`, any difference beyond the two disclosed interpolation metadata fields, a numerical change surviving the display comparison, a failing named regression, or a required consumer dependency missing from the execution profile would overturn the corresponding scoped claim.

## Actual acceptance-chain follow-through

The next authorized pass executes the actual chain. Owned run `181ee36f-66d1-4b7d-bb1e-909e441de9ab` completed the unchanged full preparation in fresh original-input root `acceptance-20260908-a`, including a fresh build, an actual 12,384-segment history manifest and the unchanged independent nominal-history oracle. The nominal proof reports `actual-cubic-conformance-passed` after checking every segment in 180.799 seconds; the outer preparation closed its process group after 205.955 seconds. Its manifest digest is `bbfb55e2c789b5e6f034745c8e7e1b3a7d502b97082862558fb8eb4c6a707749`; the nominal certificate digest is `dc65ce6a3c50caa75e11001a0dd48f3250516ea55e315fffb896d29cc2a32a57`. Neither receipt is a root-completeness or current-build acceptance result.

The current API applicability boundary remains separate from source identity: the adapter and CMake changes must be reviewed against the frozen API proof, while the four History/Interval source/header identities remain authored bindings. The API oracle runs unchanged in a distinct reference-only tree containing the original adapter and CMake objects as data, together with the actual current manifest and accepted nominal certificate. No original receipt is relabeled as current.

The legacy preparation's toolchain receipt records the compiler shim and selected source/library files, but does not establish the current before-and-after compiler/header/tool census. A new [focused enclosed-root builder](../../../../scripts/eom/prepare-f5-enclosed-root-build.mjs) composes the already reviewed dependency, file-binding and invocation helpers with the same watched build stages used by the handoff build owner. It records resolved compiler, linker, archive tools, actual make driver, shell, SDK/header census, external libraries, compile commands, actual dependency files and file-backed runtime libraries. macOS shared-cache libraries remain explicitly platform-trusted. Five argument/source controls passed before its first execution. The first captured build completed but independent review identified an omitted make/shell binding; that receipt is retained without final build acceptance. The next attempt rejected an untyped CMake cache field after configuration; the corrected builder declares `CMAKE_MAKE_PROGRAM:FILEPATH` explicitly and starts a fresh output directory. It does not weaken the field check or reuse the rejected build.

The [actual results and artifact identities](../evidence/f5-current-acceptance/results.json) now record both completed proofs and the final captured build. Unchanged API proof run `fdc7a8cd-7f3a-49b3-bbb3-9edea9cd777b` checked all 12,384 segments and closed successfully after 194.609 seconds; its accepted receipt has digest `46e073619800a15b7690a83b394f3af3be3cdfc4471a39e6e8bb3b0366d0d7a9`. Final build run `22a59898-afb3-4297-a111-68e0147b30e9` closed successfully after 23.743 seconds; its receipt has digest `b6086295446e9b447226cc0aafa401bb743281945ef613e66e6f15d59eb58e84`. Run `a55db104-8520-42b2-855c-9bbfe2e56d14` then used that freshly captured executable to generate the same campaign/run manifest. `cmp` and the evidence collector's byte comparison verify exact equality with the manifest already certified by both proofs. This licenses reuse of those exact data certificates, rather than inferring equality from a source comparison.

The unchanged ledger rejects parent-directory traversal in implementation binding paths. Therefore the executing binary and library are copied, byte-for-byte with mode preserved, into the original-input root's own ignored storage; [execution artifact identities](../evidence/f5-current-acceptance/execution-artifacts.json) bind those copies to the captured originals. The current runner must verify both sets and use the safe local paths. These copies do not create a new build generation.

## Actual prescribed-root ladder result

The current-build admission binds the reviewed source applicability, complete captured build, original API receipt, actual manifest and nominal certificate, exact safe execution copies and current runner identities. It preserves the original proof's source identities and grants only the scoped current execution applicability documented by the reviewer.

Owned run `efd61ef6-af27-47c9-874c-058a3b7fdbb7` executed the actual fixed 8/32/128 ladder through [the current wrapper](../../../../scripts/eom/run-current-f5-enclosed-root.mjs), with its original per-rung limits and an outer 5,500-second deadline. It passed on its first ladder attempt, with no parameter tuning or accepted-failure substitution, and closed successfully after 51.866 seconds. The actual producer reported complete passing row censuses for all three rungs. The unchanged independent prefix verifier accepted the genuine `[8]` and `[8,32]` prefixes; the unchanged full ledger reducer then accepted `[8,32,128]` with `totalRows: 24192`, `structurallyAccepted: true` and `h3EvidenceEligible: false`. The [final ladder result](../evidence/f5-current-acceptance/ladder-results.json) records exact commands and receipt identities; the [original reduction receipt](../evidence/f5-current-acceptance/ledger-reduction.json) preserves the instrument's complete bounded claim.

The full reducer's retained `/usr/bin/time -l` output in [check-128.log](../evidence/f5-current-acceptance/check-128.log) records 13.15 seconds of wall time and 3,891,920,896 bytes maximum resident size for that child. Those measurements apply only to the final reducer process, not the whole campaign or every process. The complete ladder remained below its fixed time limits. No remaining process from this ladder was left running according to its owned lease's terminal group-closure result.

The [independent final review](../evidence/f5-remaining-callers/final-run-review.md) accepts this bounded execution after checking 2,355 bindings, all six successfully closed stages and their logs, both original prefix receipts and the final 24,192-row reduction. This closes the actual F5 execution chain requested in this pass. The result does not promote H3 eligibility or establish EOM evolution, retention, stability, binding, new-parent consumption or overall test health. The earlier rejected builder attempts remain recorded; no bound admission, source-review or historical proof artifact was edited after the admitted run.

## Current build admission continuation

The current runner adds an explicit independently reviewed build-admission transport while retaining the historical entrypoint's default checks and all original API, prefix and final ledger predicates. The current wrapper preserves the original API receipt and binds a separate applicability review, current source identities, captured build, actual manifest and nominal certificate. The full dependency census is rechecked at every rung.

Before target use, `tests/f5-current-build-admission.test.js` verified the binding instrument against the known SHA-256 of `abc`, rejected changed `abd` bytes, and rejected an arithmetic-source change outside the reviewed API applicability transition. The owned serial Node run `9d3ea306-ebac-4ea7-b4b2-319b61812e57` also ran the existing F5 runner controls; it exited zero with a closed process group. The final wrapper, original-runner and new-builder selection in run `086e8cc7-55c0-42da-94bf-211744c316f8` reports 17 passing tests with no failures, skips or cancellations. These controls establish transport behavior only. Actual current-build admission and the 24,192-row ladder are now completed and accepted by the [separate final execution review](../evidence/f5-remaining-callers/final-run-review.md), at the unchanged no-H3 boundary recorded above.
