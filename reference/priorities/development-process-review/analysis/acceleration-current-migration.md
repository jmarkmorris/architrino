# Acceleration current execution transport

Current source admission now uses Option B under the [paired migration record](option-b-prescribed-response-and-acceleration-cutover.md). That later operational transfer preserves the scientific and historical boundaries measured in this transport record; its newer test counts and manifest identities are recorded separately.

The acceleration interface now passes the declared operational controls without changing the numerical subject, independent rational comparator or range reference. `git diff --exit-code` over those three source files reports no changes, and the current binding tests authenticate their selected digests. The new `scripts/eom/execute-f6c-acceleration.py` captures and invokes their existing numerical functions. These results establish current transport and historical-record authentication within the tests below; no new scientific evaluation or acceptance is claimed.

## Contract and provenance

The outer plan requires `braid-program/f6c-continuous-reception-acceleration-launch.v2`. Three explicit additions bind the execution bridge, the original declaration's physical archive, and the two original theorem archives. Archive routes accept only the named original roles, exact original digests and sizes, and canonical relative `.source` files under `reference/`. They cannot redirect an executable. All numerical code remains at its canonical source path and reviewed digest. Captures hold file descriptors through final rechecks and reject symlinks, changed bytes and replaced files.

The bridge supplies the unchanged instruments with an in-memory projection containing their version-one scientific contract fields. No substitute version-one plan file is fabricated. Candidate and comparison records bind the actual version-two plan bytes, which preserve physical archive locations and the bridge identity. Their existing conditional schemas and limits on authority remain unchanged. The bridge owns metadata authentication and execution transport; its shared use by both stages is not independent evidence for its own correctness. Separate positive and rejection controls test those responsibilities.

## Historical authentication defect

The original cached-root receipt binds the root theorem at `f20e4bdaaff8b6f0012fdc6135b15d568a817832fb55d5c42f80d8421a117f68` and reconstruction theorem at `6abbbbacc1671052bdd881790094dbd71ebb03d54904ac1f937edae1f3c9f936`. Its prior plan binds verifier `19c57e9b638b0beb866c86b061b2325f9567add2a85608f0c42ef1f7612d9132` and declaration `7c2a8b0bb06f46da158e0dfe2cb313dd72e2edff3c411e87c1588aa6d028f9e4`. Reading the fixed receipt and prior-plan fields establishes those identities. The retained originals are not interchangeable with current files at the same paths.

`git diff 897fe1aa7^ HEAD` over the acceleration subject and verifier shows earlier changes to fixed document selections and historical authentication literals. Their resulting predicates expect newer verifier and declaration identities from a prior plan whose bytes still name the originals. This is a historical authentication mismatch, not evidence of a mathematical error. Current source capture alone did not detect it: the early metadata check validated plan fields and hashes without entering the prior-record authentication path. The full synthetic execution exposed the theorem mismatch, and the expanded metadata check then exposed the prior verifier mismatch. Both failures remain in the evidence directory.

The current bridge restores the original prior identities in its metadata authentication while retaining the existing receipt-chain, census, claims and process-closure predicates. It routes theorem reads to the existing source archives and retains their original logical paths in scientific bindings. It does not modify the numerical modules, original plans, receipts or archived sources. Candidate comparison then calls the unchanged reconstruction and range-comparison functions. Inspection of the emission verifier and refined subject finds the same newer literals in their historical predicates; those callers remain separate repair obligations.

The first metadata attempt also rejected the closure-owner selection. `git show 897fe1aa7^:<resource-plan-path> | shasum -a 256` identifies the earlier owner as `daeb71bee6260c38a6b7e5e6237110216d9315807fe23602fbd7cfcdddc5866b`; both unchanged instruments require `8263f700a35af04b07690c81c17e0d1078eadb1fb32550cc60226b6efa0f6378`. The corresponding file diff changes its title and named concurrency participants, preserving elapsed time, exit and closure evidence. The current plan uses the owner required by those instruments and retains the recorded observation; it does not claim a fresh pilot.

## Measured controls

- The [final scoped test output](../evidence/acceleration-migration/b8ab8cc0-7a8f-4d5d-87d4-c6152c4bb69b.stdout.log) and [Python output](../evidence/acceleration-migration/b8ab8cc0-7a8f-4d5d-87d4-c6152c4bb69b.stderr.log) record 28 passing Node tests and seven passing Python tests. The Python controls exercise both full transport stages with explicitly artificial numerical callbacks, actual original-record authentication, actual source captures, publication and rechecks. They reject changed candidate bytes, promoted authority, wrong census and wrong plan identity; a simulated post-publication failure retracts only the owned public output and retains private diagnostics.
- The [final metadata output](../evidence/acceleration-migration/a037030a-b46a-4fff-8410-2a9a8edaf578.stdout.log) records successful current-plan and historical-chain validation for both stages with zero numerical calls. The [current plan validation](../evidence/acceleration-migration/current-plan-validation.json) retains the 191 deduplicated bindings and actual plan identity. This isolated production entry also checks runtime discovery, which the synthetic test replaces to avoid importing the test runner's runtime into the production census.
- The earlier [Python family run](../evidence/acceleration-migration/1dd1e633-c0c5-4819-84a4-e6618953ce4c.stderr.log) records 170 passing tests selected by `test_f6c*acceleration*.py`, including existing numerical controls. That run preceded the final transport controls and does not substitute for them.
- The [launcher and process controls](../evidence/acceleration-migration/5072ebb3-1164-49fa-b257-b0fd07cff76f.stdout.log) record 34 passing tests using artificial scientific targets. The [external observation](../evidence/acceleration-migration/external-process-observation.json) separately records sampled process and group absence after process controls. Known parser, graph and PID-reuse controls passed before host observation; finite sampling cannot cover every arbitrarily short-lived process.

One procedural correction matters: the restored historical validator was first run on actual records before its dedicated known-case test was added. That first success is not used as validated evidence. Known positive and negative historical-chain fixtures subsequently passed; the real metadata checks were then rerun, including the final run linked above. The retained failed and superseded runs preserve the sequence rather than rewriting it as an uninterrupted pass.

## Remaining acceptance

This closes the scoped acceleration transport obligation: current dependency capture, original historical authentication, both synthetic stage outputs and rejection/cleanup controls. A new numerical launch still requires its declared bounded acceptance and independent comparison; synthetic callbacks establish no numerical result. Emission/refined new-run transport, F5/circular-ladder contracts and complete recovery test reconciliation remain open. An accepted executable archive route, accepted mismatching historical identity, lost capture, unbound actual plan, or unretracted owned output after a failed final check would overturn the corresponding transport claim.
