# Prescribed-response and acceleration Option B transfers

Current status: this record preserves its migration-stage evidence and commands. The former standalone profile test is now [retired into the required B suite](option-b-repository-reconciliation.md#authorized-retirement-cleanup-and-full-b-direction); links to its source select the preserved Git revision.

## Result and authority

The operator requested the next two migrations, followed by the remaining repository-wide inventory and finite plan. Prescribed-response and acceleration now use the same externally selected source-map admission protocol as the three previously migrated root-cover profiles. The [shared transfer checker](../../../../scripts/equation-mapping/check-current-source-maps.mjs) passes all five profiles, and [the former A-loop test](https://github.com/jmarkmorris/architrino/blob/859f2b07cb17889ca2c239d82fd61455c2ba903c/tests/current-launch-bindings.test.js) now asserts that exact five-profile transfer rather than keeping a vacuous empty loop.

The [remaining migration plan](option-b-remaining-migration-plan.md) completes the requested inventory/planning step. It defines five bounded work packages, not five individual file migrations. Scientific acceptance, mathematical-map baseline approval, generator writes and Git publication are not included.

## Retained baseline and source roles

Both descriptors use the independently retained A generation at commit `7c957f86c1ca8c7ff8d4cf8341f2b3e2748b4b09`. The checker retrieves each entry with `git show`; the new [restricted role-table extractor](https://github.com/jmarkmorris/architrino/blob/859f2b07cb17889ca2c239d82fd61455c2ba903c/scripts/equation-mapping/legacy-role-bindings.mjs) reads its quoted constants and ORIGINALS/FIXED construction without executing historical code. Before target extraction, a known role-table fixture returned the expected bindings; before edits, extraction of the two actual A entries agreed with their inspected exported maps at 21 and 24 entries. The regression suite retains that known-case check and rejects unknown constants and malformed rows. This extractor supports these two fixed source constructions, not arbitrary JavaScript.

| Profile | Complete A bindings | Current-source transfer | Retained evidence | B source records / relationships |
| --- | ---: | --- | ---: | --- |
| Prescribed-response | 21 | 12 inherited identities unchanged | 9 | 15 / 29 |
| Acceleration | 24 | 12 inherited identities unchanged plus one explicit migrated operational helper successor | 11 | 16 / 31 |

Counts and identities are measured by `node scripts/equation-mapping/check-current-source-maps.mjs`. The three additional B records identify the admission entry, launcher and captured manifest reader. The acceleration helper is the prescribed launcher, so its byte change is an explicit dependency of this paired migration, not permission to refresh scientific sources. The checker obtains that successor identity from the prescribed profile's launcher record and checks the retained old helper selection before allowing this one substitution. Every other inherited current-source value must equal A.

- Prescribed [baseline](../contracts/option-b-prescribed-response-baseline.json) and [manifest](../contracts/option-b-prescribed-response-sources.jsonld): tested manifest SHA-256 `16331077cd8da6d1921d72931748094a7e5245faee846ebc4f4e52618b336553`.
- Acceleration [baseline](../contracts/option-b-f6c-acceleration-baseline.json) and [manifest](../contracts/option-b-f6c-acceleration-sources.jsonld): tested manifest SHA-256 `2bff3175b916a92816b939f06000d681438cac84344871a56bdc61e8be9ffdf0`.

These recorded digests identify tested bytes, not a standing launch authorization. A future selected invocation must supply the examined candidate's map, entry, launcher and plan identities.

Prescribed-response retains six local numerical inputs plus three repository evidence identities: the selected phase-varying history configuration, source record and original response predeclaration. Acceleration retains eight local inputs plus the saved pilot launch plan, prior full resource-plan observation and original acceleration predeclaration. Existing explicit physical archive routes and theorem provenance remain unchanged. Evidence classification follows the caller's role, not whether the path is in the repository. ORIGINALS and FIXED still enforce scientific/frozen input selections; their remaining literals are not a second live operational PINS map.

## Admission and process integration

Both entries require initialized B state before plan validation, stage construction and combined source binding. Initialization requires an external `--source-map-sha256`, captures the manifest and its declared reader, validates the profile graph, checks every source, then rechecks capture-to-use identity before exposing SOURCE_BINDINGS. Failure clears previous state. The manifest and all admitted source records enter ordinary preflight, between-stage and final source checks.

Both launch and stage CLIs require the new argument. Captured workers initialize their own state with the same externally selected identity. The prescribed launcher's shared worker helper conditionally initializes entries that expose B admission; unmigrated entry workers keep their existing interface. Acceleration now captures its helper against the admitted source record rather than a second launcher literal.

The entry/launcher diffs and selected unit/process controls preserve compute-before-publisher or consumer-before-comparison sequencing, original scope/census, 1800-second inclusive deadlines, 2 GiB aggregate RSS and original observation/host/output bounds. No scientific production computation was executed. The scientific consumer, publisher, producer, independent comparators, oracle directory and variable-cell adapter have no edits in the scoped `git diff --numstat` over those paths; this is a source-preservation statement for those paths, not a repository-wide clean-tree claim.

The shared admission tests exercise actual captured entry/worker source checks and all 20 directed cross-profile substitutions. Actual CLIs reject an untrusted map before reading a deliberately absent plan; the correct map advances to that absent-plan error. Those controls prove source admission ordering, not completion of a real scientific launch. Synthetic process fixtures test transport, observation, cancellation and completion behavior. The prescribed unit fixture combines its synthetic plan with admitted actual-repository metadata; it tests binding composition, while the copied-root shared tests establish actual source admission.

## Necessary dependent A updates

The prescribed launcher is a shared helper. Leaving its callers at the previous helper identity would reject their current execution. The following dependency-only changes close that integration; **none is counted as another completed B migration**.

| Current A consumer | Scoped update |
| --- | --- |
| `scripts/eom/execute-f6c-emission-refinement.py` | Current infrastructure helper expectation only; numerical implementation untouched. |
| `scripts/eom/run-f6c-emission-refinement-pilot.mjs` | Helper identity and the resulting execution-bridge identity. |
| `scripts/eom/launch-f6c-emission-refinement-pilot.mjs` | Helper identity only. |
| `scripts/eom/launch-f6c-refined-acceleration-pilot.mjs` | Helper identity only; historical original-generation table in its entry preserved. |
| `scripts/eom/f6c-bounded-operation.mjs` | Helper and diagnostic-launcher identities. |
| `scripts/eom/run-f6c-parent-emission-refinement-pilot.mjs` | Helper, diagnostic-launcher and coordinator identities. |
| `scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs` | Same three infrastructure identities. |
| `tests/test_f6c_emission_current_execution.py` | Synthetic current-helper expectation matching the production infrastructure contract. |

The direct diff over these eight files contains digest substitutions only. The new helper identity is `fc758e8ffc1feaa566cc8e1849f73a0c8907f5ad7567e43fffd151eeceac30a1`; bridge `23a53f2f3167cd6690ea88f35edfb51c623c73458fff3ce9693663f8de876895`; emission diagnostic launcher `8a7759d803c49470f471ef4e58171bbe010466aaf0d7abe92f6f9897881bbbb0`; bounded coordinator `6d544440f81267564f8e05eca4b0c6889bdb20ed079dbac02887b331140e349d`. The retained A baseline and historical receipts are not rewritten. A final exact-hash `rg` search under `scripts/` and `tests/` for the old helper, bridge, diagnostic-launcher and coordinator identities finds only the deliberately retained helper baseline assertion in the transfer checker; this scope does not claim absence from historical evidence elsewhere.

## Verification

Before edits, the serial baseline selection of prescribed entry/launch, acceleration, shared admission and legacy binding tests passed 85 tests. The final selections below ran to completion without failures, cancellations or skips:

| Selection | Measured result |
| --- | --- |
| Main B/admission, five-profile unit and publication/reporting regression command below | 209 passed; 7.210 seconds |
| Dependent emission/refined/bounded/parent/streamed-leaf selection below, with host process visibility | 178 passed; 218.554 seconds |
| Prescribed-response and acceleration process suites, with host process visibility | 19 passed; 15.813 seconds |
| Emission-refinement and refined-acceleration process suites, with host process visibility | 24 passed; 19.663 seconds |
| Shared-venv `test_f6c_emission_current_execution.py` | 11 passed; 2.026 seconds |
| Shared transfer checker | All five profiles pass; the preceding three manifest identities remain unchanged |
| `node scripts/validate-content.mjs --check --strict` | Passed; zero errors and warnings |
| `node scripts/check-owned-compute-launch-policy.mjs` | Passed; registered launch-site census retained |
| `git diff --check` | Passed after documentation integration |

Commands:

```bash
node --test --test-concurrency=1 tests/current-source-manifest.test.mjs tests/current-launch-bindings.test.js tests/option-b-root-cover-admission.test.mjs tests/prescribed-response-pilot-entry.test.js tests/prescribed-response-pilot-launch.test.js tests/f6c-acceleration-pilot.test.js tests/f6c-root-cover-pilot.test.js tests/f6c-cached-root-cover-pilot-launcher.test.js tests/f6c-cached-root-cover-full.test.js tests/content-integrity-reporting.test.js tests/pr-validation-receipt.test.js
node --test --test-concurrency=1 tests/f6c-emission-refinement-pilot.test.js tests/f6c-refined-acceleration-pilot.test.js tests/f6c-bounded-operation.test.js tests/f6c-parent-emission-refinement-pilot.test.js tests/f6c-streamed-leaf-diagnostic.test.js
node --test --test-concurrency=1 tests/prescribed-response-pilot-process.test.js tests/f6c-acceleration-pilot-process.test.js
node --test --test-concurrency=1 tests/f6c-emission-refinement-pilot-process.test.js tests/f6c-refined-acceleration-pilot-process.test.js
"${AAA_VENV:-../.venv}/bin/python" -m unittest discover -s tests -p test_f6c_emission_current_execution.py
node scripts/equation-mapping/check-current-source-maps.mjs
```

Intermediate implementation runs exposed incomplete synthetic CLI arguments, placeholder composition hashes/sizes, a stale fixture helper identity and an absent-plan path that failed before the intended map check. These fixtures were corrected to satisfy the new contract; their earlier runs are not passes. The sandboxed dependent run encountered `spawn EPERM` in synthetic process observation and failed to establish those process behaviors. Its completed host rerun above supplies that evidence. The streamed-leaf failure controls deliberately wait for bounded cleanup; the computation-monitoring procedure was used to observe the foreground run through its terminal result.

No full Content Integrity aggregate, full repository suite, real scientific pilot, generated write, staging, commit, push or PR mutation was performed. These selections establish the stated operational/source boundary only.

## Authored scope and falsifier

The paired implementation comprises the four entry/launcher files, shared transfer checker and restricted extractor, two baselines and two manifests, affected admission/unit/process fixtures, the eight dependent files above, and these live contract/status/plan records. Other concurrent edits in the shared checkout are outside this task.

Claim grade: measured for the named transfer checker, diffs and tests; inferred for the suitability of the operational replacement and five-package remaining plan. A changed source admitted under its prior external map digest, an omitted original obligation, stale state reused after rejection, cross-profile authorization, a scientific identity refreshed without its review, or failure of an affected process/resource control would overturn the corresponding conclusion.
