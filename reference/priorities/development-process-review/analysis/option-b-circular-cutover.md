# B-REM-3 circular operational cutover

## Result and boundary

The nine circular operational sources implement externally selected current-source admission under the existing reader, including the actual current pilot/rung entry routes. After the BR4-5 correction below, the 68-test focused command passes against the actual repository manifest and synthetic rejection controls. The separately recorded supervised 65-test process selection passed before that correction with the target process group closed; main owns its post-correction combined rerun. These are measured operational results, not scientific acceptance, a successful numerical campaign, or combined-family closure. Independent review is separate from these subject-authored controls.

The canonical interface is [option-b-circular-sources.jsonld](../contracts/option-b-circular-sources.jsonld), scope `subfield-circular-current-operation`, admission role `scripts/eom/run-current-subfield-circular-root-pilot.mjs`, and `launcher` role `scripts/eom/launch-subfield-circular-root-pilot.mjs` (OUTER). The map SHA-256 after BR4-5 is `d63e258c9fa5f5bfd520f3127c376b111b448f397cdc7c68b8b3c598e18644e4`, measured by `shasum -a 256`; it supersedes the initial handoff map `7c6adaf6b857b12c5f837a55ab73b001cc3bdfc77cf141819ac2e97433957480`. The reader remains `9097ef3fd3fb2040b48c601d81c184188e8a992aa4464694860d05d0bf76a56e` by the same command and retained-copy comparison. No reader extension was made.

## Pre-edit inventory and independence

Before editing, a known quoted-SHA fixture was checked before using the digest search on the target. `rg` searches under `scripts/`, `tests/`, `src/`, and `reference/` identified circular entry/preparation callers, fixed scientific pins, shared supervisor consumers, and historical build/reference binders. The [retained baseline descriptor](../contracts/option-b-circular-baseline.json) records the nine original source digests and unchanged reader. Their pre-edit `cp -p` copies remain at `.local-data/option-b-circular-baseline-20260912/`; they are local-only provenance, not current admission targets.

The original scientific reducer, reducer CLI, whole-history verifier, C++ adapter, and CMake fixed selectors remain. No independent reference, scientific implementation, saved scientific evidence, or scientific acceptance predicate was rewritten. The standalone pilot's old mixed `PINS` has been separated: process/bridge/watch dependencies are map-selected operational sources, while the reducer/CLI/proof digests remain fixed scientific obligations. The rung retains the corresponding scientific hash table and rechecks it at capture and execution.

An independent-acceptance blocker was raised before source edits: the retained build receipt `current-v3-build-20260908-execution-review/preparation.json`, SHA-256 `c80526d097c81627186cbbfcea7e0005d9d73288e331f4535f07982cc2bef944`, binds historical preparation generations (`prepare-f5-enclosed-root.mjs` at `4380a302ec39f8307415a7f4340c1ef0f3bb4766c378a853133f89b45c34a3a9` and circular preparation at `3d372147742b1036bb3101b7a8b39fcfeb1b5cb7176c3c1f7edeaee5635b9802`) that already differed from live files at inventory. `SubfieldCircularRootLedgerReducer.mjs` checks the receipt's actual build/source obligations. That rejection remains intact; operational map admission does not make this historical build executable as current evidence. A separately accepted current build is needed before any new numerical acceptance claim.

## Exact dependency and route obligations

The admission initializer requires the exact 22-path/role census declared in `CIRCULAR_SOURCE_ROLES`, not merely a self-consistent graph. The captured common reader checks the existing schema and graph; the initializer checks actual path/role closure and its own captured bytes. The source set covers the nine circular operational files, F5 watched-process helper, observation/process/memory owners and Python helpers, phase process/ledger bridge, frozen reducer/CLI/verifier, C++ adapter, CMake owner, and unchanged manifest reader. Preparation additionally retains its full solver-tree, reference, compiler, and executable inventory; these obligations are not replaced by the operational map.

| Route | Current operational admission and retained limit |
| --- | --- |
| Current pilot and current rung | Require external profile digest and `--source-map-sha256`; profile-selected source bytes must match the admitted map. Existing whole-entry deadline, process owner, memory owner, gate census, cleanup, and publication predicates remain. |
| Legacy pilot/rung | Require external runner digest and source-map digest. Capture the admitted modules and transport the complete original identity census into execution and all stage checks. No static operational helper digest bypass remains. |
| Legacy launch | Requires external launcher/runner/map selections and executes selected captured sources. It does not gain an independently accounted process observer; the existing supervisor rejects a caller that cannot supply one. The actual supported current route supplies the reviewed observation owner. |
| Dispatcher worker | Requires selected captured dispatcher/rung bytes, complete parent source identities, accepted rung source-map closure, exact runtime bindings, and existing scientific/process/plan/prior predicates. Shared/build and publication workers preserve the map selection. |
| Dispatcher CLI | Requires a map and then explicitly rejects full-ladder launch because full-ladder lifetime admission remains deferred. No full-ladder capability is introduced. |
| Current prior and ladder preparation | Execute captured selected initializers and dependencies, recheck before/after publication, and select current entry/memory dependencies from the map. Current pilot independent review is an explicit external path/digest, not a historical literal default. |
| Circular build preparation | Requires external self/map selection, executes a captured initializer and F5 watched-process helper, and retains all scientific source/reference/compiler/build checks. No build was launched in this migration. |

Every admission retains the first `dev`, `ino`, `size`, `mtimeMs`, and `ctimeMs` for the map, reader, and every source. Descriptor capture checks before/after metadata and immediately compares the current `lstat` path identity. Bootstrap map/initializer captures carry their original identities into the initializer, whose final recheck also catches replacement between bootstrap and graph capture. Rechecks compare with those first identities; they never renew a baseline after identical-byte replacement. Full stage transport includes all 23 source/map records and rejects missing members or conflicting duplicate identities. The two-record bootstrap exception is limited to the map and initializer required to load admission itself. Transport is an in-process/child operational identity contract, not cryptographic attestation against a malicious process with arbitrary repository and memory write access.

`sources[].bytes` deliberately contains a Buffer, as required by snapshot imports and phase-ledger workers; `bindings[].bytes` is the numeric byte length for metadata accounting. Parent identities remain in final receipts, source checks, observation-owner inputs, stage rechecks and acceptance workers. Rung runtime receipts now include the exact Node and `/usr/bin/time` bindings already required by dispatcher acceptance.

## Shared integration handoff

OUTER changed from retained `71974054ddce7fc29b8464b9a7a63f8fbb04ee5b425dc997df4d40b2804341aa` to stable `e55cc27ca8e7d6bc0141b72cd65e1b2dce26d0c13d47d92427e68887d4de4e8a`. Main released the OUTER hold after independently reviewed checker-first acceptance of the narrow retained-A exception. Main owns the eight transferred B maps, shared checker, CI integration, and emission-Python selection transport. This task did not modify them. An extraction check first passed a known sample, then compared the full `superviseRegisteredPilot` section against the retained copy and found exact byte equality; no broad supervisor refactor was performed.

The selected F5 helper is `3431be1ca2f17474775572358baa88eae8de3ce93403d58e4b1fa36d9e367d50` by `shasum -a 256`. Planck owns that file. Any later helper change requires updating its circular map binding and rerunning canonical admission; it does not authorize changes to fixed scientific build/reference selectors. Any OUTER change requires a new main-owned eight-map refresh and shared controls. No further OUTER edit is planned.

## Validation receipts

The pre-edit seven-file baseline command (the focused command below without the new admission test) passed 58 tests with zero failures/skips. The initial handoff focused selection passed 67 tests with zero failures, cancellations or skips, duration 2.181 seconds. It includes the unmodified actual repository map positive; known copied-family admission; wrong external digest/scope/role/census/initializer; duplicate JSON keys; same-byte replacements during fresh capture and after admission; inherited identity omission/conflict; symbolic substitutions; all nine actual CLI routes rejecting missing map selection; and the existing scientific/planning/resource/deadline/output negatives. The post-BR4-5 rerun adds the stage-binding directory/path replacement control and passes 68 tests, zero failures/cancellations/skips, duration 2.176 seconds.

```sh
node --test --test-concurrency=1 tests/option-b-circular-admission.test.mjs tests/subfield-circular-root-pilot.test.js tests/subfield-circular-root-rung.test.js tests/subfield-circular-root-dispatch.test.js tests/subfield-circular-current-entry.test.js tests/subfield-circular-current-prior.test.js tests/subfield-circular-current-resource.test.js tests/subfield-circular-root-preparation.test.js
```

The compute skill required owned supervision for the long process selection. Its first sandbox launch returned `spawn EPERM` before target start; the approved host retry below completed normally. It passed 65 tests with zero failures, cancellations or skips, test duration 39.782 seconds. Existing process/resource negative controls were retained, including hostile exits, orphan/drain/output bounds, cancelled workers, observation/memory ownership, 20-second deadline-guard termination, failed final admission/publication, and captured commands over 64 KiB. No resource threshold was lowered.

```sh
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --owner-thread "$CODEX_THREAD_ID" --deadline-seconds 360 --heartbeat-seconds 15 -- node --test --test-concurrency=1 tests/subfield-circular-root-launcher.test.js tests/subfield-circular-phase-process.test.js tests/subfield-circular-phase-ledger-worker.test.js tests/subfield-circular-observation-owner.test.js tests/subfield-circular-memory-owner.test.js
```

Lease/run ID: `4009b1c9-8972-4bab-a029-919310780cd7`. Owner task: `01a09587-8e95-75a1-a046-4dabf438962a`; owner thread: `01a0970a-22b4-7f01-9bb7-93fe56ed7c6c`. Started `2026-09-12T19:37:21.531Z`; finished `2026-09-12T19:38:01.375Z`; supervisor elapsed 39.857 seconds; exit code 0, no signal, `processGroupClosed: true`. Local evidence is `.local-data/owned-compute/logs/4009b1c9-8972-4bab-a029-919310780cd7.stdout.log` (46,381 bytes) and the paired empty stderr log. The lease establishes process ownership and closure, not scientific validity.

No numerical campaign, generator write, Git publication, or reference rewrite was performed. The shared Python venv was executable (Python 3.13.2); system Python was not used. Main's larger combined gate is a separate result and must not be inferred green from these scoped passes.

## BR4-5 independent-review correction

Independent review identified a missing current-path check in the serial pilot's `bind()`, which also serves rung `recheckShared()` through the captured pilot file worker. The earlier admission checks do not suffice here: replacing a parent directory after `open()` preserves the opened file descriptor and its metadata, while a subsequent pathname-based stage can resolve to another inode containing identical bytes. The previous broad statement about replacement protection was therefore insufficient for stage binding; the original focused passes did not cover this race.

Before changing the subject, the new test in `tests/option-b-circular-admission.test.mjs` accepted a literal `abc` file with its known SHA-256, then replaced its directory during `readSync()`. It explicitly confirmed unchanged opened-descriptor `dev/ino/size/mtimeMs/ctimeMs`, unchanged canonical pathname and file bytes, and a different current-path inode. The unmodified subject failed the expected rejection with `Missing expected exception`. This establishes the reported defect without modifying any independent scientific reference.

The narrow fix adds `lstatSync(filename)` for identity-bearing records after the existing descriptor recheck. Admission now additionally requires a regular current file, canonical path, and all five current-path fields equal to the original inherited identity. Digest-only historical records retain their existing behavior. The control then passed for both parent-directory replacement and direct file replacement, with a literal-good check preceding each negative. Its exact focused command is:

```sh
node --test --test-name-pattern='stage binding admits literal' tests/option-b-circular-admission.test.mjs
```

The corrected pilot SHA-256 is `7ea12ef02b4a1d5ba2da1a353b0e7720e30cfd5d3f59bfb2db0177ad11fd737f`, replacing `1586faefe6aab0dff19a55f3fc920c29b44c4de511798ad0246c4c31760374ce` in the canonical circular map. This follow-up edits only that pilot, its circular admission test, the canonical map, and this record. OUTER, reader, F5 helper, scientific files, and retained baselines are unchanged. The eight-file focused command above passes 68/68 after the fix; main's previously 132-test combined selection gains this one control and requires its own post-fix result. Independent acceptance of BR4-5 remains the reviewer's decision.

## Final dependency-closed validation

Main's consolidated final-generation 51-file selection passes 563 controls under lease `3c38de5a-0fc7-4994-bf2b-68056b2e3875`, including the corrected 133-control circular family. After host memory recovery, the previously excluded `tests/f6c-streamed-leaf-diagnostic.test.js` and `tests/option-b-f6c-coordinator-process.test.mjs` pass all 77 controls under lease `7e1ab305-01cb-48d4-a094-546258d78a80`, with zero failures/skips/cancellations, exit zero and closed process group in 214.066 seconds. The exact command, source preservation checks and observer receipt are in the [reconciliation owner](option-b-repository-reconciliation.md#resumed-terminal-results).

Together these retained results discharge B-REM-3's enumerated shared-caller regression dependency. They do not execute the deferred circular ladder, replace historical build applicability, or establish scientific acceptance. The original 170-path source snapshot and the two resumed test-file hashes match after validation; no source, map, resource limit or scientific reference changed during this resumption.

## Files and falsifiers

Operational edits are limited to the nine files enumerated in the baseline descriptor. New artifacts are the canonical map, retained-baseline descriptor, this record, `tests/option-b-circular-admission.test.mjs`, and `tests/option-b-circular-fixture.mjs`. Existing circular operational tests were updated only for required map arguments, selected operational fixtures, explicit prior selection, and actual worker receipt transport. The map records every final source digest; `shasum -a 256` and the real-map admission test are the handoff verification instruments.

The operational claim is falsified by a missing actual source-role dependency, acceptance after replacing a first-captured path, a worker that runs unselected source bytes, changed scientific predicates/reference bytes, loss of deadline/resource/cleanup negatives, or a failure of either recorded test selection on the selected source generation. Independent review and main's combined gate must decide those broader acceptance obligations. The historical-build rejection and deferred full-ladder capability remain explicit limits, not waived failures.
