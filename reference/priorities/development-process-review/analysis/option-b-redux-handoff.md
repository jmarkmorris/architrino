Continue the Option B implementation and transition campaign, preserving all necessary Option A protections until their verified replacements operate through the accepted JSON-LD knowledge-integrity design, then retire the superseded A paths.

# OptionB-Redux handoff

Current execution is owned by the [September 13 consolidated four-batch plan](option-b-repository-reconciliation.md#consolidated-full-b-closure-plan--september-13) and [live queue](../work-queue.md#operator-selected-option-b-work). Earlier “next family” passages below are dated handoff history, not the current task selection. The operator authorized larger chunks and agent assistance; preserve the original evidence while following the consolidated plan.

## Operator direction and ownership

The operator explicitly requests this successor task, named **OptionB-Redux**, to take over the implementation work of **optionB**, task `01a09587-8e95-75a1-a046-4dabf438962a`. Work in the existing local checkout `/Users/markmorris/vibe/architrino`; do not create or adopt a linked worktree. The predecessor stops campaign implementation after this handoff. You own continuation, integration, validation, and current campaign reporting within the existing owners. This is an implementation assignment, not merely a request to propose a plan. Begin with live reconciliation, then execute the next dependency-closed migration slice and continue useful authorized work without repeated permission requests for routine implementation.

The operator's end state is conformance to the [knowledge-integrity design](../../../op/git/git-backed-knowledge-architecture.md#accepted-full-b-end-state): do not insert or refresh integrity hashes in executable source, tests, or authored explanatory source as a maintenance mechanism. Store the applicable identities and relationships externally through the JSON-LD-based design. Computing and comparing a digest remains necessary; moving the expected value must preserve its original meaning and independent justification. Remove inherited embedded expectations as their consumers migrate safely. Historical source snapshots and retained evidence are not active-source cleanup targets and must not be rewritten.

Every necessary A obligation must be covered by B before its A implementation is decommissioned. The operator believes A is approximately halfway decommissioned; treat this as an impression, not a measured fraction. Neither a count of files containing quoted digests nor completion of the finite operational packages measures total functional coverage. Maintain an obligation-level account of completed, partial, blocked, and remaining work.

## Where the campaign stands and why

The campaign grew from the hash/pin recovery investigation into a broader architectural transition. Its purpose is to make integrity relationships explicit and maintainable without requiring routine edits to embedded expected digests, while retaining real source, dependency, evidence, scientific-applicability, resource, execution, and publication protection. This motivation does not establish that every prior mismatch was source corruption or that the earlier process was never tested; the historical RCA is a separate investigation.

Live handoff inspection on September 12, 2026 read the strategy, executable queue, accepted architecture, and relevant reconciliation sections. `node scripts/equation-mapping/check-current-source-dispositions.mjs` completed with 157 original discovery files, 117 live candidate files, 224 disposition rows, and three retired rows. Its declared authority is inventory accounting only. This is a bounded quoted-digest scan, not proof that every hash, dynamic dependency, A fallback, or missing graph edge has been found. Recheck the live candidate before edits; the shared checkout is changing.

The following milestones are recorded as complete at their stated scopes in the [current strategy](../priorities.md), [queue](../work-queue.md), and [reconciliation owner](option-b-repository-reconciliation.md):

- The original five launch profiles use B admission: root-cover pilot, cached-root-cover pilot, cached-root-cover full, prescribed-response pilot, and acceleration pilot. All five finite B-REM packages subsequently closed their enumerated operational transfer, role-disposition, and validation scope. Do not repeat them as unfinished migrations. Their historical retained-role decisions were not permanent exemptions from the later full-B direction.
- For the original five profiles, explicit externally selected B checkpoints and B-to-B transitions replaced recurring extraction of old A source. Eligibility is default-deny and specific to accepted path/role selections; a broad operational label does not grant scientific-reference refresh authority. The original descriptors and transfer evidence remain historical records.
- The redundant standalone launch-binding test was removed only after its exact five-profile assertion moved into required B admission coverage. The legacy A-role extractor and special-case operational-successor test were subsequently retired with recorded replacement or ended obligations. These are the three recorded retirements, not evidence that all A functionality is gone.
- The mathematical comparison loader consumes exact accepted predecessor maps at report-only scope. That predecessor approval does not approve later changed candidates, mathematical correctness, or production use. Preserve current candidate review requirements and distinguish recorded earlier acceptance from pending successor review.
- Successive Node/Python test consumers now read externally stored original expectations. The Borg assembly catalogue and coordinated Borg budget/F5 families have also transferred their identities into authored data with deterministic ignored ESM projections where synchronous consumers require them. Public values, interfaces, historical declarations, and independent scientific references were preserved within the reviewed scope.

The last implementation batch is [Borg budget transfer acceptance](option-b-repository-reconciliation.md#budget-transfer-acceptance). Its retained receipts report a successful source-only reconstruction, all 30 required Content Integrity checks with one optional broad sweep skipped, and 169 connected F5/host/release tests. The corresponding owned-run IDs are `a1fc5d38-6934-4b9c-bcf5-453e6e8c057f`, `625b5a50-834b-4bb5-ad2d-760c0c1728e3`, and `34923070-f8c9-4241-a507-ffc28fd4dc8e`. Exact commands and outputs belong to their existing owned-compute receipts. These are retained completed-candidate results, not a fresh full-gate run in this handoff or universal repository health. Source-only plain-ESM import coverage is not rendered-browser or scientific validation.

## First reads and immediate continuation

Read `AGENTS.md`, the generated startup router, and the live resumption skill/procedure before editing. Read the [work queue](../work-queue.md), [priorities](../priorities.md), recent [work log](../work-log.md), [architecture](../../../op/git/git-backed-knowledge-architecture.md), and [reconciliation owner](option-b-repository-reconciliation.md), particularly retirement cleanup, B-to-B continuation, remaining source-hash scope, and Borg/F5 acceptance. Use the [finite migration plan](option-b-remaining-migration-plan.md) for historical scope and original census, not as a competing live queue. The older sentence that no executable migration remains refers to finite-package scope, not the active full-B parent; clarify that local ambiguity during normal owner maintenance.

Inspect the live current-source admission and transition contracts under `contracts/`, the source manifests and accepted selections, `scripts/equation-mapping/current-source-manifest.mjs`, `current-source-transition.mjs`, `current-source-impact.mjs`, `check-current-source-maps.mjs`, `check-current-source-dispositions.mjs`, the retained disposition records, and actual required-gate callers. Follow the relevant consumer code rather than relying on labels or filename matches.

The queue recommends the **F6c cached-root-cover pilot/full test-binding family** next. Resolve exact consumers, including the cached pilot/full process and launcher tests, from the live tree. Their operational launch profiles already have B maps; their remaining resource-plan, retained-result, and comparison identities still need transfer. Inventory incoming bindings before editing. Move exact original expected values into reviewed records and adapt consumers without changing resource limits, numerical expectations, rejection predicates, or historical applicability. Include dependent preparation, captured-loader, admission, and build consumers when their bytes or contracts are affected. If live evidence identifies an essential prerequisite, close or explicitly route it first; do not substitute a disconnected easy hash-removal batch.

## What remains beyond the next family

1. Reconcile all surviving source-hash families: historical reconstruction and ancestry consumers; scientific producers, independent references, proof and contract selectors; fixed conformance and known-answer controls; remaining application and compiled-source expectations; and operational families outside the original-five checkpoint. A retained obligation remains necessary even where its embedded representation must change.
2. Audit JSON-LD conformance of the accumulated external records. Existing transfers include ordinary JSON fixtures and Borg data as well as JSON-LD source maps. Determine which are legitimate payloads or derived projections governed by the graph and which leave required identities or relationships outside the accepted store. Externalizing a constant into arbitrary JSON, changing an extension, or adding an unused context is not sufficient conformance. Use the established model, stable identifiers, relationship vocabulary, graph libraries, validation, and accepted selection route; do not create a second authoritative store or a bespoke parallel framework. Preserve independently established expected values during any representation change.
3. Establish A-to-B coverage by obligation and real consumer, not by removing files. Use the existing disposition/reconciliation owners to map each A protection to its B record, loader, selection authority, enforcement point, positive/negative tests, review evidence, and retirement condition. Find remaining A use across source, tests, scripts, hooks, workflows, configuration, documentation, build paths, and actual dynamic callers; state each search's limitations. Required missing/malformed/wrong selections, stale bytes, dependency removal, role changes, protected reference changes, worker/stage substitution, and bypass paths must still reject.
4. Finish family-specific accepted B successor boundaries where needed. An original-five operational checkpoint cannot silently authorize a different family or scientific-contract change. A graph must not approve itself, and recomputing a failed expected value from the same subject is not validation.
5. Retire only the exact superseded A paths after replacement coverage and dependencies have been verified. Remove or migrate redundant tests only after their assertions survive under required B coverage; never disable a failing test to make migration green. Retain historical data and still-needed compatibility readers according to their actual consumers and retention rules, without retaining unnecessary active parallel authorities. Record removals, replacement evidence, and Git recovery locators.
6. Validate connected slices and, for final closure, reconcile the required campaign selection on one stable candidate. Preserve explicit failures, skips, unavailable inputs, and deferred scientific work. Verify build/runtime projections and language-specific consumers, not only graph parseability. Full-B closure requires necessary A coverage transferred and demonstrated, remaining source-hash scope resolved, required runtime paths using B, and approved retirements accounted for. A lower literal count alone is not closure.

## Separate investigation and protected boundaries

The independently launched **Option A vetting and cascading root causes**, task `01a09884-88f2-7531-b6da-62a9f201ccfd`, was active by a compact task-status check during this handoff. Its report destination is `analysis/option-a-validation-root-cause-analysis.md`, and its evidence directory is `evidence/option-a-validation-rca/`. Preserve those actively owned files and the original incident reports. Do not duplicate, edit, or prematurely adopt that investigation's draft findings. Read its final evidence-backed conclusions when available and assess implications for this campaign's design and tests. Safe in-scope implementation need not wait for its entire historical investigation unless a finding creates a concrete dependency or risk.

The separate **Find hash truncation root cause** task is `01a09847-3746-7073-826f-f1081eef056f`. Its [dispatch integrity record](agent-dispatch-integrity.md) concerns a coordinator-authored malformed hash and recorded intact receipt, outside A/B admission. The mechanism is a lead to check, not proof of earlier corruption. Use the maintained `scripts/agent-dispatch.mjs` path for any authorized source-bound dispatch, preserving exact machine-to-tool transfer and receipts; do not transcribe hashes through model-authored prose.

The deferred historical database, scientific-input checks, circular ladders, refined transport, resource decisions, and universal recovery verification retain their existing owners and activation conditions. This handoff authorizes B implementation and meaningful connected verification, not new scientific production, renewed historical declarations, raised resource limits, or wholesale recreation of old evidence. The operator previously permitted memory-intensive validation to resume; measure live resources and use the managed-computation procedure rather than assuming memory is still ample. Preserve the shared Python venv requirement. Never alter an independent reference together with its subject to manufacture agreement.

No Git staging, commit, push, PR operation, archive, reset, or new worktree is authorized by this transfer. Existing staged and unstaged work belongs to its owners. Prior bounded generator permissions are not a standing license to regenerate unrelated outputs. Follow current generated-artifact rules, retain check-only drift records and required commands where appropriate, and distinguish permitted ignored runtime preparation from tracked-source regeneration. Do not expose private lease tokens or raw private logs in reports.

## First return and ongoing custody

First acknowledge the inherited scope and your verified live next step, then carry out the migration work. Keep the current queue, strategy, and reconciliation owner aligned; remove completed child tasks without closing the unfinished full-B parent. Leave detailed receipts in their existing evidence/runtime owners. At each meaningful checkpoint, explain what changed, what actual protection survived, scoped validation, remaining blockers, and the next dependency-ordered action. Use independently known controls before a newly written instrument touches its real target. Do not treat agreement between agents as independent evidence.

The predecessor task has no further campaign implementation planned after this transfer. The historical RCA continues independently; its existence is not an unfinished implementation obligation that requires reopening the predecessor. Keep this handoff as the transition record and use the existing live campaign owners for subsequent status rather than making this document a second queue.

## Token-refresh recovery checkpoint — 2026-09-13

This checkpoint preserves recovery across the operator's token refresh; it is not a second queue. Resume from the live [work queue](../work-queue.md), [strategy](../priorities.md), [reconciliation](option-b-repository-reconciliation.md) and [work log](../work-log.md). The active task is OptionB-Redux, stable owner `01a0989b-ef03-7a22-ad38-c94ae7b40a56`. The operator explicitly approved sending the completion summary to collaborating task `01a09884-88f2-7531-b6da-62a9f201ccfd`; `send_message_to_thread` succeeded during this checkpoint. An earlier attempt had been rejected by automatic approval review; do not treat that earlier failure as the final delivery state. No token-reset credit was requested or consumed by this task.

At this checkpoint `git rev-parse HEAD` returns `06fc05899fa972f5b43bf5e1d58e16e6b8f9ac95`, and `git --no-optional-locks status --short` returns no entries before this handoff append. These commands establish only that observed checkout state. This task performed no staging, commit, push or PR action; do not attribute the newer HEAD to this task without inspecting history. Reinspect live status and ownership after refresh before editing.

### Completed since the original handoff

| Family | Durable owner and evidence | Validated boundary |
| --- | --- | --- |
| F6c cached-root-cover test expectations | [F6c transfer](option-b-repository-reconciliation.md#f6c-test-expectation-transfer); `contracts/option-b-f6c-test-*`; `evidence/option-b-f6c-test-transfer.json` | 160 connected tests pass. Original historical strings remain preserved; the historical/current launcher mismatch was reproduced against original source and explicitly reconciled to accepted current B selection. |
| Node next-test reader/payload | [Node integration](option-b-repository-reconciliation.md#node-test-identity-graph-integration); `contracts/option-b-next-test-*`; `evidence/option-b-next-test-graph-transfer.json` | 227 connected tests pass. Payload, nine consumers and original shape-validator function remain exact. |
| Retained direct-import Node identities | [Retained integration](option-b-repository-reconciliation.md#retained-test-identity-graph-integration); `contracts/option-b-retained-test-*`; `evidence/option-b-retained-test-graph-transfer.json` | 134 connected tests pass. Payload unchanged; thirteen consumers differ only in import/admission lines. |
| Python transport and shared known answers | [Current integration and obligation mapping](option-b-repository-reconciliation.md#python-and-known-answer-graph-integration); `contracts/option-b-controlled-fixture-*`; `evidence/option-b-controlled-fixture-transfer.json` | Final runs pass 141 Node tests and 36 Python tests, zero failures; one existing circular fresh-build integration case explicitly deferred. Both payloads byte-exact; sixteen consuming paths migrated. |

The original five launch profiles and finite B-REM1–5 were already complete. Do not reintroduce those or the completed rows above as executable work. Full B remains unfinished.

### Current family implementation and validation

The current fixture admission owner is `scripts/equation-mapping/controlled-fixture-records.mjs`. It reuses unchanged `current-source-manifest.mjs`, `current-source-transition.mjs` and `current-source-impact.mjs`. The Python reader `tests/option_b_fixture_records.py` supplies its captured external selection and named consumer to the same Node engine, parses the admitted stdout bytes and returns original immutable string/tuple shapes. No direct-read fallback exists. Its pure parsing controls remain separate from admission.

The selected graph has 30 sources and 80 relationships by the existing manifest validator. It includes explicit Python-reader and mathematical-runner dependency edges. Both expected-value payloads are unchanged: `scripts/equation-mapping/fixtures/known-hash-answers.json` and `tests/fixtures/option-b-python-transport-identities.json`. Fifteen consumer sources compare exactly against their recorded Git origin after inverse import/call replacement. The mathematical acceptance test additionally copies the admitted closure; `check-moving-single-root-map.mjs` admits, captures and rechecks that closure alongside existing trial inputs. Independent mathematical implementations and original result assertions are preserved.

Required controls are in `tests/option-b-controlled-fixture-records.test.mjs`, with copied-tree support in `tests/support/option-b-controlled-fixture-tree.mjs`. Actual Node and all three Python test-module imports pass first, then missing/altered payload or selection rejects before controls or scientific subject loading. Fully revisioned graph-valid successors reject specifically for protected payload roles or missing predecessor eligibility. The checkpoint's eligibility list is empty. Current-source labels alone authorize nothing. The GitHub profile excludes the new shared-venv control by its exact test name alongside the two earlier shared-Mac-venv controls; all three are mandatory locally. Six portable fixture controls pass even with `AAA_VENV=/unavailable` when the explicit Python test is excluded; this is not evidence of a Linux run.

Final owned Node run `b7a98b67-f3af-462c-ae36-6e87b1fea50d`: seventeen-file connected selection, 142 tests, 141 passed, zero failures/cancellations, one explicit skip, exit zero, empty stderr, closed process group, 29.966 seconds. Final shared-venv Python run `73beb024-87ee-4330-896d-dc35bb04c9db`: four-file selection, 36 passed, exit zero, closed process group, 3.051 seconds. Exact commands and terminal logs are under `.local-data/owned-compute/` with these IDs. Python unittest's successful report is on stderr. Owner-aware supervisor closeout reports `clear`; no owned job remains active or uncertain.

The skipped case is `recorded current build and original proof enter phase context; substituted inputs reject` in `tests/subfield-circular-current-context.test.js`. It requires `AAA_CIRCULAR_CURRENT_CONTEXT` and an explicitly selected fresh `AAA_SUBFIELD_CIRCULAR_BUILD_RECEIPT`. Its admitted known-answer control ran. Do not fabricate a receipt, silently activate scientific work, count the skipped integration as passing, or let this distinct dependency erase the completed fixture-admission scope.

The disposition checker last reports 157 original discovery files, 115 live quoted-digest candidate files, 252 disposition rows and three retirements. Original candidate rows are preserved. These are inventory counts, not a functional completion percentage. `git diff --check` passed before this checkpoint. The full content gate was not rerun by this family: its earlier stable-corpus/equation-registry boundary remains separately unresolved unless newer live evidence establishes otherwise.

### Next work and concurrent ownership

The recommended next bounded slice is **Borg catalogue graph admission and remaining budget-consumer coverage**, preserving the already accepted F5 transfer. It has not started. The operator's latest message approves the cross-task summary and recovery capture; it does not separately select this next implementation slice. Wait for a continuation instruction, then inspect the live Borg data, contracts, generators, projections, setup/build callers and incoming bindings before changing anything. Use the existing admission engine and queue; preserve payload values and public APIs. Historical/scientific selectors and other full-B families remain later obligations.

The prevention review's accepted source-role follow-through is captured in [the existing transition contract](../contracts/option-b-current-source-transition.md#source-role-preservation-through-remaining-families), the current family's obligation-to-record-to-selection-to-consumer-to-control table, dispositions and queue. Carry that mapping, exact predecessor path/role grants, protected/ungranted default denial and final consumer-binding checks through later migrations before retiring A routes. Preserve original historical and independent-control values and applicability; never refresh an expected answer from its subject.

Collaborating task `01a09884-88f2-7531-b6da-62a9f201ccfd` owns the separate publication-receipt follow-up: `scripts/pr-validation-receipt.mjs`, `tests/pr-validation-receipt.test.js` and matching `reference/op/git/pr-lifecycle.md` text. This task left those files untouched. Its accepted prevention analysis is `evidence/option-a-validation-rca/prevention-workflow-review.md`; do not duplicate its queue or implementation. The approved completion summary already reports exact scope and evidence to that task. Preserve concurrent work and re-read its status only if a real dependency requires it.

For managed runs use the shared venv `${AAA_VENV:-../.venv}/bin/python`; it executed Python 3.13.2 during this slice. Use the owned supervisor with `$CODEX_SESSION_ID`, hard deadlines and terminal closeout. Sandbox process inspection returned `spawn EPERM`; escalated supervisor calls were approved. Do not expose private lease control tokens. Disposable authoring helpers and incoming snapshots remain in `.tmp/option-b-python-graph/`; durable proof, source-role mapping and validation are already in the repository owners above. Those scratch authoring helpers are initial in-review construction tools, not routine refresh authority.

## Recovery checkpoint — September 13 Borg setup and Pages

The [Borg runtime graph integration](option-b-repository-reconciliation.md#borg-runtime-graph-integration) now owns the complete setup/build explanation, original-obligation mapping and limits. New files are `scripts/borg/selected-runtime-admission.mjs`, its six-control test, five `option-b-borg-runtime-*` contract records and the transfer proof. Existing runtime preparation retains admitted inputs through generation; the default Pages build checks copied Borg files before return. Original data, generators, contracts, browser APIs and F5 acceptance remain preserved. Never reuse `.tmp/option-b-borg-graph/` initial authoring as an automatic expectation-refresh procedure.

Connected run `87fece94-589a-4665-ba3d-57794df0a48c` passes 170 tests; source-only/default Pages run `3cd3b6a6-46da-4146-858e-a4ae96062fd8` passes its integration test. Both have zero failures/skips and closed process groups. The disposition checker reports 260 rows, 115 candidates and three retirements, with original candidate rows preserved. Durable reconciliation, disposition, priority, queue and work-log records are updated.

Resume the remaining standalone scientific consumer admission reconciliation from the queue. The sixteen direct-import nodes establish graph dependencies and setup/build source coverage, not independently enforced standalone CLI admission. Preserve the existing F5 route and inspect each remaining producer's actual contract before changing it. No new numerical execution, build-receipt renewal, policy change or publication is authorized by this checkpoint. The separately owned publication-receipt implementation remains outside this task.

## B-CLOSE-1 active implementation checkpoint — September 13

The operator selected Batch1. Three implementation/review agents support the root task. All29 tests are now transferred in the working tree to `scripts/equation-mapping/batch-test-records.mjs` / `tests/option_b_batch_records.py`; payload contains110 original identities (13Node,97Python including6escaped). Original source strings are retained in `tests/fixtures/option-b-batch-test-original-sources.json` (under unchanged1MiB file limit). Six Python consumers have explicit paired historical/current control handling; the bridge was further fixed to retain complete source identities across requests, addressing same-byte replacement between capture and use. These changes remain in review, not final acceptance.

Scratch implementation/evidence is `.tmp/option-b-close1/`: node/, python/, five/, closure-paths.json, author-batch.mjs, author-retained.mjs and run-node.mjs/run-python.mjs. Required construction order after final source changes: author-batch; node/prepare-operational-candidates; five/author; apply generated reviewed candidates; author-retained; node/prepare-borg-candidates after retained is stable. These are initial candidate authoring scripts, not recurring automatic refresh authority. Original maps/checkpoints/test versions are retained in `evidence/option-b-batch-test-predecessors/` and agent original-source scratch. Never replace original expected values with current subject hashes.

Eight incoming runtime maps and additional F5 evolution/retained/Borg dependencies required connected closure: four strict loaders extend role censuses, genericfive maps add test closure, current F5 selector is separate from unchanged historical budget transition and original retained payload. New retainedSelectedBytes returns captured selected records. Independent review confirmed exact29candidate reconstruction and six Python historical adaptations, but final mapped-byte checks and complete connected tests remain outstanding.

Initial owned Node run `76811661-306f-452b-b7e4-890d5871f91d` finishes240tests,230passes,10failures,0skips. Failure categories: omitted emission Python binding; old source-count assertions; independent execute-f5-prehistory-handoff.py operational census; historical source replay of edited controls. Agent b_close_1_node is handling operationalPython census/replay and Borg source/output successor candidates; root updated source-count assertions and owns integration. New batch reader controls initially passed6; added Python cross-call identity and agent adversarial controls require rebuilt maps before revalidation. No scientific production, publication, old payload refresh or shared transition-engine weakening is authorized/performed. Resume through live owners and inspect actual agent/supervisor status before continuing.

### Connected verification checkpoint — September 13 23:17 UTC

The first repaired Node selection completed309tests,306passes,3failures,0skips in owned run c2949fb6-bbf8-4c0b-8ec3-6f387ceb62fc. All three failures are the previously completed F6c-test map's stale five-profile selection binding. Agent b_close_1_node owns its candidate map/checkpoint dependency transfer and all-current-graph incoming audit. Independent all-contract audit found exactly this one stale binding among511Source rows. Do not call modified-map-only validation universal.

Source-only/default Pages run70b4fc0d-6c3f-432c-890a-374a9f1283c4 passed1test,0failures/skips,81.574seconds, closed group. Python run e46c89c8-e5d2-42ab-ab21-b36de7a9fbea remains active under deadline23:33UTC and is producing unittest progress. Its observed error is independently reproduced by the exact test test_f6c_emission_current_execution.PublicationLifecycle.test_actual_transport_publication_and_cleanup_with_artificial_numerics; the historical plan fixture currently mixes rewritten control source into the original verifier's dependency closure. Python agent owns a candidate-only repair; production verifier remains unchanged. Other historical loader corrections (parentverification paired load, removal of incorrect variablecell pool adaptation) are integrated and exact21reconstruction independently passed.

After active run completes, apply reviewed candidates, then .tmp/option-b-close1/integrate.mjs assembles batch -> operational -> five -> retained -> Borg candidates in strict dependency order; append/integrate F6c-test dependency author at the appropriate final position. Rerun connected tests against stable records and record actual failures/skips. Current disposition rows305,livecandidates86,original157candidate rows preserved; do not close Batch1 until final regressions pass. No publication or scientific production has occurred.

### Final candidate running — September 13 23:25 UTC

Final integrated generation includes the reviewed emission fixture repair and F6c-test dependency successor. Node run `2a16d85e-ef10-4d1b-9ff6-3f7d6db02222` passes all 309 tests, zero failures/skips, closed group. Pages run `a1eab97f-7fc8-4465-8e2a-8fd2d692a090` passes its source-only/default Pages test, zero failures/skips, closed group. Final 705-test Python run `735ce14a-1fde-4afd-8915-e23f1ccad2ee` remains active, with advancing output and no error reported so far; deadline is 23:40 UTC. Do not close the batch before terminal success and final snapshot recheck. All earlier groups closed by owner closeout.

The independent final review in scratch `conformance-final.md` is captured in the reconciliation owner: seven historical adapters/eleven pairs, exact 21-file Python reconstruction, all 580 Source bindings across 18 current graphs plus scope-acceptance map digests match, F6c predecessor preservation verified. The durable validation snapshot records 275 source paths; rerun `.tmp/option-b-close1/candidate-snapshot.mjs --check` at terminal completion. Retention validator passes 1,572 machine files; whitespace passes. Disposition checker reports 314 rows, 86 candidates and three retirements; original 157 candidate rows equal HEAD exactly. Remaining action: terminal Python receipt, snapshot/closeout, complete B-CLOSE-1 owner/queue/strategy/work-log and final response. No bound source edits remain planned.

## B-CLOSE-1 completed handoff — September 13

The final Python run passes all 705 tests in 318.085 seconds, completing the final Node309/Python705/Pages1 generation with zero failures or skips. All three terminal leases report exit zero and closed process groups; owner closeout is clear. All275 source snapshot paths remain unchanged at completion. The [complete batch owner](option-b-repository-reconciliation.md#b-close-1-shared-controls-and-record-conformance), [exact validation commands and receipts](../evidence/option-b-batch-test-validation-snapshot.json), source-role matrix, predecessor records and transfer proofs are durable. B-CLOSE-1 is removed from the executable queue; strategy and work log record completion.

Resume at B-CLOSE-2 only when the operator selects continuation. It is the full connected F6c/F5/circular/Borg EOM execution and historical ancestry batch, not another isolated test-family slice. Use the existing selected engine and preserve original numerical references, membership, historical applicability, resource limits and real pre-execution/final-output checks. B-CLOSE-3 and B-CLOSE-4 remain as recorded. The final 86 code-literal candidates are production families; governed JSON records and historical source snapshots still legitimately contain hashes. No publication or scientific production was performed or authorized by this completion. Initial authoring scripts are not recurring refresh authority.

## B-CLOSE-2 active integration checkpoint

The operator selected the connected batch with agent assistance. Implementation remains in progress; no production candidate has been accepted. Candidate preparation is retained under `.tmp/option-b-close2/{f6c,f5-circular,borg}/`, with originals, per-path candidates, transfer proofs and incoming-binder inventories. F6c owns the production and historical-table adapters; F5/circular owns native projection and shared admission adapters; Borg owns executable admission and the Python reader bootstrap. Root owns the common Node reader, protected records, source-map integration and connected validation. Preserve candidate originals before any application; do not rerun preparation scripts over applied files.

The family transfer proofs enumerate 37 F6c and 27 F5/circular literal candidates; Borg request preparation adds its two original scalar identities separately. Historical F6c source versions must remain individually addressable by the original expected digest: the recovered versions inventory includes generations older than this batch. Pure numerical modules receive immutable identities from admitted hosts and must remain free of record I/O. Historical 35-pin and 198-member applicability remains distinct from current source selection. Native fixed tables are projected at build time from protected original records; numerical implementations and known answers remain unchanged.

New common readers are `scripts/equation-mapping/production-source-records.mjs` and `scripts/eom/production_source_records.py`; `tests/option_b_production_records.py` supplies paired test loading. These are not yet bound by a production graph. The Node fixture currently requires admission coverage correction before its selected-host positive control can pass. Independent review found the Python bootstrap must authenticate captured reader bytes before launching Node; the Borg agent implemented that fix with six passing focused controls, pending root rerun and integration. Root must finish operation-specific dependency checks, original-version selection, retained anchor identity checks and cached-admission rechecks before applying production candidates.

Next actions are to finish the reader controls, assemble protected originals and identities, integrate the dependency-ordered source maps and strict loader censuses, then run connected Node/Python/native/Borg checks on one stable candidate. F5 direct historical source checks still need explicit original/current adapters; an old digest must never be reported as the identity of rewritten current bytes. The queue remains partial until all enumerated execution and output boundaries pass. No scientific production, resource-policy change, publication or automatic expectation refresh is authorized by this checkpoint.

### Connected preparation and resource-accounting checkpoint

The common reader's eleven Node/Python bootstrap controls pass, including selected positive admission, changed bytes, same-byte replacement, retained first-read anchors, operation dependency denial and distinct older-original source selection. Root candidates now cover the F6c coordinator, parent composition and packaging adapters; their inverse checks preserve original source bodies after removing declared representation adapters. The F5 worker has prepared exact historical API input-tree and circular worker transport handling. Candidate source application and connected map validation remain outstanding.

The disposable selection fixture under `.tmp/option-b-close2/selection-fixture` contains the prepared source generation, not a second Git working tree or accepted production selection. `collect-candidates.mjs` enumerates 162 candidate paths; `author-production.mjs --records` records 65 consumers and 914 original values plus 219 provenance entries. The author is initial reviewed construction only. Do not rerun candidate preparation over applied production sources.

The independently controlled F6c census exposed an overbroad initial graph: 563 distinct physical paths with original membership exceeded the existing 512-source limit. Test clients now use their actual authenticated shared helper as record consumer; required historical control-test files remain selected. The required-path function passed a known archive/current-pair control before use, and the revised fixture author reports 255 Source rows after adding the twelve F5 replay/API inputs and two recovered historical test generations. This graph count is not the final operation resource census. The F6c agent owns exposing existing retained capture identities through a read-only accessor and merging those actual records into the existing operation accounting. Capture, selection and check behavior and numerical resource limits must remain unchanged; no hidden transitive reads may be excluded.

Root scripts are `author-production.mjs`, `reduce-production-graph.mjs`, `collect-candidates.mjs`, `prepare-selection-fixture.mjs`, `check-root-adapters.mjs` and the declared root adapter scripts under `.tmp/option-b-close2/`. The Borg map author remains candidate-only and must run only after the coherent source application and new production selection. Existing maps/selectors have preserved originals under `borg/map-integration/originals/`; apply its final candidates in the declared dependency order, review all current source bindings and preserve historical values and refresh grants. Final connected Node/Python/native/Borg regression, retention, snapshot and owned-compute closeout remain required before B-CLOSE-2 completion.

### Applied candidate and first connected regressions — September 14 02:43 UTC

Root applied 162 source candidates using exact pre-Batch2/current-file preflight and authored the 264-source production selection. Root then applied 71 reviewed map/selection/predecessor records across 17 existing maps and eight selectors. The independent Borg review verifies 984 candidate source bindings, 565 preserved predecessor source rows, 1,541 preserved predecessor edges and unchanged grants/evidence arrays for eleven selected profiles. The root all-contract audit verifies 1,263 Source bindings across nineteen current maps with zero mismatches. The retention validator passes 1,609 machine files. These are structural and byte-identity checks; behavioral acceptance is not complete.

Actual disposable coordinator admission records 31 operational sources and 269 production captures including anchors, merged into 283 distinct sources, 11,646,232 bytes and 283 retained identities under unchanged limits. The separate full historical-membership census reports 457 known physical paths with the added default archives, leaving 55 below the 512 limit; this projection is not a complete numerical launch acceptance.

First owned Node run `76ee9d20-ff02-4e23-9729-c03a96545efa` stalled after Borg client failures; root stopped its authenticated group and confirmed closure. The Borg worker found two dangling `workerBinarySignature` cleanup assignments that threw before child termination. Python run `c7c04e22-633d-45fd-b9bb-ecc184aeec2a` exited during F5 replay module loading before the suite ran; its group is closed. Remaining Node run `96b69116-b382-4f4c-aa0f-27a63f34a3b3` completed with 854 reported tests, 667 passes, 169 failures, one cancellation and seventeen skips in 274.169 seconds, exit one and closed group. Do not report the unapplied fixes as passing tests. The logs and leases are under `.local-data/owned-compute/`; `.tmp/option-b-close2/node-failure-summary.json` contains known-control-tested compact failure extraction without enormous data-URL stacks.

Agents are preparing scratch regression candidates while root owns coherent application and rebinding. F6c owns coordinator/paired/family and emission/refined process fixtures plus `tests/support/option-b-production-fixtures.mjs`; Borg owns executable cleanup, circular and streamed fixtures; F5 owns original replay and F5 fixture/acceptance routes. Root owns common historical-conformance tests and root/cached/acceleration tests. Root's five initial regression candidates and exact edits are in `.tmp/option-b-close2/root-regressions/manifest.json`. F6c regression candidates are under `f6c/regressions/`. Only explicit disposable synthetic fixture records may be rewritten to bind synthetic test modules; live original scientific records and evidence must remain unchanged. Production source candidates have already been applied: do not rerun `apply-sources.mjs` or initial preparation scripts over them. Collect regression before/after files, apply coordinated fixes, reauthor the initial unaccepted candidate graph and dependent maps from preserved originals, then rerun connected selections and final checks.

The first connected graph-impact test timed out after 120 seconds on the expanded cyclic dependency graph. A candidate anchored inverse property-path query also left an unsettled promise (owned diagnostic `fd7675c6-0497-4349-bd5f-422cf8873a0b`); it is not the selected fix. The prepared `current-source-impact.mjs` candidate retains JSON-LD conversion, N3 stores and Comunica, replacing recursive property-path evaluation with fixed local SPARQL predecessor queries over a finite visited frontier. The reachability argument is direct: each iteration adds exactly one-edge predecessors of the previous frontier, so induction includes exactly the nodes with a finite path to a seed; a finite visited set terminates even on cycles. Hand-authored cycle, upstream, disconnected self-loop and empty-set controls passed before the affected map. That map returned all original declared consumers in 59.47 milliseconds in owned diagnostic `2449a5b2-7fef-470d-9c9e-22179175545b`. Root added a permanent known-cycle regression candidate. This is a declared-impact implementation repair, not a scientific validity result or a replacement of the standard graph libraries.

### First repair chunk applied and split validation — September 14 03:06 UTC

Root applied 38 reviewed regression files from root11, F6c11, F5nine and Borgseven manifests, preserving exact before bytes in `.tmp/option-b-close2/regression-pass1/`. The durable `evidence/option-b-production-regression-pass1.json` records exact patches and before/after identities. Production selection now has 265 Source rows; all nineteen current maps pass 1,266 bindings and the retention validator passes 1,610 machine files. The shared production transfer proof is 966,053 bytes, close to the existing 1-MiB record limit; keep further repair evidence in separately bounded referenced records instead of exceeding that limit.

F6c aggregate Python run `c1162d4f-2a2f-4830-80ce-7cf57f06326e` stopped at an original test-control import before executing the group. F5 run `e9077805-f0ae-45c1-833a-0a0d0fcf6fdf` completed 170 tests with one error (`original producer applicability differs`), assigned to the F5 agent. Node pass2 `65b78ce7-c828-4340-9ede-c02230e1e040` remains active at this checkpoint with advancing TAP output; it includes the applied repairs but deliberately excludes the three still-unapplied Borg streamed/budget fixture candidates. The next integration must include `borg/regressions/stream-manifest.json` before final full coverage.

Root started per-file F6c Python run `b5a7bbeb-52a7-4395-aa70-56507452ba11` to prevent one import error hiding the other 34 modules. It remains active, with exact per-module exit/time rows in `.tmp/option-b-close2/python-f6c-file-results.json`. This is diagnostic breadth, not a pass declaration. F6c owns candidate-only Python captured-source reporting, test-only original archive access and strict disposable synthetic fixture repairs; Borg owns remaining streamed/circular fixtures; F5 owns its single Python error and remaining F5 Node failures. Root has two further test candidates in `root-regressions2/manifest.json` for an acceleration synthetic output path and original Batch1 control data. Keep source bytes frozen until active runs terminate, then integrate/rebind and rerun. The shared fixture authority permits only explicitly synthetic disposable roots with closed selected paths, actual retained identities and negative checks; it does not permit live original evidence renewal or scientific production.

### Second repair preparation — September 14 03:25 UTC

Node pass2 is terminal with 848 tests, 801 passes, 30 failures, zero cancellations and 17 skips in 296.492 seconds. Per-file Python diagnostics completed all 35 modules in owned run b5a7bbeb-52a7-4395-aa70-56507452ba11, exit one and closed group; exact module outcomes remain in `.tmp/option-b-close2/python-f6c-file-results.json`. F5's five second-pass candidates address its ten Node failures and sole Python error with focused passing controls. Fresh Borg build e097567a-b0f3-4098-94fb-847cfe3390ce passed its default admission and protocol-only probe; circular build 74908322-bc57-4c99-a6ba-bebcf5cf34c8 failed before compilation at nested snapshot loading. Both groups are closed; a bounded circular loader repair is prepared.

Root prepared eighteen production adapter candidates in `root-production-pass2/manifest.json`. Three original BoundFile constructors take a byte limit, not a digest; the adapter now preserves that argument and authenticates the selected original archive independently. Archive checks use actual binding bytes and digest rather than assuming every original class exposes `initial`. Additional exit rechecks apply to substituted archives; unmodified ordinary captures retain their original caller-owned lifetime checks and rejection behavior. Already captured external module bytes retain their original execution route, while indexed repository paths use original/current selection. The emission execution host receives the missing original capture adapter. Candidate parsing and 34 signature/archive-lifetime controls passed after a known AST-selection control; connected target verification remains required.

Pending coherent application script is `.tmp/option-b-close2/apply-pass2.mjs`. It preflights exact live before identities, records exact patches in a separately bounded second regression record, and snapshots all current maps before application. It combines root test/production, F5 pass2, F6c regressions2 plus its explicit regressions3 overlay, Borg stream/pass2 and the final F6c Node manifest. The Node worker is completing parent-batch original/current physical-source composition. F6c continues bounded synthetic fixture adapters. Do not rerun initial source application or preparation over applied sources. After this next application, author the unaccepted production candidate graph, author dependent maps, compare map bytes to the pass2 snapshot before applying, audit every current source binding, then run the complete affected selections. No scientific acceptance or publication is authorized.

### Second repair chunk applied — September 14 03:24 UTC

The coherent pass2 application is complete: 58 source files and 71 dependent map/selection records, exact source patches in `evidence/option-b-production-regression-pass2.json` (170,782 bytes). Production selection has 266 Source rows. The all-contract audit passes all 1,267 Source bindings across nineteen maps; retention passes 1,611 machine files. Root's independent adapter review found and fixed the archive proxy's binding-valued `recheck()` return: it now preserves logical path metadata while retaining a distinct physical archive binding, and preserves None returns. Actual proxy controls pass.

Active supervised runs: full Node selection with F5 process controls enabled `81529f4f-b7f8-4b1d-a788-e2de9722b02c` (deadline 03:53:11 UTC); F6c per-file Python `00d2101c-b025-4a9c-aa01-913ef7012bfd` (deadline 04:03:45 UTC); F5 Python `02e62be0-7561-48af-b066-d00f33d8146c` (deadline 03:38:59 UTC); fresh circular build `968eaf98-814d-4fb1-9d0d-6771d1a6e6e5`. Keep live source bytes frozen. Node's newly enabled process controls have two failures under investigation: kill EPERM and missing expected SIGKILL. No passing final-generation claim is made.

Pending next candidates are F6c `regressions4/manifest.json` (actual logical-view fd delegation and acceleration original source fixture); the Node worker's parent-batch older transport route remains separate from the seven ready candidates already applied. Two older cached-control archive generations must be recovered and authenticated before indexing; never treat an older generation as the default original/current equivalence pair. Applied sources must not be overwritten by earlier candidate manifests. The current before snapshots are in `regression-pass2/`; all next repairs need a new exact-before snapshot and a new bounded evidence record before coherent rebinding and verification.

### Third connected diagnostics and next combined repair — September 14 03:36 UTC

Node pass3 completed 937 tests: 866 passes, 63 failures, zero cancellations and eight circular opt-in skips in 504.365 seconds. Its source selection includes the previously excluded streamed tests and enabled F5 process controls, so this failure count is not directly comparable to pass2. F5 Python passes all 170 tests (23.853 test seconds, 44.7 supervisor seconds), closed group. F6c per-file diagnostics completed all35 modules, exit one and closed group. Compact Node failures are `.tmp/option-b-close2/node-pass3-failures.json`; Python exact log is run00d2101c. Fresh circular build968eaf98 passed before enabled controls exposed historical logical-path reads against current files.

Next combined script `.tmp/option-b-close2/apply-pass3.mjs` is prepared but not yet run. Its base38 candidates comprise root17 ordinary-capture path/rejection compatibility fixes; parent archived-source routing; three archive additions plus extended original index; Python regressions4–8; cached legacy controls; F5 deadline test; and the Borg historical invariant test. The new cached archives match exact recorded hashes and Git objects. The added circular context test archive is its pre-first-edit snapshot, whose exact inverse matches the controlled-fixture original Git object. Existing index rows/defaults and all protected evidence remain unchanged. These additions are recorded separately rather than rerunning `--records`.

Additional ready C-only candidate is `f6c-pure-hook-pass3/manifest.json`: preload the captured root-scoped coordinator before invoking the unchanged pure-hook restrictions. Actual worker controls pass7/7 and the process guard control passes; no hook assertion is weakened. Circular three-source candidate is `borg/pass3/circular-manifest.json`: projected original physical paths with unchanged logical identities and retained descriptor checks, plus exact current verifier proof applicability. Independent root review found its first draft had not wired retained descriptors into `FrozenInputs::recheck`; Borg is correcting that before application. Do not apply that earlier draft.

The streamed fixture remains a separate pending chunk: Borg owns the final file, F6c supplies provider-related exact edit fragments, and neither writes live sources. After coherent pass3 source application and dependent-map rebinding, run Python and circular verification while those candidates finish, then integrate streamed repairs and run the final connected selection. Keep the 512-source/1-GiB limits and original 35/198 applicability unchanged. No scientific continuation or publication is selected.

### Third repair applied and verification active — September 14 03:40 UTC

Root applied39 pass3 files (base38 plus coordinator preload), excluding the circular candidates still under independent review. `evidence/option-b-production-regression-pass3.json` retains108,936bytes of exact patches and archive provenance. Reauthored71 dependent records; production graph270sources; all nineteen maps pass1,271 source bindings and retention passes1,612 machine files. All prior archive defaults and evidence remain preserved. Current source bytes are frozen.

Active runs are F6c Python per-file `c06ab403-7ee9-467d-9fd1-679489c3cb84` (deadline04:18:46UTC), and focused nine-file Node `5109400d-99b1-4449-818a-f79b4b9e5374` (deadline03:59:20UTC). The focused Node selection includes enabled F5 process tests, parent preparation, historical invariants, real-worker family/coordinator controls and the common reader/bootstrap/impact tests. Full final connected Node/Python/native acceptance is still outstanding.

Remaining candidate ownership: Borg owns circular3 and final streamed fixture. Root review additionally caught build-time reads of two scientific runtime reports in the circular projector; remove that build dependency and keep report validation at runtime. F5 agent independently reviews native capture/build contracts and requested lexical candidate path retention before canonical checking. F6c agent supplied six streamed provider edit fragments; its exact two-parent provider control passes8.71seconds on the current generation. Stream normal launch currently rejects an aggregate-RSS observation gap of1,347ms under the unchanged1-second bound; primary cause is being profiled, and neither absent downstream logs nor late synthetic failures identify a separate root cause. Do not increase the bound or erase the rejection.

### Focused verification and pass4 preparation — September 14 03:47 UTC

Focused Node run5109400d completed79tests,78passes,one failure,zero skips/cancellations in79.843testseconds. All enabled F5 process controls and actual worker family/coordinator boundaries pass. The remaining historical-invariant failure is absent original engine data for `current-source-transition.mjs` and `current-source-impact.mjs`. Root recovered both from the retained proof's exact Git objects and verified equality with HEAD originals; candidates are `.tmp/option-b-close2/common-archives-pass4/manifest.json`. That candidate index also adds the exact old acceleration preparation control45f40e2c6580fa09a614a471e7375615c8d54e3fc295a68dbdb8ad2167b7690b (30,600bytes), matching the existing authenticated Batch1 original source. Existing default source generations stay unchanged. The graph author now explicitly preserves manifest-reader roles when those engine sources enter the original index.

F6c per-file c06ab403 remains active near its final modules. Current reported failures are acceleration execution2, parent preparation known capture1, refined CLI negative1, and emission current execution1. F6c regressions9 manifest has four prepared corrections; original source-role coverage for both execution plans is being audited as a batch before the remaining index update. `apply-pass4.mjs` is prepared but NOT run; it will snapshot current maps, apply common archive/index candidates plus regressions9 and the final reviewed circular3, and retain exact patches in a bounded pass4 record. Additional candidates must be merged explicitly, with unique source ownership and exact current preflight.

Circular review identified and corrected retained descriptor final checks, lexical candidate path retention and build-time runtime-report dependency. The remaining direct reducer facade requires explicit expected-digest arguments; Borg must finish both actual-facade controls before native readiness. F5 agent is independently profiling the streamed initial RSS observation gap after current Python work closes. Do not move the beginning of observation, change the1-second gap bound, or treat missing downstream files as separate causes. Borg owns the final streamed fixture; F6c's provider fragments have a passing491 control. Keep every current source frozen until active verification ends.

### Fourth repair applied — September 14 03:53 UTC

Pass4 applied13 files: common original-index/archive additions, F6c regressions9, reviewed circular production3, and streamed fixture2. The exact audit covers440 execution-source role references and selects all16 missing current/archive inputs, including the single new45f control version. The pass4 evidence record is346,369bytes; main transfer proof966,722bytes. Production graph291sources, all nineteen current maps pass1,292 Source bindings, retention1,613 machine files. Source generation is frozen. The previously failing Borg original-data invariant now passes its actual Node test (1test, zero skips/failures,3.275seconds).

Borg owns fresh builds of both Borg and circular plus eight circular opt-in controls. F6c owns a supervised rerun of the four remaining Python files on this generation. Root has not rerun the complete Node selection yet because the streamed timing correction remains pending.

Independent isolated timing confirms repeated actual captures delay the first RSS observation: initial source admission278ms, initial bind115ms, imports7.5ms, subsequent bind121ms, caller bind200ms, caller import4.9ms, identity/check47ms, coordinate admission336ms and two final binds109/108ms. First sample begins1,345ms after the unchanged entry clock, exceeding the existing one-second bound. F5 agent owns the bounded stream-only repair: authenticate the unique declared `/bin/ps` runtime binding in the existing caller/spec capture, then take the first actual observation before caller import. No invented binary identity, reset beginning, wider bound or extra scientific authority is permitted. Existing lifetime probe/sample code supports in-memory observations before log descriptors exist; the later accounting sample remains independently required. Plan mode remains unchanged unless its own failure is measured.

Do not rerun any applied pass script. Further source changes need pass5 or an equivalently explicit new before snapshot, exact patch record, dependency-ordered rebinding, all-current-map audit and stable-generation tests. Final whole Node/Python/native evidence, resource census, source snapshot and owned-compute closeout remain required for B-CLOSE-2 completion.

### Fifth repair preparation — September 14

The four-module supervised Python run `d4082080-dd97-4391-86b8-86405e24b302` is terminal, 257.397 seconds, process group closed. It reports 95 passes and one error: acceleration execution seven passes, parent preparation forty passes, refined acceleration thirty-six passes, and emission current execution twelve passes plus one deadline error. The remaining error occurs during repeated production admission while capturing operational bindings under the existing thirty-second execution deadline. The F6c worker owns a bounded bridge candidate that may reuse authenticated results only with exact request keys and complete retained-identity checks; unknown targets, missing versions and unselected consumers must preserve rejection. No time limit is raised.

Both fresh native builds passed. Circular opt-in run `62b898a3-0c0f-4640-9476-19c5e9caded4` passed eight of nine tests: all seven protocol controls and retained-context positive; its negative fixture used a noncanonical temporary-directory alias. A scratch canonical-root correction passes both context tests. It remains pending in `borg/pass4/manifest.json`.

The streamed C timing candidate in `stream-startup-profile/manifest.json` passes its known positive and missing, duplicate, wrong-size and mutated observer controls, and preserves later-gap rejection. The combined real launch reaches the registered runner, exposing an actual production transport defect: the runner supplies an environment override forbidden by the unchanged generic spawn guard. Borg's reviewed candidate passes the authenticated envelope and pins as bounded trailing arguments, preserves the first five Python arguments and original clock, and leaves the generic guard unchanged. Serialized transport measured 50,889 bytes against a 65,536-byte cap; the host argument limit is 1,048,576 bytes. Full combined normal verification remains pending with the F5 worker. All profiling runs are closed.

Root prepared `.tmp/option-b-close2/apply-pass5.mjs` and `apply-pass5-maps.mjs` but has NOT applied them. They will combine C timing, Borg transport/context and the forthcoming reviewed Python bridge candidate, with unique ownership and exact before-byte preflight. Add the bounded pass5 evidence reference to the graph author before reauthoring graph and dependent maps. Do not rerun applied passes one through four. Final unchanged-generation affected Node/Python/native verification remains required before completing B-CLOSE-2.

The combined streamed fixture now passes both first-read map replacement controls, with the original injection assertions unchanged. Its next exact failure was a runtime inventory omission caused by Python `Path.as_uri()` lazily importing urllib/ipaddress. Declaring those six files then exposed the existing positive-size source contract because urllib/__init__.py is empty. The selected repair moves URI conversion to the captured Node runner using the standard `pathToFileURL` API, preserving correct escaping without widening source-size rules. F6c owns that bridge change together with admission reuse; Borg will remove the intermediate urllib inventory addition. Measured first RSS observations in these diagnostics passed (including 731.7 ms); source ordering is not being changed on an unconfirmed timing hypothesis. All diagnostic groups are closed.

The known-control-tested source-chain audit passes the initial162 applications and four repair passes,193 current paths and all198 indexed protected source versions. Its script/result are `.tmp/option-b-close2/check-source-chain.mjs` and `source-chain-audit.json`; repeat after pass5. The current physical census of selected production plus historical membership reports one absent original launch path (`scripts/eom/launch-abc-enclosed-root-pilot.mjs`); Borg is reviewing the actual ancestry consumer before any conclusion or restoration. This census is a known retained component, not a complete launch resource receipt.

The historical path review found the already-preserved source-recovery archive with exact original digest and39465bytes. The adapter accepts that explicit original/physical route before full historical membership checking; without the declared route, missing-path rejection remains correct. No executable was restored. The corrected known-case-tested census includes existing exact recovery routes and reports484 known retained paths,27,684,945bytes, zero missing physical paths on pass4; this remains a component census, not a launch allowance or scientific acceptance. Final census must be rerun after integration.

### Operator scope concern and measured status

The operator requested status and highlighted the near500-file change. `git --no-optional-locks status --porcelain=v1 -z --untracked-files=all`, parsed after a known two-path control, reports483 changed checkout paths:198 protected production-original archives,83 JSON/JSON-LD records,93 code files,98 tests and11 documentation/other paths. The initial-application and four repair manifests directly account for90 code files,96 tests,one JSON index andsix archive additions; the dependent-map author lists71 records. These sets overlap and must not be added together as a new total. Checkout-wide counts do not attribute every edit to this task. The operator was told the implementation is substantial, four repair passes expanded it, and its growth should have been surfaced earlier.

The authorized sequence remains combined candidate verification, coherent fifth repair integration, unchanged-generation affected suites and scoped closure; this status does not solicit another selection of item1. B-CLOSE-3/4 remain separate. Bridge candidate `f6c/bridge-cache-pass5/manifest.json` is frozen with two files,9/9 focused controls passing, exact request and runtime identity checks, preserved optional-query dependency rejection and standard URI conversion. Combined C/stream/bridge fixture run `cd350ad6` is active with the F5 worker. Do not apply until that run closes and its outcome is reviewed.

### Fifth repair applied — September 14

Applied five reviewed files from C timing, Borg transport/context and Python bridge manifests. Exact before/after patches are retained in `evidence/option-b-production-regression-pass5.json` (26,662bytes). Reauthored the production graph and all71 dependent records only after exact before-state preflight. Production graph292sources; all19 current maps pass1293 Source bindings; source-chain audit passes194 final source paths and198 protected original versions; retention passes1614 machine files and whitespace passes. The main transfer proof is966945bytes. Source generation is frozen while F6c verifies emission against its unchanged30second execution deadline and Borg builds both executables and runs opt-ins.

Combined diagnostic `cd350ad6-7d15-4bb0-97d9-835ddef0de3e` passes both first-map-identity controls and reaches two Python advances, codec EOF and adapter closure; the normal case then meets the test-only15second outer supervisor deadline. The subsequent45second diagnostic `626f7d53-9f3d-498b-856b-bf1d438c0ac1` fails early before output layout, so it does not establish that increasing that test deadline resolves normal. Both groups are closed. The test deadline remains15seconds in live source. F5 owns the exact early-failure diagnosis in a disposable fixture; do not attribute it to a timing gap without the actual rejection. No production time or resource bound changed.

Do not rerun apply-pass5 or earlier scripts. Any additional source correction requires a new before snapshot and coherent record update. Final connected Node/Python/native selection, source snapshot, disposition count and owned-compute closeout remain mandatory before B-CLOSE-2 closure.

### Pass5 verification and final-worker correction

Integrated emission run `f95a083a-ef48-4972-8ff2-e5d4de843fbe` passes all13 tests,40.158 unittest seconds,41.473 supervisor seconds, group closed. The actual execution's final-cover heartbeat reaches18.698seconds under its unchanged30second deadline. Full35 F6c plus9 F5 per-file validation is active as `e6ddbf68-d3b7-4934-8ec6-0dbde06f92f9`,1200second outer bound/15second heartbeat, with exact results and per-file logs in `.tmp/option-b-close2/f6c/pass5-final44/`. First25 files pass at this checkpoint. Pass5 Borg/circular builds and all9 circular opt-in controls pass; Borg's default protocol handshake passes and its process was disposed.

The final streamed receipt checker still expected9 arguments after registered launch moved to11. The reviewed pass6 candidate reconstructs the exact added envelope from the original retained bridge binding and compares all11 arguments, retaining source identity and clock checks. Its real normal run then exposed fresh-worker initialization: preflight and final admission run in separate workers, so the final worker lacks the original tuple initialized during preflight. The narrowed correction passes `admitted.production.identities(SELF)` from stream coordinate through its own admit job and initializes it synchronously in the stream final reader. Common coordinator and generic worker code remain unchanged. The permanent regression uses a fresh uninitialized module and checks missing/wrong tuple, six argument substitutions and same-byte bridge replacement. Candidate manifest `borg/pass6/manifest.json` has exactly stream source plus its existing test file; normal run `a8cad208` is active on its copied fixture under the unchanged15second test deadline. No pass6 application yet.

Diagnostic `7f3525ce` expired after45seconds and its sidecar became unavailable. Supervisor classifies it `stale_closed`; direct `ps` observations find no original PID94956 and no rows in original groups94956/94904. No signals were sent without identity. The lease retains stale stopping bookkeeping, not a claimed successful terminal receipt; owner closeout explicitly treats `stale_closed` as resolved. Do not run global repair merely to rewrite that record or touch unrelated leases.

### Pass7 checkpoint repairs and publication coordination — September 14

This checkpoint supersedes the preceding live-state descriptions, which retain their historical outcomes. Pass6 was applied, followed by complete Python verification: owned run `6196264d-15cc-4b0a-8f69-fb4da925e908` completed with exit0 and process-group closure in581.024seconds; the known-case-tested per-file summary reports1226 tests across44 modules. Pass6 Borg/circular builds and9 circular controls passed. These receipts describe pass6, not later source generations.

The operator and designated `Commit and push changes` task clarified that B-CLOSE-2's Partial label does not prevent checkpoint publication. The currently authorized publication is commit and push only. The publication runner requires a coherent frozen candidate, intended-file inventory, no writers, accurate unfinished-work disposition, and its mandatory exact-state gate after staging. It has not run that gate on this batch. Pending RSS-ordering and fixture corrections were identified as defects affecting this checkpoint, not unrelated deferred features. The operator selected completing these repairs and validation before returning the candidate to the runner; no full campaign closure or scientific renewal is required for checkpoint publication.

Pass7 applies exactly two reviewed files: the bounded coordinator captures the authenticated streamed caller/spec/ps observer before the first source bind and observes RSS after captured helper imports; the streamed test fixes nested source-map placement and ensures malformed-receipt negatives reach the closed-target gate. The original one-second observation interval, first clock, runtime limits and negative assertions remain. The measured retained failure observed1054.366ms before the first sample; the candidate diagnostic observed486.420ms and completed. Exact applied patches are in [pass7 evidence](../evidence/option-b-production-regression-pass7.json). Do not rerun any applied source or map scripts.

After pass7, `audit-current-maps.mjs` checks19 maps/1295 Source bindings without mismatch; `check-source-chain.mjs` checks194 final source paths and198 protected original versions. The current production graph has294 sources. The component resource census measures487 retained source paths/27,759,814bytes with no missing paths; runtime/spec/output additions still require actual launch checks under unchanged512-source/1GiB limits. Retention and `git diff --check` pass. These are scoped integrity measurements, not execution or scientific acceptance.

The candidate snapshot now binds682 paths in [the validation snapshot](../evidence/option-b-production-validation-snapshot.json). Its superseded pass6 snapshot and explicit supersession receipt are preserved under `.tmp/option-b-close2/regression-pass7/`; pass6 Python run identity above remains the durable recovery pointer. Full pass7 streamed validation is active as `da28c0cb-b081-4984-b55c-be2bd5ab51ce`, with retained fixtures and unchanged test envelopes. Latest-generation native preparation and remaining connected Node validation follow. Source edits are frozen during this validation. The runner is waiting for an explicit readiness handoff; no Git mutations are performed by this task.

Pass7 validation update: full streamed run `da28c0cb-b081-4984-b55c-be2bd5ab51ce` was stopped after319.178seconds with authenticated process-group closure. Normal and archives passed, but exhausted/package reached later pre-target RSS-gap rejections; retained diagnostics establish1959.393ms and2008.104ms failure points respectively. The first-observation correction alone is insufficient. A reviewed scratch pass8 cadence candidate uses the existing checkpoint API between admission/capture phases, preserving the original clock, one-second interval and sticky failure. It is not applied. Individual synchronous phases must still fit the existing bound; static review is not a timing guarantee.

Bounded pass7 Python run `5160aee5-2a6d-4b1c-b421-404b8a9e75e1` completed74/74 tests with exit0/groupclosed in63.565seconds: accepted-frame25, refined acceleration36 and emission execution13. These supplement, rather than relabel, the pass6 full1226 test receipt. Both pass7 compiled preparations completed with exit0/groupclosed and Borg's protocol handshake passed. The exact native environment is `.tmp/option-b-close2/native-final-env.json`.

Connected75-file Node run `af2ee08e-057d-469c-913e-c68821b36a51` is active under a1200second supervisor deadline. It excludes the separately managed full streamed file. Emission/refined process controls report competing-computation rejection; exact process-row diagnosis is assigned to the Python worker before any attribution or repair. The streamed EPIPE fixture separately lacked a guaranteed diagnostic write after pipe closure before its original15second deadline; a scratch fixture correction will require a witnessed EPIPE callback and retain cleanup assertions. No production deadline is raised. The checkpoint remains unready for publication pending these bounded corrections and terminal connected validation; Partial campaign status is not itself the blocker.

Connected75-file run `af2ee08e-057d-469c-913e-c68821b36a51` is terminal failed, exit1/groupclosed,403.891seconds. TAP reports866 tests:845pass/21fail. The known-case-tested failure extractor assigns all21 failures to emission/refined-acceleration process suites and the single coordinator-process positive. Their positive errors and retained coordinator rejection identify competitor gates; negative tests stopped before intended injected stages. No code repair is inferred from these failures. Rerun all three complete files after safe fixture closure, retaining the failed receipt.

The competitor instrument authenticated a stopped Python/gate group from retained streamed fixture `Odf83K`, inner run `6edc2fa6-bc5d-41b6-b483-ffe49aaf39e9`. The outer group's closed result did not establish closure of detached nested groups. F5 matched the gate and child against their original birth/group/command evidence, re-read full identities, then resumed only the exact stopped gate; its existing lost-control handler closed its own group. Fresh process inspection finds neither original PID nor group. The inner lease remains stale-closed rather than being relabeled successful. Recovery scripts/evidence are under `.tmp/option-b-close2/stream-cadence-pass8/`. All later controls must confirm nested closure as well as outer ownership before handoff.

### Pass8 applied checkpoint — September 14

Applied the reviewed coordinator/stream cadence changes and streamed fixture EPIPE/failure-cleanup changes (three source files). The coordinator and stream use existing observations at admission seams; original clocks, one-second maximum observation gap, quotas, source identity checks and negative outcomes remain. Known-clock control, normal/exhausted/package, unchanged final-gap negative and all six renamed-directory controls passed on the candidate; actual EPIPE callback and containment assertions passed. The added fixture cleanup authenticates an original stopped gate and resumes only that gate after its coordinator is absent, leaving the admitted gate's lost-control handler responsible for its group. Its positive and adversarial identity controls passed; a full run remains required to establish actual fixture behavior.

The role review independently found seven repair-review JSONs had been included as runtime scientific-control Source rows solely by the graph author. They are not original-index targets/versions, scientific identity suppliers, or historical198 obligations, and source/API searches found no execution consumer. Pass8 removes only those runtime memberships and keeps all files immutable, referenced by exact path/SHA from the selected transfer proof. The new pass8 report is likewise review evidence rather than a runtime dependency. `check-review-evidence.mjs` passed known correct/mutated controls before verifying all eight exact references. The role correction and source patches are in [pass8 evidence](../evidence/option-b-production-regression-pass8.json). The projected multi-occupant final physical count is507; this is a projection until the actual launch verifies it, not a changed512 limit.

`audit-current-maps.mjs` now verifies19maps/1288Source bindings without mismatch; production graph287sources. `check-source-chain.mjs` verifies194 current source paths through eight applied repair passes and all198 protected original versions. Component census measures480paths/26,926,755bytes, no missing files; runtime/output additions retain original checks. Retention passes1618 machinefiles. The current validation snapshot binds675 paths; superseded pass7 snapshot and explicit receipt remain under `.tmp/option-b-close2/regression-pass8/`.

Source bytes are frozen. The Python worker runs the three complete competitor-affected Node files first, then the stream worker runs the full streamed suite. Native preparation is coordinated outside that execution window. Prior pass7 connected845passes/21failures and74Pythonpasses, plus pass6 full1226Pythonpasses, remain explicitly generation-scoped. No current full-B or scientific closure is claimed. Publication runner awaits an explicit checkpoint readiness handoff; this task performs no Git writes.

Pass8 full streamed run `ae1d7e7d-2e77-4242-96ff-27e5aabe12da` completed failed with77tests/73pass/4fail, no skips/cancellations,518.485seconds and outer group closed; retained fixture closure records report originally observed processes absent. Actual normal/exhausted/archives/multi-occupant/package and overflow/hardlink negatives pass. The multi-occupant quota repair is now verified by actual execution under the unchanged512 cap, rather than only projected.

Three remaining failures (poststdout invalidation, cleanup rename-invalid, prestdout rename-valid) reached the test harness's15second outer interruption before the intended assertion boundary; retained outcomes are timeouts with closed observed groups, not demonstrated production-clock expiration. The private-growth fixture fails before launch because it still expects to replace an embedded hash that migration removed. Borg owns one final test-only candidate correcting actual binding refresh and raising only the external test harness allowance from15 to30seconds. Production CONTROL_PROFILE120seconds/work90/K75, default production budgets, one-second RSS interval,512-source/1GiB and all assertions remain unchanged. Authorized diagnostic `c76901d6` with30second harness allowance passes the exact poststdout assertions in9.736seconds, supervisor10.374seconds/groupsclosed. This establishes finite correct behavior and observed harness variability, not a claim that30seconds is a measured minimum requirement. Source candidate and full rerun remain pending; no checkpoint readiness claim yet.

### Low-token recovery checkpoint — pass9 active

Operator requested roughly minute-by-minute checkpoints at5percent remaining tokens. Current applied generation is pass9: one streamed-test file changes external harness15→30seconds and fixes private-growth fixture binding refresh. Production code/limits are unchanged from pass8. All four formerly failing cases passed in `c97b95dd-a5eb-4cc6-bd5a-71207a7033ff` (107.231seconds, groupclosed). Pass9 source/map scripts are applied; NEVER rerun them. Production graph287sources,19maps/1288Sourcebindings pass; all9immutable review report references pass offline SHA verification. Current validation snapshot covers675paths; prior snapshots/supersession receipts are under each `.tmp/option-b-close2/regression-passN/` owner.

ACTIVE: final actual no-hook full77 streamed run `e0a96320-0abb-444f-be39-bb6c61b75b93`, start06:53:13UTC, deadline07:18:13UTC,1500second outer/15second heartbeat. Logs `.local-data/owned-compute/logs/<runId>.stdout.log` and `.stderr.log`; F5 worker `/root/b_close_2_f5_circular` owns watching, terminal totals and nested closure verification. Rootowner remains `01a0989b-ef03-7a22-ad38-c94ae7b40a56`. Do not launch competing heavy work or edit source during this run. Use registered supervisor status/stop only, not PID/name guesses. Test fixture groups are nested and require their retained external-closure records in addition to the outer lease. Exact stopped-gate recovery is recorded above; no current unresolved group reported.

NEXT AFTER TERMINAL: if77pass, ask Borg worker to refresh pass9 native admission receipts/handshake outside the stream window (pass8 native receipts exist, pass9 related records changed; `.tmp/option-b-close2/pass9-changed-records.json` inventories46changed dependent records). Use followup_task to wake completed agents; send_message alone does not restart them. Validate current snapshot, all maps, source-chain,9reviewrefs, retention/dispositions and whitespace; record final scope and inspect root owned-compute closeout. Python evidence remains pass6full1226/pass7bounded74; all106recordedPython paths were unchanged throughpass8 and onlytestJS changedpass9. Pass8 three complete process files pass26/26 after exact fixture-orphan cleanup. Pass7 broad75files passed845/866 with21competitor-gated failures; the three pass8 reruns replace that missing fault-stage evidence, not the entire suite generation. Do not claim all broad tests ran onpass9 or fullBclosure.

The designated publication task is `Commit and push changes`, id `01a0957f-bb53-7560-a762-2104d6856278`. Current authority there is commit/push only. It has been told to HOLD staging/gate until explicitready. After checkpoint repairs/verification complete, update this handoff/reconciliation/worklog with honest remaining B-CLOSE2/3/4 obligations and send ready disposition plus frozen source/snapshot, evidence and no-writer statement. It runs the mandatory final staged exact-state gate; do not perform Git mutations here. Partial queue status alone is not a publication blocker. User-selected objective remains finish checkpoint repairs/validation and return frozen candidate to that runner; no additional 'do1' needed.

Minute checkpoint: pass9 full run has reached normal/exhausted actual execution, both passing in the recorded stdout. `candidate-snapshot.mjs --check` confirms all675 captured paths unchanged. Continue existing run `e0a96320-0abb-444f-be39-bb6c61b75b93`; do not relaunch. Terminal verification and native receipt refresh remain next.

Minute checkpoint: pass9 full run remains the only selected compute; source edits remain frozen. Its retained stdout is the progress instrument. Recover by inspecting the existing registered run and the F5 worker receipt, not by starting another suite. Publication task still awaits explicit readiness.

Minute checkpoint: pass9 source-chain audit passes194 current paths through9applied repairs and198 protected versions; review-evidence audit verifies9records; retention passes1619machine files; whitespace check passes. Existing full77 run continues. These checks do not replace terminal test evidence or the publication runner's exact-state gate.

Minute checkpoint: existing pass9 run continues through containment/final-boundary controls; no new source application or competing launch is authorized during it. Final native receipt freshness, terminal source snapshot and owned-compute closeout remain after this run, followed by explicit readiness handoff to designated commit/push runner.

Minute checkpoint: final native refresh is queued with Borg, preparation only; exact planned paths are `.tmp/option-b-close2/borg/pass9/pending.json`. Borg will wait for F5 fullstream terminal, then refresh both admission receipts/handshake and9circular controls. Source generation remains pass9. Fullstream stdout already records passing EPIPE, poststdout invalidation and rename-cleanup controls; continue the existing run.

Minute checkpoint: full77 pass9 run remains active through later renamed-directory boundaries; earlier normal, multi-occupant, package, EPIPE and poststdout checks passed. Source edits remain frozen; recover via run e0a96320 rather than rerunning. No readiness release has been sent to publication runner.

Minute checkpoint: all six pass9 renamed-directory controls and active-monitor rejection passed in current fullrun stdout. Await private-growth and final-log boundary controls plus terminal totals/closure. Native refresh then final audits/readiness handoff remain unchanged; no source writes or new candidate required at this checkpoint.

### Pass9 full streamed result

Run `e0a96320-0abb-444f-be39-bb6c61b75b93` stdout reports77tests/77pass/0fail/0skip/0cancel,697.721test seconds. All previously failing fixture paths now pass within the reviewed30second test-harness allowance; production clocks, one-second sampling and source/resource caps remain unchanged. F5 is verifying terminal lease and every nested external-closure receipt; do not substitute TAP success for those checks. Borg's native refresh window follows that confirmation. Then root reruns final source snapshot/audits/closeout, records checkpoint readiness and sends the designated commit/push runner the frozen handoff.

Minute checkpoint: root supervisor status confirms full77 run completed697.849seconds, exit0/processGroupClosed=true. F5's known-case-tested extractor verifies all45 retained fixture external-closure receipts, each with nonempty original process census, groupclosed and allObservedAbsent=true. Receipt `.tmp/option-b-close2/stream-cadence-pass8/final-closure.json`. No further streamed rerun needed. Borg final native jobs8547a7ca and38a56cc8 are active; exact IDs/paths are retained in `.tmp/option-b-close2/borg/pass9/pending.json`. Handshake and9circular controls follow. Final source snapshot/checks and publication ready handoff remain next; do not start more source work.

Minute checkpoint: full streamed and nested closure remain verified; final native jobs are still represented by Borg's pass9 pending.json. No source edits, additional tests or publication staging should start before its terminal receipt. The current675-path snapshot is the frozen candidate identity for final handoff.


### Final pass9 checkpoint ready — September 14

This entry supersedes the pending actions above. The final streamed run `e0a96320-0abb-444f-be39-bb6c61b75b93` completed with77/77pass, zero failures/skips/cancellations and authenticated outer closure; the closure extractor verifies all45 nested fixture receipts. Native pass9 builds `8547a7ca-44e7-4a80-b1c1-4c7e342613b7` and `38a56cc8-f749-4cd6-8aea-c1e63a96323e` completed successfully with groups closed. The native worker records Borg V11 admission/disposal and9/9 circular controls without skips in `.tmp/option-b-close2/borg/pass9/build-verification.json`; pending.json is complete/noActiveJobs. No scientific evolution or acceptance is claimed.

Root supervisor closeout reports `clear` for owner `01a0989b-ef03-7a22-ad38-c94ae7b40a56` (1126 registered leases checked). Final audits verify675 unchanged snapshot paths,19maps/1288Sourcebindings,194 current source paths through nine repair passes,198 protected versions and9 exact review-report references. Earlier Python evidence remains pass6 full1226 and pass7 bounded74; pass8 complete three-file process rerun passes26/26, covering the21 competitor-gated failures in the retained pass7 broad866-test run. This is not a claim that the broad suite or Python suite ran anew on pass9.

Checkpoint repairs and scoped verification are complete; root and workers are frozen for publication. Release this candidate to designated task `Commit and push changes` (`01a0957f-bb53-7560-a762-2104d6856278`) for combined inventory and its mandatory `node scripts/pr-validation-receipt.mjs run --base origin/main` gate. That task has commit/push authority only. Preserve ignored scratch/runtime evidence; do not stage `.tmp/` or `.local-data/`. B-CLOSE-2 remains Partial pending full obligation/retirement reconciliation; B-CLOSE-3/4 and full A retirement remain open. Those honest campaign statuses alone do not block checkpoint publication. Next action is the publication runner gate, not another migration or test replay.

### Pass10 publication whitespace correction — September 14

The publication runner reports its pass9 gate completed successfully (26 foundational checks,31 required Content Integrity checks and animator wiring); its staged whitespace check found one trailing ASCII space in the new projection source at line35, outside a string. The earlier unstaged whitespace check did not cover that new file. Under the runner's explicit mechanical-correction window, root removed exactly that byte and recorded [pass10 evidence](../evidence/option-b-production-regression-pass10.json). All earlier repair reports remain immutable. No semantics, scientific constants, roles, limits or refresh eligibility changed.

Minute recovery checkpoint: pass10 source/maps are applied; do not rerun earlier application scripts. The known-case-tested graph comparison verifies18 graphs unchanged except Source digests; current-map audit verifies19maps/1288bindings, source-chain audit verifies194current paths through10passes and198protected versions, and review-evidence audit verifies10exact reports. The prior675-path snapshot and explicit supersession receipt are retained in `.tmp/option-b-close2/regression-pass10/`; the new snapshot captures675paths. Retention passes1620machine files and working-tree whitespace passes. Borg worker owns unchanged-header comparison and fresh native admission/build evidence under `.tmp/option-b-close2/borg/pass10/`; source/maps remain frozen during that verification. Next: terminal native evidence and owned closeout, exact changed-path inventory, then return to the publication runner for restaging and a new exact-state gate. Earlier pass9 and older broad test evidence must retain its recorded generation.

Minute recovery checkpoint: generated-header `cmp` passes against pass9 (2369bytes). The only selected jobs are native receipt refreshes `e785973e-886a-4e2d-8376-fceeb4001641` and `eb53a444-59e1-499c-9d46-3b243bd53414`, watched by the Borg worker. Exact pending state lives in `.tmp/option-b-close2/borg/pass10/pending.json`. Recover those jobs, not new runs. Native admission/disposal, final closeout and the exact-path readiness handoff follow; no broad suite or scientific execution is selected.

**Pass10 ready:** both native preparations completed with exit0 and closed groups (Borg22.569seconds, circular39.006seconds); generated and compiled headers match pass9 exactly, and default Borg V11 admission/disposal passes. The exact receipt is `.tmp/option-b-close2/borg/pass10/build-verification.json`; current environment is `.tmp/option-b-close2/native-final-env.json`. Root supervisor closeout reports clear for this task (1129leases checked). No suites were rerun or scientific evolution executed for this whitespace correction. The exact changed/new path inventory for restaging is `.tmp/option-b-close2/regression-pass10/publication-inventory.json`, with before/after hashes and one new immutable review report. Root and workers are frozen again; the designated runner now reviews that inventory, restages those paths and reruns its mandatory exact-state gate before any commit/push. This supersedes the active-job instructions immediately above.
