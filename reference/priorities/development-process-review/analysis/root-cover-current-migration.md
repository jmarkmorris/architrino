# Current one-cell root-cover migration

The baseline and cached one-cell root-cover launchers now select the current registered supervisor and host memory observer under an explicit version-two machine plan. This repairs their helper-generation mismatch introduced by the preceding streamed repair. The [current-execution contract](../contracts/root-cover-current-execution-v2.md) keeps the mathematical subject, independent comparison, historical source identities, one-cell scope, resource limits and rejection rules separate from this operational migration.

## What changed and why

A launch plan names the exact program bytes to execute. The two current entries now require their respective `pilot-launch.v2` schemas and reject the earlier machine-plan generation. Their outer-helper selections match `58f5fa058727e212cc98a32f04eb3d94c64c6a8185f9cc8a8114d9a034343b8c`; their host memory-observer selections match `ba1ce108f7f91e55bdcb7f5dd267c39484eb51bc6b8135814678c0f8c045a6da`. These are current execution identities, not replacement identities for old receipts.

The full-run source-construction tests previously read the current cached pilot while requiring its earlier bytes. Their original expected digests and transformations remain unchanged, but they now read [preserved nonexecuting sources](../evidence/root-cover-migration/preserved-sources.json) from the clean starting commit `d08cbad4aa8fbbed8a734adfe7affdf15ac4f322`. The baseline-to-cached construction control continues to compare the two current implementations. Neither source transformation is an independent mathematical validation.

The metadata-only [current plan validation](../evidence/root-cover-migration/current-plan-validation.json) records separate current plans validated with the existing entry APIs. It checks the original scientific dependencies rather than changing their selections to fit current files. The baseline plan validates 181 distinct dependencies and the cached plan 188. The actual memory utility reports 71 percent free memory; the existing parser accepts that output and the observed disk availability at capture time. These observations are host-specific and do not predict resources at a later launch.

## Measured verification

The [six retained bounded runs](../evidence/root-cover-migration/validation-runs.json) record commands, logs, deadlines and terminal process-group closure. The final two launcher suites pass 57 tests with zero failures, skips or cancellations. The existing two process suites pass 12 synthetic controls, including normal return, child failure, slow publication, startup interruption, lost monitoring and monitor-log failure. Their separate rerun under the [independent observer](../evidence/root-cover-migration/observe-process-controls.mjs) also passes; 171 process snapshots enroll 118 process identities, with no observed identities or groups present in the final host census. The [observation record](../evidence/root-cover-migration/external-process-observation.json) distinguishes that sampled absence result from complete knowledge of every short-lived process.

The two selected full-run historical-construction controls pass. They establish only that the preserved predecessor bytes produce the expected full-run source transformations; the full-run current binding failure remains open. Across the named suites there are 71 distinct passing tests, with repeated process and unit runs recorded separately rather than added as new coverage.

Before target capture, the metadata instrument passes the known SHA-256 `abc` case and literal host-memory/disk boundary cases. It then verifies all current plan bindings and rejects four mutations per profile: an old schema, old outer helper, old memory utility and full scope. The independent process observer passes literal descendant, reparented-group, reused-birth and process-row parsing controls before observing the host. The ordinary machine-plan tests also require rejection of the old schema.

`git diff --exit-code HEAD -- 'scripts/eom/*.py' scripts/eom/oracle reference/priorities/braid-program/evidence reference/priorities/development-process-review/evidence/variable-cell-migration reference/priorities/development-process-review/evidence/source-recovery` returns zero: those mathematical, original-evidence and existing historical-route scopes are unchanged. No scientific stage, broad sweep, compiled solver run, staging, commit or push occurred.

## Remaining obligation

This batch establishes current composition and bounded synthetic process behavior for the two one-cell profiles. The current machine plans are metadata selections, not accepted numerical pilot results. Full-run execution still requires its separate current composition, actual pilot evidence and resource acceptance. Acceleration and prescribed-response callers require review of their differing declarations or inputs; emission/refined callers additionally require a separately reviewed new-run interface that preserves historical transport predicates. F5 and circular-ladder migration remains open.

A mismatching current dependency, accepted mixed-generation plan, changed preserved source digest, observed surviving process or admitted resource violation overturns the corresponding result. The retained commands and records provide the concrete checks. The wider development-process campaign remains active.
