# Final recovery test coverage

Status legend: **✓ Done** = completed at the stated measured scope; **◐ Partial** = some required work remains; **○ Not done** = not executed or accepted. **Blocked** names an unmet prerequisite; **Deferred** names the condition for reopening. Historical tables below retain their original observed outcomes and are not current action lists.

## Approved items 1–8 follow-through

**◐ Partial — circular coverage; ✓ Done — selected candidate ladder.** The [current circular execution record](circular-current-execution.md) retains the complete accepted 32-phase, 2,448-row pilot. The [final independent checkpoint review](../evidence/circular-current-execution/current-rung128-independent-review.md) accepts the selected candidate’s complete 8/32/128 ladder: 168 phases and 6,048 rows, with the final rung’s external owner closed in 1,206.518 seconds. The later [second-candidate review](circular-next-candidate-review.md) accepts its eight-phase rung (288 rows); its 32/128 continuation and eight unstarted within-cap candidates remain. Six over-cap candidates retain explicit resource-return conditions. Explicitly enable `AAA_CIRCULAR_CURRENT_CONTEXT=1` for the retained reviewed-build/manifest/proof test profile; a default skip is not a pass.

**✓ Done — F5 bounded execution acceptance.** The [F5 record](f5-remaining-callers.md) retains the independently reviewed complete 8/32/128 ladder, 24,192 rows and false H3 eligibility. Explicit historical test scheduling is recorded below; ordinary live-tree historical-input failures remain distinct.

The [supervision and omitted-coverage record](supervision-and-omitted-coverage.md) closes the startup deadline controls and executes both SDK conformance profiles with SDK 1.30.0. Stdio passes. The resumed HTTP run `71f1372c-0c87-44e8-abb0-098b8ee17b92` now passes against SDK 1.30.0 after authored snapshot identity repair and rate-compliant request pacing; its closed process group and 10.586-second duration are recorded in the coverage owner. The sealed compact database remains missing. Eight scientific/input-dependent standalone checks retain their exact required inputs and are outside the operational execution selection under the [scientific-consumption disposition](scientific-consumption-disposition.md). Final candidate validation, including both Node selections and applicable GitHub jobs, was item 9 and was excluded from this continuation. None of these scoped results establishes overall health.

The first inventory is a discovery baseline, not a completed test run. The [candidate inventory](../evidence/test-coverage-inventory.json) was produced by `git ls-files -c -o --exclude-standard -z` and a path classifier after its positive and exclusion controls passed. It identifies 460 candidate paths: 326 JavaScript tests, 82 Python tests, 49 standalone check candidates, one CMake registration file and two GitHub workflow files. These are filenames, not test-case counts, independent coverage units or passing results.

## Execution groups to reconcile

| Group | Status | Existing entrypoint or owner | Remaining coverage work |
| --- | --- | --- | --- |
| JavaScript tests | ◐ Partial — reconcile remaining failures, skips and exact inputs | `scripts/run-test-sweep.mjs`, normal and `--slow` selections | Reconcile every candidate against selection, nested skips, prerequisites and process controls; files outside its naming rules remain explicit. |
| Python tests | ◐ Partial — reconcile remaining failures, skips and exact inputs | Shared-venv test modules; subject owners' reproduction commands | Determine discovery/import requirements and fixtures per family. System Python is not a substitute. |
| EOM solver compiled checks | ✓ Done — seven registered CTest entries at the recorded build; later CLI coverage remains separately owned | `src/eom/CMakeLists.txt` | Rebuild through the supported build owner, enumerate CTest registrations and any CLI checks outside CTest, then retain actual outcomes. |
| Standalone checks | ◐ Partial — reconcile remaining failures, skips and exact inputs | Check/validate scripts and package/procedure entrypoints | Separate directly runnable checks from libraries, destructive modes and generators; identify checks not discoverable by filename. |
| Publication | ○ Not done — Deferred until separately authorized final candidate validation (excluded item 9) | `scripts/pr-validation-receipt.mjs` | Validate the exact final candidate against the correct base; report the optional sweep independently of the required exit status. |
| GitHub | ○ Not done — Deferred until separately authorized final candidate validation (excluded item 9) | `.github/workflows/content-integrity.yml` and `pages.yml` | Verify applicable jobs at the published candidate; distinguish PR build from deployment after an authorized main merge. |

Reading `src/eom/CMakeLists.txt` identifies CTest entries for solver fixtures, acceleration fixtures, history pin lifetime, history terminal-state equivalence, evolution fixtures, stationary joint frontier fixtures and the Borg protocol version. This is the declared CTest selection, not proof that it exhausts solver tests.

Every applicable test or check needs an observed pass, failure, timeout, cancellation, skip or blocked prerequisite at an identified source state. Blocked and unexecuted coverage cannot count as all passed. No broad sweep, new scientific campaign, GitHub dispatch or publication was performed while constructing this inventory. The next reconciliation must include package scripts, invoked subprocesses, test-level exclusions and procedure-owned checks before calling the inventory exhaustive.

## Entrypoint reconciliation, 2026-09-08

The [entrypoint record](../evidence/test-entrypoint-reconciliation.json) records the source digests and actual selections. Calling the existing `selectTestFiles` functions without executing their selections yields 316 normal and ten slow Node files. Comparing their union against the 326 JavaScript candidates finds no missing or extra paths in this declared naming scope. It does not discover tests hidden behind another filename convention or prove that every test inside a selected file will execute.

A recording callback supplied to the existing `runChecks` function captures 34 default child commands and one omitted opt-in sweep. The callback spawns no subprocess and its synthetic success values are discarded; this inspection supplies commands, not test results. A known one-command/one-exclusion fixture passed before the real gate was inspected. The CMake registration extractor likewise passed a known multiline registration and commented-out exclusion before returning the seven actual entries. Both instruments are retained in [reconcile-test-entrypoints.mjs](../evidence/acceleration-migration/reconcile-test-entrypoints.mjs).

The record also enumerates the package commands and the commands of both GitHub build jobs by reading their live workflow files. The package's `pretest` prepares ignored runtime assets, `test` invokes Node discovery, and `build:site` builds the static site. None invokes the Python or CTest families. Content Integrity's GitHub job separately runs the production dependency audit; Pages adds deployment image tests, reconstruction from a fresh checkout, the static build, and Borg byte-identity validation. Pages deployment additionally depends on an enabled main-branch publishing path and its successful build, so a PR build cannot establish deployment success.

At the inventory stage, Python execution, standalone roles and additional naming conventions remained unresolved. The execution results below advance that baseline. Final execution must bind the then-current source state; the entrypoint record's source digests make later changes detectable.

## Baseline review and recovery execution, September 8

The operator's instruction to do all three follow-up items activated baseline review, remaining caller work and coverage execution. These results have different source states and scopes. They cannot be added together as independent coverage or represented as a single final-candidate certificate. The [caller-contract review](remaining-caller-contracts.md) distinguishes repaired operational paths from uncompleted new-run interfaces and scientific prerequisites.

| Execution | Measured outcome and instrument | Boundary |
| --- | --- | --- |
| Ordinary Python baseline | The [native unittest extraction](../evidence/final-validation/python-baseline-results.json) records 77 modules, 1,601 executed cases, and three failed modules. | Shared venv; no missing native summary and no reported skips. A setUpClass failure runs zero cases in that module. |
| Compiled Python baseline | The same extraction records five modules, 126 native unittest cases, all passed. | Each module builds its required executable through its setUpClass path; this is separate from CTest. |
| Original-input F5 regression | Native unittest in run `466269d7-1b9d-4248-bbf6-00073b2bdce7` reports 18 API/domain and 14 manifest tests passed. | Unchanged tests and oracle bytes in an explicitly assembled temporary input tree. Does not repair the live input loader or establish a new scientific result. |
| Fresh CMake/CTest | Run `95ea20ac-07d2-41ca-9447-9d6686915464` configures and builds a fresh Release tree, then reports all seven registered CTest tests passed in 83.04 seconds. | [Retained driver](../evidence/final-validation/run-ctest.sh); source build precedes testing. No assertion that CTest exhausts solver validation. |
| Content Integrity | Run `a8e3351d-a31f-433d-8d45-b3b0997ae2e3` reports 34 required checks passed, zero required or reporting failures, and one omitted opt-in sweep. | Gate completed before later code and documentation edits. Its success does not include the optional sweep. |
| Standalone check/validate selection | [First selection](../evidence/final-validation/standalone-results.json): 23 commands, 19 passed and four failed by child exit status. | The first driver returned zero after recording child failures; only each child result determines its outcome. The retained driver now propagates failures. |
| Additional audit/verify/generator selection | [Additional selection](../evidence/final-validation/additional-check-results.json): 14 commands, ten passed and four failed by child exit status. | Check modes only; no authored-source or generated-evidence rewrite. |
| Refined operational process suite | Run `0c85f00b-f410-4da3-bd59-6a944bd202ca`: twelve tests passed after correcting the current helper/outer bindings. | Synthetic operational controls; Python version-two execution remains unimplemented. |
| Circular-phase process suite | [Repaired suite](../evidence/final-validation/circular-phase-repair-results.json): 18 tests passed after a known-negative denied-probe control. | Includes persistent denial refusing group closure and termination of monitoring; does not accept any scientific phase or rung. |
| Prior slow Node baseline | Run `27e1d274-2a54-4cb4-9efd-cef4f8ec6868` reports 124 tests passed in ten selected files, without failures or skips. | Earlier completed baseline, not rerun or certified against the final working candidate. |

The [terminal record collection](../evidence/final-validation/completed-runs.json) retains run IDs, commands, terminal states and log byte bindings. Baseline parsing controls passed before target inspection, including a zero-test Python failure and an absent-summary rejection. Ordinary Python failures are `test_f5_api_domain_conformance.py` (two errors in 18 tests), `test_f5_history_manifest_conformance.py` (setUpClass failure, zero executed cases), and `test_orthogonal_plane_weave_fold_limit_certificate.py` (one failure and one error in seven tests). The first two reject the current configuration where original bytes are required. The third requires the retained master-equation generation. Reconstructing original F5 inputs proves that the two original test suites can run; it does not erase the live baseline failures.

## Standalone failures and prerequisites

The first standalone selection reports response/snapshot drift in the Archie fixture server, tool contracts and remote deployment contract, and a stale owner binding in the Borg scientific-status projection. The additional selection reports stale Markdown heading-icon override aliases, generated reference-surface drift, a stale machine-artifact receipt, and generated Archie source-index drift. The machine-artifact receipt checker stops at its first mismatch, so later receipt targets are not established by that command. Archie response/recall expectations remain with OPS-023; Borg owner semantics remain with the associated application and scientific owners. These are failures, not waivers.

The [49-path disposition record](../evidence/final-validation/standalone-dispositions.json) maps the original check/validate candidates to executed gate commands, standalone commands or required inputs. At that historical observation, ten candidates were unexecuted: two installed-SDK conformance checks require explicit SDK roots and versions; four scientific Python checkers require selected campaign evidence; two EOM campaign/resume validators require workload manifests or run/checkpoint inputs; and two equation-mapping checkers require their selected input packet. Each row names its prerequisite and owner. No installed SDK was selected at that observation; the later SDK results above supersede that availability statement; a repository `node_modules` search is not a claim about every installation on the host.

The additional `audit`, `verify`, `smoke` and `build-` filename search is a discovery aid, not an exhaustive semantic inventory of every callable program. Scientific artifact verifiers, on-demand export tools and release procedures require their own input and applicability decisions. This remains an explicit limit on the universal all-checks claim.

## Node rerun and GitHub boundary

The first recovery sweep was stopped after a denied OS group probe escaped circular-phase cleanup and left its heartbeat active. The supervisor records that run as stopped with process-group closure; unreached tests were cancelled. Its failure is preserved in [the caller review](remaining-caller-contracts.md#circular-phase-cleanup-repair). The [completed normal rerun](../evidence/final-validation/node-normal-recovery.json), run `1503e3dd-6386-424f-8b12-e18f6ac2be91`, reports 2,628 tests: 2,547 passed, 64 failed, 17 skipped and zero cancelled in 447.378 seconds by the native TAP summary. Its owned terminal record reports exit one and process-group closure. All 64 failed tests are assigned across 29 files in the [current disposition record](../evidence/final-validation/node-normal-recovery-dispositions.json). These are directly observed current-run counts, not subtraction of scoped repairs from the earlier 92-failure baseline.

The seventeen skipped results are retained verbatim in that record. Nine belong to `f5-ordinary-evolution-runner.test.js` and require `F5_RUN_PROCESS_CONTROLS=1`; the [separate enabled run](../evidence/final-validation/f5-opt-in-process-results.json) executes the entire file with no skips or failures. Seven circular-adapter tests require an explicitly selected fresh adapter binary through `AAA_SUBFIELD_CIRCULAR_ADAPTER_BIN`; the CTest build does not register that executable, and the supported preparation path still has the build-review prerequisite described in the caller review. One compact-sweep dashboard test requires the sealed local compact sweep, which was not present at its declared path. The later enabled run does not rewrite the original sweep's skip count, and the remaining eight omissions are not passes.

The full sweep also failed `active private-growth rejection retains first failure after owned cancellation` in the streamed diagnostic suite. Its retained fixture lease `e4335026-cafc-4440-a1ee-687f5b74195b` remained `stopping` after supervisor disappearance. Later host `process.kill(id, 0)` probes returned `ESRCH` for the recorded coordinator 38537, target 38558 and its group, wrapper 38534 and its group, and sidecar 38532. The [isolated unchanged control](../evidence/final-validation/streamed-private-growth-rerun.json) passed, but no causal transition was established and the original failure remains open. Neither the isolated pass nor later absence fabricates a successful terminal result for the failed fixture.

`gh run list --branch codex/peridot --limit 8 --json databaseId,headSha,status,conclusion,workflowName,url` returned [an empty list](../evidence/final-validation/github-branch-runs.json) during this review. That establishes no listed workflow runs for that branch at that observation, not a green GitHub process. Remote build and deployment validation remain dependent on an identified published candidate and applicable workflow execution. No workflow was dispatched and no publication occurred in this batch.

Recovery remains incomplete while the new-run interfaces, failed checks and explicit prerequisites above remain unresolved. A green Content Integrity subset cannot overturn those observed failures or fill missing coverage. A final all-passed claim requires a reconciled applicable selection with zero failures, cancellations, unaccounted skips or blocked checks at the identified candidate, plus its applicable remote results.

## Successor circular coverage follow-through

The [current circular execution review](circular-current-execution.md) closes the seven adapter-test omissions for its recorded fresh binary. Enabling them first exposes a v2/v3 source-schema mismatch: the adapter expects v2 while its authenticated inputs and unchanged independent verifier require v3. After the one-line adapter input-contract repair and fresh build, run `9f1aa65f-d108-40b7-a16b-8dabd23d5a59` passes all 33 selected adapter, preparation and process controls, including all seven optional adapter cases, with no skips. The old full-sweep skip/failure counts remain historical; no arithmetic subtraction or whole-suite rerun is implied. A current manifest additionally passes the unchanged analytic proof and an EOF-only process handshake with zero root requests. That was the boundary of this historical adapter run. The later complete pilot is independently accepted; the ladder remains governed by its current resource-plan and memory-observer requirements.

## ✓ Done — explicit historical-input execution selection

The canonical selector is `node scripts/run-historical-input-tests.mjs f5` or `node scripts/run-historical-input-tests.mjs fold`, launched through the owned supervisor. The F5 selection creates a fresh original-input root with the accepted materializer, authenticates it before and after execution, runs the three declared Node files serially, and runs only the two copied F5 conformance modules using the absolute shared venv. The fold selection invokes the retained original-input driver, which authenticates its four frozen inputs including the exact historical master equation before running the unchanged seven-test module. A child failure propagates as a failed selection; no unavailable input is replaced with synthetic data. F5 roots remain retained for inspection; fold's existing disposable-tree behavior is preserved.

Before execution, the known invalid selector returned exit two without a target launch, and the materializer's four controls passed in owned run `ba441d61-8024-485f-8cd6-268894f6db45`. Actual selection `5f3dcba7-9751-424c-a77c-7f48736cae7c` reports 41 Node tests passed with zero failures/skips and 32 Python tests passed; its input checks pass before and after, and the outer lease exits zero with a closed group in 26.379 seconds. Fold selection `596c9a81-f88b-45fe-b06f-70251cac2895` reports seven Python tests passed and exits zero with a closed group in 4.285 seconds. [Native logs, source identities and terminal summaries](../evidence/historical-input-selection/results.json) retain these measured scopes. These are original-input regression results, not current-equation scientific acceptance or erasure of the earlier ordinary baseline failures.

Reproduce each selection from the repository root, using a distinct supervisor lease:

```sh
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 600 --cwd . -- node scripts/run-historical-input-tests.mjs f5
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 300 --cwd . -- node scripts/run-historical-input-tests.mjs fold
```

The shared venv must execute at `AAA_VENV` or the repository-adjacent `.venv`; system Python is never a fallback. Missing archived bytes, changed source capture, a failing child, or an unclosed outer lease overturns the corresponding bounded pass. The recorded explicit selection supplements ordinary discovery; it does not silently intercept or repin the historical assertions in ordinary discovery.

Before the three status-document edits, basename `rg` across `scripts/`, `tests/` and `reference/priorities/development-process-review/` found navigation links and disposition-owner references, with no content-digest binder for those documents in that scope. Historical JSON receipts and admission-bound F5 review artifacts were preserved. Broad final-candidate and GitHub execution remain **○ Not done — Deferred** until separately authorized item 9.
