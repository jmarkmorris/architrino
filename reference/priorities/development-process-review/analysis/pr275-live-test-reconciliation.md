# PR 275 live integration expectation reconciliation

The operator authorized reconciliation of the failing test and its protected source binding, followed by completion of PR 275. GitHub's equation-dependency-map integration failed at the expectation that the live finite-ledger chain was unchanged. The retained local trial independently records a changed Master Equation file, no changed mathematical graph objects or relationships, and required review. The source wording changed outside the selected equations; that still requires review under the existing report contract.

The live integration now requires `required` when changed files or raw map differences are reported, otherwise `unchanged-relative-to-selected-baseline`, and separately retains `approval: not-granted`. Its four executed-check assertions, missing-map rejection and output-identity checks remain. Isolated unchanged and changed graph controls remain independent tests. No subject implementation or accepted mathematical baseline is modified. This is reconciliation of integration applicability, not mathematical acceptance.

The original controlled-fixture checkpoint and transition remain immutable. The separately named PR275 checkpoint records this explicitly authorized protected-test revision and selects the revised graph with unchanged source roles and empty future refresh eligibility. The original independent known-answer payloads, historical tuples, source census and graph edges are preserved; incident edge revisions follow the changed source revision. The evidence record binds the exact predecessor and successor test digests. This is an explicit new checkpoint, not a candidate granting itself refresh eligibility under the old checkpoint.

Validation must include the two report-only workflow test files, the controlled-fixture admission controls, the full exact-state publication gate, and fresh GitHub checks. Failure of unchanged/changed controls, alteration of mathematical acceptance or original payloads, or permission to refresh a protected source without another explicit decision falsifies the intended boundary.
