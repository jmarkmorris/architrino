# Acceleration and prescribed-response input review

The prescribed-response input migration is now implemented below. The acceleration interface remains the next object; the original review records why a shared launcher hash change alone cannot repair it.

The next caller migration cannot use the root-cover schema change unchanged. The root-cover Python contract ignores the outer operational schema; the acceleration subject and verifier both require `braid-program/f6c-continuous-reception-acceleration-launch.v1`, and the verifier independently fixes the declaration digest. Prescribed response fixes three original input identities inside its Python consumer as well as its JavaScript entry. These are executable requirements, not comments that can be refreshed with current helper hashes.

## Recovered source identities

After the known SHA-256 `abc` case passed, `git show 897fe1aa7^:<path>` recovered all four exact selected inputs. The configuration's historical path is `reference/priorities/braid-program/configurations/f5-phase-varying-campaign.v2.json`; the other three paths are unchanged. The [source identity record](../evidence/next-caller-inputs/source-identities.json) gives expected, recovered and current hashes and the preserved nonexecuting sources. The adjacent `.diff` files contain complete `git diff --no-index` comparisons against current files. This recovery establishes exact bytes, not scientific equivalence of the current revisions.

| Input | Selected original | Current boundary requiring review |
| --- | --- | --- |
| Approved prescribed configuration | `e92e450c8ea83086b60184d31ff5b07fe8a470b1e20088ea312592f2b38800fb` | The preserved/current diff changes `prescribed-assembly-spec.v2` to `.v3`, replaces `candidateId` with assembly identity/revision fields, changes identifiers and removes compatibility metadata. A filename ending in `.v3.json` in a historical source tuple does not make its selected old bytes schema v3. |
| Prescribed scientific fixture | `bda39fe695e8b446ac91aee96a9f867c7f48b8228f2c9f6ac547c8172e0da344` | The retained diff changes named source and candidate identities and descriptive text. The old receipt's identity must remain tied to the original bytes. |
| Prescribed-response declaration | `c08d7f53616fc2843b3a192f7e3c10229f9a9fe7abc1a8670ddb1706d95756ef` | `reduce-prescribed-acceleration-response.py` fixes this hash in `PREDECLARATION_SHA`, and its source census requires the original declaration. |
| One-cell acceleration declaration | `3ef8fb9020bae71833b1e06a119672b49a4beb5395f697dcb3d037d088e7891e` | `verify-f6c-continuous-reception-acceleration.py:validate_plan` requires the exact declaration path/hash, subject/control identities, schema, resource limits and original prior-cover closure. |

## Next implementation boundary

Retaining the existing capability means computing from its original accepted inputs under current supervision. It does not require replacing the current display configuration with its older schema. An explicit logical-to-physical historical-input selection can retain the original path/hash/byte tuple while reading the recovered regular file. Current executable helpers must still come from the current reviewed source selection; a historical input route must never become a route for executing archived supervisors.

Before implementation, specify which original data/declaration roles may use an archive, how duplicate/conflicting/unused routes are rejected, how physical captures remain bound through final cleanup, and how outputs distinguish original identity from physical provenance. Inventory the remaining original receipt closure as well; these four recovered files do not establish that every historical dependency is available. Review the acceleration verifier interface before changing its subject. Its mathematics and expected numerical results stay unchanged, and agreement after a shared interface change would validate transport implementation only.

The evidence supports proceeding with this explicit historical-input contract within the authorized supported-profile restoration. It does not support advancing all four source pins, declaring the v3 configuration equivalent, modifying both numerical subject and oracle together, or authorizing a scientific run. Existing acceleration, prescribed-response and full-run failures remain open until their complete current compositions and bounded controls pass. A recovered hash mismatch, an unrecorded contract consumer or a source-reading path outside the proposed selection would overturn the relevant finding.

## Prescribed-response implementation

The current plan, private candidate, publication job and final response use version-two schemas. Each original-input record carries `originalPath` for its established logical identity and `path` for the physical regular file being captured. The hash and byte count apply to that physical file. Only `approvedSource`, `scientificFixture` and `predeclaration` can use a different physical location, restricted to preserved `.source` files under the repository's `reference` directory. Executable, reference, test and interpreter roles cannot be redirected. Original role order, hashes and logical paths remain fixed; duplicate, conflicting, additional, escaping and executable routes are rejected.

The entry forwards the complete bound input list to the consumer. The consumer verifies it against its own source contract, keeps the physical descriptors open, and rechecks their bytes and identities through cleanup. The publisher captures those same physical files again and retains both identities in the output. Physical provenance is therefore inspectable in the plan and result without changing the original configuration or treating its current schema as equivalent. The mathematical reference and its tests remain byte-identical by `git diff --exit-code -- scripts/eom/oracle/prescribed_acceleration_response.py tests/test_prescribed_acceleration_response.py`; no response arithmetic or expected scientific result was changed.

The [current metadata plan](../evidence/prescribed-response-migration/prescribed-response-pilot-launch.v2.json) and [capture validation](../evidence/prescribed-response-migration/current-plan-validation.json) establish 181 complete declared bindings, 19 Python physical captures and four rejection controls without scientific evaluation. The shared-venv [Python test output](../evidence/prescribed-response-migration/6875a4bf-7429-4188-a8fe-a87a2de30a7b.stderr.log) records 74 passing tests in 1.806 seconds. The [three-suite Node output](../evidence/prescribed-response-migration/c646e84f-4456-4887-acf2-7210e3667926.stdout.log) records 49 passing tests, zero failures/skips/cancellations and 6133.345792 ms. The initial Python attempt found a new test fixture using the macOS temporary-directory alias; resolving that fixture path fixed the test without relaxing the production symlink rejection. Its failed output is retained. The initial Node invocation named a nonexistent launcher test and only ran 33 tests; the final invocation uses the actual `prescribed-response-pilot-launch.test.js` and supersedes that incomplete coverage.

The [preserved predecessor sources](../evidence/prescribed-response-migration/preserved-sources.json) retain original consumer, publisher, entry and control bytes. Current source selections advance only for the reviewed transport changes. This validation establishes source transport and synthetic publication behavior, not a newly accepted numerical response. An executable archive route, changed original identity, unnoticed physical replacement, wrong source census or accepted version-one current plan would falsify the corresponding claim.
