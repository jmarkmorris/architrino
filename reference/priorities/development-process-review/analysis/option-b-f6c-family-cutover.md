# Option B F6c operational family transfer

## Scope and decision

B-REM-1 is complete for the enumerated operational source-binding family. The final consolidated selection passes 327 controls with independently reviewed coverage and closed supervised processes. Four original packages remain planned; no scientific acceptance or Git publication follows from this completion.

The operator's September 12 instruction to continue B-REM-1 selects the original connected F6c operational family, not another isolated helper update. The [coordinator checkpoint](option-b-f6c-coordinator-cutover.md) supplies the independently reviewed current operational closure contract. This continuation transfers the remaining operational selections, follows their preparation and registered-entry callers, and preserves scientific reference and historical evidence obligations. The [independent family review](../evidence/option-b-f6c-family-review.md) owns acceptance of this candidate; the [paired implementation record](../evidence/option-b-f6c-paired-transfer.md) records the disjoint emission/refined assignment.

Option B moves current operational byte selections into externally selected source maps. It does not authorize a numerical run, change a scientific algorithm, update an independent scientific reference, or relabel an accepted historical output. Successful tests below measure source admission and synthetic process behavior, not the physical theory. Refined scientific transport, the parent scientific runtime-shape mismatch, and actual-data streamed acceptance remain with their existing recovery/scientific owners; this transfer does not claim those routes are restored.

## Original bindings and caller coverage

The pre-edit inventory used `shasum -a 256`, inert export inspection and `rg` over `scripts/`, `tests/`, `src/` and the relevant contract directory. The SHA-256 instrument passed the independent `abc` known vector before target hashing. The [family baseline](../contracts/option-b-f6c-family-baseline.json) records main-owned pre-edit source identities and binding tables. Exact prior bytes are retained at `.local-data/development-process-review/b-rem-1-family-baseline/<basename>.source`; those files were authenticated against the baseline before comparing their exports. The paired baseline has its own retained byte selectors. These are measured working-tree generations, not an assertion that every byte equals the checkout anchor `ab3ed93a59e69a5807397c24cefcfcb3a9642487`.

| Original operational owner | Current selection and covered callers | Retained obligation |
| --- | --- | --- |
| Common bounded coordinator | Existing six-source map plus the map itself; CLI digest, captured reader, preflight, original lifetime, file worker, registered stage and final closure | Independently frozen serial checker, exact resource/clock/process/lock conditions and false scientific authority |
| Emission entry and launcher | [Emission operational map](../contracts/option-b-f6c-emission-operational-sources.jsonld): entry, launcher, reader, two controls and two helpers; externally supplied map digest reaches stage and worker entry | All 35 remaining scientific/historical `PINS`, 16 `FIXED` rows, historical routes and Python strings |
| Refined entry and launcher | [Refined operational map](../contracts/option-b-f6c-refined-operational-sources.jsonld): the same seven-role structure with profile-specific paths | All 34 remaining scientific/historical `PINS`, 16 `FIXED` rows, seven refined inputs, two prior operations and Python strings; deferred scientific transport stays deferred |
| Parent composition | Three former helper literals become path-only `OPERATIONAL_PATHS`; pure plan validation receives the common map admission; `admitOperationalSources` covers registered execution and every hook phase | `NAMED`, `DEPENDENCIES`, `ORIGINAL`, retained source archives, mathematical census and Python bootstrap |
| Parent batch preparer and observer | Existing explicit coordinator/map selection and captured closure checker; preparation now forwards admission into parent plan validation | Original parent endpoint tokens, historical document routes, independently selected observer expectations and write-once plans |
| Packaging composition | Existing externally declared common coordinator/map admission is exposed as `admitOperationalSources` and called with the original deadline by both hook and registered paths | Five `PINS` and four `GENERIC_PINS` are fixed packaging/reference/independent-decoder obligations, not remaining current helper pins; no redundant map or reference refresh |
| Streamed composition | Five former operational literals become path-only `OPERATIONS`; v6 binds those five plus `sourceMap` and `manifestReader`, all selected by the existing common map | Scientific `PINS`, archive tuples, package/reference/closure pins, historical inputs and the complete embedded Python body |
| Streamed data preparer | Explicit streamed-module and map digests; captured module and map admission; current v6 metadata, optional explicit historical selection, bounded shared-venv runtime inventory and exclusive output publication | No inferred history, no numerical target, no independent admission or actual-data acceptance |

The basename census reached the scientific parent preparer/verifier, emission bridge, variable-cell adapter, independent package inventory and scientific controls. These remain scientific/historical consumers. Their retained selectors were not rewritten to current wrappers. B-REM-4 owns any further justified scientific-source role transfer. In particular, the emission Python bridge already selects the same prescribed and circular helper generations as its new operational graph and does not select the emission diagnostic launcher.

## Streamed capture and version boundary

The current invocation is `braid-program/f6c-streamed-leaf-invocation.v6`. Its existing closed `bindings` object gains `sourceMap` and `manifestReader`; all other top-level fields, scientific selections and resource limits remain. `admitOperationalSources` captures the spec-selected coordinator, admits the common map through the captured reader, and requires exact equality for all seven physical selections. The canonical coordinator instance still owns the private lifetime registry. Before registered execution, binding the admitted sources into that lifetime checks them against the map independently selected by the original CLI; the spec cannot replace that earlier selection.

The unchanged embedded Python capture loop already consumes every source binding, records original identities and rechecks them. Putting the map and reader in the spec therefore retains exact Node/Python full-source identity equality. Worker preflight and standalone registered entry each perform explicit admission. Final source checks retain original identities; these are not synthesized from a later hash. The registered argument vector and common coordinator bytes remain unchanged from the accepted checkpoint.

The reviewer rejected the initially considered v5 forwarding-only approach: out-of-spec map/reader sources would be absent from Python's identity inventory, and adding a registered argument would conflict with the coordinator's fixed argument predicate. That draft was replaced, not accepted. The final v6 design retains the existing capture mechanism. A v6 invocation is explicitly rejected as historical continuation until a separately reviewed historical acceptance contract exists; existing v4/v5 history predicates remain unchanged. The serial closure checker continues to reject streamed closure and does not certify scientific streamed results.

The preparer's prior v4 output was already incompatible with the prior v5 validator, as established by `git show HEAD` and live source inspection in the independent review. Current metadata preparation now supplies the explicit operational selection and existing history field required by v6. Its synthetic controls observe only one shared-venv runtime-inventory command and zero numerical target commands. This repairs metadata construction, not historical truth, scientific transport or permission to launch a real diagnostic.

## Preservation and independent boundary

Node equality checks against authenticated pre-edit exports confirm the parent, packaging and streamed scientific binding tables retained by the baseline. The new family admission suite also checks the following separately recorded embedded-body hashes:

| Body | Pre-edit and current SHA-256 |
| --- | --- |
| Parent Python bootstrap | `61d12312b4021b9a73e3d6d12e79d686ec2bbb8a7064a5807ad11c416eece95b` |
| Streamed Python body | `68f762ba2e1b3aae9ae885bd80372a402b623a8e14deec29069c959d89fc7145` |
| Packaging Python body | `e973e63d21594850ff7b6aefe46abe8717159f105e51a1a372ac7cdec50fa6cb` |

`shasum -a 256` confirms the common coordinator remains `f0a310b0902b66b60b98ab2f6b2109f352a1263ad23cc1bd5b53763c197bcd7c`, independent closure checker remains `9887632e2900efb1feecf0dfe24ae5f138e46ba7ab870c56eede66a522e84609`, and shared reader remains `9097ef3fd3fb2040b48c601d81c184188e8a992aa4464694860d05d0bf76a56e`. Their unchanged identities are stronger evidence than a test expectation refreshed alongside its subject. The common map selects the successor diagnostic helper `37fc8aab3e33229878ba1bd50182d7b6eb75b41cdbe171817ece7fcf7b0309e7`; its manifest digest is `bac429e22c115975ec905e9cbec2167ca416765ae2394b87a698ffc2e925b812`. Earlier receipts remain tied to their original map selections.

## Additional review conditions

The final review required stronger preparation identity continuity and consumer-specific worker routing, not a new scientific run. The preparer now retains its first streamed-module, map, reader and operational-source identities in private state, rejects conflicting captures, and rechecks that original state after runtime discovery and before and after CLI publication. It does not renew the identity baseline through final re-admission. Eleven preparation controls pass, including direct inventory, CLI inventory and publication-seam same-byte replacements for the map and streamed module. A replacement detected after exclusive output creation leaves an unadmitted metadata file and exits without a success report; it is not silently deleted or presented as accepted.

The actual parent and packaging hooks are exercised through the unchanged prescribed helper's real `Worker`, the captured common coordinator's initializer and `fileOperation`, and its `pureHook` restrictions. A known literal accepted hook establishes this route before target tests. Six target cases distinguish the selected-map path's unchanged scientific-configuration rejection from missing or wrong operational selection. An observing `Worker` subclass delegates construction to the real implementation and confirms its exit event and terminated thread identity before the helper promise settles. These seven controls pass under lease `56922118-6d6e-4083-b738-94402f4bdd56`, exit zero and closed process group. They establish actual consumer-specific worker routing and closure, not successful parent/package science. The separate serial and streamed suites supply full coordinator lifecycle evidence.

The historical version gate was factored into a named pure function without changing its predicate or its call position. A literal v5 case passes that gate and a literal v6 case rejects; the three existing retained-input controls remain unchanged. Passing the gate is not historical acceptance: all existing downstream historical predicates still apply.

An attempted whole-coordinator negative fixture is retained separately. Its noncanonical initial plan serialization and test-side lock selector were harness defects, not evidence against production admission. After correction, the parent reached the intended `closed fields` scientific boundary, recorded in `.local-data/braid-analysis/option-b-family-parent-EO0xVL/operation/rejection.json`. The frozen coordinator conservatively treats any rejected worker as unresolved closure and retains its guard/lock. The fixture's shorter external deadline then stopped that process; lease `fcfa37ca-4716-4197-bb27-d02c2470afcc` records closed process-group termination, not successful coordinator cleanup. The test-owned lock was recovered only after its recorded PID 34428, process group and start identity were matched to this task's closed lease and both PID/group were absent. Its original inode and bytes are retained as `option-b-family-parent-EO0xVL/retained-test-lock.json`; no foreign lock or process was touched. Later cases in that failed batch were blocked by that lock and are not independent hook results. The final routed-worker controls avoid invoking this unrelated whole-operation fatal path. No coordinator cleanup predicate was weakened to make a negative test pass.

## Validation and limitations

All counts below are measured by the named Node selections or supervisor leases. No full repository-health result, full Content Integrity run, scientific production run, generator write or Git publication is claimed.

| Instrument and bounded selection | Result |
| --- | --- |
| Final consolidated twenty-file selection | 327 pass, zero failures/skips/cancellations; 248,530.41 ms. Lease `beb8d12e-dd29-4b8d-bc13-e6277197d48a`: completed, 248.595 seconds, exit zero and closed process group |
| Earlier fifteen-file short selection below | 216 pass, zero failures/skips/cancellations; 13,966.614875 ms; final preparation and historical controls are covered by the consolidated successor |
| Original complete streamed suite | 69 pass; lease `3f28cf51-5cf8-41cd-becc-a86858094dc0`, 211.956 seconds, exit zero and closed process group |
| Added streamed map controls and positive capture controls | Six new rejection controls plus four positive Python/source-identity controls pass; lease `cdcdba93-feef-4ab0-b89b-9460c64d5846`, 11.272 seconds, exit zero and closed group |
| Final paired host-process and serial closure selection | 26 pass; lease `c0f31571-c24a-4dc4-9c88-5ca8a82b5b72`, 22.537 seconds, exit zero and closed group |
| Frozen independent checker on the final serial candidate | Accepted operational v2 closure at `.local-data/braid-analysis/option-b-current-closure-9xgnHc/observer/closure.json`; no mathematical/H3/physical acceptance. Earlier `xcgOW2` retains its own provenance |
| Five-profile transfer checker | `node scripts/equation-mapping/check-current-source-maps.mjs` passes all five original profiles; the new family is not miscounted as additional original profiles |
| Launch policy | `node scripts/check-owned-compute-launch-policy.mjs` passes its declared source inventory |
| Strict content validation | `node scripts/validate-content.mjs --check --strict` reports zero errors and warnings |

The short selection was:

```bash
node --test --test-concurrency=1 tests/current-source-manifest.test.mjs tests/option-b-root-cover-admission.test.mjs tests/option-b-f6c-coordinator-admission.test.mjs tests/f6c-bounded-operation-current-closure.test.js tests/f6c-bounded-operation.test.js tests/f6c-parent-emission-refinement-pilot.test.js tests/f6c-parent-refinement-batch-preparation.test.js tests/f6c-evidence-packaging.test.js tests/f6c-emission-refinement-pilot.test.js tests/f6c-refined-acceleration-pilot.test.js tests/option-b-f6c-paired-admission.test.mjs tests/option-b-f6c-family-admission.test.mjs tests/option-b-f6c-streamed-preparation.test.mjs tests/f6c-streamed-historical-inputs.test.js tests/current-launch-bindings.test.js
```

The final consolidated command is the short selection above with `tests/option-b-f6c-family-process.test.mjs`, `tests/f6c-emission-refinement-pilot-process.test.js`, `tests/f6c-refined-acceleration-pilot-process.test.js`, `tests/option-b-f6c-coordinator-process.test.mjs` and `tests/f6c-streamed-leaf-diagnostic.test.js` appended, run serially under `node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 420 --heartbeat-seconds 15 --`. This includes the final factored historical predicate and final preparation identity controls on the same source candidate. It comprises 219 short controls, seven consumer-worker controls, 26 paired/serial process controls and 75 streamed controls.

The new paired and family Node-only admission suites are registered in the existing required Option B Content Integrity step. That exact six-file required selection separately passes 74 tests. Host-process and shared-macOS-venv preparation controls remain explicit host selections rather than being silently added to a Linux-only test environment. `node scripts/dev/owned-compute-supervisor.mjs closeout --owner-task "$CODEX_SESSION_ID"` reports `clear` after final verification. `git diff HEAD --check` passes over combined staged and unstaged edits; this task did not alter the index.

Two unsuccessful intermediate checks retain honest dispositions. A mixed short-test name filter unintentionally selected six supervised process cases inside the sandbox; each failed with `before target spawn: spawn EPERM`, and the corresponding fixtures were retained. Re-execution with host observation passed. The first paired process run, lease `bafd6885-416b-4528-ad71-5e9ac45bb65c`, had 24 passes and two failures: serialized active-EPIPE child fixtures lacked their new `Admission` import. Adding that import in the two test harnesses and selecting the revised process-control bytes in their maps produced the passing 26-test rerun; production targets and EPIPE assertions were not changed to force a pass.

Claim grade: measured for the scoped identities, export/body preservation, admission controls and supervised synthetic process results; inferred for operational-family completeness under the independently reviewed bounded criterion. Falsifiers include an uncovered supported caller, acceptance under a missing or conflicting externally selected map, loss of an original captured identity, a changed retained scientific reference, an accepted v6 historical predecessor without its separate contract, or failure of the declared final selections. Overall migration, scientific recovery and publication remain separate obligations.
