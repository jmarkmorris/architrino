# Option B repository reconciliation

## Selected boundary

The operator selected B-REM-2 through B-REM-5 together. This record owns integration of their current-source admission and inventory coverage. A current-source manifest selects executable and supporting source bytes; it does not replace historical evidence, mathematical reference applicability, package authenticity, or an exact-state publication receipt. The [finite plan](option-b-remaining-migration-plan.md) supplies the discovery set and completion requirements. The independent per-file dispositions retain their separate review ownership.

Status: ◐ Partial — all selected migration implementation and source-role review are complete; final validation is not. B-REM-4 is complete. B-REM-2 needs the final handoff build, B-REM-3 needs two shared-caller suites after the resource gate is met, and B-REM-5 needs those results and the required scene-graph gate. The 563-test selected pass below does not close those outstanding conditions. No new scientific campaign or mathematical-map acceptance is selected.

## Pre-integration baseline

`git rev-parse HEAD` reports `ab3ed93a59e69a5807397c24cefcfcb3a9642487`. The eight existing operational manifests are retained as exact pre-integration `.source` copies under `.local-data/development-process-review/b-rem-2-5-baseline/`; these are local evidence, not new launch defaults. Their graph revisions must advance for changed source or relationship records. The retained A descriptors remain unchanged.

`node scripts/check-content-integrity.mjs`, run under the owned supervisor with a 180-second bound, reports **28 required passed, one required failed, one optional reporting sweep skipped and no unreached checks**. Lease `ec7c2de0-d54d-497d-af30-7eff1f7ad8df` records exit one, a closed process group and 33.941 seconds. Its declared stdout/stderr files retain the full results. The failed scene-graph check reports only `content/graph/textbook_toc.json` and `content/generated/markdown/textbook/toc.md` out of date. This is the baseline before the remaining production migrations, not a defect attributed to their changes. Regeneration belongs to the separately authorized `node scripts/build-scene-graph.mjs --write` procedure; no generated write was performed here. Content Integrity's permitted first step reconstructs ignored runtime assets, which is distinct from tracked source-derived regeneration.

## Shared operational successor interface

Before the circular supervisor changes, `tests/option-b-operational-successor.test.mjs` passes its hand-authored `abc` positive case and source/role/scope/reader/coverage rejection controls (two tests). `check-current-source-maps.mjs` still passes all five original profile transfers, and `shasum -a 256 scripts/eom/launch-subfield-circular-root-pilot.mjs` still reports `71974054ddce7fc29b8464b9a7a63f8fbb04ee5b425dc997df4d40b2804341aa`. Independent reviewer `01a096b8-6794-78d1-84c8-01c761a89219` reran these controls and accepted the bounded interface before releasing the supervisor edit. The checker and its dedicated controls are frozen at `a1624fa9d4a9470748ecca32772de1f990f72b915138c4e8ff5867d8b2f1ed60` and `7812357720ca848e7584bb24f70e65df1ee956850cf9866423501c359216d62f`. Actual dependency completeness still belongs to subject admission and its separate implementation review.

The transfer checker permits a changed identity only for that exact operational helper, only after matching its retained original value, and only by reading the separate circular map's unique launcher selection under `subfield-circular-current-operation`. It validates the map, admission/reader identities, every selected physical source and the old profile's unchanged scientific bindings. An unchanged original helper remains valid; a changed source cannot fall back to the original selection if the circular map is absent. This is consistency evidence for a selected candidate, not launch authorization or scientific acceptance.

The two synthetic prescribed-response/acceleration process fixtures derive their helper selection from the bytes actually captured by those fixtures. They remain explicitly synthetic transport controls, not historical evidence or independent mathematical references. The cached-pilot historical construction table retains its original helper digest unchanged. Before the shared helper changes, `node --test --test-concurrency=1 tests/prescribed-response-pilot-process.test.js tests/f6c-acceleration-pilot-process.test.js` passes 19 tests with no failures/skips/cancellations under lease `9fdd438c-1bea-4dfa-a491-5a69e5cb469f` (16.407 seconds, exit zero, closed group). This is baseline fixture validation, not the final dependency rerun.

## Emission transport handoff

The Python bridge `execute-f6c-emission-refinement.py` validates a current plan and captures its operational sources, but also embeds current prescribed-helper and circular-supervisor digests. Those two checks cannot remain fixed while the selected helpers migrate. Its numerical producer, comparison implementation, original-data routes and independent scientific validator are different obligations and remain unchanged. The existing shared-venv eleven-test transport selection passes before edits, under supervised lease `e0f1f453-d1a9-42fa-9db5-bd416f5aaa86` (1.25 seconds, exit zero, closed group); its publication test replaces numerical callbacks with explicit synthetic functions.

The current-source handoff gives the Python stage mandatory external helper/map/reader selections from the already admitted Node graph, verifies them against the independently selected plan's operational bindings, and retains those captures through stage cleanup. A direct Python invocation must supply the same selections; it must not derive acceptance from whatever files happen to be on disk. The graph reader remains the captured Node implementation rather than a new Python JSON-LD implementation. The bridge is a subordinate byte-capture consumer, not an independent graph-acceptance instrument. The plan is still externally hash-selected, while unchanged scientific validators govern its scientific projection. A direct Python invocation provides externally selected exact-byte transport, not proof that Node graph admission ran. The independent reviewer accepted this bounded contract before implementation, requiring all four selections without fallback, exact role/path matching, original capture rechecks and separate implementation-negative controls.

## Integrated controls and current limits

The shared-venv emission transport suite now passes 13 tests under lease `f00fb8c7-b943-432c-8fa7-d29c2777d7d2` (1.454 seconds, exit zero, closed group). `node --test tests/f6c-emission-refinement-pilot.test.js` passes 30 controls, including poisoned plan helper expectations: the four Python selections still come from admitted Node state. The bridge's unchanged Python Capture owns original descriptor/path identity checks through completion. Node paired captures validate identity within each capture and selected bytes between stages; they do not claim Python's stronger retained-descriptor lifetime semantics.

After the known three-test source/impact controls pass, `compareSourceManifests` compares all eight pre-integration maps with current authored successors. Each selects its corresponding admission owner for review; no old graph record is removed, and the emission map adds its bridge, Python controls and four incident relationships. Full output is retained in `.local-data/development-process-review/b-rem-2-5-baseline/current-source-impact.json`. Graph revisions advance for changed sources and incident relationships. This is declared-dependency impact, not acceptance by itself.

The first combined 22-file Node run under lease `a129cbd1-1366-4607-855e-95d1f2243cb1` reports 332 tests, 276 passed and 56 failed, with no skips/cancellations, exit one and a closed group in 45.404 seconds. Ten transfer controls encountered the circular worker's temporarily stale canonical map. Streamed controls report the unchanged host memory/disk guard before their target operations; the serial coordinator's retained `operation/rejection.json` independently confirms the same first failure. Its rejection fields are not interpreted as proof a lock remains: a later `ls -l` at the exact shared `.pilot.lock` path reports it absent. A subsequent `/usr/bin/memory_pressure -Q` reports 38% free against the helper's 40% launch minimum, while `df -k .` reports 910217436 KiB available. These observations do not establish that every failure has the same cause. No resource threshold is weakened, and no final combined pass is claimed.

The independently reviewed disposition instrument checks the exact ordered original 157 candidates separately from additional owners, the declared schema and count, final dispositions and six explicit role fields. Its three known controls pass before target use. The live target remains unresolved while F5/circular dispositions await implementation review. `node --test tests/content-integrity-reporting.test.js` passes seven reporting controls after required-gate integration. New admission and inventory controls are required checks; they do not change the mathematical trial's report-only status.

Further isolated checks pass: 180 F6c admission/unit/reporting controls; 42 current-profile/circular-admission controls after the circular map is consistent; and 37 original five-profile synthetic process controls under lease `b38d1768-08eb-4a4c-bef4-b26d77e46799` (36.844 seconds, exit zero, closed group). The five-profile transfer checker passes with the exact reviewed successor interface unchanged. The unchanged emission scientific-control selection `test_f6c_emission_refinement*.py` passes 80 tests under lease `eee7fa70-34c0-4d04-b27e-f99d4b94d6e6` (14.026 seconds, exit zero, closed group). These selections overlap other runs and are not summed into an invented unique-test total.

The second stable-map combined run, lease `0207b353-f580-4e86-a754-2f0ed5740a4f`, reports 332 tests, 286 passed, 46 failed, zero skipped/cancelled and a closed group in 44.883 seconds. The ten stale-map failures are resolved. The log reports the resource guard for 45 streamed tests; the serial coordinator's separate rejection record at `.local-data/braid-analysis/option-b-current-closure-Ov0LAp/operation/rejection.json` records the same first failure. Memory had briefly reached 40% but measured 38% immediately before this retry. This is not a clean combined result and is not grounds for relaxing the launch guard.

Independent F5 review identified an unclosed newly introduced runtime boundary: Python's admission subprocess selected Node by canonical path without externally selected executable bytes/original identity, and its returned source array lacked an independent exact census check. The F5 implementation owner is correcting those operational obligations; no numerical reference repair is selected. Final role acceptance remains pending until the corrected runtime and source-census controls are reviewed.

The final pre-review circular 13-file selection passes 132 controls together under lease `3d4f1685-29da-4043-81c9-aaf714c7d975` (41.843 seconds, exit zero, closed group). Independent review then identified a narrower untested interleaving in the legacy pilot's identity-bearing `bind`: it compared the open descriptor but did not compare the pathname's current inode after reading. The circular owner is adding that check and a directory-replacement control; the 132-test result remains pre-fix evidence, not final acceptance of the successor.

A candidate capture records 122 map/source/required-gate paths across eleven operational manifests in `.local-data/development-process-review/b-rem-2-5-baseline/final-candidate-source-sha256.txt`. It is an intermediate validation snapshot and will be compared/refreshed after the independent-review correction. It does not capture the entire ambient working tree or confer publication authority.

Independent review accepts the corrected circular stage binding at pilot `7ea12ef02b4a1d5ba2da1a353b0e7720e30cfd5d3f59bfb2db0177ad11fd737f` and map `d63e258c9fa5f5bfd520f3127c376b111b448f397cdc7c68b8b3c598e18644e4`. The new literal-good directory/path replacement control reproduced the original missing rejection before the fix and passes afterward. Main also reran all ten circular admission controls successfully. OUTER remains unchanged.

The F5 owner reports final-generation enclosed-root build lease `85206a8f-4dc2-470a-8ec2-7c47308985a3`, with all 42 stages passed and terminal group closure. The [F5 record](option-b-f5-cutover.md) supplies its exact map/helper selection and build receipt; this is build/runtime evidence, not scientific acceptance. The handoff build was not started after its next host measurement fell to 36%, below the unchanged 40% launch gate.

The final executable Node selection comprises 51 explicit files, including all F5 process controls enabled through `F5_RUN_PROCESS_CONTROLS=1`, the corrected circular family and affected original/F6c profiles. It deliberately excludes `tests/f6c-streamed-leaf-diagnostic.test.js` and `tests/option-b-f6c-coordinator-process.test.mjs`, whose complete prior runs remain resource-blocked. A green result for this selection cannot close those two obligations. The final candidate capture expands to 170 map/source/required-gate/test paths and is compared again after validation.

## Final validation and remaining conditions

The consolidated command retained in lease `3c38de5a-0fc7-4994-bf2b-68056b2e3875` runs the 51 files listed in that lease with `F5_RUN_PROCESS_CONTROLS=1` and serial file scheduling. It reports **563 passed, zero failed, skipped or cancelled**, test duration 140.102 seconds, supervisor duration 140.18 seconds, exit zero and a closed group. The independent reviewer inspected the command, stdout, empty stderr and lease closure. This is the post-correction circular/F5/shared-profile selection, not the two excluded F6c families. The 170-path SHA-256 snapshot is unchanged before and after this run by direct recomputation/comparison.

The final `node scripts/equation-mapping/check-current-source-dispositions.mjs` reports **157 original candidates, 155 current literal candidates and 196 disposition rows**. The independent authored review supplies **128 retained and 29 migrated original candidates, with zero pending**, plus 39 additional owners. The current literal count differs from the original discovery count because migrations remove some literals and add controls; both sets remain covered. Role classifications are reviewed judgments, while the CLI verifies structural coverage.

Required `node scripts/check-content-integrity.mjs` under lease `77d83fcf-35e3-4db7-8d9c-6622f9ca23d5` reports **29 required passed, one required failed, one optional reporting sweep skipped and no unreached checks**, exit one, closed group, 37.74 seconds. All required operational admission, transfer and disposition checks pass. The only failure is the scene-graph check's two stale outputs, `content/graph/textbook_toc.json` and `content/generated/markdown/textbook/toc.md`, also observed in the pre-implementation baseline. No numerical defect or causal author is inferred from that drift. The exact repair command is `node scripts/build-scene-graph.mjs --write`, followed by its strict check and the required gate; tracked regeneration has not been authorized or performed in this task.

| Remaining owner | Exact completion condition |
| --- | --- |
| B-REM-2 — ◐ Partial | Run only the remaining handoff build command in the [F5 resumption owner](option-b-f5-cutover.md#exact-build-only-resumption), after measuring at least 40% host free memory and satisfying all other unchanged resource limits. The final enclosed-root build already passed and must not be repeated merely for a new receipt. |
| B-REM-3 — ◐ Partial | Rerun `tests/f6c-streamed-leaf-diagnostic.test.js` and `tests/option-b-f6c-coordinator-process.test.mjs` with owned supervision and sufficient host resources. Their retained failures are launch-guard failures, not passes or scientific negatives. |
| B-REM-5 — ◐ Partial | Reconcile those results, obtain explicit scene-graph regeneration authority, rerun the strict scene-graph check and required Content Integrity, and verify the selected source snapshot remains unchanged. No broad scientific campaign or mathematical-map acceptance follows. |

The last direct `/usr/bin/memory_pressure -Q` after Content Integrity reports 38%, below the existing 40% launch gate. No further build or resource-bound test retry is made at that observation. The optional reporting sweep, deferred numerical campaigns, general historical reacceptance, full-ladder execution and mathematical-map acceptance were not run or claimed complete.

Owner-aware `owned-compute-supervisor.mjs closeout --owner-task "$CODEX_SESSION_ID"` reports `clear` after validation. The 170-path snapshot remains unchanged after required Content Integrity. Scoped `git diff --check` over migration code, controls and evidence reports no whitespace errors. These are scoped completion checks, not permission to publish or acceptance of the outstanding build/test/generator conditions.

## Nonliteral owner dispositions

These selections were traced by reading their actual consumers, not inferred from absence in the quoted-SHA census. Each retained binding continues to serve an obligation outside current repository source admission.

| Owner and consumer | Binding meaning | Disposition and coverage |
| --- | --- | --- |
| `package.json` and `package-lock.json`; `npm ci` in `.github/workflows/option-b-trial.yml` and dependency audit in `.github/workflows/content-integrity.yml` | Exact external dependency versions and package-distribution integrity, including JSON-LD, N3 and Comunica | Retained under npm dependency installation/audit. These hashes do not choose repository-owned source for a scientific caller. No package upgrade or audit exemption is selected. |
| `.github/workflows/` and `.github/actions/build-pages/action.yml`; GitHub Actions runner | External Action commit identities, workflow permissions, bounded jobs and deployment routing | Retained under workflow/release owners. The mathematical two-chain trial remains report-only; required operational B admission stays inside Content Integrity. No remote setting or live deployment is changed or claimed verified. |
| `scripts/config/foundational-impact-contracts.json`; `scripts/check-foundational-impact.mjs` | Path-triggered command/consumer contracts and scientific verification selection | Retained as command-routing authority. It contains no implied scientific baseline replacement. Migration checks belong to their required Content Integrity step, not a new broad numerical selection. |
| `scripts/config/generated-runtime-assets.json`; `scripts/prepare-runtime-assets.mjs` | Four ignored, reproducible runtime families and their canonical generators | Retained under runtime-asset/retention ownership. Runtime data is not an authored source-admission map. The configured generators may reconstruct ignored payloads; tracked generated artifacts retain their separate write authority. |
| `scripts/pr-validation-receipt.mjs`; `.githooks/pre-commit` and `.githooks/pre-push` | Exact staged-index, unstaged-overlay, untracked-content, base-ref, toolchain and validator-contract fingerprint | Retained under publication. Changing current manifests necessarily changes the candidate fingerprint; an old receipt cannot authorize new bytes. The mathematical trial retains its `reportOnly` flag. No publication procedure is invoked. |
| Authored `option-b-*-baseline.json` descriptors and historical `.source` archives; retained-A transfer checker | Original source selection and transfer provenance | Retained byte-for-byte. Only a specifically reviewed operational successor may replace an inherited current helper; scientific and historical bindings cannot be refreshed by that mechanism. |
| Mathematical dependency contracts; `check-moving-single-root-map.mjs` and `dependency-map-reader.mjs` | Mathematical statements, selectors, evidence and independent baseline acceptance | Retained under the report-only two-chain trial and its separate operator acceptance decision. Operational migration is not mathematical acceptance. |

Claim grade: measured for the named reads, retained source copies and baseline command; inferred for each role classification. A consumer that executes repository source using one of these retained values, a changed source admitted under an old selected manifest, or a missing candidate in the final disposition census would overturn the corresponding classification. The baseline is not a final migration result or overall repository-health claim.
