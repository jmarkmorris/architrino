# Option B repository reconciliation

## Selected boundary

The operator selected B-REM-2 through B-REM-5 together. This record owns integration of their current-source admission and inventory coverage. A current-source manifest selects executable and supporting source bytes; it does not replace historical evidence, mathematical reference applicability, package authenticity, or an exact-state publication receipt. The [finite plan](option-b-remaining-migration-plan.md) supplies the discovery set and completion requirements. The independent per-file dispositions retain their separate review ownership.

Status: ✓ Done — all five finite migration packages, including final required-gate validation. The [authorized registry refresh](#authorized-equation-registry-refresh-and-final-gate) closes the last generated-output condition with all 30 required Content Integrity checks passing. The [authorized resumption](#authorized-validation-resumption--september-12) and [independent operational review](../evidence/option-b-remaining-binding-dispositions.md#b-rem-5-independent-final-validation--authorized-resumption) retain the completed F5 builds, prior 563-test selection and complete 77-test residual selection on the unchanged migration generation. No universal test-health, scientific campaign or mathematical-map acceptance follows.

## Pre-integration baseline

`git rev-parse HEAD` reports `ab3ed93a59e69a5807397c24cefcfcb3a9642487`. The eight existing operational manifests are retained as exact pre-integration `.source` copies under `.local-data/development-process-review/b-rem-2-5-baseline/`; these are local evidence, not new launch defaults. Their graph revisions must advance for changed source or relationship records. The retained A descriptors remain unchanged.

`node scripts/check-content-integrity.mjs`, run under the owned supervisor with a 180-second bound, reports **28 required passed, one required failed, one optional reporting sweep skipped and no unreached checks**. Lease `ec7c2de0-d54d-497d-af30-7eff1f7ad8df` records exit one, a closed process group and 33.941 seconds. Its declared stdout/stderr files retain the full results. The failed scene-graph check reports only `content/graph/textbook_toc.json` and `content/generated/markdown/textbook/toc.md` out of date. This is the baseline before the remaining production migrations, not a defect attributed to their changes. Regeneration belongs to the separately authorized `node scripts/build-scene-graph.mjs --write` procedure; no generated write was performed here. Content Integrity's permitted first step reconstructs ignored runtime assets, which is distinct from tracked source-derived regeneration.

## Shared operational successor interface

Before the circular supervisor changes, `tests/option-b-operational-successor.test.mjs` passes its hand-authored `abc` positive case and source/role/scope/reader/coverage rejection controls (two tests). `check-current-source-maps.mjs` still passes all five original profile transfers, and `shasum -a 256 scripts/eom/launch-subfield-circular-root-pilot.mjs` still reports `71974054ddce7fc29b8464b9a7a63f8fbb04ee5b425dc997df4d40b2804341aa`. Independent reviewer `01a096b8-6794-78d1-84c8-01c761a89219` reran these controls and accepted the bounded interface before releasing the supervisor edit. The checker and its dedicated controls are frozen at `a1624fa9d4a9470748ecca32772de1f990f72b915138c4e8ff5867d8b2f1ed60` and `7812357720ca848e7584bb24f70e65df1ee956850cf9866423501c359216d62f`. Actual dependency completeness still belongs to subject admission and its separate implementation review.

The transfer checker permits a changed identity only for that exact operational helper, only after matching its retained original value, and only by reading the separate circular map's unique launcher selection under `subfield-circular-current-operation`. It validates the map, admission/reader identities, every selected physical source and the old profile's unchanged scientific bindings. An unchanged original helper remains valid; a changed source cannot fall back to the original selection if the circular map is absent. This is consistency evidence for a selected candidate, not launch authorization or scientific acceptance.

The two synthetic prescribed-response/acceleration process fixtures derive their helper selection from the bytes actually captured by those fixtures. They remain explicitly synthetic transport controls, not historical evidence or independent mathematical references. The cached-pilot historical construction table retains its original helper digest unchanged. Before the shared helper changes, `node --test --test-concurrency=1 tests/prescribed-response-pilot-process.test.js tests/f6c-acceleration-pilot-process.test.js` passes 19 tests with no failures/skips/cancellations under lease `9fdd438c-1bea-4dfa-a491-5a69e5cb469f` (16.407 seconds, exit zero, closed group). This is baseline fixture validation, not the final dependency rerun.

## Emission transport handoff

The Python bridge `execute-f6c-emission-refinement.py` validates a current plan and captures its operational sources, but also embeds current prescribed-helper and circular-supervisor digests. Those two checks cannot remain fixed while the selected helpers migrate. Its numerical producer, comparison implementation, original-data routes and independent scientific validator are different obligations and remain unchanged. The existing shared-venv eleven-test transport selection passes before edits, under supervised lease `e0f1f453-d1a9-42fa-9db5-bd416f5aaa86` (1.25 seconds, exit zero, closed group); its publication test replaces numerical callbacks with explicit synthetic functions.

The current-source handoff gives the Python stage mandatory external helper/map/reader selections from the already admitted Node graph, verifies them against the independently selected plan's operational bindings, and retains those captures through stage cleanup. A direct Python invocation must supply the same selections; it must not derive acceptance from whatever files happen to be on disk. The graph reader remains the captured Node implementation rather than a new Python JSON-LD implementation. The bridge is a subordinate byte-capture consumer, not an independent graph-acceptance instrument. The plan is still externally hash-selected, while unchanged scientific validators govern its scientific projection. A direct Python invocation provides externally selected exact-byte transport, not proof that Node graph admission ran. The independent reviewer accepted this bounded contract before implementation, requiring all four selections without fallback, exact role/path matching, original capture rechecks and separate implementation-negative controls.

## Integrated controls and current limits

The shared-venv emission transport suite now passes 13 tests under lease `f00fb8c7-b943-432c-8fa7-d29c2777d7d2` (1.454 seconds, exit zero, closed group). `node --test tests/f6c-emission-refinement-pilot.test.js` passes 30 controls, including poisoned plan helper expectations: the four Python selections still come from admitted Node state. The bridge's unchanged Python Capture owns original descriptor/path identity checks through completion. Node paired captures validate identity within each capture and selected bytes between stages; they do not claim Python's stronger retained-descriptor lifetime semantics.

After the known three-test source/impact controls pass, `compareSourceManifests` compares all eight pre-integration maps with current authored successors. Each selects its corresponding admission owner for review; no old graph record is removed, and the emission map adds its bridge, Python controls and four incident relationships. Full output is retained in `.local-data/development-process-review/b-rem-2-5-baseline/current-source-impact.json`. Graph revisions advance for changed sources and incident relationships. This is declared-dependency impact, not acceptance by itself.

The first combined 22-file Node run under lease `a129cbd1-1366-4607-855e-95d1f2243cb1` reports 332 tests, 276 passed and 56 failed, with no skips/cancellations, exit one and a closed group in 45.404 seconds. Ten transfer controls encountered the circular worker's temporarily stale canonical map. Streamed controls report the unchanged host memory/disk guard before their target operations; the serial coordinator's retained `operation/rejection.json` independently confirms the same first failure. Its rejection fields are not interpreted as proof a lock remains: a later `ls -l` at the exact shared `.pilot.lock` path reports it absent. A subsequent `/usr/bin/memory_pressure -Q` reports 38% free against the helper's 40% launch minimum, while `df -k .` reports 910217436 KiB available. These observations do not establish that every failure has the same cause. No resource threshold is weakened, and no final combined pass is claimed.

The independently reviewed disposition instrument checks the exact ordered original 157 candidates separately from additional owners, the declared schema and count, final dispositions and six explicit role fields. Its three known controls pass before target use. The live target remains unresolved while F5/circular dispositions await implementation review. `node --test tests/content-integrity-reporting.test.js` passes seven reporting controls after required-gate integration. New admission and inventory controls are required checks; they do not change the mathematical trial's report-only status.

Further isolated checks pass: 180 F6c admission/unit/reporting controls; 42 current-profile/circular-admission controls after the circular map is consistent; and 37 original five-profile synthetic process controls under lease `b38d1768-08eb-4a4c-bef4-b26d77e46799` (36.844 seconds, exit zero, closed group). The five-profile transfer checker passes with the exact reviewed successor interface unchanged. The unchanged emission scientific-control selection `test_f6c_emission_refinement*.py` passes 80 tests under lease `eee7fa70-34c0-4d04-b27e-f99d4b94d6e6` (14.026 seconds, exit zero, closed group). These selections overlap other runs and are not summed into an invented unique-test total.

The second stable-map combined run, lease `0207b353-f580-4e86-a754-2f0ed5740a4f`, reports 332 tests, 286 passed, 46 failed, zero skipped/cancelled and a closed group in 44.883 seconds. The ten stale-map failures are resolved. The log reports the resource guard for 45 streamed tests; the serial coordinator's separate rejection record at `.local-data/braid-analysis/option-b-current-closure-Ov0LAp/operation/rejection.json` records the same first failure. Memory had briefly reached 40% but measured 38% immediately before this retry. This is not a clean combined result and is not grounds for relaxing the launch guard.

Independent F5 review identified an unclosed newly introduced runtime boundary: Python's admission subprocess selected Node by canonical path without externally selected executable bytes/original identity, and its returned source array lacked an independent exact census check. The F5 implementation owner is correcting those operational obligations; no numerical reference repair is selected. Final role acceptance remains pending until the corrected runtime and source-census controls are reviewed.

The final pre-review circular 13-file selection passes 132 controls together under lease `3d4f1685-29da-4043-81c9-aaf714c7d975` (41.843 seconds, exit zero, closed group). Independent review then identified a narrower untested interleaving in the legacy pilot's identity-bearing `bind`: it compared the open descriptor but did not compare the pathname's current inode after reading. The circular owner is adding that check and a directory-replacement control; the 132-test result remains pre-fix evidence, not final acceptance of the successor.

A candidate capture records 122 map/source/required-gate paths across eleven operational manifests in `.local-data/development-process-review/b-rem-2-5-baseline/final-candidate-source-sha256.txt`. It is an intermediate validation snapshot and will be compared/refreshed after the independent-review correction. It does not capture the entire ambient working tree or confer publication authority.

Independent review accepts the corrected circular stage binding at pilot `7ea12ef02b4a1d5ba2da1a353b0e7720e30cfd5d3f59bfb2db0177ad11fd737f` and map `d63e258c9fa5f5bfd520f3127c376b111b448f397cdc7c68b8b3c598e18644e4`. The new literal-good directory/path replacement control reproduced the original missing rejection before the fix and passes afterward. Main also reran all ten circular admission controls successfully. OUTER remains unchanged.

The F5 owner reports final-generation enclosed-root build lease `85206a8f-4dc2-470a-8ec2-7c47308985a3`, with all 42 stages passed and terminal group closure. The [F5 record](option-b-f5-cutover.md) supplies its exact map/helper selection and build receipt; this is build/runtime evidence, not scientific acceptance. The handoff build was not started after its next host measurement fell to 36%, below the unchanged 40% launch gate.

The final executable Node selection comprises 51 explicit files, including all F5 process controls enabled through `F5_RUN_PROCESS_CONTROLS=1`, the corrected circular family and affected original/F6c profiles. It deliberately excludes `tests/f6c-streamed-leaf-diagnostic.test.js` and `tests/option-b-f6c-coordinator-process.test.mjs`, whose complete prior runs remain resource-blocked. A green result for this selection cannot close those two obligations. The final candidate capture expands to 170 map/source/required-gate/test paths and is compared again after validation.

## Pre-resumption validation and remaining conditions

The consolidated command retained in lease `3c38de5a-0fc7-4994-bf2b-68056b2e3875` runs the 51 files listed in that lease with `F5_RUN_PROCESS_CONTROLS=1` and serial file scheduling. It reports **563 passed, zero failed, skipped or cancelled**, test duration 140.102 seconds, supervisor duration 140.18 seconds, exit zero and a closed group. The independent reviewer inspected the command, stdout, empty stderr and lease closure. This is the post-correction circular/F5/shared-profile selection, not the two excluded F6c families. The 170-path SHA-256 snapshot is unchanged before and after this run by direct recomputation/comparison.

The final `node scripts/equation-mapping/check-current-source-dispositions.mjs` reports **157 original candidates, 155 current literal candidates and 196 disposition rows**. The independent authored review supplies **128 retained and 29 migrated original candidates, with zero pending**, plus 39 additional owners. The current literal count differs from the original discovery count because migrations remove some literals and add controls; both sets remain covered. Role classifications are reviewed judgments, while the CLI verifies structural coverage.

Required `node scripts/check-content-integrity.mjs` under lease `77d83fcf-35e3-4db7-8d9c-6622f9ca23d5` reports **29 required passed, one required failed, one optional reporting sweep skipped and no unreached checks**, exit one, closed group, 37.74 seconds. All required operational admission, transfer and disposition checks pass. The only failure is the scene-graph check's two stale outputs, `content/graph/textbook_toc.json` and `content/generated/markdown/textbook/toc.md`, also observed in the pre-implementation baseline. No numerical defect or causal author is inferred from that drift. The exact repair command is `node scripts/build-scene-graph.mjs --write`, followed by its strict check and the required gate; tracked regeneration has not been authorized or performed in this task.

The following table preserves the conditions at the pre-resumption checkpoint. All three are subsequently discharged in the authorized resumption below.

| Remaining owner at that checkpoint | Exact completion condition |
| --- | --- |
| B-REM-2 — ◐ Partial | Run only the remaining handoff build command in the [F5 resumption owner](option-b-f5-cutover.md#exact-build-only-resumption), after measuring at least 40% host free memory and satisfying all other unchanged resource limits. The final enclosed-root build already passed and must not be repeated merely for a new receipt. |
| B-REM-3 — ◐ Partial | Rerun `tests/f6c-streamed-leaf-diagnostic.test.js` and `tests/option-b-f6c-coordinator-process.test.mjs` with owned supervision and sufficient host resources. Their retained failures are launch-guard failures, not passes or scientific negatives. |
| B-REM-5 — ◐ Partial | Reconcile those results, obtain explicit scene-graph regeneration authority, rerun the strict scene-graph check and required Content Integrity, and verify the selected source snapshot remains unchanged. No broad scientific campaign or mathematical-map acceptance follows. |

The last direct `/usr/bin/memory_pressure -Q` after Content Integrity reports 38%, below the existing 40% launch gate. No further build or resource-bound test retry is made at that observation. The optional reporting sweep, deferred numerical campaigns, general historical reacceptance, full-ladder execution and mathematical-map acceptance were not run or claimed complete.

Owner-aware `owned-compute-supervisor.mjs closeout --owner-task "$CODEX_SESSION_ID"` reports `clear` after validation. The 170-path snapshot remains unchanged after required Content Integrity. Scoped `git diff --check` over migration code, controls and evidence reports no whitespace errors. These are scoped completion checks, not permission to publish or acceptance of the outstanding build/test/generator conditions.

## Authorized validation resumption — September 12

The operator authorized the remaining validation, explicitly including scene-graph regeneration, after closing other applications. Initial `/usr/bin/memory_pressure -Q` reports 69% free and `df -k .` reports 912055608 KiB available; both exceed the unchanged launch requirements. `git rev-parse HEAD` reports `66e0e47de3797be86855acf6318aaab6c503031c`. Despite that checkout advancement, `shasum -a 256 -c .local-data/development-process-review/b-rem-2-5-baseline/final-candidate-source-sha256.txt` passes every one of the 170 recorded paths. No migration source or selected reference is changed for this resumption.

The initial strict scene-graph check already passes. The authorized `node scripts/build-scene-graph.mjs --write` is a no-op, followed by `node scripts/build-scene-graph.mjs --check --strict` with zero errors and warnings; `git diff --stat` over its three generated targets is empty. This task does not attribute the intervening freshness repair to an author or commit.

Required `node scripts/check-content-integrity.mjs`, supervised with a 180-second deadline and 15-second heartbeat, passes **30 required checks, zero required or reporting failures, one optional sweep skipped and zero not reached**. Lease `5ec21b15-5774-4757-b3d9-4fc3e284acc9` records exit zero, no signal, closed process group and 37.695 seconds; its exact stdout/stderr are under `.local-data/owned-compute/logs/` with the same run ID. This closes the earlier required scene-graph failure for the current candidate without claiming the optional broad sweep ran. F5 handoff and the two shared-caller suites retain their separate terminal-evidence requirements.

The final F5 handoff build succeeds under lease `4189d820-4efc-4331-bf8e-cea724861221`, exit zero and closed process group in 22.392 seconds. Its terminal output reports `build-process-admission-only`, 41 successful stages/gates and 194 checked bindings. The [F5 completion record](option-b-f5-cutover.md#final-handoff-build--september-12-completion) supplies the exact build receipt `269b566a8e9bc83188ff3434b19e9a86a2b9c2c80d00c8b9d657c42f8478533d`, outer admission receipt `7ec1cb15901d1e218ed7abac05b70ea06e48ea04cbf2c77c414a340c642e54b6`, paths and source readback. The successful enclosed-root build was not repeated. No scientific data or evolution was executed.

The remaining two-file serial Node selection is supervised under lease `7e1ab305-01cb-48d4-a094-546258d78a80`, with a 600-second deadline and 15-second heartbeat; immediately before launch, `memory_pressure -Q` reports 68% and `df -k .` reports 912023484 KiB available. The lease's exact command is:

```bash
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 600 --heartbeat-seconds 15 -- node --test --test-concurrency=1 tests/f6c-streamed-leaf-diagnostic.test.js tests/option-b-f6c-coordinator-process.test.mjs
```

The two resumed test files were measured before launch with `shasum -a 256`: `tests/f6c-streamed-leaf-diagnostic.test.js` is `7a63cddd4812553e25d6dcf4dc666adc5ace1bd5e52aa7d2e16ac2e0720c8431`; `tests/option-b-f6c-coordinator-process.test.mjs` is `2cea4c0287bca6c194d8374c099cae95efc6c050722a658979162ab9aedcb539`. The shared venv executes Python 3.13.2. Their synthetic and explicitly declared stationary controls establish operational behavior, not original-data scientific acceptance.

### Resumed terminal results

The complete two-file command reports **77 passed, zero failed, skipped or cancelled**, test duration 213989.966209 ms. Lease `7e1ab305-01cb-48d4-a094-546258d78a80` records exit zero, no signal, empty stderr, 214.066 seconds and `processGroupClosed:true`. Its stdout and stderr remain at the exact same-run paths under `.local-data/owned-compute/logs/`. This is the complete previously blocked selection, not a filtered successful subset.

The serial coordinator's actual independently selected observer writes `.local-data/braid-analysis/option-b-current-closure-oEzf39/observer/closure.json`. A known field-projection control passed before readback; the receipt records schema `braid-program/f6c-bounded-operation-external-closure.v2`, `accepted:true`, `processesClosed:true`, `lockReleased:true` and `wholeGuardClosed:true`. The test also rejects wrong external coordinator/map identities before dispatch. These are operational closure results only.

After terminal execution, the full 170-path `shasum -a 256 -c` comparison passes again, and separate SHA-256 measurements of both resumed test files match their prelaunch values above. The source-role and original-five transfer checkers also pass. Final direct memory measurement is 68%. Owner-aware supervisor `closeout --owner-task "$CODEX_SESSION_ID"` reports `clear`, checking 957 leases. No owned computation is left running.

The earlier 563-test selection remains valid for its unchanged selected source generation; the new 77-test result supplies the two deliberately excluded families. These separate receipts are not presented as one new monolithic run or universal test coverage. Required Content Integrity passes its full declared selection; its optional broad sweep remains unexecuted. The earlier failed records remain retained rather than overwritten.

### Finite package closeout

The independent reviewer authenticated the new required-gate, handoff-build and two-file regression receipts and accepted final operational validation in the [review record](../evidence/option-b-remaining-binding-dispositions.md#b-rem-5-independent-final-validation--authorized-resumption). The reviewer independently repeated the 170-path preservation check after execution and authenticated the serial observer's selected source/output bindings.

| Package | Final disposition and evidence |
| --- | --- |
| B-REM-1 | ✓ Done — connected F6c operational transfer and its preserved independent review. |
| B-REM-2 | ✓ Done — captured F5 build/evolution admission, prior selected controls, both final-generation builds and independent handoff process review. |
| B-REM-3 | ✓ Done — captured circular admission, corrected local controls and all enumerated shared-caller regressions, including the resumed 77-test selection. |
| B-REM-4 | ✓ Done — independently reviewed roles for all 157 original candidates and 39 additional owners. |
| B-REM-5 | ✓ Done — migration-specific reconciliation, independent operational validation and authorized registry freshness repair with all 30 required checks passing. |

All five completed package rows are removed from the executable queue. This finite operational completion leaves the optional broad reporting sweep, deferred scientific campaigns, historical-input recovery, full-ladder capabilities, mathematical-map acceptance and general all-selection repository verification with their existing owners. No numerical result is reaccepted by a source-map update, and no Git publication is performed in this resumption. The resume skill preserved the original package completion conditions; the compute skill kept both resumed jobs under owned supervision and unchanged resource limits.

### Latest required gate after TOC regeneration

A final `validate-content.mjs --check --strict` reports zero errors/warnings across 1687 Markdown files, but the following strict scene-graph check detects fresh drift in the same two TOCs. The authorized `build-scene-graph.mjs --write` then updates only `content/graph/textbook_toc.json` and `content/generated/markdown/textbook/toc.md`; its strict check passes with 1654 sections. Their scoped Git diff contains three newly indexed headings from nuclear binding, molecular geometry and condensed matter; direct `rg` confirms those headings in the live authored files. This task did not edit those authored sources.

The subsequent required Content Integrity lease `887835f4-25db-4700-b750-c7f16cc248cc` reports **29 required passed, one required failed, one optional sweep skipped and zero unreached**, exit one, no signal, closed process group and 37.43 seconds. The scene-graph check passes; the sole failure is `content/generated/equation-mapping/corpus-equations.json` freshness. Its owning `node scripts/build-equation-mapping-corpus.mjs --check` independently reproduces that exact drift. The earlier 30-pass gate remains a valid earlier receipt, not the current whole-checkout gate result. All 170 migration snapshot paths still match after this failure.

The operator's explicit regeneration scope covers the two TOCs, not this newly failing equation registry. Inspection of `buildEquationMappingCorpus` distinguishes `--build`, which regenerates only the registry after validation, from `--write`, which can also insert links into authored Markdown. The proposed bounded follow-up is `node scripts/build-equation-mapping-corpus.mjs --build`, followed by `--check` and required Content Integrity once current corpus editing is stable. It has not been executed or authorized here.

B-REM-1 through B-REM-4 remain complete. B-REM-5's migration-specific validation is independently accepted, but its latest required-gate condition is partial until this separate generated artifact is refreshed and checked. Candidate-specific acceptance remains valid; main uses the stricter current-gate status because the requested final validation includes the freshly regenerated TOCs and the subsequent failed gate must remain visible. This supersedes the temporary all-five-complete status reached after the earlier green gate; it does not reopen source migrations or the completed memory-intensive work. Owner-aware compute closeout again reports `clear`, checking 959 leases. Final authored/document and generated-TOC changes pass scoped `git diff --check`.

### Authorized equation registry refresh and final gate

The operator's subsequent “do item 1” authorizes the generated-only equation-registry refresh and required validation. The initial `node scripts/build-equation-mapping-corpus.mjs --check` reproduces registry drift. `node scripts/build-equation-mapping-corpus.mjs --build` then succeeds, followed by `--check` with zero errors: 199 Markdown files, 4685 display equations, 23 promoted equations and 30436 symbol definitions. The strict scene-graph check also passes with 1654 sections. These are structural/freshness measurements, not mathematical acceptance.

The final registry SHA-256 is `e60ec69a3313753275e76af8eb7f202a9976560f695b0589b374088b0d1c5ff3`, measured by `shasum -a 256`. `git ls-files` returns no tracked registry path, while `git check-ignore` reports that exact path ignored. The generator's `--build` branch emits only the validated registry; the authored-link-changing `--write` branch was not used. Before/after `rg --files ... -0 | xargs -0 shasum -a 256` captures over `content/markdown/aaa` and `content/scenes` have identical path/digest sets. A known two-list difference control passed before comparison; an initial raw-string comparison differed only because discovery order varied.

Required Content Integrity lease `f59ba5e8-ee00-4c5d-b19f-60379dc2adb4` reports **30 required passed, zero required/reporting failures, one optional sweep skipped and zero unreached**, exit zero, no signal, empty stderr, closed process group and 37.271 seconds. Exact logs are `.local-data/owned-compute/logs/f59ba5e8-ee00-4c5d-b19f-60379dc2adb4.stdout.log` and its `.stderr.log` sibling. The launch command was:

```bash
node scripts/dev/owned-compute-supervisor.mjs run --owner-task "$CODEX_SESSION_ID" --deadline-seconds 180 --heartbeat-seconds 15 -- node scripts/check-content-integrity.mjs
```

The 170-path migration descriptor again passes `shasum -a 256 -c` after the gate. Owner-aware compute closeout reports `clear`, checking 961 leases. No completed build or 77-test regression family was repeated, no scientific/source-map/reference code changed, and no Git publication was performed by this task. B-REM-5 is complete and removed from the executable queue. This final successful gate supersedes the preceding registry-freshness blocker without erasing its retained failed receipt. A failing named gate on this selected state, a registry/check mismatch or changed selected migration bytes would overturn the corresponding completion claim.

## Nonliteral owner dispositions

These selections were traced by reading their actual consumers, not inferred from absence in the quoted-SHA census. Each retained binding continues to serve an obligation outside current repository source admission.

| Owner and consumer | Binding meaning | Disposition and coverage |
| --- | --- | --- |
| `package.json` and `package-lock.json`; `npm ci` in `.github/workflows/option-b-trial.yml` and dependency audit in `.github/workflows/content-integrity.yml` | Exact external dependency versions and package-distribution integrity, including JSON-LD, N3 and Comunica | Retained under npm dependency installation/audit. These hashes do not choose repository-owned source for a scientific caller. No package upgrade or audit exemption is selected. |
| `.github/workflows/` and `.github/actions/build-pages/action.yml`; GitHub Actions runner | External Action commit identities, workflow permissions, bounded jobs and deployment routing | Retained under workflow/release owners. The mathematical two-chain trial remains report-only; required operational B admission stays inside Content Integrity. No remote setting or live deployment is changed or claimed verified. |
| `scripts/config/foundational-impact-contracts.json`; `scripts/check-foundational-impact.mjs` | Path-triggered command/consumer contracts and scientific verification selection | Retained as command-routing authority. It contains no implied scientific baseline replacement. Migration checks belong to their required Content Integrity step, not a new broad numerical selection. |
| `scripts/config/generated-runtime-assets.json`; `scripts/prepare-runtime-assets.mjs` | Four ignored, reproducible runtime families and their canonical generators | Retained under runtime-asset/retention ownership. Runtime data is not an authored source-admission map. The configured generators may reconstruct ignored payloads; tracked generated artifacts retain their separate write authority. |
| `scripts/pr-validation-receipt.mjs`; `.githooks/pre-commit` and `.githooks/pre-push` | Exact staged-index, unstaged-overlay, untracked-content, base-ref, toolchain and validator-contract fingerprint | Retained under publication. Changing current manifests necessarily changes the candidate fingerprint; an old receipt cannot authorize new bytes. The mathematical trial retains its `reportOnly` flag. No publication procedure is invoked. |
| Authored `option-b-*-baseline.json` descriptors and historical `.source` archives; retained-A transfer checker | Original source selection and transfer provenance | Retained byte-for-byte. Only a specifically reviewed operational successor may replace an inherited current helper; scientific and historical bindings cannot be refreshed by that mechanism. |
| Mathematical dependency contracts; `check-moving-single-root-map.mjs` and `dependency-map-reader.mjs` | Mathematical statements, selectors, evidence and independent baseline acceptance | Retained under the report-only two-chain trial and its separate operator acceptance decision. Operational migration is not mathematical acceptance. |

Claim grade: measured for the named reads, retained source copies and baseline command; inferred for each role classification. A consumer that executes repository source using one of these retained values, a changed source admitted under an old selected manifest, or a missing candidate in the final disposition census would overturn the corresponding classification. The baseline is not a final migration result or overall repository-health claim.

## Option A retirement audit — September 12

The subsequent [authorized cleanup and full-B direction](#authorized-retirement-cleanup-and-full-b-direction) supersedes the first two proposed implementation states below. The audit findings remain their pre-implementation evidence.

### Result and scope

The operator selected an exact retirement audit after completion of the five migration packages. This section records the audit, not authorization to delete code, disable checks, advance mathematical baselines or run deferred science. Operational migration is complete within its recorded scope, but recurring verification still contains original-A dependencies. These dependencies should be distinguished from current launch fallbacks: preserving a transfer proof does not mean an old launcher remains the production path.

The current-source reader, transfer checker, disposition checker, required and maintenance gate selections, their tests, and the remaining `PINS` consumers were read at observed HEAD `859f2b07cb17889ca2c239d82fd61455c2ba903c`. `rg` over `scripts/eom`, `scripts/equation-mapping`, `src/eom` and the profile-census test located the remaining pin-table uses; consumer searches extended through scripts, tests, workflows, hooks, configuration and repository documentation. The existing per-binding [dispositions](../evidence/option-b-remaining-binding-dispositions.md) remain the detailed role inventory. This audit does not reclassify every retained scientific judgment from scratch or claim that literal search discovers every dynamic dependency.

`node scripts/equation-mapping/check-current-source-dispositions.mjs` passes with 157 original candidates, 155 current literal candidates and 196 disposition rows. `node scripts/equation-mapping/check-current-source-maps.mjs` passes all five original profiles. A separate read-only enumeration of `option-b-*-sources.jsonld` in `contracts/` validates all eleven operational manifests with the unchanged reader and compares every declared source digest with its current bytes. The digest comparator first passes the literal `abc` case and rejects changed bytes; the existing graph controls pass before target use. These checks establish declared-source consistency, not complete launch execution, scientific applicability or approval of the selected source generation.

### Exact retirement candidates

Legend: ✓ Done, ○ Not done. The audit is ✓ Done; every implementation below is ○ Not done and proposed, not a newly accepted migration package.

| Candidate and exact owner | Current responsibility | Proposed disposition and protection that must survive |
| --- | --- | --- |
| ○ Not done — consolidate `tests/current-launch-bindings.test.js` and its maintenance invocation in `scripts/check-content-integrity.mjs` | One test checks the exact five-profile name set, absence of exported `PINS`, and presence of `initializeSourceBindings`. `tests/option-b-root-cover-admission.test.mjs` already exercises the latter two properties in stronger admission cases, but iteration over `PROFILES` alone does not preserve the exact name-set assertion. | Move that exact profile-set assertion into the required admission suite, then remove the redundant standalone test and its maintenance entry. Preserve or update source links and the existing discovery disposition. Do not merely delete the test: removing an entire profile could otherwise remove its loop-generated coverage silently. This is the smallest justified cleanup. |
| ○ Not done — make retirement representable in `scripts/equation-mapping/check-current-source-dispositions.mjs`, `tests/option-b-disposition-coverage.test.mjs` and the existing disposition document | `inspectCoverage` requires the original ordered 157-path census, then requires every disposition path to exist. Allowed dispositions are `migrated`, `retained` and `out-of-scope`; none expresses an approved removal. Consequently, even a justified deletion of a listed file fails the existence assertion. | Preserve the original discovery record and add reviewed retirement semantics within the existing owner. A retired row must explain the ended responsibility or name its surviving replacement, not vanish from the inventory. Require live files for live roles; keep failures for missing unretired files, unknown current candidates and incomplete role evidence. Do not drop the checker or lower the original count to conceal removals. Known retired/missing-live/unknown-current controls must precede target application. |
| ○ Not done — separate historical transfer proof from current acceptance in `scripts/equation-mapping/check-current-source-maps.mjs`, `scripts/equation-mapping/legacy-role-bindings.mjs`, `tests/option-b-root-cover-admission.test.mjs`, `tests/option-b-operational-successor.test.mjs` and their required gate | `inspectTransfer` retrieves old entry source with `git show`, extracts A pins, and compares current graph path/digest pairs against those original identities. Only specifically coded operational successor cases alter that comparison. A valid graph with changed selected bytes is therefore not sufficient to pass the recurring transfer check. | Retain the original A descriptors and original-source transfer evidence. Before retiring the recurring A-generation comparison, accept an explicit B-to-B review boundary using the existing source maps, impact query and operator review route. Required coverage must continue to catch removed dependencies, changed scientific/reference selection, role changes, wrong profile, stale bytes, missing external selection and worker/stage substitution. Then move the one-time original-source extraction out of routine validation if it has no remaining current consumer. The extractor is currently imported by both the transfer checker and required admission tests, so it is not an unused file eligible for immediate deletion. |

The third candidate is the substantive remaining A coupling. Moving digests into JSON-LD did not remove the old-generation comparison in the required gate. Replacing that comparison requires an accepted successor rule; deleting it first would remove an existing protection against silently dropping or changing inherited requirements. The proposal does not permit a current manifest to approve itself or let a source's editor redefine an independent scientific expectation.

### Retain, with identified reasons

| Exact family or owner inspected | Disposition |
| --- | --- |
| `scripts/eom/run-f6c-emission-refinement-pilot.mjs` and `run-f6c-refined-acceleration-pilot.mjs` | Retain scientific, declaration and historical `PINS`. Their `initializeSourceBindings` and required private admission state are separate from those tables. The `SOURCE_BINDINGS[path] ?? PINS[path]` lookup is not by itself an optional-A launch path: its surrounding admission and expected-role checks must be read. Refined scientific transport remains a separately deferred capability. |
| `scripts/eom/run-f6c-streamed-leaf-diagnostic.mjs`, `prepare-f6c-streamed-leaf-invocation.mjs` and `run-f6c-evidence-packaging.mjs` | Retain scientific/codec/independent-reader selections. Current coordinator admission is separately required; packaging delegates to `admitPlanSourceBindings`. These identities protect interpretation of retained evidence rather than choosing an unreviewed legacy coordinator. |
| `scripts/eom/f6c_variable_cell_adapter.py`, `prepare-f6c-parent-emission-refinement.py` and `verify-f6c-parent-emission-refinement.py` | Retain extraction of the original full-entry pin table as historical ancestry verification. It reads preserved source bytes; it is not a reason to put `PINS` back into the current full entry. |
| `scripts/eom/execute-f5-prehistory-handoff.py` and `run-subfield-circular-root-pilot.mjs` | Retain fixed producer/reference and proof/reducer identities alongside separately selected operational sources. Deleting these checks would waive scientific applicability rather than retire a duplicate launch selector. |
| `scripts/eom/verify-f6c-bounded-operation-closure.mjs`, `tests/f6c-bounded-operation-closure.test.js` and `tests/f6c-bounded-operation-current-closure.test.js` | Retain historical-v1 and current-v2 closure responsibilities. The current schema does not make verification of still-consumed historical records redundant. |
| `scripts/eom/run-coincident-midpoint-4-2-1-frequency-and-coaxial-separated-two-planar-braid-co-rotating-resolution-coverage-calibration.mjs` and its same-basename test | Retain in this audit. Source inspection finds explicit execution rejection and test consumers of exported definitions and that rejection. Removal belongs to the calibration owner's retention/consumer decision, not an A-to-B source-selection cleanup. |
| `option-b-*-baseline.json`, retained `.source` files, package integrity, external Action pins and publication receipts | Preserve the identified historical, dependency-security and exact-candidate roles described above. Neither age nor the presence of a digest establishes redundancy. |

### Verification and limits

The following unchanged six-file selection passes all 54 tests, with zero failures, skips or cancellations, under supervised run `a0692a63-06b0-4918-a98c-6100feea4160` in 8.650 seconds, exit zero and closed process group:

```bash
node --test --test-concurrency=1 tests/current-source-manifest.test.mjs tests/option-b-root-cover-admission.test.mjs tests/current-launch-bindings.test.js tests/option-b-disposition-coverage.test.mjs tests/option-b-operational-successor.test.mjs tests/equation-dependency-map.test.mjs
```

Exact logs are retained under `.local-data/owned-compute/logs/` using that run ID. No complete resource-intensive or whole-repository sweep was repeated. The separately requested mathematical-map review uses the existing trial, whose new local receipt is `.local-data/option-b-retirement-review-20260912-fgiGuU/trial/report.json`; its interpretation and acceptance remain with that review, not this retirement audit.

Claim grade: measured for the named source reads, checker results and test run; inferred for the conditional retirement recommendations. A required test already preserving the exact profile-set assertion, an accepted B-to-B transition route used by the transfer checker, or an approved-retirement branch already implemented by the disposition checker would overturn the corresponding proposed work. A still-live consumer or missing replacement predicate blocks a proposed deletion. No source, manifest, test, gate or baseline was modified to obtain the audit results.

## Authorized retirement cleanup and full B direction

The operator clarified the end state: B must take over all A functionality, and hashes must be removed from source files. The same instruction authorizes the audit's first two cleanup items and acceptance of the exact reviewed mathematical maps for report-only comparison. The [architecture owner](../../../op/git/git-backed-knowledge-architecture.md#accepted-full-b-end-state) records the complete target. The five completed operational packages are a bounded milestone, not completion of that target. Earlier `retained` dispositions preserve necessary responsibilities and evidence; they do not exempt embedded hashes from eventual transfer into B records.

The standalone `tests/current-launch-bindings.test.js` is removed after its exact five-profile assertion is moved into required `tests/option-b-root-cover-admission.test.mjs`. That suite also retains the original rejection of exported operational `PINS` and exercises `initializeSourceBindings` rather than merely checking its type. `scripts/check-content-integrity.mjs` no longer invokes the deleted maintenance test; its required admission suite remains enabled. The original file is recoverable from Git commit `859f2b07cb17889ca2c239d82fd61455c2ba903c` and is explicitly represented as a retired additional owner in the existing inventory.

The disposition document uses v2 with the original ordered 157 candidates unchanged. A `retired` row retains its binding roles and requires operator-attributed decision, outcome, replacements and verification. Replaced responsibilities require distinct canonical replacement paths; ended responsibilities require an explicit reason and no replacement. Retired paths must be absent, including directory or dangling-symlink reappearances. Live roles, decision documents and replacements must exist as canonical regular files. A retired row cannot satisfy a live candidate or point to another retired replacement. The checker validates structure and file state; it does not infer semantic approval from an `operator-approved` string. Existing missing-candidate and incomplete-role failures remain in force.

Known controls passed before target use: `node --test tests/option-b-disposition-coverage.test.mjs` ran five tests, zero failures/skips/cancellations, including replacement/ended cases, missing authority, incomplete decisions, lost live files, missing or retired successors, and path/symlink negatives. The existing inventory is extended rather than replaced by another tracking system. Final combined verification is recorded below after independent review.

The separate [mathematical-map acceptance](../processes-git-codex-claude.md#current-map-scope-acceptance--september-12) identifies the exact prior reviewed commit, both maps and preserved review artifacts. The loader and scientific instruments are unchanged. Neither this scoped acceptance nor retirement support completes B takeover, removes the recurring A-generation comparison, or removes remaining source hash literals. The next substantive transfer must establish B-to-B review of changes and move the remaining historical/scientific/fixture selectors into external B records without losing their checks or editing a scientific subject and its reference together.

### Cleanup verification and independent acceptance

The independent reviewer (`01a096b8-6794-78d1-84c8-01c761a89219`) found and verified repairs for unresolved decision fragments and a fence/comment interaction that could expose an example heading. The final checker resolves exactly one plain ASCII ATX decision heading outside fences and comments; it is not a general Markdown parser. It rejects non-directory ancestors rather than treating `ENOTDIR` as absence. The review ran all seven known controls before the actual inventory, then granted bounded structural acceptance. Checker SHA-256 is `6d07a96ce73859e17b53b4eaaf39c5bbecea6ace4a91ae3995d2eb9247e1e826`; controls SHA-256 is `67c3b94b78f6e643f379dbcea014536f676c18b706695eb80ae95727f22dff99`, confirmed independently and by main with `shasum`/Node SHA-256.

After the final fix, supervised run `352df42b-6156-428c-b5c9-21d226e1b597` executed `node --test --test-concurrency=1` on `tests/current-source-manifest.test.mjs`, `tests/option-b-root-cover-admission.test.mjs`, `tests/option-b-disposition-coverage.test.mjs`, `tests/option-b-operational-successor.test.mjs`, `tests/equation-dependency-map.test.mjs` and `tests/content-integrity-reporting.test.js`. Its terminal log reports 67 passed, zero failures/skips/cancellations; the lease records exit zero, empty stderr, closed process group and 8.597 seconds. This supersedes the earlier pre-fix combined run as evidence for the final checker.

`node scripts/equation-mapping/check-current-source-dispositions.mjs` passes with 157 original candidates, 155 live quoted-digest candidates, 197 total rows and one retirement. A known match/mismatch-checked deep comparison against `git show HEAD` confirms the original 157 rows, 39 earlier additional owners and historical accounting review are unchanged. `node scripts/equation-mapping/check-current-source-maps.mjs` passes all five original transfers. SHA-256 checks confirm both accepted maps and both copied review artifacts exactly match the acceptance record. These are bounded source/accounting results, not proof that every possible hash encoding or dependency has been found.

The repository-wide `node scripts/validate-content.mjs --check --strict` audits 1694 Markdown files and reports one error, zero warnings: a link target `y` in `reference/priorities/aaa-corpus-rewrite/evidence/crw-005-radiation-review-2026-09-12.md:50` resolves to a missing file. That concurrent, out-of-scope document was not edited by this task; this run is not a clean whole-repository result. Scoped `git diff --check` passes. Owner-aware compute closeout reports clear after checking 969 leases. No scientific rerun, generated-source refresh or Git publication was performed.

## B-to-B continuation

The operator selected implementation of B-to-B change acceptance followed by removal of the remaining inline hashes. At the start of this continuation, launch manifests performed exact capture and verification but the recurring checker still derived expectations from A source and contained two special successor rules. That recurring A comparison is now replaced by explicit B checkpoint/transition selection. The existing manifest reader is unchanged: every launch profile captures its exact bytes, so a future change to that common reader requires an explicitly reviewed dependent-map transition rather than silent repinning.

Before edits, the known quoted-digest and long-decimal controls passed, then the existing inventory instrument enumerated 155 source candidates across its declared source extensions. A SHA-256 known-answer control preceded incoming-digest searches for candidate tests using `rg -l --hidden -F` over the repository, excluding `.git`, `.local-data`, `.tmp` and `node_modules`. Several Python conformance/control tests have executable consumers in launchers, preparers and independent verifiers; selected Node test leaves have only retained evidence/accounting references at their current digests. This is an exact-digest search, not proof against dynamic or alternative-encoding consumers. Historical records remain bound to their old bytes and are not refreshed to describe new tests.

Implementation ownership is disjoint: main integrates and transfers independent leaf test expectations, operational acceptance work owns the original-five transfer checker and its B acceptance records, and mathematical comparison work owns the report-only runner and accepted-map loader. Independent review checks preservation of authority and fixture meaning. Each transferred expected value must equal its original literal; an expectation recomputed from its own subject would invalidate the transfer. B record ownership changes where an expected value lives, not what grants scientific acceptance.

### Current acceptance mechanism

✓ Done — the [B-to-B contract](../contracts/option-b-current-source-transition.md) now governs the original-five required checker. Its externally selected accepted checkpoint contains exact predecessor maps, retained evidence obligations and explicit per-source operational eligibility. Its independently selected transition contains every exact before/after scalar change. The initial transition changes no source. Missing external selection, self-approval, omitted dependencies, role/path/scope changes, revision reuse and final same-byte file replacement reject. Routine validation neither executes nor parses old A source; the old checker and original descriptors remain preserved in checkpoint-bound historical data.

Independent review found that the old acceleration map labels two frozen scientific documents `current-source`. A broad permission based only on that label would have weakened their inherited protection. The correction makes every source default-denied unless the accepted checkpoint explicitly grants its exact path and role. Initial grants cover only admission/launcher responsibilities and the named outer/prescribed operational helpers. The actual acceleration proof and member-predeclaration paths now have negative controls that propose otherwise valid revised graphs and verify rejection. The reviewer independently passed all 37 transition controls before checking all five unchanged production maps, then accepted this bounded mechanism. Protected scientific, reference, control, resource-policy and reader responsibilities require their own reviewed transfer; this operational policy is not a permission to refresh them.

✓ Done — Content Integrity now supplies both record paths and stored expected digests from the authored selection record, and requires the new B-to-B controls. The root-cover admission suite takes its exact profile/source/evidence census from accepted B records while retaining real initialization, worker, stage and wrong-profile rejection checks. `legacy-role-bindings.mjs` and `option-b-operational-successor.test.mjs` are retired with explicit inventory dispositions; both remain recoverable at commit `859f2b07cb17889ca2c239d82fd61455c2ba903c`. `rg` across `scripts/` and `tests/` finds no executable consumer of the retired extractor or `inspectTransfer`; the old successor filename appears only in the required-gate negative assertion that it must not run. Historical references are preserved and live Markdown links to deleted source point to its exact Git generation.

✓ Done — the mathematical comparison loader consumes the separately accepted two-map B predecessor via an external acceptance digest. It authenticates the acceptance record, copied review artifacts and all 23 reviewed predecessor input files from the exact recorded Git commit. Current candidates remain separately inspected. The existing scientific checks are unchanged and remain required within the report-only trial; no new mathematical or production acceptance follows. The actual final trial reports the moving chain as `review-required` because its runner and reader changed, while the finite chain is unchanged relative to its accepted baseline. Both keep candidate approval `not-granted`.

### Source expectations transferred and validation retained

✓ Done — [known-hash-answers.json](../../../../scripts/equation-mapping/fixtures/known-hash-answers.json) holds the independently published SHA-256 known answer, encoding and named consumers. [option-b-retained-test-identities.json](../../../../tests/fixtures/option-b-retained-test-identities.json) holds original vendor integrity, prescribed-seed, historical source/output and synthetic fixture expectations with their roles and provenance. Twenty existing test files now consume these external B records. Selected values are captured as primitives or copied frozen arrays, malformed expectations fail, and expected results are never computed from their subjects. The Mermaid literal-occurrence checks still check the exact vendor strings. The new record is not a license to update an expected output after a failing scientific check.

A known single/double-quoted repeated-literal control passed before the second eleven-file transformation. Reverse substitution reconstructed the original test bodies before applying the edits. Independent review separately reconstructed all twenty original literal occurrences across those eleven consumers and accepted the value-preserving transfer. The first nine-file selection separately preserved its published known answer and vendor/seed values from the recorded Git source. Original scientific subjects and their numerical expectations were not co-edited.

The first eleven-file regression failed: lease `3b06dea8-87ba-432f-a6de-46b9880796b4` reports 69 tests, 66 passed and three failures, exit one with closed group. Inspection of the F5 interpolation test against the actual preparation API showed that it did not await the async function or supply the mandatory external map selection; it consequently launched from the wrong root and produced an unhandled rejection. The caller now awaits preparation and verification with the unchanged accepted F5 map selected from an authored external record. Its instrument, expected report and enclosure assertions remain unchanged. Independent review accepted this caller repair separately from the literal transfer; its two-test run `7f91b816-7959-4a04-975d-3a2f88adc2b3` passed in 5.521 seconds. The failed batch is retained, not relabeled as a pass or attributed to another change without evidence.

| Instrument and exact scope | Terminal result | Retained evidence |
| --- | --- | --- |
| Serial Node run of all twenty transferred test consumers | 100 tests: 99 passed, zero failed/cancelled, one existing opt-in current-context skip; 109.029 seconds; exit zero, closed group, empty stderr | Owned lease `bcad1788-2970-4983-8456-88fdcbb15efe`; its command lists all twenty paths |
| `node --test --test-concurrency=1 tests/option-b-current-source-transition.test.mjs tests/option-b-root-cover-admission.test.mjs tests/content-integrity-reporting.test.js tests/option-b-disposition-coverage.test.mjs` | 85 passed, zero failed/skipped/cancelled; 3832.66925 ms | Final integration tool output, after default-deny fix and external selection update |
| Mathematical acceptance, dependency-map and PR-validation test selection | 32 passed, zero failed | Owned lease `8daa863d-8928-4e4d-922b-ac9ce8294755` |
| Actual report-only comparison with explicit accepted-record digest | Four passing checks, `review-required`, approval `not-granted`; 1.088 seconds; exit zero, closed group, empty stderr | Owned lease `ea5d80dc-6413-422c-98dc-f8b8f606f0af`; local report `.local-data/option-b-accepted-comparison-20260912-final-wording/report.json` |
| Required Content Integrity, complete declared default selection | 28 required passed, two required failed, one optional sweep skipped, zero unreached; 39.271 seconds; exit one, closed group | Owned lease `98844e92-b91b-41e0-afd8-bca44bcb3a2f` |

Each owned lease has its exact command and stdout/stderr files at `.local-data/owned-compute/logs/<run-id>.stdout.log` and `.stderr.log`. These separate selections are not one monolithic suite or universal test health. The existing opt-in skip was not added to obtain a pass. The final mathematical receipt binds the current known-answer fixture metadata; earlier receipts from before that metadata addition remain historical, not final-current evidence.

The required gate's B admission, B checkpoint/transition and disposition checks all pass. Its two failures are outside the edited B sources: the scene-graph checker reports `content/graph/textbook_toc.json` and `content/generated/markdown/textbook/toc.md` out of date, and the terminology checker reports reception `T` instead of `T_r` at `content/markdown/aaa/reactions/mode-taxonomy.md:92`. The latter has separate corpus changes visible by `git diff HEAD --` on that file; no causal author attribution is inferred. This task edits neither that prose nor the generated TOCs. The generated repair command is `node scripts/build-scene-graph.mjs --write`, followed by `node scripts/build-scene-graph.mjs --check --strict`; it needs explicit regeneration authority or the publication procedure. The corpus owner must resolve the terminology finding before a full gate can pass. The earlier broken radiation-review link is absent from this run's failures; current content/index validation passes.

### Remaining source-hash scope

◐ Partial — `node scripts/equation-mapping/check-current-source-dispositions.mjs` reports 157 original candidates, 131 live quoted-digest source files, 208 disposition rows and three retirements. Known equality/inequality controls preceded a deep comparison with `git show HEAD`, which confirms all 157 original candidate rows remain unchanged. This is a decrease from 155 to 131 live candidate files, not a count of all hash occurrences. The detector covers quoted 64-hex values in the source extensions and directories declared by that checker; it does not prove absence of embedded/escaped strings, regex or base64 forms, shorter identifiers, generated data or undeclared dependencies.

A known path-group case preceded a repeat of the same live source census: 74 files under `scripts/`, 14 under `src/` and 43 under `tests/`. Their exact paths and inherited role obligations remain in the disposition owner. The remaining transfer is not deletion of tests or approval to drop a hash-bearing assertion:

| Remaining family | Current disposition and required continuation |
| --- | --- |
| Historical evidence and original-source reconstruction | ◐ Partial — preserve exact old identities and logical/physical routes in external records; adapt consumers without refreshing historical evidence or substituting current outputs |
| Scientific producers, independent references, proof/contract selectors and pinned conformance controls | ◐ Partial — use explicit scientific/reference records and preserved applicability checks; review incoming binders before each dependency-ordered transfer and do not co-edit a reference implementation with its subject |
| Operational families outside the original-five accepted checkpoint | ◐ Partial — their launch admission is already B, but routine successor review still needs explicit accepted family checkpoints and per-source eligibility; the five-profile selection cannot silently expand its scope |
| Remaining test, application catalogue and compiled-source expectations | ◐ Partial — externalize exact expectations using the consuming language/build contract, retain vendor/seed/synthetic distinctions, and run each affected consumer selection; no blanket code-text replacement |

The original-five maps, other operational source maps, both accepted mathematical maps, the shared manifest reader and the F5 scientific instrument produce no changes under the explicit scoped `git diff HEAD --name-only` readback. Independent review also compared the five embedded predecessor map texts with both live files and HEAD. A lost assertion, altered expected value, unreviewed eligible source, accepted protected-role change or mismatched selected receipt would falsify the corresponding bounded completion. The parent full-B task stays active; the remaining scientific and application families are not declared migrated by inventory coverage. No Git publication or scientific campaign was performed.

## Authorized gate repair and next hash transfers

The operator selected both recommended follow-ups: resolve the notation/TOC gate failures and continue dependency-ordered source-hash migration. At resumption, `git rev-parse HEAD` reports `2490eb54aef24bf6d9a49cbc7ba62f1e54553305`, and `git --no-optional-locks status --short` initially returns no changed paths. The live reception expression already consistently uses `T_r`; `check-master-equation-terminology-migration.mjs` and `build-scene-graph.mjs --check --strict` both pass before edits. Those requested corrections therefore need verification, not another rewrite or attribution to this task.

The initial required gate, owned lease `87bd7374-17c0-48a1-91ed-c8a7aae8ce39`, reports 29 required passes, one required failure, one optional skip and zero unreached; exit one, closed process group, 38.286 seconds. The only failure is the ignored equation-corpus runtime registry, not the two earlier blockers. Its declared family in `scripts/config/generated-runtime-assets.json` uses `build-equation-mapping-corpus.mjs --build`, which cannot enter the authored-link edit branch. The permitted `prepareRuntimeAssets({familyId:'equation-corpus', mode:'write'})` API refreshes only that runtime family; its owning `--check` then passes with 199 Markdown files, 4685 equations and 30443 symbol definitions. This is permitted runtime setup, not a new tracked-output regeneration scope or mathematical acceptance. Other corpus edits beginning after the initial clean read remain separate work.

### Python transport controls

The three selected Python tests are `test_f5_current_handoff.py`, `test_f6c_retained_history_export.py` and `test_f6c_acceleration_execution.py`. Known positive/negative SHA controls precede incoming-digest searches with `rg -l --hidden -F`, excluding `.git`, `.local-data`, `.tmp` and `node_modules`. Their current digests occur only in retained readiness/accounting/validation records under that search. A pathname search also identifies the acceleration entry's operational test census; inspection of its actual plan validation confirms that the external plan selects the test bytes rather than a fixed source-map digest. Its existing synthetic private-plan binding remains a test mechanism and does not rewrite a retained plan or grant scientific acceptance.

The shared-venv pre-edit controls pass: nine F5 tests under lease `a889f203-bc66-4d00-9580-eb180ce60b75`, sixteen retained-export tests by direct unittest discovery, and seven acceleration tests under lease `0d022092-42db-444b-b7c3-45bc89a6729e`. Independent pre-edit review confirms the scoped binder findings and the exact seven literal occurrences: four copies of the published `abc` answer, two positive historical-prior identities and one rejected-current-verifier identity. Producer, reference and fixed historical input bytes are not selected for editing.

The tests now capture immutable values from authored data. The existing known-answer record retains its one canonical value and adds the Python consumers. A named prior-identity fixture preserves the original positive/negative distinction. The small test-only `option_b_fixture_records.py` reader rejects duplicate JSON keys, wrong schema/algorithm/encoding/role, incorrect field census and malformed hashes, returning strings or a tuple. It is not a production graph-admission path. Its initial three hand-authored positive/negative unittest controls pass before the changed target suites run. A fourth control then verifies that malformed fixtures reject through all three actual consumers before scientific imports; all four controls pass before the final combined target run. A test-source digest alone no longer attests the external fixture generation; final validation records the reader and both JSON dependencies together with the consumer files.

### Parallel transfer boundaries

The next Node test leaves and the Borg catalogue have separate implementation ownership, with read-only independent review and main integration. Exact values, original assertion behavior, current dependencies and historical binders must be preserved before updating the current inventory. No mechanical transfer is permission to change an expected numerical output.

The Borg budget source is not an independent leaf: the F5 evolution admission identifies `BorgCertifiedBudgets.js` as a scientific contract and captures it under a module hook that presently forces JavaScript format. A JSON import would require a captured data dependency and format-aware loading, not merely moving literals. The circular release-binding generator also inventories that file as scientific source. Its standalone edit is therefore excluded from this candidate; the later coordinated transfer must preserve these consumers and independent scientific applicability. The catalogue has a separate taxonomy-check consumer that must follow the authored data when its wrapper changes. These are concrete dependency constraints, not permanent inline-hash exemptions.

### Twelve-consumer transfer and validation

The Node transfer preserves twenty-four original literal occurrences across nine consumers: `f6c-acceleration-pilot.test.js`, `f6c-evidence-packaging.test.js`, `f6c-bounded-operation-closure.test.js`, `f6c-streamed-leaf-diagnostic.test.js`, `option-b-circular-admission.test.mjs`, `option-b-f6c-coordinator-process.test.mjs`, `option-b-f6c-family-admission.test.mjs`, `option-b-f6c-streamed-preparation.test.mjs` and `subfield-circular-root-pilot.test.js`. Their authored JSON record retains original source attribution and role-specific expectations; the test-only reader captures immutable primitive values and rejects malformed records without deriving an answer from a subject. A reverse reconstruction recovers the original literal values. Together with the seven Python occurrences, this batch moves thirty-one literals out of twelve existing test files without removing their assertions.

Read-only independent review accepts the exact-value transfer, dependency registration, rejection controls and required-gate wiring. It corrected the retained closure-checker role to `fixed-operational-control`, preserving its digest. The new Node reader controls are included in the existing required B test step, with an explicit reporting regression assertion; no scientific subject, independent scientific reference, expected result or current-source map was refreshed. An initial nested test-control attempt inherited `NODE_TEST_CONTEXT` and skipped its child; the final isolated child removes that inherited harness marker and passes the actual positive and malformed-fixture cases. That harness correction does not turn a skipped attempt into evidence.

| Instrument and exact scope | Terminal result | Retained local evidence |
| --- | --- | --- |
| Serial Node run of nine changed consumers, new fixture controls and unchanged current-closure importer | 176 passed; zero failed, cancelled or skipped; exit zero, closed group; 224.644 seconds | Lease `95e43c6a-8d3c-4d6d-b546-140ed790ca49`; `.local-data/option-b-next-test-identities-receipt.json` |
| Shared-venv unittest run of three changed Python consumers and reader controls | 36 passed; 2.220 seconds unittest time, 2.378 seconds supervisor time; exit zero, closed group | Lease `ae60f565-7523-47df-92f1-3332af2d5ce1`; `.local-data/option-b-python-transfer-receipt.json` |
| Actual report-only mathematical comparison, explicitly selected accepted record | Four checks passed; `review-required`, approval `not-granted`; exit zero, closed group; 1.072 seconds | Lease `adc46fd8-bc97-4621-b4b7-ce3fa040a929`; `.local-data/option-b-next-transfers-final/report.json` |

The Node fourteen-path descriptors compare equal before/after and pass `shasum -a 256 -c`, independently read back by the reviewer. Their identical descriptor digest is `6ed25124214206703f49d98ba050d5b4b85c15fc40ec5db8e8623ed6e26d5f12`; the receipt is `690c9f69ff54c52200cdf6dab3016654b8d7e0454466ee3c24972cc61904f2e8`. The Python seven-path descriptor includes all three consumers, the reader, its controls and both external JSON records; known SHA and equality/inequality controls precede the post-run comparison, which matches every pre-run identity. These named generations attest the selected test/data files, not every transitive runtime dependency. The mathematical report uses the final known-answer metadata generation and leaves the accepted predecessor and scientific checks unchanged.

`check-current-source-dispositions.mjs` reports 157 original candidates, 119 live quoted-digest source files, 214 rows and three retirements. A known-case-controlled deep comparison against `git show HEAD` confirms all 157 original candidate rows unchanged. This is twelve fewer live source files than the previous 131-file census; it is not a universal hash detector or completion of full B takeover. The explicit scoped `git diff HEAD --name-only` readback for Development Process Review contracts and the current manifest/mathematical runner paths returns no changes.

### Catalogue continuation and required gate

The catalogue remains unedited after inspection of its actual consumers. It exposes synchronous immutable records, while `build-static-site.mjs` copies modules unchanged; browser JSON-import support has not been established. The analytical campaign benchmark also fingerprints the catalogue code. The next coordinated candidate should move all 145 ordered entries to authored JSON, preserve the synchronous API through a deterministic generated data-only ESM projection, migrate taxonomy inspection to canonical JSON, and include JSON plus projection in benchmark fingerprints. Initial generation needs the repository's explicit regeneration authority. Generated data may contain the preserved hashes; this is not a claim of zero hash literals across every executable build asset. The separate F5 budget transfer must resolve its scientific-contract and captured-loader obligations first.

The post-transfer required gate, lease `6c0f0dfb-8c88-43d7-8433-c73d4b195bb4`, reports 29 required passes, one required failure, one optional skip and zero unreached; exit one, closed group, 39.941 seconds. Its only failure is new drift in the two textbook TOCs, after the earlier strict check passed. Under the operator's explicit TOC regeneration request, `node scripts/build-scene-graph.mjs --write` updates exactly those two tracked outputs; the subsequent `--check --strict` passes with 1660 sections, zero errors and zero warnings. Reception-time notation and the equation registry pass in that failed full run. No causal editor attribution is inferred from concurrent corpus changes. Final required-gate reconciliation follows below.

Final required Content Integrity lease `1191d04c-a5d3-4caa-9f41-95c531239e20` passes all 30 required checks, with zero required/reporting failures, one optional broad sweep skipped and zero unreached; exit zero, no signal, empty stderr, closed group and 38.314 seconds. Its existing B admission step now passes 145 tests, including the new external-record controls. The final scoped `git diff --check` reports no whitespace errors in this task's test, gate, disposition, documentation and generated-TOC paths; the Node fourteen-file source descriptor still passes its SHA check after the first full integration run. Owner-aware compute closeout reports clear across 1001 leases. No test was disabled or deleted, and no Git publication was performed. These receipts establish the requested gate recovery and bounded twelve-consumer transfer, not full removal of source hashes, scientific approval or execution of the optional whole-test sweep. A changed expectation, lost assertion, fixture/source descriptor mismatch or failed selected gate would overturn the corresponding bounded acceptance.

## Authorized Borg catalogue migration

The operator selected the catalogue follow-up, including initial generation of its browser-compatible data module. At resumption, `git rev-parse HEAD` reports `72847589ba73d0bf81d07ca5b27d98072659cee9`, and `git --no-optional-locks status --short` returns no changed paths. This candidate owns catalogue data transport, taxonomy consumption, benchmark dependency coverage and setup/build integration. Borg budgets, F5 scientific contracts, historical result identities, other source families and publication are not selected.

The canonical source is `src/apps/borg/data/assembly-record-catalog.v2.json`. The generator produces `content/generated/borg/assembly-record-catalog.v2.js` as ignored runtime data, not a second authored catalogue. The machine-artifact retention policy requires reproducible runtime payloads to remain untracked. The runtime family is prepared before Borg records and is included by the existing Pages builder, preserving synchronous ordinary ESM imports without browser JSON-import extensions or asynchronous fetch. The wrapper and generator share a hash-free catalogue contract. Generator/setup bootstrap works without the output; runtime import requires the prepared projection and rejects its absence. The source-only reconstruction test now begins without the projection, verifies a missing-module rejection, then prepares the outputs and imports the published module graph to check exact ordered data and immutability. This is packaging and interface validation, not a browser-rendering or scientific acceptance claim.

The benchmark's `computeImplementationInventory` captures code/data fingerprints before and after each selected run. The transfer adds canonical JSON, generated projection, contract and generator to that inventory; existing comparison guards remain. An optional repository-root argument is a test seam with the same default production root and no new CLI override. Four original benchmark tests pass before edits. A known SHA control precedes `rg -l --hidden -F` searches of the original benchmark and test digests outside `.git`, `.local-data`, `.tmp` and `node_modules`; no matches appear under that scoped search. Independent pre-edit review confirms this inventory is operational freshness, separate from methodology, source-record and scientific applicability checks. Imports still precede inventory capture, so these checks do not establish race-free executed-generation admission.

### Catalogue transfer acceptance

✓ Done for this scoped catalogue transfer. A known-case-controlled extraction and independent deep comparison against the original `72847589ba73d0bf81d07ca5b27d98072659cee9` module preserve all 145 ordered entries and the exported API names. A separate double-quoted-64-hex counter passes its positive/negative control before measuring 145 literals in the retained original and zero in the current wrapper. The original catalogue digest is `936371611c8254d0e0639341cc96a15427ce5d4bfc988ebf4b9ec11d073f00be`; serialized catalogue equality binds `390aee5c49a5bfb84444736709472c682246308c0d05da4e8caffa93135d21fa`. The local transfer receipt is `.local-data/option-b-borg-catalog-before/transfer-receipt.json`. Its final pending-review phrase records an earlier stage; the independent review addendum supplies subsequent bounded acceptance without rewriting that receipt.

The wrapper contains no embedded catalogue array or fallback. Its shared contract retains immutable construction, exact identity-pair checks and duplicate/field validation; the data reader additionally rejects malformed schema, duplicate JSON keys and noncanonical record routes. The generator emits only serialized JSON in an ESM default export, rejects malformed UTF-8 and the tested file/directory substitutions, and avoids rewriting an already-current output. Known generator controls run before initial authorized repository generation. This protects trusted authored-data transport at the tested scope, not every hostile concurrent-filesystem race. The taxonomy scanner now iterates parsed canonical labels; its known positive/forbidden/malformed/missing-data controls precede real scanning. Generator, catalogue, benchmark and taxonomy controls now run in the existing required runtime-contract step.

| Instrument and scope | Measured terminal result | Retained evidence |
| --- | --- | --- |
| Four-file generator, catalogue, taxonomy and publication-surface controls | 32 passed, zero failed/skipped; 2062.187792 ms | Worker transfer receipt; main also reran the three-file generator/catalogue/taxonomy selection with 29 passes |
| Main benchmark/runtime-build integration controls | 19 passed, zero failed/skipped/cancelled; 619.780667 ms | Foreground tool output; required-gate selection includes these controls |
| Source-only reconstruction and complete isolated Pages build | One test passed; exit zero, closed group, empty stderr; 73.114 seconds | Lease `c86bd594-f711-4c58-98e3-76e367363baa` |
| Thirteen-file Borg consumer and unchanged analytical-identity regression | 122 passed, zero failed/skipped/cancelled; exit zero, closed group, empty stderr; 19.931 seconds | Lease `18364b69-ca45-4894-bb05-97cb1f808526`; exact selection recorded in its args |
| Complete required Content Integrity | 28 required passed, two required failed, one optional sweep skipped, zero unreached; exit one, closed group; 42.036 seconds | Lease `501632ca-898e-40c7-b32a-afd0dce11a15` |

The source-only test starts with every declared runtime output absent, generates the catalogue first, checks all five runtime families, verifies all 145 Borg records totaling 116875800 bytes, and confirms the copied sources remain unchanged. Its isolated site contains 147 public runtime outputs, 5626 files and 476706267 bytes. Importing the published catalogue module graph confirms exact canonical entries, frozen objects and no fetch during initialization. These are Node plain-ESM packaging and interface checks, not rendered-browser or scientific acceptance. The generated projection is current by `build-assembly-record-catalog.mjs --check` and ignored by `git check-ignore` at its exact path.

The full required gate is **not green**. Its only failures are freshness checks for `content/graph/textbook_toc.json`, `content/generated/markdown/textbook/toc.md` and `content/generated/equation-mapping/corpus-equations.json`; the first two belong to one scene-graph check. Corpus source changes are concurrently visible by scoped `git status`, but no causal editor attribution is inferred. This candidate does not edit those corpus files or widen catalogue-generation authority to tracked TOC regeneration. After that corpus state stabilizes, the repair commands are `node scripts/build-scene-graph.mjs --write` under regeneration authority and the permitted ignored-runtime `node scripts/build-equation-mapping-corpus.mjs --build`, followed by their checks and the complete required gate. This global freshness condition does not erase the independently verified catalogue transfer, build or consumer receipts.

Known SHA and deep-equality/inequality controls precede final preservation checks: all eighteen named candidate code/data/build/control files match their pre-gate snapshot, and all 157 original disposition rows match `git show HEAD`. The descriptor and terminal outcomes are retained in `.local-data/option-b-borg-catalog-before/integration-receipt.json`. `check-current-source-dispositions.mjs` reports 118 live quoted-digest source files, 219 disposition rows and three retirements; it still covers its declared source directories/extensions, not all generated assets or every encoding. Full B takeover remains Partial. Borg budgets and their F5 scientific-contract/captured-loader dependencies remain unedited. Owner-aware closeout reports clear across 1006 leases. No tests were removed or disabled, no historical/scientific expectations were refreshed and no Git publication was performed. A changed entry/order/export, missed data mutation, source-descriptor mismatch or failed selected consumer would falsify the corresponding bounded acceptance.

## Authorized Borg budget transfer and generated repair

The operator selected both follow-ups: coordinate the Borg budget identity transfer with F5 and circular release source contracts, and refresh generated corpus outputs before rerunning the complete required gate. The existing catalogue work remains in the shared working tree; this authorization does not discard it or publish Git changes. The transfer preserves both original allocation hashes, all allocation values, canonical strings, exported preset interfaces and general validation behavior. Independently authored budget ledgers, numerical references and historical declarations remain unchanged.

The selected design stores the original canonical allocation strings and their original hashes in authored `src/apps/borg/data/certified-budget-identities.v1.json`. A deterministic ignored JavaScript projection preserves synchronous browser imports and the existing F5 captured-module loader. The built-in preset selector compares its canonical allocations with the recorded applicability string; it does not calculate a replacement expectation from its own output. F5 must explicitly capture the authored JSON, projection, pure contract and generator as scientific-contract dependencies and reject a stale projection. The ordinary source inventory and planar circular freeze inventory must include the data and generator as well as imported JavaScript. A current successor map is a reviewed source selection, not scientific acceptance or permission to renew an expired campaign.

Pre-edit review with exact-digest `rg` across the checkout, excluding `.git`, `.local-data`, `.tmp` and `node_modules`, finds the original budget source bound by the current F5 evolution map and the August 27 F5 and September 1 Stella declarations. Both historical declarations retain their bytes and reject the migrated generation when their original source authentication applies. Shared admission-helper changes require a current build-map successor and its current test selection; they do not permit rewriting historical build receipts. The predecessor maps will be retained separately before replacement. The original-five operational transition policy does not grant scientific-contract refresh eligibility and remains unchanged.

Validation will compare the complete old and new public preset values using an original retained module, run unchanged crypto and ledger checks, exercise malformed/missing/mutated dependency controls, and reconstruct an isolated source-only deployment. Independent review must assess the source-role transition separately from transport parity. The generated repair is limited to `build-scene-graph.mjs --write` and `build-equation-mapping-corpus.mjs --build`, followed by their checks; the latter does not rewrite authored equation links. A failed exact-value comparison, omitted dependency, accepted stale projection, changed historical declaration or unsuccessful final gate leaves the corresponding obligation open.

### Budget transfer validation

The retained original module has SHA-256 `b7003c59d326cf2f2c83d82bd430638636b96c653f97b6b8ab13ccfad259044c`. Independent known-case-controlled comparison confirms the same eight export names, all non-function export values, both complete presets and exact source strings for every exported function. The combined value identity is `69da3a60ffd279951b73e013ce10276a58a942d1db46a090045d366a86b538a2`; both authored data rows retain the original canonical allocation strings and original hashes. The local baseline and final focused controls are `.local-data/option-b-borg-budget-before/baseline-parity.json` and `.local-data/option-b-borg-budget-before/final-controls.json`. The new strict contract rejects non-string hashes, duplicate keys, wrong preset identities and changed canonical applicability. A reviewer concern about final-newline regex matching was withdrawn after a direct Node control; the explicit length guard remains harmless hardening, not evidence of a proven prior defect.

The [F5 budget family transition](../contracts/option-b-f5-budget-transition.json) binds exact predecessor copies, current successor maps, enumerated graph changes and protected historical/scientific files. Its SHA-256 is `43a07536f148b9ed2e0414603abbbb216bba30e24ed66c6fae8c019752a065ec`. The new scientific-contract rows capture authored data, generator, pure contract and ignored projection; the selected generator verifies recorded applicability against its recorded hash, then F5 compares its deterministic rendering with the captured projection. Admission never regenerates a projection or calculates a new expectation. The ordinary and circular inventories include all four dependencies. Current build selection is `49401d8519c23147b6dd0926290e1be256e1fa97d00d7fe3a89d8e1e87dc2a45`; current evolution selection is `319701b260508909938fdb423aa001c179ab760a05c2303dd72fc190b5df5ea5`. Only the current `f5PreparationSelection` digest and acceptance reference changed in the retained-test fixture; a known-case-controlled comparison against commit `72847589ba73d0bf81d07ca5b27d98072659cee9` preserves every other field. This is source-family selection, not a renewed scientific declaration or a new C++ build receipt.

| Instrument and scope | Measured result | Retained lease |
| --- | --- | --- |
| Source-only reconstruction, all six runtime families and published catalogue/budget module graphs | One test passed, zero failures/skips/cancellations; 66.766 seconds; exit zero, closed group, empty stderr | `a1fc5d38-6934-4b9c-bcf5-453e6e8c057f` |
| Complete required Content Integrity gate | 30 required passed, zero failed, one optional broad sweep skipped, zero unreached; 45.061 seconds; exit zero, closed group, empty stderr | `625b5a50-834b-4bb5-ad2d-760c0c1728e3` |
| Seventeen-file connected F5, host-process and release-preparation regression, with `F5_RUN_PROCESS_CONTROLS=1` | 169 passed, zero failed/skipped/cancelled; 32.314 seconds; exit zero, closed group, empty stderr | `34923070-f8c9-4241-a507-ffc28fd4dc8e` |

Lease stdout/stderr logs live under `.local-data/owned-compute/logs/`; exact commands live in each lease's argument array. The source-only build preserves 145/145 Borg record identities totaling 116875800 bytes, deploys 148 runtime outputs in 5640 files totaling 477083900 bytes, and verifies exact published budget values, defaults, immutable objects and no initialization fetch. These are Node plain-ESM packaging checks, not browser rendering. All copied sources, 23 KaTeX files and all 386 repository images remain unchanged under that test. The main budget/request/Borg regression additionally passed 71 tests; final focused budget controls passed eight, and the full required gate reran the frozen budget and unchanged Borg checks.

Both authorized corpus generators and their subsequent checks pass. The command `rg --files -0 content/markdown content/scenes | xargs -0 shasum -a 256 | sort | shasum -a 256` returns `3dae64c63f0c2dfc07432f391942c34317a86ef9e541cd0147f2259dd3adde4e` before and after regeneration; the two generator source digests also remain unchanged. The equation registry reports 199 Markdown files, 4685 display equations, 23 promoted equations and 30444 symbol definitions. This resolves the two freshness failures in the preceding catalogue gate without editing authored corpus files or equation links.

The local integration receipt is `.local-data/option-b-borg-budget-integration/receipt.json`. Direct `shasum -a 256` checks preserve all 26 explicitly captured candidate paths across the gate and seven protected files across the transfer: the F5 and Stella historical declarations, budget ledger, historical-generation predicate, unchanged Borg regression, and F5 verification/dynamics instruments. A known-case-controlled deep comparison preserves all 157 original disposition rows. The bounded source scanner reports 117 remaining quoted-digest source files, 224 disposition rows and three retirements. The original-five operational eligibility checker is unchanged; no tests were deleted or disabled, no historical expected values were repinned, no scientific campaign was run and no Git publication was performed by this task. Owned-compute closeout reports clear across 1010 leases. Final independent source-family review is recorded separately below; full B takeover remains Partial.

### Budget transfer acceptance

✓ Done for both operator-selected follow-ups: the coordinated budget/F5 source transfer and generated-corpus repair. Read-only reviewer `01a096b8-6794-78d1-84c8-01c761a89219` accepts the exact transition record and frozen candidate with no remaining blocker. The [independent review addendum](../evidence/option-b-remaining-binding-dispositions.md#borg-budget-and-f5-transfer-independent-review) records known controls before graph comparison, exact original budget API preservation, all selected source digests, protected historical bindings and independently authenticated terminal validation receipts. The transition record's original pending-review status is preserved as earlier history; this later acceptance is confined to source/dependency transfer and measured operational integration. It does not renew historical declarations, grant generic scientific-contract refresh eligibility, approve mathematical results or authorize numerical production.

The completed budget and generated-repair subtasks are removed from executable work. Remaining source-hash removal keeps its parent active. The next recommended linked family is the F6c cached-root-cover pilot/full test bindings: exact-literal `rg` in those two test files still finds resource-plan, retained-result and comparison identities, while their operational launch profiles already have B maps. Transfer the test expectations into reviewed data without altering their values, resource obligations or historical applicability; first inventory every incoming binding before editing. A broad hash replacement or test disabling is not the continuation.

## F6c test expectation transfer

✓ Done for the F6c test-family implementation and connected verification; the separate whole-checkout gate failure remains recorded below. OptionB-Redux verified its dispatch source with `shasum -a 256` and `wc -c` before editing. The bounded incoming-digest search used each live test file's computed SHA-256 with `rg -l --hidden -F`, excluding `.git`, `.local-data`, `.tmp` and `node_modules`; only the existing disposition record referenced the current pilot-launcher and full-test identities. The pilot/full process files had no exact incoming-digest hits. Path searches across `scripts`, `tests`, contracts, workflows and hooks found test commands and historical reconstruction references. These searches do not establish absence of dynamic or alternatively encoded dependencies.

The [payload](../../../../tests/fixtures/option-b-f6c-test-identities.json) preserves 50 pilot and 25 full hash-bearing JavaScript string values, including escaped historical pin-table text. The [transfer proof](../evidence/option-b-f6c-test-transfer.json) records original Git identities, literal offsets and spellings, import additions and the separately explained assertion repair below. A known string/comment/escape case and published SHA-256 control passed before inventory; a known transformation/inverse case passed before target transformation. Both source files matched commit `5549583a1fa498a72e90aa1a170f26bec10dbd54` before editing, and inverse substitution reconstructed both exact original bodies before writes. No numerical expected result, resource limit, rejection predicate, scientific subject or independent reference was changed by string externalization.

The payload is governed by the existing JSON-LD [current-source manifest](../contracts/option-b-f6c-test-sources.jsonld), with stable Source and Relationship identities, `dependsOn` and `checks` edges, the existing context and fixed-byte contract. It is not an independently selected parallel store. The [family selection](../contracts/option-b-f6c-test-selection.json) selects an exact B checkpoint and zero-change transition through the existing `inspectCurrentSources` implementation. Every source is default-denied for refresh; the initial checkpoint grants no eligibility. The reader captures and rechecks the original file identities before returning frozen primitive values. The required [controls](../../../../tests/option-b-f6c-test-identities.test.mjs) exercise the actual loader and jsonld/N3/Comunica dependency traversal, including removed edges. An unused JSON-LD context or plain payload parsing would not satisfy these checks. Selection is an authored review boundary, not authentication of a human or scientific correctness.

| Obligation | State and enforcement |
| --- | --- |
| Original expected strings and historical construction | ✓ Done at mechanical-transfer scope: original Git source, exact string spellings and inverse source reconstruction are checked by the required transfer controls; retained source snapshots and old Git objects remain unchanged. |
| Resource limits, retained pilot-result identities and comparison applicability | ✓ Done for the 160-test connected regression: original assertions consume unchanged strings; the graph protects the payload as `scientific-control`, and selected successor attempts cannot refresh it. |
| Missing/wrong selection, stale/missing source, same-byte replacement and graph changes | ✓ Done for the focused controls: missing and wrong selections, stale or missing payload/consumer/reader, same-byte replacement, dependency removal, role substitution and protected expectation refresh reject after a passing known baseline. |
| Current versus historical outer-launcher expectation | ✓ Done for the focused repaired assertion: historical construction uses frozen source; current source binding comparison uses the already accepted five-profile checkpoint/transition. The original hash is preserved. |
| Process, preparation and captured execution | ✓ Done for the 160-test connected regression: both synthetic process files and all launch/preparation/worker/scientific implementations are unchanged. Their runtime obligations remain with the original five profiles; this test-only graph authorizes no launch. |
| Required coverage and full B closure | ◐ Partial: Content Integrity now requires the new graph/transfer controls, and its reporting test asserts their presence. Broader external-record graph conformance, remaining source families and independent semantic review remain open; this family does not close full B. |

### Historical and current comparison repair

The first connected eight-file run, owned lease `ef229b21-c317-4b0f-a855-be71b89123ea`, reports 160 tests, 159 passed, one failed, zero skips/cancellations, exit one and closed process group in 23.626 seconds. The failure is the unchanged historical-table-versus-current-source assertion: its expected outer launcher is the pre-B generation while the current source selects the accepted circular successor. An isolated execution of the original pilot test, changing only relative static-import addresses for scratch placement, reproduces that same assertion failure. This establishes the defect in the pre-transfer candidate, not the historical commit that introduced it.

The [circular integration record](option-b-circular-cutover.md#shared-integration-handoff) explains the separately reviewed outer-launcher replacement, and the externally selected accepted five-profile checkpoint contains that successor. The repair does not substitute a freshly computed subject hash. The historical entry is still authenticated against its original digest and exact construction delta; the old table's values remain present in that source. The current `SOURCE_BINDINGS` dictionary is compared with the authenticated B-selected source and retained-evidence dictionaries. All other original test code is recoverable exactly by reversing the explicitly recorded repair and string substitutions. The focused repaired assertion passes, as do the five new graph/transfer controls. A changed original value, lost rejection, unselected successor admitted, or historical source rewritten would falsify the respective preservation claim.

The existing source-only and browser builds are unaffected by this test-only payload: no browser/runtime projection, compiled source, preparation source or build contract was edited. This is a dependency-scope statement from the named path/digest searches and edited-file inventory, not a new rendered-browser or scientific validation result. The original five-profile selection and maps remain unchanged. No A implementation file is retired in this slice; only the superseded inline representations in the two live tests are removed, while their historical snapshots remain retained.

### F6c validation checkpoint

✓ Done — the corrected connected eight-file regression, owned lease `6573e70e-1009-483b-8ffd-d152d185164e`, passes all 160 tests with zero failures/skips/cancellations, exit zero, empty stderr and closed process group in 24.181 seconds. The exact command and terminal output are retained under `.local-data/owned-compute/logs/` with that run ID. The selection includes both cached launcher/admission suites, both synthetic process suites, new test-identity controls, original-five admission, B-to-B transition and gate-reporting tests. The initial failed run remains retained separately. Shared venv Python 3.13.2 was verified before this selection; its Python invocations exercise synthetic captured-program and runtime-inventory behavior, not new scientific production.

◐ Partial — the required Content Integrity run, lease `6be42c16-548c-49e5-a96b-8fc7b239d114`, reports 29 required passed, one required failed, one optional broad sweep skipped and zero unreached, exit one and closed process group in 60.386 seconds. Its failing content-reference check reports two missing targets in the separately owned `evidence/option-a-validation-rca/README.md`: `document-check.txt` and `document-coverage.md`. The RCA files are preserved for their active investigator. B admission/transition/disposition controls and all other required checks pass; this is not a whole-checkout pass. Ignored runtime preparation was performed by the required gate under its existing runtime-asset authority; no tracked-source regeneration was selected.

The post-transfer inventory checker reports 157 original discovery files, 115 live quoted-digest candidate files, 232 disposition rows and three retired rows. All 157 original disposition rows remain equal to HEAD by `assert.deepEqual` during the append-only additional-owner update. The eight new role records identify the graph, selection, checkpoint, transition, reader, payload, controls and transfer proof. The scan is inventory accounting only; neither 115 remaining files nor 75 transferred strings measures total A-to-B functional coverage. Scoped `git diff --check` passes. This completes implementation and connected verification of the F6c test family, with the global gate failure and independent semantic review boundary retained explicitly.

### External record conformance continuation

◐ Partial — source externalization and JSON-LD conformance remain distinct obligations. `rg` for the literal payload paths across repository `*.jsonld` files, together with direct reads of their real loaders, gives the following bounded disposition. The search excludes ignored files under normal `rg` rules and cannot rule out an encoded identifier, dynamic path or non-JSON-LD external binder. These are present integration findings, not conclusions about the earlier Option A incident.

| Existing payload family | Observed consumer and remaining graph obligation |
| --- | --- |
| `tests/fixtures/option-b-next-test-identities.json` | ✓ Done for this family: the [Node graph integration](#node-test-identity-graph-integration) adds explicit selected-graph admission to the existing reader, preserves the payload and nine consumers byte-for-byte, and passes all 227 connected tests. The earlier plain-JSON conformance gap is resolved. |
| `tests/fixtures/option-b-retained-test-identities.json` | ✓ Done for this family: the [retained identity integration](#retained-test-identity-graph-integration) replaces all thirteen direct imports with selected-graph admission, preserves original payload bytes and all remaining consumer source bytes, and passes all 134 connected tests. |
| `tests/fixtures/option-b-python-transport-identities.json` and known-answer payload | ✓ Done for the selected [Python and known-answer family](#python-and-known-answer-graph-integration): both payloads stay byte-exact, sixteen consuming paths use selected-graph admission, and final runs pass 141 Node and 36 Python tests. One existing fresh-build/current-context integration case remains explicitly deferred; its known-answer control ran. |
| Borg assembly catalogue | ◐ Partial: `build-assembly-record-catalog.mjs` derives an ignored ESM projection from authored data, checking source identity through generation; `BorgAssemblyRecordCatalog.js` consumes that projection through its contract. The literal `assembly-record-catalog.v2.json` path search finds no JSON-LD binding. Keep the public API and projection as payloads while adding their missing selected graph relationships and setup/build enforcement. |
| Borg budget data | ◐ Partial at full-B scope: the F5 evolution JSON-LD map explicitly binds the authored budget data, contract, generator and projection. That is actual graph coverage for the F5 family; it does not establish coverage for every browser, catalogue or other consumer. Preserve its completed F5 acceptance while checking remaining consumers. |

The Node test-identity reader/payload and retained direct-import families are complete below. The Python transport and known-answer admission slice is also complete below. The preferred next implementation slice is Borg catalogue graph admission and remaining budget-consumer coverage, preserving the accepted F5 transfer. Reuse the manifest/transition/capture/graph-query implementations rather than introducing another authoritative store. Preserve the Python known-answer and Borg build families as separately dependency-closed continuations. No previously retained A obligation is ended by the absence of a literal path hit, and no new scientific run is activated by this conformance audit.

### Latest gate reconciliation and moving corpus boundary

The two RCA link targets subsequently became available by `ls`. Required gate lease `151b79c0-3160-488c-80dd-e9fe7a496ac7` then passes content references and all B checks but reports 29 required passes, one required failure, one optional skip and zero unreached, exit one and closed group in 59.068 seconds. The remaining failure is the ignored `content/generated/equation-mapping/corpus-equations.json` freshness check. This supersedes the earlier missing-RCA-link condition as the latest gate failure; neither failed run is relabeled as a pass.

A direct `node scripts/build-equation-mapping-corpus.mjs --check` reproduces registry drift. The permitted ignored-runtime API `prepareRuntimeAssets({familyId:'equation-corpus',mode:'write'})`, followed by the same family with `mode:'check'`, passes. It uses the declared runtime family's generated-only `--build` command, not the authored-link-changing `--write` mode. Before/after tracked corpus inventories from `git ls-files -z content/markdown/aaa | xargs -0 shasum -a 256` differ under `cmp`; therefore no stable whole-corpus candidate is claimed across this preparation interval, and no causal editor attribution is assigned from that observation. The inventory files are retained under `.tmp/option-b-redux/` as disposable diagnostics; the conclusion and exact commands are preserved here.

The connected F6c result remains 160 passing tests, and a subsequent five-control run authenticates the selected family source bytes, original strings and graph behavior. The separate whole-checkout gate remains unresolved until corpus bytes and their runtime registry can be checked on one stable candidate. This does not undo the F6c transfer or authorize edits to the concurrent corpus or RCA. The next scoped implementation remains the Node test-identity reader/payload graph integration described above.

## Node test identity graph integration

✓ Done for the selected Node reader/payload graph integration: the full connected selection passes all 227 tests. The operator selected this continuation explicitly. Live inspection of `tests/support/option-b-next-test-identities.mjs` established that the previous public reader captured and validated plain JSON without graph selection. The existing [payload](../../../../tests/fixtures/option-b-next-test-identities.json), nine consumer source files and original shape/role validator are unchanged; their exact identities and Git recovery generation are retained in the [transfer record](../evidence/option-b-next-test-graph-transfer.json). The record preserves the prior expectation origins; it does not refresh values from scientific subjects.

Before editing, the published SHA-256 `abc` control passed, followed by exact-digest `rg -l --hidden -F` searches for the reader, payload, control suite and all nine consumer files, excluding `.git`, `.local-data`, `.tmp` and `node_modules`. No incoming current-digest hits were returned in that scope. Path/API searches across scripts, source, tests, contracts, workflows and hooks identified the public reader, the nine declared consumers and the existing required Content Integrity control suite. No production source, captured scientific implementation or existing launch map is edited. These searches remain bounded and do not prove absence of dynamically encoded dependencies.

The public `nextTestIdentities` API now requires the [external B selection](../contracts/option-b-next-test-selection.json). It keeps that module generation's selection and validates the exact checkpoint, zero-change transition, [JSON-LD manifest](../contracts/option-b-next-test-sources.jsonld), payload and declared sources through the existing `inspectCurrentSources` and `captureSet` implementations. It checks the requested consumer's graph membership and the payload's protected role, applies the unchanged shape/role/count validator, and rechecks original file identities before returning frozen primitive strings. There is no fallback to direct JSON loading when selection is missing or wrong. The pure shape parser remains available to its known-shape tests; the nine live consumer files still call the now-admitted public API.

The initial graph has 18 Source objects and 39 Relationship objects by `validate(document)`. Every declared consumer depends on the reader and payload, every source has admission coverage, and the graph includes the existing manifest/transition/impact modules, dependency package records and the independent closure checker used by the isolated consumer control. It uses the established context, stable path-based identities, whole-file selector, `dependsOn` and `checks` vocabulary. The checkpoint grants no operational refresh eligibility. The family record does not expand original-five launch authority or allow a changed scientific reference, fixture or contract to approve itself. This is protection for declared test dependencies, not a claim of complete scientific runtime dependency discovery.

| Obligation | Current result and enforcement |
| --- | --- |
| Exact prior expectations, role metadata and original consumer source | ✓ Done in focused controls: actual payload and every declared consumer compare byte-for-byte with the recorded Git generation; the original shape-validator function also compares exactly. |
| Closed schema, duplicates, malformed digests, roles and exact count | ✓ Done: original positive/negative controls remain; the validator implementation is unchanged. |
| Missing or wrong selection and malformed payload through the actual public reader | ✓ Done: the isolated unchanged closure consumer passes first, then missing selection, wrong selected digest and malformed payload reject before its first control runs; restoring the selected files passes again. |
| Stale, missing, symlinked or same-byte-replaced dependencies | ✓ Done in focused controls: reader, consumer and payload failures reject; a same-byte replacement during final checking also rejects, with previously returned primitive values preserved. |
| Dependency removal, consumer omission, role substitution and protected refresh | ✓ Done in focused controls: changed candidate graphs with separately selected transition bytes cannot override the unchanged predecessor's coverage or protected roles. |
| Standard JSON-LD/RDF dependency queries | ✓ Done: jsonld/N3/Comunica propagate payload changes to every declared consumer, using the union of old and new edges so removal remains visible. |
| Complete consumer regression | ✓ Done: all 227 tests in the thirteen-file selection pass, including all nine declared consumers and connected current-closure, B transition and gate-reporting controls. |
| Required gate wiring | ✓ Done for existing inclusion: `tests/content-integrity-reporting.test.js` confirms that the extended reader controls remain required. No new duplicate gate or replacement check list is introduced. |

The first two original known-shape tests passed before initial graph construction. A hand-stated two-source graph and exact `checks`/`dependsOn` census passed before the graph authoring helper read target paths. The eight expanded reader/graph controls pass; together with the current gate-reporting suite, the focused command reports 18 passed, zero failures/skips/cancellations. Shared-venv Python 3.13.2 is executable and `memory_pressure` reports 48% free before the connected regression. Those are launch-time observations, not a resource guarantee. A changed original expected value, a selected-graph bypass in a real consumer, omitted dependency accepted under the unchanged checkpoint, or lost rejection falsifies the corresponding bounded claim.

The graph/checkpoint/transition/selection and transfer proof extend the existing disposition owner by five records. The prior reader/payload/control rows retain their original roles and gain the current graph obligation; all original candidate rows remain unchanged by pre/post comparison during the update. No implementation file is deleted. The superseded route is the reader's unauthenticated direct-payload capture, recoverable through the recorded Git generation. Direct-import retained identities, Python fixtures and Borg catalogue coverage remain separate next families. Full B is still open, and this scoped selection does not reconcile the separate whole-corpus gate.

### Node graph validation and disposition

Owned run `f0cacf0c-5498-465f-b928-0547298cbad4` completes the thirteen-file connected selection with 227 passed, zero failures/skips/cancellations, exit zero, empty stderr, no signal and closed process group in 234.005 seconds. Its exact command and terminal stdout/stderr are retained under `.local-data/owned-compute/logs/` with that run ID. The selection includes all nine payload consumers, the expanded reader controls, current bounded-operation closure, B-to-B transition and current required-gate reporting. It includes the streamed-leaf and coordinator-process suites; neither was omitted for resource reasons. No new scientific production or raised resource policy was selected.

The final inventory checker reports 157 original discovery files, 115 live quoted-digest candidates, 237 disposition rows and three retirements. The literal count is unchanged because this slice adds graph governance to existing external data rather than removing more inline hashes. Scoped `git diff --check` passes. Exact-source preservation checks confirm the payload, nine consumer files and original validator body against the recorded Git generation; scoped `git diff HEAD --name-only` also reports no changes to those payload/consumer paths or the EOM/equation-mapping implementation directories. No existing launch map or scientific input is refreshed. The full-corpus gate is not rerun by this selected reader task; the preceding whole-checkout results retain their separate scope.

This completed child is removed from executable migration work while full B remains Partial. The next dependency-ordered family is the direct-import retained-test identity record, followed by Python and Borg consumers under their respective current bindings and runtime obligations. The resumption skill kept this boundary explicit, and managed computation covered the complete connected selection through terminal process closure.

## Retained test identity graph integration

✓ Done for this family — owned lease `9e8a83cb-5ab2-435a-8ace-c59a9bfe1915` passes all 134 tests in the sixteen-file connected selection, with zero failures/skips/cancellations, exit zero, empty stderr and closed process group in 106.830 seconds. The operator selected the direct-import retained-test family. Literal-path `rg` across tests, scripts, source, workflows and hooks identified thirteen importing test consumers. The [payload](../../../../tests/fixtures/option-b-retained-test-identities.json) contains vendor runtime/license/package checks, a prescribed-seed expectation, an existing accepted F5 transport selection, historical instrument and input identities, fixed output expectations and synthetic fixture identities. These roles remain exactly as authored. Their protected graph role means that the payload cannot refresh itself; it does not reclassify a vendor check as scientific evidence.

The [reader](../../../../tests/support/option-b-retained-test-identities.mjs) replaces direct JSON imports with explicit selected-graph admission through the existing manifest, transition and capture implementations. It captures this module generation's external selection, authenticates the accepted checkpoint, transition, selected map, payload and declared consumer, validates closed payload shapes and digest encodings, rechecks captured file identities, and returns a deeply frozen object. All original consumer assertions remain in place. There is no direct-import fallback or eligibility to refresh a protected expectation from its subject.

Before edits, the standard SHA-256 `abc` case passed; exact incoming-digest `rg -l --hidden -F` searches then covered the payload, thirteen consumers and two gate files, excluding `.git`, `.local-data`, `.tmp` and `node_modules`. Only the payload had a hit, in the existing disposition Markdown; its bytes are unchanged. The [transfer record](../evidence/option-b-retained-test-graph-transfer.json) retains the original identities and Git generation. This is a bounded literal search, not proof of exhaustive dynamic dependency discovery. The inverse-import control first passes a hand-authored source example, then reconstructs every original consumer byte-for-byte against retained Git. The payload also compares exactly against that generation. No scientific subject, independent reference, original expectation, historical snapshot or accepted launch map is changed.

The initial [graph](../contracts/option-b-retained-test-sources.jsonld) has 22 sources and 51 relationships by `validate(document)`, after a hand-stated two-source graph passes before candidate construction. Every consumer depends on the payload and reader; the graph also binds the common manifest/transition/query modules, package records, control suite and Mermaid runtime used by the isolated real-consumer test. This scopes expectation-loading admission; it does not claim complete graph discovery of every scientific or browser dependency. The checkpoint grants no operational refresh eligibility.

Focused `node --test --test-concurrency=1 tests/option-b-retained-test-identities.test.mjs tests/content-integrity-reporting.test.js` reports 17 passes, zero failures/skips/cancellations. Controls preserve schema rejection, duplicate-key rejection and deep immutability; an actual Mermaid configuration test passes before missing/wrong selection or altered payload rejects before the control runs. Separate copied-tree controls reject missing, stale, symlinked and same-byte-replaced dependencies, consumer omission, role substitution, dependency removal and protected refresh. Standard JSON-LD/RDF queries propagate payload changes and removed edges to all thirteen declared consumers. The isolated consumer control selects one actual runtime test; the connected regression separately runs the entire vendor/runtime suite and every other consumer without a test-name filter. Content Integrity now requires the retained-reader controls through its existing admission selection.

A changed original expectation, a consumer assertion change outside the import replacement, acceptance of a changed protected role or payload under the unchanged checkpoint, or a missing selected dependency that reaches a consumer control would falsify the corresponding claim. Seven disposition records identify the reader, tests, map, checkpoint, transition, selection and proof; the existing payload gains the graph role while original candidate rows remain unchanged by `assert.deepEqual`. Full B remains open, with Python and Borg conformance still separate continuations. The whole-corpus gate retains its earlier unresolved stable-candidate boundary.

### Retained identity validation and disposition

The complete connected run includes all thirteen declared consumers, the seven retained-reader controls, B-to-B transition controls and current gate-reporting controls. The exact command and terminal logs remain under `.local-data/owned-compute/` with run ID `9e8a83cb-5ab2-435a-8ace-c59a9bfe1915`. The shared venv executes Python 3.13.2 and `memory_pressure` reports 51% free at launch. No compiled target is rebuilt because this selection exercises JavaScript and the existing Python reference path, with no compiled implementation edits. The isolated actual-consumer test and whole-consumer regression retain their distinct scopes.

`node scripts/equation-mapping/check-current-source-dispositions.mjs` reports 157 original discovery files, 115 live quoted-digest candidates, 244 disposition rows and three retirements. No A source file is deleted. This slice governs already external identities, so an unchanged quoted-digest count does not indicate absence of progress and cannot measure full-B coverage. `git diff --check` passes. The completed retained-test child is removed from executable work; Python transport/known-answer records are next, followed by Borg under its build dependencies. The full-corpus gate was not rerun in this scoped task and remains separately unresolved on a stable candidate.

## Python and known-answer graph integration

✓ Done for the selected fixture-admission family — final connected runs pass 141 Node tests and 36 Python tests, with no failures. One existing current-context integration test remains explicitly deferred to a separately selected fresh circular build receipt; its known-answer control ran. The operator selected Python transport and known-answer graph admission. Literal-path `rg` across `tests/` and `scripts/` identified sixteen consuming paths migrated here, including Node dispatch tests absent from the authored known-answer consumer list and the mathematical dependency reader. The original consumer-list text remains historical transfer provenance; the selected graph and current proof provide the live census. Both the [published known-answer payload](../../../../scripts/equation-mapping/fixtures/known-hash-answers.json) and [historical acceleration payload](../../../../tests/fixtures/option-b-python-transport-identities.json) remain byte-exact against the [recorded Git generation](../evidence/option-b-controlled-fixture-transfer.json). No scientific subject, independent equation, prior expectation or applicability claim changes.

The [common reader](../../../../scripts/equation-mapping/controlled-fixture-records.mjs) uses the existing manifest/transition/capture engine. Node consumers capture a deeply frozen record after explicit checkpoint/transition selection. The [Python reader](../../../../tests/option_b_fixture_records.py) passes its captured selection and named consumer to that same engine through a bounded synchronous Node subprocess, then validates and returns the captured stdout bytes as the original string or tuple shape. It never reopens the fixture after admission and has no unauthenticated fallback. Its previous schema, duplicate-key, role, encoding and tuple checks remain as pure parsing functions. This avoids a separate Python acceptance implementation while preserving the language-specific rejection boundary.

The initial [selected graph](../contracts/option-b-controlled-fixture-sources.jsonld) contains 30 sources and 80 relationships by `validate(document)`. It binds both payloads, the Node admission module, Python reader, sixteen consumers, controls, shared engines and package records. Explicit edges connect the Python consumers to their Python reader and both fixtures, and connect the mathematical comparison runner and copied test trees to the common admission path. These are declared fixture-loading dependencies, not exhaustive discovery of every scientific implementation dependency. The mathematical comparison runner admits the supplied tree's fixture closure, checks that it matches the executing closure, records those inputs and rechecks their identities with the trial. Its existing scientific and prose checks remain unchanged. Isolated mathematical tests copy the full selected fixture closure so their actual CLI continues to exercise final composition.

The [source-role continuation contract](../contracts/option-b-current-source-transition.md#source-role-preservation-through-remaining-families) carries the accepted prevention review through this and later families. This family has an empty predecessor eligibility list: no source, including the operational-looking comparison runner, may refresh itself. A protected graph label on the known-answer or historical tuple payload safeguards its bytes without changing the record's original role or making it scientific evidence.

| Original obligation | B representation and selection | Actual enforcing consumer | Preservation and rejection evidence |
| --- | --- | --- | --- |
| Independent published SHA-256 `abc` expected answer | Unchanged known-answer payload under the externally selected family checkpoint and zero-change transition | Migrated Node tests, dependency-map preflight and three Python test modules through the common reader | Exact original Git bytes; inverse import-only reconstruction for fifteen consuming sources; existing known-answer comparisons remain in place; actual Node consumer and Python module imports reject missing or altered records/selections before their controls or scientific subjects |
| Original acceleration verifier/declaration remain positive historical controls; different current verifier remains negative | Unchanged acceleration tuple payload with original role, origin and applicability, protected under the same selection | `test_f6c_acceleration_execution.py` through `acceleration_prior` | Exact payload preservation, hand-authored tuple/census/encoding controls, complete acceleration execution tests and changed/missing prior rejection through the Python bridge |
| Language-specific shape and immutable capture | Existing Python validations now parse admitted bytes; Node returns a deep-frozen record | Python `known_sha256` and `acceleration_prior`; named Node consumers | Original schema/duplicate/role/encoding/malformed-value controls remain; synthetic parsing tests are distinguished from actual admission tests |
| Final selected fixture composition | Selected sources and explicit Python-reader, Node-reader and mathematical-runner relationships | Common reader's final `captureSet.check`; comparison runner's executing-versus-observed input capture and final recheck | Missing/stale/symlinked and same-byte replacement cases reject; complete mathematical acceptance and actual CLI controls retain their original assertions |
| Successor permission belongs to the predecessor | Exact selected checkpoint with no path/role grants | `inspectCurrentSources` through both language routes | Fully revisioned graph-valid payload successor rejects as protected; graph-valid comparison-runner successor rejects specifically for missing eligibility; removed consumers/edges and role substitutions reject |
| Required coverage stays visible | Existing Content Integrity admission selection and explicit local/GitHub profile split | Required reader controls and reporting tests | Node controls remain portable; the Python actual-consumer control joins the two existing shared-venv tests assigned to mandatory local validation, bringing that named local-only census to three; no implicit environment-dependent skip is introduced |

Before target captures, the published `abc` known case passed. Incoming exact-digest `rg -l --hidden -F` searches covered the identified files, excluding `.git`, `.local-data`, `.tmp` and `node_modules`; hits are retained in the transfer proof and were historical diagnostic/Markdown records, not permission to relabel their identities. Before initial graph authoring, a hand-stated two-source graph passed its exact source and relationship census. The first graph-impact control correctly rejected an incorrectly reused edge revision; its synthetic candidate was corrected to use fresh endpoint/edge revisions, preserving the original engine behavior. No expected answer was changed to obtain a pass.

A missing admitted dependency reaching a consumer, an altered original value or assertion surviving the preservation check, a protected or ungranted successor accepted under unchanged predecessor authority, or omitted fixture input in the mathematical comparison record would falsify the corresponding bounded claim. Historical A evidence and scientific acceptance remain separate. No A implementation file is deleted: direct fixture loading is replaced only in the named current consumers, while pure parser APIs retain their explicit shape-test purpose.

### Python and known-answer validation and disposition

Final owned Node lease `b7a98b67-f3af-462c-ae36-6e87b1fea50d` reports 142 tests, 141 passes, zero failures/cancellations and one explicit skip, exit zero, empty stderr and closed process group in 29.966 seconds. Its seventeen-file command covers all migrated Node test consumers, the mathematical reader through its preflight/CLI suites, the seven new fixture controls, current reporting and earlier next/retained-family controls. Final shared-venv lease `73beb024-87ee-4330-896d-dc35bb04c9db` reports all 36 tests passing across the parser and three Python consumers, exit zero and closed process group in 3.051 seconds. Exact commands and terminal stdout/stderr are retained under `.local-data/owned-compute/` with those run IDs. Python unittest writes its successful report to stderr; that stream is not itself a failure. The launch-time shared venv is Python 3.13.2 and `memory_pressure` reports 54% free.

The Node skip is `recorded current build and original proof enter phase context; substituted inputs reject` in `tests/subfield-circular-current-context.test.js`: it requires `AAA_CIRCULAR_CURRENT_CONTEXT` and an explicitly selected fresh `AAA_SUBFIELD_CIRCULAR_BUILD_RECEIPT`. This task does not manufacture that selection, claim that integration case ran or renew its scientific work. The same file's SHA control executes through the admitted record. The local/GitHub split is separately explicit: the new actual-Python-consumer admission control is mandatory locally and excluded by its exact name only in the GitHub profile, alongside the two pre-existing shared-Mac-venv controls. Portable Node controls are retained in both profiles.

`node scripts/equation-mapping/check-current-source-dispositions.mjs` reports 157 original discovery files, 115 live quoted-digest candidates, 252 disposition rows and three retirements. Eight integration owners were appended and existing payload/reader roles extended; original candidate rows remain equal by pre/post `assert.deepEqual`. `git diff --check` passes. Scoped `git diff --name-only` reports no edits to either payload, shared manifest/transition/impact engines, `scripts/eom/` or `src/eom/`; exact payload preservation is also independently checked against retained Git by the connected tests. The quoted-digest count is inventory accounting, not a migration percentage or functional coverage measure.

The completed Python/known-answer child is removed from executable work. Borg catalogue graph admission and remaining budget consumers are next, preserving completed F5 coverage. The family mapping and source-role/default-denial requirements remain attached to the existing reconciliation, contract, disposition and queue owners for later migrations. Full B and stable whole-corpus validation remain open; this scoped task did not rerun the full content gate or modify the separately owned publication receipt implementation.

The explicit portable-profile check runs the six Node-only fixture controls with `AAA_VENV=/unavailable` and the exact shared-venv test-name exclusion; all six pass. This verifies that those controls do not execute Python, not that a Linux run occurred. Final owner-aware supervisor closeout reports `clear`; no owned job remains active or uncertain.
