# SH-0-Sea Retained-Branch First Packet

Status: priority first-packet, 2026-07-04.

Proof ID: `SH-0-sea`, the `SH-0` rest shell-braid fixture embedded in a surrounding Noether sea of like assemblies.

Claim level: no accepted retained-branch evidence yet. This packet defines the first branch-record schema for the embedded test and records the current accepted-object boundary. It does not promote fixture, proxy, generated, diagnostic, or priority-prose evidence into proof evidence.

## Evidence Decision

`SH-0-sea` does not currently have receiver-normal-clean retained-branch evidence in the repo.

The inspected `SH-0-sea` mentions are taxonomy and proof-map declarations only: they define the environment-qualified proof target and say the embedded round must inherit the `SH-0` target identity, then add like-assembly population, boundary-condition, and sea-response rows. No inspected script, test, report, or priority packet supplies an accepted embedded retained row.

The closest reusable Noether sea object is `scripts/spacetime/noether-sea-density-compression-provider.v1.json`, which current checkers accept as a `theta_sea_rho_NS` density-compression provider for its own retained-window domain. It is not an `SH-0-sea` proof object because it is not tied to the `SH-0` target/source record, local boundary event row, action/exchange row, or receiver-normal branch-weight row.

The closest `SH-0` branch object is still blocked before retention. The active provider-backed seed-path route is sharper than a fresh/default rerun, but it still fails at:

- missing object: `held_release_seed_path_rows_acceptance_certificate`
- missing field: `held_release_seed_path_rows.acceptance_certificate_ref`
- candidate artifact: `held_release_seed_path_rows:5833f18e53586201`
- retained record: `retained-record:held-release-six-point:adapter-acceptance-certificate`
- source row: `two-speed-preferred-row:u0.8:v0.2`

Modeling-start clarification: `SH-0-sea` can restart as diagnostic/candidate modeling from the `SH-0` target identity without waiting for isolated `SH-0` retained closure. The model may add like-assembly Noether sea rows, boundary-condition rows, and sea-response equations around the central held-release target. Those outputs remain diagnostic/candidate until the accepted-evidence boundary closes.

Therefore the first accepted producer needed for `SH-0-sea` retained-evidence claims is still the `SH-0` seed-path acceptance certificate. A Noether sea response row cannot count as retained-branch evidence until it binds to an accepted central target/source record.

## Branch Record Schema

An `SH-0-sea` retained-branch row must close all rows below on the same accepted record.

| Row | Required content | Current status |
| --- | --- | --- |
| Target shell source row | The inherited `SH-0` one-band shell/common-sphere target identity, including retained record id, source row id, six seed-path rows, path-history streams, retained-history row, provider object, and provider provenance. | Available as a diagnostic/candidate modeling target through `held_release_seed_path_rows:5833f18e53586201`; not accepted. Active provider-backed route is blocked at `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`. |
| Sea population row | Like Noether braid assemblies surrounding the target, with declared inventory/population density, local density provider, orientation or frame assumptions, and response source. | A reusable accepted `theta_sea_rho_NS` provider exists for density-compression checking, but no accepted row binds it to `SH-0-sea`. |
| Local target-sea frame row | Rest target and rest local sea frame, with the local target identity and the surrounding Noether sea evaluated in the same frame unless a packet declares a different frame. | Defined by the proof-map target only; no accepted local frame row exists. |
| Sea-response row | A retained Noether sea pressure/tension/relaxation response row on the same branch record, using `theta_sea_rho_NS` or its current equivalent and binding the sea population to target support dynamics. It must not be a fitted generic confinement term. | Missing. Current adjacent route names the blocker as `retained_noether_sea_pressure_response_row` / `theta_sea_rho_NS`. |
| Support/envelope row | The target one-band support or common-shell envelope, reduced-radius or support-residual row, and any post-turn return condition needed to show the environment supplies stability missing from the isolated `SH-0` round. | Missing accepted support/envelope evidence for `SH-0-sea`; isolated held-release rows remain diagnostic only. |
| Event row | Boundary wake or event-ledger row entering the local target-sea region, tied to the same target/source record, local sea population, and response row. | Missing as accepted `SH-0-sea` evidence. |
| Action/energy row | Same-record target energy/action closure and sea exchange ledger, including retained wake history where used. | Missing. The retained-source adapter contract still has no accepted same-record action closure or retained wake-history ref. |
| Receiver-normal branch-weight row | Same-record retained root-ledger detail rows carrying current receiver-normal accounting. At minimum the braid-ideal row must carry finite `branchWeight`, `sourceNormalDenominator`, and `receiverNormalFactor`; broader current-equivalent rows may also carry `receiverNormalNumerator` and `unsignedReceiverNormalFactor`. Source-normal-only, Jacobian-only, and `eta^-2 |J|^-1` weights remain diagnostic only. | Missing as accepted evidence. Candidate row schemas exist, but no accepted `SH-0-sea` row carries these fields on the same retained record. |

## Search And Probe Log

Searches for exact `SH-0-sea` usage found taxonomy and proof-map declarations, but no accepted script, test, report, or priority evidence packet for an embedded retained branch.

The current-tree `SH-0` receiver-normal restart audit is consistent with this packet: it finds no accepted same-record `branchWeight` / receiver-normal rows for the isolated held-release fixture and keeps the provider-backed seed-path certificate as the first accepted-source object. That audit remains priority evidence-boundary context, not accepted proof evidence.

With active priority audit prose excluded, exact searches for the required accepted seed-path prefix and matching external authority package returned no repo hits. The provider-backed artifact id appears in owner tests, not as an accepted certificate artifact.

The retained-source probes establish the first blocker:

```bash
node scripts/braid-ideal/held-release-seed-path-rows.mjs --retained-record-id=retained-record:held-release-six-point:adapter-acceptance-certificate --source-row-id=two-speed-preferred-row:u0.8:v0.2 --provider-object-ref=candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327 --provider-artifact-hash=7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df --print-acceptance-certificate-requirement --pretty
node scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs --retained-record-id=retained-record:held-release-six-point:adapter-acceptance-certificate --source-row-id=two-speed-preferred-row:u0.8:v0.2 --print-same-record-accepted-evidence-contract --pretty
```

The seed-path requirement reports `accepted=false`, `requirement_passed=false`, and `status=seed_path_acceptance_certificate_missing` for `held_release_seed_path_rows:5833f18e53586201`.

The adapter accepted-evidence contract reports ten observed source fields and zero accepted package fields. The adapter package itself is therefore missing, and its first upstream accepted field is the same seed-path acceptance-certificate ref.

The provider-backed source carrier binds the expected six path-history candidates and 36 root/replay candidate refs for the retained record and source row, but it remains a candidate carrier. Its authorization block keeps `receiver_normal_branch_strength=false`, so it defines the receiver-normal package shape without supplying accepted branch strength.

Adjacent Noether sea probes do not close `SH-0-sea`:

```bash
node scripts/nuclear-atomic/noether-sea-response-source-target-check.mjs --require-accepted --pretty
node scripts/spacetime/noether-sea-density-compression-provider-intake.mjs --require-accepted --pretty
node scripts/proof-programs/noether-sea-compatibility-handoff-diagnostic.mjs --pretty
node scripts/braid-ideal/wiggle-return-response-target.mjs --pretty
```

The density provider and response-source target pass for their own domains. The compatibility handoff remains priority-only and reports no accepted medium-response closure. The wiggle-return target keeps the Noether sea route blocked at `retained_noether_sea_pressure_response_row` / `theta_sea_rho_NS`, after the retained-history source chain.

Targeted owner tests passed for the seed-path row ladder, retained-source adapter acceptance contract, central retained-history row, and retained-history provider/source carrier:

```bash
node --test tests/braid-ideal-held-release-seed-path-rows.test.js
node --test tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js
node --test tests/braid-ideal-central-solver-retained-history-row.test.js
node --test tests/braid-ideal-central-solver-retained-history-provider-source-carrier.test.js
```

Targeted Noether sea tests also passed, preserving the distinction between a reusable accepted provider and missing `SH-0-sea` branch proof:

```bash
node --test tests/noether-sea-density-compression-provider-intake.test.js
node --test tests/noether-sea-response-source-target-check.test.js
node --test tests/noether-sea-compatibility-handoff-diagnostic.test.js
node --test tests/braid-ideal-wiggle-return-response-target.test.js
```

Validation note: `node scripts/check-receiver-normal-clean-slate.mjs` passed. `node scripts/validate-content.mjs --check --strict` reported generated index drift in `content/scenes/scenes_index.json` and `content/markdown/markdown_index.json` for existing Noether-braid authored pages; no generator `--write` command was run in this packet.

## First Missing Accepted Object

The first missing accepted object for the whole `SH-0-sea` path is:

`held_release_seed_path_rows_acceptance_certificate.v0`

It must certify the provider-backed artifact:

`held_release_seed_path_rows:5833f18e53586201`

using the required accepted-ref prefix:

`accepted:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2:`

The matching external authority package is the next object after that:

`held_release_seed_path_rows_external_accepted_authority_package.v0`

at:

`external-authority-package:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2`

Only after those close can the retained-source adapter package accept the path-history manifest set, central retained-history row, central provider object, native path-history streams, native root-ledger detail rows, causal-root replay rows, same-record action closure, retained wake history, and provider provenance.

## Next Executable Proof Target

First modeling target: define the `SH-0-sea` surrounding like-braid Noether sea population, local target-sea frame, boundary condition, sea-response equation, support/envelope variables, action/exchange variables, and receiver-normal evidence requirements around the candidate `SH-0` target identity. This work can restart now, but it remains diagnostic/candidate.

First accepted-evidence target: produce or acquire the non-circular `held_release_seed_path_rows_acceptance_certificate.v0` for `held_release_seed_path_rows:5833f18e53586201`, then clear `held_release_seed_path_rows.acceptance_certificate_ref` for the provider-backed `SH-0` target/source record.

First `SH-0-sea`-specific accepted-evidence target after that: produce an accepted retained Noether sea pressure-response row that binds the same accepted target/source record to the like-assembly sea population, local target-sea frame, boundary event row, action/exchange row, support/envelope row, and receiver-normal branch-weight row. The proof target should show that the environment response supplies the missing post-turn retention or stable-radius condition without changing the base `SH-0` shell classification.

## Promotion Classification

- Corpus promotion: defer with blocker.
- Priority status: keep this packet as the first `SH-0-sea` evidence-boundary packet.
- Modeling status: unblocked as diagnostic/candidate model construction from the inherited `SH-0` target identity.
- Current accepted-evidence blocker: accepted seed-path authority for the inherited `SH-0` target/source record.
- Safe promotion condition: after an accepted `SH-0` seed-path authority and retained-source adapter package exist, update this packet to test the first same-record `SH-0-sea` sea-response row rather than relying on density-provider or diagnostic evidence alone.
