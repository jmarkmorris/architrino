# Explored Braid Geometries Consolidation Plan

Status: `phase-2-executed-2026-07-09` (operator direction: move the corpus toward the spindle braid as the primary family). Owner lane: corpus architecture (sub-plan of queue item `corpus_architecture_review_pure_aaa_plus_mapping_textbooks`).

## Executed — Phase 1 (2026-07-09)

- `content/markdown/aaa/noether-braid/spindle-braid.md` created and expanded into the full leading-candidate scene (definition, harmonic-matching principle, closure ladder, rest-state champion and stiffness spectrum, field-speed pin/escapement with derived $\omega_0/\gamma$ dilation, motion/inertia/isotropy results, discrete-symmetry structure, confinement problem), promoted from the braid-ideal proofing record at stated claim levels; corpus prose self-contained, no priority links.
- `symmetric-shell-braid.md` + `nested-shell-braid.md` consolidated near-verbatim into `braid-families.md` (comparison-population framing, re-entry conditions); originals deleted; externally consumed slugs preserved; corpus-wide links retargeted; `noether-braid.md` family map and reading order updated to spindle-first.

## Executed — Phase 2 (2026-07-09)

`nested-shell-braid-dynamics.md` dissolved three ways and deleted:

- Family-general machinery → `braid-mathematics.md`: Substrate and Effective Levels, Speed Hierarchy + Transverse Causal Budget Lemma (slug preserved for its 3 consumers), Spiral-Helical Motion Picture, Mass Thesis, Hinge Equation Sketch, Acceleration-Gradient Branch Comparison — lightly reworded family-generally, Document Role updated.
- Strong-field endpoint → new `content/markdown/aaa/proof-programs/terminal-alignment.md`: Braid Symmetry-Breaking Point, Local Black-Hole Duality Target, the full Terminal Alignment Label-Count block, and the Regime Map. Anchors preserved (`#terminal-alignment-label-count-target` for `entropy.md`; the common-center stationarity obstruction lemma promoted from bold paragraph to a real heading so `master-equation.md`'s anchor resolves). Preamble declares the nested-realization provenance and the open re-hosting obligation onto spindle terminal behavior.
- Nested-specific mechanism scaffold (certificate target, layer locking, planar chart, translating response + cycle stretch + oblation, retuning closure, export boundary, roadmap/hypotheses, geometry focus, minimal models, diagnostics, routed extensions) → `braid-families.md` under the `### Nested Shell Braid Dynamics` umbrella (slug `#nested-shell-braid-dynamics` hosts all retargeted plain links; `#cadence-scale-retuning-closure` preserved for `noether-sea.md` and the retuning toy model).

`nested-shell-braid-geometry.md` re-scoped and renamed to `braid-envelope-geometry.md` (family-general): preamble and Document Role restated for any braid assembly; new `## Envelope Forms` section hosting the spindle's fusiform envelope alongside the nested family's oblate spheroidal form (kept as its own subsection); canonical variables $(\xi,\lambda)$ declared envelope-general; retuning projection marked as the nested instantiation with the spindle projection named as an open obligation. All heavy anchors preserved (`#assembly-noether-sea-interface-diagnostic` ×7, `#canonical-geometry-variables` ×4, `#dynamic-exclusion-envelope`); corpus-wide rename retarget applied (incl. entourage canon path mentions).

Tables and reading order in `noether-braid.md` and the taxonomy updated (Braid Envelope Geometry and Terminal Alignment rows). Fragment audit across all five destinations: clean. `validate-content --check`: zero source-level errors; remaining errors are generated reading copies + index drift only.

## Executed — Phase 3, Terminology Generalization (2026-07-10)

- Corpus-wide survey: ~673 "nested shell braid" mentions, ~470 outside the braid scene across ~80 files; downstream prose was nested-first throughout.
- Stage 1 executed across ~45 assembly-level files (spacetime, dynamics, quantum, foundations, reactions, nuclear-atomic, cosmology, philosophy-history bridges, validation): 106 mentions generalized to `Noether braid`/`braid assembly` where the claim is assembly-level; ~90 mentions deliberately kept where the claim depends on nested structure; stale link titles aligned to the renamed chapters (`Braid Envelope Geometry`, `Terminal Alignment`). Outside-scene count now 358, dominated by the excluded mechanism files and justified keeps.
- Stage 2 (canon): `noether-braid.md` canonical-prose rule now names `spindle braid` as leading-candidate vocabulary and re-marks `shell braid`/`nested shell braid` as comparison-family vocabulary; `terminology-usage.md` Noether Braid Usage table gains a spindle row and re-scopes the nested row (the "main current fermion scaffold" example removed). Comparative glossary and mathematics terminology were already correctly guarded and untouched.
- Spec Section 58 promoted into `spindle-braid.md`: the rail-pinned cadence makes the speed pin the size pin; the coupling refit was the gauge hiding the dilation balance; the self-equilibrated bare configuration (radial equilibrium + restoring basin + closed tangential ledger, no environment) with derived size $R_M^{\mathrm{eq}}\approx3.5\,\kappa\epsilon^2/c_f^2$; closure-vs-ledger metric divergence; axis/nutation modes flagged as the decisive open sector.

## Not Generalizable — Gated on Spindle-Native Re-Derivation

These files keep nested-first prose because their mechanisms depend on frequency separation, ordered bands, or the nested closure label; each needs a spindle-native counterpart before generalization: the generation ladder (`quantum-number-mapping`, `particle-masses`, `electron`, `muon-tau`, `quarks`, `mesons`, `color-charge-su3`); the terminal-alignment/horizon program (`proof-programs/terminal-alignment.md`, `black-holes`, `horizon-chirality`, `singularity-resolution`, `cosmic-censorship-and-holography`, the Planck-scale bridge); the cadence-scale retuning map and its consumers (`return-cycle-lorentz-quantization`, retuning toy model — the spindle's iso-frequency retuning variables are a different map); the oblate Lorentz projection (fusiform projection open in `braid-envelope-geometry.md`); the spin/angular-momentum bridge (`angular-momentum-and-spin` — candidate for a spindle-native rewrite using the uniaxial structure and the native nutation numbers); the doubling-frequency lock chapter (definitionally frequency-separated); validation charts stated on nested labels (`parameter-ledger`, `architrino-si-base-units`, a0 protocols, the nested action-increment protocol).

## Remaining Obligations

- Regeneration (branch/PR process): `node scripts/validate-content.mjs --write` → `node scripts/build-scene-graph.mjs --write --strict` → `node scripts/build-textbook-md-pdf.mjs --write` → re-run `--check` mirrors.
- Re-host the terminal-alignment program onto spindle terminal behavior once the leading-candidate track reaches strong-field work (declared in the proof program's preamble).
- Spindle envelope projection (fusiform $(\xi,\lambda)$ retuning projection) — named open obligation in `braid-envelope-geometry.md`.
- On a retained spindle realization: re-host the generation and dressing hypotheses per their stated portability. On decisive rejection: re-scope the collection to absorb the spindle record instead.
- Follow-on candidate (staged, not executed): the ~600-line Assembly-Noether Sea Interface Diagnostic inside `braid-envelope-geometry.md` was already flagged by the braid-retained-branch-closure lane as a relocation/condensation candidate.

## Gate-List Verification Sweep (2026-07-10, CSO)

A corpus-wide vestige sweep adjudicated the seven remaining high-density nested-first files against this gate list. Verdict: all seven are justified keeps under the gate, not overlooked vestiges — `horizon-chirality` and `black-holes` (terminal-alignment/horizon program), `quantum-number-mapping`, `mesons`, and `color-charge-su3` (generation ladder), `parameter-ledger` and `architrino-si-base-units` (validation charts on nested labels). Addition to the gated set, verified mechanism-bound this sweep: `spacetime/lorentz-kinematics.md` (H/M/L layer axes, hierarchy-leakage error term, nested closure Stage B, and the nested stiffness decomposition are frequency-separation mechanisms; the oblate Lorentz projection dependency also applies). Also gate-adjacent: the oblation passage of `quantum/fermi-dirac-and-bose-einstein-statistics.md` — the "already oblate" envelope claim is nested-family-scoped (spindle envelope is fusiform) and must not be generalized; an over-generalization introduced during the sweep was caught and reverted the same day. Assembly-level class naming elsewhere in that file (Fermi-Dirac ↔ braid assemblies with volumetric nested orbital support) was generalized per Stage 1 policy with the nested mechanism words retained. Future sweeps should treat this section as the standing justified-keep record and re-flag only if a spindle-native re-derivation closes the corresponding gate.

## Executed — Doubling-Frequency Lock Consolidation (2026-07-10, operator-directed)

`braid-families.md#noether-braid-doubling-frequency-resonance-lock` consolidated near-verbatim into `braid-families.md` under `## Noether Braid Doubling-Frequency Resonance Lock` (headings demoted one level; assumption package, propositions, and diagnostics carried at stated claim levels; framing note marks it comparison-family material under the re-derivation gate — the `NSH-421` candidate is definitionally frequency-separated and the spindle's iso-frequency structure has no doubling ladder). Original chapter and its standalone scene deleted; the node pruned from the `noether_braid` parent scene; all corpus inbound links retargeted to the new section anchor (the loose `#exact-integer-phase-closure` fragment upgraded to the true slug `#assumption-2-exact-integer-phase-closure`). Index/scene regeneration pending as usual.

## Low-Density Tail Sweep (2026-07-10, evaluator thread)

Swept every remaining case-insensitive "nested shell braid" in `content/markdown/aaa/` outside the gated files (Gate-List Verification Sweep list plus `noether-braid/`, the three archie terminology files, the two nested-titled bridge/protocol docs, and angular-momentum-and-spin). Result: the tail is almost entirely justified keeps — mechanism-bound (inner/middle/outer roles, three-band scaffold, terminal alignment, oblation, IMO shielding tiers, A0 defined on the family) or proper-name links to "Nested Shell Braid Dynamics". Four default-scaffold occurrences were converted to "Noether braid":

- `dynamics/master-equation.md` (1): emergence-status bullet "Nested shell braids and particle assemblies" → "Noether braids and particle assemblies" (generic downstream-assembly claim, no band dependence).
- `assemblies/particle-masses.md` (2): ζ-factor Energy Storage bullet — "high-speed, nested shell braid rotations" / "For a nested shell braid, ..." → Noether braid (internal-energy-vs-rest-energy claim is assembly-level; adjacent Shielding bullet already says Noether braid).
- `assemblies/bosons/gluons.md` (1): flux tube = "coherent bundle of ambient Noether braids" (sea-constituent claim; sea composition is Noether-braid-level per noether-sea.md).

Kept (mechanism-bound or comparison; representative reasons, all other occurrences in each file same class): `spacetime/singularity-resolution.md` (middle-fulcrum/alignment + planar-horizon reopening), `spacetime/emergent-metric.md` (terminal alignment entropy density), `spacetime/noether-sea.md` (C_nest comparative-selection statements; layer partition), `spacetime/noether-sea-pro-anti-coupling.md` (H→M→L precession convention, ordered chirality), `spacetime/proper-time-and-time-dilation.md` (six-architrino concrete clock, outer-boundary oblate envelope), `dynamics/master-equation.md` remaining (1:2:4 lock target, internal phase space), `dynamics/binary-dynamics.md` (MCB-as-inner-binary; ladder), `dynamics/effective-lagrangian.md` (three-layer T^3 chart; MCB-inside), `dynamics/entropy.md` (terminal-alignment label counting), `dynamics/energy.md` (three-band-subtype definitional; routing-circuit analogy outer/middle/inner), `foundations/emergence-of-structure.md` + `foundations/ontology.md` + `foundations/architrino.md` (taxonomy ladder, Λ_NS, link), `proof-programs/terminal-alignment.md` (program stated on the family's three-band scaffold, demotion-aware), `proof-programs/collinear-breather.md`, `proof-programs/planar-bridge-closure.md`, `proof-programs/master-equation-breather.md` (targets/links), `assemblies/fermions/electron.md`, `muon-tau.md`, `weak-mixing-angle.md`, `quarks.md`, `assemblies/gauge-structure-emergence.md`, `assemblies/particle-masses.md` remaining (IMO shielding tiers, six-site axial layer, A0 definition), `assemblies/bosons/electroweak-bosons.md` (middle/outer driven to c_f; explicitly scoped "in the nested shell braid case"), `nuclear-atomic/hyde-periodic-table.md` (electron oblate envelopes, six axial architrinos, middle-binary velocities), `nuclear-atomic/condensed-matter.md` (link), `quantum/fermi-dirac-and-bose-einstein-statistics.md` non-gated lines (oblation geometry + taxonomy links), `reactions/radiation.md` ({I,M,O} roles, layers, planar lock), `cosmology/inflation-model.md`, `CMB.md`, `dark-matter.md`, `dark-energy.md` (inner/middle/outer energy scales and layers), `philosophy-history/cosmic-censorship-and-holography.md`, `treasure-physics-overlooked.md`, `theory-mapping.md`, `agency-and-internal-causation.md`, `solving-the-crisis.md` (regime tables, three ordered bands, middle-binary mechanisms), `philosophy-history/theory-bridges/*` non-gated (qubit normals, IMO tiers, coupled-ledger clocks, TeX branch-update labels), `archie/aaa-journey.md` + `archie/research-notebook.md` (historical narrative/scorecard description), `validation/closure-scorecard.md` (taxonomy rows), `validation/simulations/*` non-gated (protocol names, speed gates, L1 shielding taxonomy, A0 target).

Micro-fixes: `archie/research-notebook.md` ~275 and `archie/ideal-braid-guide.md` ~168 bare "nested braid" → "nested shell braid" (both describe the ideal-braid app's displayed object, which is the inner/middle/outer nested configuration); `quantum/reality-quantum-causality.md` ~280 "(tri-binary networks)" → "(networks of coupled binaries)" (sub-assembly coupling gloss, unrelated to the planar tri-binary).

Addendum to the tail sweep (same date, evaluator thread): the comparative glossary gained the four missing family-ladder rows — `Neutral Braid`, `Shell Braid`, `Symmetric Shell Braid`, `Planar Tri-Binary` — completing the canon mirror of `terminology-usage.md` (the glossary previously carried only the nested and spindle rows). Standing note: `validate-content --check` reports the known regeneration-pending errors (36, all stale generated reading-copy/toc links to the deleted doubling-lock chapter); fix is `node scripts/build-scene-graph.mjs --write --strict` + `node scripts/build-textbook-md-pdf.mjs --write` in the next regeneration window, per the standing generated-artifacts policy.
