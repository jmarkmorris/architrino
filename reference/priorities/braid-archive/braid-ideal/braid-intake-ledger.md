# Braid Intake Ledger

Claim level. Priority-only, temporary intake ledger, 2026-07-08, branch `codex/calcite`. Phase 1 of the braid priority sort (`braid-prio-sort.md`). This ledger classifies open braid work items (agent threads and saved prompts) and routes each to a lane; it does not start, change, or claim any work. Every routed item keeps its own claim level and fail-closed status. Retire this ledger into `work-log.md` once all rows are routed and consumed (Phase 7).

Full verbatim prompt text lives in the companion file [`braid-intake-prompts.md`](braid-intake-prompts.md) — every saved prompt (P09, P11–P16) and every thread handoff (`p10`, `o7`, `unlabeled-1/2/3`) plus the `t1` last-turn summary, copied exactly. This ledger holds only the classification and routing; that archive holds the pasteable prompt bodies, so the handoffs survive thread deletion.

## Intake Note: Thread Reachability

The six open threads `o4`, `o5`, `o6`, `o7`, `o8`, `p10` are Codex threads on the operator's side. This Cowork session attempted agent-to-agent contact via `SendMessage` to each of the six names on 2026-07-08; all six returned "No agent named '…' is reachable." They are not on this session's agent bus, so their `Closure goal:` lines could not be pulled from here. The operator then relayed five self-contained handoff prompts directly and, on 2026-07-08, retired the thread names as irrelevant (the name↔number mapping is abandoned). Intake is therefore keyed by objective, not by thread name; see Section B.

## Structural Finding

All pasted prompts (09, 11–16) and all six threads belong to the same source: the **Braid-Ideal Thread Prompt Set — 2026-07-07**, derived from the grouped Task Queue in `braid-ideal/priorities.md`. The threads are running prompts 04–08 and 10; the pasted prompts are the unassigned remainder. Therefore the entire current active braid intake routes to the `braid-ideal` hub — Group A (solver/sweep proof rows) and Group D (corpus insight survey). None of it needs routing to another braid lane. This confirms the OP-1 hub decision: `braid-ideal` is where the live braid work concentrates.

---

## A. Saved Prompts (pasted 2026-07-08)

| # | Source | One-line objective | Target lane | Claim level | Duplicates a queued task? | Decision |
| --- | --- | --- | --- | --- | --- | --- |
| P09 | Prompt 09 — Group A items 6+7 | Promote held-release seed to a retained-history solver row with same-source self-hits, run the native central-solver retained-history path on a named row, and record the operator accept/reject in-thread. | `braid-ideal` | `theorem-target` (fail-closed; no retained-branch claim) | Yes — execution prompt for existing Group A items 6–7 (`self_hit_held_release_solver_row` + `native_retained_history_promotion`). | Keep as the execution prompt; merge under Group A items 6–7 (no new queue item). Gated: needs a named row from `o4`/`o5` before it can run. |
| P11 | Prompt 11 — Group D batch 1 | Cross the 7 remaining `foundations/` chapters against the 9-item derived-structure inventory; promote-or-capture per click. | `braid-ideal` (Group D) → corpus | `theorem-target` / `effective-summary` / `speculation` (per click) | Yes — one batch of the single Group D item (`corpus_insight_survey_fork`). | Keep; route to Phase 5 (corpus promotion). Merge under Group D (no new item). |
| P12 | Prompt 12 — Group D batch 2 | Cross the 6 `dynamics/` chapters against the inventory; promote-or-capture. | `braid-ideal` (Group D) → corpus | `theorem-target` / `effective-summary` / `speculation` (per click) | Yes — Group D batch. | Keep; Phase 5. Merge under Group D. |
| P13 | Prompt 13 — Group D batch 3 | Cross the 13 `spacetime/` chapters against the inventory; promote-or-capture. | `braid-ideal` (Group D) → corpus | `theorem-target` / `effective-summary` / `speculation` (per click) | Yes — Group D batch. | Keep; Phase 5. Merge under Group D. |
| P14 | Prompt 14 — Group D batch 4 | Cross the `assemblies/` + `quantum/` chapters against the inventory; promote-or-capture (mass/dressing stays hypothesis/speculation). | `braid-ideal` (Group D) → corpus | `theorem-target` / `speculation` (mass-map/dressing capped) | Yes — Group D batch. | Keep; Phase 5. Merge under Group D. |
| P15 | Prompt 15 — Group D batch 5 | Cross the `reactions/` + `nuclear-atomic/` chapters against the inventory; promote-or-capture. | `braid-ideal` (Group D) → corpus | `theorem-target` / `effective-summary` / `speculation` (per click) | Yes — Group D batch. | Keep; Phase 5. Merge under Group D. |
| P16 | Prompt 16 — Group D batch 6 | Cross the `cosmology/` + `validation/` + `philosophy-history/` chapters against the inventory; promote-or-capture, then close the Group D queue item. | `braid-ideal` (Group D) → corpus | `theorem-target` / `effective-summary` / `speculation-comparison` (external frameworks comparison-only) | Yes — final Group D batch; closes the item. | Keep; Phase 5. Merge under Group D; this batch closes Group D on completion. |

Prompt notes:
- P09 gate/dependency: waits on a named row from `angular_momentum_held_release_sweep` (`o4`) or `breathing_ansatz_eigen_braid_hunt` (`o5`). If no named row exists, P09 stops and returns gate status — do not open a search loop.
- P11–P16 are mutually parallel and can run anytime; they share the nine-item derived-structure inventory (invariant channels/equivariant reductions; drum geometry; axial dipole; momentum screw/helicity; exact speed budget $\beta^2 c_f^2 = u^2 + v_t^2$; anti-damping trilogy; clicks at the fold set; quietness ladder; consilience principle). Their common capture home is `braid-ideal/brainstorming.md#axis-neutral-transport-channel-discussion-log`, which is one of the Phase 3 merge targets — sequence the merge and the survey so they do not fight over that file.
- Group B item 9 (`accessory_dressing_placement_decision`) is resolved/superseded by the scene reorganization (Prompt 02); no row here.

---

## B. Open Threads (captured from operator relay 2026-07-08)

Names retired (operator decision 2026-07-08): the old thread names `o4 o5 o6 o7 o8 p10` are irrelevant and the name↔number mapping is abandoned. This intake is keyed by **objective**, not by thread name, so the lost mapping costs nothing — what had to survive is each thread's next concrete step, captured as a self-contained handoff below. Six distinct objectives were captured (the six rows below; row labels are historical tags only). Five are the braid-ideal Group A absorber / retention-route cluster — see the consolidation finding in Section C. The sixth (`t1`, a Claude session, also unreachable from here) is the six-point equivariance-lemma → native-promotion chain, distinct from the absorber cluster. With `t1` captured, every live thread's next step is represented; Section B is complete. Any live thread's file work already lives in the shared working tree (git-managed from codex), so retiring the thread sessions loses nothing represented here.

| Thread | Source | One-line objective | Target lane | Claim level | Duplicates a queued task? | Decision |
| --- | --- | --- | --- | --- | --- | --- |
| `p10` | Prompt 10 — Group A item 3 (**confirmed**) | Adjudicate the branch-orientation sign at the same-source fold crossing: rigid-rotation $m=+1$ (cusp at coincidence, absorber closed) vs accelerating-worldline $m<0$ (absorptive, absorber alive); route the surviving disposition. | `braid-ideal` (Group A item 3) | `theorem-target`, diagnostic-only / fail-closed | Yes — *is* Group A item 3; adjudicates two recorded artifacts (`fold-crossing-chart-spec.md` vs `fold-crossing-click-impulse-packet.md`). | Keep (confirmed). Load-bearing task-2 adversarial control: reproduce $m=1$ rigid and $m<0$ accelerating from one path. Upstream of the cluster. |
| `o7` | Prompt 07 — Group A item 3 (**confirmed**) | Decide whether a differential (nested-shell / inner–middle-binary) config sustains cross-hit alignment $\mathbf v_j\cdot\hat{\mathbf r}_{ij}=c_f$ over a click window (booking a real $h_{\mathrm{act}}$ vs the certified pump), else hand retention to the shared dynamic-alignment / formation-history burden. | `braid-ideal` (Group A item 3 / `self_hit_held_release_solver_row`) | `theorem-target`, diagnostic / fail-closed | Yes — Group A item 3/1; consumes `p10`'s signed-$m$ result (use signed $m$, never $|m|$). | Keep (confirmed). Consolidate — same gating object as the three unlabeled rows. |
| `unlabeled-1` | relayed handoff (thread # TBD ∈ {`o4`,`o5`,`o6`,`o8`}) | Decide the tangential-absorber question on the retained-history dynamic-braid row by jointly testing (a) non-coincident cross-hit hinge sustained alignment + absorptive impulse sign + click recurrence and (b) neighbor-braid orientational AC susceptibility $\chi''(3\omega)$ vs the derived threshold; report which closes the (S)-persistence burden. | `braid-ideal` (Group A absorber) | `theorem-target`, fail-closed | Yes — same Group A gating object as `o7`. | Keep; **consolidate** (§C). Assign thread number. |
| `unlabeled-2` | relayed handoff (thread # TBD ∈ {`o4`,`o5`,`o6`,`o8`}) | Determine whether the clean finite-chord differential cross-hit drain, summed over the actual multi-charge inter-band source set of a nested shell braid (polarity cancellation resolved first, step 1), clears the certified tangential pump with every chord finite — first coincidence-free clock-beating absorber — or measure the clean multi-source ceiling, fail-closed. | `braid-ideal` (Group A; `differential-cross-hit-alignment-diagnostic.mjs`) | `theorem-target`, fail-closed | Yes — same Group A cross-hit absorber program. | Keep; **consolidate** (§C). Assign thread number. Crux: polarity cancellation. |
| `unlabeled-3` | relayed handoff (thread # TBD ∈ {`o4`,`o5`,`o6`,`o8`}) | Decide the shared dynamic-alignment / formation-history burden with a declared self-consistent dynamic-braid toy: does nested-shell braid evolution (formation, capture, ring-down) sustain the middle-binary field-speed co-linear hinge (equiv. aligned sea orientational order) over a click window? Compute the signed action in the held (non-transversal) limit; zero free amplitude; fail-closed. | `braid-ideal` (Group A item 3; new sibling script + packet) | `theorem-target`, diagnostic-level / fail-closed (explicitly does **not** need the blocked seed-path certificate) | Yes — the capstone the two surviving routes reduce to (`fold-crossing-sustained-alignment-window-packet.md`). | Keep; **consolidate** (§C) — this is the cluster's decision point. Assign thread number. |
| `t1` (Claude session) | six-point equivariance-lemma / native-kernel audit (**confirmed**) | Completed the native-kernel equivariance audit: the seven retained-history force-row paths are equivariant under $S_3\times\langle\iota\rangle$ and $C_3\times\langle\iota\rangle$, clearing the staged six-point reduction lemma for native-record application (Group A item 6 → `staged-native-kernel-audit-complete`). Next step: build the same-record binding chain via `native_retained_history_promotion` once an eigen-braid hunt names a candidate row. | `braid-ideal` (Group A item 6; six-point lemma → native promotion) | `theorem-target` (equivariance-audit result); fail-closed — no retained-branch/accepted-evidence, same-record binding blocked by design | Yes — *is* Group A item 6 (renumbered); its next step `native_retained_history_promotion` is the same object as **P09** (Group A items 6–7). | Keep (confirmed). Distinct from the absorber cluster — this is the lemma/equivariance → promotion chain. Consolidate its next step with P09; both gate on an eigen-braid hunt naming a candidate row. |
Name rows removed: the four remaining `o4`/`o5`/`o6`/`o8` placeholders are dropped because the names are retired and the mapping is abandoned. The one open question is not a name — it is whether any live thread holds a next-step objective **not** already represented by the five rows above (see Section D). The earlier `angular_momentum_held_release_sweep` / `breathing_ansatz_eigen_braid_hunt` inference for P09's upstream producer is unconfirmed; if P09 (Group A items 6–7) is later run, it will surface its own gate status if no producer row exists.

---

## C. Dedupe And Routing Summary

- **Convergent absorber cluster — the sharpest dedupe in the intake (consolidation candidate).** `p10`, `o7`, `unlabeled-1`, `unlabeled-2`, and `unlabeled-3` are all Group A cross-hit-hinge / retained-history absorber threads converging on one gating object: whether the braid's own formation/ring-down dynamics sustain the field-speed co-linear hinge (equiv. aligned sea orientational order) over a click window, decided on a retained-history dynamic-braid row. Their handoffs already report duplicate collisions — one thread "nearly rebuilt a hinge diagnostic that landed mid-flight," another "built a non-coincident hinge diagnostic, discovered the parallel thread had landed the identical result, and deleted my duplicate." Per AGENTS.md multi-agent discipline (do not run multiple agents on the same item unless outputs are explicitly different; split by disjoint write ownership), these five should be consolidated into a single owner with disjoint sub-questions rather than five parallel searches. A natural non-overlapping decomposition already exists in the content: signed-orientation adjudication (`p10`) → sustained-alignment differential hinge test (`o7`) → multi-source polarity-summed drain (`unlabeled-2`) → orientational-susceptibility bound + joint retained-history test (`unlabeled-1`) → self-consistent formation/ring-down capstone that decides the shared burden (`unlabeled-3`). Recommend the operator collapse these to one coordinated lane owner in Phase 4, or at minimum give each a disjoint file-ownership boundary and a "verify-don't-duplicate" startup check.
- **P09 and `t1` converge on the native promotion object (second dedupe).** P09 (saved prompt, Group A items 6–7) and `t1`'s next step both target `native_retained_history_promotion` — building the same-record retained-history binding chain — and both are gated on the same upstream: an eigen-braid hunt naming a candidate row. `t1` has already cleared the six-point reduction lemma against the native kernel, which is the equivariance prerequisite that promotion needs. Treat P09 and `t1`'s continuation as one promotion workstream, sequenced after (and fed by) whichever thread produces the named candidate row. This also confirms the earlier open question about P09's producer: it is an eigen-braid hunt, not necessarily the retired sweep inference.
- **No new lane queue items are created by this intake.** Every pasted prompt is an execution prompt for an item already in `braid-ideal/priorities.md`: P09 → Group A items 6–7; P11–P16 → the single Group D `corpus_insight_survey_fork` item (batched by corpus directory).
- **Every intake item routes to `braid-ideal`.** Group A is featured-realization solver/sweep/absorber proof work (P09 + the five-thread absorber cluster above); Group D (P11–P16) is the corpus promotion survey (this sort's Phase 5). Nothing routes to another braid lane.
- **Cross-lane check:** nothing in this intake belongs in `braid-retained-branch-closure`, `braid-mass-response-map`, `braid-angular-momentum-spin`, `braid-geometry-export-bridge`, `braid-nested-shell-causal-closure`, `braid-doubling-frequency-lock`, or `braid-taxonomy`. Those lanes' own queues are handled by the lane-sort phase (Phase 4), not by this intake.
- **Contention to sequence:** P11–P16 write to `braid-ideal/brainstorming.md`'s survey discussion log, which the Phase 3 brainstorm merge also targets. Run the Phase 3 merge and the Group D survey in a defined order (merge first, or freeze the survey log section during the merge) so they do not collide.

## D. Phase 1 Closed — Threads Safe To Retire

All six live threads' next steps are now captured: the five-thread Group A absorber cluster plus `t1` (six-point equivariance-lemma → native promotion). No objective remains uncaptured, so **Phase 1 intake is complete** and all six thread sessions are safe to delete — their file work is already in the shared working tree, and their next steps live here.

Relaunch guidance when the work resumes (post-deletion), so the sort's Phase 4 does not inherit the old collisions:

1. **Absorber cluster** → one coordinated owner over the five disjoint sub-questions (§C decomposition): signed-orientation adjudication → sustained-alignment differential hinge → multi-source polarity-summed drain → orientational-susceptibility + retained-history joint test → self-consistent formation/ring-down capstone.
2. **Native promotion** → one workstream merging P09 and `t1`'s continuation on `native_retained_history_promotion`, sequenced after an eigen-braid hunt names a candidate row.
3. **Corpus survey (Group D, P11–P16)** → six directory batches; sequence against the Phase 3 brainstorm merge (shared `brainstorming.md` survey log).

Closure goal: delete the six retired threads (all captured), then I proceed to Phase 2 lane hygiene; relaunch the braid-ideal work per the three consolidated workstreams above rather than as independent sessions.
