# Noether Braid Retained-Branch Strategy Memo Capture

Status: priority-only strategy capture, 2026-07-06.
Source: attached read-only strategy memo from the current operator session.
Claim level: priority routing, derivation-closure target, and hypothesis capture only. This packet does not claim a retained branch, accepted evidence, score movement, or corpus promotion.
Corpus disposition: defer with blocker. Promote only after a same-record retained-history row, receiver-normal branch-strength evidence, action/wake/event/support rows, and stability evidence exist.

## Live-Tree Correction

The memo's strategic route is useful, but the live implementation boundary is narrower than "surface-speed rows are missing."

- [Shell-Braid Run Matrix](sh-run-matrix.md) already declares `SH-0` transverse surface-speed and prehistory rows such as `sh0-g0-vt080-kick-at-release` and `sh0-g0-vt080-moving-prehistory`.
- held-release-seed-path-rows.mjs (retired script: `scripts/braid-ideal/held-release-seed-path-rows.mjs`) already accepts run-handle, target-center group velocity, surface-speed fraction, and prehistory-mode metadata.
- sh-0-sea-diagnostic-candidate-model.mjs (retired script: `scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs`) already passes the same metadata into embedded `SH-0-sea` candidate rows.
- held-release-causal-wake-toy.mjs (retired script: `scripts/braid-ideal/held-release-causal-wake-toy.mjs`) is still the missing physics runner: it accepts `--group-velocity` and `--include-self-hits`, but it does not integrate tangential surface speed, a spin axis, `kick-at-release`, or history-consistent `moving-prehistory`.

Therefore the next useful object is not another metadata row or acceptance-certificate sweep. It is the actual transverse-surface-velocity held-release dynamics.

## Strategic Decision

Original strategy decision: add `angular_momentum_held_release_sweep` as the top braid-ideal diagnostic route. The later proofing audit refines the live priority order: `six_point_symmetry_invariant_lemma` is the first proof-moving object, and the sweep is its capped executable witness.

Definition: `angular_momentum_held_release_sweep` means extending the held-release toy so the six-point seed can carry declared tangential surface velocity around a spin axis, with both `kick-at-release` and `moving-prehistory` modes. The sweep should run the already-declared `sh0-g0-vt025`, `sh0-g0-vt050`, `sh0-g0-vt080`, `sh0-g0-vt095`, and `sh0-g0-vt099` rows and report the reduced-radius sign sequence after the first turn.

Implementation spec: see [Angular-Momentum Held-Release Sweep Spec](angular-momentum-held-release-sweep-spec.md) for the axis-neutral rigid-rotation formula, field-speed causal-wake terminology, `kick-at-release` versus `moving-prehistory` distinction, kinematic angular-momentum diagnostic boundary, and required implementation checklist.

Proofing update: see [Six-Point Equivariant Reduction Proof Audit](six-point-equivariant-reduction-proof-audit-2026-07-06.md). The sweep is the best search-moving diagnostic, but the first proof-moving object is the equivariant reduction lemma already scaffolded by `six-point-symmetry-invariant-lemma-row.mjs`; the sweep should be capped and treated as the lemma's executable witness. The frozen-octahedral mean-power value remains motivation only and must not be consumed across charts as a held-release theorem hypothesis.

Required diagnostic outputs:

- fixed-point-drift residual against the equivariant reduction channel;
- post-first-turn reduced-radius acceleration sign;
- second-turn or bounded-window detection if present;
- root-coverage and field-speed status;
- prehistory mode and surface-speed fraction in artifact metadata;
- explicit fail-closed proof status: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, and no accepted seed-path certificate.

Success condition: at least one row shows post-first-turn inward reduced-radius acceleration, a second turn, or a bounded oscillation window on one diagnostic record with causal-root coverage intact. That row becomes the candidate input for a native central-solver retained-history run and for the central row of `SH-0-sea`.

Failure condition: every surface-speed and prehistory row remains outward after the first turn. That failure still moves the proof state by strengthening the isolated Euclidean-void no-return target and making a computed Noether sea response the dominant remaining return mechanism.

## Priority Effects

1. `braid-ideal`: raise `angular_momentum_held_release_sweep` above the current reduced-equation and source-acquisition targets because it can test the missing return-response mechanism directly.
2. `braid-retained-branch-closure`: convert the repeated escape result into a scoped Euclidean-void no-return theorem for the symmetric partner-wake-only channel, with hypotheses that exclude self-hits and Noether sea response.
3. `braid-ideal` and `braid-mass-response-map`: replace fitted `SH-0-sea` response placeholders with a computed like-braid wake-sum route over nearby Noether braid population rows.
4. `app-solver` / native central solver: defer accepted retained-history packaging until the angular-momentum sweep identifies a row worth promoting.
5. `braid-angular-momentum-spin`: if a surface-speed threshold produces return behavior, treat the angular-momentum ledger as a branch coordinate and spin-program input, not as a decorative initial condition.
6. Frozen octahedral refinement and acceptance-certificate acquisition sweeps: freeze unless they are needed to support a return-turn chart or a real accepted central-solver row.

No top-level workstream rank change is made by this packet. It only updates the local braid-ideal route and creates explicit downstream pressure on related lanes.

## Ranked Route

| Rank | Route | Priority decision | Evidence role |
| --- | --- | --- | --- |
| 1 | Angular-momentum-bearing held-release sweep | Do first. | Tests whether the missing return response appears when the seed carries transverse surface motion. |
| 2 | Scoped Euclidean-void no-return theorem | Do second or in parallel. | Turns the isolated escape evidence into a theorem target under explicit partner-wake-only hypotheses. |
| 3 | Computed `SH-0-sea` like-braid wake sum | Do after the sweep target is shaped. | Tests whether surrounding Noether braid population can supply inward projection without fitting a generic response amplitude. |
| 4 | Native central-solver retained-history run | Sequence after a promising diagnostic row exists. | Only route that can produce same-record retained-history evidence for operator acceptance. |
| 5 | Controlled self-hit fold-layer chart | Queue behind the sweep and central-solver route. | Tests whether return response lives in same-source delayed roots. |
| 6 | Bounded-speed live ledger / coupled fixed point | Hold until a return-turn chart exists. | Rigorous certification object, but expensive without a candidate return mechanism. |
| 7 | Frequency-family ranking | Keep dormant. | Classification support only until one-band return behavior exists. |
| 8 | Higher-order frozen octahedral refinement | Freeze. | Refines a chart already rejected for retention and zero-mean speed rescue. |
| 9 | Acceptance-certificate acquisition sweeps | Stop as a search task. | The missing object requires real accepted production and operator authority, not another search. |
| 10 | Larger assembly-braid fallback | Keep guardrailed. | Too large without a six-site return mechanism or Noether sea selection result. |

## Hypothesis Cards

### H1 - Retention Is An Environment Theorem

Hypothesis: the isolated six-site neutral branch in the Euclidean void is non-retainable, and retained Noether braid behavior requires a local Noether sea response row. If true, `SH-0-sea` is not a fallback after isolated `SH-0`; it is the first physical theorem target.

First mathematical task: prove or falsify a no-return statement for the isolated partner-wake channel, then express the Noether sea term as the missing inward projection needed to reverse the post-turn escape floor.

### H2 - Angular Momentum Is A Branch Coordinate

Hypothesis: retained branches require nonzero internal angular momentum, so the zero-velocity held-release seed is a negative-control boundary rather than the representative candidate. If the sweep finds a threshold in surface-speed fraction, that threshold becomes a retained-branch coordinate and a direct input to the angular-momentum/spin lane.

First mathematical task: identify the minimum surface-speed row, if any, that changes the post-first-turn acceleration sign or produces a second turn.

### H3 - The Escape Channel Is A Reaction Corridor

Hypothesis: the symmetric compression-scatter channel is a useful reaction or dissociation corridor rather than wasted failure evidence. Its near-exact symmetry and clean outward release could become a controlled source term for nested-shell causal closure, reaction provenance, or wake-emission studies.

First mathematical task: classify the escape channel by action/wake output once a retained-history solver row exists, without treating it as branch retention.

### H4 - Retention Lives In The Self-Hit Fold Layer

Hypothesis: same-source delayed roots supply the missing return response, and the current field-speed-crossing screens gate away the mechanism instead of merely protecting against pathology. This is high risk because it requires native solver coverage and controlled root accounting.

First mathematical task: run this only after the angular-momentum sweep and central-solver path identify a bounded or near-bounded candidate window.

### H5 - The Noether Sea Return Term Is A Delayed Echo

Hypothesis: the surrounding Noether braid population supplies return response through delayed wake reflection or echo timing, not through a static pressure constant. This predicts a density- and phase-dependent retention window rather than a monotone amplitude threshold.

First mathematical task: replace fitted `SH-0-sea` response with a delayed like-braid wake sum and compare the inward projection against the current post-turn escape floor.

### H6 - The Six-Point Symmetry Lemma Collapses The Search Space

Hypothesis: the face-opposite six-point manifold is invariant under symmetric retained-root force laws, reducing the isolated `SH-0` problem to a small delay system suitable for a no-return theorem and rotating generalization.

First mathematical task: complete `six_point_symmetry_invariant_lemma` and use it as the vehicle for the Euclidean-void no-return theorem.

## Evidence Boundary

- No retained Noether braid exists at any Proof ID in this packet.
- `SH-0` remains diagnostic only. The accepted-evidence chain is still blocked at `held_release_seed_path_rows_acceptance_certificate.v0` / `held_release_seed_path_rows.acceptance_certificate_ref`, followed by the external accepted-authority package and repo authorization.
- Receiver-normal branch-strength evidence remains mandatory: `branchWeight`, `sourceNormalDenominator`, `receiverNormalFactor`, and the same-record row context are proof-critical. Source-normal-only, Jacobian-only, frozen-ledger, toy, fixture, or candidate-provider rows do not authorize retention.
- Bounded-speed live-ledger and coupled fixed-point closure remain open. Frozen fixed-speed ledger evidence cannot be consumed as a live bounded-speed retained branch.
- `SH-0-sea` requires its own same-record rows: like-assembly population, local target-sea frame, sea-response, support/envelope, action/exchange, boundary event, and receiver-normal rows.
- Current diagnostic gap: the held-release toy has only escape evidence after the first turn. The Noether sea response rows are candidate/fitted or metadata-level until a computed wake-sum or accepted solver row closes the mechanism.

## Follow-Up Prompt

```text
Closure goal:
Implement the priority-only angular-momentum held-release sweep so the existing `SH-0` surface-speed and prehistory rows test real transverse-surface-velocity dynamics instead of metadata-only run handles.

Task:
- Extend `scripts/braid-ideal/held-release-causal-wake-toy.mjs` with `--surface-speed-fraction`, `--spin-axis`, and `--prehistory-mode=stationary-held-release|kick-at-release|moving-prehistory`.
- Preserve the current default run exactly when no new options are supplied.
- Run or prepare the `sh0-g0-vt025`, `sh0-g0-vt050`, `sh0-g0-vt080`, `sh0-g0-vt095`, and `sh0-g0-vt099` rows in `kick-at-release` and `moving-prehistory` modes.
- Reuse the existing wiggle-window and reduced-radius diagnostics where possible.
- Add tests proving option metadata round-trips, default behavior is unchanged, and every diagnostic output remains fail-closed with no retained-branch claim or score movement.

Scope:
- Primary files: `scripts/braid-ideal/held-release-causal-wake-toy.mjs`, `tests/braid-ideal-held-release-causal-wake-toy.test.js`, and the braid-ideal priority/work-log files needed to record results.
- Do not mint an accepted seed-path certificate.
- Do not claim retained branch closure.

Expected output:
- Implemented sweep options or an exact blocker if the toy cannot support them.
- Diagnostic classification of post-first-turn reduced-radius behavior.
- Validation commands and results.
```
