# Epicyclic Hunt Handoff

Promotion status: `executed-2026-07-09-family-closed-no-candidate`. The hunt was run to completion on the native machinery ([spec Section 39](fold-crossing-chart-spec.md#39-epicyclic-candidate-row-hunt-apoapsis-only-survives-the-apsidal-phase-knob-finds-a-new-seed-direction--and-the-family-is-closed-without-a-candidate-2026-07-09)): the two declared knobs were added to the owner script (tests 6 passing), twenty-one variants were scored on released dynamics at frozen $\kappa^*$, and the Section 5 null outcome is the result — the isolated spindle braid has no self-confined release in the per-layer epicyclic family. No candidate row is named; the gate stays fail-closed. Durable outputs: the apoapsis-only rule, the $\varphi_M\approx+30°$ seed-record direction (geometry-track input), the knobs as reusable production machinery, and per-variant convention-pair seed scores on the record. This packet remains as the scope record of that hunt; it authorizes no further work.

This packet scoped the epicyclic candidate-row hunt: the search over the per-layer epicyclic family of the spindle braid for the next named candidate row for the (still-open) retained-history gate `native_retained_history_promotion`. Nothing here is accepted evidence; the packet authorizes no score movement.

## 1. The Family

Per-layer epicyclic releases of the Section 22 spindle-braid champion (tabled geometry otherwise untouched): each layer released from apsis radius $\rho_a(1+e_a)$ with angular-momentum-matched tangential speed $\beta_a/(1+e_a)$ and held prehistory the matched slower circle ($\omega/(1+e_a)^2$ — exactly representable as a production moving-circular source, so release carries no position/velocity discontinuity). Existing knobs: $(e_I,e_M,e_O)$ via `--ei/--em/--eo` on the owner script spindle-braid-native-retained-history-confirmation-run.mjs (retired script: `scripts/braid-ideal/spindle-braid-native-retained-history-confirmation-run.mjs`). Knobs the hunt must add (small, declared extensions of the same script): per-layer apsidal phase offsets (release point along the epicycle) and the periapsis-vs-apoapsis start choice.

## 2. Measured Gradient (2026-07-09, [spec Section 36](fold-crossing-chart-spec.md#36-non-circular-same-level-release-the-epicyclic-apoapsis-seed-improves-native-closure-and-survives-coherently--but-the-isolated-braid-still-expands-2026-07-09))

$e_M$-only apoapsis starts improve the native seed record under both self-consistent conventions: fitted-$\kappa^*$/circular-need $0.4721\to0.4531\to0.4464$ and frozen-$\kappa^*$/held-circle-need $0.4721\to0.4417\to0.4188$ at $e_M=0.05/0.1$. The seed-record score of a non-circular seed is convention-dependent, so the hunt's primary objective is NOT the seed row.

## 3. The Hunt Objective (declared)

Primary: released-dynamics scoring on the native machinery with chart-clean click booking at $\rho_c=0.05$ and frozen $\kappa^*=0.315105$ (declared, no refit): maximize tube-coherence longevity and minimize the radial expansion rate $\dot{\overline{\mathrm{rad}}}$ over a declared window ($\ge2$ rotations), with the Section 36 rows as the baseline ($e_M=0.05$: no halt, $\mathrm{rad}_M$ $1.05\to10.4$ over 2 rotations). Secondary/diagnostic: both seed-record conventions reported per variant. A candidate row is a family member whose released expansion rate is consistent with zero (or bounded oscillation) over the window — the first self-confined isolated release, if it exists.

## 4. Constraints

Fail-closed throughout (`retainedBranchClaim=false`, `scoreMovement=no_score_increase`); first-blocker reporting, no partial credit; tabled champion geometry (radii, tilts, azimuths) untouched — only the declared epicyclic knobs move; regulators fixed at the Section 32 declared set; report the Section 4 failure modes of the [native confirmation packet](spindle-braid-native-confirmation-handoff.md) honestly, plus the structural expectation from Section 36 Result 3 (the radial-support deficit is structural — a null result closing the isolated epicyclic family is a valid and valuable outcome; do not tune past the declared knob set to avoid it).

## 5. Expected Output

Either a named epicyclic candidate row (parameters tabled, released-dynamics rows attached) for a fresh retained-history confirmation thread, or an honest family-closure report (the isolated spindle braid has no self-confined release in the epicyclic family), plus the per-variant convention-pair seed scores for the record.
