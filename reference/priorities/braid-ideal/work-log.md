# Braid Ideal Work Log

This file is the chronological work log for the `braid-ideal` priority area. Use it for dated agent status, proof-attempt notes, checker narratives, handoffs, failed paths, and operator/developer communication that must remain discoverable but should not crowd the live priority tracker.

Use `brainstorming.md` for provisional ideas, insights, conceptual maps, and draft corpus-promotable text when this priority area has one. Use the main priority tracker in this directory for the compact current queue, blockers, promotion routing, and next action. Keep focused proof packets, certificates, app specs, and requirement notes in their own sibling files when they need a stable structure.

## Log Entries

### 2026-07-07 - Sustained-Alignment Window Measured: Cross-Hit Clicks Are Transient and Insufficient; Both Surviving Retention Routes Reduce to One Formation-History Burden

Discharged the "exhibit or exclude sustained cross-hit alignment" fork left open by [chart spec Section 7](fold-crossing-chart-spec.md#7-non-coincident-hinge-geometry-2026-07-07). The non-coincident cross-hit fold restores the Section 2 finite $A_2$ impulse only if the alignment $\mathbf v_j\cdot\hat{\mathbf r}_{ij}=c_f$ is *sustained* over a click window; Section 2 assumed the window depth $\mu_0$ by hand. New sibling script [fold-crossing-sustained-alignment-window-diagnostic.mjs](../../../scripts/braid-ideal/fold-crossing-sustained-alignment-window-diagnostic.mjs) (`fold_crossing_sustained_alignment_window_diagnostic.v0`), tests [braid-ideal-fold-crossing-sustained-alignment-window-diagnostic.test.js](../../../tests/braid-ideal-fold-crossing-sustained-alignment-window-diagnostic.test.js) (5 passing); derivation in the new [Fold-Crossing Sustained-Alignment Window Packet](fold-crossing-sustained-alignment-window-packet.md).

Construction: a middle-binary source orbits radius $\rho_M$ at super-field $\beta_M$; an outer receiver orbits $\rho_R$ at $\beta_R$ (nested-shell middle/outer coplanar geometry). For each reception time over one source period the diagnostic solves the causal roots of the receiver against the source path, tracks the near-fold root ($\min|D_s|$), and measures the reception window over which the fold-born root persists at finite chord — deriving $\mu_0$, the recurrence $N_{\mathrm{click}}$, the booked Section 2 impulse, the receiver-normal sign, and the absorbed fraction of the certified per-rotation pump.

Result (moves proof state). The cross-hit fold is reached at finite chord in every configuration ($\min|D_s|$ to $0.003$, chord $O(\rho)$), confirming Section 7 — but the alignment is **transient, not sustained**: the fold is visited for only $\sim1$–$3\%$ of the source period, so with $N_{\mathrm{click}}=1$ the kinematic clicks book $<0.5\%$ of the pump (absorbed $0.1$–$0.4\%$ at $\beta_M\in\{1.05,1.1,1.2\}$). Extending the widest window to full dwell at fixed curvature (a conservative lower bound on the held-hinge transfer) raises the absorbed fraction only to $\approx2.3\%$ — still insufficient; the true held limit drives the fold non-transversal ($a\to0$), where the Section 2 formula no longer applies and the transacted action is set by the dwell dynamics.

Disposition `cross_hit_alignment_transient_not_sustained_insufficient_reduces_to_declared_hinge_branch`: sustained cross-hit alignment is not kinematically generic; a chart-clean pump-beating hinge click requires the declared middle-binary hinge branch that holds $v_M=c_f$ co-linearly over a dwell arc. That is exactly the dynamic-alignment / formation-history burden already isolated for induced sea orientational polarization, so **the two remaining retention routes reduce to one problem**: whether the braid's formation/recycling dynamics sustain a field-speed co-linear middle-binary hinge (equivalently, aligned sea orientational order) over a click window — a retained-history dynamic-braid computation, not another fixed-geometry or kinematic toy. Claim level: derivation for the transient-window measurement and the conservative full-dwell shortfall; assessment for the reduction to the formation-history burden. `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, `free_amplitude_parameter_count=0`, fail-closed at the seed-path certificate and `central_solver_retained_history_row`; no new schema or validator.

Coordination note: this builds on (does not duplicate) the parallel-thread [fold-crossing-hinge-geometry-diagnostic.mjs](../../../scripts/braid-ideal/fold-crossing-hinge-geometry-diagnostic.mjs) / Section 7, which established the instantaneous cross-hit fold; this diagnostic adds the derived-window / absorber-inequality half.

Validation: `node --test tests/braid-ideal-fold-crossing-sustained-alignment-window-diagnostic.test.js` (5 tests) and `node --check` pass.

Next closure goal: on the retained-history dynamic-braid row (once the seed-path certificate unblocks it), decide the shared dynamic-alignment / formation-history burden — does formation/ring-down hold the middle-binary field-speed co-linear hinge (or the aligned sea orientation) over a click window — which gates both surviving retention routes.

### 2026-07-07 - Independent Verification: Non-Coincident Cross-Hit Hinge Restores the A_2 Finite Impulse (Parallel-Thread Result Reproduced)

Independent reproduction of the non-coincident hinge geometry result landed the same day by the parallel-thread owner script [fold-crossing-hinge-geometry-diagnostic.mjs](../../../scripts/braid-ideal/fold-crossing-hinge-geometry-diagnostic.mjs) ([spec Section 7](fold-crossing-chart-spec.md#7-non-coincident-hinge-geometry-2026-07-07)). This thread built a separate hermetic diagnostic while the owner script was in flight; both reached the same conclusion, so the duplicate script/test/packet were removed and the finding is recorded here as verification rather than a second implementation.

Reproduced facts (independent hermetic model: source on a circle radius $\rho$ at rim fraction $\beta_s$, receiver at finite base separation, fold set $D_s=c_f-\mathbf V_s\cdot\hat{\mathbf r}=0$). (i) Mach threshold: the source causal fold exists only for $\beta_s>1$ (zero folds below field speed, two above; Mach half-angle $\arcsin(1/\beta_s)$), so the fold birth is the field-speed hinge itself. (ii) Finite-chord generic fold: for $\beta_s>1$ the two folds sit at finite chord $r_c=O(\rho)$ (e.g. $0.777$ at $\rho=1$, separation $1.5$, $\beta_s=1.1$), $D_s$ crosses zero linearly (curvature $a=0.775\neq0$ — generic Whitney $A_2$, not the symmetric cusp), $D_T$ finite. (iii) Coincidence control: shrinking the base separation reproduces the closed symmetric channel — chord collapses $0.777\to0.046$ and the (regularizer-free) Section-2 impulse diverges $2.66\to297$ as $r_c\to0$, matching the owner script's softening-independent-only-at-finite-chord finding. The parallel owner test suite passes independently here (`node --test tests/braid-ideal-fold-crossing-hinge-geometry-diagnostic.test.js`, 5 tests), and its disposition `non_coincident_cross_hit_hinge_restores_a2_finite_impulse_contingent_on_sustained_velocity_alignment` is confirmed.

Agreement on claim level and residual burden: the click-absorber route is not closed in general — the surviving requirement is a hinge that sustains $\mathbf v_j\cdot\hat{\mathbf r}_{ij}=c_f$ at finite separation (the same dynamic-alignment / formation-history burden gating induced sea orientational polarization), plus the impulse sign (polarity/geometry-dependent for a cross-hit) and the recurrence $N_{\mathrm{click}}$. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, blocked at the seed-path certificate and the central retained-history row; no duplicate artifact retained.

### 2026-07-07 - Dynamic Induced Polarization: First Absorptive Sea Tangential Channel, but Insufficient at Cluster Stiffness; All Fixed-Geometry / Single-Site Absorbers Now Closed

Answered the induced-sea-orientational-polarization half of the standing absorber closure goal at the level the static relaxation diagnostic could not reach. The existing [static induced-polarization diagnostic](sh-0-sea-diagnostic-candidate-model.md#induced-sea-orientational-polarization---2026-07-07) holds the central braid fixed and relaxes neighbor orientations to an energy equilibrium — a RADIAL retention question (result: anti-retentive). The closure goal names the dynamic TANGENTIAL response: neighbor orientations responding with a phase lag to the central braid's *rotating* wake. New sibling script [sh-0-sea-dynamic-polarization-drive-diagnostic.mjs](../../../scripts/braid-ideal/sh-0-sea-dynamic-polarization-drive-diagnostic.mjs) (`sh_0_sea_dynamic_polarization_drive_diagnostic.v0`), tests [braid-ideal-sh-0-sea-dynamic-polarization-drive-diagnostic.test.js](../../../tests/braid-ideal-sh-0-sea-dynamic-polarization-drive-diagnostic.test.js) (3 passing); derivation in the new [Dynamic Induced Polarization section](sh-0-sea-diagnostic-candidate-model.md#dynamic-induced-polarization---tangential-absorber-test---2026-07-07).

Results (moves proof state). (i) Threefold-drive lemma (derivation, witnessed): the $C_3$-symmetric rotating braid presents a $2\pi/3$-periodic wake at each fixed neighbor, so the orientational drive torque has only threefold harmonics $m=3,6,9$ ($m=1,2,4,5$ vanish to $3\times10^{-15}$); the axial dipole $2(1,1,1)\parallel\hat{\mathbf n}$ is rotation-invariant and supplies no AC drive, so the leading drive is the $m=3$ harmonic ($\sum_k|\tau_k^{(3)}|^2$ from $0.109$ at $\beta=0.1$ to $0.529$ at $\beta=0.9$, causal delay included). (ii) Sign-definite absorption (derivation): unlike the static sea's orientation torque $c_0$ (Corollary S: exact zero cyclic work), a phase-lagged response dissipates $\langle P\rangle=\tfrac12\sum_m(m\omega)\chi''(m\omega)\sum_k|\tau_k^{(m)}|^2\ge0$ — the first sea tangential channel that always drains (never pumps) the rotation, with effective damping $\Phi_{\mathrm{tan}}^{\mathrm{ind}}\approx(0.03$–$0.17)\chi''(3\omega)$. (iii) Threshold (computation + assessment): absorbing the certified pump $c_1\beta$ needs $\chi''(3\omega)\gtrsim8.6$–$19.2$, i.e. orientational stiffness $K\lesssim0.026$–$0.058$; the cluster stiffness the static relaxation exhibits ($K\sim0.3$–$0.5$, $\chi''_{\max}\sim1$–$1.7$) falls short by $5$–$15\times$, so at plausible $\chi''\sim1$ the damping is $\le10\%$ of the pump — the same order as the static sea and breathing channels.

Disposition `dynamic_induced_polarization_absorptive_but_insufficient_at_cluster_stiffness`: the channel is genuinely dissipative but insufficient under the cluster-derived stiffness; sufficiency requires a near-Goldstone-soft neighbor orientation mode ($K\lesssim0.05$) *and* resonance tuning ($\gamma\approx K/3\omega$), reducing the branch to one internal quantity — the neighbor braid orientational AC susceptibility $\chi''(3\omega)$ — that only a retained-history dynamic-braid computation can fix. Claim level: derivation for the threefold-drive lemma, absorptive sign, and damping functional form; computation for the drive harmonics and $\chi''$ threshold; assessment for the cluster-stiffness shortfall. `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, fail-closed at the seed-path certificate; the reported $\chi''$ is a threshold, never a fitted amplitude (`free_amplitude_parameter_count=0`).

Closure-goal synthesis. With this result, every fixed-geometry and single-site-self-hit tangential absorber is now closed or insufficient: static aligned sea a certified non-absorber ($\le10\%$, Corollary S); common-phase breathing $\le27\%$; dynamic induced polarization $\le10\%$ at cluster stiffness (this entry); and the symmetric circular self-hit fold is a cusp born at coincidence with an absorptive branch-orientation sign but a regulator-dependent magnitude (no regularization-independent chart impulse — the fold-crossing chart-spec Section 6 measurement and the click-impulse diagnostic). The regularization-independent hinge-click impulse the closure goal named is therefore falsified on the realized symmetric channel; the two surviving retention routes are a **non-coincident hinge geometry** (asymmetric or nested-shell self-hit/cross-hit born at finite chord $r_c=O(\rho)$, restoring the $A_2$ finite impulse) and a **near-Goldstone-soft neighbor orientation mode** ($K\lesssim0.05$). Both require objects beyond the fixed-source / symmetric-channel toys.

Validation: `node --test tests/braid-ideal-sh-0-sea-dynamic-polarization-drive-diagnostic.test.js` (3 tests) and `node --check` pass.

Next closure goal: exhibit or exclude a non-coincident hinge geometry (asymmetric/nested-shell configuration whose self-hit or cross-hit fold is born at finite chord), and compute the neighbor braid orientational AC susceptibility on a retained-history dynamic-braid row to decide whether $\chi''(3\omega)$ reaches the derived absorber threshold.

### 2026-07-07 - Fold-Crossing Branch-Orientation Sign Verified Absorptive: Refines the Chart-Spec Rigid-Rotation m=1

Verified the magnitude half and refined the branch-orientation half of the same-day [Fold-Crossing Chart Spec](fold-crossing-chart-spec.md) for `self_hit_held_release_solver_row` (Group A item 3). Before writing, inspected `git status --short --untracked-files=all` and the priority tracker; the chart-spec / fixture / measurement artifacts had landed, so this thread verifies rather than duplicates. New sibling files: verification packet [fold-crossing-click-impulse-packet.md](fold-crossing-click-impulse-packet.md), owner script [fold-crossing-click-impulse-diagnostic.mjs](../../../scripts/braid-ideal/fold-crossing-click-impulse-diagnostic.mjs) (`fold_crossing_click_impulse_diagnostic.v0`), and tests [braid-ideal-fold-crossing-click-impulse-diagnostic.test.js](../../../tests/braid-ideal-fold-crossing-click-impulse-diagnostic.test.js) (9 passing). The diagnostic drives [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) via CLI for the real worldlines and reproduces the naive regularization sweep; a hermetic canonical rotating-channel model with the same fold sign structure backs the unit tests.

Construction: on the incoming worldline of one receiver site through the field-speed hinge, the same-source causal residual $F_T(s)=\lVert\mathbf X(T)-\mathbf X(s)\rVert-c_f(T-s)$ is solved for its grazing root, and two tangential impulses are integrated side by side — the naive absolute branch weight $\lvert D_T/D_s\rvert$ with the toy's Jacobian-floor clamp, softened denominator, and self-hit-minimum-delay gate, and the chart-defined signed orientation $m=D_T/D_s$ with exact $D_s$ and none of those regularizers. Witnesses: nstep convergence at fixed cut, regularization invariance across softening / floor / min-delay, and a coincidence cut-distance sensitivity sweep.

Result (moves proof state): the chart spec's coincidence birth is confirmed directly — the executable cut sweep gives $\mathcal I_{\mathrm{tan}}=-9.0,-23.1,-55.7,-137.1,-588.3$ as the resolved chord runs $0.04\to0.0025$, so there is no regulator-independent magnitude. But the chart spec's $m=1$ is an artifact of its fixed-$\beta$ rigid-rotation reconstruction, which sets $\mathbf V(T)=\mathbf V(s_r)$ and freezes the tangential speed the pump is changing. On the actual accelerating crossing the receiver crosses field speed ahead of its own emitting past, so $D_T<0<D_s$ and $m<0$ at every reception time past the hinge, arbitrarily close to birth: measured on `vt095`, $D_s=+0.00629$, $D_T=-0.00270$, $m=-0.43$ at $T=0.4304$ (hinge $0.42893$), steepening to $m=-0.87$ at $T=0.44$; `vt080` and `vt099` carry the identical absorptive sign. The signed impulse is bit-identical across the softening / Jacobian-floor / self-hit-minimum-delay triples that drive the recorded naive $12.4\to9.8\to10.6\,c_f$ ejection drift (reproduced: `vt095` final field-speed ratio $12.39$ / $9.83$ / $10.55$ under floor $0.05$-mindelay $0.002$ / floor $0.2$ / mindelay $0.05$), and its sign is absorptive at every coincidence cut.

Disposition `absorptive_branch_orientation_sign_regulator_independent_magnitude_coincidence_sensitive`. The symmetric-channel hinge click is not closed by an ejective sign, as the naive toy and a fixed-$\beta$ reading imply; its direction is absorptive and its amount is set by the coincidence-stratum spatial self-regularization the point-transceiver ontology already carries. This is a discrepancy against the chart-spec closure that the branch-orientation sign, not the magnitude, decides. Each witnessed crossing books a diagnostic-only $h_{\mathrm{act}}$ ledger row (transacted cycle increment $-1\cdot h_{\mathrm{act}}$, direction only, no action closure), reusing the closed-cycle action-unit convention with no new validator or schema.

Checker status: `node --check` on the new script passed; `node --test tests/braid-ideal-fold-crossing-click-impulse-diagnostic.test.js` passed (9 tests); `node scripts/check-polarity-notation-drift.mjs` and `node scripts/validate-content.mjs --check --strict` passed. Claim level: diagnostic/verification rows only; `retainedBranchClaim=false`, `scoreMovement=no_score_increase`; every output fail-closed at the seed-path certificate and `central_solver_retained_history_row`; no retained branch, no accepted evidence, no stability claim.

Open operator decision: admit the sign-definite, spatially-self-regularized symmetric-channel hinge transfer for the absorber program, or require a non-coincident hinge geometry (chart-spec route) for a chart-clean magnitude. Named follow-up producer boundary: a `central_solver_retained_history_row` contract carrying the signed branch orientation across $\Sigma$ (not $\lvert m\rvert$), the coincidence-stratum spatial self-regularization, and same-record $h_{\mathrm{act}}$ ledger rows.

Next closure goal: operator adjudication of the absorptive-sign versus chart-clean-magnitude question, then either the signed-orientation central-solver contract extension or the non-coincident hinge-geometry hunt.

### 2026-07-07 - Induced Sea Orientational Polarization Falsified: Overdamped Relaxation Drives the Sea Anti-Retentive, Aligned-Order Caveat Hardens

Closed the last standing static/quasi-static sea-alignment route from the orientational-order analysis. New sibling script [sh-0-sea-induced-polarization-diagnostic.mjs](../../../scripts/braid-ideal/sh-0-sea-induced-polarization-diagnostic.mjs) (`sh_0_sea_induced_polarization_diagnostic.v0`) with tests in [braid-ideal-sh-0-sea-induced-polarization-diagnostic.test.js](../../../tests/braid-ideal-sh-0-sea-induced-polarization-diagnostic.test.js) (6 tests passing); recorded run `--samples=6 --iteration-cap=2000 --convergence-tolerance=1e-7`. Reuses exports from [sh-0-sea-diagnostic-candidate-model.mjs](../../../scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs) (kernel, FCC sites/directions, floor, fail-closed authorization).

Construction: each of the 12 FCC nearest-neighbor braids carries one declared $SO(3)$ orientation on the held six-site face-opposite decoration (dipole reversal reachable within $SO(3)$, so no separate conjugation variable), evolving by overdamped gradient relaxation on the total potential-superposition energy of the 13-braid cluster — central-shell plus shell-shell bonds, central braid held fixed as the `SH-0` target identity, torque $\tau_k=\sum_{i\in k}(x_i-X_k)\times F_i$, $F_i=-\nabla_i U$, kernel constants identical to the wake-sum runs. Relaxation step, iteration cap, and convergence tolerance are declared integration parameters (not response amplitudes): `free_amplitude_parameter_count=0` at run, spacing, and ensemble levels. Order parameter is the mean per-neighbor dipole projection onto the central dipole; $\Pi_R\mathcal A^{\mathrm{sea}}$ of the relaxed shell is tested against the escape floor $-0.0934863484737535$ with the standard deadband.

Result (moves proof state on the failure condition — a clean falsification): relaxing from the aligned start, the paired-antiphase start, and every seeded disordered start, the relaxed radial response is outward ($\Pi_R>0$) at every declared spacing $a_{\mathrm{FCC}}\in\{3,3.5,4,4.25,4.5,5,5.25\}$, reversing the sign of the assumed-aligned inward reference and clustering near the anti-retentive conjugate limit. At the named candidate spacing $4.25$: relaxed aligned $\Pi_R=+0.2837$, disordered mean $\Pi_R=+0.2934$ (min $+0.2511$, max $+0.3365$, $0/6$ inward crossings), against the assumed-aligned reference $-0.2833$. No start crosses the inward floor at any spacing; the only inward disordered seed is one of six near-overlap samples at $a_{\mathrm{FCC}}=3.0$ against an outward mean $+0.587$. Exact controls verified as designed: conjugation antisymmetry (all-neighbor conjugation flips $\Pi_R$ to $\le3\times10^{-16}$), paired-antiphase null in both the charge-conjugation and $SO(3)$ $180^\circ$-rotation representations, and paired-antiphase as an exact response null but not an energy fixed point (torque $\approx1.47$ at $4.25$), so relaxation escapes it. The tension the run was built to test resolves rather than being assumed away: the center-shell energy prefers dipole-reversed neighbors ($+0.864$ aligned versus $-0.864$ conjugate at $4.25$), and overdamped relaxation realizes that preference in response space; neighbor-neighbor frustration keeps the dipole order parameter small and mildly positive ($O\approx0.12$-$0.23$) because the FCC dipole-shell sum cancels, but the retention-relevant higher-multipole response is robustly anti-retentive.

Disposition `induced_polarization_relaxes_anti_retentive_no_bounded_window`. The static induced-polarization retention route is falsified at the diagnostic level: left to relax under its own potential-superposition energetics, the fixed-geometry sea polarizes to expel the central braid. Combined with the frustration, paired-antiphase null, and stochastic-disorder rejections, no static or quasi-static mechanism in the fixed-geometry sea supplies the central-aligned order that guaranteed retention requires. The aligned-order caveat on every sea-screened held-release row therefore hardens from "conditional on an alignment mechanism" to an open formation-history / dynamic-alignment burden, and the mechanism hunt sharpens toward the hinge-click absorber route (`self_hit_held_release_solver_row`). Recorded in the [model packet](sh-0-sea-diagnostic-candidate-model.md#induced-sea-orientational-polarization---2026-07-07) and the [run matrix](sh-run-matrix.md). Claim level: diagnostic rows only; `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, every output fail-closed at the central seed-path certificate; no retained branch, no accepted evidence, no stability claim.

Next action: `self_hit_held_release_solver_row` (hinge-click absorber route) is now the sole remaining retention-mechanism candidate at the sea/environment level; the producer boundary is the regularization-independent fold-crossing treatment on the central-solver retained-history path.

### 2026-07-07 - Sea-Screened Clock Corollary (Corollary S): the Aligned FCC Sea is a Certified Non-Absorber of the Tangential Pump

Decided the sea-export branch of the (S)-persistence / anti-damping absorption question. Extended the planar rotating-channel screen with the computed `sh0sea` sea term and certified the sign of the net along-velocity force against the certified pump band. New section [Corollary S](delayed-escape-certificate-lemma-proof-packet.md#corollary-s---sea-screened-clock-the-aligned-fcc-sea-is-a-non-absorber) in the [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md); owner-script extension `seaEnclosure`/`certifySea` in [planar-tangential-screen-interval-certificate.mjs](../../../scripts/braid-ideal/planar-tangential-screen-interval-certificate.mjs), tests extended to 6 passing.

Construction and result (moves proof state on the success condition — a certified sign decision for the sea tangential term against the certified pump band): the attempt `aa` FCC nearest-neighbor shell (12 like braids held static, dipole-aligned, $a_{\mathrm{FCC}}=4.25$, zero fitted amplitude, same kernel row — $c_f=1$, $\kappa=1$, zero softening, receiver-normal over source-normal, partner-wake plus one-way static sea) is placed around the axis-neutral rotating braid. Because the sources are held static (source Jacobian $1$; delay does not move them), the sea along-velocity projection decomposes exactly as $\Pi_{\mathrm{tan}}^{\mathrm{sea}}(\beta,\varphi)=c_0(\varphi)-\beta Q(\varphi)$, both $\beta$-independent geometric sums over rotation phase $\varphi$. Two certified facts decide the absorber question: (i) $c_0$ (the static orientation-locking torque) is odd about the $C_3$-symmetric phases with exact zero rotation-cycle average (witness: cyclic mean $\le4\times10^{-17}$, oddness residual $\le3\times10^{-16}$) — it transacts no net tangential action; (ii) the velocity-linear coefficient is interval-certified $-0.1269\le Q(\varphi)\le0.2746$ over one $2\pi/3$ period (4096 phase boxes, outward-rounded, min sea distance $\ge1.469$), so $\sup_\varphi\lvert Q\rvert\le0.2746<c_1=2.881$ with non-absorber margin $\ge2.606$. The aligned FCC sea removes at most $\approx10\%$ (cyclic mean $\approx2.4\%$) of the certified pump and cannot cancel it. The octahedral chart reproduces the recorded release-instant $\Pi_R=-0.2833417889031177$ to 12 digits, so the sea screen and the recorded dipole wake sum are the same object.

Decision `sea_screened_clock_survives_aligned_sea_non_absorber`: the certified clock survives sea screening with the rate reduced only from $c_1$ to $c_1-\sup\lvert Q\rvert\ge2.606$, giving $\Delta t\le1.495\,c_f\rho^2/\kappa$ (isolated $1.353$; inside the Lemma T tube $\theta c_1-\sup\lvert Q\rvert\ge1.166>0$ at $\theta=\tfrac12$, so the sea-screened clock is unconditional there too). The certified inward radial projection $\Pi_R=-0.283$ holds the radius — inducing the recorded sub-field return turns — while the uncancelled pump drives the sites through $c_f$: radial support without a tangential absorber, exactly matching the recorded [sea-screened held-release rows](sh-0-sea-diagnostic-candidate-model.md#sea-screened-held-release-rows---2026-07-07). Order-robust: conjugate order flips $Q$ (same $\sup\lvert Q\rvert$), paired-antiphase gives $Q\equiv0$ and $\Pi_R\equiv0$, isotropic disorder averages to zero, so the non-absorption holds in every orientation order; the orientational-order caveat (aligned order is frustrated, not self-selected) conditions only the sea's radial retention supply, never the clock. Claim level: interval certificate conditional on the declared rounding model for the $Q$ bound; derivation for the decomposition, $c_0$ cancellation, order-robustness, and clock; no retained branch, no accepted evidence, `scoreMovement=no_score_increase`. Consequence: the (S)-persistence burden's sea-export branch is discharged; the remaining absorber candidates narrow to hinge clicks (`self_hit_held_release_solver_row`) and induced sea orientational polarization.

Validation: `node --test tests/braid-ideal-planar-tangential-screen-interval-certificate.test.js` (6 tests) passes.

Next closure goal: the (S)-persistence lemma via the two remaining absorbers — the regularization-independent fold-crossing (hinge click) treatment on the central-solver retained-history path, and the induced sea orientational-polarization linear response the fixed-source diagnostic cannot compute.

### 2026-07-07 - Rigid-to-Released Transfer Lemma Proved: (S)-Failure Clock Unconditional Inside a Certified Tube; Rigid-Family Rejection Upgraded to an Open Neighborhood

Proved Lemma T and Corollary T in the [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md), eliminating the certified clock's transfer hypothesis. Lemma T is a Lipschitz-in-history deviation bound: if a released row's per-site history stays within $(\delta,\nu)$ of a rigid planar-hexagon rotating history over one window depth ($3\rho/c_f$), then its along-velocity wake acceleration stays within $T=L_x(\beta)\delta+L_v(\beta)\nu$ of the certified screen value $\Phi_{\mathrm{tan}}(\beta)$. Proof chain: root shift $\le2\delta/(1-\beta)$ through the Lemma 5 transversality floor (T1); pair-vector displacement $D=2\delta/(1-\beta)$ with per-partner root-distance floors (T2); unit-vector, shifted-event velocity (ansatz acceleration $\beta^2$ exact), normal, and branch-weight perturbation steps (T3-T6); mean-value inverse-square bound with $\lVert D(\mathbf u/\lvert\mathbf u\rvert^3)\rVert=2/\lvert\mathbf u\rvert^3$ (T7); sum and velocity-direction projection (T8). Every coefficient is built from the certified rigid floors, so the interval script evaluates $L_x$, $L_v$, and the tube radius per $\beta$-box (schema `planar_tangential_screen_interval_certificate.v1`, tests extended to 4 including an adversarial perturbation witness that independently re-solves causal lags on displaced configurations). Corollary T: inside the tube $T+c_1\nu\le\tfrac12 c_1\beta'$, the actual rim fraction obeys $d\beta'/dt\ge\tfrac12 c_1\kappa\beta'/(c_f\rho^2)$, so for any released row either tube residence fails first (monitored, checkable, same class as (S)/(D$_{\mathrm{op}}$)) or (S) fails within $\Delta t\le(2c_f\rho^2/(c_1\kappa))\ln(0.985/\beta_0')$. Consequence: no relative equilibrium, periodic row, or slowly varying sub-field row exists inside the certified tube — the rotating-wave packet's rigid-family rejection upgrades from the exact ansatz to an open neighborhood, so admissible eigen-braids must sit beyond the tube in breathing amplitude, non-planarity, or history deviation, or be sea-coupled. Recorded constants: per-partner root-distance floors $(0.524,1.029,1.486,1.749,1.018)\rho$; tube $1.9\times10^{-3}\rho$ at $\beta=0.25$, $1.7\times10^{-3}\rho$ at $\beta=0.5$, $3.3\times10^{-4}\rho$ at $\beta=0.9$, minimum $4.8\times10^{-5}\rho$ near the field-speed edge; witness conservatism $340\times$-$1900\times$, so tube sharpening (direction-resolved constants) is the named follow-up. Claim level: derivation with coefficients certified conditional on the declared rounding model; no retained-branch claim, no accepted evidence, `scoreMovement=no_score_increase`.

Validation: `node --test tests/braid-ideal-planar-tangential-screen-interval-certificate.test.js` (4 tests) and `node scripts/validate-content.mjs --check --strict` pass.

### 2026-07-07 - Self-Hit Owner Row: Candidate Request Bound, Comparison Witness Consumed; Probe Result Independently Reproduced

Executed the owner-row half of `self_hit_held_release_solver_row` and independently reproduced the sea-shell self-hit probe. Toy runs (`--fcc-sea-spacing 4.25 --include-self-hits`, defaults otherwise) on the three return-turn rows plus the `vt000` control, with self-hit-disabled baselines regenerated for frame-level comparison: same-source roots are absent sub-field (frame metrics bit-identical to the baselines with first divergence exactly at the crossing frame, $t=0.48/0.44/0.44$), the sub-field return turns at $t=0.176/0.346/0.390$ are self-hit-robust, and post-hinge the naive kernel ejects (final field-speed ratios $12.7/12.4/12.3$ versus $2.5/2.3/2.5$; final $R\approx32$ versus $\approx4$; max branch weight $164$ versus $25$) — matching the packet's [Self-Hit Probe Inside the Sea Shell](sh-0-sea-diagnostic-candidate-model.md#self-hit-probe-inside-the-sea-shell---2026-07-07) numbers exactly, and adopting its claim level: the ejection is regularization-dependent, so the toy witnesses the uncontrolled fold crossing, not the controlled click, and cannot decide the click-absorber question.

Owner-row edits ([self-hit-held-release-solver-row.mjs](../../../scripts/braid-ideal/self-hit-held-release-solver-row.mjs), tests extended to 6 passing): the row now consumes the named `sh0sea-aa-fcc-dipole-wake-sum:a-fcc-4.25` request via `--candidate-same-record-request-json` into `candidate_same_record_target_binding` — verifying the request schema (`sh_0_sea_candidate_same_record_request.v0`), the candidate identity, and that this row is a listed consumer; the binding reports `candidate_request_bound_not_accepted`, authorizes nothing, and propagates the request's `held_release_seed_path_rows_acceptance_certificate.v0` blocker. A new `--sea-self-hit-comparisons-json` input consumes with/without-self-hit toy result pairs through `summarizeSeaScreenedSelfHitComparison` into a diagnostic witness (`sea_screened_self_hit_comparison_row.diagnostic.v0` rows; run handles `sh0sea-a4.25-selfhit-vt080/vt095/vt099-moving-prehistory`), whose aggregate decision is `naive_self_hit_kernel_uniformly_ejective_toy_cannot_decide_controlled_click`; a sub-field divergence would be flagged as a self-hit policy defect and fail the witness. The row's artifact status, first missing object, authorization flags, and negative controls are unchanged: fail-closed at `central_solver_retained_history_row`, no retained-branch claim, `scoreMovement=no_score_increase`.

Capture decision: no new discussion-log entry — the fold-crossing/controlled-click mechanism analysis is already captured in the packet's probe section; results recorded here and in the [run matrix](sh-run-matrix.md) sea-screened self-hit paragraph. Next action per the sharpened producer boundary: the regularization-independent fold-crossing treatment on the central-solver retained-history path (canonical fold-set chart, chart-defined finite click impulse, same-record $h_{\mathrm{act}}$ action-ledger rows); toy-level probing of this task is exhausted.

### 2026-07-07 - Independent Verification of Lemma T (Rigid-to-Released Transfer) and Its Certified Tube Coefficients

Independent verification pass over the Lemma T transfer bound and Corollary T (unconditional clock in the certified tube) added to the [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md), performed in a separate thread from the authoring one. Proof-chain audit: every step T1-T8 was re-derived independently and found correct — the root shift $|t_r'-t_r|\le2\delta/(1-\beta)$ via the ansatz residual's Lemma 5 slope floor evaluated at the actual root; the pair-vector deviation $D=2\delta/(1-\beta)$; the unit-vector bound $2D/d_j^{\mathrm{lo}}$; the shifted-event source-velocity bound $\nu_s=\nu+2\beta^2\delta/(1-\beta)$ using the ansatz's exact centripetal magnitude; the normal deviations and the weight quotient with actual source-normal floor $S/2$; the row bound via the mean-value derivative $\lVert Dg\rVert=2/\lvert\mathbf u\rvert^3$ with the segment-norm floor $d_j^{\mathrm{lo}}/2$ under cap (A); and the projection term $2F(\beta)\nu/\beta$. An independent uniform-floor derivation of the same functional (all-partner floors $d\ge0.48$, source-normal $\ge0.58$, $W\le3.44$) yields $\lvert\Delta\Phi_{\mathrm{tan}}\rvert\le2867\,\delta/(1-\beta-\nu)+166\,\nu+298\,\nu/\beta$, numerically checked in Node; the owner script's per-box certified coefficients are strictly tighter everywhere ($L_x=382$ versus $5734$ at $\beta=0.5$; $3891$ versus $28670$ at $\beta=0.9$), with the gain traceable to the per-partner root-distance floors ($0.524$ to $1.749$ versus the uniform $0.48$) — the two derivations are mutually consistent conservative bounds on the same deviation functional. Empirical deviation witness: using the residual scan's two-ring $\alpha$ offset as a controlled channel deviation ($\delta=\alpha$, $\nu=0$), the measured $\lvert\Phi_{\mathrm{tan}}(\alpha,\beta)-\Phi_{\mathrm{tan}}(0,\beta)\rvert$ stays below the transfer bound at every sampled $(\alpha,\beta)$ with maximum measured-over-bound ratio $3.2\times10^{-5}$. Executable state: a transient `cleanNumber is not defined` defect in the in-flight coefficient block was observed and was fixed by the authoring thread within minutes (schema now `planar_tangential_screen_interval_certificate.v1`); the certifier reruns clean with the `transferLemma` block ($\theta=1/2$, tube radius per box, minimum $4.8\times10^{-5}$ at $\beta\approx0.985$, marks from $\beta=0.05$ to $0.9$) and all tests pass (3/3). Proof-state consequence confirmed: the transfer hypothesis is eliminated in favor of the checkable tube-residence condition; the certified clock runs unconditionally inside the tube; and the rigid-family rejection upgrades to an open neighborhood of the rigid family, so any admissible eigen-braid row must sit outside the tube or be sea-coupled. No files changed by this verification beyond this entry.

### 2026-07-07 - Self-Hit Probe Inside the Sea Shell: Exact Hinge Timing; Naive Kernel Ejects Chart-Dependently; Producer Boundary Sharpened

Executed the sharpened toy-level form of `self_hit_held_release_solver_row`: the sea-screened rows rerun with `--include-self-hits` at $a_{\mathrm{FCC}}=4.25$ (`vt000` control, `vt080`/`vt095`/`vt099` moving-prehistory, a no-sea `vt095` isolation control, and Jacobian-floor/minimum-delay regularization variants). [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) gained the `firstSelfHitRoot` event. Two results, recorded in the [SH-0-sea packet](sh-0-sea-diagnostic-candidate-model.md#self-hit-probe-inside-the-sea-shell---2026-07-07) and [run matrix](sh-run-matrix.md):

Positive witness: same-source roots open at exactly the first recorded field-speed crossing in every row, with sub-field trajectories unchanged to the step — sub-field motion admits no same-source roots, so the sea-screened return turns are self-hit-robust by construction and the click hypothesis's fold-set location gets an executable witness. Negative with mechanism: the partner kernel applied unchanged to same-source rows produces a violent outward ejection at the first click (speeds $0.98\to\approx12\,c_f$ within $0.04$ time units, branch weights to $163$ at the $0.05$ Jacobian floor, final $R\approx32$, $\approx33$k missing partner roots voiding (WP) post-hinge), and the ejection magnitude moves with the regularization parameters (final $v_{\max}$ $12.4/9.8/10.6\,c_f$ across floor $0.05\to0.2$ and min-delay $0.002\to0.05$) — a resolution-limited caustic artifact witnessing the uncontrolled Whitney-fold crossing, not the controlled click. Disposition `naive_self_hit_kernel_ejects_at_hinge_no_absorption_in_toy`: the toy cannot decide the click-absorber question; the producer boundary for `self_hit_held_release_solver_row` sharpens to a regularization-independent fold-crossing treatment (canonical fold-set chart, chart-defined finite click impulse, same-record $h_{\mathrm{act}}$ action-ledger rows) on the central-solver retained-history path. Checker consumption fail-closed on all five rows (pre-crossing margins identical to sea-only; ordering witness consistent).

Validation: `node --test tests/braid-ideal-held-release-causal-wake-toy.test.js` (13 tests, including the new hinge-timing witness) passes; default-run byte-invariance holds apart from the new `firstSelfHitRoot: null` event key (declared, event-provenance addition).

### 2026-07-07 - SH-0-Sea Orientational-Order Condition Derived: Aligned Order Frustrated, Paired-Antiphase Exactly Null, Stochastic Retention Rejected; Induced Polarization Named

Executed the orientational-order closure with the new sibling diagnostic [sh-0-sea-orientation-order-diagnostic.mjs](../../../scripts/braid-ideal/sh-0-sea-orientation-order-diagnostic.mjs) (`sh_0_sea_orientation_order_diagnostic.v0`; tests in [braid-ideal-sh-0-sea-orientation-order-diagnostic.test.js](../../../tests/braid-ideal-sh-0-sea-orientation-order-diagnostic.test.js), 5 passing). Ensembles compared under pairwise potential superposition of declared held braids at the wake-sum kernel constants, zero free amplitude: aligned, conjugate-aligned control, paired-antiphase, and isotropic disorder from a declared seeded uniform $SO(3)$ sampler (seed $20260707$, $200$ samples per spacing).

Hard artifacts (exact): (i) dipole-shell cancellation lemma — for aligned dipoles $p=2(1,1,1)$ the FCC nearest-neighbor shell's point-dipole sum vanishes exactly ($6$ bonds at $\cos^2\theta=2/3$, factor $-1$, against $6$ at $\cos\theta=0$, factor $+1$), so orientation selection sits at higher multipole/near-field order — consistent with the earlier $\approx a_{\mathrm{FCC}}^{-5}$ far-field decay; (ii) paired-antiphase null lemma — conjugating opposite-shell neighbors makes $\Pi_R$ vanish exactly (observed $\le3\times10^{-16}$), so that ensemble can never supply retention; (iii) conjugation sign rule — flipping all neighbors negates $\Pi_R$ exactly, making conjugate-aligned exactly anti-retentive.

Computed results at $a_{\mathrm{FCC}}=4.25$: bond energetics are frustrated (attractive class $-0.433$ prefers aligned, transverse class $+0.577$ prefers flipped; exact negation under flip), the retention-favorable aligned relative orientation is the energetically disfavored one (center-shell $+0.864$ versus $-0.864$ conjugate), and seeded non-uniform samples undercut every uniform ensemble energy ($\min\approx-4.4$ versus aligned $2.59$, conjugate $0.86$, paired $4.61$) — the pair-potential ground state is a non-uniform pattern, so static energetics does not select the aligned order that guaranteed retention requires. Stochastic retention for isotropic disorder fails both declared readings: annealed (fast re-randomization) — ensemble-mean response consistent with zero at every spacing, no mean-response window; quenched (frozen configuration) — crossing probability peaks at $\approx0.43$ near $a_{\mathrm{FCC}}=3.75$ and stays below one half everywhere, no majority-retention window; with a zero mean, repeated re-randomization supplies no net inward bias. Verdict fields: `stochastic_retention_supported=false`.

Proof-state movement: the environment-theorem route (H1) now carries an explicit derived burden — guaranteed sea retention requires central-aligned neighbor dipole order that static pair energetics does not supply and disorder cannot replace; the named remaining environment mechanism is induced sea orientational polarization (neighbor orientations responding to the central braid's wake), which requires a responsive-orientation diagnostic beyond the fixed-source model. Fail-closed throughout: no retained-branch claim, `scoreMovement=no_score_increase`, evidence blocked at `held_release_seed_path_rows_acceptance_certificate.v0`; the analysis is a diagnostic orientational-order condition, not an accepted Noether sea selection rule. Derivation captured in the [model packet](sh-0-sea-diagnostic-candidate-model.md#orientational-order-condition-and-stochastic-retention); summary in the [run matrix](sh-run-matrix.md).

Validation: `node --test tests/braid-ideal-sh-0-sea-orientation-order-diagnostic.test.js` (5/5), full `node --test tests/braid-ideal-*.test.js`, and content checks per the closeout.

### 2026-07-07 - Sea-Screened Held-Release Rows: First Sub-Field Return Turns; No Bounded Window; Checker Consumption Fail-Closed

Executed the dynamical consumption of the computed FCC dipole wake sum at the named sea-spacing candidate $a_{\mathrm{FCC}}=4.25$. [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) gained `--fcc-sea-spacing` and `--fcc-sea-held-window`: when active, the released seed evolves inside the attempt `aa` FCC shell of $72$ held static face-opposite sea sources (aligned orientation, exact static causal roots with $J=1$, receiver-normal branch weights, one-way environment by declared held histories, spacing rejected fail-closed below the $2\sqrt2$ overlap floor). The default run is byte-identical with the flag absent (verified against a pre-change baseline: identical `metrics.csv` and `result.json` modulo timestamp). Release-instant cross-check: the toy reproduces the wake-sum row $\Pi_R=-0.283341788903118$ to $13$ digits in every row.

Result across eight rows (`vt000` control, `vt025`/`vt050` kick, `vt025`-`vt099` moving-prehistory; details in the [run matrix](sh-run-matrix.md) and the new packet section [Sea-Screened Held-Release Rows](sh-0-sea-diagnostic-candidate-model.md#sea-screened-held-release-rows---2026-07-07)): the three previously outward-only rows (`vt080`/`vt095`/`vt099` moving-prehistory) now show an expansion-to-compression return turn at $t=0.176/0.346/0.390$, at $R\approx1.001$-$1.013$, strictly before the field-speed crossing ($t=0.466/0.430/0.422$) — the program's first sub-field return turns, removing the prior all-post-crossing discount. No bounded window: each row then hovers or compresses near $R\approx1$ while the tangential channel drives the sites through $c_f$ (radial support without a tangential absorber — the embedded-environment witness of the same missing-absorber conclusion as the breathing-hunt rejection), and the bounce escapes through the shell; `vt095` at duration $6$ confirms escape. Checker consumption ([delayed-escape-certificate-check.mjs](../../../scripts/braid-ideal/delayed-escape-certificate-check.mjs)) on all eight rows: fail-closed everywhere, signed margins $-468$ to $-489$ on the return-turn rows at unit coupling, ordering witness consistent; the row output carries the explicit caveat that the certificate envelope bounds partner sources only, naming the sea-aware envelope extension as an open escape-lemma obligation. Disposition `sea_screened_return_turns_without_bounded_window`; fail-closed throughout; no retained branch, no stability claim, no score movement.

Validation: `node --test tests/braid-ideal-held-release-causal-wake-toy.test.js` (12 tests, including the wake-sum release cross-check, sub-field return-turn witness, default sea-absence invariance, and overlap-floor rejection) passes.

### 2026-07-07 - Independent Verification of the Planar Tangential Screen Interval Certificate

Independent verification pass over the executed interval certificate in [planar-tangential-screen-interval-certificate.mjs](../../../scripts/braid-ideal/planar-tangential-screen-interval-certificate.mjs), performed in a separate thread from the executing one. Checks: the test suite passes (3/3); a deterministic rerun reproduces the recorded constants exactly ($c_1=2.8814045$, $c_2=2.9241856$, radial upper bound $-0.6724697$, source-normal floor $0.6934749$, $10330$ boxes, zero failures); a dense independent cross-check of the sampled scan (1931 points of $\Phi_{\mathrm{tan}}(\beta)/\beta$ over $[0.02,0.985]$, range $[2.8876,2.9185]$) lies strictly inside the certified band with proper outward slack, and the sampled radial maximum and minimum source-normal respect the certified rows; the certified-clock arithmetic $\ln(0.985/0.02)/c_1=1.35243\le1.353$ is confirmed; and a soundness read of the interval core found outward widening on every operation, extrema-aware sine enclosures with trig padding and clamping, zero-rejecting interval division, enclosed $\pi$ and degree conversion, and a lag residual matching the sampled scan's planar geometry exactly, including the unfloored source-normal convention with the separate certified no-engagement row for $J_f=0.05$. No defects found; no files changed by the verification. The certificate's claim level (rigorous conditional on the declared rounding model) and fail-closed rows are as recorded.

### 2026-07-07 - SH-0-Sea Delayed-Echo Wake Sum: Phase-Dependent Window Confirmed; Orientation Order Named; Candidate Fed To Same-Record Chain

Executed the delayed-echo follow-on to the computed dipole wake sum. [sh-0-sea-diagnostic-candidate-model.mjs](../../../scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs) gains `--wake-sum-run --neighbor-motion=breathing|orbiting`: the 12 FCC neighbor braids carry declared moving held histories (common phase; breathing $\delta=0.2$, $\Omega\in\{1,2,4\}$; orbiting about a declared axis, default $\hat z$, $\omega\in\{0.25,0.5,0.75\}$), causal roots solved per directed pair by bisection of the strictly increasing causal residual against the declared window $W=24$, branch weight $|1/\max(J,0.05)|$ with $J=(c_f-\mathbf v_s\cdot\hat{\mathbf n})/c_f$ per the toy kernel. Motion parameters are declared history parameters, not response amplitudes; `free_amplitude_parameter_count=0` everywhere; the $\delta\to0$ limit reproduces the static sum exactly (tested); super-field-speed motion rows are excluded fail-closed (tested).

Result: H5's phase-dependence prediction is confirmed at the declared level. The static boundary $5.3469$ splits into a guaranteed (all-phase) and a phase-conditional boundary: breathing $\Omega=1$: $5.022/5.908$; $\Omega=2$: $4.948/6.253$; $\Omega=4$: $3.512$ guaranteed while the phase-conditional window extends past the entire declared range, with sign-reversing phase spread $1.33$ at the candidate spacing (all-phase crossing fails at $4.25$, some-phase holds). Orbiting about $\hat z$: no guaranteed window at any tested $\omega$ — the phase-mean at $4.25$ collapses to $-0.0944$ as the rotating braid dipole averages away — while the phase-conditional boundary grows to $5.946/6.869/8.223$. Insight worth keeping: the sea's retention supply is phase- and orientation-dependent; dipole-aligned neighbor order (static or breathing) yields a guaranteed retention window, freely reorienting neighbors yield only phase-conditional retention. Sea orientational order is therefore a named condition on the environment-theorem (H1) route — the delayed-echo structure is real, and the next theory object it points at is the orientation ensemble the Noether sea actually supplies.

Candidate feed: both wake-sum runs now emit `sh_0_sea_candidate_same_record_request.v0`, binding the named $a_{\mathrm{FCC}}=4.25$ candidate (identity, kernel, held-history and motion declarations, target binding, geometry carrier ref) to the required same-record objects (seed-path acceptance certificate, external authority package, repo authorization, retained-source adapter package, receiver-normal root-detail rows, action closure, accepted sea-response row at the candidate spacing) and its downstream consumers (`self_hit_held_release_solver_row`, `native_retained_history_promotion`, SH-0-sea same-record rows). The request is fail-closed at `held_release_seed_path_rows_acceptance_certificate.v0` and authorizes nothing; no queue item is added because the consumption burden already lives with the existing Group A rows.

Fail-closed discipline: every motion output keeps `accepted=false`, no retained-branch claim, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no central-solver retained-history acceptance. Tests extended to 19 passing in [braid-ideal-sh-0-sea-diagnostic-candidate-model.test.js](../../../tests/braid-ideal-sh-0-sea-diagnostic-candidate-model.test.js). Results recorded in the [Shell-Braid Run Matrix](sh-run-matrix.md) SH-0-sea section and the [model packet](sh-0-sea-diagnostic-candidate-model.md) Delayed-Echo Motion Result section.

Validation: `node --test tests/braid-ideal-sh-0-sea-diagnostic-candidate-model.test.js` (19/19), full `node --test tests/braid-ideal-*.test.js`, and content checks per the closeout.

### 2026-07-07 - Planar Tangential Screen Interval-Certified: $2.881\,\beta\le\Phi_{\mathrm{tan}}\le2.925\,\beta$; (S)-Failure Becomes a Certified Clock on the Rigid Channel

Executed the directed-rounding interval certification whose target and burdens were staged in the [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md) Interval Certification Home section, closing the packet's interval-certification obligation. New executable [planar-tangential-screen-interval-certificate.mjs](../../../scripts/braid-ideal/planar-tangential-screen-interval-certificate.mjs) (tests in [braid-ideal-planar-tangential-screen-interval-certificate.test.js](../../../tests/braid-ideal-planar-tangential-screen-interval-certificate.test.js), 3 tests) implements outward-rounded interval arithmetic ($10^{-13}$ relative per operation, $10^{-12}$ absolute on sin/cos, clamped) over the exact planar-hexagon rotating-channel residual of [axis-neutral-rotating-wave-residual-scan.mjs](../../../scripts/braid-ideal/axis-neutral-rotating-wave-residual-scan.mjs): certified constant-lag enclosures per directed pair (bisection with conservative sign tests; indeterminate midpoints contracted through the $\lvert G'\rvert\ge1-\beta_{\mathrm{hi}}$ transversality floor of Lemma 5), then adaptive $\beta$-subdivision accepting a box only when the tangential enclosure is positive, the radial enclosure is negative, and the enclosure is tight to the declared tolerance. Result over $\beta\in[0.02,0.985]$ (10330 boxes, zero unresolved): certified band $2.881\,\beta\le\Phi_{\mathrm{tan}}(\beta)\le2.925\,\beta$ (computed conservative constants $2.8814$/$2.9242$, recorded outward), certified radial sign $\Phi_{\mathrm{rad}}\le-0.672$, and certified source-normal floor $\ge0.693\,c_f$ — so the sampled scan's unfloored kernel is the floored kernel on the whole interval ($J_f=0.05$ provably never engages), and interval-certification burdens 1 and 3-4 are discharged (burden 2 was discharged by Theorem M; burden 5 maintained: all constants same-record, no cross-chart imports). Downstream consequence recorded in the packet as the certified-clock corollary: $d\beta/dt\ge c_1\kappa\beta/(c_f\rho^2)$ on the rigid channel, so an isolated rigid rotating row reaches the certified ceiling from any $\beta_0\ge0.02$ within $\Delta t\le(c_f\rho^2/(c_1\kappa))\ln(0.985/\beta_0)\le1.353\,c_f\rho^2/\kappa$ — (S)-failure upgraded from sampled trend to certified clock, conditional on the labeled rigid-to-released transfer hypothesis, which replaces the executed obligation in the packet's remaining-obligations list. The anti-damping remark and queue notes now cite the certified band. Claim level: rigorous interval certificate conditional on the declared rounding model; no retained-branch claim, no accepted evidence, `scoreMovement=no_score_increase`.

Validation: `node --test tests/braid-ideal-planar-tangential-screen-interval-certificate.test.js` (3 tests) and `node scripts/validate-content.mjs --check --strict` pass; certificate runtime $\approx0.3$ s.

### 2026-07-07 - Root-Count Persistence Theorem: (M) Discharged Under (S); Persistence Burden Reduces to (S) Alone

Closed the (M) half of the escape-certificate persistence obligation in the [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md), with a stronger result than the targeted monotone-separation form — no separation condition is needed. Lemma 5 (causal-root uniqueness and transversality): under the (S) sub-field cap alone, the causal residual $\varphi_T(s)=\|\mathbf X_\ell(T)-\mathbf X_{\ell'}(s)\|-c_f(T-s)$ is strictly increasing in emission time with rate $\ge(1-\beta)c_f$ (reverse triangle inequality plus the Lipschitz speed cap), so each directed pair retains at most one causal root anywhere in the sub-field regime — root bifurcation is impossible, every retained root is transversal with source-normal Jacobian $\ge1-\beta$ above any declared floor $J_f\le1-\beta$, and the root map is locally Lipschitz with $dt_r/dT\in[(1-\beta)/(1+\beta),(1+\beta)/(1-\beta)]$, discharging the root-side of (WP). Lemma 6 (coverage persistence): $\varphi_T(-h)$ is strictly decreasing in reception time under the receiver-side cap, so root coverage seeded at $T_0$ (the checkable window-depth inequality, recorded as zero missing roots) persists while (S) holds, giving exactly one root per pair. Theorem M: (M) holds with $m=1$ as a theorem inside every (S)-valid window, $K_{\mathrm{sgn}}=\kappa W_{\max}(1+\beta)^2(\tfrac14+2/\gamma_{\mathrm{op}}^2)$ with the root-count factor eliminated, and the certificate's hypothesis-persistence burden reduces to (S) alone — (D$_{\mathrm{op}}$) persists with the channel by Lemma A, (M) persists under (S) by Lemmas 5-6. Interval-certification burden 2 of the packet (certified root-count completeness) is discharged by the same theorem. Converse discipline recorded: once (S) fails the protection lapses (super-field source segments can fold the residual), and any sub-field multi-root or missing-root event is a runner audit failure in the same class as $\operatorname{Fix}(G)$ drift.

Executable witness: all eleven 2026-07-07 rows record `maxRootsPerDirectedPair=1`, `missingRoots=0`, and `smallJacobianRoots=0` over their entire runs — including post-crossing segments beyond the theorem's guarantee zone — so the checker ([delayed-escape-certificate-check.mjs](../../../scripts/braid-ideal/delayed-escape-certificate-check.mjs), schema `braid-ideal-delayed-escape-certificate-check.v3`) now consumes $m=1$ with provenance `theorem_M_confirmed_by_record` and falls back to the measured cap with an unresolved-timing flag only if a record ever shows a larger global max. Margins are numerically unchanged (the measured cap was already one); the movement is proof-state: (M) is no longer a monitored hypothesis. All rows remain fail-closed; tests extended for the provenance fields.

Validation: `node --test tests/braid-ideal-delayed-escape-certificate-check.test.js tests/braid-ideal-held-release-causal-wake-toy.test.js` passed; `git diff --check` clean on scoped files.

### 2026-07-07 - SH-0-Sea Dipole Wake Sum Computed: Fitted Amplitude Removed; Retention Window $a_{\mathrm{FCC}}\in[3,5.3469]$ With Zero Free Amplitude

Executed Group A `sh0sea_dipole_wake_sum`. [sh-0-sea-diagnostic-candidate-model.mjs](../../../scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs) no longer carries a fitted response amplitude anywhere in the output path: the provider-seeded $K\Phi$ probe, the produced `pressure_tension`/`boundary_wake` amplitude rows, and every fitted CLI knob (`--response-amplitude`, `--produced-source-phi`, and relatives) are removed, and the new `--wake-sum-run` mode computes the sea response as the master-equation-kernel delayed sum over the 12 FCC nearest-neighbor braids with declared held histories: face-opposite six-site decorations (signed-polarity dipole $p=2(1,1,1)$, $|p|=2\sqrt3$, aligned orientation declared), static over the declared window $W=24$, kernel constants identical to the escape-floor toy (coupling $1$, softening $0.05$, $c_f=1$, branch weight $1$ for held static sources against release-time static receivers), no undeclared environment degrees of freedom, `free_amplitude_parameter_count=0` at every output level.

Result (moves proof state on the success condition): across the declared spacing range $a_{\mathrm{FCC}}\in[3,12]$ (step $0.25$; all $432$ directed roots covered per spacing; field speed clean; every spacing above the shell-overlap constraint $2\sqrt2$), the computed projection $\Pi_R\mathcal A^{\mathrm{sea}}$ is inward at every spacing and crosses the same-record escape floor $-0.0934863484737535$ (deadband $10^{-9}$) for $a_{\mathrm{FCC}}\le5.34690143$ — a computed retention window with zero free amplitude. Sample rows: $-1.0814$ at $3$, $-0.3712$ at $4$, $-0.1025$ at $5.25$, $-0.0811$ at $5.5$; far-field decay $\approx a_{\mathrm{FCC}}^{-5}$ (observed). Named sea-spacing candidate for downstream same-record work: `sh0sea-aa-fcc-dipole-wake-sum:a-fcc-4.25` ($\Pi_R=-0.2833$, inward margin $0.1899$). Hypothesis reading: executable support for H1 (retention as an environment theorem) at the declared held-history level; H5 (delayed echo) is only partially tested — static held histories give a monotone amplitude-like spacing threshold, so the predicted density/phase-dependent structure still needs a moving-neighbor-history variant. The prior fitted-amplitude crossing reading ($\Phi_{\mathrm{probe}}=0.038$) is superseded by the computed result rather than withdrawn: the environment can supply the required inward projection without any fitted parameter.

Fail-closed discipline preserved: run, source-row, spacing-row, retention-window, and requirement outputs all keep `accepted=false`, no retained-branch claim, `scoreMovement=no_score_increase`, no accepted seed-path certificate, and no central-solver retained-history acceptance; the accepted-provenance replacement requirement now binds to the computed `dipole_wake_sum` source row (`sh_0_sea_dipole_wake_sum_source:2f3aad5e6cced01f`) with the same staged blocker chain, first missing object unchanged at `held_release_seed_path_rows_acceptance_certificate.v0`. Root-coverage loss truncates the retention window fail-closed (`truncated_by_root_coverage`), verified by test. Tests rewritten in [braid-ideal-sh-0-sea-diagnostic-candidate-model.test.js](../../../tests/braid-ideal-sh-0-sea-diagnostic-candidate-model.test.js) (15 passing), including explicit fitted-path-gone assertions (removed exports absent; legacy fitted CLI flags produce byte-identical output; no `Phi_probe`/produced-amplitude fields in the output).

Queue update: `sh0sea_dipole_wake_sum` removed from Group A and remaining items renumbered; results recorded in the [Shell-Braid Run Matrix](sh-run-matrix.md) SH-0-sea section and the [SH-0-Sea Diagnostic Candidate Model packet](sh-0-sea-diagnostic-candidate-model.md). Next closure candidates: the moving-neighbor-history delayed-echo variant of this run, and consumption of the named $a_{\mathrm{FCC}}=4.25$ candidate by the same-record chain (`self_hit_held_release_solver_row` / retained-source acquisition).

Validation: `node --test tests/braid-ideal-sh-0-sea-diagnostic-candidate-model.test.js` (15/15), full `node --test tests/braid-ideal-*.test.js`, and content checks per the closeout.

### 2026-07-07 - Escape-Certificate Packet Completed: Interval-Certification Home, Step-Label Ledger, Promotion Classification

Closed out the `delayed_escape_certificate_lemma` packet obligations that remained after the statement/proof and signed-sharpening passes. The [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md) now carries: an explicit step-label ledger (hypothesis rows (C), (WP), (S), (D$_{\mathrm{op}}$), (P), (M), (W), (H); Lemmas 1-4, Lemma A, Theorem, Corollary as derivations; the anti-damping remark as sampled-diagnostic consumption; persistence routes as speculation); the new Interval Certification Home section making the packet the owner of the planar-tangential-screen upgrade from sampled diagnostic ($\Phi_{\mathrm{tan}}\approx2.9\beta$) to certified inequality — target band $c_1\beta\le\Phi_{\mathrm{tan}}(\beta)\le c_2\beta$ with five labeled burdens (certified constant-lag enclosures via the $c_f(1-\beta)$ transversality floor; root-count completeness pinning $m=1$; outward-rounded residual evaluation; finite $\beta$-subdivision with the ratio-bound endpoint discipline near $\beta=0$; same-record constant discipline), no new validator or schema required; and a Promotion Classification naming the candidate corpus destinations (Symmetric Shell Braid return-response section; nested-shell-braid-dynamics theorem-target restatement) behind the operator-review blocker. The dominate-or-exploit routing of the anti-damping driving term is recorded exactly: the radial envelope never needs to dominate $\Phi_{\mathrm{tan}}$ (orthogonal in Lemma 1 — the exploit branch), and the domination question lives entirely in (S)-persistence under the labeled rigid-to-released transfer hypothesis. Independent proof audit of the signed packet performed in this thread: Lemma A's floor ($4\|\mathbf x_\parallel\|^2+\|\mathbf x_\perp\|^2\ge R^2$, $\gamma_{\mathrm{op}}=\sqrt2$ on the reference seed), the (P)-secured same-polarity radial-sign discard, the antipodal $2R$ row, the $K_{\mathrm{sgn}}/K_{\mathrm{iso}}$ window $[0.466,0.498]$ at $\gamma_{\mathrm{op}}\in[1.15,1.40]$, and the energy-comparison bootstrap all reverified; terminology normalized to causal-root wording per canon. Fail-closed status unchanged: no retained-branch claim, no accepted evidence, `scoreMovement=no_score_increase`.

### 2026-07-07 - Signed Envelope Sharpening: Polarity-Resolved Certificate Constant Halves the Margin Requirement

Sharpened the delayed escape certificate's envelope constant on the symmetric channel, replacing the isotropic $K_{\mathrm{iso}}=5m\kappa W_{\max}(1+\beta)^2/\gamma^2$ bound with the polarity-resolved $K_{\mathrm{sgn}}=m\kappa W_{\max}(1+\beta)^2(\tfrac14+2/\gamma_{\mathrm{op}}^2)$ in the [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md). Three proof pieces: (i) same-polarity partner rows are discarded by an exact radial-sign argument — their radial force component is $+\kappa W(R-\hat{\mathbf x}\cdot\mathbf X_{\mathrm{src}}(t_r))/(d^2+\varepsilon^2)^{3/2}\ge0$ whenever past radii do not exceed the current radius, secured by the new checkable past-radius cap (P) plus the in-window monotonicity supplied by the bootstrap; (ii) the antipodal row is controlled by its exact $2R$ separation from $\iota$-invariance; (iii) new Lemma A (channel separation floor) proves every opposite-polarity non-antipodal separation on $\operatorname{Fix}(C_3\times\langle\iota\rangle)$ equals $\sqrt{4\|\mathbf x_\parallel\|^2+\|\mathbf x_\perp\|^2}\ge R$, with equality exactly on planar configurations, so $(D_{\mathrm{op}})$ holds unconditionally at $\gamma_{\mathrm{op}}=1$ and no same-polarity separation hypothesis appears anywhere in the signed certificate. Consequences: the same-polarity close-pass channel (recorded at $\gamma_{\mathrm{same}}\approx3.5\times10^{-3}$ in the `vt000` control, where $K_{\mathrm{iso}}$ diverges) no longer threatens the envelope, and the $(D_{\mathrm{op}})$ persistence burden collapses into channel persistence, leaving (S) and (M) as the only open persistence obligations.

Checker rerun ([delayed-escape-certificate-check.mjs](../../../scripts/braid-ideal/delayed-escape-certificate-check.mjs), schema bumped to `braid-ideal-delayed-escape-certificate-check.v2` with a dual-envelope ledger carrying $\gamma_{\mathrm{op}}$, the (P) check, the sharpening ratio, and channel-floor consistency; tests extended to verify both envelope compositions and the strict sharpening): across the seven rows with admissible certificate times, $K_{\mathrm{sgn}}/K_{\mathrm{iso}}\in[0.466,0.498]$ — the signed envelope closes $50.2\%$-$53.4\%$ of the margin requirement per row (signed requirements $2K_{\mathrm{sgn}}/R_0\in[402,565]$ versus isotropic $[807,1214]$). All eleven rows remain fail-closed (`windowCertificateGranted=false`, `eternalNoReturnCertificate=false`); the measured full-record opposite-polarity floor is $\gamma_{\mathrm{op}}\ge1.0000$ in every row, with the Lemma A equality value attained as trajectories traverse planarity — the floor is tight. The remaining deficit driver is $W_{\max}=(1+\beta)/\max(J_f,1-\beta)\approx40$ as $\beta\to1$, which no separation-side sharpening removes; at $\beta\lesssim0.5$ the signed requirement is $O(30\kappa/R_0)$, halving the certifiable-coupling threshold.

Validation: `node --test tests/braid-ideal-*.test.js` passed and `git diff --check` clean on scoped files.

### 2026-07-07 - Native Central-Solver Kernel Equivariance Audit Complete: Both Lemma Actions Preserved; Lemma Cleared For Native Records At The Equivariance Obligation

Completed the Group B `six_point_symmetry_invariant_lemma` follow-up: a read-only equivariance audit of the native central-solver kernel against the two lemma actions, $S_3\times\langle\iota\rangle$ and $C_3\times\langle\iota\rangle$, recorded in the new Native Central-Solver Kernel Equivariance Audit section of the [Six-Point Symmetry Invariant Lemma Proof Packet](six-point-symmetry-invariant-lemma-proof-packet.md). Seven retained-history force-row paths were audited with exact source locations: causal-root search for all three source-history forms plus bracketing, bisection, and time-proximity dedupe (`src/solver/src/CausalRootSolver.cpp`); receiver-normal-over-source-normal branch weighting (`compute_receiver_normal_fields`); delay evaluation including the delayed-potential fixed-point path (`src/solver/src/Geometry.cpp`); same-source classification (`src/solver/src/RootLedger.cpp`); retained-root ledger row construction (no pruning, invariant sort keys); the MotionSampler causal-delay pair force law (single-root selection by invariant scalars; polarity only through the charge-product sign, exactly $\sigma_i\sigma_j$ for unit polarity charges; isotropic softening); and delayed-hit row emission (polarity-neutral unsigned strength with both signed polarity units carried in the bridge metadata schema for same-record consumers). Verdict: equivariant under both actions on every audited path in exact arithmetic; no non-equivariant force-row code path exists; polarity enters only through $\sigma_i\sigma_j$ or is deferred to the same-record consumer.

Documented convention mismatches (not equivariance defects): status-code handling of small source-normal denominators plus consumer-side magnitude floors instead of a sign-preserving Jacobian floor (including the delayed-potential $\max(0.08,|\kappa|)$ floor); single-root selection versus the packet's all-root sum (coincident on the (A3) unique-root window, owned by the obligation-3 ledger blocker in multi-root windows); branch weight absent from the integrated pair force (delayed source velocity zeroed); and the floating-point ordering caveat (pathKey-ordered source sums, fixed component order) that limits bitwise — not exact-arithmetic — equivariance, per Corollary 5. Named non-equivariant surfaces excluded from lemma scope with locations: the axis-fixed `comparison_b` cross product (`src/solver/src/SolverCAbi.cpp` lines 582-584 and the `SolverAppBridge.mjs` mirror), the single-sided observer-field polarity factor (field-level convention; receiver polarity unit must bind from the same record), the Animator delayed-hit display adapter (source-normal speed hard-coded to zero), and T3 periodic-box stepping (already a named negative control).

Proof-state movement: the success condition holds — the staged lemma is cleared for native-record application at the kernel-equivariance obligation. Queue update: `six_point_symmetry_invariant_lemma` moved to `staged-native-kernel-audit-complete`; the remaining native-application blocker is the same-record binding chain by design (retained record, source row, provider object, receiver-normal branch-strength rows), still fail-closed at the retained-root-ledger and `retained_record_id`/`provider_object_ref` fields. No retained-branch claim, no accepted-evidence claim, no authorization flag changes, `scoreMovement` remains `no_score_increase`; solver sources untouched (read-only audit).

Validation: `node --test tests/braid-ideal-six-point-symmetry-invariant-lemma-row.test.js` passed; owner-row CLI with the packet proof ref validates and fails closed at the retained-root ledger; `node scripts/check-receiver-normal-clean-slate.mjs` passed.

### 2026-07-07 - Breathing-Ansatz Eigen-Braid Hunt: No Zero-Average Row; Pump Suppression Floors Near 3/4

Executed the two-frequency breathing hunt per the [Axis-Neutral Rotating-Wave Spectrum Packet](axis-neutral-rotating-wave-spectrum-packet.md) next steps via the new sibling diagnostic [axis-neutral-breathing-residual-scan.mjs](../../../scripts/braid-ideal/axis-neutral-breathing-residual-scan.mjs) with tests in [braid-ideal-axis-neutral-breathing-residual-scan.test.js](../../../tests/braid-ideal-axis-neutral-breathing-residual-scan.test.js) (seven tests passing, including exact $\delta\to0$ agreement with the rigid scan). Formulation results recorded in the packet: Lemma R2 (periodic-lag reduction — co-rotating residuals depend on time only through the breathing phase, unique smooth periodic delays under sub-field speeds, cycle averages independent of $\phi_0$, so $\phi_0$ is quotiented out of the box) and the pointwise extension of the axial no-balance lemma to common-phase radial breathing (candidates again forced planar; grid witness max $-0.128$).

Hunt result (kernel conventions identical to the first hunt; $c_f=1$, $\kappa=1$, receiver-normal over floored source-normal weights, partner-wake only, no self-hits; fail-closed throughout): the cycle-averaged tangential pump is strictly positive at every admissible cell of the declared planar box $\beta\in[0.05,0.9]$, $\delta\in[0.05,0.45]$, $\Omega/\omega\in[0.25,4]$ (1206 cells) and of the extended delay-resonant probe $\delta\le0.8$, $\Omega\le8$ (467 cells), with zero sign changes on all axes and no candidate row. Sharper mechanism: breathing suppresses the pump monotonically in the breathing rim velocity $\delta\Omega$ but floors at $\approx0.73$ of the rigid pump at the sub-field admissibility edge, nearly $\beta$-independent, so the surviving driving term is $\approx2.1\beta$; the instantaneous pump reverses on about a third of the cycle without ever reversing on average. Disposition `breathing_family_no_zero_average_row_in_scanned_box` — packet failure mode 1 extends from the rigid family to the common-phase constant-$\omega$ breathing family, and the edge-limited suppression relocates any surviving breathing zero into hinge/self-hit territory (failure mode 2 flavor). No row named; `native_retained_history_promotion` remains gated; the lissajou-lane figure catalog remains blocked on admissible rows. Remaining untested enlargements are listed in the packet (constant-angular-momentum breathing, axial-height breathing, symmetry-broken per-ring modes, first-harmonic radial balance).

Queue update: `breathing_ansatz_eigen_braid_hunt` removed from Group A and remaining items renumbered. Validation: `node --test tests/braid-ideal-axis-neutral-breathing-residual-scan.test.js` and the prior rigid-scan test file pass; the rigid scan script is untouched.

### 2026-07-07 - Delayed Escape Certificate Lemma Stated and Proved; Eleven-Row Witness Consumption Fail-Closed

Drafted the [Delayed Escape Certificate Lemma Proof Packet](delayed-escape-certificate-lemma-proof-packet.md), discharging ranked proofing action 3 of the [Six-Point Equivariant Reduction Proof Audit](six-point-equivariant-reduction-proof-audit-2026-07-06.md) at priority level. Structure: Lemma 1 (reduced-radius identity $\ddot R=\hat{\mathbf x}\cdot\mathbf a+\|\mathbf v_\perp\|^2/R$ with nonnegative centrifugal sign on the common-radius channel), Lemma 2 (causal-root distance floor $d\ge D/(1+\beta)$ from the sub-field-speed cap alone, replacing the audit's memory-depth monotonicity hypothesis), Lemma 3 (branch-weight cap $W_{\max}=(1+\beta)/\max(J_f,1-\beta)$), Lemma 4 (derived delayed inward-force envelope $\ddot R\ge-K/R^2$ with $K=5m\kappa W_{\max}(1+\beta)^2/\gamma^2$), the certificate theorem (energy-comparison bootstrap: outward $\dot R(T_0)=v_0$ with margin $v_0^2>2K/R(T_0)$ implies no return turn before the first violation of the monitored hypotheses, and eternal escape with $\dot R\ge\sqrt{v_0^2-2K/R_0}$ if none occurs), and the axis-neutral rotating corollary (identical inequalities for the $C_3\times\langle\iota\rangle$ channel; the tangential anti-damping pump has favorable sign for escape and threatens only the sub-field-speed hypothesis). Key consequence: a return turn can never be the first event; it must be preceded by a declared hypothesis violation.

Executable witness consumption: new owner checker [delayed-escape-certificate-check.mjs](../../../scripts/braid-ideal/delayed-escape-certificate-check.mjs) with tests in [braid-ideal-delayed-escape-certificate-check.test.js](../../../tests/braid-ideal-delayed-escape-certificate-check.test.js) (3 tests) evaluated all eleven 2026-07-07 rows (`vt000` control plus the ten declared sweep rows). All eleven fail closed: compression-first rows admit no certificate time before the field-speed crossing; the outward-only rows carry $3$-$26$ admissible candidates but fail the margin inequality decisively ($v_0^2\le2.5\times10^{-2}$ versus $2K/R_0\in[8.1\times10^2,1.2\times10^3]$; deficits $-807$ to $-1214$), driven by unit coupling and $\beta\to1$ near the crossing. The theorem's ordering statement is witnessed consistently in every row, measured branch weights sit inside the Lemma 3 cap, `windowCertificateGranted=false` and `eternalNoReturnCertificate=false` everywhere, `retainedBranchClaim=false`, `scoreMovement=no_score_increase`. Certifiable margins are expected only for weaker effective coupling, larger release radius, or sea-screened kernels; the checker is the object those downstream rows (`sh0sea_dipole_wake_sum`, sea-stabilized rows) should call.

Queue update: `delayed_escape_certificate_lemma` moved to `staged-awaiting-operator-review`. Remaining obligations: operator review, hypothesis-persistence lemma for the infinite-horizon branch, signed sharpening of $K$, and interval certification of the planar tangential screen inside this framework.

Validation: `node --test tests/braid-ideal-*.test.js` passed (209 tests) and `git diff --check` clean on scoped files.

### 2026-07-07 - Capped Axis-Neutral Held-Release Angular-Momentum Sweep Executed: All Ten Rows All-Outward

Implemented the capped sweep per the [Angular-Momentum Held-Release Sweep Spec](angular-momentum-held-release-sweep-spec.md) (implementation checklist items 1-10) and the capped-witness discipline of the [Six-Point Equivariant Reduction Proof Audit](six-point-equivariant-reduction-proof-audit-2026-07-06.md). [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) gained `--surface-speed-fraction` ($v_t=f_v c_f$), `--spin-axis` (default `1,1,1`, normalized to $\hat{\mathbf n}$), and `--prehistory-mode` (`stationary-held-release` default, `kick-at-release`, `moving-prehistory`). `kick-at-release` keeps the static hold path and applies the rigid-rotation release velocity $\mathbf v_i(0)=\omega\hat{\mathbf n}\times\mathbf x_i(0)$; `moving-prehistory` writes the hold-window source path history as $\operatorname{Rot}(\hat{\mathbf n},\omega t)\mathbf x_i(0)$ in chord-accuracy samples consumed by the existing causal-root search, branch weighting, and reduced-radius paths, and releases with the exactly matched derivative (measured position/velocity release jump `0` in every row, answering spec proofing question 2). New per-run metadata: surface-speed fraction, actual tangential speed, angular rate, normalized spin axis, prehistory mode, kinematic $\mathbf J_{\mathrm{kin}}=\sum_i\mathbf x_i\times\mathbf v_i$ (diagnostic bookkeeping only; no physical mass), hold-history representation, and release-continuity residuals. New per-run diagnostics: reduced-radius sign sequence and $\operatorname{Fix}(G)$-drift residual (cyclic $C_3$ position/velocity residuals plus antipodal pair opposition; labeled a runner/root-selection defect channel, not a physical signal). The default run is preserved exactly: byte-identical `metrics.csv` and identical frames, events, root stats, and turn rows against a pre-change baseline; `stationary-held-release` rejects nonzero $f_v$ fail-closed.

Sweep result (toy defaults, $c_f=1$, coupling 1, ten declared rows, both prehistory modes, all fail-closed, `retainedBranchClaim=false`, `scoreMovement=no_score_increase`): no row shows a second turn or a bounded window; causal-root coverage is clean (zero missing roots) in every row; $\operatorname{Fix}(G)$ drift is bounded at $\le 5\times10^{-15}$. `vt025`/`vt050` compress once and escape (first turns at $t\approx0.90$ to $t\approx1.05$); `vt080`/`vt095`/`vt099` never compress (outward-only from release, minimum reduced radius `1.0`). The compression boundary matches the release balance of $v_t^2/R$ against the net inward radial release acceleration $\approx0.25$ on the unit fixture, giving marginality at $f_v\approx0.5$. Post-first-turn inward reduced-radius acceleration rows do appear at `vt025`/`vt050`, but the `vt000` zero-angular-momentum control shows the same signature and all such rows sit after the field-speed crossing (every row crosses $c_f$ between $t=0.14$ and $t=0.73$), so no candidate row is named and `native_retained_history_promotion` remains gated. Per-row numbers are recorded in the [Shell-Braid Run Matrix](sh-run-matrix.md).

Proof-state movement: the all-outward failure condition holds across the full axis-neutral rotating family, raising the all-outward prior and feeding the strengthening of the no-return theorem from zero-angular-momentum release to axis-neutral rotating release under partner-wake-only assumptions (spec proofing question 4), to be consumed by `delayed_escape_certificate_lemma`. Queue update: `angular_momentum_held_release_sweep` removed from the Group A queue and remaining items renumbered.

Validation: `node --test tests/braid-ideal-held-release-causal-wake-toy.test.js` (9 tests, including new metadata round-trip, moving-prehistory release-continuity, default-invariance, and fail-closed rejection tests) and the full `node --test tests/braid-ideal-*.test.js` suite passed; `git diff --check` clean on scoped files.

### 2026-07-06 - First Eigen-Braid Hunt: Rigid Rotating-Wave Family Closed By Rejection In Scan

Executed the first numerical hunt for the $u=0$ rotating-wave spectrum via the new priority diagnostic [axis-neutral-rotating-wave-residual-scan.mjs](../../../scripts/braid-ideal/axis-neutral-rotating-wave-residual-scan.mjs) with tests in [braid-ideal-axis-neutral-rotating-wave-residual-scan.test.js](../../../tests/braid-ideal-axis-neutral-rotating-wave-residual-scan.test.js) (four tests passing). Two results, recorded in [Axis-Neutral Rotating-Wave Spectrum Packet](axis-neutral-rotating-wave-spectrum-packet.md): the axial no-balance lemma (analytic sign argument: rigid two-ring rotating waves have strictly negative axial residual for any $\alpha>0$; grid witness max $-0.159$), which forces any rigid single-frequency candidate to the planar hexagon; and the planar tangential anti-damping screen (tangential residual strictly positive over $\beta\in[0.02,0.985]$, $\approx2.9\beta$, zero sign changes), so the wake pumps the rotation and no rigid rotating-wave equilibrium exists in the scanned sub-field range. Disposition `rigid_u0_rotating_wave_family_no_admissible_row_in_scan`; fail-closed throughout; no retained-branch claim. Third independent chart now showing the anti-damping sign phenomenon (after the frozen octahedral zero-mean obstruction and the held-release escape), logged as qualitative consilience only. Program consequence: the eigen-braid hunt moves to the non-rigid breathing (two-frequency, Lissajous-class) ansatz, with the hinge self-hit and Noether sea routes as the alternative absorbers of the pumped tangential action.

### 2026-07-06 - Downstream Cascade And Consilience Principle

Began the cross-directory cascade of braid-scene insights at operator direction. [black-holes.md](../../../content/markdown/aaa/spacetime/black-holes.md) Horizon Interface section now carries the dipole-quiet horizon hypothesis (alignment condition = vanishing axial polarity dipole; entropy label counting enumerates the surviving higher-moment and phase content; precession and dipole extinguish together), with the braid-scene evidence boundary inherited. [particle-masses.md](../../../content/markdown/aaa/assemblies/particle-masses.md) exposed-response section now carries the dressing quietness ladder as the candidate mechanism setting $\zeta(A)$ and $\mathcal Z_{\mathrm{tf}}^{ab}(A)$ for dressed fermions, labeled routing hypothesis. [noether-braid.md](../../../content/markdown/aaa/noether-braid/noether-braid.md) now states the consilience working principle: multi-route convergences are treated as seams of the ontology, unified mechanisms must earn one new prediction, and stubborn convergences mark missing parts. Noted during the pass: the corpus already declares the photon carrier as a coaxial contra-rotating pro/anti planar pair (black-holes.md Horizon-Adjacent Photon Channel), so the speed-budget composite-photon argument independently supports existing canon rather than proposing new structure — logged as a consilience instance. Remaining cascade targets staged: singularity-resolution.md (maximum-curvature core vs dipole-quiet limit), the photon chapters (speed-budget constraint and rotation-sense polarization algebra), lorentz-kinematics.md (exact quadrature channel), quantum statistics chapters (click ensembles), and theory-bridges ('t Hooft cogwheel comparison).

### 2026-07-06 - Operator Batch Integration: Corrections, Speed Budget, Spectrum Packet, Lissajou Lane

Integrated the operator's brainstorm batch. Corpus corrections and promotions: comparison-map clarification for polarity exchange and the per-receiver versus collective-moment cancellation clarification in [shell-braid.md](../../../content/markdown/aaa/noether-braid/symmetric-shell-braid.md); new `Exact Speed Budget on the Channel` section there (exact quadrature $\beta^2c_f^2=u^2+v_t^2$ on the axis-neutral channel; light-clock square root as channel kinematics with $\beta_\ast$ pinning labeled hypothesis); new `Relation to the Symmetric Shell Channel` section in [nested-shell-braid.md](../../../content/markdown/aaa/noether-braid/nested-shell-braid.md) (integer-frequency check upholds the permutation-symmetry claim while restoring only periodicity; explicitly not a falsification of nested retention; orthogonal-plane precession-axis survival; formation question); net-charge terminology replacing monopole wording in the accessory subsection. Captured in the discussion log with claim levels: field-speed hinge specialness, action quantization as root-topology clicks with the hinge as double-entry accountant, transverse-acceleration re-pointing thresholds, unrequited wake momentum, two-braid fore-aft photon coupling, 't Hooft cogwheel circulant comparison, and the potential-superposition terminology decision. New artifacts: [Axis-Neutral Rotating-Wave Spectrum Packet](axis-neutral-rotating-wave-spectrum-packet.md) drafted in parallel per operator choice (constant-lag reduction lemma discharged; spectrum system and pitch-quantization interface stated as theorem targets, fail-closed), and the new [lissajou](../lissajou/priorities.md) priority lane. No retained-branch, stability, or accepted-evidence claim anywhere in this batch.

### 2026-07-06 - Void-Versus-Sea Insight Promoted And Brainstorming Pointer Replacement

Operator-directed open-research promotion: the `Euclidean Void Versus Noether Sea` insight was recast into [shell-braid.md](../../../content/markdown/aaa/noether-braid/symmetric-shell-braid.md) as `Isolated Release and the Return-Response Question` — the symmetry-versus-stability separation, the isolated escape result stated as a scoped diagnostic, and the reframing of retention as a return-response question with the rotating channel as the first internal candidate and the local Noether sea response as the environmental candidate. The brainstorming section body was replaced with a promotion pointer per the delete-and-recast instruction; superseded text remains in git history. Remaining promotion candidates staged for later passes: the ideal-braid same-level hypothesis, the collapse-stop-reversion mechanism map, the two-speed preferred-configuration/light-clock relation, and the $h$-basin pattern, plus unswept brainstorming files in the other braid lanes.

### 2026-07-06 - Corpus Promotion Of Concurred Axis-Neutral Channel Material

Operator-directed promotion of the concurred discussion material into the reader-facing braid corpus, in two claim tiers. Derivation tier: [shell-braid.md](../../../content/markdown/aaa/noether-braid/symmetric-shell-braid.md) gains an `Axis-Neutral Invariant Channel` section carrying the face-opposite seed, the $S_3\times\langle\iota\rangle$ and $C_3\times\langle\iota\rangle$ channels with explicit assumptions, the drum geometry, exact 120-degree phasing, the axial polarity-dipole identity, the momentum-screw/helicity structure with the wake-ledger conservation caveat, polarity-conjugation degeneracy, and drift-broken $\iota$ fore-aft asymmetry; no retention claim is made. Hypothesis tier: [nested-shell-braid.md](../../../content/markdown/aaa/noether-braid/nested-shell-braid.md) gains an `Accessory Dressing and Apparent Energy` subsection explicitly labeled candidate/hypothesis, carrying the Thomson octahedral dressing, the multipole quietness ladder, the monopole/multipole masking split for the weak channel, the quark leakage and confinement-as-multipole-completion speculation, and the circulant neutrino reading with the democratic-axis comparison observations (Koide $\pi/4$, trimaximal $(1,1,1)/\sqrt3$) marked as comparison targets only. Validation: `node scripts/validate-content.mjs --check --strict` passes with 0 errors after fixing four markdown-link parses of functional bracket notation in the proof packet ($[\mathbf X]\!(T)$ spacing); `build-scene-graph --check --strict` and `build-textbook-md-pdf --check` report 0 errors with generated drift noted for `toc.md` and the ambient atomic reading copy, left for the regeneration/PR flow per policy.

### 2026-07-06 - Axis-Neutral Transport Discussion Capture And Epsilon Notation Pass

Started the ongoing [Axis-Neutral Transport Channel Discussion Log](brainstorming.md#axis-neutral-transport-channel-discussion-log) in `brainstorming.md`, capturing the operator/agent discussion that followed the proof packet: polarity-conjugation degeneracy, drift-broken $\iota$, the two-worldline $\operatorname{Fix}(C_3)$ reduction, exact 120-degree phasing, the axial dipole identity, the clock-equivalent transport hypothesis, the eigen-braid rotating-wave spectrum target, the shell/nested symmetry dichotomy, the Lissajous/phase-closure connection, and the Koide-angle and electron-personality speculations, each with claim level. Also applied the operator terminology correction: prose now uses signed polarity-unit labels $\epsilon_{+,\bullet}$/$\epsilon_{-,\bullet}$ in the proof packet, tracker note, and this log, while owner-script identifiers such as `P_x`/`E_x` remain stable runtime implementation identifiers. No proof-status, evidence, or authorization change.

### 2026-07-06 - Six-Point Symmetry Invariant Lemma Proof Packet

Drafted [Six-Point Symmetry Invariant Lemma Proof Packet](six-point-symmetry-invariant-lemma-proof-packet.md), proving the equivariant reduction for the face-opposite six-point seed under the partner-wake master-equation kernel: the zero-angular-momentum $S_3\times\langle\iota\rangle$ form reduces to the two-function $(a,b)$ delay system, and the axis-neutral rotating $C_3\times\langle\iota\rangle$ form reduces to a closed three-function system in $\epsilon_{+,x}$, covering both `kick-at-release` and `moving-prehistory` sweep modes. Corollaries: common sphere and common speed hold on the rotating channel; $\mathbf J_{\mathrm{kin}}$ is exactly parallel to the axis-neutral spin axis on that channel; fixed-point drift beyond roundoff is a runner defect; the tangent template $(A,B,B)$ is the stabilizer corollary.

Executable change: [six-point-symmetry-invariant-lemma-row.mjs](../../../scripts/braid-ideal/six-point-symmetry-invariant-lemma-row.mjs) now accepts `--force-law-equivariance-proof-ref` and an options-level proof ref. Supplying the packet ref advances the fail-closed ladder from `force_law_equivariance_proof_missing` to `fail_closed_missing_retained_root_ledger` / `six_point_symmetry_invariant_lemma_row.retained_root_ledger_ref`; the default row is unchanged. [braid-ideal-six-point-symmetry-invariant-lemma-row.test.js](../../../tests/braid-ideal-six-point-symmetry-invariant-lemma-row.test.js) now proves the ladder advance, the obligation discharge map, unchanged default output, and that every authorization flag remains false with `scoreMovement=no_score_increase`.

Decision: obligations 1-2 discharged at priority level for the declared kernel class (verified against the implemented toy kernel), obligation 3 discharged conditionally (unique root per directed pair in the sub-field-speed window; invariant policy otherwise), obligation 4 blocked by design at same-record binding. No retained branch, force/action closure, return response, Noether sea response, branch chart, moving certificate, observer export, or score movement is authorized. The native central-solver kernel still requires its own equivariance audit before the lemma applies to native records. Next proofing consumers: the capped angular-momentum held-release sweep as executable witness, then the delayed escape certificate lemma on the reduced three-function system.

### 2026-07-06 - Six-Point Equivariant Reduction Proof Audit

Captured the operator-supplied proofing response in [Six-Point Equivariant Reduction Proof Audit](six-point-equivariant-reduction-proof-audit-2026-07-06.md). Decision: priority-only proof routing, no corpus promotion, no score movement, and no retained-branch claim. The audit reorders the first two local targets: `six_point_symmetry_invariant_lemma` is now the first proof-moving object, while `angular_momentum_held_release_sweep` is the capped diagnostic witness.

Live scaffold: [six-point-symmetry-invariant-lemma-row.mjs](../../../scripts/braid-ideal/six-point-symmetry-invariant-lemma-row.mjs) remains fail-closed at `same_record_force_law_equivariance_proof_for_six_point_symmetry_invariant_lemma` / `six_point_symmetry_invariant_lemma_row.force_law_equivariance_proof_ref`. The audit explicitly forbids importing frozen-octahedral mean-power values as held-release no-return hypotheses; those values remain motivation only because they were certified on a different chart.

### 2026-07-06 - Angular-Momentum Held-Release Sweep Spec

Captured the operator-supplied angular-momentum construction in [Angular-Momentum Held-Release Sweep Spec](angular-momentum-held-release-sweep-spec.md). Decision: priority-only implementation and proofing spec, no corpus promotion, no score movement, and no retained-branch claim. The packet makes the first sweep axis-neutral rigid rotation about $(1,1,1)/\sqrt{3}$, defines surface-speed fraction as $v_t=f_v c_f$, separates `kick-at-release` from history-consistent `moving-prehistory`, and records the correction that causal roots use wake propagation at field speed $c_f$, not light delay.

Implementation boundary: [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) still needs `--surface-speed-fraction`, `--spin-axis`, and `--prehistory-mode`; outputs must keep kinematic angular momentum as a diagnostic unit-weight branch-ledger row, not a mass or retention claim.

### 2026-07-06 - Noether Braid Strategy Memo Capture

Captured the attached read-only strategy memo in [Noether Braid Retained-Branch Strategy Memo Capture](noether-braid-retained-branch-strategy-memo-2026-07-06.md). Decision: priority-only routing, no corpus promotion, no score movement, and no retained-branch claim. The memo drives `angular_momentum_held_release_sweep` as the top next braid-ideal target.

Live-tree correction: [Shell-Braid Run Matrix](sh-run-matrix.md), [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs), and [sh-0-sea-diagnostic-candidate-model.mjs](../../../scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs) already carry transverse surface-speed and prehistory metadata. The missing object is the actual physics path in [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs): surface-speed fraction, spin axis, `kick-at-release`, and history-consistent `moving-prehistory` are not yet integrated there.

Priority routing: raise the angular-momentum held-release sweep above further certificate search; turn repeated isolated escape into a scoped Euclidean-void no-return theorem target; replace fitted `SH-0-sea` response placeholders with a computed like-braid wake-sum route; sequence native central-solver retained-history promotion only after a diagnostic row is worth accepting; keep frozen octahedral refinement and acceptance-certificate acquisition sweeps frozen unless they directly support a return-turn chart or real accepted row.

### 2026-07-04 - Shell-Braid Run Matrix Runner Options

Implemented diagnostic run-matrix metadata for the shell-braid variants in [Shell-Braid Run Matrix](sh-run-matrix.md). [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now accepts Proof ID, run handle, source row id, target-center group velocity, transverse surface-speed fraction, and prehistory mode options, and emits source artifact id/hash plus fail-closed evidence status. [sh-0-sea-diagnostic-candidate-model.mjs](../../../scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs) now passes the same metadata through for embedded `SH-0-sea` rows.

Decision: the new options implement diagnostic/candidate run tracking only. They do not produce an accepted seed-path certificate, retained force/action closure, Noether sea response closure, stability row, branch chart, moving certificate, observer export, or score movement. The accepted-evidence blocker remains `held_release_seed_path_rows_acceptance_certificate.v0` at `held_release_seed_path_rows.acceptance_certificate_ref`, followed by the matching external accepted-authority package, repo authorization, retained-source adapter package, and same-record receiver-normal root-detail rows.

### 2026-07-04 - SH-0 Retained-History Evidence Audit

Audited the `SH-0` retained-history evidence handles against the live owner scripts and captured the result in [SH-0 Retained-History Evidence Audit](sh-0-retained-history-evidence-audit.md). The audit separates the fresh/default construction from the active provider-backed acquisition path. Fresh/default seed-path and stream-manifest artifacts still fail at `held_release_seed_path_rows[*].retained_record_id`, but the active provider-backed path for `held_release_seed_path_rows:5833f18e53586201` is sharper: it is blocked at `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`.

Decision: `SH-0` has useful diagnostic and contract evidence, but no accepted retained-history evidence. The next cleanup target is a non-circular `held_release_seed_path_rows_acceptance_certificate.v0` plus its matching external accepted-authority package and repo authorization. Retained branch output, return/stability rows, Noether sea stabilization, branch-chart/moving certificate, observer export, and score movement remain unauthorized.

### 2026-07-03 - GitHub Actions Run Artifact Seed-Path Certificate Sweep

Continued the active seed-path source-acquisition goal by checking the remaining GitHub Actions run surface for a real accepted same-record seed-path certificate or matching external authority package. The current branch is `codex/despina` in `jmarkmorris/architrino`. Recent run metadata showed only the `Content Integrity` workflow with a single `validate-content` job shape for the current branch, PR #200, the #200 main merge, PR #199, and the #199 main merge; no job step advertised an artifact-producing certificate issuer.

Actions surfaces inspected: current-branch runs `28662795029` and `28648799358`; #200-related runs `28632643827`, `28632492353`, and `28632372063`; and #199-related runs `28613589996`, `28613337173`, and `28613073882`. Exact log searches for `held_release_seed_path_rows_acceptance_certificate`, `held_release_seed_path_rows_external_accepted_authority_package`, `external-authority-package:held-release-seed-path-rows`, `accepted:held-release-seed-path-rows`, `held_release_seed_path_rows:5833f18e53586201`, and `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327` returned no hits. `gh run download` reported no valid downloadable artifacts for all eight runs.

Decision: no GitHub Actions run log or artifact produced a real accepted same-record `held_release_seed_path_rows_acceptance_certificate.v0` or matching `held_release_seed_path_rows_external_accepted_authority_package.v0`. Combined with the current verifier output, the active first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; the non-repo authority package that must accompany any matching certificate remains `held_release_seed_path_rows_external_accepted_authority_package.v0` at `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`; and even a matching certificate/package pair still requires `repo_authorization_for_accepted_held_release_seed_path_rows` at `held_release_seed_path_rows.acceptance_certificate_ref`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Acceptance-Authority Pattern And Conditional Verification Probe

Continued the active seed-path source-acquisition goal by checking whether any repo-local accepted-authority pattern can issue the missing seed-path certificate without circularity. A read-only acceptance-pattern sidecar found no reusable repo-local issuer or repo-authorization route for `held_release_seed_path_rows_acceptance_certificate.v0`. The seed-path verifier remains a conditional verifier/fail-closed contract; the retained-source adapter accepted-evidence package remains a downstream consumer because it requires `held_release_seed_path_rows_acceptance_certificate_ref` as its first accepted-evidence field; nearby internal-tangent and preferred-curve authority rows only reach `conditional_on_external_accepted_authority`; proof-program frontier classifiers remain fail-closed; and unrelated equation-mapping certificate producers that can return `accepted=true` emit their own schemas rather than the seed-path schema, external authority package, or repo authorization route.

Local in-memory probe: constructing the verifier's expected same-record seed-path certificate payload for `held_release_seed_path_rows:5833f18e53586201`, retained record `retained-record:held-release-six-point:adapter-acceptance-certificate`, source row `two-speed-preferred-row:u0.8:v0.2`, provider object `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327`, and provider hash `7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df` moves the verifier only from `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref` to `held_release_seed_path_rows_external_accepted_authority_package` / `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`. Constructing the matching expected external authority package then moves the verifier only to `repo_authorization_for_accepted_held_release_seed_path_rows` / `held_release_seed_path_rows.acceptance_certificate_ref`, with `accepted=false`, `requirement_passed=false`, `conditionally_verified=true`, and all authorization flags still false.

Decision: this checkpoint sharpens the authority boundary but does not produce accepted evidence and does not satisfy the four-hour proof condition by itself. The first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; the exact non-repo authority package still required after a matching certificate is `held_release_seed_path_rows_external_accepted_authority_package.v0`; and even that package is insufficient without a separate repo authorization object for `held_release_seed_path_rows.acceptance_certificate_ref`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Repo, GitHub, And Local Artifact Seed-Path Certificate Sweep

Continued the active seed-path source-acquisition goal in team-agent mode after rereading the objective file, workspace routing, corpus-advancement workflow, source-mining workflow, and current braid-ideal owner files. The local verifier command for `held_release_seed_path_rows:5833f18e53586201`, retained record `retained-record:held-release-six-point:adapter-acceptance-certificate`, source row `two-speed-preferred-row:u0.8:v0.2`, provider object `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327`, and provider hash `7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df` still reports `accepted=false`, `requirement_passed=false`, `first_missing_object="held_release_seed_path_rows_acceptance_certificate"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"`. The same command emits the exact required certificate prefix `accepted:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2:`, exact required external authority refs, exact external package ref `external-authority-package:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2`, and the provider hash in both expected payloads.

Worker A searched the current working tree, local refs and tags, remote branch/tag metadata, GitHub code search, GitHub issue search, GitHub PR search, and bounded checks for PR #199 and PR #200. The only current-source hits were owner code, tests, and this work-log; local refs with the exact accepted prefix or package ref were only work-log prose on `codex/despina` and `origin/codex/despina`. No GitHub issue, PR, remote metadata, tag, or source-object hit produced a real accepted same-record seed-path certificate or matching external authority package. Worker B searched readable portions of `.codex/attachments`, `.codex/archived_sessions`, `/private/tmp`, and the app temp root. It found goal-objective prose and archived session transcripts only; no target-bearing JSON or artifact path, exact accepted-ref prefix, or exact package ref existed in the readable local non-repo surfaces. Its absence result carries the bounded caveat that several temp subpaths were unreadable. Worker C traced the upstream owner chain and confirmed that seed-path rows, stream-manifest set, retained-history row, provider object, provider/source carrier, and adapter accepted-evidence package remain candidate, diagnostic, or contract surfaces. The adapter package consumes `held_release_seed_path_rows_acceptance_certificate_ref`; it cannot non-circularly mint that same upstream input.

Decision: no accepted same-record `held_release_seed_path_rows_acceptance_certificate.v0` was produced or found in this checkpoint, and this checkpoint does not satisfy the four-hour proof condition by itself. The active blocker remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`. The first non-repo authority package that must accompany a matching certificate remains `held_release_seed_path_rows_external_accepted_authority_package.v0` at `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`; after a matching certificate and package, repo authorization remains blocked at `repo_authorization_for_accepted_held_release_seed_path_rows` / `held_release_seed_path_rows.acceptance_certificate_ref`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Provider-Hash Bound Seed-Path Authority Probe

Continued the active seed-path source-acquisition goal after rereading the objective and current startup workflow files. The live direct seed-path requirement and adapter-nested source requirement still report `accepted=false`, `requirement_passed=false`, `first_missing_object="held_release_seed_path_rows_acceptance_certificate"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"` for `held_release_seed_path_rows:5833f18e53586201`.

Worker results: the verifier-precision worker found no remaining accepted-certificate ladder gap; current code and tests already prove the ordered fail-closed ladder of missing seed-path certificate, then missing `held_release_seed_path_rows_external_accepted_authority_package`, then missing `repo_authorization_for_accepted_held_release_seed_path_rows`. The CLI-binding worker confirmed that the direct seed-path requirement already supports `--provider-artifact-hash=7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df`; when that option is supplied, both the expected certificate payload and expected external authority package payload bind the provider artifact hash. The earlier null provider-hash probe was a caller omission, not a verifier gap. The reachable-ref worker checked 160 refs and found only work-log prose, owner scripts, and tests for the seed-path certificate/package names and exact authority package ref; no real accepted same-record source certificate or matching external authority package was found.

Local probes: the direct command `node scripts/braid-ideal/held-release-seed-path-rows.mjs --retained-record-id=retained-record:held-release-six-point:adapter-acceptance-certificate --source-row-id=two-speed-preferred-row:u0.8:v0.2 --provider-object-ref=candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327 --provider-artifact-hash=7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df --print-acceptance-certificate-requirement` preserves the same first blocker while emitting the fully bound provider hash in the expected certificate and expected external-authority package payloads. The adapter accepted-evidence contract still reports ten observed source fields, zero accepted package fields, and the seed-path source artifact as `candidate_provider_backed_source_unaccepted`.

Decision: no code change is warranted from this cycle, and no real accepted same-record `held_release_seed_path_rows_acceptance_certificate.v0` was produced. This checkpoint does not satisfy the four-hour proof condition by itself. The exact active blocker remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after a matching certificate, the next non-repo package remains `held_release_seed_path_rows_external_accepted_authority_package.v0` at `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`; after a matching package, repo authorization remains blocked at `repo_authorization_for_accepted_held_release_seed_path_rows` / `held_release_seed_path_rows.acceptance_certificate_ref`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Current-Source Team-Agent Seed-Path Authority Refresh

Resumed the active seed-path source-acquisition goal after rereading the goal objective and startup workflow files. The live seed-path requirement still reports `accepted=false`, `requirement_passed=false`, `first_missing_object="held_release_seed_path_rows_acceptance_certificate"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"` for `held_release_seed_path_rows:5833f18e53586201`. The retained-source adapter accepted-evidence contract still reports ten observed source fields and zero accepted package fields; its first package field remains `held_release_seed_path_rows_acceptance_certificate_ref`, so the adapter remains a consumer of the seed-path certificate rather than a non-circular producer.

Worker A searched current source, reachable refs, remote ref/tag metadata, GitHub PR/issue surfaces, and merged PR metadata for PR #199 and PR #200. No accepted same-record seed-path certificate or matching external accepted-authority package was found. The only reachable remote hits were verifier/consumer code, tests, and work-log prose that preserve the blocker; `origin/codex/holmium`, `origin/codex/molybdenum`, and remote tags had no term hits. GitHub exact code search returned no default-branch hits in the coordinator probe, while one worker retry hit API rate limiting, so this remains a current-source boundary rather than a four-hour proof completion claim.

Worker B confirmed from the live code path that [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) verifies supplied certificate JSON but does not mint `held_release_seed_path_rows_acceptance_certificate.v0`; the producer path remains `candidate_provider_backed_source_unaccepted` and emits `reason="producer_does_not_authorize_accepted_path_row_evidence"`. Worker C reproduced the source-owner matrix: seed-path rows, path-history manifest set, retained-history row, provider object, provider/source carrier, native path-history manifests, native root-ledger details, causal-root replay, action closure, wake history, provider provenance, and the adapter accepted-evidence package are candidate, missing, or diagnostic surfaces only, and none can authorize the seed-path certificate. Worker D found no accepted local candidate in the repo, attachment roots, readable `/private/tmp`, or readable app temp roots.

The two readable app-temp `seed-path-certificate.json` probes are rejected by the live verifier with `accepted=false`, `requirement_passed=false`, and `certificate_conditionally_verified=false`. They still use fixture-style external refs, omit `source_row_id`, use an accepted certificate ref that lacks the required `two-speed-preferred-row:u0.8:v0.2` segment, and have no matching `held_release_seed_path_rows_external_accepted_authority_package.v0`.

Decision: no real accepted same-record `held_release_seed_path_rows_acceptance_certificate` was produced in this cycle, and the four-hour proof condition is not satisfied by this checkpoint alone. The live acquisition ladder remains: first acquire a non-circular `held_release_seed_path_rows_acceptance_certificate.v0` at `held_release_seed_path_rows.acceptance_certificate_ref`; then acquire the matching `held_release_seed_path_rows_external_accepted_authority_package.v0` at `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`; then clear `repo_authorization_for_accepted_held_release_seed_path_rows` at the same acceptance-certificate field. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Four-Worker Seed-Path Source-Owner Matrix Checkpoint

Continued the seed-path source-acquisition run with four read-only workers plus local CLI probes. Worker A searched the working tree, reachable local git refs through `git grep`, readable Codex attachment roots, `/private/tmp`, and the readable app temp root for `held_release_seed_path_rows_acceptance_certificate`, `held_release_seed_path_rows_external_accepted_authority_package`, `accepted:held-release-seed-path-rows:`, `external-authority-package:held-release-seed-path-rows:`, and `5833f18e53586201`. No real accepted same-record certificate or matching external authority package was found. The only readable temp certificate JSON probes remain rejected because they use fixture-style external refs, omit `source_row_id`, carry an incomplete accepted-ref shape, and have no matching external authority package.

Worker B confirmed that the retained-source adapter acceptance pattern is only a verifier/consumer pattern for this boundary. It consumes `held_release_seed_path_rows_acceptance_certificate_ref` as the first accepted-evidence package field, so using the adapter package to produce that certificate would be circular. Worker C reproduced the current source-owner matrix: seed-path rows, stream-manifest set, retained-history row, provider object, provider/source carrier, six native path-history manifest refs, thirty-six native root-ledger detail refs, thirty-six causal-root replay refs, same-record action closure, retained wake history, and provider provenance are all candidate or diagnostic-only surfaces, with zero accepted package fields. Worker D found no uncovered decoy class; existing tests already reject stale seed refs, wrong-family refs, same-family stale refs, correct-prefix temp probes, ref-only payloads, copied-null payloads, candidate refs, and conditionally verified synthetic certificate/package pairs.

Local probe: the live retained-source adapter accepted-evidence contract still reports ten observed source fields, zero accepted package fields, and the first package field `held_release_seed_path_rows_acceptance_certificate_ref` mapped to `held_release_seed_path_rows:5833f18e53586201`. A matching synthetic certificate alone advances only to `held_release_seed_path_rows_external_accepted_authority_package` / `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`. A matching synthetic certificate plus matching external authority package advances only to `repo_authorization_for_accepted_held_release_seed_path_rows` / `held_release_seed_path_rows.acceptance_certificate_ref`, with `accepted=false`, `requirement_passed=false`, and authorization flags false.

Decision: this checkpoint does not produce a real accepted same-record `held_release_seed_path_rows_acceptance_certificate` and does not satisfy the four-hour proof condition by itself. It keeps the acquisition ladder exact: first acquire the non-circular seed-path certificate, then its non-repo external authority package, then repo authorization for the same field; if those clear, the next distinct upstream package owner is `held_release_path_history_stream_manifest_set_acceptance_certificate` for `held_release_path_history_stream_manifest_set:49297bfc0206431a`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path Repo-Authorization Diagnostic Surface

Continued the seed-path source-acquisition run by integrating a read-only sidecar audit of the conditionally verified certificate/package state. The sidecar confirmed that the current tree already exposes `repo_authorization_for_accepted_held_release_seed_path_rows` and `held_release_seed_path_rows.acceptance_certificate_ref` as the post-package repo-authorization blocker inside [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs), and that the retained-source adapter threads the same nested source requirement rather than creating an independent certificate producer.

Executable coverage: [braid-ideal-held-release-seed-path-rows.test.js](../../../tests/braid-ideal-held-release-seed-path-rows.test.js) now asserts `required_repo_authorization_object`, `required_repo_authorization_field`, and `repo_authorization_status` for both the pending certificate/package state and the matching certificate-plus-package state. [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) now asserts the same fields on the adapter's nested `source_requirement` and CLI pass-through diagnostic.

Decision: this is a blocker-proof precision improvement only. It does not produce a real accepted same-record `held_release_seed_path_rows_acceptance_certificate`, does not change authorization semantics, and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. The active first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; if a matching certificate and matching external authority package are supplied, the remaining repo-local blocker is `repo_authorization_for_accepted_held_release_seed_path_rows` at the same field.

### 2026-07-03 - Seed-Path Repo-Authorization Missing-Field Consistency

Continued the seed-path source-acquisition run by probing the conditionally verified certificate/package state directly. Before this pass, a matching synthetic `held_release_seed_path_rows_acceptance_certificate.v0` plus matching `held_release_seed_path_rows_external_accepted_authority_package.v0` correctly reported `first_missing_object="repo_authorization_for_accepted_held_release_seed_path_rows"` and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"`, but its `missing_fields` array was empty. That made the fail-closed diagnostic less exact than the first-missing field it already exposed.

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now carries `held_release_seed_path_rows.acceptance_certificate_ref` in `missing_fields` when a supplied certificate and external authority package are only conditionally verified and repo authorization is still blocked. [braid-ideal-held-release-seed-path-rows.test.js](../../../tests/braid-ideal-held-release-seed-path-rows.test.js) and [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) now assert that both the seed-path requirement and the nested adapter source requirement preserve that nonempty missing-field list.

Decision: this is a diagnostic consistency improvement only. It does not produce a real accepted same-record seed-path certificate, does not make the synthetic certificate/package pair accepted, and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. The active accepted-evidence blocker remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; if a matching certificate and package are supplied, the remaining blocker is still `repo_authorization_for_accepted_held_release_seed_path_rows` at the same field.

### 2026-07-03 - Adapter Contract Seed-Path Authority JSON Pass-Through

Continued the seed-path source-acquisition run with three read-only sidecars and a local adapter-contract pass. The seed-path verifier already distinguishes three fail-closed states: no matching certificate, matching certificate but missing `held_release_seed_path_rows_external_accepted_authority_package.v0`, and matching certificate plus package that still remains `repo_authorization_for_accepted_held_release_seed_path_rows` / `held_release_seed_path_rows.acceptance_certificate_ref`.

Executable change: [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now lets the accepted-evidence contract pass optional `seedPathAcceptanceCertificate` and `seedPathExternalAuthorityPackage` objects into the existing nested `held_release_seed_path_rows_acceptance_certificate_requirement.v0`. Its CLI path also accepts `--seed-path-acceptance-certificate-json` and `--seed-path-external-authority-package-json` when printing `--print-same-record-accepted-evidence-contract`. This does not create a new certificate shell or producer; it only lets the existing contract surface the live seed-path verifier's exact nested blocker if a proposed source certificate/package is supplied.

Sidecar findings: the source sweep found no accepted same-record seed-path certificate or authority package in the repo, readable attachment roots, `/private/tmp`, or the readable app temp root. The two readable temp `seed-path-certificate.json` probes still use fixture authority refs, a wrong accepted-ref shape, and omit `source_row_id`, so the live verifier rejects them. The upstream source-owner matrix remains candidate-only for the first four adapter package fields: seed-path rows, stream-manifest set, retained-history row, and provider object all have reproducible current ids but no repo-local accepted producer.

Regression coverage: [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) now verifies that the adapter contract's first field map row advances from `held_release_seed_path_rows_acceptance_certificate` to `held_release_seed_path_rows_external_accepted_authority_package` when a matching certificate is supplied, and then to `repo_authorization_for_accepted_held_release_seed_path_rows` when a matching external authority package is also supplied. In all cases `accepted=false`, `requirement_passed=false`, and authorization flags remain false.

Validation passed: `node --test --test-reporter=dot tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js`, `node --test --test-reporter=dot tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js tests/braid-ideal-held-release-seed-path-rows.test.js`, full `node --test --test-reporter=dot tests/braid-ideal-*.test.js`, `git diff --check`, `node scripts/validate-content.mjs --check --strict`, `node scripts/build-scene-graph.mjs --check --strict`, and `node scripts/build-textbook-md-pdf.mjs --check`. Decision: no accepted same-record seed-path certificate was produced. The live first missing object/field without supplied external evidence remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; the first non-repo authority package after a matching certificate remains `held_release_seed_path_rows_external_accepted_authority_package.v0`; and even a matching certificate/package pair remains repo-authorization blocked rather than accepted. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Adapter Contract Source-Artifact Blocker Ladder

Continued the four-hour seed-path source-acquisition run with four read-only workers and a local contract integration pass. Worker A found no real accepted same-record `held_release_seed_path_rows_acceptance_certificate` in the repo, readable attachment roots, `/private/tmp`, or the readable app temp root; the two readable `seed-path-certificate.json` temp probes still use fixture-style external refs, omit `source_row_id`, and fail the live verifier. Worker B confirmed that the retained-source adapter accepted-evidence package cannot non-circularly mint the seed-path certificate because that package consumes `held_release_seed_path_rows_acceptance_certificate_ref` as its first field. Worker C reduced the upstream owner ladder to candidate source objects, and Worker D found no uncovered decoy class requiring a new test.

Executable change: [central-solver-retained-source-adapter.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter.mjs) now preserves `first_missing_object` and `first_missing_field` on each consumed source artifact summary. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now carries those summaries into the `source_acquisition_field_map` rows under `source_artifact`. [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) now asserts the exact current blockers for the first four accepted-evidence package fields:

- `held_release_seed_path_rows:5833f18e53586201` -> `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`
- `held_release_path_history_stream_manifest_set:49297bfc0206431a` -> `held_release_path_history_stream_manifest_set_acceptance_certificate` / `held_release_path_history_stream_manifest_set.acceptance_certificate_ref`
- `central_solver_retained_history_row:5cea06554a38513a` -> `central_solver_retained_history_row_acceptance_certificate` / `central_solver_retained_history_row.acceptance_certificate_ref`
- `central_solver_retained_history_provider_object:7d4a8fe0a9792327` -> `central_solver_retained_history_provider_object_acceptance_certificate` / `central_solver_retained_history_provider_object.acceptance_certificate_ref`

Contract probe: `node scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs --retained-record-id=retained-record:held-release-six-point:adapter-acceptance-certificate --source-row-id=two-speed-preferred-row:u0.8:v0.2 --print-same-record-accepted-evidence-contract` now exposes those four live `source_artifact` blockers in one accepted-evidence contract output while keeping `accepted_for_package=false`, `accepted=false`, and all authorization flags false.

Focused validation passed for the adapter, provider/source carrier, acceptance certificate, seed-path rows, stream-manifest set, retained-history row, and provider-object tests. Decision: this is a source-owner visibility improvement only. It does not produce the accepted same-record seed-path certificate, does not satisfy the four-hour proof condition by itself, and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. The live first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after that certificate is acquired, the paired non-repo object remains `held_release_seed_path_rows_external_accepted_authority_package.v0`.

### 2026-07-03 - Provider-Object CLI Provider-Backed Blocker Probe

Continued the upstream accepted-evidence package ladder after making the stream-manifest set directly reproducible. The central retained-history provider-object builder already derived the current provider object through a two-stage provider-shell path, but the CLI still emitted only the default missing-retained-record object.

Executable change: [central-solver-retained-history-provider-object.mjs](../../../scripts/braid-ideal/central-solver-retained-history-provider-object.mjs) now accepts `--retained-record-id`, repeated `--durable-manifest-ref`, comma-separated `--durable-manifest-refs`, and `--auto-durable-manifest-refs`. For the current retained record, the CLI derives `central_solver_retained_history_provider_object:7d4a8fe0a9792327`, preserves `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327`, binds `held_release_seed_path_rows:5833f18e53586201`, binds `held_release_path_history_stream_manifest_set:49297bfc0206431a`, binds `central_solver_retained_history_row:5cea06554a38513a`, and stops at `central_solver_retained_history_provider_object_acceptance_certificate` / `central_solver_retained_history_provider_object.acceptance_certificate_ref`. [braid-ideal-central-solver-retained-history-provider-object.test.js](../../../tests/braid-ideal-central-solver-retained-history-provider-object.test.js) now verifies this provider-backed CLI blocker path.

Decision: this is an upstream source-owner accuracy improvement only. It does not bypass the earlier seed-path certificate or stream-manifest certificate blockers and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. If the seed-path and stream-manifest certificates are acquired, the next provider-object owner is now directly reproducible from the CLI as `central_solver_retained_history_provider_object_acceptance_certificate` for `central_solver_retained_history_provider_object:7d4a8fe0a9792327`.

### 2026-07-03 - Stream-Manifest Set CLI Provider-Backed Blocker Probe

Continued upstream after the seed-path certificate blocker remained unresolved. The provider/source carrier already embeds `held_release_path_history_stream_manifest_set:49297bfc0206431a` as the next package owner after the seed-path certificate, but the manifest-set CLI only emitted the default missing-retained-record artifact even though the builder had a provider-backed mode.

Executable change: [held-release-path-history-stream-manifest-set.mjs](../../../scripts/braid-ideal/held-release-path-history-stream-manifest-set.mjs) now accepts `--retained-record-id`, `--provider-object-ref`, `--provider-artifact-hash`, repeated `--durable-manifest-ref`, comma-separated `--durable-manifest-refs`, and `--auto-durable-manifest-refs`. The current direct CLI probe emits `held_release_path_history_stream_manifest_set:49297bfc0206431a`, `source_status="candidate_provider_backed_source_unaccepted"`, and stops at `held_release_path_history_stream_manifest_set_acceptance_certificate` / `held_release_path_history_stream_manifest_set.acceptance_certificate_ref`. [braid-ideal-held-release-path-history-stream-manifest-set.test.js](../../../tests/braid-ideal-held-release-path-history-stream-manifest-set.test.js) now verifies this provider-backed CLI blocker path and preserves all authorization flags as false.

Decision: this is an upstream source-owner accuracy improvement, not accepted evidence. It does not bypass the still-earlier `held_release_seed_path_rows_acceptance_certificate` blocker and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. If the seed-path certificate is acquired, the next exact package-owner object is now directly reproducible from the CLI as `held_release_path_history_stream_manifest_set_acceptance_certificate` for `held_release_path_history_stream_manifest_set:49297bfc0206431a`.

### 2026-07-03 - Seed-Path Exact Authority Requirement Diagnostics

Continued the source-acquisition run with four read-only workers plus a local verifier pass. The workers found no real accepted same-record `held_release_seed_path_rows_acceptance_certificate.v0` or `held_release_seed_path_rows_external_accepted_authority_package.v0` in the repo, attachment roots, or readable temp roots. Hits remain owner scripts, tests, priority prose, target contracts, candidate provider/source refs, and temp probes rejected by the live verifier.

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now exposes exact same-record requirement fields for `required_external_authority_ref`, `required_external_authority_verification_ref`, and `required_external_authority_package_ref` alongside the existing family-prefix diagnostics. For the current retained record/source row those fields resolve to `external-authority:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2`, `external-verification:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2`, and `external-authority-package:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2`. [braid-ideal-held-release-seed-path-rows.test.js](../../../tests/braid-ideal-held-release-seed-path-rows.test.js) now asserts those exact refs, and [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) verifies that the nested adapter source requirement carries them.

Decision: this is a blocker-map precision improvement only. It does not produce the accepted same-record certificate, does not flip authorization flags, and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. The live first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`. After that certificate is acquired, the next package owner in the retained-source adapter field map is `held_release_path_history_stream_manifest_set_acceptance_certificate` for `held_release_path_history_stream_manifest_set:49297bfc0206431a`.

### 2026-07-03 - Retained-History Row CLI Provider-Backed Blocker Probe

Continued the seed-path source-acquisition run by checking the upstream retained-history row diagnostics from the command line. The retained-history row builder already had a provider-backed mode that sharpens from missing provider provenance to `central_solver_retained_history_row_acceptance_certificate` / `central_solver_retained_history_row.acceptance_certificate_ref`, but the CLI always emitted the default fail-closed row because it did not accept the retained-record and provider-object options. That made worker probes see the older `central_solver_retained_history_row.provider_provenance.provider_object_ref` blocker unless they imported the builder directly.

Executable change: [central-solver-retained-history-row.mjs](../../../scripts/braid-ideal/central-solver-retained-history-row.mjs) now accepts `--retained-record-id`, `--provider-object-ref`, and `--provider-artifact-hash`. [braid-ideal-central-solver-retained-history-row.test.js](../../../tests/braid-ideal-central-solver-retained-history-row.test.js) now verifies that the direct CLI emits `candidate_provider_backed_source_unaccepted`, preserves all authorization flags as false, and stops at `central_solver_retained_history_row_acceptance_certificate` / `central_solver_retained_history_row.acceptance_certificate_ref` for a provider-backed row.

Decision: this is a blocker-map accuracy improvement only. It does not produce `held_release_seed_path_rows_acceptance_certificate`, does not create a new certificate shell, and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. The seed-path first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after a matching seed-path certificate, the paired non-repo source object remains `held_release_seed_path_rows_external_accepted_authority_package.v0`.

### 2026-07-03 - Adapter Same-Record Accepted-Ref Exactness

Continued the seed-path source-acquisition run by auditing the retained-source adapter accepted-evidence package after the seed-path certificate verifier was made same-record exact. The adapter criterion already required package-level `retained_record_id` and `source_row_id`, but each accepted-looking package field still only needed the correct accepted-ref family. That left a decoy path where package metadata could name the current retained record/source row while a field such as `held_release_seed_path_rows_acceptance_certificate_ref` or `provider_provenance_ref` carried a same-family ref for another retained record or source row.

Executable change: [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now derives required accepted-ref prefixes from the adapter's retained record and source row for the package provenance refs and all ten accepted-evidence fields. The live accepted-evidence contract now prints exact prefixes such as `accepted:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2:` and `accepted:provider-provenance:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2:`. [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) now rejects same-family stale package provenance refs and same-family stale accepted-evidence refs across every package field.

Decision: this closes a downstream adapter false positive without producing accepted seed-path evidence. The live seed-path first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after a matching certificate, the paired non-repo package remains `held_release_seed_path_rows_external_accepted_authority_package.v0`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path Same-Record External Authority Exactness

Audited the seed-path source-intake verifier after the worker-integration checkpoint and found one remaining same-family decoy: a certificate could use the `external-authority:held-release-seed-path-rows:` and `external-verification:held-release-seed-path-rows:` families while still naming a stale or temp-probe authority string rather than the exact retained record/source row pair.

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now requires the certificate's external accepted-authority ref and external verification ref to match `external-authority:held-release-seed-path-rows:<retained-record-id>:<source-row-id>` and `external-verification:held-release-seed-path-rows:<retained-record-id>:<source-row-id>` exactly. Its accepted certificate ref must also begin with `accepted:held-release-seed-path-rows:<retained-record-id>:<source-row-id>:` before any certificate-specific suffix. [braid-ideal-held-release-seed-path-rows.test.js](../../../tests/braid-ideal-held-release-seed-path-rows.test.js) now rejects same-family `temp-probe` authority refs and stale same-family certificate refs as non-authorizing.

Current-source refresh: repo-wide and attachment-root searches found only owner scripts, tests, work-log prose, and goal objective files for the seed-path certificate/package names. Readable temp-root hits were two old `seed-path-certificate.json` probes; the live verifier rejects them at `held_release_seed_path_rows.acceptance_certificate.external_accepted_authority_ref`, `held_release_seed_path_rows.acceptance_certificate.external_accepted_authority_verification_ref`, `held_release_seed_path_rows.acceptance_certificate.held_release_seed_path_rows_acceptance_certificate_ref`, and `held_release_seed_path_rows.acceptance_certificate.source_row_id`. GitHub code, issue, and PR searches for `held_release_seed_path_rows_acceptance_certificate` and `held_release_seed_path_rows_external_accepted_authority_package` returned no source authority.

Decision: this closes a same-prefix stale-authority false positive without producing accepted evidence. The first live missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after a matching certificate, the paired non-repo package remains `held_release_seed_path_rows_external_accepted_authority_package.v0`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path Authority Pattern Worker Integration

Integrated three read-only source-acquisition workers after tightening the seed-path external authority ref families. The workers agreed that the live repo can reproduce `held_release_seed_path_rows:5833f18e53586201`, but cannot honestly emit an accepted `held_release_seed_path_rows_acceptance_certificate.v0`. The current seed-path CLI is a source-intake diagnostic: it prints `accepted=false`, `requirement_passed=false`, `first_missing_object="held_release_seed_path_rows_acceptance_certificate"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"` for the retained record/source row/provider object under this lane.

Authority pattern: the retained-source adapter pattern can guide the seed-path certificate shape, but it cannot authorize the seed-path input without circularity. The adapter accepted-evidence package consumes `held_release_seed_path_rows_acceptance_certificate_ref` as its first accepted package field, so using the adapter authority to mint that certificate would let a downstream consumer authorize one of its own upstream inputs. The seed-path authority must therefore be a distinct non-repo seed-path authority object/package using the seed-path-specific `accepted:held-release-seed-path-rows:`, `external-authority:held-release-seed-path-rows:`, `external-verification:held-release-seed-path-rows:`, and `external-authority-package:held-release-seed-path-rows:` families.

Source-owner matrix: seed-path rows, held-release path-history stream manifests, central retained-history row, central retained-history provider object, provider/source carrier, central retained-source adapter, native/app durable path-history stream manifests, native root-ledger detail rows, causal-root replay rows, same-record action closure, retained wake history, provider provenance, the same-record accepted-evidence package contract, held-release causal wake toy output, tests, fixtures, and negative controls are all candidate, unverified, diagnostic, fixture, or missing. None can authorize the seed-path certificate.

Decision: this cycle sharpens the exact non-repo source authority but still produces no accepted certificate. Earliest unavailable object remains `held_release_seed_path_rows_acceptance_certificate.v0` at `held_release_seed_path_rows.acceptance_certificate_ref`; the paired non-repo package remains `held_release_seed_path_rows_external_accepted_authority_package.v0`, and even a conditionally verified pair remains blocked at repo authorization for accepted seed-path rows. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path External Authority Ref Family Tightening

Continued the source-acquisition proof after the adapter accepted-evidence field-family tightening. The seed-path verifier already required a seed-path-specific accepted certificate ref and a same-record external authority package ref, but its `external_accepted_authority_ref` and `external_accepted_authority_verification_ref` checks still admitted generic `external-authority:*` and `external-verification:*` families. That could let a matching synthetic certificate reach conditional verification with a non-seed-path authority label.

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now requires `external-authority:held-release-seed-path-rows:<retained-record-id>:<source-row-id>` and `external-verification:held-release-seed-path-rows:<retained-record-id>:<source-row-id>` for the certificate's non-repo authority fields. The requirement diagnostic now prints those required prefixes alongside the exact `held_release_seed_path_rows_external_accepted_authority_package.v0` package schema and package-ref prefix.

Regression coverage: [braid-ideal-held-release-seed-path-rows.test.js](../../../tests/braid-ideal-held-release-seed-path-rows.test.js) now uses the same-record seed-path authority ref family for matching synthetic certificates and rejects otherwise complete certificates whose authority refs use generic wrong-family `external-authority:*` / `external-verification:*` labels.

Decision: this makes the exact non-repo authority source object sharper without producing or accepting seed-path evidence. The first live missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after a matching certificate, the next non-repo package remains `held_release_seed_path_rows_external_accepted_authority_package` / `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Adapter Accepted-Evidence Field Prefix Tightening

Integrated a read-only decoy audit after the adapter required-mode correction. The audit found no accepted seed-path certificate or external authority package in the live checkout, and it confirmed that the seed-path-specific verifier rejects stale `accepted:seed-path-rows:*`, wrong package families, correct-prefix `temp-probe` packages, ref-only payloads, copied-null payloads, and matching synthetic certificate/package pairs as non-authorizing. It also identified one broader adapter decoy class: non-seed accepted-evidence package fields could still use any generic `accepted:*` family to satisfy the same-record package criterion.

Executable change: [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now declares field-specific accepted-ref families for the same-record package provenance refs and all ten accepted-evidence package fields. Wrong-family accepted refs for path-history stream manifests, retained-history rows, provider objects, native path-history streams, native root-ledger detail rows, causal-root replay rows, same-record action closure, retained wake history, and provider provenance now fail at their own `central_solver_retained_source_adapter.accepted_evidence.*` field instead of letting the package criterion advance.

Regression coverage: [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) now mutates otherwise complete packages with wrong-family provenance refs and wrong-family accepted-evidence refs, then verifies that each mutated field fails closed with the declared required prefix and no adapter acceptance-certificate ref.

Decision: this closes the remaining known adapter package-family false positive without producing or accepting seed-path evidence. The exact first seed-path blocker remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after a matching certificate, the next non-repo object remains `held_release_seed_path_rows_external_accepted_authority_package` / `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Adapter Required Evidence Mode Kept Accepted-Only

Audited the retained-source adapter required-evidence mode after tightening the seed-path certificate boundary. A complete synthetic `central_solver_retained_source_adapter_same_record_accepted_evidence_package.v0` can satisfy the same-record package criterion, but it still leaves the adapter artifact at `accepted=false` because the non-repo external authority verification is absent.

Executable change: [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now keeps `--require-same-record-accepted-evidence` accepted-only. The CLI fails unless `artifact.evidence_evaluation.accepted === true`, not merely when the same-record accepted-evidence package criterion is internally satisfied. When a complete package lacks external authority verification, the diagnostic reports `same_record_accepted_evidence_conditionally_satisfied_repo_authorization_blocked` and stops at `central_solver_retained_source_adapter.external_accepted_authority_verification_ref`.

Regression coverage: [braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js](../../../tests/braid-ideal-central-solver-retained-source-adapter-acceptance-certificate.test.js) now verifies that required mode exits nonzero for a complete synthetic package, preserves `requirement_passed=false`, records `same_record_accepted_evidence_criterion_passed=true`, and keeps authorization flags false.

Decision: this closes the outer adapter semantic false positive where a conditionally satisfied package criterion could be mistaken for accepted evidence. It does not produce the accepted seed-path certificate. The exact first seed-path blocker remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; the adapter-side next authority after a complete package remains external accepted-authority verification. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path Required Mode Kept Accepted-Only

Audited the seed-path verifier's required-mode semantics after the external package identity tightening. The verifier still distinguishes two levels: a supplied certificate plus external authority package can be internally `conditionally_verified`, but the artifact remains `accepted=false` and still stops at `repo_authorization_for_accepted_held_release_seed_path_rows` / `held_release_seed_path_rows.acceptance_certificate_ref`.

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now keeps `held_release_seed_path_rows_acceptance_certificate_requirement.v0.requirement_passed=false` unless the verifier itself returns `accepted=true`. This means `--require-acceptance-certificate` now fails even for a matching conditionally verified certificate/package pair, while still emitting the conditionally verified diagnostic and the repo-authorization blocker.

Regression coverage: [braid-ideal-held-release-seed-path-rows.test.js](../../../tests/braid-ideal-held-release-seed-path-rows.test.js) now verifies that normal CLI output can report `conditionally_verified=true` for a synthetic matching pair, but required mode exits nonzero with `requirement_passed=false`, status `seed_path_acceptance_certificate_and_external_authority_conditionally_verified_repo_authorization_blocked`, first missing object `repo_authorization_for_accepted_held_release_seed_path_rows`, and first missing field `held_release_seed_path_rows.acceptance_certificate_ref`.

Decision: this closes the remaining semantic false positive where a conditionally verified JSON pair could be mistaken for a passed accepted-certificate requirement. It does not produce the accepted certificate. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path External Package Ref Family Tightening

Continued the source-acquisition run after the adapter accepted-ref prefix tightening and audited the next seed-path external-authority package boundary. The live seed-path requirement still reports `accepted=false`, `requirement_passed=false`, `first_missing_object="held_release_seed_path_rows_acceptance_certificate"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"` for `held_release_seed_path_rows:5833f18e53586201`.

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now requires `held_release_seed_path_rows_external_accepted_authority_package.v0` to carry an `external_accepted_authority_package_ref` in the declared same-record seed-path package identity, `external-authority-package:held-release-seed-path-rows:<retained-record-id>:<source-row-id>`, instead of accepting any generic or merely prefix-matching `external-authority-package:*` ref. The verifier and requirement diagnostic now emit this required package-ref prefix directly as `required_external_authority_package_ref_prefix`.

Regression coverage: [braid-ideal-held-release-seed-path-rows.test.js](../../../tests/braid-ideal-held-release-seed-path-rows.test.js) keeps the matching package on `external-authority-package:held-release-seed-path-rows:<retained-record-id>:<source-row-id>`, verifies that a wrong package family such as `external-authority-package:central-retained-source-adapter-review:*` fails at `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`, verifies that a correct-prefix `temp-probe` ref still fails, and verifies that copied-null certificate payloads over fail-closed artifacts cannot conditionally verify when `retained_record_id`, `source_row_id`, `provider_object_ref`, or `provider_artifact_hash` are absent.

Decision: this closes another target-only package-family decoy and a copied-null fail-closed source-artifact decoy without producing or accepting the seed-path certificate. The exact first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; after a matching certificate, the next non-repo source object/field remains `held_release_seed_path_rows_external_accepted_authority_package` / `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path Adapter Accepted-Ref Prefix Tightening

Integrated two read-only worker reports into the current seed-path source-acquisition run. The live checkout, reachable refs, PR/issue surfaces, readable attachments, and readable temp roots still contain no real `held_release_seed_path_rows_acceptance_certificate.v0` or `held_release_seed_path_rows_external_accepted_authority_package.v0` for `held_release_seed_path_rows:5833f18e53586201`. The current first missing object/field remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`; if a matching certificate is supplied, the next exact non-repo object/field remains `held_release_seed_path_rows_external_accepted_authority_package` / `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`.

The audit found one outer-adapter false-positive path: a stale fixture-shaped `accepted:seed-path-rows:*` string could satisfy the retained-source adapter same-record accepted-evidence criterion because the adapter criterion used a generic `accepted:` prefix check for every package field. This did not authorize closure or score movement, but it could allow a candidate adapter certificate to advance to external-verification blocking with the wrong seed-path accepted-ref family.

Executable change: [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now requires `held_release_seed_path_rows_acceptance_certificate_ref` to use the same canonical accepted-ref prefix required by the seed-path verifier: `accepted:held-release-seed-path-rows:*`. The criterion diagnostic now also reports the accepted-ref prefix expected for each evidence field. The regression test keeps complete packages on the canonical prefix and verifies that stale `accepted:seed-path-rows:*` refs fail at `central_solver_retained_source_adapter.accepted_evidence.held_release_seed_path_rows_acceptance_certificate_ref`.

Decision: the decoy path is closed without producing or accepting a seed-path certificate. The adapter still cannot produce the first accepted seed-path certificate or its external accepted-authority package. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path Authority Surface Sweep

Continued the four-hour source-acquisition run for `held_release_seed_path_rows:5833f18e53586201` after rereading the active goal objective and live startup files. The current collision map keeps the owned write set to this braid-ideal lane; unrelated equation-mapping files remain ambient exclusions.

Live diagnostics still report `accepted=false`, `requirement_passed=false`, `first_missing_object="held_release_seed_path_rows_acceptance_certificate"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"` for the provider-backed seed-path artifact. The retained-source adapter accepted-evidence contract still reports ten observed source fields and zero accepted package fields; the first package field is `held_release_seed_path_rows_acceptance_certificate_ref`, and its embedded source requirement still names `held_release_seed_path_rows_external_accepted_authority_package.v0` as the next object after a matching certificate is supplied.

Authority sweep results: PR #199, PR #200, GitHub issue/PR search, GitHub code search, reachable local and remote refs, `/Users/markmorris/vibe`, readable attachment roots, and readable temp roots still do not contain a real accepted same-record seed-path certificate or external accepted-authority package. The only `accepted:seed-path-rows:*` refs remain adapter test fixtures. The only readable temp JSON certificate candidates use fixture-style external refs, omit `source_row_id`, and have no matching `held_release_seed_path_rows_external_accepted_authority_package.v0`, so they remain decoys.

Source-owner matrix: the seed-path rows, stream manifest set, retained-history row, provider object, native path-history manifests, native root-ledger detail refs, causal-root replay refs, same-record action closure, retained wake history, and provider provenance are all observed or reproducible as current source surfaces, but none is accepted authority for the adapter package. The first accepted-evidence blocker therefore remains the non-circular `held_release_seed_path_rows_acceptance_certificate`; if a matching certificate is supplied, the next exact non-repo authority object is `held_release_seed_path_rows_external_accepted_authority_package` at `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`.

Decision: no accepted same-record seed-path certificate was produced in this cycle, and this checkpoint still does not satisfy the four-hour proof condition by itself. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path External Authority Package Boundary

Integrated the latest team-agent source-acquisition cycle for the current seed-path blocker. All workers agreed that no repo-local, attachment-local, or readable temp-root accepted `held_release_seed_path_rows_acceptance_certificate.v0` exists for `held_release_seed_path_rows:5833f18e53586201`; tests, fixtures, work-log prose, requirement shells, temp JSON probes, provider/source carrier outputs, and adapter package rows remain non-authorizing. The two readable temp JSON certificate candidates still fail the live verifier because they use fixture-style external refs and omit required same-record binding fields.

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now names the next non-repo source object explicitly as `held_release_seed_path_rows_external_accepted_authority_package.v0`. A matching `held_release_seed_path_rows_acceptance_certificate.v0` alone no longer satisfies `--require-acceptance-certificate`; it sharpens the blocker to `held_release_seed_path_rows_external_accepted_authority_package` / `held_release_seed_path_rows.external_authority.external_accepted_authority_package_ref`. The external authority package must bind the accepted certificate ref/hash, external authority and verification refs, `held_release_seed_path_rows:5833f18e53586201`, artifact hash `5833f18e53586201775fdcd490efcc1e649841e5268a15eea022cad9ff706063`, retained record `retained-record:held-release-six-point:adapter-acceptance-certificate`, source row `two-speed-preferred-row:u0.8:v0.2`, source run/dataset identity, provider object ref/hash, all six row ids and row hashes, and negative-control rejection.

Adapter impact: [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) still consumes the seed-path certificate as the first accepted-evidence package field. Its embedded seed-path requirement now carries the external authority package schema as the first non-repo authority object. This keeps the adapter non-circular: the adapter can verify that a real source package exists later, but it still cannot produce the first accepted seed-path certificate or its external accepted-authority package.

Decision: this advances the four-hour proof target by making the exact non-repo source object machine-readable and CLI-testable. It still does not produce the accepted same-record seed-path certificate, does not satisfy the four-hour proof condition by itself, and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement.

### 2026-07-03 - Seed-Path Acceptance Certificate Source-Intake Verifier

Added a direct source-intake verifier for the first seed-path blocker without adding a repo-local certificate producer. [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now emits `held_release_seed_path_rows_acceptance_certificate_requirement.v0` with `--print-acceptance-certificate-requirement`, accepts `--acceptance-certificate-json=<path>`, and supports `--require-acceptance-certificate`.

Executable status: for the current provider-backed artifact, the requirement diagnostic still reports `accepted=false`, `requirement_passed=false`, `first_missing_object="held_release_seed_path_rows_acceptance_certificate"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"`. The required source object schema is now explicit: `held_release_seed_path_rows_acceptance_certificate.v0` must bind `held_release_seed_path_rows:5833f18e53586201`, artifact hash `5833f18e53586201775fdcd490efcc1e649841e5268a15eea022cad9ff706063`, retained record `retained-record:held-release-six-point:adapter-acceptance-certificate`, source row `two-speed-preferred-row:u0.8:v0.2`, source run/dataset identity, provider object ref/hash, all six seed-path row ids and row hashes, `held_release_seed_path_rows_acceptance_certificate_ref`, an external accepted-authority ref, an external verification ref, accepted same-record evidence, non-repo external authority, and negative-control rejection.

Verifier boundary: a matching supplied JSON certificate can only become `seed_path_acceptance_certificate_conditionally_verified_repo_authorization_blocked`. It does not set `accepted=true`, does not flip any authorization flag, and does not move score. Ref-only accepted strings, copied candidate refs, missing source-row binding, missing provider binding, missing row ids or row hashes, fixture-style external authority refs, and missing non-repo external authority still fail closed. This keeps the adapter package non-circular: the adapter may consume a real `held_release_seed_path_rows_acceptance_certificate_ref` later, but the adapter package still cannot produce that first field.

Adapter wiring: [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now embeds this `held_release_seed_path_rows_acceptance_certificate_requirement.v0` under the first package field map row, `held_release_seed_path_rows_acceptance_certificate_ref`. The adapter contract still reports ten observed source fields and zero accepted package fields, but its first row now carries the exact source-object schema, required fields, expected row ids, expected row hashes, retained record, source row, provider object ref/hash, and missing field for the seed-path certificate.

Local source sweep: targeted searches under the repo, nearby attachment roots, and local temp roots found no real accepted seed-path certificate. Two temp JSON files from the verifier test were found under the system temp directory; with the stricter verifier they are rejected because they use fixture-style external authority refs and do not bind `source_row_id`. They are decoys, not source authority.

Decision: this advances the four-hour proof target by making the exact non-repo source object machine-readable and testable. It still does not produce the accepted same-record certificate, does not satisfy the four-hour proof condition by itself, and does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement.

### 2026-07-03 - Seed-Path Acceptance Authority Non-Circularity Check

Continued the retained-source adapter accepted-evidence package run in team-agent/coordinator mode for the current retained record/source row and integrated five read-only worker reports with local CLI verification:

- retained record: `retained-record:held-release-six-point:adapter-acceptance-certificate`
- source row: `two-speed-preferred-row:u0.8:v0.2`
- provider/source carrier: `central_solver_retained_history_provider_source_carrier:ba5a407d8e85bfc5`
- provider object: `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327`
- provider object hash: `7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df`
- seed-path artifact: `held_release_seed_path_rows:5833f18e53586201`

Worker A confirmed that the seed-path producer cannot honestly emit `held_release_seed_path_rows_acceptance_certificate` from the current provider-backed artifact. The artifact is reproducible, but its evaluator still returns `accepted=false`, `reason="producer_does_not_authorize_accepted_path_row_evidence"`, and `first_missing_field="held_release_seed_path_rows.acceptance_certificate_ref"`.

Worker B confirmed that the nearest reusable acceptance pattern is the retained-source adapter acceptance-certificate contract, but using that contract as the seed-path certificate producer would be circular because the package already consumes `held_release_seed_path_rows_acceptance_certificate_ref` as its first accepted source field. The seed-path certificate itself has no dedicated accepted producer schema in `scripts/braid-ideal`.

Worker C confirmed that no upstream provider/source-carrier field can directly promote the seed-path certificate. The stream manifest set, retained-history row, provider object, native path-history manifests, native root-ledger detail rows, causal replay rows, same-record action closure, retained wake history, and provider provenance are all candidate or unverified inputs, not accepted package inputs.

Worker D confirmed the decoy boundary. The only accepted-looking seed-path ref is `accepted:seed-path-rows:*` in the retained-source adapter acceptance-certificate test fixture; it is not runtime authority. The broader repo exact-name search found only the producer marker, adapter contract, tests, and work-log prose for `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows_acceptance_certificate_ref`.

Worker E reduced the ladder: the first acquisition-moving object remains `held_release_seed_path_rows_acceptance_certificate`, filling `held_release_seed_path_rows.acceptance_certificate_ref`; the provider/source carrier remains candidate-only behind `central_solver_retained_source_adapter.acceptance_certificate_ref`; and the adapter accepted-evidence package remains package-level blocked at `central_solver_retained_source_adapter.accepted_evidence.accepted_same_record_retained_source_adapter_evidence` with ten observed fields and zero accepted fields.

Decision: no accepted same-record source certificate was produced in this cycle, and this checkpoint does not satisfy the four-hour proof condition by itself. The next exact source authority is a non-circular accepted seed-path certificate authority for `held_release_seed_path_rows.v0` that binds `held_release_seed_path_rows:5833f18e53586201`, the retained record, source row, six path rows, source run/dataset identity, provider object ref/hash, and negative-control rejection. Until that object exists, ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized.

### 2026-07-03 - Seed-Path Source Artifact CLI Bound To Accepted-Evidence Contract

Resumed the retained-source adapter accepted-evidence package run after the first blocker-confirmation cycle. The seed-path source producer now has a command-line acquisition path for the exact current provider-backed source artifact consumed by the adapter contract:

- retained record: `retained-record:held-release-six-point:adapter-acceptance-certificate`
- source row: `two-speed-preferred-row:u0.8:v0.2`
- provider/source carrier: `central_solver_retained_history_provider_source_carrier:ba5a407d8e85bfc5`
- provider object: `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327`
- provider object hash: `7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df`
- seed-path artifact: `held_release_seed_path_rows:5833f18e53586201`

Executable change: [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) now accepts `--retained-record-id`, `--provider-object-ref`, and `--provider-artifact-hash`, so the current provider-backed seed-path artifact can be reproduced directly from the CLI instead of only through imports from the adapter/carrier builders. The focused test now asserts that this CLI emits `held_release_seed_path_rows:5833f18e53586201`, keeps `source_status="candidate_provider_backed_source_unaccepted"`, and stops at `held_release_seed_path_rows.acceptance_certificate_ref` with all authorization flags false.

Contract status: the retained-source adapter accepted-evidence contract still reports ten observed package fields and zero accepted package fields for this same retained record/source row. The first package field remains `held_release_seed_path_rows_acceptance_certificate_ref`; its current source value is the unverified `held_release_seed_path_rows:5833f18e53586201`, whose own first missing object/field is `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`.

Decision: this advanced source acquisition ergonomics and traceability, but it did not produce an accepted same-record source certificate. Ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized. The next exact missing object is still `held_release_seed_path_rows_acceptance_certificate`.

### 2026-07-03 - Seed-Path Acceptance Certificate Worker Cycle

Ran the retained-source adapter accepted-evidence package continuation in team-agent/coordinator mode against the current retained record/source row:

- `retained-record:held-release-six-point:adapter-acceptance-certificate`
- `two-speed-preferred-row:u0.8:v0.2`

Worker A confirmed that `held_release_seed_path_rows:5833f18e53586201` is reproducible from the repo-local builder, but remains `candidate_provider_backed_source_unaccepted`. Its direct first missing object/field is `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`. The accepted-looking `accepted:seed-path-rows:*` value exists only inside the retained-source adapter acceptance-certificate test fixture.

Worker B confirmed that the current provider provenance is still candidate-only. The provider object ref `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327` and the provider/source carrier `central_solver_retained_history_provider_source_carrier:ba5a407d8e85bfc5` bind the retained record and source row, but do not provide an accepted provider provenance ref. The provider/source carrier remains blocked at `central_solver_retained_source_adapter.acceptance_certificate_ref`, while the accepted-evidence package still requires an accepted same-record `provider_provenance_ref`.

Worker C confirmed that the native/app source bridge is real producer plumbing but remains candidate-only for all six native/provider package fields: durable path-history stream manifests, native root-ledger detail rows, causal-root replay rows, same-record action closure, retained wake history, and provider provenance. The current contract reports ten observed package fields and zero accepted package fields.

Decision: no accepted same-record source certificate was produced in this cycle. The current first source-certificate blocker for the retained-source adapter accepted-evidence package remains `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`. The package-level blocker remains `central_solver_retained_source_adapter_same_record_accepted_evidence_package` / `central_solver_retained_source_adapter.accepted_evidence.accepted_same_record_retained_source_adapter_evidence`. This does not authorize ideal-braid closure, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement.

### 2026-07-03 - Accepted-Evidence Package Provenance Class Tightened

Tightened the retained-source adapter accepted-evidence package criterion so package provenance cannot be a ref-only shell. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now requires `same_record_accepted_evidence_package_ref`, `same_record_accepted_evidence_package_authority_ref`, and `same_record_accepted_evidence_package_verification_ref` to be accepted-class refs, and requires `same_record_accepted_evidence_package_artifact_hash` to be a `sha256:` artifact hash.

Executable status: the current tree still has no real `central_solver_retained_source_adapter_same_record_accepted_evidence_package.v0` outside the test fixture. The live contract still reports ten visible source values and zero accepted package fields: four upstream artifact or row ids are unverified, and six native/provider refs are still `candidate:` refs. The first missing field therefore remains `central_solver_retained_source_adapter.accepted_evidence.accepted_same_record_retained_source_adapter_evidence`.

Decision: this does not authorize the adapter, the retained branch, chirality, Noether sea stabilization, return/stability, branch-chart/moving certificate, or score movement. It prevents a synthetic package-provenance shell from emitting the candidate adapter acceptance certificate while preserving the same acquisition target: a real accepted same-record package for the same retained record and source row.

### 2026-07-02 - Same-Record Accepted-Evidence Package Provenance Tightened

Closed a false-positive intake path in the retained-source adapter accepted-evidence criterion. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now requires the same-record accepted-evidence package to carry its own package provenance fields: `same_record_accepted_evidence_package_ref`, `same_record_accepted_evidence_package_authority_ref`, `same_record_accepted_evidence_package_verification_ref`, and `same_record_accepted_evidence_package_artifact_hash`. The criterion also rejects copied candidate refs in the ten accepted-evidence fields instead of treating any non-empty string as acceptable.

Executable status: accepted-looking refs without package provenance remain source-acquisition blocked at `central_solver_retained_source_adapter.accepted_evidence.same_record_accepted_evidence_package_ref`; copied candidate source refs remain blocked at the copied field, for example `central_solver_retained_source_adapter.accepted_evidence.native_root_ledger_detail_rows_ref`. A complete declared package still emits only a candidate adapter acceptance-certificate ref and then stops at `central_solver_retained_source_adapter.external_accepted_authority_verification_ref`.

Decision: this is not a new downstream gate and does not authorize accepted adapter evidence, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement. The active missing object remains a real same-record accepted-evidence package with package provenance plus accepted-class replacements for all ten mapped source fields.

### 2026-07-02 - Same-Record Accepted-Evidence Field Map Added

Sharpened the retained-source adapter accepted-evidence contract from a blank package template into a provider-facing field map. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now includes `source_acquisition_summary` and `source_acquisition_field_map` in `central_solver_retained_source_adapter_same_record_accepted_evidence_contract.v0`, mapping each required package field to the current provider/source carrier path, observed ref count, observed ref class, and required accepted replacement.

Executable status: for the current retained record and source row, all ten package fields have a visible current source value, but zero fields are accepted for the package. The first four fields are unverified artifact or row ids, while native path-history, native root-ledger detail, causal-root replay, same-record action closure, retained wake history, and provider provenance are candidate refs. The accepted package is therefore still absent, and the first missing field remains `central_solver_retained_source_adapter.accepted_evidence.accepted_same_record_retained_source_adapter_evidence`.

Decision: this does not create a new closure gate and does not authorize adapter evidence. It makes the acquisition boundary concrete: replace each mapped candidate or unverified carrier value with an accepted same-record package field, then rerun `--require-same-record-accepted-evidence`.

### 2026-07-02 - Same-Record Accepted-Evidence Requirement Gate Added

Added a strict acquisition gate for the retained-source adapter evidence package. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now supports `--require-same-record-accepted-evidence`; without a complete same-record package it exits nonzero and emits `central_solver_retained_source_adapter_same_record_accepted_evidence_requirement.v0`, naming `central_solver_retained_source_adapter_same_record_accepted_evidence_package` and the first missing field `central_solver_retained_source_adapter.accepted_evidence.accepted_same_record_retained_source_adapter_evidence`.

Executable status: with a complete `central_solver_retained_source_adapter_same_record_accepted_evidence_package.v0`, the requirement gate passes and emits the candidate adapter acceptance certificate, but still advances only to `central_solver_retained_source_adapter.external_accepted_authority_verification_ref`. The gate does not authorize accepted adapter evidence, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement.

Decision: Worker A now has a concrete provider-facing command that fails closed until the exact accepted same-record package exists. The active closure goal remains incomplete until a real accepted evidence package and a non-repo external accepted authority package are supplied for the same retained record and source row.

### 2026-07-02 - Same-Record Accepted-Evidence Contract Added

Added a machine-readable source-intake contract for the retained-source adapter evidence package. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now emits `central_solver_retained_source_adapter_same_record_accepted_evidence_contract.v0` with `--print-same-record-accepted-evidence-contract`, binding the expected retained record id, source row id, provider/source carrier id and hash, adapter id and hash, package schema, and all required accepted-evidence refs.

Executable status: the contract is explicitly `source_acquisition_contract_not_accepted_evidence`; it does not set any authorization flag or produce an accepted retained-source adapter. It names the package needed to fill `central_solver_retained_source_adapter.accepted_evidence.accepted_same_record_retained_source_adapter_evidence` and the next downstream field `central_solver_retained_source_adapter.external_accepted_authority_verification_ref` after a complete package is supplied.

Decision: the source acquisition surface is now explicit enough for a provider to construct the required `central_solver_retained_source_adapter_same_record_accepted_evidence_package.v0` without relying on test fixtures. The active closure goal still requires a real accepted evidence package for the same retained record and source row.

### 2026-07-02 - Same-Record Accepted-Evidence JSON Intake Added

Advanced the retained-source adapter certificate from programmatic-only evidence injection to a CLI source-intake path. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) now recognizes `central_solver_retained_source_adapter_same_record_accepted_evidence_package.v0` and accepts `--same-record-accepted-evidence-json=<path>` plus `--external-accepted-authority-verification-json=<path>` for source-package intake.

Executable status: with no evidence package it still stops at `central_solver_retained_source_adapter.accepted_evidence.accepted_same_record_retained_source_adapter_evidence`; with a complete same-record accepted-evidence package it emits a candidate `central_solver_retained_source_adapter_acceptance_certificate` ref and advances to `central_solver_retained_source_adapter.external_accepted_authority_verification_ref`. A wrong accepted-evidence package schema fails closed. The repo artifact still does not authorize accepted adapter evidence, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement.

Decision: the hard artifact moved from a test-only accepted-evidence bundle to a reusable source-intake path. The active closure goal is still incomplete until a real accepted same-record evidence package and non-repo external accepted authority package are supplied for the same retained record and source row.

### 2026-07-02 - Retained-Source Adapter Acceptance-Certificate Target Added

Implemented the next fail-closed certificate boundary for the retained-source adapter. [central-solver-retained-source-adapter-acceptance-certificate.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter-acceptance-certificate.mjs) emits `central_solver_retained_source_adapter_acceptance_certificate.v0`, consumes the bound provider/source carrier, checks that same-record accepted evidence is declared for the retained record and source row, and rejects cross-record evidence before emitting a candidate adapter acceptance-certificate ref.

Executable status: without same-record accepted evidence it remains `adapter_acceptance_certificate_source_acquisition_blocked`; with the required same-record evidence refs it emits a candidate `central_solver_retained_source_adapter_acceptance_certificate` ref and stops at `central_solver_retained_source_adapter.external_accepted_authority_verification_ref`. External verification now requires an explicit `central_solver_retained_source_adapter_external_accepted_authority_package.v0` object; string-only external-authority refs are rejected. With a declared matching external-authority package it records conditional verification but still stops at `central_solver_retained_source_adapter.accepted_retained_source_adapter_ref`. The repo artifact does not authorize accepted adapter evidence, retained branch output, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement.

Decision: the adapter acceptance-certificate source object is now executable as a conditional target, but the active closure goal is not complete. The next exact missing object is a non-repo external accepted authority that can promote the candidate certificate to an accepted retained-source adapter ref for the same retained record and source row.

### 2026-07-02 - Retained-History Provider/Source Carrier Added

Implemented the source-side carrier requested by the provider acquisition ladder. [central-solver-retained-history-provider-source-carrier.mjs](../../../scripts/braid-ideal/central-solver-retained-history-provider-source-carrier.mjs) emits `central_solver_retained_history_provider_source_carrier.v0`, composing the existing retained-source adapter with explicit native/app provenance for `SolverAppBridge.createPathHistoryStreamF64` / `PathHistoryRowF64` path-history streams and `SolverAppBridge.buildRootLedgerDetailF64WithModule` / `RootLedgerDetailRowF64` root-ledger detail rows. The CLI can emit the bound candidate with `--retained-record-id=... --source-row-id=...`, while the default CLI path remains fail-closed at the missing retained record id.

Executable status: with no retained record id it still stops at `held_release_seed_path_rows[*].retained_record_id`; with a retained record id it auto-binds six candidate native/app durable path-history stream manifest refs and stops at `central_solver_retained_source_adapter.same_record_binding.source_row_id`; with a source row id it binds thirty-six candidate native root-ledger detail refs, thirty-six causal-root replay refs, same-record action closure, retained wake history, provider object provenance, and the nested `central_solver_retained_source_adapter.v0`, then stops at `central_solver_retained_source_adapter.acceptance_certificate_ref`. A declared certificate still stops at `central_solver_retained_source_adapter.external_accepted_authority_verification_ref`.

Decision: the hard artifact moved from an adapter-only target to a concrete fail-closed provider/source carrier. Ideal-braid closure, accepted retained branch evidence, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, and score movement remain unauthorized. The next exact missing field is the adapter acceptance certificate for the same retained record and source row, followed by external accepted-authority verification if that certificate is declared.

### 2026-07-02 - Workers A-C Minimal Closure Ladder Integration

Integrated the retained-source adapter, `R_J`, and local Noether sea stabilization handoffs without changing claim level. Minimal closure ladder: (1) acquire a real retained-history provider/source carrier for `central_solver_retained_source_adapter.v0`; the live first field is `held_release_seed_path_rows[*].retained_record_id`, sharpening to `central_solver_retained_source_adapter.same_record_binding.source_row_id` after durable streams and then to `central_solver_retained_source_adapter.acceptance_certificate_ref` after the same retained record binds native root-detail refs, causal replay, action closure, wake history, and provider provenance; (2) populate `same_record_angular_momentum_residual_row` so `braid_ideal_chirality_retained_history_target.residual_vector.R_J.value.measurement_passed` can test signed `L_int` reversal with magnitude preservation on that same accepted retained record; (3) if the Noether sea route is used, populate `retained_noether_sea_pressure_response_row.theta_sea_rho_NS` on the same ideal-braid record, local Noether sea state, nearby Noether braid population rows, boundary wake data, and action/exchange provenance, with at least one post-turn row satisfying `\ddot R_{\mathrm{toy}}(t_i)+\Pi_R\mathcal A^{\mathrm{sea}}(t_i)<0` and the current diagnostic floor `\Pi_R\mathcal A^{\mathrm{sea}}(t_i)<-0.0934863494737535`; (4) only after those rows exist, revisit return/stability rows and branch-chart/moving certificates.

Decision: next honest target is provider acquisition, not another target-only checker. No ideal-braid closure, retained branch certificate, chirality authorization, Noether sea stabilization, branch-chart/moving certificate, return/stability claim, or score movement advanced in this pass.

### 2026-07-02 - Retained-Source Adapter Producer Boundary Added

Advanced the `internal_tangent_authority_derivation` evidence boundary from a consumer-side accepted-bridge criterion to a source-side adapter target. [central-solver-retained-source-adapter.mjs](../../../scripts/braid-ideal/central-solver-retained-source-adapter.mjs) emits `central_solver_retained_source_adapter.v0`, which binds six held-release seed path rows, six durable path-history stream manifest refs, one central retained-history provider object ref, one preferred-curve `source_row_id`, thirty-six native root-ledger detail refs, thirty-six causal-root replay refs, same-record action closure, retained wake history, retained record id, and provider provenance before asking for an adapter acceptance certificate.

Executable status: the adapter fails closed first at `held_release_seed_path_rows[*].retained_record_id`; with a provider-backed retained record and six durable streams it sharpens to `central_solver_retained_source_adapter.same_record_binding.source_row_id`; with source row, root details, causal replay, action closure, and wake history present it sharpens to `central_solver_retained_source_adapter.acceptance_certificate_ref`. It remains non-authorizing: `accepted_retained_source_adapter_ref=null`, retained-branch authorization flags stay false, and the artifact cannot itself promote accepted internal tangent authority.

Checker status: `node --check scripts/braid-ideal/central-solver-retained-source-adapter.mjs` passed, the focused adapter test passed with 4 tests, the full `node --test tests/braid-ideal-*.test.js` suite passed with 132 tests, and `git diff --check`, `node scripts/validate-content.mjs --check --strict`, and `node scripts/build-scene-graph.mjs --check --strict` passed.

Current blocker: acquire or produce the accepted adapter certificate and external accepted-authority verification for the same retained record and source row, then connect that accepted retained-source adapter to the central internal tangent-authority accepted-bridge criterion.

### 2026-07-02 - Chirality `R_J` Producer-Owner Scout Handoff

Thread 3 completed the read-only `R_J` producer-owner scout with no repo file changes. It found no clean repo-local central/native, quantum, angular-momentum, or retained-history producer surface that can emit or adapt a non-fixture same-record `same_record_angular_momentum_residual_row` for `braid_ideal_chirality_retained_history_target.v0`.

Exact first missing object/field remains `same_record_angular_momentum_residual_row` -> `braid_ideal_chirality_retained_history_target.residual_vector.R_J.value.measurement_passed`; live failure reason `same_record_angular_momentum_residual_row_missing`. The owning chirality target names the `R_J` relation, including internal angular-momentum sign reversal with magnitude preservation, but it is a consumer/target surface rather than a source producer.

Producer-owner decision: `source_acquisition_blocked`. Central retained-history rows, native/app solver path-history and motion surfaces, the quantum history-layer phase extractor, the three-binary angular-momentum runner, braid-angular-momentum priority packets, oblate residual artifacts, and sampled or cross-lane angular-momentum rows do not bind the braid-ideal retained record, provider object, retained root ledger, causal-root replay, same-record action closure, and `R_J` angular-momentum relation on one accepted retained record. Dirty checkpoint rows, target contracts, diagnostics, fixtures, priority prose, proxy refs, candidate refs, T3 rows, H39/theta3minus quotient rows, sampled residuals, equation-mapping rows, and nuclear-atomic rows remain non-authorizing.

Next action: acquire or produce a same-record angular-momentum residual source object for `R_J` with retained record binding, provider provenance, retained root ledger identity, causal-root replay, same-record action closure, the `L_int` sign-reversal / magnitude-preservation row, and negative controls. Do not reopen chirality authorization, retained branch output, particle-sector promotion, or score movement from another target-only checker.

### 2026-07-02 - Same-Record Action Closure Added To The Central Bridge

Advanced the `internal_tangent_authority_derivation` bridge from tangent/root-margin compatibility to tangent/root-margin/action compatibility. [central-solver-internal-tangent-authority-vector-rows.mjs](../../../scripts/braid-ideal/central-solver-internal-tangent-authority-vector-rows.mjs) now consumes `same_record_action_closure_row.v0` rows and checks $\left|\Delta A_{\mathrm{internal}}-\Delta A_{\mathrm{clock}}\right|\le\epsilon_A$ on the same retained record.

Executable status: the full mathematical bridge now requires a shared `source_row_id` across the minimum-gain row, retained-vector witness row, preferred-curve equation artifact, retained-root detail rows, and same-record action closure row. If preferred-curve math and root-detail rows pass but action rows are absent, the bridge reports `preferred_curve_passed_same_record_action_closure_rows_missing`; if the action residual exceeds its tolerance, it reports `fail_closed_same_record_action_closure_rows_failed`; if the action rows belong to a different source row, it reports `fail_closed_same_record_source_row_binding_missing`.

Checker status: focused bridge, retained-history request, provider object, and tangent-certificate tests passed with 39 tests after the action-closure evaluator and request row family were added. The full `node --test tests/braid-ideal-*.test.js` suite passed with 128 tests, and `git diff --check`, `node scripts/validate-content.mjs --check --strict`, and `node scripts/build-scene-graph.mjs --check --strict` passed.

Current blocker: accepted central retained-solver refs must still replace the diagnostic action closure row with accepted same-record action evidence, while also carrying accepted retained-root detail rows, retained path-history/path-error rows, tangent target, active margin gradient, post-provider margin, retained root/wake/path ledgers, provider provenance, and acceptance certificates.

### 2026-07-02 - Retained-Root Detail Rows Evaluated At The Central Bridge

Advanced the `internal_tangent_authority_derivation` bridge from requesting retained-root detail rows to evaluating them at the same-record central bridge boundary. [central-solver-internal-tangent-authority-vector-rows.mjs](../../../scripts/braid-ideal/central-solver-internal-tangent-authority-vector-rows.mjs) now consumes `same_record_retained_root_ledger_detail_row.v0` rows and checks the causal-root residual/sensitivity carrier, same retained-record binding, nonzero root `jacobian`, nonzero source-normal denominator, finite branch weight, finite receiver-normal factor, and source-row identity.

Executable status: the full mathematical bridge now requires a shared `source_row_id` across the minimum-gain row, retained-vector witness row, preferred-curve equation artifact, and retained-root detail rows. If preferred-curve math passes but root-detail rows are missing, the bridge reports `preferred_curve_passed_retained_root_ledger_detail_rows_missing`; if root-detail rows are present but belong to a different source row, it reports `fail_closed_same_record_source_row_binding_missing`.

Checker status: the focused central bridge test passed with 13 tests after the evaluator and negative controls were added. The focused touched-artifact set passed with 36 tests, the full `node --test tests/braid-ideal-*.test.js` suite passed with 125 tests, and `git diff --check`, `node scripts/validate-content.mjs --check --strict`, and `node scripts/build-scene-graph.mjs --check --strict` passed.

Current blocker: accepted central retained-solver refs must still replace the diagnostic retained-root detail rows with accepted same-record root-detail evidence plus retained path-history/path-error rows, tangent target, active margin gradient, post-provider margin, retained root/action/wake/path ledgers, provider provenance, and acceptance certificates.

### 2026-07-02 - Chirality `R_J` Angular-Momentum Source Scout Handoff

Thread 2 completed the read-only `R_J` source-acquisition scout with no repo file changes. It found no clean repo-local non-fixture accepted or nearer-accepted same-record angular-momentum residual source object for `braid_ideal_chirality_retained_history_target.v0`.

Exact first missing object/field: `same_record_angular_momentum_residual_row` -> `braid_ideal_chirality_retained_history_target.residual_vector.R_J.value.measurement_passed`; live failure reason `same_record_angular_momentum_residual_row_missing`. Cross-lane angular-momentum ledgers, wake-history diagnostics, priority prose, and central retained-history hooks are non-authorizing because they do not bind the braid-ideal retained record, central retained-history row, provider object, retained root ledger, causal-root replay, same-record action closure, and angular-momentum reversal relation on one retained record.

Evidence decision: `source_acquisition_blocked`. The smallest useful next artifact is a non-fixture same-record `same_record_angular_momentum_residual_row` source object for `R_J`, with retained record binding, provider provenance, retained root ledger, causal-root replay, same-record action closure, and the declared `L_int` sign-reversal / magnitude-preservation row. Do not reopen chirality authorization, retained branch output, particle-sector promotion, or score movement from diagnostics, fixtures, cross-row bundles, H39/theta3minus quotient rows, or mathematical-pass-but-unaccepted rows.

### 2026-07-02 - Chirality `R_action` Residual Rows Added

Advanced the accepted measurement-row mode for `braid_ideal_chirality_retained_history_target.v0` so `R_action` now has two accepted same-record action/energy residual rows, one for each paired pro/anti branch row. The rows bind to the same retained record, central retained-history row, provider object, and `same_record_action_ledger_rows` hook, preserve the charge-conjugate action/energy equality relation, and report zero action and energy difference residuals.

Checker status: `node --check scripts/braid-ideal/matter-antimatter-chirality-retained-history-target.mjs` passed, the focused chirality test passed with 9 tests, the full `node --test tests/braid-ideal-*.test.js` suite passed with 122 tests, path-scoped `git diff --check` passed, `node scripts/validate-content.mjs --check --strict` passed, and `node scripts/build-scene-graph.mjs --check --strict` passed. The direct artifact audit reports `R_phase`, `R_root`, `R_self`, `R_wake`, `R_action`, and `R_charge` passing with validation errors `[]`. The first residual blocker is now `R_J`, with missing same-record angular-momentum residual rows.

Partition outcome: the compact tracker records only the advanced blocker and promotion gate, while [matter-antimatter-chirality-retained-history-target.md](matter-antimatter-chirality-retained-history-target.md) carries the structured row-schema status. This work-log entry carries the dated checker narrative.

### 2026-07-02 - Retained-Root Differential Carrier Added

Resumed the active `internal_tangent_authority_derivation` goal after the accepted-bridge same-record binding checkpoint. The retained-history tangent-response target now carries an explicit root-ledger differential source: the causal-root residual $\Phi_{ab}(t,\tau;q)=\|\mathbf x_a(t;q)-\mathbf x_b(t-\tau;q)\|^2-c_f^2\tau^2=0$ and the root-sensitivity equation $\partial_i\tau_{ab}=-(\partial_i\Phi_{ab})/(\partial_\tau\Phi_{ab})$ when the root Jacobian is away from zero.

Executable status: [oblate-spheroid-internal-tangent-authority-certificate.mjs](../../../scripts/braid-ideal/oblate-spheroid-internal-tangent-authority-certificate.mjs) and [central-solver-retained-history-row.mjs](../../../scripts/braid-ideal/central-solver-retained-history-row.mjs) now require same-record retained-root detail rows with root identity, residual, `jacobian`, source-normal denominator, receiver-normal factor, branch weight, root kind, entry kind, status code, and state flags. The central bridge accepted-evidence criterion also requires `same_record_retained_root_ledger_detail_rows_ref` so an accepted-looking evidence bundle cannot omit the root-detail differential carrier.

Checker status: focused tests passed for the retained-history row, provider object, internal tangent-authority certificate, and central bridge after the code changes. The full `node --test tests/braid-ideal-*.test.js` suite passed with 122 tests.

Current blocker: accepted central retained-solver refs must bind the same retained record and source row as the passing bridge rows and must include retained-root detail rows, retained path-history/path-error rows, tangent target, active margin gradient, post-provider margin, retained root/action/wake/path ledgers, provider provenance, and acceptance certificates.

### 2026-07-02 - Coordinator Validation And Source-Acquisition Decision

Inspected all five worker threads plus the collision-only app thread after resuming the braid-ideal lane. The latest completed handoffs remain source-acquisition blocked at accepted central retained-history / retained-record evidence, with no repo-local accepted retained evidence or score movement.

Validated the visible accepted-bridge same-record binding checkpoint in [central-solver-internal-tangent-authority-vector-rows.mjs](../../../scripts/braid-ideal/central-solver-internal-tangent-authority-vector-rows.mjs) and [braid-ideal-central-solver-internal-tangent-authority-vector-rows.test.js](../../../tests/braid-ideal-central-solver-internal-tangent-authority-vector-rows.test.js). Current validation passed `node --check` for the touched bridge script/test, the focused bridge test, the affected internal tangent-authority test, the full `node --test tests/braid-ideal-*.test.js` suite with 122 tests, path-scoped `git diff --check`, `node scripts/validate-content.mjs --check --strict`, and `node scripts/build-scene-graph.mjs --check --strict`.

Dispatch decision: do not launch another target-only checker. The next useful work remains source acquisition or a real central retained-solver provider adapter that binds native path-history/root-ledger outputs to one retained record, all six held-release seed path rows, provider provenance, durable stream manifests, retained root ledger identity, causal-root replay, same-record action closure, and acceptance certificates.

### 2026-07-02 - Accepted Bridge Same-Record Binding Tightened

Resumed the `internal_tangent_authority_derivation` goal under the partitioned priority-file convention. Confirmed the branch was current with `origin/codex/galatea`, read [../README.md](../README.md), [priorities.md](priorities.md), [brainstorming.md](brainstorming.md), and this work log before writing, then kept the live tracker to a compact blocker refinement.

Executable change: `central_solver_internal_tangent_authority_accepted_bridge_criterion.v0` now requires accepted evidence to supply both `retained_record_id` and `source_row_id`. The retained record id must match the central retained-history request, and the source row id must be shared by a passing minimum-gain witness row, passing retained-vector witness row, and passing preferred-curve equation artifact. Negative controls now fail when accepted refs are declared against the wrong retained record or the wrong passing source row.

Checker status: `node --test tests/braid-ideal-central-solver-internal-tangent-authority-vector-rows.test.js` passed with 10 tests. The full `node --test tests/braid-ideal-*.test.js` suite passed with 122 tests, and `git diff --check`, `node scripts/validate-content.mjs --check --strict`, and `node scripts/build-scene-graph.mjs --check --strict` passed.

Current blocker: replace fixture/declared accepted refs with actual accepted central retained-solver refs bound to the same retained record and source row as the passing bridge rows.

### 2026-07-02 - Retained-History Chirality Residual Ladder Resume

Resumed after the priority-directory partitioning pass. Read [../README.md](../README.md), [priorities.md](priorities.md), [brainstorming.md](brainstorming.md), and [matter-antimatter-chirality-retained-history-target.md](matter-antimatter-chirality-retained-history-target.md) before making changes. The current tracker is already compact and routes detailed status to the focused chirality packet, so no tracker migration was needed.

Current executable status: the accepted measurement-row mode for `braid_ideal_chirality_retained_history_target.v0` reports `R_phase`, `R_root`, `R_self`, `R_wake`, and `R_charge` as passing measurements of the current provider-backed source state. The first residual blocker is `R_action`, with missing same-record action/energy residual rows. The packet remains priority-only and does not authorize accepted chirality evidence, matter/antimatter discovery, particle-sector promotion, retained branch evidence, or score movement.

Partition outcome: conceptual material about the octahedral support asymmetry, Noether sea stabilization route, and braid-level orbit-reversal possibility remains in [brainstorming.md](brainstorming.md). The structured target, residual definitions, schema behavior, and promotion gate remain in [matter-antimatter-chirality-retained-history-target.md](matter-antimatter-chirality-retained-history-target.md). This work-log carries the dated resume/status narrative only.

Next action: supply same-record action/energy residual rows so `R_action` can become the next passing chirality residual measurement, then rerun the focused chirality target tests and priority markdown validation.

### 2026-07-02 - Preferred-Curve Tangent-Authority Residual Bridge Resume

Resumed the paused `internal_tangent_authority_derivation` work after the priority-directory partitioning pass. Pulled the latest branch state (`git pull --ff-only`: already up to date), then read [../README.md](../README.md), [priorities.md](priorities.md), [brainstorming.md](brainstorming.md), and this work log before writing.

Current executable status: the preferred-curve tangent-authority lane now has the curve stationarity equation, same-source minimum-gain retained-history response, tangent-null margin lift, branch-clock-lock replacement residual, and central bridge precondition in executable form. The central bridge separates core preferred-curve stationarity from replacement-ready math: vector rows can pass, preferred-curve math can pass, and the bridge still fails if $\left|\|\mathbf T(q)\|-A_{\mathrm{clock,rms}}(q)\right|>\epsilon_{\mathrm{lock}}$.

Partition outcome: the conceptual equation route and readable proof boundary remain in [brainstorming.md](brainstorming.md). The live tracker now only records the compact promotion gate and current blocker for `internal_tangent_authority_derivation`. This work-log entry carries the dated resume/checker narrative only.

Current blocker: accepted central retained-solver rows must supply retained path-history errors, retained-root/action/wake/path ledgers, provider provenance, a tangent target matching the assigned clock-lock burden, and positive post-provider causal-root margin on the same retained record.

Validation: `node --test tests/braid-ideal-*.test.js`, `git diff --check`, `node scripts/validate-content.mjs --check --strict`, and `node scripts/build-scene-graph.mjs --check --strict` passed in the resumed thread before this partitioning note.

### 2026-07-02 - Central Accepted-Bridge Criterion Added

Advanced the `internal_tangent_authority_derivation` bridge from a mathematical-pass acceptance blocker to an explicit accepted-evidence criterion. The bridge now emits `central_solver_internal_tangent_authority_accepted_bridge_criterion.v0`, listing the same-record accepted refs required for central retained-history acceptance, preferred-curve internal tangent-authority acceptance, retained path-error rows, minimum-gain rows, vector witness rows, tangent target rows, active margin-gradient rows, provider acceleration rows, post-provider root-margin rows, branch-clock replacement residual rows, retained-root/action/wake/path ledgers, and provider provenance.

Checker status: the focused bridge test suite passes with a new conditional accepted-evidence fixture. The criterion remains non-authorizing: a fully supplied accepted-ref fixture can satisfy the criterion only as `conditional_on_external_accepted_authority`, while `accepted=false`, `accepted_internal_tangent_authority_ref=null`, and all bridge authorization flags stay false.

Current blocker: replace fixture refs with accepted central retained-solver refs on the same retained record, then rerun the full braid-ideal suite and content checks.

### 2026-07-02 - Coordinator Pause Resume With Partitioned Handoff

Resumed the paused coordinator wave under the priority-directory partitioning convention. Pulled the latest branch state (`git pull --ff-only`: already up to date), then read [../README.md](../README.md), [priorities.md](priorities.md), [brainstorming.md](brainstorming.md), and this work log before recording the handoff. The main tracker was left compact; no status history or conceptual draft material was moved into it.

Latest inspected braid-ideal handoffs remained source-acquisition blocked, not checker-hardening-ready. The repeated first source boundary is accepted central retained-history / retained-record evidence: `held_release_seed_path_rows[*].retained_record_id`, then provider provenance, durable stream manifests, retained root ledger identity, causal-root replay, wake/action/path rows, and same-record action closure on one retained record.

The active `internal_tangent_authority_derivation` blocker remains the same accepted central retained-solver row family. The preferred-curve equation, minimum-gain retained-history response, vector-row bridge, and branch-clock-lock replacement residual are useful hard mathematical artifacts, but they remain non-authorizing until the same retained record supplies accepted path-history errors, retained-root/action/wake/path ledgers, provider provenance, and a tangent target matching the assigned clock-lock burden.

Next action: source acquisition or a real producer boundary for accepted central retained-solver rows. Do not add another target-only checker for this lane unless a concrete false-positive acceptance path appears.

### 2026-07-02 - Braid-Ideal Source-Acquisition Worker Integration

Resumed the coordinator wave with braid-ideal as the active focus. Started from `git status --short --untracked-files=all` and treated the modified priority/app/media/equation/nuclear work-log files as collision exclusions. Inspected the five existing worker threads plus the collision-only app thread before dispatching new work.

Latest worker handoffs produced no repo edits and no accepted retained evidence. The source-acquisition matrix still converges on the central retained-history provider boundary:

- Thread 1: `central_solver_retained_history_provider_object.v0` remains blocked first at `six_held_release_seed_path_rows_for_retained_record` / `held_release_seed_path_rows[*].retained_record_id`; downstream provider provenance, durable stream refs, retained root ledger identity, causal-root replay, and same-record action closure remain absent.
- Thread 2: same-record minimum-norm retained-history gain has no accepted source object; the gain witness is a mathematical route only until retained path-error rows, active causal-margin gradient rows, retained root ledger identity, same-record action closure, and provider provenance bind to one retained record.
- Thread 3: retained-history tangent-response rows are absent as clean accepted evidence; exact tangent-response row names exist only as non-authorizing target/checkpoint context, with the same parent retained-record/provider-object blocker.
- Thread 4: active causal-margin gradient and post-provider root-budget margin rows have no accepted same-record source; retained root ledger and retained record identity are still missing before those rows can count.
- Thread 5: accepted branch-chart / moving retained branch certificate remains source-acquisition blocked at `accepted_same_record_branch_chart` for `q:index-ratio:f2`; `same_record_identity.accepted_branch_chart_ref` is still a proxy and `torque_wake_retained_active_row_branch_certificate_evidence_object/v0.branch_certificate_ref` remains absent downstream.

Local coordinator inspection found the only promising non-target-only next boundary in the central solver/app bridge rather than another braid-ideal target checker. Current native/app surfaces expose path-history streams, root-ledger buffers/detail, path-history lifecycle/readback, native-file manifest/index/chunk artifacts, and work-packet planning. They do not yet expose braid-ideal accepted retained-record identity, central retained-history provider provenance, durable same-record provider object refs, or acceptance certificates. The next scout should therefore classify whether those central solver outputs can become a real retained-history producer adapter for the six held-release seed path rows, or whether they remain generic stream/root-ledger infrastructure without accepted same-record binding.

Next action: dispatch at most one read-only central retained-solver producer-boundary scout. The scout should inspect the native/app bridge path-history and root-ledger producer surfaces as possible inputs to `held_release_seed_path_rows[*].retained_record_id`, not add another target-only braid-ideal checker.

### 2026-07-02 - Central Retained-Solver Producer-Boundary Scout Handoff

Thread 1 completed the read-only central retained-solver producer-boundary scout with no repo file changes. It found real central solver producer infrastructure, but no accepted retained-history provider input.

Producer-boundary facts:

- Native path-history stream surfaces carry `pathKey`, `segmentIndex`, time bounds, position, velocity, `errorBound`, stream manifest data, index/chunk refs, checksums, and run/dataset/config metadata.
- Native root-ledger detail surfaces carry source/receiver/root keys, `hitTime`, delay, residual, Jacobian, bracket fields, receiver-normal factors, and status codes.
- Root-ledger transition classification can distinguish retained/appeared/disappeared/folded root-detail transitions.
- The app bridge can host stream handles, native-file storage, dynamic replay metadata, generic provenance, path-history readback, and work-packet planning.
- The bridge checker and fixtures validate this solver/app plumbing, but remain smoke/validation coverage and do not constitute accepted retained-history evidence.

Evidence decision: `source_acquisition_blocked_internal_producer_owner`. The central solver gives a credible implementation substrate, but no clean producer currently binds native path-history and root-ledger outputs to one retained record, all six held-release seed path rows, provider object provenance, durable manifest refs, retained root ledger identity, causal-root replay, same-record action closure, and acceptance certificates.

Exact first missing field remains `held_release_seed_path_rows[*].retained_record_id` for `six_held_release_seed_path_rows_for_retained_record`, consumed by `central_solver_retained_history_provider_object.v0`.

Next action: implement or acquire a retained-history provider input, not another checker. The useful edit boundary is a central solver adapter/source object that binds native path-history/root-ledger outputs to one retained record and all six held-release seed path rows with provider provenance, while staying fail-closed until durable manifests, retained root ledger identity, causal-root replay, same-record action closure, and acceptance certificates are present.

### 2026-07-07 - Interior Ponderomotive Diagnostic Closed Negative At Six-Accessory Count

Executed `interior_field_ponderomotive_diagnostic` (Group A) as a test-charge, fail-closed diagnostic on the axis-neutral rotating 120-degree-phased channel of the six-point lemma. New owner script `scripts/braid-ideal/interior-axial-potential-gradient-diagnostic.mjs` with tests in `tests/braid-ideal-interior-axial-potential-gradient-diagnostic.test.js` (6 tests, all passing). Strict potential-superposition terminology: superposed delayed potential and gradient at a hypothetical static receiver, delays at field speed $c_f$ on the exact rotating worldlines, receiver-normal numerator of a static receiver, shared source-normal Jacobian-floor convention. Canonical $c_f=1$, $R=1$, $\kappa=1$, $\varepsilon=0.05$, $\beta\in\{0.3,0.6,0.9\}$.

Recorded results:

- On-axis staticity exact: sampled oscillatory residual $\le7\times10^{-10}$ at every $\beta$; delayed center gradient matches the softened analytic $6\kappa h/(R^2+\varepsilon^2)^{3/2}$ to eight digits (test-pinned).
- Delayed polarity sorting confirmed: $\partial_\parallel\Phi\ge1.40\,\kappa/R^2$ on the interior axis $|z|<R/\sqrt3$, so $\epsilon_{-,\bullet}$ is pushed toward the positrino face and $\epsilon_{+,\bullet}$ toward the electrino face, matching the leading-order entry.
- Single-test-charge trap minimum exists: $\epsilon_-$ minimum at $\rho\approx0.13R$, $z\approx+0.80R$ ($\iota$-mirrored for $\epsilon_+$), sampled watershed depth $\approx0.68\,\kappa/R$ over the polar escape route; depth is static-well dominated (ponderomotive share at the minimum $\approx0.007\,\kappa/R$) while the ponderomotive term supplies the transverse barrier ($\sim1.6\times10^2\,\kappa/R$ near the rotating ring) that blocks infall onto the positrino sites. Quiver validity passes at $\beta\in\{0.6,0.9\}$, fails at $\beta=0.3$.
- Mechanism comparison negative: per-accessory mutual repulsion at every $C_3$-compatible candidate spacing with all six sites inside the sampled basin runs $7.3$ to $40\,\kappa/R$ against depth $0.68\,\kappa/R$ — shortfall factor $\ge10$, coupling-independent since both sides scale as $\kappa$; parity would need spacing $\approx4.9R$, outside any basin.

Decision: `ponderomotive_accessory_confinement_disfavored_origin_cluster_repulsion_objection_reinstated` at six-accessory count. Queue item closed negative; capture entry added to the discussion log; negative recorded in the owning `central_inventory_inside_hollow_support` slot in `braid-retained-branch-closure`. Surviving positives for downstream use: exact on-axis staticity, delayed axial sorting, single-accessory axial-interior trapping, and the transverse ponderomotive barrier. The dressing-geometry program should not consume ponderomotive depth alone at $N=6$; candidate rescues are per-lobe count reduction, deeper traps from breathing or nested structure, sea response, and dynamic accessory placement.

Next action: `sh0sea_dipole_wake_sum` remains the next Group A diagnostic; any dressing-geometry follow-up should target the rescue candidates above rather than re-running the static six-accessory comparison.

### 2026-07-07 - Fold-Crossing Chart Spec Draws the Self-Hit Producer Boundary

Drafted the producer-boundary specification for `self_hit_held_release_solver_row` (Group A item 3) as the packet-level definition of the controlled hinge click, consuming the same-day self-hit ejection witness as its failure-mode fixture. New packet `reference/priorities/braid-ideal/fold-crossing-chart-spec.md`; sibling fixture `scripts/braid-ideal/fold-crossing-chart-fixture.mjs` with tests `tests/braid-ideal-fold-crossing-chart-fixture.test.js` (8 tests, all passing). No native central-solver production code, ABI, held-release toy default, corpus chapter, or the cited escape-certificate packet was touched. Meta wrapper: `reference/op/codex-goal-seeking-prompt-template.md`.

Normal-form statement (derivation). On the same-source directed pair the causal residual $F_{ij}=r_{ij}-c_f(T-T_{\mathrm{em}})$ near the field-speed crossing takes the Whitney-fold ($A_2$) normal form $F_{ij}(\tau;T)=\tfrac12 a(\tau-\tau_*)^2-\mu(T)$ with fold curvature $a=\partial_\tau^2 F_{ij}|_*$ and unfolding parameter $\mu(T)=-F_{ij}(\tau_*;T)$. Chart coordinates $(\xi,\mu)$, $\xi=\tau-\tau_*$, with $\Sigma_{ij}=\{\mu=0\}$. The same-source root pair is $\xi_\pm=\pm\sqrt{2\mu/a}$ (born at $\mu=0$, absent for $\mu<0$), realizing the integer root-count change of the click. The source-normal denominator is $D_s^\pm=a\xi_\pm=\pm\sqrt{2a\mu}\to0$, so the toy branch weight $|m^\pm|=|D_T/D_s|=|D_T|/\sqrt{2a\mu}\sim\mu^{-1/2}$ diverges (recorded up to 163 at the 0.05 floor). Its replacement is the density-of-states reading of $D_s$ from `architrino.md`: $1/D_s$ is a root-map Jacobian to be integrated, not a coefficient to be evaluated.

Click-impulse bound (derivation for finiteness, hypothesis for coefficients). The control-sweep identity $\dot\mu=-\partial_T F_{ij}(\tau_*;T)=D_T$ makes the receiver-normal numerator the fold-crossing rate. With $dT=d\mu/D_T$ the per-branch reception-time impulse is $\int f^\pm\,dT=(\kappa/r_c^2)\int_0^{\mu_0}d\mu/\sqrt{2a\mu}=(\kappa/r_c^2)\sqrt{2\mu_0/a}$: $D_T$ cancels exactly and the $\mu^{-1/2}$ caustic is integrable, giving the finite chart impulse $|\Delta\mathbf p_{\mathrm{click}}|=\chi\,(\kappa/r_c^2)\sqrt{2\mu_0/a}+O(\mu_0^{3/2})$, independent of the Jacobian floor, softening, and time step (they only cap the pointwise peak, not the integral). On the rigid channel ($r_c\sim\rho$, $a\sim\beta^2 c_f^2/\rho$, $\mu_0\sim\rho$) this scales as $|\Delta\mathbf p_{\mathrm{click}}|\sim\kappa/(\beta c_f\rho)$. Absorber comparison against the certified pump ($\Phi_{\mathrm{tan}}\in[2.881,2.925]\beta$, clock $\Delta t\le1.353\,c_f\rho^2/\kappa$): one click per rotation absorbs the pump iff $\beta\le1/(2\pi c_1)\approx0.055$; near the field-speed ceiling it falls short by $\approx18\beta$, so sufficiency needs $\approx18$ clicks per rotation or a larger per-click $\chi\sqrt{\mu_0/a}$. Both $\kappa$ and $\rho$ cancel in the ratio — sufficiency is a pure rim-fraction threshold.

This is the success branch, not the failure branch: a finite regularization-independent impulse exists, so the hinge-click absorber route is not closed at the kernel level; the click-absorber question becomes a computable central-solver target with the declared same-record ledger contract (action-ledger $h_{\mathrm{act}}$ rows, root-ledger fold-transition rows via the existing retained/appeared/disappeared/folded classes, and receiver-normal fields carrying the density-of-states flag and the chart-integrated weight that replaces the pointwise $|m|$ on fold-flagged rows).

Fixture / tests run. `node --test tests/braid-ideal-fold-crossing-chart-fixture.test.js` — 8 pass / 0 fail. Asserts: finite chart impulse; cusp ($a\le0$) falls out of the chart; $D_T$-independence (spread 0); regularization-independence (spread below $10^{-12}$); the naive-ejection witness IS regularization-dependent ($v_{\max}$ spread $2.6\,c_f$); the executable rejection predicate returns false on the chart impulse and true on the witness; the $\beta\le0.055$ absorber threshold with $\approx18\times$ shortfall at the ceiling; fail-closed report fields.

Queue / packet disposition. `self_hit_held_release_solver_row` note updated with the spec/fixture and disposition `fold_crossing_chart_spec_finite_impulse_absorber_question_computable`; status stays `next` (spec is not implementation, fail-closed at `central_solver_retained_history_row`). Standing constraints honored: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema, signed polarity-unit notation, potential-superposition and causal-delay terminology throughout.

Blockers. None for the spec itself; the click-absorber decision is blocked on the central-solver retained-history implementation of the Section 3 contract (the geometric coefficients $\chi,\mu_0,a$ and the recurrence $N_{\mathrm{click}}$ can only be measured on the retained-history row).

Next closure goal: implement the fold-crossing chart contract on the central-solver retained-history path for `self_hit_held_release_solver_row` and measure $(\chi,\mu_0,a,N_{\mathrm{click}})$ on the `vt095` sea+self-hit row, testing $N_{\mathrm{click}}\Delta\beta_{\mathrm{click}}\ge\int_{\mathrm{rot}}d\beta_{\mathrm{pump}}$ against the certified pump.

### 2026-07-07 - Fold-Crossing Chart Measurement on vt095: Symmetric Self-Hit Is a Cusp at Coincidence

Implemented the fold-crossing chart contract as a measurement over the recorded `sh0sea-a4.25-selfhit-vt095-moving-prehistory` crossing and tested the Section 2 finite-impulse derivation of `fold-crossing-chart-spec.md`. New owner script `scripts/braid-ideal/fold-crossing-chart-measurement.mjs` with tests `tests/braid-ideal-fold-crossing-chart-measurement.test.js` (7 tests, all passing). No native central-solver production code or ABI, held-release toy default, corpus chapter, or cited packet was modified. The toy was run read-only (`--surface-speed-fraction 0.95 --prehistory-mode moving-prehistory --fcc-sea-spacing 4.25 --include-self-hits`) to extract the recorded crossing inputs.

Recorded crossing inputs: $\rho=0.816497$ (`referencePerpendicularRadius` $=\sqrt{2/3}$), $c_f=1$, $\kappa=1$, first field-speed crossing $T_c=0.43$ with `speedMax` $\beta_c=1.00196$, first self-hit root opens at $T_c$ exactly, `maxBranchWeight` $=163.5$, `smallJacobianRoots` $=24$, self-hit min delay $=dt=0.002$.

Measurement (derivation on the reconstructed rigid-rotation self-hit residual $F=2\rho\sin(\omega\Delta/2)-c_f\Delta$, $\omega=\beta c_f/\rho$; the near-constant crossing radius makes this a faithful model). Two symmetry-forced degeneracies coincide at the self-hit onset:

- Cusp, not fold. Over the offset window $\varepsilon_\beta=\beta-1$ the measurement finds $r_c^2/\varepsilon_\beta\to16.0$ and $a/\sqrt{\varepsilon_\beta}\to-1.50$ (both ratios constant to $<3\%$), so $r_c\sim\sqrt{\varepsilon_\beta}\to0$ and $a=\partial^2_\tau F\sim\sqrt{\varepsilon_\beta}\to0$: an $A_3$ cusp, not the generic $A_2$ Whitney fold Section 2 assumed. At $\beta_c=1.00196$: $\Delta_*=r_c=0.1768$, $a=-0.0666$, $D_s=D_T=0.00393$.
- Branch orientation unity. By reflection symmetry $D_T=D_s$ exactly, so $m=D_T/D_s=1$ throughout — no caustic in the orientation factor. The recorded `maxBranchWeight` $163$ is therefore a partner-pair or floor artifact, not the geometric self-root.
- Fold born at coincidence. $r_c=c_f\Delta_*\to0$ at birth, so the causal-root fold coincides with the spatial coincidence stratum $\{r_{ij}=0\}$ (the other singular locus in `architrino.md`, regulated by the spatial regulator, not by the fold chart). The self-hit branch force $f=\kappa|m|/(r_c^2+\mathrm{reg}^2)$ with $|m|=1$, $r_c^2\propto\varepsilon_\beta$ gives $\Delta\beta_{\mathrm{click}}$ that grows $0.30\to5.4$ as the spatial regulator runs $0.2\to0.001$ (log growth) — regulator-dependent, the mechanism of the toy's regularization-dependent ejection ($v_{\max}$ across $12.4/9.8/10.6\,c_f$).

Measured $(\chi,\mu_0,a,N_{\mathrm{click}})$ outcome: $a=-0.0666$ (and $\to0$ at the crossing), $r_c=0.1768$ (and $\to0$), $\chi$ and $\mu_0$ undefined because the $A_2$ chart is degenerate (cusp at coincidence), $N_{\mathrm{click}}=1$ per crossing (the opened self-hit branch is continuous, not a recurring discrete click). Absorber test $N_{\mathrm{click}}\Delta\beta_{\mathrm{click}}\ge\int_{\mathrm{rot}}d\beta_{\mathrm{pump}}$: even at the regulator-inflated value $4.31$ against pump-per-rotation $2\pi c_1\kappa/(c_f^2\rho)=22.17$, absorbed fraction $\approx0.19$, and not a legitimate chart impulse.

Disposition `symmetric_self_hit_fold_is_cusp_at_coincidence_no_regulator_independent_click_impulse` — the failure branch for the symmetric circular self-hit channel (the channel the held-release toy and `vt095` actually realize). No finite chart-defined click impulse exists there because the fold births at coincidence, so the naive hinge-click absorber route is closed at the kernel level for this channel. This does not close the general click hypothesis: Section 2's finite impulse survives for any hinge geometry realizing a non-coincident generic fold ($r_c=O(\rho)$ at birth), which the symmetric single-site self-hit cannot. The spec was corrected accordingly (new Section 6; Section 5 disposition marked superseded for this channel). Combined with the same-day Corollary S (aligned FCC sea a certified non-absorber, item 1) and the induced-polarization negative, both named kernel-level absorbers are now closed on the symmetric channel.

Fixture/tests run. `node --test tests/braid-ideal-fold-crossing-chart-measurement.test.js` — 7 pass / 0 fail (self-hit root exists only above $c_f$; $m=1$ reflection symmetry; cusp-at-coincidence confirmed; click impulse regulator-dependent; absorber verdict one click $\approx0.19$; contract rows carry the degeneracy flags; fail-closed report). The prior `tests/braid-ideal-fold-crossing-chart-fixture.test.js` (8 tests) still passes.

Queue / packet disposition. `self_hit_held_release_solver_row` note extended with the measurement and verdict; status stays `next` (measurement is not implementation, fail-closed at `central_solver_retained_history_row`). Standing constraints honored: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema, signed polarity-unit notation, potential-superposition and causal-delay terminology.

Blockers. The symmetric-channel hinge-click route is closed; the surviving possibility requires exhibiting a non-coincident hinge geometry, which needs an asymmetric or nested-shell self-hit/cross-hit configuration not yet available in the toy.

Next closure goal: test whether a non-coincident hinge geometry exists — an asymmetric or nested-shell configuration whose hinge fold is born at finite chord $r_c=O(\rho)$, restoring the Section 2 $A_2$ finite impulse — or else accept the symmetric-channel closure and route the retention program to induced sea orientational polarization (dynamic-alignment / formation-history burden).

### 2026-07-07 - Non-Coincident Hinge Geometry: Self-Hit Coincidence Theorem, Cross-Hit Finite-Chord Rescue

Resolved the geometry fork left by the vt095 measurement. New owner script `scripts/braid-ideal/fold-crossing-hinge-geometry-diagnostic.mjs` with tests `tests/braid-ideal-fold-crossing-hinge-geometry-diagnostic.test.js` (5 tests, all passing). No native central-solver code/ABI, toy default, corpus chapter, or cited packet modified. Spec `fold-crossing-chart-spec.md` extended (new Section 7; Section 6 branch-orientation bullet reconciled with the parallel signed-orientation packet).

Two results:

- Self-hit coincidence theorem (derivation). For any smooth worldline a same-source root nucleates from $\Delta\to0$ exactly at the speed crossing, because $\lvert\mathbf X(T)-\mathbf X(T-\Delta)\rvert=\lvert\mathbf v\rvert\Delta+O(\Delta^2)$ forces the root condition $\lvert\mathbf v\rvert\Delta=c_f\Delta$ to birth at $\Delta=0$. Confirmed on an explicitly asymmetric path (radial drift $0.15$ + tangential pump $0.4$): fold chord $r_c=0.073/0.178/0.345/0.647$ at $\beta=1.02/1.05/1.1/1.2$, $\to0$ as $\beta\to1$. This upgrades the vt095 circular measurement to a general statement — no single-site self-hit path escapes coincidence birth, so the self-hit magnitude is never chart-clean.
- Cross-hit finite-chord fold (derivation + measurement). A fold between distinct sites $i\neq j$ sits where $D_{s,ij}=c_f-\mathbf v_j\cdot\hat{\mathbf r}_{ij}=0$ (the alignment condition: source moving along the sightline to the receiver's causal position at $c_f$). Head-on realization: $r_c=L=1.0$ (finite), $a=\partial^2_\tau F=-g=-0.5\neq0$ (generic $A_2$), Section-2 impulse $=1.41$ finite, and softening-independent (impulse flat to $3\times10^{-4}$ below softening $0.02$) because the finite chord bounds the kernel $1/(r_c^2+\varepsilon^2)$ away from the coincidence stratum. The residual $|m|$ caustic is the ordinary $A_2$ density-of-states factor (spec Section 1.3), regulated by the causal-root Jacobian, not by softening; decoupling holds while the click window stays non-coincident ($L$ larger than the window reach — a real constraint, test-pinned).

Reconciliation with the parallel Fold-Crossing Click-Impulse Packet (`fold-crossing-click-impulse-diagnostic.mjs`, another thread): it corroborates the coincidence birth on the recorded worldline and adds the signed-orientation result — on the accelerating crossing the receiver outruns its own past, so $D_T<0<D_s$ and $m<0$ (absorptive), and the naive toy ejection was the absolute-value weight $|m|$ discarding that sign. My Section 6 $m=1$ was the fixed-$\beta$ reconstruction artifact; corrected in the spec with a forward pointer. Combined picture: the symmetric self-hit transfer is directionally absorptive with a coincidence-sensitive (spatially-self-regularized) magnitude; a non-coincident cross-hit hinge additionally gives a chart-clean (softening-independent) magnitude, with sign set per geometry by $\operatorname{sign}(D_T)$.

Disposition `non_coincident_cross_hit_hinge_restores_a2_finite_impulse_contingent_on_sustained_velocity_alignment`: the click-absorber route is not closed in general — only the single-site self-hit magnitude is coincidence-bound. The surviving requirement is a hinge geometry that sustains $\mathbf v_j\cdot\hat{\mathbf r}_{ij}=c_f$ at finite separation — the field-speed middle-binary hinge with the coplanar/co-linear alignment of `nested-shell-braid.md` at the braid symmetry-breaking point — which is the same dynamic-alignment / formation-history burden already gating induced sea orientational polarization. The two remaining open burdens are one problem.

Tests run. `node --test` on the three fold-crossing suites: 20 pass / 0 fail (8 fixture + 7 measurement + 5 hinge-geometry). Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema, signed polarity-unit / potential-superposition / causal-delay terminology.

Blockers. Exhibiting the sustained cross-hit alignment needs the actual differential (nested-shell / inner-middle-binary) dynamics, not available in the current single-shell held-release toy.

Next closure goal: exhibit the sustained cross-hit alignment in a differential nested-shell or inner/middle-binary configuration — show the middle-binary field-speed hinge holds $\mathbf v_j\cdot\hat{\mathbf r}_{ij}=c_f$ at finite separation over a click window, booking a real $h_{\mathrm{act}}$ against the pump — or resolve the shared dynamic-alignment / formation-history burden that also gates induced sea orientational polarization.

### 2026-07-07 - Self-Hit Brake Measured on the Production Central-Solver Path: Sign Absorptive, Magnitude Reduces to a Declared Coincidence Stratum

Executed the central-solver branch of the self-hit hinge-absorber operator decision: rather than leave the signed-orientation contract as a named follow-up, drove it directly against the production causal-root runtime. New owner script `scripts/braid-ideal/self-hit-brake-central-measurement.mjs` with tests `tests/braid-ideal-self-hit-brake-central-measurement.test.js` (6 passing). The runtime is consumed read-only via `import { solveMovingCircularSameSourceCausalRoots } from src/solver/app/AbsoluteHistoryRootRuntime.mjs`; no native runtime, ABI, bridge schema, contract fixture, toy default, corpus chapter, or cited packet was modified. The click-impulse packet was extended (new "Central-Solver Path Measurement" section; classification row updated) and the owner row `self-hit-held-release-solver-row.mjs` gained an additive `central_solver_self_hit_brake_finding` field. Meta wrapper: `reference/op/codex-goal-seeking-prompt-template.md`.

Production reproduction (exact). On the recorded `vt095` crossing ($\rho=\sqrt{2/3}$, $\beta_c=1.00196$) the production same-source root returns chord radius $r_c=0.17662$, $D_s=D_T=0.0039177$, and emitted signed branch orientation `receiverNormalFactor` $=+1.0000$ — reproducing the rigid-rotation reconstruction to the digit and confirming the runtime already emits the signed $m=D_T/D_s$.

Three producer gaps localized on the production path (each a concrete, named line of the runtime, not a vague obligation): (1) `buildMovingCircularSameSourceRoot` exposes `branchWeight = unsignedReceiverNormalFactor` $=\lvert m\rvert$, discarding the emitted sign — exactly the naive-kernel error; (2) the same-source history is a fixed-$\omega$ rigid circle, so $D_T=D_s$, $m=+1$ identically, and it cannot carry the pump-driven tangential acceleration — reconstructing an accelerating crossing with the production receiver-normal definition returns $D_T<0<D_s$, $m<0$ at every reception time past the hinge; (3) coincidence handling is a numerical floor `safeDistance = max(EPSILON, distance)`, `EPSILON=1e-9`, so the signed click impulse is log-divergent as the declared stratum shrinks. Absorbed fraction of the certified per-rotation pump ($22.17$) runs $0.031/0.115/0.371/2.69/9.67$ as $\rho_c$ runs $0.2/0.1/0.05/0.01/0.001$, so the beat-the-clock verdict flips with $\rho_c$. The T3 engine already models this input as `jacobianFloorOrDeclaredStratum` (`missing_same_record_jacobian_floor_or_declared_stratum`).

Reconciliation with the same-day "Non-Coincident Hinge Geometry" entry: that thread's self-hit coincidence theorem (same-source roots birth at $\Delta\to0$ for any smooth worldline) is confirmed independently on the production runtime here — the rigid-circle same-source model reflection-locks the sign and floors the coincidence distance, so the production path also yields no chart-clean single-site magnitude. Combined disposition `central_solver_self_hit_brake_sign_decided_absorptive_magnitude_reduces_to_declared_coincidence_stratum`: the production path decides the symmetric self-hit sign (absorptive) and reduces its magnitude to one declared physical input (the coincidence-stratum length scale) plus two mechanical runtime extensions; a chart-clean magnitude still requires the non-coincident cross-hit hinge, whose sustained-alignment burden is the shared dynamic-alignment / formation-history problem. All outputs fail-closed: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no central retained-history acceptance, no new validator or schema; signed polarity-unit / potential-superposition / causal-delay terminology throughout.

Tests run. `node --test` on the new suite (6 pass) and the owner-row suite (6 pass, additive field non-breaking); the three prior fold-crossing suites (20 pass) unaffected.

Blockers. The symmetric-channel magnitude is blocked on an operator ontology input — the declared coincidence-stratum length scale — which is not the agent's to invent; the chart-clean route stays blocked on the sustained cross-hit alignment (differential nested-shell dynamics).

Next closure goal: operator decision on the coincidence-stratum length scale (which would make the symmetric self-hit brake a decidable finite magnitude on the production path, beating the clock for $\rho_c\lesssim0.02$), or continue to the cross-hit dynamic-alignment route as the chart-clean alternative — the two are the branches of the one hinge-absorber decision.

### 2026-07-07 - Central-Solver Runtime Extended: Signed Same-Source Branch Orientation and Accelerating Same-Source History Landed

Landed the two mechanical producer-gap fixes named by the central-solver path measurement directly in the production causal-root runtime `src/solver/app/AbsoluteHistoryRootRuntime.mjs` (operator-authorized; other lanes idle during the change). Both are additive and backward-compatible; no toy default, corpus chapter, or cited packet interface was broken.

Change 1 — signed branch orientation. `receiverNormalFields` now emits an explicit `signedBranchOrientation` $=D_T/D_s$ on every root (partner and same-source), propagated through the `...receiverNormal` spread. The contractual unsigned `branchWeight` is unchanged (the force strength stays $\lvert m\rvert$ by contract), so the same-source hinge force law can consume the sign without disturbing existing consumers or the F64 parity contract.

Change 2 — accelerating same-source history. `evaluateMovingCircularSourcePhase` and `evaluateMovingCircularSourceHistory` accept an optional `angularAcceleration` (default $0$ reproduces the fixed-$\omega$ rigid circle exactly, bit-for-bit), and both moving-circular request builders carry it through source normalization. With $\alpha\neq0$ the tangential speed sweeps through the field-speed edge, so `solveMovingCircularSameSourceCausalRoots` returns the pump-driven crossing with $m=D_T/D_s<0$ directly — no local reconstruction. The `...F64` twin delegates to the runtime, so it inherits both changes.

Consequence: the self-hit brake central measurement (`self-hit-brake-central-measurement.mjs`) now reads the absorptive sign and the geometry off the production runtime rather than reconstructing them; the accelerating rows return $m=-0.997/-0.992/-0.987$, and the absorbed-fraction-vs-stratum sweep is unchanged ($0.031/0.115/0.371/2.69/9.67$ across $\rho_c=0.2\ldots0.001$). Two of the three producer gaps are now `resolved`; the remaining gap is the operator ontology decision (declared coincidence-stratum length scale), which the runtime already models as `jacobianFloorOrDeclaredStratum`.

Tests / validation. `tests/absolute-history-root-runtime.test.js` extended (signed field emitted equals $1.5$ on the partner fixture; rigid same-source $m=+1$ with $r_c=0.17662$; accelerating same-source $m<0$ with $D_T<0<D_s$; zero-acceleration exact backward-compatibility) — 4 pass. `check-solver-contract-fixtures` passes (same-source F64 invariants intact); `check-receiver-normal-clean-slate` and `check-animator-runtime-wiring` pass; the full braid-ideal self-hit / fold-crossing suite plus the runtime suite run 45/0. The WASM-bridge and precision checks fail only on `emscripten: missing` (no WASM toolchain in this environment), and migration-parity fails on a pre-existing manifest path assertion (`animator-causal-root-smoke.path`) confirmed by stashing the runtime change — both environmental, not from this change. Standing constraints honored: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema, signed polarity-unit / potential-superposition / causal-delay terminology.

Blockers. The symmetric self-hit brake magnitude remains blocked on the declared coincidence-stratum length scale (operator ontology decision); the chart-clean alternative remains the sustained cross-hit alignment burden.

Next closure goal: unchanged — declare the coincidence-stratum length scale (now sufficient on its own to make the production self-hit brake a decidable finite magnitude, since the runtime carries the signed orientation and accelerating history), or route to the cross-hit dynamic-alignment configuration.

### 2026-07-07 - Coincidence-Stratum Scale Audit (Read-Only); Finding Captured in brainstorming.md

Ran the operator's closure goal as a read-only corpus audit: whether the theory supplies an independent short-distance length that fixes the self-hit coincidence stratum $\rho_c$ by derivation rather than by tuning. No code, schema, validator, or acceptance state changed; whole-lane fail-closed status unchanged; `validate-content --check --strict` clean.

Outcome (idea-level; derivation and links captured in [brainstorming.md](brainstorming.md) "The Coincidence-Stratum Regulator Is the $d_0$ Scale"): the only substrate length is $R_*=\kappa\epsilon^2/c_f^2$, and the candidate short-distance length $d_0=R_{\mathrm{MCB}}$ is $O(1)\,R_*$ — too large by roughly $50\times$ for the coincidence-born self-hit to beat the certified pump clock (which needs $\rho_c\lesssim0.02\,\rho$). Routes the program to the non-coincident cross-hit hinge as the load-bearing absorber; self-hits stay in the ontology (they set $d_0$).

Next closure goal: pursue the sustained cross-hit velocity-alignment ($\mathbf v_j\cdot\hat{\mathbf r}_{ij}=c_f$ at finite separation) in a differential nested-shell / inner-middle-binary configuration, the one dynamic-alignment / formation-history burden shared with induced sea orientational polarization.

### 2026-07-07 - Differential Cross-Hit Absorber Probe (Diagnostic); Finding Captured in brainstorming.md

Executed the cross-hit closure goal as a new fail-closed toy probe. New owner script `scripts/braid-ideal/differential-cross-hit-alignment-diagnostic.mjs` with tests `tests/braid-ideal-differential-cross-hit-alignment-diagnostic.test.js` (5 passing). Two coplanar circular bands (fast inner source, slower pumped receiver); integrated signed tangential drain measured against the certified pump ($22.17$) with a softening-independence guard separating clean finite-chord contributions from near-coincidence contamination. No native/central-solver code, ABI, schema, corpus chapter, or other lane's script touched; whole-lane fail-closed status unchanged.

Outcome (idea-level; derivation and sweep captured in [brainstorming.md](brainstorming.md) "Differential Cross-Hit Is the Strongest Clean Absorber Yet"): the differential cross-hit is absorptive for like polarity and the strongest clean channel found — a single source reaches a chart-clean, softening-independent $0.742$ of the pump, set primarily by radial band proximity (frequency ratio saturates near $0.15$; alignment is many narrow clicks, not a held window). Crossing the pump in one source needs the fold to approach coincidence (not clean). Disposition `differential_cross_hit_beats_pump_only_by_approaching_coincidence_clean_ceiling_below_pump`.

Next closure goal: sum the clean finite-chord cross-hit tangential drain over the actual multi-charge inter-band source set of a nested shell braid (or an inner/middle binary pair) and test whether the clean total clears the certified pump with all chords finite (no coincidence approach); the surviving alternative lever is a genuine locked sustained alignment, the shared dynamic-alignment / formation-history burden.

## 2026-07-08 — Braid priority sort: Phase 0 pre-sort baseline snapshot

Pre-sort baseline for the braid priority cleanup (`braid-prio-sort.md`), captured on branch `calcite`. Records the size and last-modified time (America/New_York) of every braid lane's three role files so the operator can diff lane state from codex after the sort. No files were moved or edited to produce this entry; append-only. Several files show a 2026-07-07 21:49 mtime from the `calcite` checkout rewriting working-tree files, not from content edits.

| Lane | priorities.md | brainstorming.md | work-log.md |
| --- | --- | --- | --- |
| `braid-ideal (hub)` | 49.7 KB · 2026-07-07 21:49 | 209.2 KB · 2026-07-07 21:49 | 247.7 KB · 2026-07-07 21:49 |
| `braid-angular-momentum-spin` | 121.5 KB · 2026-07-03 21:09 | 1.2 KB · 2026-07-03 14:08 | 26.2 KB · 2026-07-03 14:08 |
| `braid-doubling-frequency-lock` | 15.8 KB · 2026-07-06 12:01 | 0.6 KB · 2026-07-04 20:30 | 1.1 KB · 2026-07-06 12:01 |
| `braid-geometry-export-bridge` | 392.8 KB · 2026-07-05 15:26 | 1.5 KB · 2026-07-03 14:08 | 103.6 KB · 2026-07-03 14:08 |
| `braid-mass-response-map` | 79.1 KB · 2026-07-06 12:01 | 1.4 KB · 2026-07-04 20:30 | 3.9 KB · 2026-07-06 12:01 |
| `braid-nested-shell-causal-closure` | 33.3 KB · 2026-07-03 21:09 | 1.5 KB · 2026-07-03 14:08 | 3.1 KB · 2026-07-03 21:09 |
| `braid-retained-branch-closure` | 25.5 KB · 2026-07-07 21:49 | 1.5 KB · 2026-07-04 20:30 | 4.5 KB · 2026-07-07 14:02 |
| `  neutral-braid (subdir)` | 2.8 KB · 2026-07-04 20:30 | 0.7 KB · 2026-07-04 20:30 | 0.6 KB · 2026-07-03 14:08 |
| `  shell-braid (subdir)` | 1.7 KB · 2026-07-07 14:28 | 0.8 KB · 2026-07-07 14:28 | 0.6 KB · 2026-07-07 14:28 |
| `  nested-shell-braid (subdir)` | 1.5 KB · 2026-07-04 20:30 | 0.7 KB · 2026-07-04 20:30 | 0.6 KB · 2026-07-03 14:08 |
| `braid-taxonomy` | 2.9 KB · 2026-07-07 21:49 | 0.7 KB · 2026-07-07 21:49 | 22.0 KB · 2026-07-07 21:49 |

Adjacent locations (not braid lanes; listed for diff context):

| Location | priorities.md | brainstorming.md | work-log.md |
| --- | --- | --- | --- |
| `aaa-work-threads` | 157.0 KB · 2026-07-05 15:26 | 7.6 KB · 2026-07-06 12:01 | 2.6 KB · 2026-07-03 14:08 |
| `proof-programs` | 21.7 KB · 2026-07-03 21:09 | 1.5 KB · 2026-07-03 14:08 | 234.1 KB · 2026-07-03 14:08 |

Total-file counts per lane at baseline (context for the sort): `braid-ideal` 20 (incl. `braid-prio-sort.md`); `braid-angular-momentum-spin` 33; `braid-doubling-frequency-lock` 6; `braid-geometry-export-bridge` ~139 (≈115 `octahedral-fold-aware-*`, incl. the `h39-shared-domain-coefficient-series-engine` at 349.4 KB and the `root-tangent-cauchy-majorant-tail-lemma`); `braid-mass-response-map` 30; `braid-nested-shell-causal-closure` 7; `braid-retained-branch-closure` 8 files + 3 subdirs (`neutral-braid` 19, `shell-braid` 141, `nested-shell-braid` 7); `braid-taxonomy` 6.

Note the two file-role hygiene outliers to fix in Phase 2/3: `braid-ideal/brainstorming.md` (209.2 KB) and `braid-ideal/work-log.md` (247.7 KB) are far larger than any other lane's, and `braid-geometry-export-bridge/priorities.md` (392.8 KB) carries the octahedral certificate frontier (OP-2 re-index only; archiving deferred).

### 2026-07-08 - Braid Priority Sort: Phase 2 Lane-Hygiene Verification

Phase 2 of the braid priority sort ([braid-prio-sort.md](braid-prio-sort.md)), branch `codex/calcite`. Verified file-role separation across all braid lanes. No claim changes; no certificate/packet files touched; no content moved.

Findings:

- Every immediate lane already carries a `work-log.md` (eleven checked: the eight braid lanes plus the `neutral-braid`, `shell-braid`, and `nested-shell-braid` subdirs of `braid-retained-branch-closure`; `aaa-work-threads` and `proof-programs` also have one). The "ensure a work-log.md exists" sub-goal is already satisfied; no stubs were created.
- The work-log → brainstorming migration (Phase 2's main action) has effectively no targets. All work-logs are disciplined dated-status logs (`### YYYY-MM-DD - result + status`), not misfiled brainstorm essays. Spot-verified the two largest (`braid-ideal` 978 lines; `braid-angular-momentum-spin` 448 lines) and the header structure of the other nine. Entries are proof-attempt status, promotions performed, captures made, and dated decisions with claim levels; `braid-ideal` already routes ideas to `brainstorming.md` by pointer (e.g., the Axis-Neutral Transport Channel Discussion Log). The earlier concern that ideas were misfiled into work-logs is not borne out by the current state.
- Residual Phase-2 item folded forward: the "keep only the concise ranked queue in `priorities.md`" action is real (several `priorities.md` are large: `braid-angular-momentum-spin` 124 KB, `braid-geometry-export-bridge` 393 KB, `braid-mass-response-map` 81 KB), but trimming embedded explanation out of `priorities.md` sends it to the unified `brainstorming.md`, which is the Phase 3 (OP-3) merge target, and the `braid-geometry-export-bridge` giant is OP-2-deferred. Folding the priorities trim into Phase 3/4 avoids double-editing the same large files and respects OP-2. Proposed as a `braid-prio-sort.md` revision pending operator ratification.

Phase 2 status: file-role separation verified; work-log/brainstorming axis already clean; priorities-trim folded forward pending ratification.

### 2026-07-08 - Braid Priority Sort: Phase 3 Progress (Brainstorm Merge + Priorities Trim)

Phase 3 of the braid priority sort ([braid-prio-sort.md](braid-prio-sort.md)), branch `codex/calcite`. Content relocation only; no claim changes; certificate/packet files untouched.

- Brainstorm merge (OP-3) complete: all ten lanes' `brainstorming.md` files merged into this unified `braid-ideal/brainstorming.md` (added a dated Contents TOC and a "Merged Lane Brainstorms (2026-07-08)" section with `(from: lane)` tags for the five non-empty lanes). Every source brainstorm is now a resolving stub pointing here.
- Priorities trim routed by content type (operator decision 2026-07-08): doctrine → this brainstorming file; reference surfaces + proof-spec → focused sibling files; per-item dated status → `work-log.md`. `braid-geometry-export-bridge` excluded (OP-2 deferred).
- `braid-mass-response-map/priorities.md` de-bloated 81 KB → 35 KB: extracted "Mass Translation Doctrine" → brainstorming; "PDG 2025 Mass Benchmark Surface" → `pdg-2025-mass-benchmark-surface.md`; "First Quantitative Deliverable A_0" → `a0-reference-attractor-family-deliverable-spec.md`; "Assembly-Level Reduction Status" → `assembly-level-reduction-status.md`. Pointers left in the queue.
- `braid-angular-momentum-spin/priorities.md`: extracted "CERN Reconstruction Signals" → `cern-reconstruction-signals.md`.
- Remaining Phase 3 (delicate): per-item embedded status/blocker narrative inside queue items (mass-response Task Queue items 1 and 4; angular-momentum Task Queue giant lines, up to ~9.9 KB/line) → move to `work-log.md`, leaving concise queue items. Plus the smaller lanes (`braid-ideal` 50 KB, `braid-nested-shell-causal-closure`, `braid-doubling-frequency-lock`, `braid-retained-branch-closure`) priorities to check.

### 2026-07-08 - Braid Priority Sort: Phase 3 Priorities-Trim Results

Route-by-content-type trim (operator decision 2026-07-08). Verbatim moves; no claim-level changes; certificate/packet files untouched; all moved status preserved in work-logs / sibling files.

- `braid-mass-response-map`: 81 KB -> 30.6 KB. Doctrine -> unified brainstorming; PDG benchmark, `$A_0$` deliverable spec, and Assembly-Level Reduction Status -> sibling files; queue items 1 and 4 condensed (full status in that lane's work-log).
- `braid-angular-momentum-spin`: 124 KB -> 34.7 KB. CERN reconstruction signals -> sibling; ~30 dated dispositions / audit-continuation entries -> that lane's work-log; queue items 2 and 3 condensed (originals in work-log).
- `braid-retained-branch-closure`: 26 KB -> 13.8 KB. Queue items 3, 4, 14, 20 condensed (full status in that lane's work-log).
- `braid-ideal` (hub): left intact. Its two large Group A items (`delayed_escape_certificate_lemma`, `self_hit_held_release_solver_row`) are the live absorber-hunt working state the relaunching threads consume; a clean condense needs hand-treatment, deferred to avoid disrupting active work.
- `braid-nested-shell-causal-closure` (34 KB): not trimmed; its bulk is embedded reference/contract sections (Lorentz-Test Residual Interface, MIT 8.962 GR Recovery Interface, Shared Branch Certificate Contract) that are sibling-file candidates. Deferred pending a check that "Shared Branch Certificate Contract" is not cross-referenced by other lanes.
- `braid-doubling-frequency-lock` (16 KB), `braid-taxonomy` (2.9 KB): left as-is; no giant lines, already concise enough.

### 2026-07-08 - Braid Priority Sort: Phase 4 Lane Sort and Routing (OP-1)

Phase 4 of the braid priority sort ([braid-prio-sort.md](braid-prio-sort.md)), branch `codex/calcite`. Reconciliation outcome: the eight braid lanes are already well-separated under OP-1; the phase resolved to documentation, not merges.

- Dual-owner "six-point": not a conflict. `braid-ideal` owns the **six-point symmetry invariant lemma** (`six_point_symmetry_invariant_lemma`, two packet files); `braid-retained-branch-closure` owns the **six-site neutral braid base certificate** (`neutral_braid_base_certificate`, all-pairs root ledger). Distinct objects (symmetry reduction vs base inventory); the lemma reduces the base seed. No move or pointer needed.
- Dual-owner "nested shell braid": not a duplicate. `braid-retained-branch-closure/nested-shell-braid/` is a `support-packet-index` (model source packets, subordinate to the parent retained-branch tracker); `braid-nested-shell-causal-closure` is the scored workstream for rest-mass / proper-time / relativistic-limits / photon closure. Differentiated scopes. Added clarifying cross-pointers in both `priorities.md` files so future workers do not treat them as duplicates.
- Fold dormant/duplicate lanes: none. All eight braid lanes are active and differentiated; `braid-doubling-frequency-lock` (alternative realization) and `braid-taxonomy` (bookkeeping) are small but purposeful, not dormant or duplicate.
- Route open tasks to taxonomy-home lane: no cross-lane task moves needed. Each lane's queue is already taxonomy-appropriate; the active intake concentrates in `braid-ideal` (Group A absorber + Group D corpus survey + native promotion), as recorded in the intake ledger.
- `braid-geometry-export-bridge`: re-index verified only (OP-2 deferred). `priorities.md` is active (Rank 9) and points at the live H39 tail-bound frontier via the source-covariance pause note; no octahedral certificate files moved, no archive subfolder created.
- Renumbering: no queue items were removed in this phase, so no renumbering was required.

Guardrail: link paths for the two inserted cross-pointers verified by relative depth; no claim-level changes; certificate/packet files untouched. Operator to run the standard `--check` set from codex (validate-content, build-scene-graph) as the Phase 6 validation handoff.

### 2026-07-08 - Braid Priority Sort: Phase 5 Corpus Promotion-Readiness Audit

Phase 5 of the braid priority sort ([braid-prio-sort.md](braid-prio-sort.md)), branch `codex/calcite`. Audit against [promotion-policy-into-corpus.md](../braid-retained-branch-closure/promotion-policy-into-corpus.md) and [proof-id-crosswalk.md](../braid-taxonomy/proof-id-crosswalk.md). No corpus files were edited in this audit.

Proof-state bound (crosswalk): no braid branch is retained at any Proof ID (`NB-0`, `SH-0`, `NSH-0`, families). Every current object is a branch target, row evidence, diagnostic/rejection, fixture, chart, downstream consumer, or uninspected corpus — all `not_retained`. So no retained-branch results exist to promote; the promotable surface is limited to reader-safe theorem-target discipline, effective-summary, and scoped-negative / first-failure language.

Already-promoted surface (confirmed by corpus grep 2026-07-08): the braid corpus has 13 noether-braid chapters and already carries the safe promotable mechanism/theorem-target material — anti-damping (7 files), axis-neutral channel (5), exact speed budget (2), pump language (5), and `Corollary S` (1). The 2026-05-22 policy application already promoted the theorem-target retained-branch certificate language plus scoped fixed-speed octahedral negatives into [noether-braid.md](../../../content/markdown/aaa/noether-braid/noether-braid.md); the 2026-07-06/07 passes promoted the axis-neutral invariant channel, drum geometry, speed budget, dressing ladder, and consilience principle.

Not-yet-promoted (priority-only): the very recent 2026-07-07 absorber-cluster negatives — `certified non-absorber` phrasing, the self-hit coincidence theorem, and the cross-hit hinge results (0 corpus files each). These are legitimate reader-safe scoped-negatives / theorem-targets under the open-factory framing, but they belong to the ACTIVE Group A absorber cluster (five threads converging on one gating object; consolidation pending; about to relaunch), so the results may still shift.

Policy guidance: the promotion policy states that while retained-branch, validation, and observer-export rows remain open, the remaining promotion surface should be "a dedicated validation-method destination rather than a broad braid corpus migration." The theory-focus policy likewise prefers advancing derivations over promotion proliferation.

Decision (pending operator ratification): no new unilateral corpus promotions in this cleanup sort. The safe surface is already promoted; the only not-yet-promoted candidates are in-flight absorber-cluster negatives whose systematic, reviewed promotion is the job of the pending Group D corpus-insight survey (P11-P16 in [braid-intake-prompts.md](braid-intake-prompts.md)), under operator review. Option retained for the operator: draft the absorber-cluster scoped-negatives (Corollary S completion, self-hit coincidence theorem) as reader-safe theorem-target / scoped-negative language once the cluster stabilizes.

Operator ratification 2026-07-08: accepted the audit — promote nothing in this sort; route systematic new-candidate promotion to the pending Group D survey (P11-P16) under operator review. Phase 5 complete with zero corpus edits.


## 2026-07-08 - Operator d0 decision applied to the symmetric self-hit brake (item 3)

Highest-priority Group A item `self_hit_held_release_solver_row` (item 3): the 2026-07-07 central-solver measurement had localized the symmetric self-hit brake to a single open producer gap - the declared coincidence-stratum length scale (`jacobianFloorOrDeclaredStratum`), an operator ontology decision the agent does not invent. The operator supplied it: the stratum is `d0`, of order the near-field two-body scale $\kappa\epsilon^2/c_f^2$ ($R_*$; in the minimum-circular-binary reading $d_0=R_{\mathrm{MCB}}$, the collapse-arresting radius; exact derivation from $(\kappa,\epsilon,c_f)$ open, [particle-masses](../../../content/markdown/aaa/assemblies/particle-masses.md)).

Evaluated at $d_0$ the symmetric single-site self-hit magnitude is $\approx50\times$ the certified per-rotation tangential pump, so the symmetric self-hit is dispositioned NOT load-bearing as a retention absorber. The decision was landed in both scripts so they tell one story:

- Owner row [self-hit-held-release-solver-row.mjs](../../../scripts/braid-ideal/self-hit-held-release-solver-row.mjs): `central_solver_self_hit_brake_finding` now carries `declared_coincidence_stratum` (symbol `d0`, operator_decision_date 2026-07-08, `symmetric_self_hit_load_bearing:false`, `symmetric_self_hit_magnitude_vs_pump:"about_50x_too_large"`); the coincidence gap moved to `resolved_producer_gaps`; `open_producer_gaps`/`first_missing_object` re-point to the non-coincident cross-hit hinge.
- Source measurement [self-hit-brake-central-measurement.mjs](../../../scripts/braid-ideal/self-hit-brake-central-measurement.mjs): the `coincidence_stratum_is_a_numerical_floor` gap status flips to `resolved_by_operator_declared_d0_2026_07_08`; a new `operator_ontology_input` block records $d_0$; `declared_stratum_supplied_by_operator:true`.

Both scripts now emit the single disposition `central_solver_self_hit_brake_sign_absorptive_magnitude_set_by_operator_declared_d0_not_load_bearing`, and both `first_missing_object`s are `non_coincident_cross_hit_hinge_sustained_alignment_over_a_click_window`. Tests updated to match: `node --test tests/braid-ideal-self-hit-brake-central-measurement.test.js tests/braid-ideal-self-hit-held-release-solver-row.test.js` -> 12 pass, 0 fail.

Surviving retention-mechanism frontier: the non-coincident cross-hit hinge that sustains $\mathbf v_j\cdot\hat{\mathbf r}_{ij}=c_f$ at finite separation - a formation-history / dynamic-alignment burden (the same burden gating induced sea orientational polarization), not a self-hit magnitude question. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no central retained-history acceptance; the promotion half stays at `central_solver_retained_history_row`. The dated 2026-07-07 disposition strings in the historical packet/run-matrix/work-log narrative are left as-recorded; a forward-pointer note to this decision was added to [fold-crossing-click-impulse-packet.md](fold-crossing-click-impulse-packet.md) and [sh-run-matrix.md](sh-run-matrix.md).


## 2026-07-08 - Cross-hit hinge click-rate: the internal-frequency requirement (item 11)

Operator selected the non-coincident cross-hit hinge (over the soft neighbor-orientation mode) as the surviving tangential-pump absorber channel to derive-or-falsify. The spec Section 2.5 had reduced sufficiency to the per-rotation click count $N_{\mathrm{click}}$ and left it as "a hypothesis only the retained-history row can supply." That was too pessimistic: the click rate is a property of the frequency content of the configuration, not of the force ledger, so it admits a kinematic bound with no retained-history dynamics.

New owner script [cross-hit-hinge-click-rate-diagnostic.mjs](../../../scripts/braid-ideal/cross-hit-hinge-click-rate-diagnostic.mjs) (tests [braid-ideal-cross-hit-hinge-click-rate-diagnostic.test.js](../../../tests/braid-ideal-cross-hit-hinge-click-rate-diagnostic.test.js), 5 passing) and [spec Section 8](fold-crossing-chart-spec.md#8-cross-hit-hinge-click-rate-the-internal-frequency-requirement-2026-07-08):

- Result 1 (single-frequency braid -> $N_{\mathrm{click}}=0$): a cross-hit click is born where $A_{ij}=\mathbf v_j\cdot\hat{\mathbf r}_{ij}$ crosses $c_f$. If every site shares one $\omega$ the configuration is a global rigid rotation $\mathrm{Rot}(\omega t)$, which preserves inner products, so $A_{ij}(t)=\mathbf v_j(0)\cdot\hat{\mathbf r}_{ij}(0)=$ const. No sign change, no clicks; the exception $A_{ij}\equiv c_f$ is a static sustained hinge, not a recurring click. Delay-independent (rigid rotation carries causal roots to causal roots). Confirmed numerically: $A_{ij}$ constant to $1.6\times10^{-15}$ (single shell) / $2.7\times10^{-15}$ (multi-shell one frequency), zero crossings.
- Result 2 (needs $\ge2$ frequencies): a cross pair with frequencies $\omega_{\mathrm{out}},\omega_{\mathrm{in}}$ has $A_{ij}$ periodic in the beat $(\omega_{\mathrm{out}}-\omega_{\mathrm{in}})t$; over one outer rotation $N_{\mathrm{click}}\lesssim2P\lvert1-\omega_{\mathrm{in}}/\omega_{\mathrm{out}}\rvert$ across $P$ straddling directed pairs. Verified: ratio 1.5 -> 0 clicks (amplitude did not straddle $c_f$), ratio 6 -> 90 clicks tracking 5 beats/rotation.
- Verdict vs certified pump ($N_{\mathrm{click}}^{\mathrm{req}}=2\pi c_1\beta\approx18\beta$; pump-per-rotation reproduced at 22.17, per-click 1.25): single-frequency `closed_negative`; multi-frequency open only with a fast inner shell $\lvert1-\omega_{\mathrm{in}}/\omega_{\mathrm{out}}\rvert\gtrsim\pi c_1\beta/P$ (ratio $\gtrsim2.5$ for $P=6$, $\beta\to1$) AND amplitude straddling. Disposition `cross_hit_click_train_requires_fast_inner_shell_single_frequency_braid_gives_zero`.

This sharpens the Section 7 sustained-alignment/formation-history burden into two separable checkable conditions on the nested-shell architecture (fast inner frequency + amplitude straddling) and closes the single-shell reading of the cross-hit route negatively without the gated retained-history certificate chain. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema. Next closure goal: test whether the fast-inner-shell ratio clearing $N_{\mathrm{click}}\gtrsim18\beta$ is simultaneously compatible with amplitude straddling at an admissible inner rim fraction, or whether the two necessary conditions are mutually exclusive and close the cross-hit route as well.

Verification: `node --test tests/braid-ideal-cross-hit-hinge-click-rate-diagnostic.test.js` -> 5 pass, 0 fail.


## 2026-07-08 - Two-frequency coexistence test: the cross-hit conditions coexist (item 11)

Executing the prior closure goal. Section 8 left the cross-hit route open under two separable necessary conditions: (A) $N_{\mathrm{click}}\ge2\pi c_1\beta_{\mathrm{out}}$ (fast inner shell) and (B) amplitude straddling ($A_{ij}$ reaching $c_f$). Question: coexist at admissible $\beta_{\mathrm{in}}$, or mutually exclusive?

New owner script [cross-hit-hinge-coexistence-diagnostic.mjs](../../../scripts/braid-ideal/cross-hit-hinge-coexistence-diagnostic.mjs) (tests [braid-ideal-cross-hit-hinge-coexistence-diagnostic.test.js](../../../tests/braid-ideal-cross-hit-hinge-coexistence-diagnostic.test.js), 5 passing) and [spec Section 9](fold-crossing-chart-spec.md#9-two-frequency-coexistence-test-the-conditions-are-not-mutually-exclusive-2026-07-08). Two-shell config: 3 outer ($R_{\mathrm{out}}$, $\omega_{\mathrm{out}}$) + 3 inner ($R_{\mathrm{in}}=qR_{\mathrm{out}}$, $\omega_{\mathrm{in}}=r\omega_{\mathrm{out}}$), $\beta_{\mathrm{in}}=r\beta_{\mathrm{out}}q$; clicks counted on outer-receiver/inner-source directed pairs over one outer rotation.

Verdict: NOT mutually exclusive — they coexist. Reference point $r=2.5$, $q=0.44$, $\beta_{\mathrm{out}}=0.98$: $\beta_{\mathrm{in}}=1.08$, straddles ($\max A=1.08$), $N_{\mathrm{click}}=27\ge17.7$; coexistence region broad (20/36 grid points at $\beta_{\mathrm{out}}=0.98$; all $q\in[0.2,0.8]$ clear), minimum admissible $\beta_{\mathrm{in}}\approx1.18$. Structural facts:

- $\beta_{\mathrm{in}}\ge1$ is the single binding gate: sub-field inner shell (e.g. $r=2,q=0.44\Rightarrow\beta_{\mathrm{in}}=0.86$) gives exactly zero clicks; the train switches on as $\beta_{\mathrm{in}}$ crosses 1.
- Condition (A) delivers condition (B): $\beta_{\mathrm{in}}=r\beta_{\mathrm{out}}q$, so the frequency increase raising the click count also carries the inner binary super-field. Cooperative, not antagonistic.
- Frequency-ratio saturation: once $\beta_{\mathrm{in}}\ge1$, total absorption is $r$-independent (flat within 20% over $r\in[3,8]$) because click count $\propto r$ while per-click impulse falls as source centripetal accel grows. Control variables are $\beta_{\mathrm{in}}, q$, not raw $\omega_{\mathrm{in}}$.

Coefficient caveat: the indicative impulse total ($\approx3\times$ pump at reference) uses schematic Section 2.2 $\chi,\mu_0$, so its absolute size is order-of-magnitude only; robust results are coexistence, the $\beta_{\mathrm{in}}\ge1$ gate, and saturation. Disposition `cross_hit_absorber_conditions_coexist_binding_gate_is_super_field_inner_binary`. The cross-hit click absorber survives; the retention question reduces to one sharper burden: is a super-field inner binary ($\beta_{\mathrm{in}}\gtrsim1$) with coplanar hinge alignment itself a self-consistent retained-history configuration (own root-budget, causal-margin, pump balance)? Same formation-history burden, now a concrete nested-shell target. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Verification: `node --test tests/braid-ideal-cross-hit-hinge-coexistence-diagnostic.test.js` -> 5 pass, 0 fail.


## 2026-07-08 - Super-field inner-binary self-consistency: the two routes are one crux (item 11)

Executing the prior closure goal: is the inner binary at $\beta_{\mathrm{in}}\gtrsim1$ (required by the cross-hit absorber) itself a self-consistent retained-history configuration, or unsustainable? New owner script [super-field-inner-binary-consistency-diagnostic.mjs](../../../scripts/braid-ideal/super-field-inner-binary-consistency-diagnostic.mjs) (tests [braid-ideal-super-field-inner-binary-consistency-diagnostic.test.js](../../../tests/braid-ideal-super-field-inner-binary-consistency-diagnostic.test.js), 5 passing) and [spec Section 10](fold-crossing-chart-spec.md#10-self-consistency-of-the-super-field-inner-binary-the-two-routes-are-one-crux-2026-07-08).

- Result A (no inner absorber): a click needs a super-field source ($|A_{ij}|\le|\mathbf v_j|=\beta_j c_f$). The outer shell is sub-field ($\max A_{\mathrm{out}\to\mathrm{in}}=0.43<c_f$, cannot straddle); same-shell partners share the inner frequency ($A_{ij}$ constant to $2.4\times10^{-15}$). So the inner binary's only possible cross-hit absorber is a strictly faster distinct third shell.
- Result B (regress diverges): shell $k$ needs a faster super-field shell $k{+}1$; the tower is strictly increasing and bounded below by 1, so it never terminates on a cross-hit. The click relocates the pump inward rather than dissipating it.
- Result C (terminal reduction): the innermost shell has only breathing ($\le27\%$), sea ($\le10\%$), and the self-hit; only the self-hit can reach the pump, but its magnitude is the $d_0$ coincidence-stratum question ($\approx50\times$ pump, not chart-clean), and above $\beta=1$ the partner pump is itself uncertified (the interval certificate bails for $\beta\ge1$). Terminal balance couples the uncertified super-field pump and the $d_0$-dependent self-hit.
- Result D (self-hit is super-field): $F(\Delta)=2\rho\sin(\beta\Delta/2\rho)-c_f\Delta$, $F'(0)=\beta-1$: no self-hit root below field speed, born at $\beta=1$, finite count (order 1 through $\beta\sim3$). Self-hit brake and cross-hit inner binary share the super-field regime.

Verdict `super_field_inner_binary_self_consistency_reduces_to_d0_self_hit_balance_cross_hit_and_self_hit_routes_are_one_crux`: the cross-hit route does not close negatively but unifies with the self-hit route - both terminal closures reduce to the $d_0$-set self-hit tangential balance on a super-field shell. This removes the illusion that cross-hit and self-hit were independent hedges; the retention hunt's two surviving candidates are one crux. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Next closure goal: compute the super-field tangential force balance on a single rigid shell for $\beta\gtrsim1$ - the uncertified partner-wake pump plus the $d_0$-regularized self-hit brake - and determine whether it has a zero at an admissible $\beta^*$ (self-balancing terminal shell, retention closes affirmatively) or stays net anti-damping (no terminal shell, forcing Noether-sea/field dissipation as the only remaining sink).

Verification: `node --test tests/braid-ideal-super-field-inner-binary-consistency-diagnostic.test.js` -> 5 pass, 0 fail.


## 2026-07-08 - Super-field tangential balance: the field-speed pin (item 11) + normal-convention audit

Operator asked to FIRST verify all cross-hit math handles source-normal and receiver-normal correctly, then compute the super-field balance.

Audit (against production `receiverNormalFields` in [AbsoluteHistoryRootRuntime.mjs](../../../src/solver/app/AbsoluteHistoryRootRuntime.mjs)): canonical convention is $\hat{\mathbf r}=(\mathbf x_{\mathrm{rec}}-\mathbf x_{\mathrm{src}})/|\cdots|$ (source$\to$receiver), $D_s=c_f-\mathbf v_{\mathrm{src}}\cdot\hat{\mathbf r}$ (jacobian/denominator), $D_T=c_f-\mathbf v_{\mathrm{rec}}\cdot\hat{\mathbf r}$ (numerator, same $\hat{\mathbf r}$), $m=D_T/D_s$ (signed), branchWeight $=|m|$. The click-rate/coexistence/consistency diagnostics use the source-normal correctly (their alignment scalar $=c_f-D_s$ to $2\times10^{-16}$), but they summed impulse MAGNITUDE only, so the absorptive/ejective sign $\operatorname{sign}(D_T)$ was not evaluated there - fine for counts/availability, but the balance needs signed $m$ and must not assume $D_s>0$ (super-field). Existing fold/self-hit scripts and the interval certificate use the same convention (certificate uses unsigned $|D_T|/D_s$ and requires $D_s>0$, valid only sub-field).

New owner script [super-field-tangential-balance-diagnostic.mjs](../../../scripts/braid-ideal/super-field-tangential-balance-diagnostic.mjs) (tests [braid-ideal-super-field-tangential-balance-diagnostic.test.js](../../../tests/braid-ideal-super-field-tangential-balance-diagnostic.test.js), 5 passing) and [spec Section 11](fold-crossing-chart-spec.md#11-super-field-tangential-balance-the-field-speed-pin-2026-07-08):

- Validation: the signed-normal point evaluator reproduces the certified band $2.881\beta\le\Phi_{\mathrm{tan}}\le2.925\beta$ for $\beta\in[0.1,0.985]$ (measured $\Phi/\beta\in[2.888,2.919]$, all in-band); signed=unsigned sub-field ($D_T>0$).
- Result 1: extended across $\beta=1$, the partner pump stays anti-damping and grows ($\Phi_{\mathrm{tan}}=2.87,2.92,2.98,3.08,3.24,3.58,4.28$ at $\beta=0.985..1.4$), single-root, non-caustic ($D_s>0$). The pump does NOT switch off super-field - the Section 10 "uncertified pump might vanish" escape is closed.
- Result 2 (inputs, provenance recorded): self-hit brake absorptive ($m<0$), onsets at $\beta=1$, $\sim50\times$ pump at declared $d_0$ (exact size $d_0$-gated).
- Result 3: no static super-field $\beta^*$ (pump only grows); switching structure gives a dynamic field-speed pin - above $\beta=1$ net braking pushes down, below $\beta=1$ the pump pushes up, so $\beta\approx1$ is a self-limiting attractor.

Verdict `partner_pump_persists_super_field_no_static_beta_star_switching_self_hit_yields_field_speed_pin_gated_on_d0_sea_field_dissipation_not_required`: the retention hunt closes affirmatively-conditionally - a terminal shell exists (the field-speed-pinned inner binary) and does NOT require Noether-sea/field dissipation; gated on $d_0$ for exact pin stability (declared $\sim50\times$ suffices). Claim: derivation+measurement for the validated super-field pump; hypothesis for the pin as a proven attractor (trajectory needs the retained-history solver). Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Next closure goal: confirm the field-speed pin as an actual attractor on the native retained-history solver (a two-shell row with the inner binary released near $\beta=1$), and invert the pin balance $\Phi_{\mathrm{partner}}(1)+\Phi_{\mathrm{self}}(1;d_0)=0$ to DERIVE $d_0$ as the coincidence stratum that makes the field-speed edge marginally stable - turning the open $d_0$ value (particle-masses.md) into a fixed point of the retention balance rather than a free operator input.

Verification: `node --test tests/braid-ideal-super-field-tangential-balance-diagnostic.test.js` -> 5 pass, 0 fail.


## 2026-07-08 - Field-speed pin: attractor confirmed (reduced), d0 inversion is a bound (item 11)

Executing the prior closure goal: confirm the field-speed pin as an attractor and invert the balance for d0. New owner script [field-speed-pin-attractor-diagnostic.mjs](../../../scripts/braid-ideal/field-speed-pin-attractor-diagnostic.mjs) (tests [braid-ideal-field-speed-pin-attractor-diagnostic.test.js](../../../tests/braid-ideal-field-speed-pin-attractor-diagnostic.test.js), 6 passing) and [spec Section 12](fold-crossing-chart-spec.md#12-field-speed-pin-attractor-confirmation-and-d0-inversion-2026-07-08). Reduced reference integrator (validated signed-normal partner pump + switching self-hit brake); explicitly NOT the native solver - native retained-history confirmation stays gated.

Modeling correction found during the run: the self-hit brake is a MULTIPLE of the LOCAL pump, not a fixed magnitude. So $\Phi_{\mathrm{net}}(\beta)=\Phi_{\mathrm{partner}}(\beta)[1-\varrho\,\Theta(\beta-1)]$ and, since the partner pump grows super-field, the pin threshold is the ratio $\varrho>1$ (not brake=pump at a single beta).

- Result 1 (attractor): for $\varrho>1$ the field-speed edge $\beta=1$ is a two-sided attractor - releases $\{0.9,0.95,1.05,1.1\}$ all converge to $\beta\approx1$ (to $1.00$ at $\varrho=1.5$, to $\approx0.98$ at the declared $\varrho\approx50$). $\varrho=1$ marginal (attracts from below, neutral above); $\varrho=0.5$ super-field runaway (no pin).
- Result 2 (d0 inversion): the marginal condition $\varrho=1$ against the measured self-hit-brake-vs-stratum curve (absorbed fraction $0.031/0.115/0.371/2.69/9.67$ at $\rho_c=0.2/0.1/0.05/0.01/0.001$) lands at $\rho_c^*\approx2.2\times10^{-2}$. The pin holds for $d_0\le\rho_c^*$ - an INEQUALITY, not a unique fixed point, because over-braking is also stable. The declared $d_0=R_{\mathrm{MCB}}\sim\kappa\epsilon^2/c_f^2$ ($\varrho\approx50$) sits well inside the stable range and gives a firm, over-damped pin.

Honest correction to the closure goal: inverting the balance does NOT uniquely derive $d_0$; it BOUNDS it ($d_0\le\rho_c^*\approx0.022$, equivalently the self-hit brake must exceed the partner pump above field speed). The unique $d_0$ still comes from $R_{\mathrm{MCB}}$ physics, now bounded above by the retention balance. Verdict `field_speed_edge_two_sided_attractor_for_ratio_above_one_balance_bounds_d0_by_marginal_stratum_declared_d0_over_damped_inside_range`. Claim: derivation+measurement (reference) for the attractor and marginal stratum; hypothesis for the native-solver pin (gated) and for $\varrho(\beta)$ staying $>1$ as $\beta$ grows (its $\beta$-dependence unmeasured). Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Next closure goal: (a) native retained-history confirmation of the pin (gated two-shell row released near $\beta=1$); (b) measure $\varrho(\beta)$ above field speed - if it stays $>1$ the pin is unconditional in the super-field range, else there is an upper escape; (c) reconcile $\rho_c^*\approx0.022$ against $R_{\mathrm{MCB}}\sim\kappa\epsilon^2/c_f^2$ (particle-masses.md).

Verification: `node --test tests/braid-ideal-field-speed-pin-attractor-diagnostic.test.js` -> 6 pass, 0 fail.


## 2026-07-08 - Signed re-check of the cross-hit coexistence absorption (item 11)

Executing the audit-flagged proof re-check. Section 9 summed cross-hit click MAGNITUDES ("~3x surplus" absorption) without evaluating the per-click receiver-normal sign $m=D_T/D_s$. Redone with the signed convention (validated in Section 11). New owner script [cross-hit-signed-absorption-recheck.mjs](../../../scripts/braid-ideal/cross-hit-signed-absorption-recheck.mjs) (tests [braid-ideal-cross-hit-signed-absorption-recheck.test.js](../../../tests/braid-ideal-cross-hit-signed-absorption-recheck.test.js), 3 passing) and [spec Section 13](fold-crossing-chart-spec.md#13-signed-re-check-of-the-cross-hit-coexistence-absorption-2026-07-08).

- Result 1 (sign = polarity, robust): for a fixed source->receiver polarity product the tangential transfer has a definite sign (net = +/- |sum|, no per-pair cancellation). Like-polarity source -> net absorptive; opposite-polarity -> net ejective. Regulator-robust sign.
- Result 2 (neutral binary self-cancels): the physical inner binary is neutral (one +, one -), so its members deliver opposite-sign transfers to each outer receiver that substantially cancel (cancellation grows with fold-pole resolution, ~25% coarse to >=90% fine). The net is far below the magnitude sum.
- Caveat: the instantaneous-proxy magnitudes (both net and |sum|) are not converged - they grow as the $D_s\to0$ fold pole is resolved. A converged number needs the causal root-sum / density-of-states, not the instantaneous reception-time integral.

Verdict: the Section 9 "~3x surplus" overstated the case (magnitude sum + regulator dependence). The coexistence NECESSARY conditions (Section 8/9: enough clicks + straddling) are sign-independent and stand; the SUFFICIENCY (neutral inner binary net-absorbs the pump via cross-hits) is NOT established. This does NOT affect the field-speed pin (Sections 11-12), which rests on the self-hit/partner-pump balance, and it reinforces Section 10 (the self-hit pin is the load-bearing terminal absorber; the cross-hit is not an independent clean absorber). A dated correction pointer was added to Section 9. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Verification: `node --test tests/braid-ideal-cross-hit-signed-absorption-recheck.test.js` -> 3 pass, 0 fail.


## 2026-07-08 - Cross-hit absorption on the causal root-sum (fidelity raise, item 11)

Operator chose (b): raise fidelity of the cross-hit signed transfer from the instantaneous proxy to the actual causal root-sum. Also clarified geometry: besides the nested shell there is the shell braid, whose planar equal-radius state is closest to the certified hexagon pump; being single-frequency it yields zero cross-hit clicks (Section 8), so the cross-hit absorber requires the nested two-frequency structure.

New owner script [cross-hit-causal-absorption.mjs](../../../scripts/braid-ideal/cross-hit-causal-absorption.mjs) (tests [braid-ideal-cross-hit-causal-absorption.test.js](../../../tests/braid-ideal-cross-hit-causal-absorption.test.js), 4 passing) and [spec Section 14](fold-crossing-chart-spec.md#14-cross-hit-absorption-on-the-causal-root-sum-fidelity-raise-2026-07-08). For each reception time $T$ it solves $|\mathbf X_i(T)-\mathbf x_j(T_{\mathrm{em}})|=c_f(T-T_{\mathrm{em}})$ for every emission root and sums the signed $m=D_T/D_s$ contribution (source vel at emission, receiver vel at reception), projected tangential.

- Converges: neutral-binary net $0.214\to0.220\to0.224$ across $N_T=2000/4000/8000$ (proxy diverged); caustic is measure-zero in $T$, no regulator needed.
- Neutral inner binary does NOT absorb: $\approx97\%$ polarity cancellation; net avg tangential force $\approx+0.035$ vs pump $\approx2.84$ ($\sim1\%$, slightly ejective).
- Polarity sets sign; even a fully imbalanced (charged) inner shell reaches only $\sim20\%$ of the pump (insufficient league with breathing $\le27\%$, sea $\le10\%$).

Verdict `cross_hit_absorber_closed_for_neutral_braid_causal_root_sum_net_about_one_percent_of_pump_and_ejective`: the Section 9 "~3x surplus" collapses on the causal root-sum; the cross-hit is not an independent absorber for the neutral braid. Necessary coexistence conditions (Section 8/9) still hold; the field-speed self-hit pin (Sections 11-12) is unaffected and is now the sole remaining absorber path. Section 13's proxy magnitude non-convergence is superseded by this converged causal result. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Verification: `node --test tests/braid-ideal-cross-hit-causal-absorption.test.js` -> 4 pass, 0 fail.


## 2026-07-08 - Iso-frequency planar tri-binary restart: internal torque closure on the rail (item 11)

Operator corrected the model inventory (my cross-hit toys used a non-neutral 5-site schematic) and restarted the planar case on the true geometry: six architrinos as three neutral antipodal axes (3 electrinos : 3 positrinos), radii $R_I<R_M<R_O$, one common frequency (iso-frequency planar state), middle binary riding the $c_f$ rail as the hinge/clicker. New owner script [planar-tri-binary-iso-frequency-evaluator.mjs](../../../scripts/braid-ideal/planar-tri-binary-iso-frequency-evaluator.mjs) (tests [braid-ideal-planar-tri-binary-iso-frequency-evaluator.test.js](../../../tests/braid-ideal-planar-tri-binary-iso-frequency-evaluator.test.js), 6 passing) and [spec Section 15](fold-crossing-chart-spec.md#15-iso-frequency-planar-tri-binary-restart-internal-torque-closure-on-the-rail-2026-07-08). Full causal-root solve per partner, signed $m=D_T/D_s$; anchors: rigidity to $2\times10^{-15}$; equal-radii limit reproduces the certified hexagon band (2.9028 at $\beta=0.9$, layer-symmetric).

- Regime map at the rail: common frequency orders speed with radius, so outer is the super-field self-hit holder (self roots 0/0/1 for I/M/O; middle exactly at root birth). Inverts the 4:2:1 picture (inner super-field) - flagged to operator and accepted as the working reading for the 1:1:1 state.
- Sign reversal: nesting turns the inner layer's partner-wake tangential residual NEGATIVE (braking, $\Phi_I\approx-1.1$) - the pump is not sign-definite across nested layers; cross-layer delayed-wake geometry alone can brake a sub-field layer.
- Net-torque closure: $N=2\sum_a R_a\Phi_a$ crosses zero along a curve near $q_O\approx2.2$, $q_I\approx0.25$-$0.35$ (bracketed $+0.175\to-0.104$ across $q_O=2.0\to2.2$ at $q_I=0.3$). At closure: inner braked, middle mildly pumped (the escapement/clicker throughput at the rail), outer nearly free with one open self-hit root as its $d_0$ brake. First configuration family in the hunt where the tangential ledger closes internally with no external absorber.

Caveats: prescribed rigid worldlines (necessary-condition residuals, not dynamics); partner channel only; radial balance and axis-phase scan pending; no sustained cross-pair hinge tuned. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Verification: `node --test tests/braid-ideal-planar-tri-binary-iso-frequency-evaluator.test.js` -> 6 pass, 0 fail.


## 2026-07-08 - Forward-looking lock-dynamics tasks captured (Group E)

Operator request following the Section 15 iso-frequency result (with the note that the outer-binary-super-field ordering was not the prior intuition — serendipitous if it holds). Two contingent tasks articulated and queued as Group E items 17-18: `iso_frequency_lock_perturbation_basin` (disturbance tolerance of the f:f:f lock; escapement sub-question: is recovery quantized in h_act clicks?) and `iso_frequency_lock_formation_autotune` (do three independent binaries at different frequencies entrain through delayed wakes into an f:f:f braid — the formation-history burden in its sharpest testable form; sub-question: does the middle binary lock to the c_f rail first and act as the master clock?). Both depend on the Section 15 candidate row and responsive worldlines (native retained-history solver); a staged kinematic half of item 17 (perturbed ansatz, rigidity-degradation measurement) is possible earlier.


## 2026-07-08 - Rail closure refinement: radial support rejects the circular ansatz (item 11)

Executed the Section 15 closure goal on the extended evaluator (radial projection + supportRatio + netZeroInQO + phaseScan; tests extended to 8 passing; second certified anchor: equal-radii $a_{\mathrm{rad}}=-0.7605\le-0.672$).

- Net-torque zeros precise: dip with two roots, $q_O^*=2.0916$ / $2.3724$ at $q_I=0.3$ ($2.1590$ at $q_I=0.35$); ledger at the lower zero $\Phi=(-1.142,+0.160,+0.087)$, $N=6\times10^{-9}$.
- Support ratios at the zero: $10.9/1.97/0.32$ (I/M/O) - all layers bound (net inward), but inner over-bound and outer under-bound by $\sim5$-$6\times$ vs middle; structural (needed centripetal $\propto R$, wake binding concentrates at small radius; deep nesting required by tangential closure drives ratios apart).
- Phase scan $\pm60°\times\pm60°$: cannot repair (best I/O $\approx12.5$; outer phase past $+30°$ unbinds the outer). Outer self-hit is like-polarity repulsive (outward) - worsens, not helps.

Verdict `iso_frequency_rail_circular_ansatz_fails_simultaneous_tangential_and_radial_closure_candidate_row_must_be_non_circular_or_environmentally_confined`. Surviving readings: eccentric/breathing layers (the lane mandate already says do not assume circular orbits) or sea confinement. Forward lock-dynamics tasks captured as Group E items 17-18. Fail-closed throughout.

Verification: `node --test tests/braid-ideal-planar-tri-binary-iso-frequency-evaluator.test.js` -> 8 pass, 0 fail.


## 2026-07-08 - Non-circular relaxation: m=2 shape mode rejected; unified closure metric (item 11)

Executed the Section 16 closure goal on a new owner script [planar-tri-binary-breathing-extension.mjs](../../../scripts/braid-ideal/planar-tri-binary-breathing-extension.mjs) (tests 4 passing; [spec Section 17](fold-crossing-chart-spec.md#17-non-circular-relaxation-the-m2-shape-mode-is-rejected-2026-07-08)): inner/outer layers get the m=2 antipodal-symmetric radius modulation at constant angular rate (middle circular, rail exact); cycle-sampled residuals; unified closure metric with one global least-squares $\kappa^*$ over all layers, both components, full cycle.

- Circular family bottoms at global $0.646$ at ($q_I=0.5$, $q_O=1.6$) - milder nesting than the net-torque curve ($0.888$ there); even at optimum the wake coherently supplies only ~1/3 of the required pattern.
- $\psi$ is exactly a gauge at constant angular rate (inter-axis angles never change; verified to $10^{-6}$): one knob per layer, not two.
- Circular is a shape-space local minimum: every breathing direction degrades ($0.646\to0.66$-$0.95$ across $e\in\{0.1,0.2,0.4\}$ singly and jointly). The m=2 mode is REJECTED.

Combined Sections 15-17 verdict: iso-frequency deep-nesting tangential closure and radial self-support are mutually exclusive; the lowest shape mode cannot bridge. Missing ingredient is structural: (i) speed modulation / Kepler-like rate variation - for the middle layer this is the escapement ticking (rail crossed twice per cycle, self-hit clicks transacted; needs the $d_0$ self-hit channel in the balance); (ii) frequency hierarchy (4:2:1 / 3:2:1, inner-super-field per the original intuition); (iii) out-of-plane structure; (iv) sea confinement (the outer binding deficit is where an external medium term would sit; the outer's own self-hit is repulsive-outward and cannot rescue it). Fail-closed throughout.

Verification: `node --test tests/braid-ideal-planar-tri-binary-breathing-extension.test.js` -> 4 pass, 0 fail.


## 2026-07-08 - Autonomous window: frequency locks, Kepler mode, counter-rotation; the harmonic-matching principle (item 11)

Operator-approved ~40-minute autonomous exploration following the Section 17 shape rejection. Two new owner scripts, both on the unified global-closure metric (one global $\kappa^*$, both components, full common period):

- [planar-tri-binary-frequency-lock-evaluator.mjs](../../../scripts/braid-ideal/planar-tri-binary-frequency-lock-evaluator.mjs) (tests 4 passing): per-layer integer frequency multiples $n_a\omega$, middle on rail, declared caustic regulator soft=0.02 (beats revive fold passages; min|D_s| reported). 4:2:1 restores inner-super-field ($\beta_I=1.4$ at $q_I=0.7$) per the original intuition — but circular locked states score 0.96–1.0 (wake essentially uncorrelated with the single-harmonic circular requirement), INCLUDING at the Kepler-third-law radii $n_a^2R_a^3=$const (operator's "nature echoes Kepler" prompted the test; retardation kills the static-binding scaling — during one antipodal wake transit at $\beta\sim1$ the pair rotates $\sim115°$). Counter-rotation variants ($\pm1$ circulation per layer): 0.85–0.99.
- [planar-tri-binary-kepler-extension.mjs](../../../scripts/braid-ideal/planar-tri-binary-kepler-extension.mjs) (tests 4 passing): equal-area speed modulation, $r^2\dot\varphi=$const per layer with common period (Kepler second law within iso-frequency; exact $e{=}0$ regression to 0.646). $\psi$ is a live knob here (gauge argument fails under speed modulation — verified) but every scanned direction degrades (0.65–0.94).

Structural finding (the harmonic-matching principle): a circular orbit needs a single-harmonic rotating force; only the co-rotating-frame DC part of the wake supplies that. Rigid co-rotation concentrates ALL wake power in the DC part — hence the iso-frequency family is the planar champion (0.646) and carries the Section 15 internal-transfer structure — while beats, counter-rotation, and speed modulation shift wake power into unmatchable harmonics. Fair continuation for locks (untried, expensive): harmonically-forced response orbits deformed at the beat harmonics.

Verdict: in-plane prescribed-orbit freedoms exhausted; the remaining 0.646 must come from out-of-plane structure (sphere-state shell braid / 3D nesting), the $d_0$ self-hit channel at the rail, sea confinement, or $m{=}1$ modes. Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Verification: `node --test tests/braid-ideal-planar-tri-binary-frequency-lock-evaluator.test.js tests/braid-ideal-planar-tri-binary-kepler-extension.test.js` -> 8 pass, 0 fail.


## 2026-07-08 - Sphere state evaluated; planar-era league table complete (item 11)

Continuation of the autonomous window. New owner script [sphere-state-shell-braid-evaluator.mjs](../../../scripts/braid-ideal/sphere-state-shell-braid-evaluator.mjs) (tests 3 passing; [spec Section 19](fold-crossing-chart-spec.md#19-sphere-state-and-the-completed-planar-era-league-table-2026-07-08)): the shell braid sphere state (three orthogonal-plane neutral binaries, equal radius/frequency, full 3D causal solve). Result: 0.986-0.997 - the three-fold orthogonal symmetry does NOT cancel the beating wake content; the harmonic-matching principle extends to 3D. Also measured the pure planar hexagon in the unified metric: 0.960 (binding coherent; pump dominates).

Completed league table: nested co-rotating iso-frequency 0.646 (champion; nesting internally cancels the pump, 0.96->0.646 - the largest single closure gain found); every rigidity-breaking family (locks, counter-rotation, sphere state) ~0.85-1.0; every lowest shape mode degrades the champion. Sharpened conclusion: coherence requires co-rotation rigidity; the right 3D lift is a RIGID tilted nested configuration (rigid-body rotation preserves the DC-only wake structure), not the non-rigid sphere state. Fail-closed throughout.

Verification: `node --test tests/braid-ideal-sphere-state-shell-braid-evaluator.test.js` -> 3 pass, 0 fail.


## 2026-07-08 - Rigid tilted-nested braid: outer tilt beats the planar floor; axial-dipole limit emerges (item 11)

Executed the Section 19 closure goal. New owner script [rigid-tilted-nested-braid-evaluator.mjs](../../../scripts/braid-ideal/rigid-tilted-nested-braid-evaluator.mjs) (tests 5 passing; [spec Section 20](fold-crossing-chart-spec.md#20-rigid-tilted-nested-braid-first-improvement-below-the-planar-floor-2026-07-08)): three nested neutral binaries as tilted dumbbells rigidly co-rotating about one axis (rigidity with tilts verified to $1.3\times10^{-15}$; $\alpha=0$ regression = 0.6460 exact). Tilt rescales each layer's kinematic need ($\omega^2R_a\cos\alpha_a$) and adds a vertical balance row.

- Inner tilt degrades (0.65->0.74 by 60°) - the over-binding diagnosis confirmed from a new direction.
- Outer tilt: degrades to ~0.83 mid-range, then turns and crosses BELOW the planar floor at ~75° (0.616), monotone down to 0.527 at 89°; with radius rescan, (0.5, 2.0, 84°) reaches 0.5237 - the first freedom all day to beat 0.646, a 19% gain with the outer still orbiting ($\beta_O\approx0.21$).
- Emerging geometry: as $\alpha_O\to90°$ the outer pair approaches a STATIC AXIAL DIPOLE on the rotation axis above/below the co-rotating planar core - an existing corpus structure (derived-structure inventory) discovered unprompted.
- Metric-honesty caveat: the global metric weights by $|a_{kin}|^2$ and the outer need $\to0$ near polar, so part of the gain is the outer leaving the ledger (its need-relative residual grows 1.05->3.4). Polar-family scoring needs ABSOLUTE residuals for the axial pair + need-relative for the orbiting core. Untried: middle tilt, azimuthal stagger, counter-tilt.

Fail-closed throughout: `retainedBranchClaim=false`, `scoreMovement=no_score_increase`, no accepted seed-path certificate, no new validator or schema.

Verification: `node --test tests/braid-ideal-rigid-tilted-nested-braid-evaluator.test.js` -> 5 pass, 0 fail.


## 2026-07-08 - Polar scoring, counter-tilt champion, flywheel family (item 11)

Executed the Section 20 closure goal widened by operator input (their original gyroscopic-flywheel pictures: binaries orbiting mutually-inclined "Lorentzian" rings; noted their velocity-flattening panel). [Spec Section 21](fold-crossing-chart-spec.md#21-polar-scoring-counter-tilt-and-the-flywheel-family-2026-07-08); `polarScore` added to the tilted evaluator; new [inclined-rings-flywheel-evaluator.mjs](../../../scripts/braid-ideal/inclined-rings-flywheel-evaluator.mjs); 11 tests passing.

- No axial-dipole levitation: static polar caps always pulled inward ($F_z<0$, inverse-square decay); the $90°$ limit is not a static structure. Bare inner+middle core scores 0.53/0.46 - better than any six-site configuration: the outer binary is a net closure burden in every tested arrangement.
- Counter-tilt champion: $(-15°,0,84°)$ at $(0.5,2.0)$ → 0.5088. Middle tilt degrades sharply (rail resists).
- Flywheel family: exact planar regression (after fixing a node-convention chirality trap - staggered nodes must phase-compensate or the $\iota\to0$ limit mirrors phase order against rotation sense); uniform inclination = global rotation (invariant, consistency check passed); relative inclination degrades EXCEPT small inner-ring tilts (~free): layer tilt tolerance $\propto1/\beta$, matching the operator's Lorentzian-flattening intuition layer-by-layer.

Standings: rigid counter-tilted dumbbells 0.5088 > planar 0.646 > all non-rigid families. The unsolved element is the outer binary. Fail-closed throughout.

Verification: `node --test tests/braid-ideal-inclined-rings-flywheel-evaluator.test.js tests/braid-ideal-rigid-tilted-nested-braid-evaluator.test.js` -> 11 pass, 0 fail.


## 2026-07-08 - Group E item 19 captured: translating-braid screw-motion closure

Operator asked how group velocity along the axis couples to the tilts. Answer: not yet modeled (all evaluators are rest-frame); captured as Group E item 19. Key enabling observation: rotation + axial drift = screw motion = still rigid, so the single-time closure evaluation survives under translation; only the causal wake geometry becomes fore-aft anisotropic. The item is the executable form of the operator's Lorentzian-flattening intuition and the tri-binary version of the Theorem A1 translating-binary benchmark: track optimal $(\omega,R_a,\alpha_a)$ vs drift $u$; test cadence $\to\omega/\gamma$ (time dilation from closure) and tilt-flattening toward the photon-limit 2D state.


## 2026-07-08 - Outer-layer tuning: azimuth optimum, new champion 0.4721, hierarchy reversed (item 11)

Executed the Section 21 closure goal ([spec Section 22](fold-crossing-chart-spec.md#22-outer-layer-tuning-the-caps-join-the-braid-2026-07-08); evaluator gains receiver-subset scoring; tests 8 passing).

- Outer azimuth: strong live knob; optimum $\theta_O\approx330$-$345°$ ($\sim30°$ behind the inner axis vs the Z3 slot at 240°). Cap-polarity swap degrades (0.48$\to$0.59): with the counter-tilted inner, cap orientation is physically selected.
- New champion $\mathbf{0.4721}$: $(q_I,q_O)=(0.5,1.65)$, $(\alpha_I,\alpha_M,\alpha_O)=(-12°,0°,84°)$, $\theta_O=330°$; flat basin 0.472-0.476. Ladder: 0.646 planar $\to$ 0.524 tilt $\to$ 0.509 counter-tilt $\to$ 0.472 tuned. Outer residual collapses 0.97 $\to$ 0.33-0.50.
- Hierarchy REVERSED: core with tuned caps 0.4685 < bare core 0.5039 - the Section 21 "outer as burden" was an untuned-azimuth artifact; correctly placed, the caps improve the core (incl. the rail row). Six-site braid is functional, not core+parasite.
- Champion geometry: equatorial rail pair at root birth, counter-dished inner ($\beta_I\approx0.48$), slow polar caps ($\beta_O\approx0.17$) offset behind the inner axis; rigid, one frequency, fully sub-field except the hinge.

Fail-closed throughout. Verification: `node --test tests/braid-ideal-rigid-tilted-nested-braid-evaluator.test.js` -> 8 pass, 0 fail.


## 2026-07-08 - Brainstorm capture: angles vs weak mixing, chirality, strain, L*(u) ladder

Operator brainstorm on the Section 22 champion, captured in [brainstorming.md](brainstorming.md#brainstorm-capture-2026-07-08-champion-geometry-springboards-operator-session): (1) misalignment angles (cap azimuth lag ~30° vs Weinberg 28.7°; inner dish ~12° vs Cabibbo 13.0°) - corpus-consistent mapping direction via the axial-frame doctrine, flagged coincidence-class pending sharper optima; the running-with-u test folded into item 19. (2) "Who leads" = braid chirality: C exact (full polarity swap), P broken by selected geometry (measured via the node-convention trap), cap orientation selected - the weak-sector parity texture; ties to queue item 12. (3) Accelerative strain on angles/radii queued as item 20 (champion stiffness spectrum). (4) Preferred inventory per group velocity: L*(u) ladder (h-click quantized), off-ladder relief channels (re-geometrize / transact / self-accelerate), inertia as closure-cost curvature along u - folded into item 19 notes.


## 2026-07-08 - Who-leads clarified: front cap under travel; symmetry ledger corrected

Operator clarified "who leads" = the front cap along the travel direction. Measured the cap-swap mechanism on the champion: the selected locking anti-aligns cap dipole with inner-dish dipole (opposite polarities vertically adjacent; north cap bound, Fz=-0.094 toward equator); the swap flips nearest vertical neighbors to repulsive (measured anti-binding) and degrades every layer. Symmetry ledger corrected in [brainstorming.md](brainstorming.md): the law is C-even and P-even; true enantiomers are degenerate; earlier "mirror scores differently" runs were partial flips, not parity images; the braid is a chiral object with handedness $h=\hat{\mathbf d}\cdot\hat{\boldsymbol\omega}$. Under drift the front cap is sign$(\hat{\mathbf d}\cdot\hat{\mathbf u})$; parallel vs anti-parallel drift are not symmetry-related, so item 19 will select a preferred leader (helicity-polarity locking; anti-braid leads oppositely by C-degeneracy) - a sharp in-model prediction.


## 2026-07-08 - Brainstorm entry 5 (decay vs speed/acceleration) + session insight index

Operator centrifuge idea assessed and captured: speed dependence of decay is established physics (muon rings, Mossbauer rotors) and a recovery obligation whose mechanism is item 19's cadence question (clicks paced by omega/gamma); the new testable axis is ACCELERATION, where clock-hypothesis bounds (~1e18 g, pure 1/gamma upheld) become a quantitative constraint on the champion's closure-stiffness Hessian (item 20) - a falsification-budget-style contact with tested physics. Added a session insight index to [brainstorming.md](brainstorming.md) covering all in-play threads with capture homes (pin, d0 bound, cross-hit closure, harmonic matching, league table/unified metric, Kepler-lock rejection, tilt tolerance ~ 1/beta, champion + hierarchy reversal, axial dipole, weak-mixing angles, chirality/front cap, stiffness, L*(u) ladder, lock dynamics, screw rigidity).


## 2026-07-08 - Item 20 executed: stiffness spectrum (saddle, backbone, pump-in-gradient); decay-dilation requirement promoted

[Spec Section 23](fold-crossing-chart-spec.md#23-champion-stiffness-spectrum-saddle-backbone-and-the-pump-in-the-gradient-2026-07-08). Gradient + full 7x7 Hessian + eigenmodes around the Section 22 champion. Spectrum (soft->stiff): -0.34 (relative-tilt combo), -0.04, +0.05 (theta_O - flat basin quantified), +0.23 (rail knob beta_M), +3.7 (alpha_O), +5.3 (alpha_M), +18.9 (q_I = rigidity backbone). Champion is a SADDLE; descent + q_I->0.47 refines to 0.4531 (systematic minimizer next). beta_M gradient negative (-0.27; 0.4468 at beta_M=1.1): the anti-damping pump appearing in the closure landscape, with the excluded self-hit brake as the pin that stops the slide at the rail - a consistency closure across Sections 11-12/23, not an instability. Clock-hypothesis bridge: operator directed promotion of the decay-dilation recovery obligation into the corpus - new row "Decay-rate dilation and the clock hypothesis" in braid-recovery-requirements.md (Relativistic and Gravitational Targets; owners proper-time + lorentz-kinematics); validator clean on that file (the single reported error is an ambient broken link in app-causal-delay-feedback/v1-product-requirements.md, another lane, NOT in this write set - it will block pre-commit until that lane fixes it).

Verification: `node --test tests/braid-ideal-champion-stiffness-spectrum.test.js` -> 3 pass, 0 fail.


## 2026-07-08 - Spindle braid: family name ratified and threaded

Operator ratified "spindle braid" for the rigid uniaxial tilted-dumbbell family (Sections 20-23; champion = its rest-state candidate). Corpus: new Comparative Glossary row + named-family paragraph in the Configuration Space chapter (both textbook-facing, candidate-not-retained claim level; validator 0 errors). Priorities: naming notes in the spec Section 20 header block, item 20, and the brainstorm insight index. Operator direction noted for later: once the champion survives native confirmation, archive the superseded configuration families (deferral gate = native retained-history confirmation).


## 2026-07-08 - Item 19 pass 1: spindle braid under drift - time dilation from pump-plus-pin; the electrino cap leads

[Spec Section 24](fold-crossing-chart-spec.md#24-spindle-braid-under-drift-time-dilation-from-pump-plus-pin-and-the-electrino-leads-2026-07-08); new owner script + 5 tests passing. Screw motion preserves rigidity exactly (3e-16); rest anchor reproduces the champion (0.4721).

- No interior cadence optimum in the partner channel at any drift - the anti-damping pump signature (Section 23's rail gradient) persists at every u; the landscape is ~self-similar in gamma-normalized cadence (slopes parallel to ~5%).
- Time dilation from pump-plus-pin: the coincidence-birth theorem applies to helical worldlines - self-hit opens at TOTAL speed c_f, i.e. at c = sqrt(1-u^2) - so the measured pump meets the brake at pinned cadence omega_0/gamma. Clock and decay-rate dilation (cadence-paced transactions) derived from closure + pin; mechanism half of the corpus decay-dilation recovery row. Caveats: fixed geometry (pass 1), d0-dependent brake magnitude (pin needs only sign + onset), partner-channel metric.
- Who leads: the ELECTRINO cap leads (closure prefers drift anti-parallel to the polarity dipole; gaps 0.044/0.104/0.174 at u=0.2/0.4/0.6 - grows with speed, per the operator intuition); anti-braid leads positrino-first by exact C-degeneracy. Helicity-polarity locking selected by closure. Curiosity: preferred-direction drift at u=0.2 scores 0.4721 = rest value to 4 digits (slow preferred travel nearly free).

Fail-closed throughout. Verification: `node --test tests/braid-ideal-spindle-braid-screw-drift-evaluator.test.js` -> 5 pass, 0 fail.


## 2026-07-08 - Item 19 pass 2: the angles run; motion is preferred. Pro/anti Q&A captured

Operator Q&A (pro/anti, helicity, circulation) captured as brainstorm entry 6: anti-braid = exact C-conjugate (degenerate in isolation - asymmetry burden on formation kinetics + sea texture, with the helicity-polarity lock as the C-odd environmental handle); the measured lock IS a helicity lock (champion left-handed, electrino-first; P-enantiomer right-handed, degenerate); all six sites circulate the travel line in one selected common sense; cw/ccw = handedness label, pro/anti = polarity label, independent but three-way correlated by closure.

Pass 2 ([spec Section 25](fold-crossing-chart-spec.md#25-drift-pass-2-the-angles-run-and-motion-is-preferred-2026-07-08); `pass2Optimize` added; 6 tests passing): per-u geometry re-opt at pinned cadence, preferred direction. (1) THE ANGLES RUN: inner dish -12/-4/+8/+16 deg at u=0/0.2/0.4/0.6 (through flat near u~0.25); cap lag 30->6 deg; alpha_O ~ constant (caps stay near-polar - no equatorward flattening). The weak-mixing-flavored misalignments are running quantities; any angle-observable mapping needs a declared kinematic point. (2) MOTION PREFERRED: f_opt 0.4721/0.4351/0.4110/0.4037 - the rest state is not the family optimum; still descending at u=0.6. Preferred-frame-sensitive; partner-channel caveats (no brake, no radiation cost, q_I frozen). Open: finite optimal u vs descent to the photon limit. L*(u) falls slightly slower than 1/gamma (geometry compensates).

Verification: `node --test tests/braid-ideal-spindle-braid-screw-drift-evaluator.test.js` -> 6 pass, 0 fail.


## 2026-07-08 - Item 19 pass 3: finite optimal speed; P/CP structural answer captured

Operator Q&A: total site speed = Euclidean vector sum (transverse rotation + axial drift) in the void frame - the Pythagorean origin of gamma; near-free slow travel = the flat Lorentz bottom at fixed geometry, while the pass-2 tilt (re-opt geometry) is toll-gated by the L*(u) ladder (inertia as quantized toll); P/CP hierarchy answered structurally (P-even law + chiral object + maximal helicity-polarity lock => sector-selective maximal P violation with leading-order CP exactness; small CP violation next-order via sea texture or running-angle path interference) - captured as brainstorm entry 7.

Pass 3 ([spec Section 26](fold-crossing-chart-spec.md#26-drift-pass-3-a-finite-optimal-speed--the-moving-family-is-inertia-anchored-2026-07-08)): extended preferred-direction sweep. FINITE OPTIMUM: broad flat basin u* ~ 0.5-0.65 (f ~ 0.404-0.409), steep climb beyond (0.4131 at 0.7, 0.4444 at 0.8) - the moving family is inertia-anchored, not photon-limit-runaway. Cap azimuth swings through exact alignment near the basin (30 deg lag at rest -> 18 deg lead at 0.8); dish angle keeps running. Robust: the structure (tilt -> basin -> climb); artifact-prone: basin location (partner channel, no brake/radiation, descent noise ~0.003). Preferred-frame accounting: reconciliation burdens = L*(u) toll gating + sea dressing (evaluator is sea-less; observed matter kinematics demand the dressed basin sit at rest-relative-to-sea). Fail-closed throughout.


## 2026-07-08 - Terminology (site, drift), discrete-symmetry corpus promotion, native handoff packet

Operator session continuation: (1) `site` and `drift` added to the terminology canon (terminology-usage.md, new "Site and Drift Usage" section; site = instantaneous architrino position = emission AND reception point; drift = uniform unaccelerated translation). (2) P/CP structural discussion promoted to the corpus as "Spindle Braid Discrete-Symmetry Structure" in the Configuration Space chapter (derivation-sketch claim level; parity-even law + chiral objects + maximal helicity-polarity lock => sector-selective maximal P violation, leading-order CP exactness, small CP next-order; validator 0 errors). (3) Brainstorm entries 8-12 captured (budget square + ultimate-energy stop = d0/max-curvature link; proper time = click count; MM orientation flag; super-field drift flag; h-transaction flag with the rail-poised middle as the measured h/2 borderline). (4) NATIVE HANDOFF: [spindle-braid-native-confirmation-handoff.md](spindle-braid-native-confirmation-handoff.md) written - the named candidate row for `native_retained_history_promotion` (item 4 status -> candidate-row-named); packet specifies the exact six-worldline seed, the release protocol, the same-record acceptance rows (incl. co-equal nu_J and nu_rec per the receiver-normal audit, d0 coincidence handling, h_act click logging), failure modes, and the same-thread acceptance rule.


## 2026-07-08 - Sea-dressed drift first pass: sparse static sea negligible

[Spec Section 27](fold-crossing-chart-spec.md#27-sea-dressed-drift-first-pass-sparse-static-sea-is-negligible-2026-07-08). Toy static sea (6 aligned dipole pairs at the SH-0-sea spacing 4.25, void-static) added to the screw-drift evaluator as environment sources: closure curve shifts uniformly by <~7e-4 at every u - negligible; landscape shape untouched. Sharpened reconciliation: denser/closer sea, dynamically responsive sea (orientational polarization = the sea's live channel per 2026-07-07), or the L*(u) transaction toll as primary anchor. All drift-evaluator tests remain green.


## 2026-07-09 - Exuberance-batch capture: MCB-via-spindle, score calibration, time pedagogy, h-constancy, horizon sign flip, analog tipping; items 21-22

Operator late-night batch captured as brainstorm entries 13-19 with intent-notes (why written down, power, relationships): MCB attack via the ultimate spindle (unifies d0 / r_max-curv / ultimate-energy stop; FCC-vs-HCP 2018 fork refiled); score calibrated (0.47 rest / 0.40 moving; zero belongs to the native+certificate rungs, not the rigid chart - the three-rung epistemology ladder); time pedagogy (absolute observer integer-ledger method; analog EE gate dual; "analog substrate, integer bookkeeping"); the h-constancy question split topological/dynamical with h_act(u) as the named computable; beyond-horizon sign flip = family change not time reversal (GR r/t role-swap mapping; white-hole echo = SMBH recycling; photon does not splat - different assembly native to the edge); analog h/2 tipping (separatrix decides for donor AND receiver). Queue items added: 21 `relativistic_time_mapping_document` (operator-directed corpus doc), 22 `corpus_architecture_review_pure_aaa_plus_mapping_textbooks` (pure-AAA corpus + GR/quantum/LCDM+ mapping textbooks; cross-lane hosted). Project-meta Lorentzian-curve note filed for the history/comic material.


## 2026-07-09 - MM-analog first pass; interior coalescence derived; exuberance batch 2 captured

Brainstorm entries 20-24 captured (zero-entropy crystalline core with FCC/HCP domains + defects; mass-is-a-defect; 50/50 emergence universality; the frame irony for item 21; horizon-state coalescence DERIVED - all-rail forces equal cylindrical radii: the spindle horizon state is a one-cylinder "barrel" with stacked heights/phases, confirming the operator's one-ring-with-phase-offsets intuition; provenance = each architrino's retained ledger).

MM-analog pass 1 ([spec Section 28](fold-crossing-chart-spec.md#28-mm-analog-first-pass-orientation-anisotropy-alignment-torque-and-the-u15-puzzle-2026-07-09); `residualsPerp` added; 7 tests passing): kinematic budget anisotropy (linear c<=1-u perpendicular vs Pythagorean parallel; perpendicular at parallel cadence = clicking regime, middle crosses c_f twice/cycle); measured closure anisotropy 16-25% at relativistic drift, closure prefers axis-parallel flight (orientation torque, rifle-bullet flavor - candidate isotropy-recovery mechanism AND constraint); scaling ~u^1.5 in [0.05,0.2] - not cleanly quadratic; residual first-order component = the leakage risk; finer small-u study + regulator checks + gain/loss decomposition required before observable claims. Fail-closed.

## 2026-07-09 — Anisotropy scaling law resolved (spec Section 29)
- New instrument `scripts/braid-ideal/spindle-drift-anisotropy-scaling-scan.mjs` (+4 tests, green): finer u grid (0.0125–0.2), rest-baseline decomposition, soft/Nt artifact sweeps.
- The Section 28 "u^1.5" was a linear–quadratic CROSSOVER (u_x ≈ 0.063, mid-grid): gap = 0.102|u| + 1.61u².
- Perpendicular loss = 2.01u² (clean quadratic); helicity-AVERAGED parallel cost = 0.422u² (constant coefficient across the whole grid); the ONLY first-order term is the leader-selected helicity-polarity lock (−0.102|u|), odd in u.
- MM-analog verdict: orientation anisotropy is second order (no MM-class first-order leakage). Residual exposure is Hughes-Drever-class (spin-coupled), gated on the open closure-to-energy map.
- Regulator/sampling artifact checks pass (≤2e-5 / ≤1e-8).
- Next: h_act(u) invariance computation (brainstorm entry 16).

## 2026-07-09 - Native retained-history confirmation run EXECUTED; candidate REJECTED (queue item 4); rest state disperses; the pin is a speed attractor

[Spec Section 30](fold-crossing-chart-spec.md#30-spindle-braid-native-retained-history-confirmation-run-certificate-rejected-the-rest-state-disperses-the-pin-is-a-speed-attractor-2026-07-09). The spindle braid rest-state candidate (tabled parameters, untouched) was seeded as exact moving-circular histories, held, and released onto the production `AbsoluteHistoryRootRuntime` path (read-only; released history consumed through the runtime's own linearized-segments policy). Seed anchor: native evaluation reproduces the prescribed champion to 4 digits (0.47212 vs 0.47209; kappa* 0.31511 vs 0.31504). All packet Section 3 certificate rows produced same-record (root ledger with per-root D_s/D_T/W^rec, nu_J=+0.22 positive floor, gaps, finite memory, d0-stratum same-source policy with d_min < rho_c, per-layer force rows, h_act click ledger, stability row). Decisive answers: shape NO (tube lost at ~0.13 rotation, dispersal, first blocker `shape_loss_radial_under_support`); rail QUALIFIED YES (first native evidence of the field-speed pin as a speed attractor: beta_M turns at 1.083 and decays to 1.019 from above while radius runs away); tangential closure YES at the seed record, NOT sustained dynamically. Findings: native cap fall-in (Section 21 no-levitation reproduced), inner-radius sweep through the refined q_I~0.47 without settling, integrator-converged (dt-halving <= 6e-4), soft-hardening reproduces the caustic ejection artifact (chart-clean click booking remains the brake-magnitude path). Operator decision in the same thread: REJECTED per the run recommendation (first blocker `shape_loss_radial_under_support`; scoreMovement=no_score_increase). Queue item 4 status `run-executed-candidate-rejected-2026-07-09`; the gate stays fail-closed awaiting the next named candidate row; the archive call does NOT ripen. Owner script + 4 tests landed and are reusable for the next candidate row.

## 2026-07-09 — h_act(u) first pass (spec Section 31; renumbered — the native confirmation run in the operator's parallel thread owns Section 30)
- New instrument `scripts/braid-ideal/spindle-braid-click-action-invariance-evaluator.mjs` (+4 tests, green).
- Stored action per click = 1/gamma EXACTLY at frozen geometry (invariance would require gamma-growing inertia — relativistic mass as condition, not assumption).
- Transacted angular impulse per click: Jz/Jz0 = 1 ± 0.236|u| − 1.28u² (helicity-odd linear, helicity-even quadratic; regulator-clean). NOT invariant at frozen geometry.
- Pump-work action reverses sign by u≈0.5–0.6: frozen geometry off-family at mid-drift; claims are small-u only.
- Re-optimized readout instrument-limited (saddle-landscape wander changes intrinsic J scale).
- REFRAME: a Jz-conserving trajectory geo(u) exists (bracketed above/below at u=0.2). If it coincides with the closure-optimal trajectory, h-constancy is DERIVED. Queued as priorities item 23.

## 2026-07-09 - Chart-clean click booking on the native run: coincidence push rules the poised clicker; native two-sided escapement at moderate stratum; shape loss stratum-robust

[Spec Section 32](fold-crossing-chart-spec.md#32-chart-clean-click-booking-on-the-native-run-the-coincidence-push-rules-the-poised-clicker-a-two-sided-escapement-appears-at-moderate-stratum-shape-loss-is-stratum-robust-2026-07-09). `--chart` mode added to the native confirmation-run owner script: fold-flagged same-source rows are substep-integrated with the production runtime's unsoftened signed branch orientation (density-of-states integral per the Section 3.3 contract), declared d0 stratum in the denominator, Section 3.1 click rows with witness; cross pairs keep the canonical regularized kernel; anchor 2.6884 absorbed fraction at rho_c=0.01 reproduced on the accepted brake-measurement machinery. Findings: at rho_c=0.01 the quasi-static REPULSIVE coincidence push (reflection-locked m=+1 at slow crossings) ejects the poised clicker at release (+0.18 beta in one step) and the geometry scrambles - the "50x pump, not load-bearing" object is natively destructive at the poised seed; at rho_c=0.05 a genuine native two-sided escapement appears (beta_M hunting just below the rail through ~10^3 crossing transactions - the Section 12 marginally-below-edge pin, first native realization); dispersal occurs at every regulator setting (pointwise, chart d0-scale, chart moderate-stratum), so the queue item 4 rejection (`shape_loss_radial_under_support`) is now regulator-complete. Sign structure measured: absorptive only on fast crossings, repulsive on slow ones (beta-dot gated). Tests 4 passing; fail-closed throughout. Next: sea-confined native release on the same machinery.

## 2026-07-09 — Brake attack checkpoint (in flight; owner script written, capture pending)
- New instrument `scripts/braid-ideal/spindle-rail-click-booking-vector-evaluator.mjs`: full-VECTOR chart-clean click booking (radial + tangential, all live branches, no min-delay gate) + sustained supra-rail channel map over (beta, betaDot).
- First numbers (cut=0.005, kappa=1, rho=1): click impulse across the crossing is tangentially ABSORPTIVE (−98.8, ≫ pump-per-rotation 18.1 → over-damped pin, consistent with Section 12's rho~50) and radially INWARD (−0.83).
- Sustained channel: at betaDot=0 EXACTLY m=+1 (reflection symmetry confirmed on the evaluator — matches the production-runtime finding; force outward+forward, a pump). The brake and the INWARD radial appear only with betaDot>0: at beta=1.02, betaDot=0.1 → m=−0.30, radial −0.48 (inward), tangential −2.98 (brake). CONFINEMENT IS DYNAMIC: the self channel confines radius only while the pump is actively driving — a self-regulating orbit-pin candidate.
- Witnesses (convergence/cut-sweep) and the (beta,betaDot) full map still to run; spec Section 32 capture pending. Operator redirected to the corpus spindle-first reweighting pass (b) — resume here after.

## 2026-07-09 — Corpus spindle-first editorial reweighting pass (operator-directed, option b)
- Scope: presentation reweighting only — no claim changes, no archive of alternatives (the archive call does not ripen on a rejected candidate, per spec Section 30).
- noether-braid-taxonomy.md: new "Leading Candidate: The Spindle Braid" section (definition, league standing, native-release honesty, alternatives-as-comparison-population); iso-frequency table row now points at the spindle.
- noether-braid.md: leading-candidate paragraph in the overview (spindle-first with fail-closed status).
- noether-braid-configuration-space.md: spindle named-family paragraph upgraded to leading-candidate standing + native-release honesty (dispersed under release, radial under-support; confinement is the open burden).
- nested-shell-braid.md: comparative-standing note (nested families = comparison population; hypotheses stated so they re-host on whichever family is retained).
- neutral-braid.md: leading-candidate cross-pointer (spindle is a neutral braid; certificate target still open).
- Generated artifacts regenerated per AGENTS (scene graph --write --strict, agent startup orientation, textbook reading copies); full check-content-integrity re-run in flight (iOS package step is slow); no corpus->priorities links introduced (checked 0).

## 2026-07-09 — Brake attack, evaluator track complete (spec Section 32)
- Vector click booking: click impulse absorptive AND inward, converged, sign-stable across cuts (magnitude stratum-set as derived).
- Sustained supra-rail channel = phase-sensitive valve: m=+1 exactly at betaDot=0 (outward pump), m>1 decaying (boosted outward — THE native dispersal mechanism, explained), m<0 driven (brake+inward).
- Confinement is dynamic: only the rail-straddling clicker regime confines; steady riders disperse. Middle must CLICK, not ride.
- Amortized budgets: circular-geometry clicks are tangential-heavy (ratio ~70); consistency needs ≲12 → NON-CIRCULAR (epicyclic breathing) middle named as the candidate-row design target, quantified.
- 4 tests green (19 total across the session's four new instruments).

## 2026-07-09 — Reduced chatter integrator, first pass (spec Section 33)
- The clicker EXISTS at reduced level: deterministic, pump-driven, self-timed relaxation oscillation (sawtooth through the rail, several clicks/rotation). No external tipping needed; external wakes phase-shift clicks (analog h/2 tipping rides on an intrinsic clock). Null test clean.
- Circular-chatter radial supply ~3% of centripetal (bracket s*≈0.97) — confirms Section 32 budget verdict independently; epicyclic re-angling is the lever.
- dt-halving witness FAILS: pointwise 1/r² click spike = the Section 2.3 artifact biting its own instrument. Numbers provisional; structure robust.
- Named second pass: event-based integrator with chart-integrated click impulses + Section 32 valve force between events.
- 3 tests green (22 total across five new instruments this session).

## 2026-07-09 - Sea-confined native release: static aligned shell ANTI-confines; sea route closed at minimal declared scope

[Spec Section 35](fold-crossing-chart-spec.md#35-sea-confined-native-release-the-static-aligned-shell-anti-confines-the-sea-route-closes-at-its-minimal-declared-scope-2026-07-09). `--sea` mode added to the native confirmation-run owner script: SH-0-sea FCC-12 static aligned shell as a one-way held environment (72 zero-radius static production sources), canon-named spacings only (4.25 and 2*sqrt(2)). Seed record: sea radial supply is percent-level and OUTWARD on the under-supported layers (caps +7% of need at 4.25; +93% at min non-overlap spacing); unified seed closure worsens 0.4721 -> 0.4985. Release at 4.25 disperses FASTER than isolated (halt t=3.1 vs 4.8, middle ejected). Verdict: the static aligned shell neither dresses (Section 27) nor confines - sea route closed at its minimal declared scope; surviving sea reading requires a dynamically responsive (orientational-polarization) or self-consistent sea; surviving isolated route: non-circular same-level release. Also housekeeping: duplicate concurrently-landed Section 32s resolved (brake-attack/chatter sections renumbered 33/34, internal refs fixed). Tests 4 passing; fail-closed; queue item 4 rejection stands, now with the sea route measured.

## 2026-07-09 — Event-local fine study (spec Section 34; item 24 step 2a)
- Click event is SELF-LIMITING: continuum limit = perfect ceiling → the pin is a SLIDING MODE on beta=1 ("ride BY clicking"); Section 33 sawtooth amplitude was dt-artifact.
- Epicyclic lever measured, dt-converged: ratio(vr) = 0.013 + 1.02·vr — click-synchronized radial damper; clicks cluster past perigee in eccentric orbits (self-organizing).
- Pure sliding limit ESCAPES (constant outward drift ~0.24 at support 0.76): rectification needs DISCRETE clicks. Hypothesis of the session: h-quantized transaction may be the confinement enabler, not decoration.
- 3 tests green (25 total, six instruments). Next: 2b discrete-event orbit integrator.

## 2026-07-09 - Non-circular same-level release: epicyclic apoapsis seed improves native closure (0.4464) and survives coherently; isolated-braid verdict route-complete

[Spec Section 36](fold-crossing-chart-spec.md#36-non-circular-same-level-release-the-epicyclic-apoapsis-seed-improves-native-closure-and-survives-coherently--but-the-isolated-braid-still-expands-2026-07-09). `--em/--ei/--eo` per-layer epicyclic seed added to the native run owner script (a.m.-matched apoapsis start, held prehistory = matched slower circle, production-native, no release discontinuity; kappa frozen at 0.315105, declared). Findings: seed closure improves under both self-consistent conventions (fitted-kappa circular-need 0.4721 -> 0.4531 -> 0.4464; frozen-kappa held-circle-need 0.4721 -> 0.4417 -> 0.4188; the mixed frozen-kappa/circular-need pairing is flat-to-worse and reported for completeness) - a new hunt axis, with the convention ambiguity arguing the family be scored by released dynamics; released epicyclic braid is the best-behaved release measured (no halt through 2 rotations, ~500-580 two-sided escapement transactions, no ejection, no cap fall-in) but still expands self-similarly - the radial-support deficit is structural across ALL declared bookings and canon-named variants (circular/chart/sea/epicyclic), so the isolated-braid verdict on queue item 4 is route-complete. Tests 4 passing; fail-closed. Next: the epicyclic hunt (minimize native seed residual over (e_I,e_M,e_O, apsidal phases) at frozen kappa*) and the dynamically-responsive-sea question.

## 2026-07-09 — Discrete-event orbit integrator (spec Section 35; item 24 step 2b)
- Rectification real (clicks cluster outward-phase; strengthens with quantum) but percent-scale: s_min ≈ 0.99 (q→0) → 0.97 (q=0.15). Knife edge: escape below, collapse at s=1.0.
- Strong discreteness-as-enabler hypothesis REJECTED at reduced level; weak form survives, measured.
- Division of labor quantified: clicker = speed pin + last-percent radial trim; geometry/sea must close radial support to ≳97% BEFORE release. Caps (0.53) now the binding constraint.
- New survivability statistic for the search: radial support ratios ≈ 1 on all layers at unified kappa*, closure residual second.
- 4 tests green (29 total, seven instruments this session).

## 2026-07-09 — Support-first configuration search (spec Section 36; item 25)
- Instrument validates against native seed support rows (0.90/0.75/0.53 two-digit match).
- Caps fixed by qO pull-in (0.53→~1.0). Middle structural at 0.72 until the INNER AZIMUTH thetaI opened (frozen at the 120° convention in every prior search): thetaI ≈ −24° lifts M to 0.88.
- SUPPORT-CANDIDATE V1: qI 0.462, qO 1.236, alphaI −10.4°, alphaM −2.7°, alphaO 84°, thetaO 337°, thetaI −23.7°; ratios (0.96, 0.88, 0.98); closure 0.3241 — new best by 31%, found by chasing SUPPORT. Objectives co-improve → basin looks physical.
- Static toy sea ruled out as support fix (negligible at named spacing, harmful closer).
- Gap: 0.88 + 0.03 clicker < 0.97. Routes: polish basin (alphaO trap check), epicyclic release at v1, responsive sea.
- buildBraid extended backward-compatibly with thetaI/thetaM knobs (7/7 drift tests still green). 4 new tests green (33 total, eight instruments).

## 2026-07-09 — v1 basin polish, sum rule, Row 2 tabled (spec Section 37; item 25 complete for this pass)
- Basin floor real (descent flat; alphaO=84° sharply optimal, not a trap).
- Frame-scaling lambda-probe: SUPPORT SUM RULE — allocation across layers free, total supply capped at mean ~0.94 at unified kappa*. v1 is the Pareto allocation. True deficit = ~6% GLOBAL, not 12% middle.
- Reading: no rigid bare-void spindle self-supports; the missing ~6% is the responsive sea's job (static sea ruled out) — the corpus "Noether sea stability" obligation now has a number.
- Support-candidate v1 tabled as Candidate Row 2 in the handoff packet (same machinery/discipline as Row 1 + support rows in the certificate; four named release diagnostics incl. the dispersal clock and inward-slip test).
- Next evaluator track: responsive-sea ~6% feasibility estimate.

## 2026-07-09 — Responsive-sea feasibility estimate (spec Section 38)
- Linear-response instrument with full causal double delay: sea confinement margin OSCILLATES IN SIGN with shell spacing — spacing-selective confinement, a retardation resonance (instantaneous response would be Onsager-destabilizing; delay makes confinement possible). Harmonic matching at the medium scale.
- Magnitude: best single shell R≈3.5 needs alpha≈11 vs rough O(1) per assembly — marginal-plausible for a dense phase-selected multi-shell sea. Sum rule makes allocation free; sea owes only the mean 6%.
- Named spacing 4.25 NOT at a confining peak for this braid — co-tuning question handed to SH-0-sea.
- 3 tests green (40 total, nine new instruments this session).

## 2026-07-09 — Epicyclic candidate-row hunt (spec Section 39; epicyclic-hunt-handoff executed)
- Owner script gained the two declared knobs: per-layer apsidal phase offsets (`--phi-i/m/o`) and apoapsis/periapsis start (`--start-i/m/o`); exact-prehistory preserved (release is necessarily at an apsis); both seed-record conventions (`conventionPair`) built into the report. 6 tests green.
- 21 variants at frozen kappa*=0.315105, chart rc=0.05, 2-rotation window (best row extended to 3). Periapsis starts destructive (supra-rail birth → over-absorption scattering). e_M lever real but shallow (0.877→0.776 over e_M 0.05→0.25); e≥0.3 ejects the inner pair; e_I/e_O/all-layer null-to-worse.
- NEW seed direction: phi_M ≈ +30° → fitted/circular 0.3077 (below v1's 0.3241, on the OLD champion), frozen/held 0.3680; narrow asymmetric optimum. Diagnostic only; handed to the item-25 basin.
- Released dynamics: whole family expands; clean floor dradM ≈ 0.75 (em=0.25, phi=30°: steady through rotation 3, halt on radius bound at t=17.87); lower scores are I–M scattering artifacts (inner kicked to constant-speed coast), first-blocker-flagged.
- VERDICT: family closed, no candidate named — the packet's authorized null outcome, consistent with the Section 37 sum rule. Gate stays fail-closed; knobs remain as reusable machinery for Candidate Row 2 release diagnostics.

## 2026-07-09 — Sea estimate: kernel upgrade + multi-shell sum (spec Section 39)
- Receiver-normal kernel: resonance bands survive; peak shifts to R=4.0; NAMED SPACING 4.25 now inside the confining band (edge) — co-tuning anomaly softens.
- Multi-shell natural-density sum: unorganized sea nets weakly confining (bands outweigh); organization helps ~1.6x.
- Magnitude: alpha_req ~ 27-42 vs O(1) — linear dipole falls 25-40x short. Routes: near-critical enhancement (sea assemblies are marginal braids), saturated ORIENTATION response (the live channel), near-field corrections. Target handed to SH-0-sea: beat alpha_eff ~ 27.
- 4 tests green (41 total). Evaluator track now holds for the Row 2 native verdict.

## 2026-07-09 — Candidate Row 2 native run: REJECTED (spec Section 40); phi_M affinity null
- Owner script gained the tabled `--row=2` selection (v1 geometry, pinned transverse cadence omega=1/cos alpha_M) and seed support rows in the certificate; 8 tests green. Native seed validates the tabling instrument to 4 digits (kappa*, closure, all three support ratios).
- Release (both bookings, 3-rot windows, frozen seed-fitted kappa*=0.3005): tube loss t≈0.80 — dispersal clock UNCHANGED vs Row 1 (0.83) despite deficit 24%→6%: linear support-deficit scaling refuted; the clock is tangential. Cap fall-in cured. Middle: no inward slip; speed-attractor pin third native confirmation; chart escapement holds middle radius ~1.00 for ~0.2 rot.
- Allocation dynamics: free allocation confirmed dynamically as RUNAWAY (I over-bound >1, M →0.03) — the failure mode, not equilibration.
- First blocker `shape_loss_inner_tangential_brake_spiral` (inner braked −0.22 at seed, unchanged Row 1→Row 2; spiral in → rail crossing → coincidence push). Operator REJECTED in-thread; gate stays fail-closed.
- phi_M breathing-phase affinity check: NULL at v1 (phi=0 optimal, breathing-averse in e_M) — v1's azimuths already absorbed the old champion's +30° direction; basin-is-physical corroboration.
- Named next: tangential-closure-targeted row (item-25 objective + per-layer tangential residuals, or dynamically sustained I→M transfer).

## 2026-07-09 — Orientation saturation closes the gap (spec Section 40)
- Saturated orientation (live channel), braid's OWN axial dipole p0=2.20, single 6-site shell at the NAMED spacing 4.25, fast alignment: 4-6% inward margin — the Section 37 requirement MET (linear-dipole had missed by 25-40x). alpha_eff = p0/|E| ≈ 38 > 27, the weak-field enhancement verified.
- Band structure persists (3.0 loosening −19%, 4.0 peak +12%, 4.25 edge 4-6%): the named spacing sits where SUPPLY ≈ DEFICIT — capture-grade speculation: 4.25 derivable as the sea-braid balance fixed point.
- Alignment speed decisive (slow limit 10x weaker); reorientation-time estimate lands at ~the rotation period — the fast limit needs (and suggests) a reorientation RESONANCE. Third commensurability of the day.
- 4 tests green (45 total, ten new instruments this session).

## 2026-07-09 — Row 2 verdict received; tangential-sibling first pass (spec Section 41; numbering notice added)
- Numbering notice added at spec top (two threads duplicated numbers 32–40); renumbering pass queued as item 26 (quiet window, both threads holding).
- Row 2 verdict (native thread): cap fall-in CURED (support statistic validated); dispersal clock UNMOVED (0.80 vs 0.83 despite 24%→6%) — linear support-deficit scaling refuted; real clock = inner tangential brake −0.22 (family-invariant across rows); allocation runs away dynamically; phi_M+30° null corroborates v1 basin.
- Cross-stack confirmation on my instrument: tau = (−0.218, +0.096, ~0) at v1; net torque ≈ 0 — conservative inner→middle TRANSFER (tidal-flavored), not a loss.
- Extended search J = Σ[(s−1)² + λτ²] (first pass, λ=2): M's row halves, O zeroes, support+closure hold — INNER ROW IMMOVABLE (−0.2176→−0.2168). Candidate tangential sibling of the sum rule.
- Routes named: (a) escapement-return M→I (+0.22 target), (b) counter-sense inner (scoped, expect rejection), (c) sea tangential rows on the orientation instrument.

## 2026-07-09 — Escapement-return study (spec Section 42)
- Internal M→I return BUDGET-BOUNDED below need: conservation caps it at the transfer (~0.19) times a geometric fraction; uniform-phase clicks average the first-order return to O(q²); phase-pinned (commensurate — fourth resonance condition of the arc) reaches only O(0.1) at large quantum vs 0.218 need. Isolated braid cannot re-supply its inner.
- Sea tangential rows (orientation instrument extended): AT THE NAMED SPACING the sign structure is exactly right — inner +0.032 forward, middle ~0 (left to its escapement), outer +0.031; off-band it flips. Single shell covers ~15% of the inner need.
- THE METABOLISM PICTURE: sea feeds braid angular momentum → internal wake channel transfers inner→middle → escapement absorbs surplus → outgoing wake drives sea orientation order. Matter is an open system in steady state with its bath; isolated matter runs down. Row 3 must be SEA-DRESSED (responsive sea, not static).
- 5 tests green on the instrument (session braid-ideal instruments all green).

## 2026-07-09 — Dressed budget refinement + Row 3 design draft (spec Section 43)
- Uniform sea CANCELS its own tangential bands (gas supplies radial only); structure required for torque.
- FCC coordination shells (12/6/24 at a, a√2, a√3; a=4.25; p0=2.20): radial mean 6.6% — THE RADIAL LEDGER CLOSES at estimate grade from pre-existing named quantities, nothing fit. Inner torque +0.044 (~20% of need).
- No lag resonance: gamma/omega >= 2 required (one-sided); gamma/omega = 0.5 turns radial NEGATIVE. Handed to SH-0-sea: what sets assembly reorientation rate, is 2*omega available?
- Candidate Row 3 DESIGN DRAFT added to the handoff packet (responsive-sea-dressed release of v1; FCC 12-shell, p0=2.20, gamma=2omega; five decisive diagnostics incl. the metabolism ledger). Not tabled — operator decision pending.
- 6 tests green on the instrument.

## 2026-07-09 — Candidate Row 3 TABLED (operator decision)
- Handoff packet Row 3 block flipped DESIGN → TABLED; priorities updated. Paste-ready prompt delivered to operator for the native run thread. Evaluator track holds for the Row 3 verdict; renumbering pass (item 26) remains queued for a quiet window.

## 2026-07-09 — Candidate Row 3 executed and REJECTED (native run thread; spec Section 44)
- Environment built exactly as tabled (FCC-12 at a=4.25, p0=2.20 verified = braid's own axial dipole 2.1978, gamma=2*omega, orientation-only, retained orientation history, both loop legs causally delayed per pair). Owner script `--responsive-sea`; tests 11 passing. Seed anchor reproduces Row 2 (kappa* 0.30048, closure 0.32404, support 0.96/0.88/0.98).
- FIVE DIAGNOSTICS: clock unmoved (0.855/0.8525 vs 0.83/0.80); sea DEEPENS the inner brake (tau_I −0.243 vs −0.218 bare; estimate said +0.032 forward); metabolism ledger BACKWARD (net −0.082 vs +0.11); radial ledger OPENS (sea supply −13.7%/+7.2%/−38.0% vs +6.6% est.; dressed support 0.822/0.952/0.600); order parameter ~0.32, no secular growth — no metabolism loop closes.
- ROOT CAUSE ISOLATED (5-variant cross-check): kernel reproduces published Section 42 rows under the estimate's uniform-2R/c convention; true FCC-12 doubles them; EXACT per-pair causal delays flip both ledgers. Anti-confining phase slip (the packet's named failure mode) at the seed record. Exact-delay confining band moved inward: R≈3.25–3.5 (tau_I forward +0.114 = 52% of need), named 4.25 near the trough.
- First blocker `sea_anti_confines_at_exact_causal_delays`; operator REJECTED in-thread; gate fail-closed. Bonus: first native inward middle slip (dressed chart booking, r_M 1.0→0.63); cap fall-in stays cured; honesty rows clean (orientation dt-witness 0.075°, zero clamped lookups).
- Standing rule captured: no candidate row tabled from a uniform-loop-delay estimate again; exact-delay promotion is a gate on estimate-grade sea results.

## 2026-07-09 — Row 3 verdict folded in; exact-delay correction (spec Section 45)
- Row 3 REJECTED: `sea_anti_confines_at_exact_causal_delays`. My uniform 2R/c idealization was the error (braid radius order-one vs shell radius; per-pair phase spread ~2.5 rad). Blocker reproduced exactly on this stack (+4.2% → −9.7% at 4.25).
- Instrument upgraded: exact per-pair delays now the claim-bearing default; uniform mode pinned as regression (published rows preserved in tests, 7/7 green).
- Band relocated: confining R ≲ 3.5; supply=deficit fixed point R* ≈ 3.4 (6-shell) / 3.5 (FCC-12); inner torque ~half need in-band — cross-validates the native run. Sections 40/42/43 named-spacing claims and entry 31's 4.25-fixed-point REFUTED at exact-delay grade (forward-only downgrade).
- THE QUESTION INVERTS: homogeneous v1 sea equilibrates at ~3.4, not 4.25. Either the named constant revises (a prediction!) or sea assemblies differ in cadence/geometry (band tracks THEIR phases — commensurability inversion: ~0.8ω sea puts its band at 4.25). Provenance audit of 4.25 + cadence solve = Row 4 prerequisite (concurring with native thread).

## 2026-07-09 — Row 4 prerequisite executed (spec Section 46)
- PROVENANCE: 4.25 = "eligible crossing row nearest the window midpoint" of the SH-0-sea amplitude-only retention window [3, 5.35], computed for the OLD two-speed row (u0.8/v0.2) — a selection convention, never a derived point.
- CADENCE/DYNAMICS SOLVE: exact-delay relaxation sweep at 4.25 — anti-confining at EVERY gamma (−2.6% to −9.9%; test-pinned). The band is geometric (braid cadence + loop phases); sea dynamics cannot move it. The 0.8ω reconciliation is dead at this grade.
- RESOLUTION: no conflict — R*≈3.4 sits inside the legacy window; the midpoint convention is what falls. RECOMMENDATION to operator: revise the named spacing to R*≈3.4 (canon-adjacent constant — operator decision), then table Row 4 at a=3.4.
- Relax mode upgraded to exact delays (uniform mode preserved as pinned regression); 8/8 tests green.

## 2026-07-09 — Candidate Row 4 TABLED (operator middle-path decision)
- Row 4: responsive-sea release of v1 at a=3.40 (exact-delay fixed-point region, mild-surplus side), FCC-12, p0=2.20, gamma=2omega — ONE change from Row 3 plus a binding no-uniform-delay validation obligation.
- Named spacing constant NOT re-declared; decision deferred pending Row 4 verdict (middle path).
- Seed expectations computed exact-delay (mean radial ~+10% FCC-12; inner torque ~+0.12 ≈ 55% of need; clock finally has a computed reason to move).
- Paste-ready prompt delivered to operator.

## 2026-07-09 — Candidate Row 4 executed and REJECTED (native run thread; spec Section 45)
- Environment as tabled (a=3.40, FCC-12, p0=2.20, gamma=2*omega, exact per-pair delays everywhere; `--rsea-spacing` tabled selection; tests 12 passing). Bare anchor = Rows 2/3. Pre-release cross-check re-anchor at 3.40: fast-limit rows reproduce the packet (+13.6% radial, tanI +0.117, Nt-stable).
- DIAGNOSTICS: clock MOVED for the first time — EARLIER (0.725/0.7725 vs 0.83/0.80/0.855); tau_I lifted to the expected −0.10 and HELD (sea feed steady +0.117→+0.13; beta_I decay halved) — THE INNER-BRAKE INVARIANT IS ENVIRONMENTAL, NOT STRUCTURAL (first native break); net sea torque POSITIVE as predicted (+0.0285 seed, growing); radial surplus NEVER ARRIVED (native mean +1.2% vs ~+10% tabled; gamma=2*omega relaxation collapses the radial channel at the in-band spacing while the tangential channel is lag-robust — channel-dependent fast-alignment requirement, new named finding); order parameter no secular growth (late rise = shell-crossing artifact).
- Allocation mismatch confirmed as proximate killer: sea supply lands O-heavy/M-negative; M starves to 0.783 dressed (below bare 0.880), rides out; braid never enters surplus (escapement surplus-regulation question mooted).
- First blocker `radial_surplus_lag_collapse_middle_starvation`; operator REJECTED in-thread; gate fail-closed; named-constant re-declaration stays open (3.40 delivered the tangential physics, not the radial).
- Row 5 gate: joint lag-robustness map over (gamma/omega, a) at settled-relaxation grade, both channels at once — or a scoped negative closing the FCC-12 orientational family at p0=2.20.

## 2026-07-09 — Row 4 verdict folded in; joint lag-robustness map: SCOPED NEGATIVE (spec Section 47)
- Row 4 (run thread): REJECTED but the arc's biggest positive — inner-brake invariant BROKEN environmentally (tau_I −0.218 → −0.10, held; sea feed steady; first native break), net sea torque positive as predicted; killer = radial lag-collapse + M-starvation (clock moved EARLIER, 0.725).
- Instrument correction: relax Euler step unstable at Nt=8/gamma=2 (gamma·dt≈1.6); map re-run settled at Nt≥16 — directionally consistent with the native lag-collapse.
- THE MAP: radM NEGATIVE in every (a, gamma) cell (−6 to −14%) while radO +12-25% and tanI forward/lag-robust everywhere; alphaM sweep (−30°..+30°) never crosses zero. SCOPED NEGATIVE: the FCC-12 orientational-dipole sea at p0=2.20 cannot feed the middle, family closed at declared scope (test-pinned).
- Routes ranked: complementary allocation (braid feeds M via lambda<1, sea feeds caps+inner-tangential — the sum rule makes allocation free), positional/breathing (phonon) sea, quadrupole/near-field.
- Named-constant re-declaration stays open (3.40 delivered tangential physics only).
- 9/9 tests on the instrument.
