# Explored Braid Geometries Consolidation Plan

Status: `phase-2-executed-2026-07-09` (operator direction: move the corpus toward the spindle braid as the primary family). Owner lane: corpus architecture (sub-plan of queue item `corpus_architecture_review_pure_aaa_plus_mapping_textbooks`).

## Executed — Phase 1 (2026-07-09)

- `content/markdown/aaa/noether-braid/spindle-braid.md` created and expanded into the full leading-candidate scene (definition, harmonic-matching principle, closure ladder, rest-state champion and stiffness spectrum, field-speed pin/escapement with derived $\omega_0/\gamma$ dilation, motion/inertia/isotropy results, discrete-symmetry structure, confinement problem), promoted from the braid-ideal proofing record at stated claim levels; corpus prose self-contained, no priority links.
- `symmetric-shell-braid.md` + `nested-shell-braid.md` consolidated near-verbatim into `explored-braid-geometries.md` (comparison-population framing, re-entry conditions); originals deleted; externally consumed slugs preserved; corpus-wide links retargeted; `noether-braid.md` family map and reading order updated to spindle-first.

## Executed — Phase 2 (2026-07-09)

`nested-shell-braid-dynamics.md` dissolved three ways and deleted:

- Family-general machinery → `braid-mathematics.md`: Substrate and Effective Levels, Speed Hierarchy + Transverse Causal Budget Lemma (slug preserved for its 3 consumers), Spiral-Helical Motion Picture, Mass Thesis, Hinge Equation Sketch, Acceleration-Gradient Branch Comparison — lightly reworded family-generally, Document Role updated.
- Strong-field endpoint → new `content/markdown/aaa/proof-programs/terminal-alignment.md`: Braid Symmetry-Breaking Point, Local Black-Hole Duality Target, the full Terminal Alignment Label-Count block, and the Regime Map. Anchors preserved (`#terminal-alignment-label-count-target` for `entropy.md`; the common-center stationarity obstruction lemma promoted from bold paragraph to a real heading so `master-equation.md`'s anchor resolves). Preamble declares the nested-realization provenance and the open re-hosting obligation onto spindle terminal behavior.
- Nested-specific mechanism scaffold (certificate target, layer locking, planar chart, translating response + cycle stretch + oblation, retuning closure, export boundary, roadmap/hypotheses, geometry focus, minimal models, diagnostics, routed extensions) → `explored-braid-geometries.md` under the `### Nested Shell Braid Dynamics` umbrella (slug `#nested-shell-braid-dynamics` hosts all retargeted plain links; `#cadence-scale-retuning-closure` preserved for `noether-sea.md` and the retuning toy model).

`nested-shell-braid-geometry.md` re-scoped and renamed to `braid-envelope-geometry.md` (family-general): preamble and Document Role restated for any braid assembly; new `## Envelope Forms` section hosting the spindle's fusiform envelope alongside the nested family's oblate spheroidal form (kept as its own subsection); canonical variables $(\xi,\lambda)$ declared envelope-general; retuning projection marked as the nested instantiation with the spindle projection named as an open obligation. All heavy anchors preserved (`#assembly-noether-sea-interface-diagnostic` ×7, `#canonical-geometry-variables` ×4, `#dynamic-exclusion-envelope`); corpus-wide rename retarget applied (incl. entourage canon path mentions).

Tables and reading order in `noether-braid.md` and the taxonomy updated (Braid Envelope Geometry and Terminal Alignment rows). Fragment audit across all five destinations: clean. `validate-content --check`: zero source-level errors; remaining errors are generated reading copies + index drift only.

## Remaining Obligations

- Regeneration (branch/PR process): `node scripts/validate-content.mjs --write` → `node scripts/build-scene-graph.mjs --write --strict` → `node scripts/build-textbook-md-pdf.mjs --write` → re-run `--check` mirrors.
- Re-host the terminal-alignment program onto spindle terminal behavior once the leading-candidate track reaches strong-field work (declared in the proof program's preamble).
- Spindle envelope projection (fusiform $(\xi,\lambda)$ retuning projection) — named open obligation in `braid-envelope-geometry.md`.
- On a retained spindle realization: re-host the generation and dressing hypotheses per their stated portability. On decisive rejection: re-scope the collection to absorb the spindle record instead.
- Follow-on candidate (staged, not executed): the ~600-line Assembly-Noether Sea Interface Diagnostic inside `braid-envelope-geometry.md` was already flagged by the braid-retained-branch-closure lane as a relocation/condensation candidate.
