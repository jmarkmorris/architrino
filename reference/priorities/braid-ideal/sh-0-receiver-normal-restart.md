# SH-0 Receiver-Normal Restart Audit

Status: priority evidence audit, 2026-07-04.

Scope: isolated `SH-0`, the held-release one-band shell fixture with six architrinos on the common shell, diametrically opposed binaries, and the axis-neutral angular-momentum frame. This file re-audits the retained-branch evidence under the receiver-normal standard. It does not rewrite [SH-0 Retained-History Evidence Audit](sh-0-retained-history-evidence-audit.md).

## Verdict

`SH-0` must restart for retained-branch, force/action, return-response, stability, Noether sea response, branch-chart, moving-certificate, observer-export, and score-movement claims.

The geometry/support part survives only as diagnostic geometry: the fixture is still useful as a common-sphere, center-zero, antipodal-pair, axis-neutral six-point seed in the `SH-0` proof-map class. It does not supply accepted retained force/action evidence.

No accepted same-record receiver-normal row was found for the held-release fixture. The current closest live route is still source-acquisition blocked before receiver-normal branch strength can count: the provider-backed seed-path artifact needs a non-circular accepted seed-path certificate, then a matching external accepted-authority package, then repo authorization, before the retained-source adapter can accept path-history, root-ledger, causal replay, action, wake, and provenance refs on one retained record.

## Receiver-Normal Standard Applied Here

Retained force/action evidence must use current receiver-normal accounting on the same retained record. A retained root-detail row must carry, at minimum:

- `retained_record_id` and `source_row_id`;
- root identity and residual fields;
- `jacobian` for root transversality diagnostics;
- `sourceNormalDenominator`;
- `receiverNormalFactor`;
- `branchWeight`, the receiver-normal branch strength corresponding to $W^{\mathrm{rec}}=\lvert D_t/D_s\rvert$;
- the same path-history, action, wake, and provider-provenance evidence chain needed by the retained-source adapter.

Historical source-normal-only, Jacobian-only, or eta/J quotient weights remain diagnostic. `J` or `D_s` can support root transversality, root-floor, and conditioning statements. They cannot authorize retained force/action closure.

## Evidence Classification

| Evidence or claim | Current classification | Reason |
| --- | --- | --- |
| `SH-0` support taxonomy: one controlled radial band/common shell at rest | geometry-only | The proof-map classification survives as the correct support family for the held-release fixture. It is not a retained-branch result. |
| Six-point held-release placement: three positive sites on one triangular face and three negative sites on the opposite face | geometry-only | This establishes the axis-neutral fixture identity and the common-sphere support setup. It does not bind force/action rows. |
| Center-zero, common-sphere, common-speed, and antipodal-pair symmetry under declared symmetry assumptions | geometry-only | These are useful invariant-manifold diagnostics for the seed. They do not prove retained branch dynamics. |
| Single compression-to-expansion turn in the held-release toy | diagnostic-only | The toy shows why the fixture is worth testing, but it has no accepted retained record, no accepted path-history stream, no accepted action row, and no accepted receiver-normal branch-strength row. |
| `held-release-causal-wake-toy.mjs` branch-weight calculation | diagnostic-only | The toy computes `sourceJacobian`, `receiverNormalFactor`, and `branchWeight`, but it is not central-solver retained-history evidence and is not same-record accepted evidence. |
| Rigid fixed-speed octahedral root-ledger and no-go diagnostics | diagnostic-only | Root-ledger diagnostics can report root access, small Jacobian conditions, or transversality. They cannot be consumed as retained force/action closure. |
| Default `held_release_seed_path_rows:12dbf261ff617926` | restart-required for retained evidence | The default artifact fails first at `held_release_seed_path_rows[*].retained_record_id`. |
| Provider-backed `held_release_seed_path_rows:5833f18e53586201` | restart-required for retained evidence | It binds retained record, source row, provider object, and provider hash, but it remains unaccepted at `held_release_seed_path_rows_acceptance_certificate` / `held_release_seed_path_rows.acceptance_certificate_ref`. |
| Local stream manifests and candidate durable native path-history refs | diagnostic-only | They are request/contract evidence until accepted same-record manifest refs and package provenance exist. |
| Candidate native root-ledger detail refs in `central_solver_retained_history_provider_source_carrier.v0` | diagnostic-only | They are candidate refs only. They do not expose accepted same-record root-detail rows with receiver-normal branch strength. |
| Internal tangent-authority bridge root-detail fixtures with `branchWeight` | diagnostic-only | The bridge and tests prove the checker can require `branchWeight`, `sourceNormalDenominator`, and `receiverNormalFactor`, but the rows are synthetic/non-authorizing and are not accepted `SH-0` held-release rows. |
| Same-record action-closure, retained wake-history, and provider-provenance refs in the current adapter route | restart-required for retained evidence | The carrier exposes candidate refs, but the accepted-evidence package reports zero accepted package fields. |
| Retained branch, return response, stability row, Noether sea response, branch chart, moving certificate, observer export | restart-required | Each downstream claim waits on one accepted retained record carrying seed-path, path-history, root-detail receiver-normal, action, wake, and provenance evidence. |

## Current Search Result

The search found no accepted same-record `branchWeight` / receiver-normal rows for the held-release fixture.

Found but non-authorizing:

- `scripts/braid-ideal/held-release-causal-wake-toy.mjs` computes `branchWeight` from a toy causal root calculation. It is diagnostic only.
- `scripts/braid-ideal/central-solver-internal-tangent-authority-vector-rows.mjs` requires `branchWeight`, `sourceNormalDenominator`, and `receiverNormalFactor` on same-record retained-root detail rows. The live CLI run emitted zero such rows for `SH-0`; the tests supply synthetic internal-tangent fixtures and still keep `accepted=false`.
- `scripts/braid-ideal/central-solver-retained-history-provider-source-carrier.mjs` binds the retained record and source row, but only emits candidate native path-history and root-ledger refs. Its authorization block keeps `receiver_normal_branch_strength=false`.
- `scripts/check-receiver-normal-clean-slate.mjs` passed, so no stale eta/J shortcut is currently accepted by the clean-slate guard.

## First Missing Accepted Source Object

The exact first missing accepted source object remains:

- object: `held_release_seed_path_rows_acceptance_certificate.v0`;
- field: `held_release_seed_path_rows.acceptance_certificate_ref`;
- provider-backed artifact: `held_release_seed_path_rows:5833f18e53586201`;
- retained record: `retained-record:held-release-six-point:adapter-acceptance-certificate`;
- source row: `two-speed-preferred-row:u0.8:v0.2`;
- provider object: `candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327`;
- provider hash: `7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df`;
- required accepted-ref prefix: `accepted:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2:`.

The current verifier then requires the matching `held_release_seed_path_rows_external_accepted_authority_package.v0` at:

- `external-authority-package:held-release-seed-path-rows:retained-record:held-release-six-point:adapter-acceptance-certificate:two-speed-preferred-row:u0.8:v0.2`.

After a matching certificate and package, the remaining repo-local authorization blocker is still:

- object: `repo_authorization_for_accepted_held_release_seed_path_rows`;
- field: `held_release_seed_path_rows.acceptance_certificate_ref`.

## Smallest Producer/Test Boundary

No current script produces the accepted seed-path certificate. The current smallest verifier is:

```bash
node scripts/braid-ideal/held-release-seed-path-rows.mjs --retained-record-id=retained-record:held-release-six-point:adapter-acceptance-certificate --source-row-id=two-speed-preferred-row:u0.8:v0.2 --provider-object-ref=candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327 --provider-artifact-hash=7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df --print-acceptance-certificate-requirement --pretty
```

The smallest missing producer should be a narrowly scoped seed-path certificate issuer, either as a new script such as `scripts/braid-ideal/held-release-seed-path-rows-acceptance-certificate.mjs` or as an explicit producer mode added to `scripts/braid-ideal/held-release-seed-path-rows.mjs`. Its first test should live in `tests/braid-ideal-held-release-seed-path-rows.test.js` and prove that the produced object:

1. uses schema `held_release_seed_path_rows_acceptance_certificate.v0`;
2. binds the exact retained record, source row, provider object, provider hash, six row ids, and six row hashes above;
3. carries the required accepted-ref prefix;
4. carries or verifies the matching `held_release_seed_path_rows_external_accepted_authority_package.v0`;
5. preserves all negative-control rejections;
6. still fails closed until repo authorization is present at `held_release_seed_path_rows.acceptance_certificate_ref`.

Only after that certificate boundary closes should the retained-source adapter package be treated as the next producer boundary for receiver-normal branch strength. Its first accepted receiver-normal row must replace candidate native root-ledger refs with accepted same-record root-detail evidence carrying `branchWeight`, `sourceNormalDenominator`, and `receiverNormalFactor` on the same retained record and source row.

## Replacement Route

1. Produce the non-circular `held_release_seed_path_rows_acceptance_certificate.v0`.
2. Produce the matching `held_release_seed_path_rows_external_accepted_authority_package.v0`.
3. Clear repo authorization at `held_release_seed_path_rows.acceptance_certificate_ref`.
4. Populate `central_solver_retained_source_adapter_same_record_accepted_evidence_package.v0` with accepted replacements for the ten package fields: seed-path certificate, path-history manifest certificate, central retained-history row certificate, provider-object certificate, native path-history stream manifest set, native root-ledger detail rows, causal-root replay rows, same-record action closure, retained wake history, and provider provenance.
5. Require the accepted native root-ledger detail rows to carry same-record receiver-normal branch strength: `branchWeight`, `sourceNormalDenominator`, and `receiverNormalFactor`, with `jacobian` retained as a transversality diagnostic.
6. Only then revisit retained branch, return response, stability, Noether sea response, branch chart, moving certificate, and observer export.

## Promotion Decision

- Corpus promotion: defer with blocker.
- Priority status: restart-required for all retained-branch claims; geometry/support survives as diagnostic-only priority material.
- Proof-map impact: the current proof-map wording already matches this status by keeping `SH-0` as started but non-promoted diagnostic row evidence.
- Next durable capture: update this file or the existing evidence audit only after an accepted seed-path certificate exists or after a new accepted receiver-normal root-detail row is produced on the same retained record.
