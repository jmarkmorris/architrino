# Shell-Braid Run Matrix

Status: priority run matrix, 2026-07-04.

Scope: consolidated diagnostic/candidate run matrix for shell-braid Proof IDs in the braid-ideal lane. This file tracks controlled run variations under Proof IDs such as `SH-0`, `SH-0-sea`, and `SH-L`; it does not create accepted retained evidence, change proof status, or promote any branch.

## Matrix Granularity Decision

Use one consolidated shell-braid run matrix while the rows share the same source-object and retained-evidence blockers.

Use per-ID sections inside this file:

- `SH-0` for isolated one-band/common-shell rest target runs;
- `SH-0-sea` for the same central target embedded in surrounding like-braid Noether sea rows;
- `SH-L` for moving observer-export rows after a retained shell branch exists.

Split a section into its own matrix only when it gets a distinct runner, accepted-source object, validator, or evidence package large enough that the consolidated matrix stops being readable.

## ID Rule

Proof IDs name branch targets. Run handles name controlled variations.

Do not mint a new Proof ID for every surface-speed, prehistory, or release variation. A run handle should travel with the runner output, source artifact hash, retained record, source row, and evidence status. For example, `SH-0` remains the Proof ID, while `sh0-g0-vt080-moving-prehistory` is a diagnostic run handle under that Proof ID.

All rows below are priority-level diagnostic/candidate targets unless they later bind to accepted source rows, same-record receiver-normal root-detail rows, action/wake/event/support rows, and any required Noether sea response rows.

## Shared Conventions

| Field | Meaning |
| --- | --- |
| Proof ID | Branch target or downstream consumer being exercised. |
| Run handle | Stable local run/evidence handle, not a Proof ID. |
| Group velocity | Target-center group velocity for the diagnostic run. Existing candidate source rows may carry raw replay drift; record that separately in the runner output. |
| Surface speed | Tangential speed magnitude on the common shell, reported as a fraction of field speed `$c_f$`. |
| Prehistory mode | How the retained-history window is primed before the release or measurement. |
| Evidence status | Current allowed claim level for the row. |

The held-release rows are history-primed: the source history is generated during a hold interval, then the release or diagnostic perturbation is measured. A `kick-at-release` row intentionally changes velocity after the held history has been primed, so it tests the velocity factor as a diagnostic mismatch. A `moving-prehistory` row primes the path history with the same transverse surface motion it uses at release, so it is the history-consistent version if transverse motion survives the first screen.

## SH-0 Isolated Rest Target

`SH-0` is the isolated one-band/common-shell rest target: six architrinos on the shell, diametrically opposed binaries, and the axis-neutral angular-momentum frame. These rows test whether group-zero surface motion changes the isolated diagnostic behavior. They do not claim retained closure.

Capped axis-neutral sweep executed 2026-07-07 with [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) at toy defaults (duration 3, dt 0.002, hold 4, $c_f=1$, coupling 1, spin axis $(1,1,1)/\sqrt{3}$). Outcome across all ten declared nonzero rows: no second turn, no bounded window, causal-root coverage clean (zero missing roots), $\operatorname{Fix}(G)$-drift residual bounded at $\le 5\times10^{-15}$, and every row fail-closed. The compression-first versus outward-only boundary sits between `vt050` and `vt080`: at release the geometric outward rate $v_t^2/R$ passes the net inward radial release acceleration ($\approx 0.25$ on the unit fixture, so marginal at $f_v\approx0.5$). Post-first-turn inward reduced-radius acceleration rows appear at `vt025`/`vt050` but also appear in the `vt000` zero-angular-momentum control and occur after the field-speed crossing, so they do not name a candidate row under the capped-witness discipline. `moving-prehistory` released with zero position/velocity discontinuity in every row.

| Run handle | Group velocity | Surface speed | Prehistory mode | Purpose | Evidence status |
| --- | --- | ---: | --- | --- | --- |
| `sh0-g0-vt000-held-release` | `0` | `0.00 c_f` | stationary held release | Baseline target identity and restart control for the provider-backed seed-path route. | diagnostic/candidate; retained evidence blocked at `held_release_seed_path_rows_acceptance_certificate.v0`. Toy control 2026-07-07: inward>outward, first turn `t=1.08`, no second turn, field-speed crossing `t=0.73`, drift `2.1e-15`. |
| `sh0-g0-vt025-kick-at-release` | `0` | `0.25 c_f` | stationary hold, transverse kick at release | Low transverse-speed diagnostic isolating velocity-factor sensitivity from history priming. | diagnostic only; executed 2026-07-07: inward>outward, first turn `t=1.028`, no second turn, roots clean, field-speed crossing `t=0.71`, drift `2.0e-15`. |
| `sh0-g0-vt050-kick-at-release` | `0` | `0.50 c_f` | stationary hold, transverse kick at release | Mid transverse-speed kick screen. | diagnostic only; executed 2026-07-07: marginal outward>inward>outward, first turn `t=0.904`, no second turn, roots clean, field-speed crossing `t=0.652`, drift `2.7e-15`. |
| `sh0-g0-vt080-kick-at-release` | `0` | `0.80 c_f` | stationary hold, transverse kick at release | High but sub-field transverse-speed kick screen. | diagnostic only; executed 2026-07-07: outward-only, never compresses (min radius `1.0`), roots clean, field-speed crossing `t=0.502`, drift `9.8e-16`. |
| `sh0-g0-vt095-kick-at-release` | `0` | `0.95 c_f` | stationary hold, transverse kick at release | Near-field-speed kick screen for root-margin and return-response sensitivity. | diagnostic only; executed 2026-07-07: outward-only, never compresses, roots clean, field-speed crossing `t=0.29`, drift `1.8e-15`. |
| `sh0-g0-vt099-kick-at-release` | `0` | `0.99 c_f` | stationary hold, transverse kick at release | Near-edge kick screen before the exact field-speed limit. | diagnostic only; executed 2026-07-07: outward-only, never compresses, roots clean, field-speed crossing `t=0.14`, drift `5.0e-15`. |
| `sh0-g0-vt100-kick-at-release` | `0` | `1.00 c_f` | stationary hold, transverse kick at release | Exact field-speed surface-motion edge diagnostic; expect causal-root margin risk. | diagnostic edge case only; outside the capped ten-row sweep. |
| `sh0-g0-vt025-moving-prehistory` | `0` | `0.25 c_f` | transverse moving prehistory | Low transverse-speed history-consistent release. | diagnostic/candidate only; executed 2026-07-07: inward>outward, first turn `t=1.048`, no second turn, roots clean, field-speed crossing `t=0.708`, drift `1.5e-15`, release continuity exact. |
| `sh0-g0-vt050-moving-prehistory` | `0` | `0.50 c_f` | transverse moving prehistory | Mid transverse-speed history-consistent release. | diagnostic/candidate only; executed 2026-07-07: outward>inward>outward, first turn `t=0.926`, no second turn, roots clean, field-speed crossing `t=0.642`, drift `1.7e-15`, release continuity exact. |
| `sh0-g0-vt080-moving-prehistory` | `0` | `0.80 c_f` | transverse moving prehistory | High sub-field history-consistent release and first likely comparison to the current `u0.8:v0.2` source-row route. | diagnostic/candidate only; executed 2026-07-07: outward-only, never compresses, roots clean, field-speed crossing `t=0.536`, drift `2.4e-15`, release continuity exact. |
| `sh0-g0-vt095-moving-prehistory` | `0` | `0.95 c_f` | transverse moving prehistory | Near-field-speed history-consistent release. | diagnostic/candidate only; executed 2026-07-07: outward-only, never compresses, roots clean, field-speed crossing `t=0.492`, drift `3.2e-15`, release continuity exact. |
| `sh0-g0-vt099-moving-prehistory` | `0` | `0.99 c_f` | transverse moving prehistory | Near-edge history-consistent release before the exact limit. | diagnostic/candidate only; executed 2026-07-07: outward-only, never compresses, roots clean, field-speed crossing `t=0.484`, drift `3.9e-15`, release continuity exact. |
| `sh0-g0-vt100-moving-prehistory` | `0` | `1.00 c_f` | transverse moving prehistory | Exact field-speed history-consistent surface-motion limit. | diagnostic edge case only; outside the capped ten-row sweep; do not treat as accepted retained evidence. |

## SH-0-Sea Embedded Target

`SH-0-sea` inherits the central `SH-0` target identity, then adds surrounding like-braid Noether sea rows, a local target-sea frame, boundary-condition rows, candidate sea-response rows, support/envelope variables, action/exchange variables, and receiver-normal evidence requirements.

The sea rows should consume the `SH-0` run handle they embed. They remain diagnostic/candidate until the central target/source record is accepted and same-record receiver-normal accounting exists.

Computed dipole wake sum executed 2026-07-07 with [sh-0-sea-diagnostic-candidate-model.mjs](../../../scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs) `--wake-sum-run`. The fitted response amplitude is removed from the script; the sea response is now the master-equation-kernel delayed sum over the 12 FCC nearest-neighbor braids with declared held histories (aligned orientation, static over the declared window, signed-polarity dipole $2(1,1,1)$ per braid, kernel constants identical to the escape-floor toy: coupling $1$, softening $0.05$, $c_f=1$, branch weight $1$ for held static sources), zero free amplitude anywhere in the output path. Across the declared range $a_{\mathrm{FCC}}\in[3,12]$ (step $0.25$, held-history window $24$, all $432$ directed roots covered, field speed clean at every spacing), the projected response $\Pi_R\mathcal A^{\mathrm{sea}}$ is inward at every spacing and crosses the same-record escape floor $-0.0934863484737535$ for $a_{\mathrm{FCC}}\le5.3469$: a computed retention window $a_{\mathrm{FCC}}\in[3,5.34690143]$ exists (lower edge set by the declared range minimum above the shell-overlap constraint $2\sqrt2$, upper edge a computed floor crossing). Sample rows: $\Pi_R=-1.0814$ at $a_{\mathrm{FCC}}=3$, $-0.3712$ at $4$, $-0.1025$ at $5.25$, $-0.0811$ at $5.5$, $-0.0519$ at $6$; far-field decay is approximately $a_{\mathrm{FCC}}^{-5}$ (observed). Named sea-spacing candidate for downstream same-record work: `sh0sea-aa-fcc-dipole-wake-sum:a-fcc-4.25` ($\Pi_R=-0.2833$, inward margin $0.1899$ below the required floor). All outputs fail closed at the central seed-path certificate; no retained `SH-0-sea` branch is claimed.

Delayed-echo variant executed 2026-07-07 with `--wake-sum-run --neighbor-motion=breathing|orbiting`: the 12 neighbor braids carry declared moving held histories (common phase, causal roots solved per directed pair by bisection against the declared window; branch weight $|1/\max(J,0.05)|$ per the toy kernel; motion parameters are declared history parameters, not response amplitudes — zero free amplitude preserved). The H5 phase-dependence prediction is confirmed at the declared level, splitting the static boundary $5.3469$ into a guaranteed (all-phase) and a phase-conditional boundary. Breathing ($\delta=0.2$, 16 phases): $\Omega=1$ gives $[3,5.022]$ guaranteed versus $5.908$ phase-conditional; $\Omega=2$ gives $4.948$ versus $6.253$; $\Omega=4$ (rim speed $0.8\,c_f$) gives $3.512$ guaranteed while the phase-conditional window extends past the entire declared range, and at the named candidate spacing $4.25$ the all-phase crossing fails while the some-phase crossing holds (phase spread $1.33$ versus static $|\Pi_R|=0.283$, including sign reversal). Orbiting about $\hat z$ ($\omega\in\{0.25,0.5,0.75\}$): the guaranteed window does not exist at any tested $\omega$ — the phase-averaged response at $4.25$ collapses to $-0.0944$ (rotating braid dipole) — while the phase-conditional boundary grows to $5.946/6.869/8.223$. Reading (diagnostic-only): retention supplied by the sea is phase- and orientation-dependent; dipole-aligned (static or breathing) neighbor order gives a guaranteed window, while freely reorienting neighbors give only phase-conditional retention, so sea orientational order becomes a named condition for the environment-theorem route. Super-field-speed motion rows are excluded fail-closed. The runs also emit `sh_0_sea_candidate_same_record_request.v0`, feeding the named $a_{\mathrm{FCC}}=4.25$ candidate (identity, kernel, held-history/motion declarations, target binding) into the same-record chain, fail-closed at the seed-path certificate.

Orientational-order and stochastic-retention analysis executed 2026-07-07 with [sh-0-sea-orientation-order-diagnostic.mjs](../../../scripts/braid-ideal/sh-0-sea-orientation-order-diagnostic.mjs) over the declared ensembles (aligned, conjugate-aligned control, paired-antiphase, seeded isotropic disorder). Exact results: the aligned point-dipole shell sum cancels ($6$ bonds at $\cos^2\theta=2/3$ against $6$ at $\cos\theta=0$), the paired-antiphase ensemble supplies $\Pi_R=0$ exactly, and conjugating all neighbors flips $\Pi_R$ exactly (conjugate-aligned is exactly anti-retentive). Computed results at $a_{\mathrm{FCC}}=4.25$: nearest-neighbor bond energies are frustrated ($-0.433$ aligned-preferring versus $+0.577$ flip-preferring classes), the retention-favorable aligned relative orientation is energetically disfavored ($+0.864$ versus $-0.864$ center-shell), and sampled non-uniform configurations undercut every uniform ensemble energy. Stochastic retention fails both readings: the disordered ensemble mean response is consistent with zero (no annealed window anywhere) and the quenched crossing probability peaks at $\approx0.43$ and stays below one half at every declared spacing (no majority window). Verdict: the sea does not supply the aligned order that guaranteed retention requires through static pair energetics, and disordered stochastic retention cannot replace it; the named remaining environment mechanism is induced sea orientational polarization (neighbor orientations responding to the central braid wake), which needs a responsive-orientation diagnostic beyond the fixed-source model. All outputs fail-closed; derivation recorded in the [model packet](sh-0-sea-diagnostic-candidate-model.md#orientational-order-condition-and-stochastic-retention).

Induced sea orientational polarization executed 2026-07-07 with [sh-0-sea-induced-polarization-diagnostic.mjs](../../../scripts/braid-ideal/sh-0-sea-induced-polarization-diagnostic.mjs) (`sh_0_sea_induced_polarization_diagnostic.v0`; recorded run `--samples=6 --iteration-cap=2000 --convergence-tolerance=1e-7`). Each of the 12 neighbor braids carries one declared $SO(3)$ orientation on the held six-site decoration (dipole reversal reachable within $SO(3)$, no conjugation variable), evolving by overdamped gradient relaxation (declared torque descent $\tau_k=\sum_{i\in k}(x_i-X_k)\times F_i$) on the total potential-superposition energy of the 13-braid cluster — central-shell plus shell-shell bonds, central braid held fixed as the `SH-0` identity, kernel constants identical to the wake-sum runs; the relaxation step, iteration cap, and convergence tolerance are declared integration parameters, so `free_amplitude_parameter_count=0` throughout. Exact controls (test-pinned): conjugation antisymmetry (all-neighbor conjugation flips $\Pi_R$ to $\le3\times10^{-16}$), paired-antiphase null in both the charge-conjugation and the $SO(3)$ $180^\circ$-rotation representations, and paired-antiphase as an exact response null that is not an energy fixed point (nonzero torque $\approx1.47$ at $4.25$), so relaxation escapes it. Signed result: relaxing from aligned, paired-antiphase, and every seeded disordered start, the relaxed radial response is outward ($\Pi_R>0$) at every declared spacing $a_{\mathrm{FCC}}\in\{3,3.5,4,4.25,4.5,5,5.25\}$, reversing the sign of the assumed-aligned inward reference and clustering near the anti-retentive conjugate limit; at $4.25$ the relaxed aligned $\Pi_R=+0.2837$ and the disordered mean $\Pi_R=+0.2934$ ($0/6$ crossings), against the assumed-aligned reference $-0.2833$. No start crosses the inward escape floor at any spacing (the lone $a_{\mathrm{FCC}}=3.0$ inward seed is one of six near-overlap samples against an outward mean $+0.587$). Frustration keeps the literal dipole order parameter small and mildly positive ($O\approx0.12$-$0.23$; the FCC dipole-shell sum cancels, so response is a higher-multipole effect the order parameter only weakly tracks) while the response is robustly anti-retentive. Disposition `induced_polarization_relaxes_anti_retentive_no_bounded_window`: the central braid energetically prefers dipole-reversed neighbors ($+0.864$ aligned versus $-0.864$ conjugate at $4.25$), and overdamped relaxation realizes that preference in response space, so the fixed-geometry sea polarizes to expel rather than retain. The static induced-polarization retention route is falsified at the diagnostic level; combined with the frustration, paired-antiphase null, and stochastic-disorder results, no static or quasi-static sea mechanism supplies the required central-aligned order, so the aligned-order caveat on all sea-screened rows hardens to an open formation-history / dynamic-alignment burden and the mechanism hunt sharpens toward the hinge-click absorber route (`self_hit_held_release_solver_row`). All outputs fail-closed; derivation recorded in the [model packet](sh-0-sea-diagnostic-candidate-model.md#induced-sea-orientational-polarization---2026-07-07).

Sea-screened held-release rows executed 2026-07-07 with [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) `--fcc-sea-spacing 4.25` (the named sea-spacing candidate; toy defaults otherwise, held window $24$, $72$ held static sea sources with exact static causal roots and receiver-normal branch weights, one-way environment by declared held histories — the dipole-aligned neighbor-order case of the delayed-echo variant above, where the guaranteed window holds). Release-instant cross-check: the toy's release sea radial projection equals the computed wake-sum row $-0.283341788903118$ to $13$ digits in every row; sea root coverage is complete (zero held-window misses); $\operatorname{Fix}(G)$-drift stays at runner roundoff ($\le2.5\times10^{-12}$). Headline: the three previously outward-only rows (`vt080`/`vt095`/`vt099` moving-prehistory) now show a return turn (expansion-to-compression) at $t=0.176/0.346/0.390$, at $R\approx1.001$-$1.013$, before the first recorded field-speed crossing ($t=0.466/0.430/0.422$) — the program's first sub-field return turns, defeating the prior discount that all inward post-turn rows sat after the crossing. No bounded window: every row subsequently compresses or hovers, crosses field speed while the radius barely moves (radial support without a tangential absorber), and escapes through the shell (site-to-sea-site passes down to $0.038$; `vt095` extended to duration $6$ confirms escape, $R=8.7$ rising). Compression-first rows (`vt000` through `vt050`) turn earlier than isolated (for example `vt000` first turn $t=0.958$ versus $1.08$) with no second turn. Escape-certificate checker consumption ([delayed-escape-certificate-check.mjs](../../../scripts/braid-ideal/delayed-escape-certificate-check.mjs)) on all eight rows: fail-closed everywhere; `vt080`/`vt095`/`vt099` carry $8$-$19$ signed admissible certificate times with margins failing at $-468$ to $-489$ (unit coupling); ordering witness consistent in every row; the toy output carries the explicit caveat that the certificate envelope bounds partner sources only, so sea-row margins are diagnostic, not lemma-backed.

Sea-screened self-hit rows executed 2026-07-07 with `--fcc-sea-spacing 4.25 --include-self-hits` (toy defaults otherwise; self-hit minimum delay $=dt$): run handles `sh0sea-a4.25-selfhit-vt080-moving-prehistory`, `sh0sea-a4.25-selfhit-vt095-moving-prehistory`, `sh0sea-a4.25-selfhit-vt099-moving-prehistory`, plus the `vt000` control. Same-source roots are absent sub-field as the causal residual requires (frame-level metrics bit-identical to the self-hit-disabled baselines up to the crossing frame; first divergence exactly at the crossing: $t=0.48/0.44/0.44$ at frame resolution); the return turns at $t=0.176/0.346/0.390$ are unchanged. Post-hinge the self-hit channel amplifies the runaway instead of absorbing it: the same-source polarity product is $+1$, so every self-hit repels the receiver from its own past position — final field-speed ratios $12.7/12.4/12.3$ versus $2.5/2.3/2.5$ without self-hits at duration $3$, final $R\approx32$ versus $\approx4$, max branch weight $164$ versus $25$. Claim level per the packet's [Self-Hit Probe Inside the Sea Shell](sh-0-sea-diagnostic-candidate-model.md#self-hit-probe-inside-the-sea-shell---2026-07-07): the ejection magnitude is regularization-dependent, so these rows witness the uncontrolled fold crossing, not the controlled click — the naive self-hit kernel is uniformly ejective and the toy cannot decide the click-absorber question. The owner row [self-hit-held-release-solver-row.mjs](../../../scripts/braid-ideal/self-hit-held-release-solver-row.mjs) consumes these comparisons as a diagnostic witness (`naive_self_hit_kernel_uniformly_ejective_toy_cannot_decide_controlled_click`) and binds the named candidate request (`candidate_request_bound_not_accepted`); everything remains fail-closed at `central_solver_retained_history_row`, no retained branch, no score movement.

| Run handle | Embedded central run | Group velocity | Surface speed | Prehistory mode | Purpose | Evidence status |
| --- | --- | --- | ---: | --- | --- | --- |
| `sh0sea-g0-vt000-held-release` | `sh0-g0-vt000-held-release` | `0` | `0.00 c_f` | stationary held release | First candidate Noether sea stabilization row around the central target. | diagnostic/candidate; executed 2026-07-07 at $a_{\mathrm{FCC}}=4.25$: first turn `t=0.958` (isolated `1.08`), no second turn, field-speed crossing `t=0.628`, escape through shell; blocked at the central seed-path certificate. |
| `sh0sea-g0-vt025-kick-at-release` | `sh0-g0-vt025-kick-at-release` | `0` | `0.25 c_f` | stationary hold, transverse kick at release | Low-speed kick screen in the sea environment. | diagnostic only; executed 2026-07-07: first turn `t=0.908`, no second turn, crossing `t=0.608`. |
| `sh0sea-g0-vt050-kick-at-release` | `sh0-g0-vt050-kick-at-release` | `0` | `0.50 c_f` | stationary hold, transverse kick at release | Medium-response check for a surface-kicked central target. | diagnostic only; executed 2026-07-07: first turn `t=0.816`, no second turn, crossing `t=0.552`. |
| `sh0sea-g0-vt080-kick-at-release` | `sh0-g0-vt080-kick-at-release` | `0` | `0.80 c_f` | stationary hold, transverse kick at release | High sub-field response check for velocity-factor sensitivity in the sea environment. | diagnostic only; declared, not yet executed. |
| `sh0sea-g0-vt025-moving-prehistory` | `sh0-g0-vt025-moving-prehistory` | `0` | `0.25 c_f` | transverse moving prehistory | Low-speed history-consistent sea check. | diagnostic/candidate only; executed 2026-07-07: first turn `t=0.920`, no second turn, crossing `t=0.606`. |
| `sh0sea-g0-vt050-moving-prehistory` | `sh0-g0-vt050-moving-prehistory` | `0` | `0.50 c_f` | transverse moving prehistory | Mid-speed history-consistent sea check. | diagnostic/candidate only; executed 2026-07-07: first turn `t=0.828`, no second turn, crossing `t=0.550`. |
| `sh0sea-g0-vt080-moving-prehistory` | `sh0-g0-vt080-moving-prehistory` | `0` | `0.80 c_f` | transverse moving prehistory | First history-consistent moving-surface sea stabilization check. | diagnostic/candidate only; executed 2026-07-07: **sub-field return turn** `t=0.176` at `R=1.0010` before crossing `t=0.466`; then compression, crossing, escape; checker fail-closed (signed margin $-489$). |
| `sh0sea-g0-vt095-moving-prehistory` | `sh0-g0-vt095-moving-prehistory` | `0` | `0.95 c_f` | transverse moving prehistory | Near-field-speed sea stabilization check. | diagnostic/candidate only; executed 2026-07-07: **sub-field return turn** `t=0.346` at `R=1.0088` before crossing `t=0.430`; shallow hover near `R=1` to `t=0.61`, then escape (confirmed to duration 6); checker fail-closed (signed margin $-468$). |
| `sh0sea-g0-vt099-moving-prehistory` | `sh0-g0-vt099-moving-prehistory` | `0` | `0.99 c_f` | transverse moving prehistory | Near-edge history-consistent sea check. | diagnostic/candidate only; executed 2026-07-07: **sub-field return turn** `t=0.390` at `R=1.0127` before crossing `t=0.422`; then escape; checker fail-closed (signed margin $-478$). |
| `sh0sea-g0-vt100-moving-prehistory` | `sh0-g0-vt100-moving-prehistory` | `0` | `1.00 c_f` | transverse moving prehistory | Exact field-speed embedded edge diagnostic. | diagnostic edge case only; outside the executed set. |

Self-hit probe inside the sea shell executed 2026-07-07 (`--fcc-sea-spacing 4.25 --include-self-hits`; rows `vt000`, `vt080`/`vt095`/`vt099` moving-prehistory, plus a no-sea `vt095` isolation control and regularization variants; details in the [SH-0-sea packet](sh-0-sea-diagnostic-candidate-model.md#self-hit-probe-inside-the-sea-shell---2026-07-07)). Hinge timing exact: the new `firstSelfHitRoot` event coincides with the first recorded field-speed crossing in every row, and sub-field trajectories are unchanged (the sea-screened return turns are self-hit-robust by construction). The naive self-hit kernel ejects rather than absorbs: an impulsive same-source kick at the crossing (branch weights to $163$; speeds jump to $\approx12\,c_f$ within $0.04$ time units; final $R\approx32$; $\approx33$k missing partner roots), with magnitude dependent on the Jacobian floor and self-hit minimum delay (final $v_{\max}$ $12.4/9.8/10.6\,c_f$ across tested regularizations) — a resolution-limited caustic artifact, not a controlled click. Disposition `naive_self_hit_kernel_ejects_at_hinge_no_absorption_in_toy`; checker consumption fail-closed on all five rows with (WP) correctly reported lost post-hinge.

Self-hit brake measured on the production central-solver path 2026-07-07 ([self-hit-brake-central-measurement.mjs](../../../scripts/braid-ideal/self-hit-brake-central-measurement.mjs), tests 6 passing; [click-impulse packet section](fold-crossing-click-impulse-packet.md#central-solver-path-measurement-2026-07-07)). Driving the production same-source causal-root runtime (`solveMovingCircularSameSourceCausalRoots` in [AbsoluteHistoryRootRuntime.mjs](../../../src/solver/app/AbsoluteHistoryRootRuntime.mjs), consumed read-only) on the `vt095` crossing reproduces $r_c=0.17662$, $D_s=D_T=0.0039177$, and the emitted signed orientation `receiverNormalFactor` $=+1.0000$ to the digit. Three producer gaps are localized on the production path: the same-source `branchWeight` exposes $\lvert m\rvert$ and discards the emitted sign; the rigid-circle same-source history reflection-locks $m=+1$ and cannot carry the pump acceleration (an accelerating reconstruction with the production receiver-normal definition gives $D_T<0<D_s$, $m<0$, absorptive); and coincidence handling is a numerical floor (`safeDistance=max(EPSILON,distance)`, `EPSILON=1e-9`), so the signed click impulse is log-divergent — absorbed fraction of the certified per-rotation pump ($22.17$) runs $0.031/0.115/0.371/2.69/9.67$ as the declared stratum $\rho_c$ runs $0.2/0.1/0.05/0.01/0.001$, flipping the beat-the-clock verdict. Disposition `central_solver_self_hit_brake_sign_decided_absorptive_magnitude_reduces_to_declared_coincidence_stratum`: sign decided absorptive, magnitude reduced to one declared coincidence-stratum length scale (the runtime's `jacobianFloorOrDeclaredStratum` input) plus two mechanical runtime extensions; a chart-clean magnitude still requires the non-coincident cross-hit hinge. Fail-closed; no retained branch; `scoreMovement=no_score_increase`. The two mechanical extensions were then landed additively in the production runtime ([AbsoluteHistoryRootRuntime.mjs](../../../src/solver/app/AbsoluteHistoryRootRuntime.mjs)): an explicit `signedBranchOrientation` on every root, and an optional `angularAcceleration` on the moving-circular history (default $0$ backward-compatible) so the same-source root returns $m<0$ from the production path; `check-solver-contract-fixtures` and the extended runtime tests pass. Only the coincidence-stratum ontology decision remains.

Sea tangential absorber decision certified 2026-07-07 ([Corollary S](delayed-escape-certificate-lemma-proof-packet.md#corollary-s---sea-screened-clock-the-aligned-fcc-sea-is-a-non-absorber); owner-script extension `certifySea` in [planar-tangential-screen-interval-certificate.mjs](../../../scripts/braid-ideal/planar-tangential-screen-interval-certificate.mjs)). The static aligned FCC shell ($a_{\mathrm{FCC}}=4.25$) placed around the axis-neutral rotating braid gives an exact along-velocity decomposition $\Pi_{\mathrm{tan}}^{\mathrm{sea}}(\beta,\varphi)=c_0(\varphi)-\beta Q(\varphi)$: $c_0$ (static orientation torque) has certified zero rotation-cycle average (odd about the $C_3$-symmetric phases; witness $\le3\times10^{-16}$), and the velocity-linear coefficient is interval-certified $\sup_\varphi\lvert Q\rvert\le0.2746<c_1=2.881$ (non-absorber margin $\ge2.606$; 4096 phase boxes; octahedral chart reproduces the recorded release-instant $\Pi_R=-0.2833417889031177$ to 12 digits). Disposition `sea_screened_clock_survives_aligned_sea_non_absorber`: the aligned FCC sea supplies inward radial support (inducing the sub-field return turns above) but removes at most $\approx10\%$ of the certified tangential pump, so the (S)-failure clock survives sea screening ($\Delta t\le1.495\,c_f\rho^2/\kappa$ versus isolated $1.353$). Order-robust (conjugate flips $Q$, paired-antiphase gives $Q\equiv0$, disorder averages to zero); the orientational-order caveat conditions only the radial retention supply. Fail-closed; no retained branch; `scoreMovement=no_score_increase`.

Dynamic induced-polarization tangential test executed 2026-07-07 ([Dynamic Induced Polarization section](sh-0-sea-diagnostic-candidate-model.md#dynamic-induced-polarization---tangential-absorber-test---2026-07-07); owner script [sh-0-sea-dynamic-polarization-drive-diagnostic.mjs](../../../scripts/braid-ideal/sh-0-sea-dynamic-polarization-drive-diagnostic.mjs)). Letting the central braid rotate over the static shell, the $C_3$ symmetry restricts the neighbor orientational drive torque to threefold harmonics ($m=3,6,9$; $m=1,2,4,5$ vanish to $3\times10^{-15}$), with the $m=3$ drive $\sum_k|\tau_k^{(3)}|^2$ rising from $0.109$ ($\beta=0.1$) to $0.529$ ($\beta=0.9$). The phase-lagged response is the first sign-definite absorptive sea tangential channel, $\Phi_{\mathrm{tan}}^{\mathrm{ind}}\approx(0.03$–$0.17)\chi''(3\omega)$, but beating the pump needs $\chi''\gtrsim8.6$–$19.2$ ($K\lesssim0.05$), a factor $5$–$15$ softer than the cluster orientational stiffness. Disposition `dynamic_induced_polarization_absorptive_but_insufficient_at_cluster_stiffness`: insufficient at cluster stiffness ($\le10\%$ of the pump), reducing the branch to the neighbor braid orientational AC susceptibility. Fail-closed; no retained branch; `scoreMovement=no_score_increase`.

## SH-L Downstream Moving Export

`SH-L` is not a surface-speed sweep of an unretained `SH-0` row. It is the downstream moving observer-export consumer after a retained `SH-0` or retained `SH-0-sea` branch exists.

| Run handle | Source branch requirement | Group velocity | Surface speed | Purpose | Evidence status |
| --- | --- | --- | ---: | --- | --- |
| `shl-from-retained-sh0` | retained `SH-0` branch | `>0` | inherited from retained branch | Test moving shell export only after isolated shell retention closes. | blocked; no retained `SH-0` branch exists. |
| `shl-from-retained-sh0sea` | retained `SH-0-sea` branch | `>0` | inherited from retained branch | Test moving shell export after sea-stabilized shell retention closes. | blocked; no retained `SH-0-sea` branch exists. |

## First Executable Gap

The current first accepted-evidence blocker is unchanged:

- object: `held_release_seed_path_rows_acceptance_certificate.v0`;
- field: `held_release_seed_path_rows.acceptance_certificate_ref`;
- candidate artifact: `held_release_seed_path_rows:5833f18e53586201`.

The diagnostic run-matrix metadata gap is now implemented in three runner surfaces:

- [held-release-seed-path-rows.mjs](../../../scripts/braid-ideal/held-release-seed-path-rows.mjs) accepts `--proof-id`, `--run-handle`, `--source-row-id`, `--target-center-group-velocity`, `--surface-speed-fraction`, and `--prehistory-mode`.
- [sh-0-sea-diagnostic-candidate-model.mjs](../../../scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs) accepts `--run-handle`, `--embedded-central-run-handle`, `--source-row-id`, `--target-center-group-velocity`, `--surface-speed-fraction`, and `--prehistory-mode`.
- [held-release-causal-wake-toy.mjs](../../../scripts/braid-ideal/held-release-causal-wake-toy.mjs) accepts `--surface-speed-fraction`, `--spin-axis` (default `1,1,1`, normalized to $\hat{\mathbf n}$), and `--prehistory-mode`; it writes the rigidly rotating hold-window source path history for `moving-prehistory`, applies the rigid-rotation release velocity for `kick-at-release`, and emits surface-speed fraction, actual tangential speed, angular rate, normalized spin axis, prehistory mode, kinematic $\mathbf J_{\mathrm{kin}}$ (diagnostic bookkeeping only), release-continuity residuals, reduced-radius sign sequence, and $\operatorname{Fix}(G)$-drift residuals per run. It also accepts `--fcc-sea-spacing` and `--fcc-sea-held-window` for the sea-screened embedded rows (72 held static FCC sea sources, one-way declared environment, release-instant wake-sum cross-check, per-step sea radial-projection rows, and the certificate-envelope caveat in `seaShellDiagnostics`).

Both outputs carry source artifact id/hash, source row id, run handle, prehistory mode, target-center group velocity, surface speed, and fail-closed evidence status. `kick-at-release` and `moving-prehistory` remain separate because they answer different questions about history priming.

Example isolated diagnostic row:

```bash
node scripts/braid-ideal/held-release-seed-path-rows.mjs --proof-id=SH-0 --run-handle=sh0-g0-vt080-moving-prehistory --source-row-id=diagnostic-source-row:sh0-g0-vt080-moving-prehistory --target-center-group-velocity=0,0,0 --surface-speed-fraction=0.8 --prehistory-mode=moving-prehistory --retained-record-id=retained-record:held-release-six-point:adapter-acceptance-certificate --provider-object-ref=candidate:central_solver_retained_history_provider_object:7d4a8fe0a9792327 --provider-artifact-hash=7d4a8fe0a97923270179f2ca0b49b4bc0d6b6ba3251b26e82569bdb4bd1f91df --pretty
```

Example embedded Noether sea diagnostic row:

```bash
node scripts/braid-ideal/sh-0-sea-diagnostic-candidate-model.mjs --run-handle=sh0sea-g0-vt080-moving-prehistory --embedded-central-run-handle=sh0-g0-vt080-moving-prehistory --source-row-id=diagnostic-source-row:sh0-g0-vt080-moving-prehistory --target-center-group-velocity=0,0,0 --surface-speed-fraction=0.8 --prehistory-mode=moving-prehistory --pretty
```

Only after the accepted seed-path certificate, matching external accepted-authority package, repo authorization, retained-source adapter package, and same-record receiver-normal root-detail rows exist can any row in this matrix be used for retained force/action or stability closure.
