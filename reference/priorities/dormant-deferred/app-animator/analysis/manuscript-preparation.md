# Animator Manuscript Preparation

The coordinator freshly read all six original bodies before drafting: priorities 1–86, queue 1–66, brainstorming 1–13, log 1–9, design 1–192 and 193–370, and simulation mode 1–77. Native wc records 621 lines / 30,328 bytes; recursive rg --files lists exactly those six originals. Scoped git --no-optional-locks status reported no changes in this lane before preparation. Bounded rg searches for the lane path and the design/simulation basenames under scripts, tests, src, .github and .githooks returned no matches; that is not a repository-wide consumer audit. Baseline SHA-256 is retained in task scratch and all six entries passed after reading. No outside app, runtime, interface code, schema, test, browser app or scientific record was inspected or run. The new manuscript must attribute implementation and closure claims to these source documents rather than assert live behavior.

The source metadata remains active, rank 13, value 6.43, cost 4.3, ROI 1.50 inside a dormant-deferred path. The queue retains ANIM-001 runtime cutover and ANIM-002 observer framing In progress, ANIM-003 timeline Observer/Audio and ANIM-004 canonical transforms Queued, ANIM-005 recorded EOM playback Verified at source. Exact dependencies, completion conditions and source-reported tests belong in coverage/support. This synthesis does not reconcile lifecycle or implement any task. The blank work log and absence of extra brainstorm ideas are themselves preserved dispositions, not grounds for an empty-subject disposition: the design and playback owners contain developed material.

The manuscript's subject is one authored composed scene within the explicit scene network, canvas-first editing and an observer participant, plus strict separation of prescribed animation and accepted recorded EOM playback. It should explain canonical scene documents, browser-local drafts, preview data and repo-ready JSON without treating transient UI state as the scene. The source's eleven current-state statements include substantial runtime, canonical document/preview/export/local persistence, tree/path/orbit/preview/export workflows, pause/warp/image/video/graphic timeline support, placeholder Audio/Observer menu paths, observer terminology over cameraPaths/cameraShots internals, framing participation/autoscale mathematics without controls, canonical bridge and narrow Split Group mutation, explicit staging contract without runtime imports, and source-closed recorded playback. Preserve these as source-time distinctions rather than fresh app inspection.

Design reading preserves all four layout alternatives and each cost/benefit: single canvas with guides, split design/observer, picture-in-picture, dedicated observer mode. The preferred near-term arrangement is a dominant design view and smaller observer inset reading one state with shared time, selection and focus; it is a design direction, not a new UI choice or measured usability winner. Shared controls are timeline/time, active interval/path, selection/focus, point of interest, visibility filters, pause/warp scale and object identity. Design edits positions/paths/nesting/guides; observer edits emphasis/composition/follow-target/distance/reveal timing. Drag semantics must not silently cross those domains.

The observer participant has position path, target relation, reveal state and overlay stack; anchors include root, assembly frame, path point, constituent target and inherited frame plus offset. Guides include origin, target, sight line, cone/attention corridor and optional composition/safe regions. Intervals carry teaching intent, required/optional assembly participation and overrides, and autoscale follows that intent rather than every visible object. Reveal choices preserve proxy-only, proxy-plus-path, shell, constituents, transfer focus and reaction-stage focus. Overlay objects retain attachment, local/world anchoring, fades and interval/reveal visibility. Five implementation stages remain source direction, not completed controls.

The composite rule is display metadata over related 4-tile assemblies, initially lanes 1 and 20: adjacency, label, span, reveal and proxy/constituent presentation do not create one assembly. A Photon label tile/token is artwork, not an assembly row. Composite groupings are not dissociate/associate targets; timing stays bound to individual upstream reactant/product endpoints. No primitive or solver ontology follows from visual grouping. Preserve historical exact lane/tile restrictions as the design source's local scope without extrapolating them to all current theory or app structures.

Media scope keeps jpg/jpeg/png/svg images, mp4/mov video and mp3 audio; webp/webm/aac/m4a are excluded in this source phase, not a universal browser-support claim. Media lives in the relevant Animator asset directories. Images/video are viewport overlays, span-bound, draggable/resizable in observer view; explanatory callout text, leaders and attachment remain the small language. The six primitives, four-step reveal order and six-layer rendering stack must be fully dispositioned. Structure edits use one canonical model with real shared transforms, local parent/child nesting, and Unbound Architrinos as edit outputs rather than menu stamps; richer display follows that foundation.

Inputs include authored scenes/local drafts, directly authored assembly/path/timing, accepted imports or explicit staging contracts and media. Outputs include canonical composition, previews/local drafts, repo JSON and observer/framing/overlay state. Handoff retains participant identities/roles, corridors/provenance paths, timing anchored to individual assembly endpoints, observer hints and labels. No upstream reaction re-solving, live review/request state consumption, external runtime import or shared mutable UI state is authorized.

Playback source names eom-recorded-playback-handoff.v1, exact eom_evolution_contract/v0, canonical record SHA-256, master_eom_binding/v1, eom-solver engine and concrete version, concrete run identity/claim grade, accepted evidence and completion. Preserve literal v0 as source-time contract, not silently repin to current evolution v1. Mismatched/altered/stale identities, foreign contracts/models, non-EOM engine, failed grade, unaccepted evidence or unfinished run fail closed. Animator worker admits records, evaluates recorded cubic histories and packages typed frames; it has no integrator, master-equation evaluation, root search or scientific acceptance logic. Admission against an existing accepted record is distinct from creating its scientific acceptance. EOM solver computes; Borg runs/inspects; Animator replays and authors.

Reverse authored handoff preserves animator-prescribed-scene-handoff.v1 and assembly-view-record.v0, validated normalized scene and canonical Borg adapter, separate scene/assembly/record pins and exact flags: engineId prescribed-geometry, motionAuthority authored-prescribed, claimGrade chart-hypothesis, evidenceStatus display-only, physicsInvoked false, recordOnlyReplay true. Unsupported schemas/motions, missing paths/parents, invalid time/coordinates, missing polarity and solver-derived/mixed motion are rejected. One-shot same-origin postMessage structured clone transfers after validation/sealing; Borg rechecks and freezes its own copy. No shared store, ongoing channel, mutable scene or EOM continuation authority. Do not execute publication merely because the source uses that word.

Recorded frames/trails/field shells/delayed hits retain engine/run/contract/model/evidence/status/hash provenance; interpolation is not evolution, cache does not upgrade authority, authored paths remain separate and no raw unpinned record gains admission. No timestep/root/particle/field-speed/integration/run controls belong here. Offline/high-precision work belongs to EOM/Borg. All six completed-work source claims and five non-goals remain supporting exact statements, with no independent live validation in this campaign. Full coherent exposition, independent source comparison, scoped link/math/whitespace and bounded real-browser visual review are still required before editorial acceptance.
