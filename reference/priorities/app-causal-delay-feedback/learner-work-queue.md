# Causal Delay Feedback Learner Work Queue

This is the canonical intake and execution ledger for learner-facing Causal Delay Feedback changes. The handoff snapshot under the operator's Documents folder is non-canonical.

## Rules

1. Add every new idea as **Queued**; do not rely on chat history as the only record.
2. An agent claims an item by setting it to **In progress** and naming itself.
3. An item is **Verified** only after focused tests plus relevant browser confirmation. A code edit alone does not consume it.
4. An item remains in this file after verification as a short durable decision record.
5. If a new request changes an earlier decision, add a new row referencing the superseded row; do not silently rewrite history.

## Audit Baseline

Live comparison on 2026-07-25 used the current checkout, focused CDF source and test inspection, and `causal-delay-feedback.html?replay=mock` in the browser. `Landed`, `partial`, and `missing` below describe that baseline; none of those labels means Verified. A row marked **Awaiting verification** has implementation and focused-test coverage but still needs the relevant current browser confirmation.

| ID | Status | Owner | Request / acceptance condition | 2026-07-25 live audit | Relevant source / test locations |
| --- | --- | --- | --- | --- | --- |
| CDF-001 | Verified | Operator browser review + focused CDF tests | Replace the old top-right Story / Path History / Roots / Forward Buildup / Sandbox lozenges with a scalable ordered, directly clickable table of contents. Show `Lesson n of N` beside each title; Laboratory is the final entry. Back and Next follow the same order. | **Verified.** Focused tests cover the ordered lesson sequence and retired destinations. Operator browser review accepted nonsequential Lesson Two/Three jumps and confirmed that each jump updates the correct top-left copy and scene. | `causal-delay-feedback.html`; `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackModes.js`; `tests/causal-delay-feedback-modes.test.js` |
| CDF-002 | In progress | CDF continuation | Remove learner-facing Path History, Routes/Roots, and Branch Lab from normal navigation while retaining useful diagnostic code dormant. Do not create duplicate routes, runtimes, state stores, or evaluators. | **Partial.** Branch Lab is already absent from normal navigation and the app remains one route/runtime. Browser still exposes Path History and Roots. | `CausalDelayFeedbackModes.js`; `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackRuntime.js`; `tests/causal-delay-feedback-modes.test.js` |
| CDF-003 | Awaiting verification | — | Every lesson that uses the paired positrino/electrino paths uses the exact Lesson One coordinate template: axes, margins, path origins, stroke width, body size, and label placement. | **Partial.** Existing Story path lessons share a fixed chart viewport and tested path/body grammar. Lesson Five and later paired-path lessons are absent. | `CausalDelayFeedbackDisplayContract.js`; `CausalDelayFeedbackRuntime.js`; `tests/causal-delay-feedback-runtime.test.js` (`Story graph viewport`, `all Story steps use one desktop chart template`) |
| CDF-004 | Queued | — | Lesson One is titled **Meet the Electrino and Positrino Transceivers**, keeps the simple default-speed introduction with no speed choices, and includes: “Wakes emitted earlier have had longer to expand, so they have a larger radius.” The solid white current-emission marker appears on both active bodies and remains distinct from wake history. | **Partial.** Title, default-speed scope, paired current-emission markers, and wake-history distinction are present. The required sentence is not. | `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackRuntime.js`; `tests/causal-delay-feedback-modes.test.js`; `tests/causal-delay-feedback-runtime.test.js` |
| CDF-005 | Verified | Operator browser review + focused CDF tests | Lesson Two ends at the same state where Lesson Three starts: the 50% time-axis state. Lesson Three advances from there to 80%. | **Verified.** Shared constants and focused tests pin the 50%→80% handoff; operator browser review accepted Lesson Two's stop, Lesson Three's identical start, and its advance to 80%. | `CausalDelayFeedbackStoryMode.js`; `tests/causal-delay-feedback-modes.test.js`; `tests/causal-delay-feedback-runtime.test.js` |
| CDF-006 | Verified | Operator browser review + focused CDF tests | Lesson Three contains the reciprocal-relationship explanation and omits implementation caveats such as progress-alignment narration. | **Verified.** Focused tests pin the learner-facing Lesson Three copy. Operator browser review confirmed the progress-alignment caveat is absent and accepted the reciprocal explanation as jointly carried—without redundant prose—by the title, Relationship One/Two block, and graph. | `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackModeController.js`; `tests/causal-delay-feedback-modes.test.js`; operator browser acceptance |
| CDF-007 | Verified | Operator browser review + focused CDF tests | Lesson Four teaches wake shape changing with speed. Changing speed returns the animation to its initial frame, stops playback, clears completion/resume state, and shows Play. | **Verified.** Focused tests pin reset of playing, resumable, and completed state after speed changes. From the completed Lesson Four state, operator browser review confirmed that selecting 0.3 then 0.9 returns each scenario to its initial frame with Play restored, selected-speed focus retained, and Expanded/Compressed labels following the selected scenario. | `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackModeController.js`; `tests/causal-delay-feedback-modes.test.js`; `tests/causal-delay-feedback-runtime.test.js`; operator browser acceptance |
| CDF-008 | In progress | CDF continuation | Promote **Forward Wake Buildup** to Lesson Five. Remove its separate mode and “Declared field speed display fixture” supertitle. Use the Lesson One frame, start at the normal path origins at emission zero with no inherited wake history, and grow the first fronts from both emitters. Do not call it a bow shock. | **Partial.** A paired forward-buildup renderer and explicit non-self-interaction evidence boundary exist, but the browser exposes it as a separate mode and it starts from the ordinary replay time/history. | `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackRuntime.js` (`drawFieldSpeedCompressionScene`); `tests/causal-delay-feedback-runtime.test.js` (`Forward Wake Buildup reuses`) |
| CDF-009 | Verified | Operator browser review + focused CDF tests | Use one shared bottom timeline rail across lessons and Laboratory with timeline-only First Frame, Play/Pause, and matching Last Frame controls followed by the scrubber. Space pauses/resumes without restarting. A completed lesson must not overload Play as Replay. Lesson-sequence navigation belongs in the separate standard top-right icon strip. Remove playback controls from the lesson-information panel and top header; accessible labels/tooltips are required. | **Verified.** The shared rail contains only First Frame, Play/Pause, Last Frame, and scrubber. Focused tests cover shared icons, pause/resume, completion, repeated endpoints, keyboard transport, and DOM separation. Desktop/390×844 browser QA confirmed the layout, and the operator explicitly accepted exact-frame resume plus repeatable First/Last behavior and the absence of bottom lesson navigation. | `causal-delay-feedback.html`; `src/runtime/TransportControlIcons.js`; `CausalDelayFeedbackRuntime.js`; `CausalDelayFeedbackModeController.js`; `tests/transport-control-icons.test.js`; focused CDF tests; operator browser acceptance |
| CDF-010 | Awaiting verification | CDF continuation | Laboratory editable paths have real C1 tangent continuity in their evaluated geometry and user-visible rendering after interior drags. A browser drag regression must catch visible one-sided tangent mismatch rather than proving only an unused helper or nominal interpolation mode. | **Repaired after failed operator review; focused/browser regression passing.** Root cause was a second sparse history/control re-fit applied after the already-smooth 1,800-sample quintic deformation. That conflicting re-fit was removed. Rendering and evaluation now consume the same directly deformed C1 path. A 220-unit interior browser drag passes a display-scale one-sided tangent-dot threshold above 0.999 and produces the durable smooth-curve capture. Explicit operator browser acceptance remains required. | `CausalDelayFeedbackTimedPath.js`; `CausalDelayFeedbackRuntime.js` (`deformPathAroundPathTime`, `drawC1TimedPath`); `tests/causal-delay-feedback-runtime.test.js` (`no visible one-sided tangent kink`); `scripts/capture-causal-delay-feedback-browser-qa.mjs` (`laboratory-path-drag-c1`); `browser-qa/laboratory-path-drag-c1-purple-1440x900.png`; operator browser acceptance pending |
| CDF-011 | Queued | — | Repeated Laboratory interior drags keep both fixed endpoints unchanged and keep the editable path on screen. Add a regression that exercises repeated drags, not only one drag. | **Partial.** Endpoint immobility and several local fairing drags are tested. No explicit screen-bound constraint or repeated-drag screen-bound regression exists. | `CausalDelayFeedbackRuntime.js` (`deformPathAroundPathTime`); `tests/causal-delay-feedback-runtime.test.js` (`path line drag keeps path endpoints fixed`) |
| CDF-012 | In progress | Codex | The Laboratory solid white current-emission dot and its fading wake arc use the same freshly calculated emission-point state. After a path drag, their origins remain coincident. | **In progress.** The render path is being consolidated so each current-emission marker consumes the exact live wake-link source object used by its fading arc, with a direct before/after-drag coincidence regression. | `CausalDelayFeedbackRuntime.js` (`drawSandboxTransmissionGhost`, `drawWakes`, `getVisibleWakeSeries`); `tests/causal-delay-feedback-runtime.test.js` |
| CDF-013 | Verified | Operator browser review + focused CDF tests | The shared timeline scrubber remains available for slow replay-time inspection in a clean bottom canvas rail, outside the top header and below the time axis. The rail never covers axes, paths, lesson information, lesson navigation, or other controls at desktop or phone widths. Keep the ordered lesson plan below the separate standard top-right app-icon strip. | **Verified.** Both story and Laboratory viewports reserve the bottom rail. Desktop and 390×844 browser QA confirmed separation from axes, paths, information, TOC, and controls; the mobile information-panel clearance was corrected before acceptance. The operator explicitly accepted the completed chart-aligned bottom-rail layout. | `causal-delay-feedback.html`; `CausalDelayFeedbackRuntime.js`; `tests/causal-delay-feedback-runtime.test.js`; operator browser acceptance |
| CDF-014 | Queued | — | The Laboratory settings popover stays open after option changes until explicit dismiss or click outside. | **Landed in event wiring.** Setting handlers do not dismiss; the document click-outside handler does. Browser interaction proof remains. | `CausalDelayFeedbackRuntime.js` (`bindEvents`, `toggleSettings`, `hideSettings`); `tests/causal-delay-feedback-runtime.test.js` |
| CDF-015 | Queued | — | Remove the Laboratory lower-left diagnostic data lozenge and redundant “Positrino above / Electrino below” legend. Keep direct on-path labels. | **Partial.** The diagnostic readout is hidden at the audited browser state, but the redundant polarity legend remains visible. | `causal-delay-feedback.html`; `CausalDelayFeedbackRuntime.js`; `tests/causal-delay-feedback-modes.test.js`; browser QA capture script |
| CDF-016 | Queued | — | Add Lesson Six, **Inverse-Square Spreading**, on the shared paired-path frame. Start at a calm 50% state. Teach only the transmitted quantity per unit spherical-wakefront area: fixed amount over `4 pi R^2`, hence `1/R^2`; do not generalize this to unsupported field amplitude. | **Missing.** No lesson definition, renderer, navigation entry, focused test, or browser proof exists. | `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackRuntime.js`; focused tests and capture script |
| CDF-017 | Queued | — | Add Lesson Seven, **Acceleration**, on the shared paired-path frame at a calm 50% start. Teach that a wake changes velocity: from behind may increase speed, from ahead may decrease speed, and off-axis may turn the path. Do not imply every acceleration is sideways. | **Missing.** No lesson definition, renderer, navigation entry, focused test, or browser proof exists. | `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackRuntime.js`; focused tests and capture script |
| CDF-018 | Queued | — | Add Lesson Eight, **Wakes Combine by Superposition**, with one receiver and two independently colored transmitter paths. Begin wake-first; combine arriving wake vectors into a net wake, then show that the net wake determines displayed acceleration. Keep individual contributions secondary and omit a data panel. | **Missing.** No lesson definition, three-path display fixture, renderer, focused test, or browser proof exists. | `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackRuntime.js`; focused tests and capture script |
| CDF-019 | Queued | — | Keep the teaching/model/evidence boundary explicit across every lesson and Laboratory state. Representative replay, EOM record replay, declared display fixtures, local drag previews, and unavailable providers must remain visibly and semantically distinct; display parity is not physics acceptance. | **Partial.** Replay and display-fixture boundaries already exist in state, copy, canvas datasets, and tests. New Lessons Five through Eight must preserve them. | `CausalDelayFeedbackReplayAdapter.js`; `CausalDelayFeedbackEomReplayAdapter.js`; `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackRuntime.js`; all focused CDF tests |
| CDF-020 | Queued | — | Rebuild focused browser QA around the final lesson order at desktop and portrait sizes, including reduced motion, high contrast, transport state, Lesson Two→Three handoff, Lesson Four speed reset, Lesson Five emission-zero start, Laboratory drag/settings behavior, and no retired learner surfaces. | **Partial.** The capture script covers the old Story/mode structure and several accessibility variants; it still captures Roots, Forward Buildup as a mode, and Sandbox naming. | `scripts/capture-causal-delay-feedback-browser-qa.mjs`; `reference/priorities/app-causal-delay-feedback/browser-qa/` |
| CDF-021 | Queued | — | Preserve public teaching launch readiness: the standalone route and apps-page exposure remain live, the complete lesson flow is navigable and understandable without operator explanation, and Laboratory remains simple enough for younger learners. | **Partial.** Route/app exposure and a usable canvas are present. The canonical lesson flow and Laboratory cleanup are not complete. | `causal-delay-feedback.html`; `src/apps/navigator/StandaloneAppLaunchRuntime.js`; `priorities.md`; final focused tests and browser QA |
| CDF-022 | Queued | — | After the prerequisite lessons, plan and add **Continuous Delayed Feedback / Reciprocal Causal Chain** on the standard paired-path diagram. Begin with an early fading arc reaching the partner path near 0–10%, then alternate hit-to-emission arcs between the paths toward 90–100% for as many clear hops as the frame supports. Teach that feedback is continuous but delayed: each one-way influence propagates before reception changes the partner's motion while transmission continues, and a full round trip contains two one-way propagation delays. State that the drawn arcs are sampled trace points, not discrete-only physics, and keep all model/display claims scoped. | **Missing and intentionally sequenced after prerequisites.** No lesson plan, chain evaluator/projection, renderer, focused test, or browser proof exists. | Future focused requirements card; `CausalDelayFeedbackStoryMode.js`; `CausalDelayFeedbackRuntime.js`; focused tests and capture script |
| CDF-023 | Queued | — | Trial source-colored path transmission markers: pale pink on the positrino path and pale blue on the electrino path, each matching the initial color of that source's fading arc. Keep the markers subtle and preserve path visibility. Compare the trial against the current neutral white-dot baseline before recommending adoption; do not silently replace the baseline. | **Not implemented by decision.** This is a visual-polish comparison task, not an accepted color change. | `CausalDelayFeedbackRuntime.js` (`drawTransmissionPointMarker`); focused screenshot comparison and browser review |
| CDF-024 | Awaiting verification | CDF continuation | Reuse the existing web-app top-right strip assets and semantics in this order: Table of Contents, lesson back, lesson forward, Home, Search. The icon back/forward controls replace text Back/Next and remain lesson-sequence navigation, never timeline transport. TOC content opens below the strip. Home returns to Applications through the standalone-app navigation contract; Search searches the learner destinations. Do not report this item Verified until focused tests and desktop/phone browser QA confirm the controls and non-overlap. | **Implemented and focused-tested.** The strip reuses the canonical TOC pill, chevrons, Home path, Search geometry, and control styling. Agent browser QA confirmed TOC collapse, lesson search/jump, lesson back/forward, real Home navigation to Applications, and desktop/390×844 layout. Explicit operator browser acceptance remains required. | `index.html` (`scene-hud-tools` assets); `style.css` (standard control styling); `src/apps/navigator/StandaloneAppHomeRuntime.js`; `causal-delay-feedback.html`; `CausalDelayFeedbackModeController.js`; `tests/causal-delay-feedback-modes.test.js`; operator browser acceptance pending |
| CDF-025 | Verified | Operator browser review + focused CDF tests | Move the shared `time` axis label just above the horizontal axis in every Lesson and Laboratory so the bottom timeline rail cannot underlap it. Use one display-contract token for the position, preserve the axis/frame, and avoid the paths and lesson-information panel. | **Verified.** `TIME_AXIS_LABEL_POSITION` is shared by the one background renderer, the focused contract test pins it north of the axis, and operator browser review accepted the resulting global placement. | `CausalDelayFeedbackDisplayContract.js`; `CausalDelayFeedbackRuntime.js` (`drawBackground`); `tests/causal-delay-feedback-runtime.test.js` |
| CDF-026 | Awaiting verification | CDF continuation | Remove the learner-visible numeric replay-time readout from the bottom rail without leaving an empty spacer. Preserve the scrubber's internal state and expose the current replay time through the slider's accessible value text. | **Implemented and focused-tested.** The output node and grid column are gone; `aria-valuetext` carries the replay time on the slider. Desktop and 390×844 agent browser QA show no visible readout or empty spacer. Explicit operator browser acceptance remains required. | `causal-delay-feedback.html`; `CausalDelayFeedbackRuntime.js` (`updateNowControl`); `tests/causal-delay-feedback-runtime.test.js`; operator browser acceptance pending |
| CDF-027 | Verified | Operator browser review + focused CDF tests | Size and align the entire bottom timeline rail to the transformed chart axis: its left edge begins at the Y-axis/X-axis intersection and its right edge ends immediately before the time-axis arrowhead. Use the same shared geometry in every Lesson and Laboratory, retain only a small arrowhead-safe inset, and keep the rail below rather than over the axis. | **Verified.** The focused geometry test pins the rail to the transformed shared axis constants. Desktop and 390×844 browser QA showed the rail below the chart without panel/axis overlap, and the operator explicitly accepted the chart-aligned timeline span. | `CausalDelayFeedbackDisplayContract.js`; `CausalDelayFeedbackRuntime.js` (`alignBottomRailToTimeAxis`); `tests/causal-delay-feedback-runtime.test.js`; operator browser acceptance |
| CDF-028 | Verified | Operator browser review + focused shared-transport tests | First Frame returns the active lesson to its initial state; Last Frame jumps to the final frame with wakes visible; either action remains repeatable and never changes the selected lesson. Last Frame uses the shared right-pointing triangle plus right-side vertical-bar icon and accessible label. | **Verified.** Focused tests pin the shared Last Frame SVG/label and runtime transport state. Operator browser review confirmed Last→First→Last behavior and the expected initial/final scenes. | `src/runtime/TransportControlIcons.js`; `CausalDelayFeedbackRuntime.js` (`resetReplayTime`, `jumpToLastFrame`); `tests/transport-control-icons.test.js`; `tests/causal-delay-feedback-runtime.test.js` |
| CDF-029 | Queued | — | Original Laboratory entry/reset intake: start at literal `t=0` with no inherited story cursor. | **Superseded before implementation by CDF-030 after operator clarification.** Retained as a stable intake record; do not implement the literal empty-start behavior. | `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackRuntime.js`; superseding CDF-030 |
| CDF-030 | Awaiting verification | CDF continuation | Give Laboratory one stable useful early entry/reset state around 10–15% of its replay range, with bodies and causal-delay paths/wakes already visible. Entering from any lesson or after refresh must use that same state rather than inherit an arbitrary story/scrub cursor; First Frame in Laboratory returns to it. Preserve explicit user scrubbing while the learner remains in Laboratory. Implement and focused-test this before requesting the next CDF-010 browser drag test. | **Implemented and focused-tested.** Entry and Laboratory First Frame use one stable 16% replay fraction; in-Laboratory scrubbing remains untouched. Agent browser preflight at 390×844 confirmed the early bodies/path plus a visible causal-delay wake and slider 160. Explicit operator browser acceptance remains required. | `CausalDelayFeedbackModeController.js`; `CausalDelayFeedbackRuntime.js` (`handleLearnerModeChange`, `resetLaboratoryScenarioPlayback`); `tests/causal-delay-feedback-runtime.test.js`; operator browser acceptance pending |
| CDF-031 | Awaiting verification | CDF continuation | Laboratory playback is non-looping. Reaching the final frame stops playback and holds the final state; it must not wrap to the beginning automatically. Play and First Frame remain the explicit ways to continue or return. Browser acceptance must confirm a final-frame hold beyond the prior loop boundary and explicit recovery with First Frame/Play. | **Implemented and focused-tested.** Laboratory elapsed time clamps rather than wraps, terminal playback stops, and explicit Play at the held end restarts from the stable Laboratory entry state. Agent browser preflight held slider 1000 across the former loop boundary. Explicit operator browser acceptance remains required. | `CausalDelayFeedbackRuntime.js` (`tick`, `getReplayTimeForElapsedSeconds`, `setPlaying`); `tests/causal-delay-feedback-runtime.test.js`; operator browser acceptance pending |
| CDF-032 | Queued | — | Restyle the bottom scrubber to the app's purple control theme. Use a normal purple active/middle track variation and either a purple-edged or solid-purple thumb, choosing the cleaner accessible treatment. Preserve clear contrast and focus visibility; exclude red, blue, and pink so transport chrome stays semantically distinct from source/wake colors. | **Queued for a separate visual-polish pass.** No style change is authorized as verified until focused accessibility/style checks and explicit operator browser review. | `causal-delay-feedback.html` range styling; desktop/phone contrast and focus browser review |
| CDF-033 | Queued | — | Align Lesson Four's `Expanded` and `Compressed` labels to the visible wake geometry at every speed and frame. `Expanded` is left-aligned with the outermost trailing/rear wake ring; `Compressed` is right-aligned with the outermost leading/front wake ring. Derive both anchors from actual visible wake extents rather than free caption positions, while preserving readability and preventing initial overlap. | **Queued from operator browser review.** Current labels can drift too far apart from the wakes they describe. | `CausalDelayFeedbackRuntime.js` (`drawStoryMotionWakeComparison`); `CausalDelayFeedbackStoryMode.js`; focused extent/label geometry tests; operator browser acceptance pending |

CDF-001 and CDF-005 were promoted to **Verified** after focused-test coverage and explicit operator browser acceptance.
