# Mapping Benchmarks Work Log

This file is the chronological work log for the `mapping-benchmarks` priority area. Use it for dated agent status, proof-attempt notes, checker narratives, handoffs, failed paths, and operator/developer communication that must remain discoverable but should not crowd the live priority tracker.

Use `brainstorming.md` for provisional ideas, insights, conceptual maps, and draft corpus-promotable text when this priority area has one. Use `priorities.md` for strategy, status, blockers, and promotion routing, and use `work-queue.md` for accepted executable tasks and their local order. Keep focused proof packets, certificates, app specs, and requirement notes in their own sibling files when they need a stable structure.

## Log Entries

### 2026-08-22 Shared-record strong-field electromagnetic benchmark

- Added a focused benchmark that binds photon--photon scattering, vacuum
  birefringence, amplitude-dependent polarization and propagation,
  electron--positron pair production, and pair backreaction to one versioned
  source, sea, boundary, photon, pair, detector, and provenance record.
- Made coverage an all-five-components predicate; no weighted scalar may hide
  a failed behavior row.
- Routed the packet through existing queue row `XTM-006` and existing equation
  owners rather than creating a duplicate executable task.
- Preserved QED equations and observations as effective recovery targets, not
  substrate premises, and changed no benchmark status or equation maturity
  score.

### 2026-08-22 Priority owner renamed to mapping-benchmarks

Renamed the priority directory from
`reference/priorities/cross-theory-mapping/` to
`reference/priorities/mapping-benchmarks/` and updated live links, ranking
labels, owner-facing titles, and benchmark references. Stable `XTM-*` task IDs
and `cross_map.*` failure codes remain unchanged because this is an owner-name
migration, not a task or failure-schema migration. No benchmark status, rank,
evidence grade, or physics claim changed.

### 2026-08-22 Benchmark execution-ownership normalization

- Linked the lane to the shared equation/benchmark/domain mapping router.
- Reclassified 23 case-local task lists as draft candidate closure steps; the
  central work queue remains the only executable authority.
- Replaced the serial XTM-002 through XTM-010 scheduling chain with each
  family's actual native-carrier requirement.
- Declared that a cross-family dependency exists only when a row consumes a
  named accepted carrier produced by another row.
- Changed no benchmark status, rank, acceptance result, or reader-facing claim.
