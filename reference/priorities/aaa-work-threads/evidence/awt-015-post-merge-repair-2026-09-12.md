# AWT-015 repair after the runtime migration merge

## Completion

**AWT-015 is complete on September 12, 2026.** The operator-authorized generated repair is applied and checked. The final complete runners establish the following operational validation result at head `7c957f86c1ca8c7ff8d4cf8341f2b3e2748b4b09` plus the retained working changes:

| Final gate | Measured result | Supervisor ID |
| --- | --- | --- |
| Ordinary Node, all 333 selected files | 2,716 passed, zero failed, one declared optional skip; 2,717 tests; 553.526 seconds | `9f6382d6-4c2f-452b-b55b-09b7fb32321e` |
| Slow Node, all ten selected files | 124/124 passed; zero skips or cancellations; 299.937 seconds | `4c16c98a-4ecc-4016-aa1a-3beec32ac2bf` |
| Shared-venv Python discovery | 1,685/1,685 passed; 737.080 seconds | `04f48d1c-c340-4993-b694-be2cfb1922c8` |
| Maintenance-inclusive integrity after closure records | 36 required checks passed, zero failed, zero unreached; one optional sweep skip; 32.7 seconds | `cf26afcb-efe6-486a-91d0-059691fa0e3a` |

The ordinary skip is the explicitly optional sealed compact-sweep local database, not a hidden required body. Integrity's optional ordinary-sweep step is independently covered by the full ordinary run above. The supervisors report exit zero and process-group closure for every final run. All source-binding, snapshot, streamed cleanup and current-launch failures retained earlier in this record have passing final gate coverage. The earlier intermittent nested-supervisor completion-record loss remains unexplained and retained as a reliability observation; these later passes do not retroactively repair its record or prove a general absence of recurrence.

`cmp` confirms identical tracked diffs across final ordinary Node, Python and slow Node for `scripts/`, `src/`, `tests/` and `content/markdown/aaa/`; the after-run diff SHA-256 is `74ad1a512d1ffc5f0d431118fbce4c557c991efe7ddfd16d57072e4ce4e1d9a1`. The same comparison passes for the separately captured untracked source/test checksum inventory, whose file SHA-256 is `86c089dc8c099c69e10fce7f7fe9fdb5a97da0e8ca67e037babf4997f02fe1a6`. The 32-destination existence check and zero former-path search results are recorded below. Falsifiers are a surviving consumer of a former path, a required gate failure on this state, or a source/reference binding whose substance was not preserved by its recorded review.

The live queue entry and current-focus item are removed. This closes file-layout and operational validation work; historical receipts, mathematical references and scientific acceptance boundaries retain their recorded authority. No staging, commit, push, PR action or scientific campaign was performed. Earlier failed runs and pause records below are historical evidence, not the current disposition.

After queue removal and incoming-link updates, the final maintenance-inclusive integrity run above passes. `validate-priority-ranking.mjs` and scoped `git diff --check` also pass. `node scripts/dev/owned-compute-supervisor.mjs closeout --owner-task "$CODEX_SESSION_ID"` reports `status: clear` after checking 898 retained leases. No computation remains owned by this task, and it is stopped for operator repository actions.

## Authorized regeneration and final validation

The operator's “Do 1” explicitly authorizes the reviewed regeneration batch and its dependent checks. Work resumes at 13:39:32 UTC on the same `7c957f86` head; `cmp` finds the scoped tracked source/test/corpus diff identical to the preceding checkpoint. The four canonical generators write the seven previewed targets and pass their checks. Ignored runtime preparation `6ded0829-0c77-414e-913a-6ee8578394f6` completes with exit zero and process-group closure, and `prepare-runtime-assets.mjs --check` passes all four families. The MCP deployment fixture follows the new full-corpus snapshot through only its three candidate identity fields, retaining fixture-only authority. Complete ordinary Node `38ccc46e-7223-473b-90ef-9dcc2d8c445e` is the first sweep after this application.

This sweep exposes an omitted downstream generation step: `mcp-tool-contract.v1.json` and `mcp-stdio-smoke.v1.json` still carry the preceding source-index digest. The actual `snapshotProvenance` implementation copies that digest from the snapshot; neither the query engine nor the stdio adapter needs a code change. The unchanged tool-contract and stdio generators run in scratch, and a complete byte comparison proves that both previews differ only by replacing the old snapshot digest with `14a8608620bc0e61a232355d24df1fc55b222475def2fc97238848d87ebc6ccb`. The literal comparison passes a known fixture before target use. Their exact commands are `node scripts/archie-service/validate-mcp-tool-contracts.mjs --write` and `node scripts/archie-service/check-fixture-mcp-server.mjs --write`, each followed by `--check`. Both dependent refreshes are included in the operator-authorized generation repair; the first ordinary run will be retained as a failed intermediate result, then the corrected final state will be tested in full.

The campaign's 32 layout rows were re-extracted only after the extractor passed a known one-row fixture. `fs.existsSync` verifies every declared destination exists and every former location is absent. Exact former-path `rg -n -F -f` across `scripts/`, `tests/`, `src/`, `apps/`, `.github/` and `reference/` returns zero hits. The pattern list, destination inventory and empty result are retained under `.local-data/awt-015/2026-09-12/resumed-completion/` as `former-layout-paths.txt`, `layout-destination-check.json` and `former-layout-reference-hits.txt`. This checks declared paths and literal references in the named scope; successful test execution supplies the separate consumer evidence.

The intermediate ordinary sweep completes 2,718 tests: 2,712 passes, five failures, one optional skip and zero cancellations, in 515.263 seconds. Four failures are the two MCP fixture dependencies above. The fifth is `Object.entries(entry.PINS)` in the cached-pilot binding test: the separately authorized Option B task removed that interface while this test selection was in progress. Before/after tracked diffs capture the transition, and the completed owner's updated portable selection passes 3/3. The optionB task reports completion before the final sweeps begin. Its source changes and historical comparison controls remain with that owner; this task does not overwrite them.

Both MCP generators are now applied canonically and their checks pass. The tool fixture preserves eight positive cases, nine negative cases and five bounded tools; the stdio fixture preserves initialization, tool calls, missing-source and unknown-tool responses. The startup router is regenerated again after the completed integrity-script change. Maintenance-inclusive integrity `87b0257b-3468-47d7-8369-815cc933ee44` passes **36 required checks**, with zero failures, one optional sweep skip and zero unreached checks, in 38.0 seconds. Its supervisor reports exit zero and process-group closure. `validate-priority-ranking.mjs` passes 26 active owners and 14 ranked rows.

The final settled ordinary sweep is `9f6382d6-4c2f-452b-b55b-09b7fb32321e`, with all available process controls and the reviewed fresh circular build enabled. Complete shared-venv Python discovery is repeated under `04f48d1c-c340-4993-b694-be2cfb1922c8` because Python code references the pilot launchers changed by the concurrent task. The tracked input diff and previously untracked source/test checksums are retained as `settled-final.before.diff` and `settled-untracked.before.sha256` under the local evidence directory. Final terminal results and after-run comparisons remain required before queue removal.

The final ordinary runner reports **2,717 tests: 2,716 passes, zero failures, one optional skip and zero cancellations**, in 553.526 seconds over all 333 ordinary files. The full shared-venv Python runner reports **1,685 tests, all passing**, in 737.080 seconds. Both supervisors report exit zero and process-group closure. `cmp` reports identical tracked source/test/corpus diffs before and after ordinary Node. Complete slow Node follows serially under `4c16c98a-4ecc-4016-aa1a-3beec32ac2bf`.

## Resumed completion attempt

The operator resumed this task on September 12 at 12:24:42 UTC, authorizing completion or a planned checkpoint at 13:24:42 UTC. `git rev-parse HEAD` returns `7c957f86c1ca8c7ff8d4cf8341f2b3e2748b4b09`. The initial `git --no-optional-locks status --short` shows concurrent root-cover Option B implementation and its new manifest, baseline, inventory and tests. Those files remain with their active owner. The preceding checkpoint below is retained as historical evidence rather than rewritten as a current result.

The three deferred test repairs are now implemented. The conformance control loads the exact retained `6e3467a0…` reference through its existing archive, with its digest checked before import. The literal-entry control checks the original entry's full digest and original `19c57e9b…` comparison pin, independently of the current adapter interface. Five current synthetic-fixture bindings were propagated through their declared callers after reviewing these changes; the original scientific decoder/control bindings remain preserved. The streamed postpublication control distinguishes ordinary failed cleanup, which must certify process closure and release its lock, from external deadline fallback, which retains unresolved ownership. Both paths must reject publication, leave no public stream and close every independently observed process/group.

Shared-venv `unittest` across the four affected conformance, adapter, diagnostic and stream modules passes **227/227** in 64.360 seconds under supervisor `108661b4-3d36-4a8c-adeb-23ae118658ac`. The focused Node `postpublish` control passes **1/1** in 1.794 seconds under `20003a33-c4e1-41c9-a161-105c9b8e8a89`; the retained ordinary-cleanup fixture is `/private/var/folders/y8/6b6hk2ld5rl_b9dh0xzf0yr00000gn/T/f6c-stream-ylqqXj`. The previous externally terminated fixture remains separate evidence. These are operational/synthetic controls, not a new scientific campaign or oracle acceptance.

`node scripts/build-equation-mapping-corpus.mjs --check` now passes over 199 Markdown files, 4,685 displays and 23 promoted equations. Maintenance-inclusive integrity `c5a0a1c7-1266-4142-af78-f62f47d7b59e` reports **33 required passes, one required failure, one optional sweep skip and no unreached checks** in 34.7 seconds. Its sole failure is generated textbook reading-copy freshness for `architrino-textbook.md` and `dynamics.md`; the exact writer is `node scripts/build-textbook-md-pdf.mjs --write`. Runtime preparation completed within this gate. The first concurrently started ordinary sweep observed some runtime snapshots before that preparation completed; its terminal result must be followed by settled-state verification, not treated as final acceptance.

The earlier OPS-030 candidate is stale after an obsolete anchor was removed above its selected passages. `git diff 4a8e760bae44dbc868714e579ba60779ae0be9d2 HEAD -- content/markdown/aaa/dynamics/master-equation.md` shows that two-line deletion; relocating the eleven reviewed selectors by two lines preserves all their previously reviewed content digests. The revised candidate is retained locally as `.local-data/awt-015/2026-09-12/resumed-completion/moving-single-root-dependencies.candidate.jsonld`, SHA-256 `3f514463171563dfe48e02c73f24e92fb2ebb42534f249a7a9c0015993f6d4e7`. The existing reader's known controls ran before candidate validation. Its complete report-only trial at the head above passes the numerical, displayed-result, existing numerical-test and finite-ledger polynomial checks, retains `approval: not-granted`, and returns `review-required`. The operator's explicit map-refresh and reading-copy regeneration decision is pending; no canonical map or generated reading copy was changed by this attempt.

Full ordinary Node `e80d128c-bdda-487c-bfbe-dac15f316e17` and shared-venv Python discovery `a54204bc-cc88-447d-8f44-06268e7275a7` are running under owned deadlines. The existing fresh circular build is reused only after `git diff --name-only 88568b42ff84cc04149cabab325c8056aac39939 HEAD -- src/eom scripts/eom/prepare-subfield-circular-root.mjs scripts/eom/prepare-f5-enclosed-root.mjs` returns no changes; current-context tests still verify its complete source/build receipt. No mathematical oracle or retained numerical output was changed.

The five-file snapshot rerun `773eadca-a610-4fcf-87f9-6ee70bff7981` reproduces all five early failures after runtime preparation, overturning startup timing as a sufficient explanation. The source-index fixture still binds stale reading-copy output, the fixture-only MCP deployment contract names the previous full-corpus snapshot, and the Master EOM binding records its pre-anchor-cleanup source digest. `git show 7c957f86^:content/markdown/aaa/dynamics/master-equation.md | shasum -a 256` reproduces the binding's `bb735786…` expectation; the complete `git diff 7c957f86^ 7c957f86 -- content/markdown/aaa/dynamics/master-equation.md` contains only removal of two obsolete HTML anchors and their blank lines. All binding statements, equations, singular-event dispositions and machine fields remain unchanged by that exact transition. After reading the binding's source-refresh procedure and complete contract, this task updates only its digest to current `99257f52…`; focused Borg and final strict validation remain required. This refresh makes no new physical claim.

The active `optionB` task has independently repaired the canonical moving-single-root map within its operator-authorized implementation. Its changes take precedence over this task's unused local candidate. This task will verify the settled canonical result and will not overwrite the concurrent owner's map. The pending decision is consequently narrowed to generated-artifact repair.

The first resumed ordinary sweep finishes **2,706 tests: 2,687 passes, eighteen failures and one skip**, with zero cancellations, in 571.288 seconds. The eighteen failures comprise ten stale-map controls, four Archie snapshot/contract controls, the Borg source binding, and three streamed fixtures. The canonical map subsequently passes **13/13** in 6.525 seconds after the other task's repair. The reviewed Borg digest refresh passes **256/256** in 16.148 seconds. Only the MCP fixture's three candidate snapshot identity fields are refreshed; its `fixture_only`, `remoteReady: false`, public authorization and accepted-main restrictions remain unchanged. The deployment validator passes all 24 negative cases. Reading-copy and source-index fixture regeneration remain necessary before the complete gates can close.

The three streamed failures all pass the retained focused rerun `d2961714-a608-43d7-8ff2-5645b9b3504f` in 19.545 seconds. Its `postpublish` case takes the external-deadline path and records observed closure, an absent public stream and retained lock. The first full-run `postpublish` failure occurs before these assertions: the nested supervisor stops updating its lease during deadline termination, and the test's cleanup assertion masks that original failure. The fixture is retained at `/private/var/folders/y8/6b6hk2ld5rl_b9dh0xzf0yr00000gn/T/f6c-stream-YRWVut`; its nested supervisor status returns `stale_closed` for lease `0e7e9903-ae7e-432d-a14a-d2e8b8348913`. Scoped `ps` over its supervisor, envelope, coordinator and every PID retained in its resource observations returns no rows at 12:44 UTC. That later absence does not retroactively supply the missing completion record or a complete independent observer trace. The test now retains the original exception in its failure message, while still refusing cleanup of an unresolved fixture. No timing limit or process-closure condition is relaxed. The complete streamed file is being rerun with all fixtures retained under `5f3b01d2-c36b-4ae9-b174-ad3938b57216`; an isolated rerun does not replace final full-suite acceptance.

Full shared-venv Python discovery `a54204bc-cc88-447d-8f44-06268e7275a7` completes **1,685 tests, all passing**, in 743.357 seconds. Its supervisor confirms exit zero and process-group closure. The higher count includes the formerly unimportable conformance module. The complete streamed file `5f3b01d2-c36b-4ae9-b174-ad3938b57216` completes **69/69**, with no skips or cancellations, in 226.818 seconds, also with process-group closure. The cause of the earlier lost supervisor record is not established by these later passes; its retained failure is not erased or reclassified as a pass.

The first resumed complete slow selection `11ea9f23-c5bd-4aad-9237-b2eee6b3de6f` reports **124 tests: 121 passes, three failures and no skips or cancellations**, in 296.064 seconds. All three failures are in the full-corpus snapshot file; the explicit validation error names changed `proper-time-and-time-dilation.md` source bytes. Filesystem timestamps and two subsequent SHA-256 observations show that this concurrent chapter changed after runtime generation. All slow process controls, including unresolved-admission termination, pass. After the three chapter hashes stabilize, ignored runtime preparation `c8ec7fbf-9c99-471b-a7c4-68e260029400` completes successfully before the next ordinary sweep; the local MCP fixture's candidate identities follow that exact snapshot without changing its authority fields.

The generated repair has a scratch preview under `.tmp/awt-015-generated-review`; unchanged generators read canonical sources through symlinks or scratch copies, and their writes are confined to scratch outputs. The scene-graph, reading-copy, source-index and startup-router checks all pass there. Canonical regeneration remains pending. The complete target inventory and dependency order appear below; this scratch result does not count as a passing canonical gate.

The next full ordinary sweep is `f2a29ee4-1371-419f-9d38-35c41d5adc84`, selecting all **333** current ordinary files, including the completed concurrent Option B implementation. Its pre-run tracked source/test/corpus diff is `.local-data/awt-015/2026-09-12/resumed-completion/final-node-input-diff.before`, SHA-256 `c67eff9f41aa7d1dc8768abcd18dc0132e1b36996e6a1dd4891b8d17ed28e714`. This scoped snapshot is not an inventory of untracked files; the suite itself discovers the new Option B tests. The planned stop remains 13:24:42 UTC.

### Resumed terminal results and regeneration checkpoint

The following results come from the native runner logs under `.local-data/owned-compute/logs/<supervisor-id>.stdout.log` and `.stderr.log`. Each supervisor lease reports a terminal status and `processGroupClosed: true`.

| Complete selection | Native result | Supervisor ID |
| --- | --- | --- |
| Shared-venv Python discovery | 1,685 tests, all passing; 743.357 seconds | `a54204bc-cc88-447d-8f44-06268e7275a7` |
| Ordinary Node, all 333 selected files | 2,711 tests: 2,708 pass, two fail, one optional skip, zero cancellations; 518.176 seconds | `f2a29ee4-1371-419f-9d38-35c41d5adc84` |
| Slow Node, all ten selected files | 124 tests, all passing; zero skips or cancellations; 297.637 seconds | `a910bd4d-68e7-41eb-893d-6124de37001b` |
| Maintenance-inclusive content integrity | 33 required passes, three required failures, one optional sweep skip, zero unreached checks; 32.8 seconds | `92fcb33e-2e9c-44ff-95ef-cabd1705296a` |

The two ordinary failures are `tests/archie-service-contracts.test.js:15` and `tests/archie-service-source-index-snapshot.test.js:21`. Both native errors identify the stale generated source-index fixture, including its reading-copy digest. All 69 streamed controls pass inside this complete ordinary run. The one optional skip concerns the absent sealed 660-row compact-sweep database; synthetic dashboard controls execute. Integrity's optional Node sweep is separately covered by the complete ordinary run above, without changing the integrity profile or reporting its three required failures as passes.

The three required integrity failures are scene-graph/TOC freshness, startup-router freshness and textbook reading-copy freshness. `git diff --no-index` against independently generated scratch outputs identifies these seven canonical targets:

| Generator | Canonical targets requiring replacement |
| --- | --- |
| Scene graph | `content/graph/textbook_toc.json`; `content/generated/markdown/textbook/toc.md` |
| Textbook Markdown | `content/generated/markdown/textbook/reading-copies/architrino-textbook.md`; `dynamics.md`; `noether-sea-and-effective-spacetime.md` in that same directory |
| Archie fixture source index | `tests/archie-service/fixtures/source-index/source-index-snapshot.v1.json` |
| Startup orientation | `reference/op/agent-startup-orientation.generated.md` |

The startup preview changes only the integrity-script source fingerprint and line count. Source-index generation and its check pass six sources, two graph edges, two metadata records and ten negative cases. Scene-graph and reading-copy checks report zero errors and warnings after the scratch setup includes the existing asset/PDF inputs. The scratch router's source-list extractor passed a known one-path fixture before reading the real source list. No mathematical reference or acceptance setting changes in these previews. They establish reproducible generation at the observed source state, not independent scientific correctness.

Review diffs are retained under `.local-data/awt-015/2026-09-12/resumed-completion/`:

| Preview | SHA-256 |
| --- | --- |
| `generated-markdown-preview.diff` | `6d26f7b391f1cef6577caa67aaa7ca04ea7b9034f9f5fd5b8b89c582a55ed031` |
| `generated-graph-preview.diff` | `42106d8efa8435d1d6def95da26960e7038d8063caef22b0774e5a48d3bc06da` |
| `source-index-fixture-preview.diff` | `41ef6cabb653adf1a71a2512fb9a330a860e6eb43494fd5fec7625d036bf18dc` |
| `startup-router-preview.diff` | `6f9f21fdc895095a633b31f9553bd05c40611544d018f6ab84cccf0e952ef633` |

After explicit regeneration authorization, run the following in order, checking each generated family before continuing:

```bash
node scripts/build-scene-graph.mjs --write --strict
node scripts/build-scene-graph.mjs --check --strict
node scripts/build-textbook-md-pdf.mjs --write
node scripts/build-textbook-md-pdf.mjs --check
node scripts/archie-service/build-source-index.mjs --write
node scripts/archie-service/build-source-index.mjs --check
node scripts/build-agent-startup-orientation.mjs --write
node scripts/build-agent-startup-orientation.mjs --check
node scripts/prepare-runtime-assets.mjs --write
node scripts/prepare-runtime-assets.mjs --check
```

Then refresh only the fixture-only MCP contract's `candidateSnapshotId`, `candidateSnapshotSha256` and `candidateRepositoryRef` from the newly generated full-corpus snapshot; validate the contract while preserving `fixture_only`, `remoteReady: false`, public authorization and accepted-main restrictions. Verify the affected source-index, full-corpus and MCP controls, then rerun the complete ordinary/slow Node gates and maintenance-inclusive integrity against the final state. The passing full Python result remains applicable if its inputs remain unchanged; no speculative repeat is required. Any new source or implementation change requires reassessing that boundary.

`cmp` reports identical before/after/checkpoint snapshots of `git diff --binary -- scripts src tests content/markdown/aaa`, with SHA-256 `c67eff9f41aa7d1dc8768abcd18dc0132e1b36996e6a1dd4891b8d17ed28e714`; this is scoped tracked-byte stability, not an inventory of untracked files or a claim that the shared checkout is clean. `git diff --check` passes over this task's eight code/contract/test files and the AWT owner directory. The earlier lost nested-supervisor record remains retained and unexplained, as described above; later green controls do not establish its cause.

The owned-compute closeout command returns `status: clear` after checking 887 retained leases for task `01a092fa-4fe4-72c3-b7e5-b1ab10902279`. All requested complete sweeps have finished. AWT-015 remains **In progress** at the regeneration checkpoint: the pending explicit decision is required by the Generated Artifacts rule in `AGENTS.md`, and the prepared replacements have not been applied to canonical targets. The canonical dependency-map repair from the separately authorized Option B task passes its controls and needs no replacement by this task. The next observation that would remove the remaining gate obstruction is the named canonical generation checks and complete Node/integrity gates passing after the reviewed batch. No Git publication was performed.

## Earlier attempt before repository actions

The operator selected completion of runtime-contract propagation and evidence-link repair, followed by full validation, and requested an explanation of the separate OPS-030 decision. The starting head is `88568b42ff84cc04149cabab325c8056aac39939`; `git --no-optional-locks status --short` returned no changes at 11:17 UTC. Later concurrent changes to the content-integrity policy and its test remain outside this repair. No publication or scientific execution is authorized by this work.

## Current findings and repair scope

The first integrity run after the merge, `3453a7a8-d90e-40a1-9b4d-d2e8b3275dda`, reports 33 required passes, one required failure, one optional skip and no unreached checks in 42.4 seconds. The earlier missing-evidence links and current-launch-binding failures pass. The remaining failure is generated startup-router freshness; its exact writer is `node scripts/build-agent-startup-orientation.mjs --write`. The concurrent policy edit may change the gate's current selection, so subsequent results must state the selected profile.

The complete `git diff 4a8e760ba HEAD -- src/prescribed-path-analysis/SubfieldCircularRootLedgerReducer.mjs` shows that the merge removed the declaration and loading of `review`, while retaining uses of that variable. The current builder also emits path-only host capabilities, whereas the reducer still requires hashes for its tools and external libraries. Both are incomplete propagation of the accepted runtime-capability boundary. The bounded repair retains byte checks for authored sources, mathematical references, repository headers, built artifacts and stage logs, and validates host-capability structure separately. Build review and root-execution authorization remain external, as the reducer's existing result already states.

Before changing current source admission, `git show 4a8e760ba:reference/priorities/development-process-review/evidence/circular-current-execution/v3-build-review.json` and `jq` inspection confirm that the retained independent review already accepts circular adapter `a06246ca3aac60d500981b19fcffabb9612dc3a4085fc4fb3c441e8839726b7a` and CMake `dc78fe2643e6d7f76cf7787b02133e9815226ff7248aff4c6fec790a528d53f4` for the recorded build. These are the current builder's unchanged subject identities. Selecting these source identities does not accept a newly compiled executable or a new scientific result. The original build receipt is preserved; current-context regression uses a newly prepared build receipt explicitly supplied to the test. Its proof remains the original unchanged manifest-conformance proof, with substitution rejection retained.

The pre-edit caller inventory is `.local-data/awt-015/2026-09-12/post-merge-repair/circular-ledger-consumers.txt`, measured by scoped basename searches in `scripts`, `tests`, `src` and `reference/priorities`. Current operational source pins are updated only after reviewing the corresponding implementation changes; historical records and independent numerical sources remain unchanged.

## OPS-030 explanation

The dependency map records passages supporting a bounded chain of equations and claims. Its candidate repairs eleven stale line selections. Nine selections retain the original text digest; two refer to the same paragraph whose wording now names and links the existing numerical instrument. The earlier candidate trial passes the graph, displayed-prose, existing numerical and polynomial controls, while keeping `approval: not-granted`. Applying the candidate would repair source references, not approve the map scientifically or transfer authority from the existing checks. The operator requested explanation rather than application; the explicit decision reserved in the Operations queue remains pending. The candidate must be checked against the new head before application.

## Validation

Full shared-venv Python discovery started under `9cde9300-6cb9-4364-820b-dbc0fe4010ae`. The fresh circular adapter build `73f2d7eb-9980-444a-aa7f-dc414a6f73f3` completed its declared build-only path, with no root calls or execution authorization. Full ordinary Node started under `a5815b7c-109e-4f38-af4e-828b7412572e`, enabling the process controls, current-context control and freshly built adapter. These first runs are baseline measurements; a later repair requires appropriate verification of the changed final state.

## Repairs and independent boundaries

The first complete ordinary Node run reports 2,610 passes, 25 failures and one skip over 2,636 tests in 351.517 seconds. One streamed test file failed during module setup, so its lower test census is not evidence of complete coverage. The first Python discovery reports 1,653 tests with eleven failures and fourteen errors in 711.047 seconds. These are the native runner totals; the corresponding supervisor logs retain their full failure records.

The reviewed repair changes the following contracts:

| Area | Change and verification boundary |
| --- | --- |
| Circular build/context | The reducer accepts the current builder's path-only host capabilities while retaining authored-source, mathematical-reference, repository-header, executable and stage-log byte checks. Twenty-three current-context controls pass with the explicitly selected new build and original proof. The historical pilot receipt correctly rejects changed preparation source; it was not silently replaced with an unreviewed fresh build. |
| Current operational callers | Three retained preview records identify exact before/after digests and changed lines. The preview first passed the independently known SHA-256 `abc` case and a two-level dependency fixture. Only reviewed current dependencies were applied. The third preview's proposed `CONTINUATION_MATH` replacement was expressly excluded because it is a historical mathematical binding. |
| Python entry points | The continuous and refined preparation functions retained late uses of `project_paths` after the migration removed its initialization. The obsolete host-module closure predicates were removed; authored-source rechecks, deadlines, publication retraction and cleanup remain. The parent producer and verifier now validate bounded, unique, normalized absolute path-only runtime records and reject aliases to authored sources. Fifteen focused plan/interface controls pass. These parallel metadata-validator changes test the runtime contract, not a mathematical rule. |
| Historical comparisons | Exact original authored source files were restored from Git only after matching their existing retained SHA-256 expectations. The full-entry literal-pin tests use the original entry archive; cached full-composition controls pass 34/34. The refined replay manifest retains its original digests and explicitly routes changed controls and mathematical sources to archives. The parent legacy parity test also reads its original producer source. No old receipt is relabeled as current execution. |
| Refined reference pair | The merge selected the later `6e3467a0…` mapping while retaining the original `7574dc0f…` core, whose own required mapping is `23a9d66b…`. The refined preparation, checker and historical pilot now consistently name the original pair. A focused real captured-reference control passes. Numerical function bodies and the independent mathematical oracles were not changed by this repair. |
| Current execution controls | Private synthetic plans bind current authored execution and test files while preserving the retained example plans, historical routes, scope and scientific claims. These tests exercise real capture/publication with explicitly artificial numerical callbacks. They do not execute or independently accept a scientific campaign. |
| Streamed fixtures | The historical transport remains `e2df205f…` and is copied from its explicit archive into the isolated test fixture. Current adapter/control bindings follow the reviewed interface changes. A Python fixture containing lowercase JavaScript `true` was corrected to `True`. The retained-selection positive and unconsumed-input negative controls then pass. |
| Replaced-source cleanup | The retained `postpublish` fixture shows the worker rejecting `original postcleanup source replaced`, public output absent, lock retained, and the independent owner closing all observed processes/groups at its 15-second deadline with `SIGTERM`. The test now requires that specific rejection and closure boundary instead of assuming ordinary exit code one. This is bounded external termination, not successful coordinator self-certification. |
| Unresolved admission deadline | The slow control initially expired its 600 ms work allowance during process inspection, before entering the intended callback. Its isolated fixture now provides five seconds for startup while preserving the implementation's fifteen-second cleanup reserve, records callback entry, and requires termination at the original twenty-second boundary. The focused control passes in 20.214 seconds. Production timing and mathematical limits are unchanged. |

The final pre-sweep selection of current launch bindings, acceleration/emission/refined controls and circular pilot controls passes **102/102** in 7.774 seconds. This focused pass does not replace full-suite completion.

## Current gate obstruction and pause request

The operator requested a pause at the next checkpoint for repository actions. No further repair batch starts after that request. The already running ordinary Node sweep is `2f43facd-a07d-445e-9c49-509ee39302a7`; full Python discovery is `a11857cc-6a23-4c0e-a0ff-e3e0dd92624a`. Their terminal results and owned-process closeout belong below before handoff.

The maintenance-inclusive integrity run `5203b55e-c458-4370-b6d7-b3e95f4fd088` stops at its first prerequisite: `equation-corpus: corpus-equation-cd180315de09143c: missing canonical source link`. It reports zero required passes, one required failure and 34 unreached checks in 20.5 seconds. The concurrent [self-interaction review](../../aaa-corpus-rewrite/evidence/crw-005-self-interaction-switch-review-2026-09-12.md) records the same new-display link and registry drift. Its authored chapter is outside this repair. The required subsequent writer is `node scripts/build-equation-mapping-corpus.mjs --write`, followed by `--check` and `node scripts/prepare-runtime-assets.mjs --write`; the equation writer edits authored links and therefore remains with the settled corpus/publication workflow. The startup router was regenerated and its check passed before this gate.

The complete slow run `2ddd6e07-0c65-494e-991d-0ef2c2862113` has five failures: three full-corpus snapshot checks, fresh-checkout runtime preparation, and the unresolved-admission control subsequently repaired and focused-tested above. The first four remain obstructed by the concurrent corpus/generated state. The exact full-corpus writer is `node scripts/archie-service/build-full-corpus-source-index.mjs --write`; it is also part of ignored runtime preparation after the equation prerequisite is repaired. No focused rerun is reported as a green complete slow suite.

AWT-015 remains open until the complete owner-defined gates pass. OPS-030 remains unapplied. Historical source replay is not a new current scientific launch profile: in particular, the streamed continuation still names its separately reviewed original mathematical and transport generations. The remaining-caller owner must retain that distinction when selecting executable inputs; passing synthetic lifecycle tests alone does not make the historical campaign ready to run.

## Checkpoint results

The terminal ordinary Node sweep reports **2,704 tests: 2,691 passes, twelve failures, one skip and zero cancellations**, in 579.682 seconds over all 331 ordinary files. Supervisor `2f43facd-a07d-445e-9c49-509ee39302a7` reports exit one and `processGroupClosed: true`. The failures are ten tests in `tests/equation-dependency-map.test.mjs` for the pending OPS-030 map; one generated equation coverage test in `tests/equation-mapping-corpus.test.js`; and the `postpublish` case in `tests/f6c-streamed-leaf-diagnostic.test.js`. The absent sealed compact fixture remains the sole declared skip.

The final `postpublish` case reaches the specific original-source-replacement rejection and observed process closure, then fails the assertion `uncertified cleanup retains its lock`: this run released the lock in 1.851 seconds. That result overturns the repair's unconditional retained-lock expectation, derived from the separately retained deadline-termination case. The next review must distinguish successful ordinary cleanup from external-deadline fallback and assert the corresponding lock boundary. It must retain the source-replacement, failed-completion, public-output and observed-process checks. The final stdout/public-output assertions in this particular test body occur after the failed lock assertion and therefore were not executed. No further repair is made after the operator's pause request.

The complete slow suite reports **124 tests: 119 passes and five failures**, with zero skips or cancellations, in 257.387 seconds. Supervisor `2ddd6e07-0c65-494e-991d-0ef2c2862113` records exit one and process-group closure. Its later unresolved-admission focused control passes 1/1 under `5c6a3a51-1885-4350-9cb5-9be775964b38`; the complete slow suite has not been rerun after that fixture change.

The source/test change snapshot is `.local-data/awt-015/2026-09-12/post-merge-repair/source-and-test-changes.diff`, SHA-256 `dea65ada38567f2146c09d6576ebfd59b183c33ab14d02c37150eb133efe2229`. It is the output of `git diff --binary -- scripts/eom src/prescribed-path-analysis tests ':!tests/content-integrity-reporting.test.js'` and excludes the concurrent integrity-policy test. `git diff --name-only -- scripts/eom/oracle` returns no changes: this task did not edit mathematical oracle files. Restored original source bytes, replay routes, tracker records and the generated startup router are recorded separately from this code/test snapshot. No staging, commit, push or PR action was performed.


The terminal shared-venv Python discovery reports **1,662 tests, one failure and one import error** in 770.106 seconds. Supervisor `a11857cc-6a23-4c0e-a0ff-e3e0dd92624a` records exit one and process-group closure. The import error in `tests/test_f6c_refined_acceleration_conformance.py:33` checks the current canonical mapping against the retained `6e3467a0…` generation after the current execution repair selected `a0c54612…`. Its test bodies were not executed; the total is not full coverage. The exact `6e3467a0…` source was preserved in `reference/priorities/braid-program/evidence/source-replay/verify-f6c-continuous-reception-acceleration.6e3467a017c3.py.source`; the next repair must give this historical comparison its explicit original reference, rather than globally replacing its expected digest. The other failure, `tests/test_f6c_variable_cell_adapter.py:1059`, correctly parses the original full-entry archive and then incorrectly compares its original `19c57e9b…` root-checker pin with the current adapter's `3221c44e…` checker generation. The resumed control must keep those two generations separate.

`node scripts/dev/owned-compute-supervisor.mjs closeout --owner-task "$CODEX_SESSION_ID"` reports **clear**, checking 867 retained leases for this task. All current runs are terminal; no computation remains owned by this task. Code edits stopped at the operator's pause request. Only this checkpoint and its queue/log pointers were finalized afterwards. The task is paused for operator repository actions, with no publication performed and AWT-015 still **In progress**.

The next executable repair is the three test-boundary issues above: ordinary versus deadline-fallback lock disposition, explicit original mapping selection for conformance controls, and original versus current root-checker identity in the literal-entry test. Then rerun the affected controls and complete gates after the corpus generation is settled. OPS-030 requires its separate explicit decision and a fresh candidate review at the resulting repository head. These are distinct obligations; neither a source hash refresh nor a successful lifecycle fixture grants scientific acceptance.
