# AWT-015 residual validation

## Authorized closure-repair continuation

At 2026-09-12 01:41:39 UTC the operator selected the ordered closure-repair effort, through completion or a one-hour checkpoint at 02:41:39 UTC. This extends repair scope to the previously reported ordinary failures and required source-impact reviews. It does not grant publication or scientific acceptance. The new baseline is retained under `.local-data/awt-015/2026-09-12/closure-repair/`; concurrent source and process-owner changes remain protected.

### Current Master Equation binding review

Before refreshing App Solver's current binding, `git diff d8370991f^ -- content/markdown/aaa/dynamics/master-equation.md` establishes that the accepted source differs only in causal-surface terminology. `shasum -a 256` reconfirms current source identity `bb7357868a4f900aa566b8a43107d111dcd435cab677361f9bba3dceaadb384b`. The complete binding was read and every section compared with that source transition: causal constraint and strict past domain; positive-separation definitions; signed playback; sharp transmitter-only weight and per-root sum; finite-width constant multiplier and sharp limit; retained-history requirements; singular-event dispositions; all four machine fields; independent static-transmitter, playback-turn and quadratic-history controls; and the non-claims. All formulas, assumptions and status-bearing statements remain unchanged across the source transition. The renamed surface still has radius $c_f(T_r-T_t)$ around the emission position, so its intersection condition and Jacobians are unchanged. This is a semantic-equivalence review at maintenance grade; it neither proves the physical law nor accepts a new scientific result. Only the current binding's source digest is eligible for refresh. Historical certificates keep their separate recorded source identities. Borg tests and strict scene/content checks remain required validation.

### Repairs and focused verification

The current-binding review above preceded the App Solver digest-only refresh. `node --test --test-concurrency=1 tests/borg-*.test.js` passes all 256 tests, zero failures/skips, in 13.169 seconds under supervisor `2cd4bd70-8ada-46e3-87ee-55a813d5db22`. Strict content/scene validation remains part of the complete rerun.

The [historical source-replay manifests](../../braid-program/evidence/source-replay/README.md) recover exact input bytes instead of refreshing scientific expectations. F5's four original authored inputs were recovered from `897fe1aa7^`, and its accepted enclosure report retains its pre-existing digest. The orthogonal-plane Master Equation was recovered at `bfbb3ea3e`, with SHA-256 `0e8219c7a3cc750747c43030d5b998ddbf2de525a5469d32fa5e52fbae5ec63f`. F6c's two scientific implementations were recovered at `6bddaa9802684c96ea51a5483733b3f21ff4fc15` and its two operational helpers at `713c53a0acf5c8c78002873d9ebc8887bd8d4bcc`. This task preserves historical scientific digest constants, numerical validators, certificate code and oracle code. Later current-orchestration changes and their separately reviewed dependency identities are recorded below. The new bounded materializer's two known/negative controls passed before target use. Its Python and Node helpers reconstruct disposable test inputs; they do not alter the live display configuration or authorize BP-018/BP-019 runs.

Native focused results are 41/41 F5 Node tests in 20.261 seconds (`e69c8708-11bd-4ad8-9a51-508377c57208`), 40/40 F5 Python conformance tests in 4.678 seconds, 7/7 orthogonal-plane certificate tests in 3.915 seconds (`c0fa91ff-01e2-4182-aa0c-35d349cd2f43`), and 31/31 F6c pilot controls in 4.159 seconds. The first restored F5 Python run exposed two mock targets that still pointed at the checkout module; those targets now patch the unchanged module loaded in the isolated replay. The subsequent full focused run passes. Certificate receipt reproduction is a determinism check, not new independent evidence.

BP-007's already-approved retirement is now enforced before both coordinator and worker execution. Its eight focused tests pass, covering both rejection routes, unchanged historical pins, protocols and V2 guards. Historical artifact verification remains available. The Braid queue closes retained executable maintenance while preserving the named-consumer reopening condition; no calibration was launched.

Operations' dependency/action contract was reviewed against the existing package lock and actual workflow/composite-action files. The policy now records the four existing direct dependencies, 653 locked production entries and the already-used upload-artifact action SHA. `npm audit --package-lock-only --audit-level=moderate --omit=dev` reports zero vulnerabilities. Every lock entry has integrity metadata by the bounded Node inventory. The action-source collector's known local/external fixture passed before repository use; the seven focused policy tests then pass, including composite actions and local action-path checks. No package or external action version changed.

The equation-corpus tests replace fixed whole-corpus totals with independent display-delimiter and filesystem counts, while preserving the promoted-equation expectations. The new census passed a hand-counted four-display fixture before corpus use. The canonical corpus generator added 23 missing source links without changing displayed mathematics, then scene graph and reading copies were regenerated before Archie source and full-corpus snapshots. The authored remote-deployment fixture has no generator; its three local candidate identity fields were refreshed explicitly from the rebuilt full-corpus snapshot, preserving unaccepted-main and remote-not-ready status. The MCP tool and stdio fixtures use their canonical writers and retain `mcp-tool-contract/v1`. The 28 focused Archie contract/loopback/deployment tests pass. Final generator checks remain required after the dependency-ordered refresh.

### Later concurrent orchestration review

The 02:17 production-root regression exposed a later change to `scripts/eom/prepare-f5-enclosed-root.mjs`: its SHA-256 is now `3431be1ca2f17474775572358baa88eae8de3ce93403d58e4b1fa36d9e367d50`. The inspected complete Git diff removes external compiler/library/interpreter hashes and associated host rechecks, changes the toolchain record's stated authority, and retains authored-source checks, built-artifact checks, actual history generation, the unchanged independent conformance invocation and proof receipt checks. No root, interpolation or oracle calculation changes. This is a current orchestration change, not a historical scientific input. The test-replay manifest may bind this current orchestration generation after this review; its frozen scientific inputs and numerical implementation digests remain unchanged. That refresh makes the current orchestrator available to its existing regression tests. It does not ratify the altered production toolchain-record contract or supply a fresh build/proof acceptance; those remain with the active source owner. The initial production-root regression failures are retained and must not be presented as passing transport execution.

### Current build-helper dependency review

Before synchronizing current build-helper dependencies, the complete diff of `prepare-subfield-circular-root.mjs` was reviewed alongside the F5 orchestration diff above. The change records host tools, SDK headers and external libraries as current capabilities. It retains exact authored-source/reference checks, repository-header bindings, compile flags, compiler dependency enumeration, built-artifact identity, stage-log identity and the `build-recorded-pending-independent-review` result. The two current F5 builders and the current prehistory build launcher still name the previous orchestration/helper digests, so their source-snapshot admission fails before any compilation. The bounded repair updates only those current dependency identities in dependency order. It does not change numerical sources, old receipts, their hashes, independent mathematical references, or the build-review acceptance boundary. A fresh build may support the adapter's no-root plumbing controls; it is not a new accepted scientific build or an authorized root campaign.

### OPS-030 reviewed candidate

The exact-head review at `4a8e760bae44dbc868714e579ba60779ae0be9d2` locates eleven stale Master Equation selector occurrences. Nine bind unchanged text shifted six lines earlier; two bind a paragraph whose only textual change adds the existing numerical instrument link. The candidate preserves nine original digests, refreshes the two changed paragraph digests and propagates the affected revision identities. Its namespace, historical comparison baseline and `not-granted` acceptance status remain unchanged. The locator passed known unique/duplicate controls before source use; the reader's existing preflight also passed.

Supervisor `c5184750-d995-4934-8b4d-440671106166` executes the candidate trial. Graph consistency, existing numerical verifier, displayed-prose check, existing scientific test and finite-ledger polynomial controls pass. The result remains `review-required`, `approval: not-granted`; it does not accept either dependency chain. Candidate and review records remain at `.local-data/awt-015/2026-09-12/closure-repair/moving-single-root-dependencies.candidate.jsonld`, `option-b-candidate-review.json` and `option-b-candidate-trial/report.json`. OPS-030 expressly reserves hash refresh to an operator decision. A concrete approval request was sent after these checks; the canonical map is unchanged pending that answer.

### Completed rerun and later repairs

Full shared-venv Python discovery under `436af35e-d587-4534-a4ab-f2e362aea4c7` reports **1,653 tests, ten failures and fourteen errors**, in 703.980 native seconds; the supervisor records exit one and process-group closure. The original F5 and orthogonal-plane failing locations are absent from its failure summary. All reported failures/errors are F6c acceleration, emission, refinement, variable-cell or single-leaf/streamed diagnostic consumers. One import failure and one teardown error remain among the reported errors. Concurrent test deletions and newly reachable F5 bodies change the test universe, so its count must not be subtracted from the previous run to claim improved coverage.

Ordinary Node under `e6015643-d5ff-4542-9045-53a3242114f8` reports **2,703 tests: 2,621 passes, 81 failures, one skip, zero cancellations**, in 351.007 native seconds over 331 files. The supervisor records exit one and process-group closure. This run enables `F5_RUN_PROCESS_CONTROLS=1`, `AAA_CIRCULAR_CURRENT_CONTEXT=1` and the prior current-v3 circular adapter binary. Nine F5 process controls execute; the circular profile exposes an old-binary freshness failure and its six dependent failures, plus the separate current-context failure. The sole skip is the sealed compact-v2 database, absent at its declared path; no V1 database is relabeled. The native failure summary grouped by the previously checked `rg '^test at '`/`cut`/`sort`/`uniq -c` pipeline gives these 81 locations:

| File under `tests/` | Failures | Disposition after this run |
| --- | ---: | --- |
| `current-launch-bindings.test.js` | 1 | Current F6c bridge and later shared outer-launcher changes require current-contract propagation. |
| `equation-dependency-map.test.mjs` | 10 | Checked OPS-030 candidate awaits operator decision. |
| `f5-enclosed-root-current-build.test.js`, `f5-prehistory-handoff-build.test.js` | 2 | Reviewed helper dependency synchronization above; the later 18-test selection passes. |
| `f6c-acceleration-pilot.test.js` | 1 | Current composition/source-generation mismatch. |
| `f6c-cached-root-cover-full.test.js` | 2 | Literal retarget-only composition expectations no longer match the concurrent runtime-capability changes. |
| `f6c-cached-root-cover-pilot-launcher.test.js` | 1 | Same exact-composition obligation. |
| `f6c-emission-refinement-pilot.test.js` | 1 | Current source-generation mismatch. |
| `f6c-evidence-packaging.test.js` | 3 | Current packaging/declared-source controls require reconciliation. |
| `f6c-refined-acceleration-pilot.test.js` | 1 | Two newly recovered replay files disappeared during the run. They were restored from their recorded Git blobs and exact hashes; the repeated 31-test selection passes. Their removal cause is not attributed. |
| `f6c-streamed-leaf-diagnostic.test.js` | 46 | Current dependency pins and admission controls fail after the runtime-capability source changes. These are 46 native failing locations, not 46 independently established defects. |
| `subfield-circular-current-context.test.js` | 1 | Existing reviewed-context admission remains separate from plumbing controls. |
| `subfield-circular-memory-owner.test.js`, `subfield-circular-observation-owner.test.js` | 2 | Current owner dependency assertions. |
| `subfield-circular-root-adapter.test.js` | 7 | Fresh-build prerequisite and dependent setup failures; all seven later pass with the newly selected binary below. |
| `subfield-circular-root-pilot.test.js`, `subfield-circular-root-preparation.test.js`, `subfield-circular-root-rung.test.js` | 3 | Preparation is repaired in the later helper selection; pilot/rung admission remains separate. |

The ten-file slow selection `c10d3b43-1ba1-4b59-ab33-17ce22532a50` reports **124 tests: 123 passes, one failure, zero skips/cancellations**, in 247.664 native seconds. The failure is `runtime-asset-fresh-checkout.test.js`, whose index-based export tried to copy the concurrently deleted `2026-08-28-f5-unattended-certification-session.md`. The export now includes working-tree additions and deletions, using Git's explicit deleted-file inventory rather than silently skipping arbitrary missing files. It checks that deleted paths remain absent and retains every runtime/source/deployment assertion. Focused run `223ef2a5-5fbe-4f99-8d5a-f1933b6b738a` passes in 63.532 native seconds, reconstructing 146 runtime outputs and a complete 5,491-file Pages payload. This focused correction does not rewrite the preceding slow-suite result.

Content-integrity run `b2d1b5a1-14cb-4948-b8f9-98088c98d87e` reports 31 required passes, three failures, one optional reporting-sweep skip and no unreached checks. The two notation findings are resolved by clarifying the transmitter-position definition and using $T_r$ for the radial example's selected reception event, preserving its generic trajectory parameter $T$, mathematics and claim boundary. The terminology checker passes. The next full integrity run `502c850b-7d53-41c2-8c7f-d58638b1659d` reports **32 required passes, two failures, one optional reporting-sweep skip, zero unreached checks**, in 31.5 seconds. Generated scene, reading-copy, equation-corpus and source-index checks pass. Remaining failures are 15 links to ten missing evidence files and supported launch profiles' current repository bindings. The deleted files were not deleted at the continuation baseline, by its saved status inventory; current `git --no-optional-locks status --short` records their deletions. Their deletion purpose is not inferred and their authored content is not restored by this repair.

The existing F5 production input-tree transport now captures all new regression-helper dependencies. After the current orchestration review, a fresh 93-file root under `.local-data/f5-original-input-trees/awt-015-20260912-test-transport-reviewed` passes all 41 Node preparation/prefix/reducer tests in 20.004 seconds (`8692c7a6-35ab-412d-91d7-beba235075bf`) and 32 Python tests in its two-file conformance profile in 4.473 seconds (`51039114-6b62-422b-a1af-13d68b3512e6`). That profile excludes the separate eight-test actual-cubic file included in the earlier 40-test checkout selection. These captured-root checks do not run a new scientific campaign.

The current-helper focused selection passes 18 tests; dependent F5 launch/startup/admission tests pass 12/12 in 10.993 seconds (`694397ba-77a4-4253-9c68-ee34e0a77238`). Fresh build `beb4bd66-e46f-42d5-8778-c450f5639e40` completes in 19.794 seconds with status `build-recorded-pending-independent-review`, zero root calls and no root-execution authority. It preserves the old build and writes only `.local-data/braid-analysis/subfield-circular-root-pilot-20260827-v1/awt-015-control-build-20260912`. The seven adapter plumbing tests pass in 8.173 seconds with its explicit binary (`ee4c4121-5338-4372-ac2b-b7c2e8f75799`), including fixed-grid/zero-coordinate controls, unchanged manifest bytes, FIFO rejection and refusal of an unaccepted proof. They invoke no root API and supply no scientific build acceptance.

Final ordinary Node run `8775408d-2f7d-4aa7-a6a4-d8c97c4b9499` completed at 02:30:04 UTC with all three opt-in profiles and the fresh adapter binary. The native runner reports **2,703 tests: 2,594 passes, 108 failures, one skip and zero cancellations**, over 331 files in 270.818 seconds. Supervisor status confirms terminal `failed`, exit one, 271.019 seconds and `processGroupClosed: true`. The sole skip remains the unavailable sealed compact-v2 database. This terminal result supersedes the earlier 81-failure continuation result for this selection; it is still an observation of a concurrently edited checkout, not an immutable candidate.

The previously checked native-summary grouping reports 108 failing locations in 19 files. They group as follows; this grouping is navigation, not an assertion of 108 distinct defects.

| Remaining group under `tests/` | Failing locations | Concrete next obligation |
| --- | ---: | --- |
| `current-launch-bindings.test.js` | 5 | Reconcile all five current profiles with the reviewed final repository-source generation. Four first reject the shared outer launcher; the acceleration profile first rejects its Python executor. |
| `equation-dependency-map.test.mjs` | 10 | Apply the checked candidate only after the reserved OPS-030 operator decision, then rerun its existing tests and trial. |
| F6c acceleration, emission-refinement and refined-acceleration process files | 29 | The 9/10/10 controls reject `registered supervisor generation differs` before their intended synthetic process stages. Complete the shared launcher contract migration through these callers. |
| `f6c-streamed-leaf-diagnostic.test.js` | 46 | Its fixture rejects current dependency identity before most diagnostic controls. Review the final current implementation and its fixture contract together; do not refresh historical scientific expectations. |
| Other F6c pilot, bounded-operation, cached-root, evidence-packaging and parent-refinement files | 13 | Resolve current composition, source admission and packaging assertions in the existing owners. Native log file/line locations identify each remaining assertion. |
| Circular current-context, memory-owner, observation-owner, root-pilot and root-rung files | 5 | Current-context requires a reviewed build generation; memory/observation expectations still require host-runtime binding fields being removed by the concurrent migration. Plumbing success supplies neither production-context acceptance nor that contract decision. |

The complete `git diff -- scripts/eom/launch-subfield-circular-root-pilot.mjs` at 02:35 UTC shows the concrete shared change: external Node/`ps` byte bindings are removed, the receipt changes from `runtimeBindings` to `runtimeCapabilities`, and publication checks retain repository sources while omitting those external tools. The final Node log records actual launcher digest `e25de9683772ac3efde61050ae054f2f27ad921c2af03c29fc984cabc2aa3920` versus callers' expected `58f5fa058727e212cc98a32f04eb3d94c64c6a8185f9cc8a8114d9a034343b8c`. This directly establishes the mismatch and changed receipt contract; it does not establish which task authored the transition or grant permission to overwrite its ongoing migration. The final log also records executor `84857be9d28c0debc6a6846140fb5cd15b1bcc527b4870d7c080f54d6e17ebb3` against expected `c67359fbf8ffeee9bb6d6fc2887c4a35dc7bf4b6af7da4d7577ef017e42ce789`. The required repair is reviewed propagation of the final current contract and its tests, rather than blanket acceptance of changed bytes.

### Checkpoint handoff

AWT-015 remains **In progress**. The remaining ordered work is current runtime-capability contract propagation, disposition of the ten missing evidence files and their 15 incoming links, the OPS-030 decision, then complete ordinary/slow Node, shared-venv Python and content-integrity reruns on settled inputs. The exact full commands remain `node scripts/run-test-sweep.mjs`, `node scripts/run-test-sweep.mjs --slow`, `"${AAA_VENV:-../.venv}/bin/python" -m unittest discover -s tests -p 'test_*.py' -v`, and `node scripts/check-content-integrity.mjs`, under owned supervision. Preserve all three ordinary-suite opt-ins and select a fresh declared adapter binary. Passing focused controls do not close this boundary.

The final replay audit at `.local-data/awt-015/2026-09-12/closure-repair/replay-final-audit.json` first passes the known SHA-256 `abc` case and then verifies every declared input in the three manifests: 21 F5 files, seven orthogonal-plane files and 19 F6c files. All source digests match. `node scripts/validate-machine-artifact-retention.mjs` passes index and working-tree file, collection, branch and generated-output checks over 1,491 machine files. Final equation, reading-copy and full-corpus checks pass in the retained `*-final-check.log` records; these establish their recorded source snapshot only. A later mismatch or a failed final-state full gate falsifies any current completion claim. No scientific acceptance or Git publication occurred.

The retained `final-node-failure-locations.txt` contains all native failure-summary locations, and `current-caller-binding-handoff.txt` contains exact occurrences of the two mismatching current digests under `scripts/eom/` and `tests/`, measured with literal-hash `rg` searches. These are scoped navigation inventories, not blanket refresh authorization. In particular, the later shared-outer change also reaches the prehistory build launcher whose earlier helper-only checks passed; those earlier focused checks do not cover the later outer-launch path. `checkpoint-shared-status.txt` and `checkpoint-shared.diff` preserve the shared working state for comparison, not attribution of every edit to this task. At 02:36 UTC, `git diff --check` passes and `node scripts/validate-priority-ranking.mjs` passes 26 active owners and 14 ranked rows. The supervisor's owner-specific closeout returns `status: clear`; no owned computation remains live. The initial sandboxed closeout could not spawn its process inspection; rerunning the same command with approved host access produced the clear result.

The final integrity log's 15 missing-link diagnostics are retained verbatim in `final-missing-evidence-links.txt`. They name ten distinct targets; `git ls-files --deleted` on those ten explicit paths returns all ten. This corrects the earlier status message's six-link/five-file count, which described an earlier observation rather than the final integrity log. Their replacement, relocation or historical retention must be resolved against the deletion owner's intended disposition before changing incoming provenance links.

## Original residual-validation attempt (historical)

The sections below retain the first attempt's scope, failures and handoff. Their “unchanged” and “remaining” statements describe that earlier attempt and are superseded only by the explicitly measured continuation above.

## Scope and baseline

The operator selected AWT-015 residual repair and complete validation, with no publication or changes to concurrent CRW-005 work. `git --no-optional-locks status --porcelain=v1`, `git diff --binary`, `git rev-parse HEAD`, and `git diff --name-only -z | xargs -0 shasum -a 256` recorded the baseline in `.local-data/awt-015/2026-09-12/`. The baseline contains 13 modified CRW-005 corpus/review files. No layout move is repeated. Existing scientific receipts retain their historical bindings.

Baseline HEAD is `4a8e760bae44dbc868714e579ba60779ae0be9d2`. This task's authored changes are limited to:

- `src/prescribed-path-analysis/AllCandidateAnalyticalCampaign.mjs`: exact dated review token.
- `src/prescribed-path-analysis/analytical-measure-coverage.v2.json`: reviewed source identity, token and explanation.
- `reference/priorities/aaa-work-threads/priorities.md`: current blocker and evidence routing.
- `reference/priorities/aaa-work-threads/work-queue.md`: live AWT-015 validation state, preserving its acceptance boundary.
- `reference/priorities/aaa-work-threads/work-log.md`: dated repair and validation record.
- `reference/priorities/aaa-work-threads/campaigns/data-file-layout-completion.md`: current residual-only boundary and supported Node command.
- This evidence record.

Claim grade: measured for the named command results and source-byte comparisons; inferred for the explicitly bounded semantic review. No scientific acceptance or publication follows from the maintenance.

## Methodology impact review before digest refresh

The September 8 [29-measure review](../../development-process-review/analysis/semantic-closeout-review.md#g9-methodology-impact) resolved the older methodology transition. Current admission has since diverged again. `git show d8370991f^:content/markdown/aaa/noether-braid/braid-analysis-methodology.md | shasum -a 256` returns the coverage contract's accepted `611b89ce6a099571a54acbf76730925a9238438754c0f7c2c61419cfa3ee83bc`; `shasum -a 256` on the current chapter returns `faf2c9e4a78036ede08bfd3e2a531b5a3b2e4a11233bb1fd0b0eea9ff964f609`. The complete `git diff d8370991f^ d8370991f -- content/markdown/aaa/noether-braid/braid-analysis-methodology.md` contains exactly one sentence edit, replacing “delayed causal isochrons” with “delayed causal wake surfaces.” This establishes the mismatching transition directly, rather than attributing it by last touch. It is later terminology maintenance, not a layout-induced mathematical change.

Manual review of that sentence, its causal-root definitions, all 29 rows of [the coverage contract](../../../../src/prescribed-path-analysis/analytical-measure-coverage.v2.json), and the [terminology table](../../../../content/markdown/aaa/archie/terminology-usage.md) finds no analytical coverage change. The replacement identifies the same sphere of radius $c_f(T-T_t)$ centered at the transmitter's emission position. Its intersection with the probe still satisfies $g_j=r_j-c_f(T-T_t)=0$, with $T_t<T$ and simple-root factor $D_{t,j}=c_f-\widehat{\mathbf r}_j\cdot\mathbf V_j$. Thus the causal-root ledger, identity/transversality and pointwise-wake rows retain the same mathematical inputs and reduction. Every other paragraph, formula and coverage obligation is byte-identical across the inspected transition. Period, separation, receiver ledgers, residual screens, exposure, flux, modal coefficients, sensitivity, convergence and grading therefore retain the September 8 dispositions and independent references. This is a source-statement impact review, not a new numerical or physical acceptance.

The authorized repair advances only current methodology admission: its source digest, the dated `impactReview` token, the explanatory note, and the validator's exact required token. All 29 measure rows, allowed statuses, reductions, raw-evidence requirements and independent oracles remain unchanged. The existing changed-byte and wrong-token negative controls must continue to reject. Historical campaign/calibration bindings are not refreshed. Any additional source difference, changed equation/domain, or accepted wrong-token/changed-byte control falsifies this disposition.

## Focused validation and content integrity

The initial three-file Node selection (`all-candidate-analytical-rebuild`, `borg-scientific-status`, `borg-platonic-relationships`) returned 21 passes and five methodology-admission failures. The shared-venv FSC T0 suite returned five passes. These focused results are not overall health.

The post-repair command `node --test --test-concurrency=1 tests/all-candidate-analytical-rebuild.test.js tests/analytical-campaign-database.test.js tests/endpoint-residual-search-campaign.test.js tests/coincident-midpoint-4-2-1-frequency-and-coaxial-separated-two-planar-braid-co-rotating-resolution-coverage-calibration.test.js` reports 30 tests, 29 passes, one failure, no skips or cancellations, in 16.976 seconds. The sole remaining failure is the frozen calibration implementation identity: expected `7cceed6734253268c47ec53bfa81fcd204a9db626816825ff0e78b657dd47c65`, actual `c03cdbd37165368d04f92bd90882143b02eaa29eaf6198f6d7a19a7aca0cfdb8`. This is the exact unresolved runner/test disposition recorded by the [Braid Program owner](../../braid-program/work-queue.md#routed-methodology-coverage-review-from-ops-018). No calibration hash, scientific input or acceptance assertion is changed. The changed-byte and wrong-review-token controls pass within the methodology test.

`node scripts/check-borg-scientific-status-projection.mjs` passes its 28 adjudication rows; shared-venv `scripts/borg/verify-platonic-relationship-assignments.py` passes its 145 exact registry rows (six assigned, 139 fail-closed unassigned). `node --test --test-concurrency=1 tests/borg-scientific-status.test.js tests/borg-platonic-relationships.test.js` passes all 17 tests with no omissions. These instruments confirm current contract consistency and exact-assignment behavior for the two earlier repaired pins; they do not independently reaccept the underlying scientific campaigns.

Content integrity, supervised run `7cdb9876-8916-47e6-830d-8a9ea0a01de2`, reports 32 required passes, two required failures, one optional sweep skipped and none unreached in 37.2 seconds. Both browser/release checks pass. The failures are scene-graph freshness (`content/graph/textbook_toc.json`, `content/generated/markdown/textbook/toc.md`) and reading-copy freshness (`architrino-textbook.md`, `dynamics.md`, `noether-sea-and-effective-spacetime.md` under `content/generated/markdown/textbook/reading-copies/`). These are not the OPS-017 exception. The commands for an authorized regeneration after corpus editing settles are `node scripts/build-scene-graph.mjs --write` and `node scripts/build-textbook-md-pdf.mjs --write`, followed by their corresponding `--check` commands and the full gate. This task does not regenerate these tracked outputs from the concurrent corpus batch. The gate itself prepares only its declared ignored runtime outputs.

The initial default-discovery Node command was `node --test --test-concurrency=1 --test-timeout=600000`, supervised run `17e53cbf-7bc1-42b8-b866-76f024b703ce`; the first historical-form attempt with `tests/` failed at module resolution before discovery, run `858a2714-297c-4453-a6ea-7b3689923f1f`. Python used `../.venv/bin/python -m unittest discover -s tests -p 'test_*.py' -v`, run `a82198d7-1dfe-49d1-a8d8-c3bbb38a8e03`, on the executable shared Python 3.13.2 venv. Both initial selections had one-hour supervisor deadlines and 15-second heartbeats. The initial Node run began before the methodology repair and before content integrity prepared ignored runtime assets; it was stopped as documented below and has no complete-suite result. Every native log remains under `.local-data/owned-compute/logs/<runId>.stdout.log` and `.stderr.log`.

`node scripts/run-test-sweep.mjs --list` and `--slow --list` enumerate 330 ordinary and ten slow Node files; `rg --files tests` with the Python test-name suffix enumerates 78 Python files. These are discovery inventories, not pass counts. Before using `rg '^test at '` to inventory native Node failure summaries, its known-case check on `focused-node.log` returned exactly the five known failures, all in `all-candidate-analytical-rebuild.test.js`, at lines 74, 118, 192, 220 and 273. Native runner totals remain authoritative; the search supplies only file/line navigation.

`node scripts/validate-priority-ranking.mjs` passes 26 active owners and 14 ranked rows. `git diff --check` passes at this checkpoint. A checksum recheck preserves 12 of the 13 original CRW-005 files exactly and detects a concurrent change to `aaa-corpus-rewrite/work-queue.md`; subsequent `git status` also shows a concurrent operations queue edit. This task edits neither queue. Those changes remain ambient work and are not reverted or counted as this task's repairs.

## Other source-binding boundary

The ordinary sweep also reaches the Borg live-contract source-snapshot assertion. The [App Solver binding](../../app-solver/contracts/master-eom-binding-v1.md#source-snapshot-ownership-and-refresh-procedure) reserves its digest review to that owner. `git show d8370991f^:content/markdown/aaa/dynamics/master-equation.md | shasum -a 256` reproduces the accepted `c402cc83621b60f0a80ad4f1f48a4db1a6fa12a5a6c2874d02b9d93e03c2cf05`, while the current chapter hashes to `bb7357868a4f900aa566b8a43107d111dcd435cab677361f9bba3dceaadb384b`. The inspected `d8370991f` source diff changes causal-surface terminology. This establishes a subsequent mismatching source transition; it does not attribute that failure to the layout pass. The next action is the binding owner's statement-by-statement review and required Borg/content/scene checks. This task leaves its digest unchanged, honoring the campaign's stop condition for failures outside the layout change.

## Diagnostic-run restart

Run `17e53cbf-7bc1-42b8-b866-76f024b703ce` was stopped through the supervisor after the six results of `f6c-cached-root-cover-full-process.test.js` had printed but its process remained alive; its monitor-loss assertion failed. The supervisor records terminal `stopped`, 456.419 seconds and `processGroupClosed: true`. A subsequent process-table search for that fixture's synthetic child commands found no child launched by this run; older unrelated September 1 processes were left untouched. The stopped log is incomplete diagnostic evidence, not a finished full sweep.

Replacement run `05e3b59d-6b87-41f5-8283-456eacda1b1c` used `node scripts/run-test-sweep.mjs` for all 330 ordinary files, with the canonical 120-second per-test timeout and a 2,400-second supervisor deadline. The separate ten-file selection uses `node scripts/run-test-sweep.mjs --slow`, run `f4b821cf-3d3d-4da7-b655-56e43b47205a`, with the canonical 600-second per-test timeout and a 2,400-second supervisor deadline. This split changes the operational timeout, not the test universe, expected results or scientific acceptance conditions.

## Ordinary Node result

The native runner reports **2,700 tests: 2,640 passes, 42 failures, 18 skips and zero cancellations**, in 496.604 seconds, across the 330 selected files. Supervisor `05e3b59d-6b87-41f5-8283-456eacda1b1c` reports terminal `failed`, exit one, 496.778 seconds and `processGroupClosed: true`. This replacement ran after the methodology repair and ignored runtime preparation. `rg '^test at '` grouped by filename with `cut`, `sort` and `uniq -c` identifies the following 42 native failure locations in 13 files, after the five-failure known-case check above. The native totals, rather than the grouping, establish the result.

| Test file under `tests/` | Failures | Measured mismatch and next disposition |
| --- | ---: | --- |
| `archie-service-contracts.test.js` | 1 | Source-index fixture identity differs at reading copies and textbook TOC. Rebuild the canonical source index and its dependent fixtures in owner order after corpus regeneration; the diagnostic names `node scripts/archie-service/build-source-index.mjs --write`. |
| `archie-service-mcp-loopback-http.test.js` | 1 | `SNAPSHOT_IDENTITY_MISMATCH`; same ordered snapshot/fixture review, retaining `mcp-tool-contract/v1`. |
| `archie-service-mcp-remote-deployment-contract.test.js` | 1 | `SNAPSHOT_IDENTITY_MISMATCH`; review dependent deployment snapshots after canonical source refresh. |
| `archie-service-source-index-snapshot.test.js` | 1 | Source-index snapshot differs at reading copies and textbook TOC. |
| `borg-eom-migration.test.js` | 1 | Current Master Equation digest differs from the App Solver live binding; owner procedure described above. |
| `coincident-midpoint-4-2-1-frequency-and-coaxial-separated-two-planar-braid-co-rotating-resolution-coverage-calibration.test.js` | 1 | Frozen implementation identity remains mismatched; Braid Program must resolve the retained runner/test disposition. |
| `equation-dependency-map.test.mjs` | 10 | The Option B assumptions binding is stale, and definition selectors no longer meet their expected value/level assertions. Follow the existing OPS-030 decision boundary before refreshing this mathematical binding. |
| `equation-mapping-corpus.test.js` | 4 | Corpus size is 4,661 where assertions expect 4,658. Review the corpus change and expectation together after concurrent corpus editing settles. |
| `f5-enclosed-root-ledger-reducer.test.js` | 16 | Approved configuration and retained pilot fixture differ at `constituents`. Braid Program must recover the original matching frozen input or review a successor evidence set. |
| `f5-enclosed-root-prefix.test.js` | 1 | Same approved-configuration/pilot-constituents mismatch. |
| `f5-enclosed-root-preparation.test.js` | 2 | Frozen v3 approved-configuration hash mismatch; do not substitute the current display configuration for the historical source. |
| `f6c-refined-acceleration-pilot.test.js` | 1 | Frozen launcher `scripts/eom/launch-prescribed-response-pilot.mjs` expected `f178c5d393ca741a0e82aa9865fa796d5901f1751be954183735db1f4a3f6a31`, actual `9af9a6a33b3b1c5889550953496be13d0698e5d24e9033dbdd5ffcb82deeafe2`; owner source-impact/replay disposition required. |
| `operations-public-security-policy.test.js` | 2 | Dependency expectation contains only Mermaid but the package includes Comunica, JSON-LD and N3; a workflow action assertion expects `ea165f8d65b6e75b540449e92b4886f43607fa02` and receives `undefined`. Operations must review the actual dependency/action contract before updating expectations. |

The native log explicitly marks 18 skips: one sealed local compact-sweep fixture is absent; nine bounded process/registered-gate controls require `F5_RUN_PROCESS_CONTROLS=1`; one current-build/original-proof production-context control requires `AAA_CIRCULAR_CURRENT_CONTEXT`; seven sub-field binary/manifest controls require `AAA_SUBFIELD_CIRCULAR_ADAPTER_BIN`. The gates are declared in `compact-sweep-dashboard-exporter.test.js`, `f5-ordinary-evolution-runner.test.js`, `subfield-circular-current-context.test.js` and `subfield-circular-root-adapter.test.js`. Skipped bodies did not execute. No count from the earlier interrupted run is combined with this result. These errors establish current gate failures, not their historical cause or a scientific refutation.

## Slow Node result

The native runner reports **124 tests: 120 passes, four failures, zero skips and zero cancellations**, in 244.108 seconds, across all ten slow files. Supervisor `f4b821cf-3d3d-4da7-b655-56e43b47205a` reports terminal `failed`, exit one, 244.292 seconds and `processGroupClosed: true`.

Three failures in `tests/archie-service-full-corpus-source-index.test.js` compare the stored full-corpus snapshot against current authored sources. The stored snapshot differs, its `--check` fails, and validation reports a source-input hash mismatch for `background-and-simple-action`. The exact refresh command is `node scripts/archie-service/build-full-corpus-source-index.mjs --write`, after canonical sources and generated reading copies settle, followed by `--check` and the dependent tests. The deterministic comparison between two new builds passes before the stored-snapshot assertion fails; later assertions in each failed test body are not reached.

The remaining failure, `tests/runtime-asset-fresh-checkout.test.js`, occurs during ignored runtime reconstruction in its disposable source-only export. `node scripts/prepare-runtime-assets.mjs --write` fails at the equation-corpus family with nine `missing canonical source link` diagnostics: `corpus-equation-64ee29567fb2e6aa`, `corpus-equation-6e68e862aa5efb32`, `corpus-equation-9c165fd0ccf57a2d`, `corpus-equation-ca9a7334a66b7f31`, `corpus-equation-817cee980597181e`, `corpus-equation-d2cf70d4831f7c61`, `corpus-equation-3e12b755b7ba158c`, `corpus-equation-2368c567c43735b7`, and `corpus-equation-d74d654addbba1e3`. The test's subsequent runtime checks and Pages build are not reached. The equation-corpus owner must reconcile source links/anchors before repeating fresh-source reconstruction; existing ignored outputs cannot stand in for this failing test.

Together the two finished Node selections cover all 340 files in the duration-split inventory, with 2,760 passes, 46 failures and 18 skips. This is complete file selection, not complete execution of every body or a passing suite. The earlier stopped diagnostic run contributes nothing to these totals.

## Full Python result

The shared-venv discovery run completed: native `unittest` reports **1,682 tests in 726.128 seconds, one failure and four errors**, exit one. Supervisor `a82198d7-1dfe-49d1-a8d8-c3bbb38a8e03` reports terminal `failed`, 727.798 seconds and `processGroupClosed: true`. It was not stopped or timed out.

| Native failing location | Measured error and required disposition |
| --- | --- |
| `test_f5_api_domain_conformance.py`: `test_incomplete_census_and_binding_changes_never_become_receipts`, `test_real_source_axial_acceleration_is_independent_constant_control` | Both error at `load_frozen_sources`: `frozen source hash mismatch: approved-config`. The oracle binds `phase-varying-prescribed-display-history.v3.json` to `e92e450c8ea83086b60184d31ff5b07fe8a470b1e20088ea312592f2b38800fb`. Braid Program owns the original-source/captured-input disposition; do not change the frozen expectation to the current display configuration. |
| `test_f5_history_manifest_conformance.py`: `setUpClass` | The same approved-config mismatch prevents the class from running. The 1,682 executed-test count therefore does not mean every discovered test body ran. |
| `test_orthogonal_plane_weave_fold_limit_certificate.py`: `test_frozen_inputs_and_extension_provenance_match` | The retained certificate binds the Master Equation at `0e8219c7a3cc750747c43030d5b998ddbf2de525a5469d32fa5e52fbae5ec63f`; current bytes are `bb7357868a4f900aa566b8a43107d111dcd435cab677361f9bba3dceaadb384b`. This is a historical certificate binding, distinct from the current App Solver synchronization pin. Braid Program owns exact-source replay or an independently reviewed successor certificate. |
| `test_orthogonal_plane_weave_fold_limit_certificate.py`: `test_receipt_reproduces_byte_for_byte` | Its declared certificate command refuses the changed frozen Master Equation before producing the temporary receipt, returning exit one. The certificate and its independent ordinary oracle remain unchanged. |

These are measured gate failures, not evidence that a numerical theorem is false. Their earlier first mismatching transitions are not established by this run. No historical receipt, oracle or frozen implementation pin is rewritten to make the suite green.

## Handoff and closure decision

AWT-015 remains **In progress** under its unchanged acceptance boundary. The complete validation attempt is terminal, but the required gates do not pass. The next work is to resolve the owner dispositions named above, settle concurrent authored sources, perform authorized generated/fixture refresh in dependency order, and rerun the complete gates with required input-gated coverage available. No commit, push, PR, merge or branch operation was performed.

At handoff, `git diff --check` passes and `node scripts/validate-priority-ranking.mjs` passes 26 active owners and 14 ranked rows. `shasum -a 256 -c .local-data/awt-015/2026-09-12/baseline-sha256.txt` confirms 12 original files unchanged and the concurrent CRW queue changed. `git --no-optional-locks status --short` also shows additional concurrent corpus and owner-log edits beyond the baseline; this task's edits remain the seven files listed at the beginning. These runs describe the observed shared checkout, not an immutable publication candidate. `node scripts/dev/owned-compute-supervisor.mjs closeout --owner-task 01a092fa-4fe4-72c3-b7e5-b1ab10902279` returns `status: clear`; no owned computation remains live. A passing final-state rerun, with setup failures and required skips resolved, is the checkable condition that would overturn this open disposition.
