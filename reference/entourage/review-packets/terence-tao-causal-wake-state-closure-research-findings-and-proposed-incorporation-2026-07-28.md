Closure goal: Preserve the bounded causal-wake-state closure research, source responses, assessed findings, and proposed incorporation plan in one durable packet without promoting the consultation into theory, ontology, conservation law, retained-branch status, or solver authority.

# Causal Wake-State Closure: Research Findings and Proposed Incorporation Plan

- Date: 2026-07-28
- Status: consolidated research packet; substantively complete consultation response assembled and assessed, with one recorded response-format deviation
- Attribution: this document does not contain a response from Terence Tao or any other external human. The role label in the consultation prompt selected an internal analysis perspective only.
- Authority: research note only; no corpus, ontology, conservation, retained-branch, or solver promotion
- Source preservation: the full available external-service responses are embedded in the source appendices of this packet

## Consolidated research finding

The consultation narrows the causal wake-state closure problem but does not
resolve it. The strongest current conclusions are:

1. finite accumulated acceleration at coincident same-transmitter root birth
   is governed by the exact weighted-integrability condition, not by a
   necessary power-law bound;
2. analytic maturity in the exact linear birth variable may first have a
   nonzero cubic term, but neither cubic order, its coefficient, nor a
   transition scale is selected;
3. an exact point-local reception cannot change an ordinary
   $L^1(dT_e\,d\boldsymbol\omega)$ wake account by a finite amount;
4. a uniformly distributed finite account on a declared shrinking spherical
   cap converges weak-* to a finite atom; its density diverges while its total
   measure remains finite, and the exact higher-order coefficient depends on
   the declared patch geometry;
5. radial receiver acceleration does not force radial wake momentum unless
   the reception map is sector-blind or a separate booking condition imposes
   radial transfer;
6. same-point angular booking cancels identically and selects no transfer
   direction, whereas emission-center booking imposes radial transfer if
   event-wise angular closure is required;
7. if emission-center closure requires every finite momentum-account increment
   between every pair of velocities in a connected open domain to be collinear
   with that velocity increment, then the momentum-account map is affine,
   $\mathbf p(\mathbf V)=a\mathbf V+\mathbf b$; normalization at rest or
   rotational equivariance removes $\mathbf b$ under the stated domain
   assumptions;
8. atom visibility is selected by the observation operator, but the supplied
   source does not prove that the available choices reduce to an exhaustive
   two-horn dilemma;
9. finite impulse alone establishes neither existence nor uniqueness for the
   state-dependent delayed evolution; the state modulus, branch rule, event
   ordering, jump-map regularity, history phase space, and account
   representation remain constitutive obligations; and
10. no available response derives a photon Gate B restriction, physical
   conservation law, retained
   branch, wake ontology, or solver update.

Plainly: the research replaces several broad claims with exact conditional
statements and counterexamples. The central missing choice is still the
mathematical representation of reception transfers, followed by one complete
predeclared update on that representation.

## Proposed incorporation plan

### Batch A — owning priority corrections

Target:
`reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md`
and its directly dependent priority summaries.

1. Replace the claimed implication from finite weighted impulse to
   $M(T)=O(T^{2+\delta})$ with the exact weighted-integrability condition.
2. Preserve cubic order only as the first possible analytic order on the exact
   linear birth chart.
3. Add the logarithmic counterexample and an explicit cubic-versus-quartic
   nonuniqueness pair.
4. Declare whether reception accounts are ordinary densities, finite Radon
   measures, finite patches, or nonlocal redistributions.
5. Add the sector-blind equivariance theorem, its retained-history
   counterexample, and the exact same-point, emission-center, and
   free-propagation angular identities.
6. Add the finite-increment affine-rigidity theorem only with the explicit
   all-pairs or triangle-connected richness hypothesis, and state that the
   theorem is conditional on emission-center booking rather than a consequence
   of radial acceleration.
7. Require every measure-valued account proposal to declare its observation
   operator; atom visibility cannot be classified from the measure class
   alone.
8. Add the finite-cap scaling and weak-* atomic limit only after declaring the
   exact cap geometry; keep the transverse patch radius, wake thickness, and
   emission-time regulator distinct.
9. State well-posedness as a one-sided birth-normal-form problem requiring a
   predeclared branch rule, a declared history phase space, integrable state
   sensitivity, regular event-time and jump maps, and a solution class
   compatible with any jumps.
10. Record the Q6 guard: this account analysis supplies no photon Gate B
    consequence.

Plainly: Batch A repairs the proof record and makes the representation fork
explicit. It does not choose a maturity law, account measure, or conservation
mechanism.

Promotion classification: `promote now` inside the owning priority research
record; `defer with blocker` for reader-facing wake-state claims.

### Batch B — reader-facing kinetic-account corrections

Targets:
`content/markdown/aaa/dynamics/energy.md`,
`content/markdown/aaa/dynamics/effective-lagrangian.md`, and
`content/markdown/aaa/dynamics/master-equation.md`.

1. Retain the existing work-power compatibility equation
   $P'(s)=K'(s)/s$ and add its near-zero domain assumptions.
2. Add the kinetic-Lagrangian reconstruction
   $\ell_K(s)=sP(s)-K(s)$ under the stated differentiability assumptions.
3. Add the ontology-neutral conditional theorem that arbitrary oblique radial
   increments can remain radial in momentum-account space only when
   $P(s)=as$, and hence $K(s)=as^2/2$ under work-power compatibility and
   $K(0)=0$.
4. State that a generalized kinetic chart requires revariation of the complete
   action; it cannot be obtained by substituting $K$ directly as the kinetic
   Lagrangian.

Plainly: Batch B corrects mathematical scope in the existing kinetic-account
scaffold. The coefficient $a$ is bookkeeping and is not primitive architrino
mass.

Promotion classification: `promote now` as safe mathematical clarification,
subject to reconciliation with concurrent edits in those files.

### Priority disposition

- MEC-002 remains `In progress`: select and derive the reception-account
  representation and a complete constitutive transfer map.
- MEC-003 remains `Deferred / blocked`: no maturity law, transition scale,
  branch rule, or uniqueness estimate is selected.
- MEC-004 remains `Deferred / blocked`: emission capacity, bounded accounts,
  transfer coefficients, and independently derived conservation remain absent.

Plainly: the proposed edits sharpen the live blockers; they do not satisfy a
queue completion condition.

## Stage 1 — Consultation prompt issued

```text
Closure goal:
Determine whether the attached Master Equation wake-state packet supports a sharper theorem, counterexample, safe correction, or constitutive closure condition, and specify the single decisive next calculation that could materially advance its owning priority without importing observer-level physics or promoting diagnostic evidence.

Selected role: Terence Tao — Analysis and Well-Posedness Engineer.

Reason for selection: the obstruction is a state-dependent delayed/hybrid evolution problem with an independently transported surface state, singular same-transmitter root birth, a proposed minimal state representation, nonuniqueness of admissible maturity laws, conservation-functional underdetermination, and a need for local existence/uniqueness or a no-go theorem. The needed work is functional analysis, asymptotic integrability, well-posedness, counterexample construction, and proof architecture rather than relativity mapping, topology, invariant-measure selection, effective metrics, or broad foundational commentary.

Attached target file — this is the only repository file you may open or inspect:
`/Users/markmorris/vibe/architrino/reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md`

Access warning: you have access only to this prompt and the single attached target file. Do not open, search, list, quote, or infer any other repository file, repository history, source code, test, prior conversation, council response, or external source. Do not infer unseen repository state. Treat every link and path mentioned inside the attachment as unavailable context. If a conclusion needs unseen information, put it under `## 9. Missing context` rather than guessing.

Current authority and claim grade from the attachment:
- Purpose: execute the independently evolving wake-state route selected for Master Equation closure.
- Claim grade: derived minimum-state requirements and a derived obstruction under the current Architrino primitives.
- Current result: a smallest necessary state class is proposed/inferred, but its coupling and conserved accounts are not determined; no finite accepted coincident same-transmitter transition follows.
- Promotion: priority-only. It licenses no ontology, canon, conserved-quantity, retained-branch, or solver change.

Precise unresolved blocker:
Construct, or prove impossible under the stated primitives, one smallest independently evolving present-time wake state and predeclared causal update that simultaneously:
1. reproduces the regular transmitter-side acceleration law;
2. gives finite and unique continuation through coincident same-transmitter root birth;
3. closes energy, momentum, and angular momentum with non-circular motion, wake, and boundary accounts;
4. never reads future receiver history and never defines wake changes as the negative of a measured motion-account residual.

Established equations and allowed assumptions:

The complete present state is sought in the form

$$
\mathcal S_T
=
\left(
\{\mathcal H_i^T\}_{i=1}^{N},
\{\mathcal W_i(T;T_t,\boldsymbol\omega)\}_{T_t\le T}
\right),
\qquad
\mathcal W_i=(m_i,e_i,\boldsymbol\pi_i).
$$

Here $\mathcal H_i^T$ is retained path history; $\boldsymbol\omega\in S^2$ labels a direction on an emitted causal surface; $m_i$ is a nonnegative acceleration-coupling or maturity output; $e_i$ is a scalar wake-energy account; and $\boldsymbol\pi_i$ is a vector wake-momentum account. Surface resolution is motivated by the fact that different receivers can intersect different directions on one emitted surface. Minimality of this representation is inferred, not fully proved.

Plainly: the candidate state remembers worldline history plus direction-by-direction wake data. Its three wake outputs say how strongly a surface acts and what scalar and vector accounts it carries. The packet has not proved that these must be three independent stored variables or that no smaller representation can generate them.

Free propagation is

$$
\mathbf Y_i(T;T_t,\boldsymbol\omega)
=
\mathbf X_i(T_t)+c_f(T-T_t)\boldsymbol\omega,
\qquad
(\partial_T+c_f\partial_R)(m_i,e_i,\boldsymbol\pi_i)=\mathbf0.
$$

Plainly: each emitted directional surface element moves outward at primitive wake speed $c_f$ and carries its current outputs without reading a future receiver.

At a reception event $\mathsf h=(r,t,T_r,T_t)$, the missing predeclared local map is

$$
\left(
\Delta\mathbf V_r,
\Delta m_t,
\Delta e_t,
\Delta\boldsymbol\pi_t
\right)
=
\mathcal R_{q_tq_r}
\left(
\mathcal H^T,
\mathcal W_t(T;T_t,\boldsymbol\omega_{\mathsf h})
\right).
$$

On a certified regular simple-root chart, its receiver component must reduce to

$$
\frac{d\mathbf V_r}{dT_r}
=
\kappa\,\sigma_{tr}|q_tq_r|
\frac{c_f}{r^2|D_t|}
\hat{\mathbf r}_t.
$$

Plainly: $D_t$ is the transmitter-side causal-root derivative. Receiver motion may affect later geometry and signed root playback, but it may not be inserted into the instantaneous acceleration strength.

On the exact quadratic same-transmitter birth control, the unsuppressed acceleration scales as $T^{-3}$. If maturity changes it to $M(T)T^{-3}$, finite accumulated acceleration requires

$$
\int_0^L \frac{M(T)}{T^3}\,dT<\infty.
$$

The attachment then states $M(T)=O(T^{2+\delta})$ for some $\delta>0$, equivalently $M(z)=O(z^{2+\delta})$ for $z=|D_t|/c_f$, and says an analytic candidate first admits a cubic leading order. Audit carefully whether the displayed big-O condition is necessary, sufficient, or only power-law/analytic-class necessary; give a correction if the implication is too strong.

Plainly: the wake coupling must turn off fast enough near root birth to make the total velocity change finite. The exact mathematical regularity assumptions needed for the packet’s claimed cubic threshold are themselves part of this consultation.

Candidate motion and total accounts are written with unknown native functions $K$ and $P$:

$$
E_{\mathrm{motion}}=\sum_i K(\|\mathbf V_i\|),
\qquad
\mathbf P_{\mathrm{motion}}
=\sum_iP(\|\mathbf V_i\|)\hat{\mathbf V}_i,
$$

$$
E_{\mathrm{tot}}=E_{\mathrm{motion}}+\sum_i\int e_i\,dT_t\,d\boldsymbol\omega,
$$

$$
\mathbf P_{\mathrm{tot}}=\mathbf P_{\mathrm{motion}}+\sum_i\int\boldsymbol\pi_i\,dT_t\,d\boldsymbol\omega,
$$

$$
\mathbf J_{\mathrm{tot}}
=\sum_i\mathbf X_i\times P(\|\mathbf V_i\|)\hat{\mathbf V}_i
+\sum_i\int\mathbf Y_i\times\boldsymbol\pi_i\,dT_t\,d\boldsymbol\omega.
$$

One update must prove

$$
\Delta E_{\mathrm{tot}}+\Phi_E=0,
\qquad
\Delta\mathbf P_{\mathrm{tot}}+\boldsymbol\Phi_P=\mathbf0,
\qquad
\Delta\mathbf J_{\mathrm{tot}}+\boldsymbol\Phi_J=\mathbf0.
$$

Plainly: $K$ and $P$ are unknown Architrino-native motion-account functions; $e_i$ and $\boldsymbol\pi_i$ are wake accounts; and the $\Phi$ terms record quantities crossing the retained boundary. None may be chosen after evolution merely to cancel a residual.

Exact derived/proposed distinction:
- Derived or established within the attachment: direction-resolved free propagation; the regular receiver acceleration component; the need for finite coincident-birth impulse; the fact that the current primitives do not select $K$, $P$, emission capacity, reception transfer, or a unique maturity law; ordinary nonzero-separation fold impulse remains integrable when maturity stays bounded; finite retained history requires explicit boundary flux.
- Inferred or proposed: the strict minimality of $(m,e,\boldsymbol\pi)$ as a representation; the claimed pointwise $O(T^{2+\delta})$ consequence without additional regularity; any particular maturity interpolation; any motion-account functions; any emission capacity; any reception-transfer rule; any conserved physical charge; any finite unique coincident transition.

AAA theory constraints:
- Architrinos have no primitive physical mass.
- The substrate law is acceleration-first; do not recast it as force.
- Allowed substrate primitives are delayed path history, polarity, causal wakes, Euclidean void, and absolute time.
- Relativity, quantum theory, the Standard Model, thermodynamics, and GR are recovery targets, observational constraints, or comparison frameworks, not substrate premises.
- Keep $c_f$ symbolic in derivations; set $c_f=1$ in any numerical example.
- Distinguish primitive wake speed, effective signal speed, photon speed, and measured observer speed; only $c_f$ appears in this packet.
- Do not introduce electromagnetic potentials, magnetic acceleration, curved substrate spacetime, quantum postulates, primitive mass, or single-architrino thermodynamics.
- Do not infer stability without an equilibrium or realized persistent branch.
- Do not infer physical realization from prescribed geometry, topology, or same-implementation replay.

Historical anti-ratchet safeguard:
A locally correct identity must not be promoted into the Master Equation, wake ontology, solver evolution, a conservation law, retained-branch status, or a downstream recovery mechanism without the derivation appropriate to that claim. Explicitly identify:
1. rigorous consequences of the attachment;
2. inverse constructions fitted to a desired result;
3. statements requiring missing physical interpretation;
4. statements unsafe to propagate;
5. the calculation required before any promotion.

Detailed questions:

1. Minimal-state theorem or counterexample. On the allowed present-time surface state, state the sharpest theorem you can justify for why direction resolution is necessary. Distinguish “the state must determine $(m,e,\boldsymbol\pi)$” from “these are independent primitive coordinates.” Either give a proof decomposition under explicit locality and independent-reception assumptions, or give a strictly smaller counterexample representation. Failure means only an intuition-level dimensional count.

2. Coincident-birth asymptotic audit. Starting from $\int_0^L M(T)T^{-3}\,dT<\infty$, give necessary and sufficient conditions at the weakest defensible regularity. Then treat separately: nonnegative measurable $M$, monotone $M$, regularly varying/power-law $M$, and analytic $M$. State exactly when cubic leading order follows. Also audit the change of variable to $z=|D_t|/c_f$: name the extra asymptotic relation between $z$ and $T$ needed for equivalence. Failure means accepting the attachment’s big-O implication without proof.

3. Nonuniqueness and missing scale. Construct at least two explicit maturity laws that satisfy the corrected integrability condition and approach the regular value on a declared regular domain, but yield different transition impulses or outgoing histories. Identify the minimal extra condition—matching, capacity, variational, semigroup, or another allowed native condition—that could select one law. If no allowed condition selects uniquely, state a no-go theorem with its assumptions. Failure means proposing an arbitrary interpolation as closure.

4. Conservation compatibility. Treat $K$, $P$, the reception map $\mathcal R_{q_tq_r}$, and boundary flux as unknown predeclared objects. Derive the local functional compatibility equations required for energy, momentum, and angular momentum to close on arbitrary nonsymmetric reception histories. Determine whether Euclidean translation/rotation and absolute-time translation alone constrain $K$ and $P$, or whether an action/constitutive pairing is still required. Give a counterexample showing why post hoc residual cancellation is vacuous. Failure means importing $m\mathbf v$, $\tfrac12mv^2$, or defining wake accounts by the residual.

5. Trilemma adjudication. Formalize the attachment’s claim that universal transmitter-history-only regular acceleration, reception-updated dynamically visible wake state, and non-circular conservation cannot all hold without an additional visibility/account rule. State a theorem with minimal assumptions or provide a counterexample. Separate a dynamically active wake component from a passive bookkeeping coordinate, and audit boundedness under repeated reception. Failure means broad verbal agreement without a proof skeleton.

6. Positive-emission-capacity obstruction. Under continuous emission, formulate the strongest finite-window or infinite-window statement justified about positive wake energy, emitter debit, reception loading, and lower boundedness. Identify all logically distinct escape routes that remain within the allowed primitives, and label which ones introduce genuinely new constitutive data. Failure means using thermodynamic or electromagnetic premises.

7. Well-posedness architecture and decisive control. Specify the minimal regularity, causality, branch-transversality, event-separation, and boundary assumptions under which a hybrid transport/reception update of this form could have local existence and uniqueness. Then give the single highest-value next analytic control or bounded simulation, with variables, inputs, method, pass condition, fail condition, and the precise claim grade it could change. Failure means recommending an unconstrained broad simulation or treating implementation parity as independent evidence.

Required response format:

## 1. Finding in plain language

State the central conclusion without relying on repository context.

## 2. Prompt-bounded assumptions

List only assumptions explicitly supplied in this prompt or the attached file.

## 3. Detailed answers

Answer each numbered question separately. Show the mathematics, intermediate steps, sign conventions, boundary assumptions, and domains of validity.

Every technical unit or displayed equation must be immediately followed by a `Plainly:` explanation suitable for an AP-STEM senior or sophomore electrical-engineering student. Do not allow more than three technical paragraphs without a `Plainly:` interlude.

## 4. Strongest derived result

State the strongest theorem, identity, counterexample, estimate, or no-go result that follows. Give claim grade, assumptions, scope, and an operator-checkable falsifier.

## 5. Unsafe inference audit

Identify conclusions the attachment does not justify, including inverse constructions, missing interpretation, and unsafe promotion.

## 6. Safe corrections

List exact local corrections that follow from the supplied mathematics and require no unseen repository context or new ontology.

## 7. Rejected paths

List proposed arguments or mechanisms that fail, with the mathematical reason.

## 8. Decisive next calculation

Specify the single highest-value next derivation, analytic control, or simulation: variables, inputs, method, pass condition, fail condition, and what claim it would change.

## 9. Missing context

List conclusions that cannot be reached because the prompt and attachment are insufficient. Do not guess their answers.

Do not treat the selected role as authority. Grade each claim as derived, inferred, or guessed, and attach an operator-checkable falsifier. Do not infer unseen repository state.
```

## Stage 1 — Internal response received

## 1. Finding in plain language

The packet supports a sharper negative result and several safe corrections, but not a constitutive closure.

Direction-by-direction distinguishability is necessary only up to the equivalence classes that future receptions can actually distinguish. The state must determine the coupling, scalar account, and vector account at each distinguishable surface location, but the supplied assumptions do not prove that these are independent primitive coordinates.

The coincident-birth condition is exactly weighted integrability. A pointwise bound of the form \(M(T)=O(T^{2+\delta})\) is sufficient, not necessary, even when \(M\) is nonnegative and monotone. Cubic is merely the first possible nonzero Taylor order when \(M\) is analytic in a variable \(z\) satisfying \(z\sim aT\), with \(a>0\).

There is also a sharper conservation obstruction. If \(e\) and \(\boldsymbol\pi\) are ordinary densities integrated against \(dT_t\,d\boldsymbol\omega\), changing them only at one reception coordinate changes their integrals by zero. A finite receiver-account change therefore requires an atomic measure, a finite reception patch, or a nonlocal response. Under the atomic, strictly local, no-intrinsic-angular-account option, angular compatibility for arbitrary non-circular receptions forces the motion momentum account to be linear in velocity, up to one undetermined scalar account coefficient.

No supplied principle selects the maturity law, its transition scale, the energy function, emission capacity, or a bounded reception ledger. The current result remains priority-only and fail-closed.

## 2. Prompt-bounded assumptions

- Absolute time, Euclidean position, delayed path history, polarity, and causal wakes are the allowed substrate ingredients.
- Free wake propagation occurs at symbolic primitive speed \(c_f\).
- The regular receiver acceleration is transmitter-side and proportional to \(c_f/(r^2|D_t|)\) along the stated radial direction.
- At the exact quadratic coincident same-transmitter birth, the unsuppressed acceleration scales as \(T^{-3}\).
- The proposed state is indexed by transmitter, emission time, and direction.
- A reception map must be predeclared and causal.
- Motion energy and momentum accounts have the rotationally covariant forms \(K(\|\mathbf V\|)\) and \(P(\|\mathbf V\|)\hat{\mathbf V}\).
- Wake energy and momentum are integrated over emission time and direction.
- No intrinsic angular account is presently included; wake angular momentum is orbital.
- Retained-window loss must appear as explicit boundary flux.
- The packet supplies no action, constitutive pairing, emission capacity, maturity generator, reception-transfer law, or physical interpretation of the account variables.
- No observer-level law or primitive mass is assumed.

## 3. Detailed answers

### 3.1 Minimal-state theorem or counterexample

Define two directions to be reception-equivalent when no admissible future reception can distinguish their local output or their locally updated continuation:

\[
\boldsymbol\omega_1\sim\boldsymbol\omega_2
\quad\Longleftrightarrow\quad
\mathcal O_{\boldsymbol\omega_1}(S,H)
=
\mathcal O_{\boldsymbol\omega_2}(S,H)
\ \text{for every admissible present state \(S\) and future history \(H\)}.
\]

Plainly: two directions may share one state coordinate only if every allowed future experiment treats them identically.

**Direction-separation theorem.** Assume:

1. the evolution is Markovian in the proposed present state;
2. receptions at disjoint surface locations may occur independently;
3. a local reception can change the output or later continuation at one location without necessarily changing another;
4. future receiver behavior is determined by the present state.

Then any sufficient present state must separate the quotient \(S^2/{\sim}\). In particular, one scalar shared by the whole emitted surface is insufficient whenever two directions are not reception-equivalent.

Proof skeleton: take two histories with identical transmitter history and identical global emission scalar, but apply an admissible update at \(\boldsymbol\omega_1\) in only one history. Independent reception permits a later receiver at \(\boldsymbol\omega_1\) or \(\boldsymbol\omega_2\) to distinguish the histories. A Markov state that still identifies the two histories cannot determine both future outcomes, contradicting sufficiency.

Plainly: if one part of the surface can be changed without changing another part, the state must remember which part changed.

This proves directional distinguishability, not three independent primitive coordinates. A strictly smaller representational counterexample is one latent scalar field \(u(T_t,\boldsymbol\omega)\) with predeclared outputs

\[
m=f(u),\qquad e=g(u),\qquad
\boldsymbol\pi=h(u)\boldsymbol\omega .
\]

Plainly: one stored number per direction can determine all three advertised outputs if a constitutive relation ties them together.

That counterexample does not establish that such a constitutive relation is physically correct or conservation-compatible. It establishes only that independence of \((m,e,\boldsymbol\pi)\) has not been proved. Without continuity or locality assumptions on the encoding, even component-count arguments are meaningless because several real values can be encoded pathologically into one real value. A genuine coordinate-minimality theorem would need a regular encoding assumption and an independently reachable open set of output values.

There is a further state-class issue. If a reception is exactly localized at one pair \((T_t,\boldsymbol\omega_{\mathsf h})\), then ordinary \(L^1\) density fields cannot record a finite account transfer through a pointwise change. The account state must instead support atoms, finite patches, or nonlocal redistribution.

**Grade:** derived conditional theorem; inferred operational quotient.
**Falsifier:** exhibit a Markov present state that identifies two independently updatable, reception-distinguishable directions yet uniquely predicts both future receptions. A regular one-variable constitutive encoding already falsifies any unconditional claim that the three outputs must be independent coordinates.

### 3.2 Coincident-birth asymptotic audit

For nonnegative measurable \(M\), the weakest necessary-and-sufficient condition is exactly

\[
M\in L^1\!\left((0,L),T^{-3}dT\right),
\qquad\text{equivalently}\qquad
\int_0^L M(T)T^{-3}\,dT<\infty .
\]

Plainly: the suppressed acceleration has finite total effect exactly when its weighted area is finite.

The bound

\[
M(T)\le C T^{2+\delta}
\quad\text{near \(T=0\), for some \(\delta>0\)}
\]

Plainly: this more-than-quadratic pointwise decay guarantees convergence, because the remaining integrand is bounded by \(CT^{-1+\delta}\).

It is not necessary. Let \(T_n=2^{-n}\), put \(M(T)=T_n^2\) on intervals of width \(T_n^3\) centered near \(T_n\), and set \(M=0\) elsewhere. The weighted contribution of the \(n\)-th spike is of order \(T_n^2\), so the integral converges, but \(M(T_n)/T_n^{2+\delta}=T_n^{-\delta}\) diverges for every \(\delta>0\).

Plainly: very narrow quadratic-height spikes can have finite total area even though no uniform more-than-quadratic bound holds.

For the natural monotonicity orientation—\(M\) nondecreasing as \(T\) moves away from birth—weighted integrability implies

\[
M(T)=o(T^2)\qquad(T\downarrow0).
\]

Plainly: a monotone maturity cannot repeatedly hide large values in narrow spikes, so it must be smaller than quadratic pointwise.

That condition is still not sufficient. For example,

\[
M(T)=\frac{T^2}{\log^2(eL/T)}
\]

Plainly: this monotone function is not bounded by any \(T^{2+\delta}\), yet its weighted integral converges because \(\int dT/[T\log^2(eL/T)]\) is finite.

An exact monotone characterization is obtained on dyadic shells. With \(t_n=2^{-n}L\), weighted integrability is equivalent, up to fixed multiplicative constants and an index shift, to

\[
\sum_{n=0}^{\infty}\frac{M(t_n)}{t_n^2}<\infty .
\]

Plainly: for monotone maturity, convergence is decided by summing its quadratic-normalized values over successively halved time scales.

If “monotone” instead means nonincreasing as \(T\) increases, nonnegativity and integrability force \(M\) to vanish near birth; otherwise its positive lower tail makes the integral diverge.

For a regularly varying law

\[
M(T)=T^\alpha L(T),
\]

Plainly: \(\alpha\) is the leading power and \(L\) changes more slowly than any fixed power.

The classification is:

- \(\alpha>2\): convergence, under the usual eventual positivity and local boundedness of \(L\);
- \(\alpha<2\): divergence;
- \(\alpha=2\): the borderline condition is exactly \(\int_0^L L(T)\,dT/T<\infty\).

For a pure power \(M(T)\sim CT^\alpha\), \(C>0\), the condition is simply \(\alpha>2\).

If \(M\) is analytic in \(T\) and not identically zero, write

\[
M(T)=a_kT^k+O(T^{k+1}),\qquad a_k\ne0.
\]

Plainly: \(k\) is the first nonzero Taylor order.

Weighted integrability holds exactly when \(k>2\). Thus the first admissible integer order is \(k=3\), but analyticity does not require a cubic term: quartic and higher leading orders are also admissible.

The change to \(z=|D_t|/c_f\) preserves the same exponent only if \(z\) and \(T\) are asymptotically comparable, for example

\[
z(T)=aT(1+o(1)),\qquad 0<a<\infty ,
\]

Plainly: near birth, \(z\) must shrink at the same linear rate as \(T\); defining \(z\) alone does not prove this.

More generally, if \(z(T)\sim aT^\beta\) and \(M(z)\sim Cz^k\), then integrability requires

\[
\beta k>2.
\]

Plainly: a nonlinear relation between the root derivative and time changes the maturity exponent required for finite impulse.

Therefore “analytic in \(z\) implies cubic first” is valid only after proving \(\beta=1\). For general \(\beta\), the first analytic order is the least integer strictly greater than \(2/\beta\).

**Grade:** derived.
**Falsifier:** direct evaluation of the weighted integral contradicting any stated convergence classification, or a certified birth asymptotic showing that \(z/T\) does not stay between positive finite bounds.

### 3.3 Nonuniqueness and the missing scale

Assume, solely for an explicit control, that \(z=aT\) near birth with \(a>0\), and introduce a transition scale \(z_0>0\), equivalently \(\tau=z_0/a\). Two admissible effective coupling laws are

\[
M_3(z)=
\begin{cases}
(z/z_0)^3,&0\le z<z_0,\\
1,&z\ge z_0,
\end{cases}
\qquad
M_4(z)=
\begin{cases}
(z/z_0)^4,&0\le z<z_0,\\
1,&z\ge z_0.
\end{cases}
\]

Plainly: both switch off sufficiently fast at birth and exactly recover the regular value after the same declared threshold, but one grows cubically and the other quartically.

For a scalar singular coefficient \(A\,T^{-3}\) with constant \(A\ne0\), their birth-region impulses are

\[
I_3=A\int_0^\tau\frac{(T/\tau)^3}{T^3}\,dT
=\frac{A}{\tau^2},
\qquad
I_4=A\int_0^\tau\frac{(T/\tau)^4}{T^3}\,dT
=\frac{A}{2\tau^2}.
\]

Plainly: both impulses are finite, but they differ by a factor of two, so finite integrability does not determine a unique outgoing velocity or history.

Smooth versions can be produced by joining either near-zero law to \(1\) with a smooth transition. Even requiring any finite number of matching derivatives at \(z_0\) does not restore uniqueness: a sufficiently small smooth perturbation supported strictly inside \((0,z_0)\) preserves positivity, endpoint matching, and weighted integrability while changing the impulse.

There is also a consistency distinction. The stated free law transports \(m\) unchanged along characteristics:

\[
(\partial_T+c_f\partial_R)m=0.
\]

Plainly: a stored value \(m\) cannot mature from zero to one during free propagation under this equation.

Thus the displayed \(M(z)\) laws can only be:

- reception-time output functions computed from \(z\) and an invariant stored state;
- solutions of a new sourced transport law;
- or emission labels whose apparent change comes from sampling different elements.

Treating \(m\) simultaneously as freely constant and as an age-evolving maturity is not closed.

The minimal extra selector is not another endpoint condition. It is a predeclared native constitutive generator, including its scale and initial data, such as a causal semigroup generator for the stored state plus a reception output map. Local Lipschitz regularity of that generator would provide mathematical uniqueness; it would not derive the generator itself.

**No-go statement.** Nonnegativity, finite birth impulse, recovery of \(M=1\) on a regular domain, and any finite collection of endpoint matching conditions leave an infinite family of maturity laws with different impulses. No unique transition follows from those conditions alone.

**Grade:** derived nonuniqueness; the two laws are inverse constructions used only as counterexamples.
**Falsifier:** derive, from an allowed supplied primitive, an additional functional equation whose unique solution satisfies all boundary and integrability requirements without introducing a free scale or fitted datum.

### 3.4 Conservation compatibility

Let

\[
\mathbf p(\mathbf v)=
\begin{cases}
P(\|\mathbf v\|)\mathbf v/\|\mathbf v\|,&\mathbf v\ne0,\\
\mathbf0,&\mathbf v=0,
\end{cases}
\qquad
k(\mathbf v)=K(\|\mathbf v\|).
\]

Plainly: these are the most general rotationally covariant single-particle motion accounts already assumed by the packet.

For a continuous receiver segment with acceleration \(\mathbf a\) and speed \(s>0\),

\[
\dot k
=
K'(s)\hat{\mathbf v}\cdot\mathbf a,
\qquad
\dot{\mathbf p}
=
\left[
P'(s)\hat{\mathbf v}\hat{\mathbf v}^{\mathsf T}
+
\frac{P(s)}{s}
\left(I-\hat{\mathbf v}\hat{\mathbf v}^{\mathsf T}\right)
\right]\mathbf a .
\]

Plainly: energy-account change reads the acceleration component along velocity, while momentum-account change can have both parallel and transverse pieces.

At an instantaneous reception with continuous position and velocity jump \(\mathbf I=\Delta\mathbf V_r\), local conservation requires

\[
\Delta E_{\mathcal W}
=
-\bigl[K(\|\mathbf V_r+\mathbf I\|)-K(\|\mathbf V_r\|)\bigr]-\Phi_E,
\]

Plainly: the wake and boundary energy change must cancel the chosen motion-energy change.

\[
\Delta\mathbf P_{\mathcal W}
=
-\bigl[\mathbf p(\mathbf V_r+\mathbf I)-\mathbf p(\mathbf V_r)\bigr]
-\boldsymbol\Phi_P,
\]

Plainly: the wake and boundary momentum change must cancel the chosen motion-momentum change.

\[
\Delta\mathbf J_{\mathcal W}
=
-\mathbf X_r\times
\bigl[\mathbf p(\mathbf V_r+\mathbf I)-\mathbf p(\mathbf V_r)\bigr]
-\boldsymbol\Phi_J .
\]

Plainly: because position does not jump, the receiver’s instantaneous orbital-angular-account change is position crossed with its momentum-account change.

These are compatibility identities, not selection principles. They determine required account changes only after \(K\), \(P\), the support of the wake update, and the boundary convention have already been fixed.

A sharper obstruction follows from the measure used in the packet. If \(e\) and \(\boldsymbol\pi\) are ordinary integrable densities and a reception changes them only at one coordinate \(a=(T_t,\boldsymbol\omega_{\mathsf h})\), then

\[
\int \Delta e\,dT_t\,d\boldsymbol\omega=0,
\qquad
\int \Delta\boldsymbol\pi\,dT_t\,d\boldsymbol\omega=\mathbf0 .
\]

Plainly: changing an ordinary function at one point does not change its integral, so it cannot balance a finite receiver update.

A local finite transfer therefore requires an atomic update such as

\[
\Delta\mu_E=\alpha\,\delta_a,
\qquad
\Delta\boldsymbol\mu_P=\boldsymbol\beta\,\delta_a,
\]

Plainly: the wake accounts must be measures with a finite lump at the reception coordinate, unless the reception acts over a finite patch or emits a nonlocal response.

For a strictly local atomic update at the intersection point \(\mathbf Y(a)=\mathbf X_r\), zero local boundary flux gives

\[
\alpha=-\Delta k,
\qquad
\boldsymbol\beta=-\Delta\mathbf p .
\]

Plainly: the atom carries the exact scalar and vector account changes required at that event.

Free propagation of the momentum atom changes its orbital angular account at rate

\[
\frac{d}{dT}
\left(\mathbf Y\times\boldsymbol\beta\right)
=
c_f\,\boldsymbol\omega\times\boldsymbol\beta .
\]

Plainly: a freely moving momentum atom preserves orbital angular account only when its vector account is parallel to its propagation direction, unless another angular account compensates it.

The regular impulse is radial, so \(\mathbf I=\lambda\boldsymbol\omega\). Requiring \(\Delta\mathbf p\parallel\boldsymbol\omega\) for every incoming \(\mathbf V\), every direction \(\boldsymbol\omega\), and every admissible \(\lambda\) forces

\[
\mathbf p(\mathbf V)=a\mathbf V,
\qquad
P(s)=a s
\]

for one constant account coefficient \(a\).

Plainly: if every local radial reception must hand its momentum account to a radially propagating wake atom without an intrinsic angular account, the motion momentum map must be linear in velocity.

Proof skeleton: write \(\mathbf p(\mathbf v)=g(\|\mathbf v\|)\mathbf v\). Resolve \(\mathbf v\) into components parallel and perpendicular to \(\boldsymbol\omega\). The transverse component of \(\mathbf p(\mathbf v+\lambda\boldsymbol\omega)-\mathbf p(\mathbf v)\) is \([g(\|\mathbf v+\lambda\boldsymbol\omega\|)-g(\|\mathbf v\|)]\mathbf v_\perp\). Vanishing for arbitrary \(\mathbf v_\perp,\lambda,\boldsymbol\omega\) forces \(g\) to be constant.

This coefficient is an account scale, not primitive physical mass. The result does not select its value or select \(K\).

Euclidean rotations and translations justify scalar dependence on speed, vector covariance, and orbital position-cross-momentum form. They do not determine the functions \(K\) and \(P\). Absolute-time translation also does not create a conserved scalar without a supplied action or constitutive balance law. An action or equivalent native pairing is still required to relate \(K\), \(P\), acceleration, emission, and reception.

Post hoc cancellation is vacuous because for any chosen differentiable \(K\) and \(P\), one may define wake increments by the negatives above. Choices such as \(K(s)=0\), \(K(s)=s^4\), or any other regular scalar produce different “conserved” ledgers for the same motion. Agreement then reports only how the ledger was defined.

**Grade:** derived compatibility identities; derived conditional point-event no-go; derived conditional linear-\(P\) theorem.
**Falsifier:** a finite local point update of ordinary densities that changes their stated integrals; or a nonlinear rotationally covariant \(P\) whose arbitrary radial increments are always radial and whose transported local wake momentum preserves angular account without extra angular support.

### 3.5 Trilemma adjudication

Split the wake state into a dynamically visible part \(W^{\mathrm d}\), read by later acceleration, and a passive account part \(W^{\mathrm p}\), never read by acceleration.

Let the universal regular law be \(A=F(\mathcal H_t,\text{current root geometry})\), with no dependence on earlier receptions. Suppose a reception changes \(W^{\mathrm d}\) by a map \(U\), and suppose the visibility is nontrivial: some admissible later reception has different acceleration when it samples \(W^{\mathrm d}\) and \(U(W^{\mathrm d})\). Independent reception then produces two histories with identical transmitter history and current root geometry but different earlier receptions. The later accelerations differ, contradicting universality.

Plainly: a reception-updated state cannot both affect later acceleration and remain absent from a law claimed to depend only on transmitter history.

Therefore one of the following must hold:

- the regular law is enlarged to read reception history or visible wake state;
- the reception update lies in the kernel of all later acceleration observations;
- or no dynamically visible reception update occurs.

A passive coordinate can provide a mathematical counterexample to an overbroad trilemma. If \(K\), \(P\), and a reception rule are predeclared, a passive account can be updated by fixed formulas that balance them while the active regular law remains unchanged. This does not contradict the precise theorem because the updated coordinate is not dynamically visible. It also does not derive that account’s interpretation or boundedness.

For repeated receptions, a passive scalar account obeying

\[
b_n=b_0-\sum_{j=1}^{n}\Delta K_j
\]

Plainly: its value is the initial capacity minus the cumulative receiver energy-account gains.

It is bounded below for all admissible histories only if those partial sums are uniformly bounded above or a replenishment/boundary rule is supplied. If \(\Delta K_j\ge\varepsilon>0\) infinitely often, then \(b_n\to-\infty\). An active wake component similarly needs an invariant bounded state set or a predeclared Lyapunov/capacity estimate; mere local update existence does not prevent blow-up or unbounded ledger drift.

**Grade:** derived visibility theorem; inferred terminology split.
**Falsifier:** two histories with identical transmitter history and root geometry but different visible wake states that nonetheless produce the same acceleration for every allowed future reception, while visibility remains operationally nontrivial.

### 3.6 Positive-emission-capacity obstruction

Let \(Q_E(T)\ge0\) be the total energy-account emission rate into retained wake state, before reception and boundary loss. On a finite window \([a,b]\),

\[
E_{\mathcal W}(b)-E_{\mathcal W}(a)
=
\int_a^b Q_E(T)\,dT
+\mathcal R_E[a,b]
-\Phi_E[a,b],
\]

Plainly: wake energy changes by emitted capacity plus reception transfers minus what crosses the retained boundary, using the declared sign convention.

If \(Q_E>0\) almost everywhere, the emission contribution is positive, but need not have a positive lower bound. If \(Q_E\ge q_0>0\), it is at least \(q_0(b-a)\). On an infinite window, positive emission causes unbounded cumulative wake energy exactly when

\[
\int_0^\infty Q_E(T)\,dT=\infty .
\]

Plainly: “positive forever” alone does not imply divergence; an integrable positive emission rate can have finite total capacity.

For an isolated global accounting domain with no outgoing boundary flux and no compensating reception return, conservation requires a debit of the same cumulative amount from another declared account. If that funding account is lower bounded, nonintegrable positive emission is impossible indefinitely.

The logically distinct escape routes within the allowed primitives are:

- emission stops or its rate is integrable — new emission constitutive data;
- emission draws from a finite predeclared capacity and therefore eventually weakens or stops — new capacity and depletion data;
- receptions causally replenish emission capacity — new reception-transfer data and no guarantee for an isolated transmitter;
- a retained finite window exports energy through explicit boundary flux — required bookkeeping for the window, but not removal of global energy;
- emission produces signed account contributions whose total is controlled — new sign and pairing data, and incompatible with an assumption of strictly positive wake energy if applied to the same account;
- the scalar is denied a physical energy interpretation — logically possible, but then it cannot establish physical energy closure;
- another passive reservoir is added — new state and constitutive data, not a derivation from the current primitives.

Allowing the account to run to \(-\infty\) is an algebraic escape only; it fails lower boundedness.

**Grade:** derived finite- and infinite-window account statements.
**Falsifier:** a lower-bounded, globally conserved account with nonintegrable strictly positive isolated emission and no debit, return, or boundary term.

### 3.7 Well-posedness architecture

A defensible local existence-and-uniqueness theorem would require at least:

- histories in a specified complete state space, for example \(C^1\) retained trajectories with absolutely continuous velocities;
- wake coupling in a space with defined directional traces, and wake accounts as finite Radon measures if receptions are point-local;
- free propagation as a strongly continuous causal transport or push-forward semigroup;
- locally integrable emission data and explicit retained-boundary traces;
- simple roots uniformly separated from \(D_t=0\) on regular charts;
- a certified one-sided normal form at each admitted root birth, with unique branch selection;
- separation of event times, or a deterministic commuting/priority rule for simultaneous events;
- no event accumulation in finite time unless a separate Zeno continuation theorem is proved;
- reception and emission maps locally Lipschitz on an invariant bounded set;
- acceleration and its state-Lipschitz modulus locally integrable through the singular birth;
- causal dependence only on retained history and present wake state;
- explicit behavior when required history has exited the retained domain.

Plainly: existence needs finite accumulated acceleration; uniqueness additionally needs small changes in the present state to cause integrably controlled changes in future acceleration.

At a coincident birth, finite impulse alone is insufficient for uniqueness. A useful sufficient condition is

\[
\int_0^\varepsilon
\left(
\|A(T,S)\|
+
\operatorname{Lip}_S A(T,\cdot)
\right)dT
<\infty .
\]

Plainly: both the acceleration and its sensitivity to the state must have finite area near birth.

Under these assumptions, one may solve by causal method of steps: transport the wake and integrate the history-dependent motion between events, apply the unique jump map at each event, and continue until the first boundary of the certified chart. This would prove only local mathematical well-posedness of the declared constitutive system.

Exact point updates make ordinary function spaces unsuitable for the account variables; point evaluation also is not defined on a generic \(L^1\) equivalence class. The state must either use continuous directional fields with finite-patch updates, or measure-valued accounts with a declared atomic reception rule.

**Grade:** inferred theorem architecture, based on standard mathematical sufficiency logic but not proved for an unseen concrete map.
**Falsifier:** a supplied map meeting these hypotheses that nevertheless admits two continuations from the same state, or a missing hypothesis exposed by a concrete finite-time failure.

## 4. Strongest derived result

**Conditional point-reception conservation theorem.** Assume:

1. a finite reception occurs at one emission coordinate;
2. wake accounts are updated only locally at that coordinate;
3. positions are continuous through reception;
4. the wake carries no intrinsic angular account;
5. the momentum account freely propagates with the intercepted surface direction;
6. regular reception impulses are radial;
7. closure is required for arbitrary non-circular incoming velocities.

Then:

- ordinary \(L^1(dT_t\,d\boldsymbol\omega)\) wake densities cannot balance a finite receiver-account jump;
- the local account must be atomic, patch-supported, or nonlocally redistributed;
- under the atomic strictly local option, angular compatibility for all receptions forces \(P(s)=a s\), with \(a\) an undetermined account coefficient.

**Claim grade:** derived, conditional mathematical theorem.
**Scope:** representation and local account compatibility only; it establishes neither physical momentum nor a conserved realized branch.
**Operator-checkable falsifier:** provide a concrete predeclared local reception map satisfying all seven assumptions with ordinary densities and a finite integral change, or with nonlinear \(P\) and no compensating angular support, then directly evaluate the energy, momentum, and angular totals through reception and subsequent free propagation.

## 5. Unsafe inference audit

The supplied material does not justify:

- that \((m,e,\boldsymbol\pi)\) are independent primitive coordinates;
- that ordinary function-valued wake accounts suffice for point receptions;
- that weighted integrability implies any pointwise \(O(T^{2+\delta})\) bound;
- that analyticity alone selects cubic order;
- that \(z\) is asymptotically linear in \(T\);
- that any particular transition scale or maturity interpolation is selected;
- that a freely transported \(m\) can also mature with age without another rule;
- that Euclidean or absolute-time symmetry selects \(K\) or \(P\);
- that inverse residual cancellation is a conservation derivation;
- that a passive balancing ledger is dynamically meaningful or lower bounded;
- that positive emission is globally sustainable;
- that local finite impulse implies unique continuation;
- that the wake accounts have physical energy or momentum interpretation;
- that any conditional linear-\(P\) result licenses primitive mass language;
- that a prescribed map, implementation replay, or same-path comparison provides independent physical evidence;
- any Master Equation, wake ontology, solver, conserved-quantity, retained-branch, or recovery-mechanism promotion.

## 6. Safe corrections

1. Replace “finite impulse hence \(M(T)=O(T^{2+\delta})\)” with:
   “Finite impulse is equivalent to \(M\in L^1((0,L),T^{-3}dT)\). A pointwise \(O(T^{2+\delta})\) bound is sufficient but not necessary.”

2. Add:
   “For nonnegative nondecreasing \(M\), integrability implies \(M=o(T^2)\), but this remains insufficient without the weighted integral condition.”

3. Replace “analytic first admits cubic” with:
   “If \(M\) is analytic in a variable asymptotically linear in \(T\), any nonzero leading Taylor order must be at least three; cubic is the first allowed order, not a uniquely selected order.”

4. Add the required change-of-variable hypothesis \(z(T)\sim aT\), or state the general criterion \(\beta k>2\) when \(z\sim aT^\beta\).

5. Distinguish stored transported maturity from reception-time effective coupling. If \(m\) changes along a free characteristic, the zero-source transport equation must be replaced or supplemented by a constitutive output map.

6. Specify whether \(e\) and \(\boldsymbol\pi\) are densities, finite measures, or patch-integrated quantities. A point reception cannot create a finite integral change in an ordinary density by changing one point.

7. Under strictly local atomic line-of-action exchange with no intrinsic angular account, record the conditional compatibility requirement \(P(s)=a s\), without interpreting \(a\) as primitive mass.

8. State that finite impulse and local uniqueness are separate gates; uniqueness requires an integrable state-sensitivity bound.

## 7. Rejected paths

- **Dimensional counting of \((m,e,\boldsymbol\pi)\):** fails without reachability and regular-encoding assumptions.
- **Pointwise big-\(O\) inferred from an integral:** fails because integrable spikes and logarithmic borderline laws are counterexamples.
- **Cubic interpolation as closure:** fails because infinitely many admissible higher-order and smoothly perturbed laws yield different impulses.
- **Changing stored \(m\) while enforcing homogeneous free transport:** internally incomplete unless the change occurs only at events or through a separate output map.
- **Pointwise update of ordinary account densities:** gives zero integral transfer.
- **Post hoc residual cancellation:** works for arbitrary \(K\), \(P\), and acceleration laws, so it has no selecting power.
- **Symmetry alone as a source of \(K\) and \(P\):** constrains covariance but not the radial functions or their pairing.
- **Passive ledger presented as active wake dynamics:** preserves the regular law only because the ledger is observationally invisible.
- **Positive continuous emission with no debit:** violates global lower-bounded conservation whenever cumulative emission is nonintegrable.
- **Finite impulse used as proof of uniqueness:** ignores state sensitivity, branch choice, simultaneous events, and event accumulation.
- **Boundary deletion:** manufactures loss unless the deleted account appears in \(\Phi_E,\boldsymbol\Phi_P,\boldsymbol\Phi_J\).

## 8. Decisive next calculation

Perform one **certified coincident-birth normal-form and state-sensitivity calculation** before choosing any maturity interpolation.

**Variables**

- the exact local causal-root equation for the quadratic same-transmitter control;
- birth-time parameter \(T\);
- \(D_t(T)\), \(z(T)=|D_t(T)|/c_f\), separation \(r(T)\), and radial direction;
- a symbolic candidate effective coupling \(M(z,W)\);
- the derivative of the receiver update with respect to the complete present state \(W\).

**Inputs**

- the finite Taylor jet of the stated exact quadratic control at birth;
- the regular acceleration component already supplied;
- a declared causal reception map, without fitted post-evolution residuals;
- symbolic \(c_f\).

**Method**

1. Derive the first nonzero asymptotic term \(z(T)=aT^\beta(1+o(1))\).
2. Substitute it into the singular acceleration.
3. Evaluate both the impulse integral and the integral of the state-Lipschitz modulus.
4. Prove one-sided branch uniqueness by a Volterra/Carathéodory contraction or exhibit two continuations.
5. Check matching to the declared regular domain without assuming a fitted scale.

**Pass condition**

\[
\int_0^\varepsilon
\left(
\|A(T,S)\|
+
\operatorname{Lip}_S A(T,\cdot)
\right)dT<\infty
\]

Plainly: the candidate passes only if the birth produces both finite accumulated acceleration and finite sensitivity to the initial present state.

It must also give one certified outgoing continuation from each admissible incoming state and recover the regular law on the declared regular chart.

**Fail condition**

- divergent impulse;
- finite impulse but divergent state-sensitivity integral;
- more than one admissible branch;
- \(z\) not asymptotically comparable to the variable used in the maturity proof;
- or regular matching that requires an unprovided scale or future receiver data.

**Claim changed**

A pass would raise only the coincident-birth continuation from “unresolved” to “derived local mathematical well-posedness for the declared constitutive candidate.” It would not establish conservation, physical interpretation, global existence, retained-branch status, or solver acceptance. A fail would provide a decisive analytic no-go for that candidate maturity/reception map.

## 9. Missing context

The supplied packet does not determine:

- the exact quadratic control’s Taylor coefficients or the exponent \(\beta\) relating \(z\) to \(T\);
- whether reception is mathematically point-local, patch-local, or distributional;
- the intended function or measure spaces for wake state;
- whether the line-of-action assumption constrains \(\boldsymbol\pi\) itself or only the receiver acceleration;
- whether an intrinsic angular account is categorically forbidden or merely absent from the current candidate;
- any native action or constitutive pairing;
- admissible emission and reception capacities;
- a lower bound for any scalar account;
- the exact retained-boundary geometry and flux convention;
- event simultaneity, event ordering, or Zeno policy;
- the full reception map needed for a complete well-posedness proof;
- any independent oracle or accepted instrument capable of promoting a local mathematical result.

## Stage 1 — Preliminary disposition

- The internal response materially narrows the blocker but does not resolve it.
- Its strongest unconditional correction is that finite coincident-birth
  impulse is equivalent to weighted integrability. The pointwise
  $O(T^{2+\delta})$ condition is sufficient, not necessary, under the live
  packet's declared general class.
- Its strongest conditional advance is that an exact point reception cannot
  transfer a finite account through ordinary
  $L^1(dT_e\,d\boldsymbol\omega)$ densities by changing a singleton.
- Its $K$--$P$ compatibility equation is already present in the Energy
  chapter. The response adds useful domain assumptions and a kinetic-Lagrangian
  reconstruction, but the equation itself is a duplicate.
- No current result selects a maturity function, reception-transfer
  coefficient, emission capacity, bounded account, or dynamically visible
  wake response.
- Priority disposition: MEC-002 remains `In progress`; MEC-003 and MEC-004
  remain `Deferred / blocked`. No queue row should change status.

Plainly: the consultation found a real mathematical error and two sharper
conditional theorems. It did not construct the one causal update required to
close the priority.

## Stage 2 — Follow-on prompts and responses

### Follow-on A — Coincident-birth asymptotic control

#### Prompt issued

```text
Closure goal:
Determine the exact coincident same-transmitter birth asymptotics needed to assess and safely incorporate the internal Tao-style response, including whether z=|D_t|/c_f is asymptotically linear in elapsed birth time and what the sharp maturity-integrability statement should be.

Use live repository access. This is a report-only mathematical follow-on; do not edit any file.

Read the exact owning material needed, prioritizing:
- reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md
- reference/priorities/master-equation-closure/characteristic-tail.md
- reference/priorities/app-solver/analysis/analysis-transmitter-factor-fold-and-coincident-birth.md
- any directly named exact quadratic control owner.
Preserve claim grades and distinguish prescribed-path control from realized dynamics.

Tasks:
1. Extract the exact quadratic same-transmitter control and derive r(T), D_t(T), z(T), and the singular acceleration scaling at the birth endpoint, showing sign conventions and domains.
2. Decide whether the live owner proves z=aT exactly, z~aT, or neither; identify a and all assumptions.
3. Replace any unsafe implication from weighted integrability to pointwise big-O with the sharpest correct statement under the regularity actually declared in live files.
4. Audit whether cubic is necessary, sufficient, first analytic order, or selected; give counterexamples where relevant.
5. Give exact proposed edits, each mapped to a file and heading, but do not apply them.
6. Give claim grade and operator-checkable falsifier for every proposed mathematical change.

Every technical unit must be immediately followed by Plainly:. Do not infer stability, physical realization, retained branches, or solver authority from a prescribed control.

Return: Findings; Derivation; Safe proposed corpus/priority edits; Rejected promotions; Remaining blocker.
```

#### Response received

## Findings

1. The exact quadratic prescribed control proves that the endpoint-born root has
   $z=aT$ **exactly**, with $a=\alpha/c_f$, not merely asymptotically.

2. The generic smooth-crossing analysis proves only
   $z=(\alpha/c_f)t+O(t^2)$.

3. The sharp maturity condition is weighted integrability. The live implication
   from weighted integrability to $O(T^{2+\delta})$ is false under the declared
   nonnegative, locally regular class.

4. Cubic suppression is sufficient and is the first possible nonzero analytic
   order. It is not necessary in the general locally regular class, and no cubic
   maturity function is selected by current primitives.

Plainly: the exact prescribed path gives a clean linear coordinate conversion, but the strongest general requirement on maturity is an integral condition, not a pointwise power law. Cubic behavior becomes mandatory only after adding analyticity as an assumption.

## Derivation

Let the exact quadratic control be

$$
\mathbf X(s)
=
\hat{\mathbf e}
\left(
c_fs+\frac12\alpha s^2
\right),
\qquad
c_f>0,
\qquad
\alpha>0.
$$

Plainly: this is a prescribed one-dimensional history whose speed crosses the primitive wake speed at $s=0$. It is an analytic control, not a realized EOM solution.

Take reception time $T_r=T>0$ and a past transmitter time $T_t=S<T$. On the local forward chart,

$$
\mathbf X(T)-\mathbf X(S)
=
\hat{\mathbf e}(T-S)
\left[
c_f+\frac{\alpha}{2}(T+S)
\right].
$$

Plainly: the bracket gives the average along-track speed between the two path points.

The causal-root equation is

$$
\left\|
\mathbf X(T)-\mathbf X(S)
\right\|
=
c_f(T-S).
$$

On the local forward branch, cancellation of $T-S>0$ gives

$$
c_f+\frac{\alpha}{2}(T+S)=c_f,
\qquad
S=-T.
$$

Plainly: the newborn noncoincident root is exactly the path point at the opposite time. The statement is local to the retained quadratic chart; it is not a global census of every root of a globally extended parabola.

At this root,

$$
\mathbf r(T)
=
\mathbf X(T)-\mathbf X(-T)
=
2c_fT\,\hat{\mathbf e},
\qquad
r(T)=2c_fT,
\qquad
\hat{\mathbf r}=\hat{\mathbf e}.
$$

Plainly: the quadratic displacement cancels between $T$ and $-T$, so the root separation is exactly linear in elapsed birth time.

The transmitter and receiver velocities are

$$
\mathbf V_t(-T)
=
(c_f-\alpha T)\hat{\mathbf e},
\qquad
\mathbf V_r(T)
=
(c_f+\alpha T)\hat{\mathbf e}.
$$

Therefore the canonical transmitter- and receiver-side factors are

$$
D_t
=
c_f-\hat{\mathbf r}\cdot\mathbf V_t(-T)
=
\alpha T>0,
\qquad
D_r
=
c_f-\hat{\mathbf r}\cdot\mathbf V_r(T)
=
-\alpha T.
$$

Plainly: the transmitter factor approaches zero from the positive side, while signed root playback reverses orientation because $D_r/D_t=-1$. The acceleration uses $|D_t|$; the signed playback ratio is a separate record.

Hence

$$
z(T)
=
\frac{|D_t|}{c_f}
=
\frac{\alpha}{c_f}T
=
aT,
\qquad
a=\frac{\alpha}{c_f}.
$$

Plainly: $z$ is exactly linear on this control. In normalized numerical units, $c_f=1$ and $a=\alpha$, but the derivation should retain symbolic $c_f$.

For same-transmitter reception, $\sigma=+1$ and
$K_i=\kappa|q_i|^2>0$. The canonical contribution is

$$
\mathbf A_{\mathrm{v1}}(T)
=
K_i
\frac{c_f}{r(T)^2|D_t(T)|}
\hat{\mathbf e}
=
\hat{\mathbf e}
\frac{K_i}{4\alpha c_f}T^{-3}.
$$

Plainly: inverse-square separation supplies $T^{-2}$ and the transmitter-side compression supplies another $T^{-1}$. Their product is the nonintegrable $T^{-3}$ singularity, directed forward along $\hat{\mathbf e}$.

If a nonnegative maturity factor $\mathcal M(z)$ multiplies this contribution, the accumulated acceleration is

$$
\Delta\mathbf V(L)
=
\hat{\mathbf e}
\frac{K_i}{4\alpha c_f}
\int_0^L
\frac{\mathcal M(aT)}{T^3}\,dT
=
\hat{\mathbf e}
\frac{K_i\alpha}{4c_f^3}
\int_0^{aL}
\frac{\mathcal M(z)}{z^3}\,dz.
$$

Plainly: because $z=aT$ exactly, the time and $z$ formulations have precisely the same convergence condition.

The sharp necessary-and-sufficient local condition is therefore

$$
\boxed{
\int_0^\varepsilon
\frac{\mathcal M(z)}{z^3}\,dz<\infty
}
$$

for some sufficiently small $\varepsilon>0$.

Plainly: this weighted integral, not a particular power law, is the complete local finiteness criterion for a nonnegative maturity on the exact control.

For the generic smooth crossing in the same owner, with
$t=T_r-T_0\to0^+$,

$$
\tau_*(t)=2t+O(t^2),
\qquad
r_*(t)=2c_ft+O(t^2),
\qquad
D_t(t)=\alpha t+O(t^2),
$$

and consequently

$$
z(t)
=
\frac{\alpha}{c_f}t+O(t^2),
\qquad
\mathbf A_{\mathrm{new}}(t)
=
\hat{\mathbf e}
\frac{K_i}{4\alpha c_f}t^{-3}
+O(t^{-2}).
$$

Plainly: exact linearity belongs to the quadratic control. A generic smooth prescribed crossing supplies only leading-order linearity and the same leading singular power.

### Sharp regularity audit

Under the live files’ general nonnegative, locally regular assumption,

$$
\int_0^\varepsilon
\frac{M(T)}{T^3}\,dT<\infty
\quad\not\Longrightarrow\quad
M(T)=O(T^{2+\delta})
\ \text{for any }\delta>0.
$$

Plainly: weighted integrability does not by itself force a pointwise power margin above quadratic.

For example, for sufficiently small $T$,

$$
M_1(T)
=
\frac{T^2}{[\log(1/T)]^2}
$$

is nonnegative, continuous at $T=0$ after setting $M_1(0)=0$, and satisfies

$$
\int_0^\varepsilon
\frac{M_1(T)}{T^3}\,dT
=
\int_0^\varepsilon
\frac{dT}{T[\log(1/T)]^2}
<\infty.
$$

But for every $\delta>0$,

$$
\frac{M_1(T)}{T^{2+\delta}}
=
\frac{T^{-\delta}}{[\log(1/T)]^2}
\longrightarrow\infty.
$$

Plainly: this is a direct counterexample to the current “Hence $O(T^{2+\delta})$” sentence, even without oscillations or sign cancellation.

Conversely,

$$
M_2(T)
=
\frac{T^2}{\log(1/T)}
$$

satisfies $M_2(T)=o(T^2)$ but

$$
\int_0^\varepsilon
\frac{M_2(T)}{T^3}\,dT
=
\int_0^\varepsilon
\frac{dT}{T\log(1/T)}
=
\infty.
$$

Plainly: even pointwise “faster than quadratic” is not sufficient at the logarithmic borderline. The weighted integral must remain the primary statement.

If an additional pure-power asymptotic is declared,

$$
M(T)\sim CT^p,
\qquad
C>0,
$$

then the condition is equivalent to

$$
p>2.
$$

Plainly: the power-law statement in the fold packet is correct within its explicitly stated pure-power class.

If $M$ is analytic at $z=0$,

$$
M(z)=\sum_{n=0}^{\infty}a_nz^n,
$$

then finite weighted impulse requires

$$
a_0=a_1=a_2=0.
$$

Thus every analytic admissible maturity obeys

$$
M(z)=O(z^3),
$$

and the first possible nonzero Taylor degree is three.

Plainly: cubic is the minimal analytic order, not a universally necessary profile. An analytic maturity may begin at fourth or higher order, and the current primitives do not select its coefficients, transition scale, or global shape.

A nonanalytic finite example is

$$
M(z)=\frac{z^{5/2}}{1+z^{5/2}},
$$

while an analytic cubic example is

$$
M(z)=\frac{z^3}{1+z^3}.
$$

Plainly: both give finite impulse, but only the second is analytic at birth. Their disagreement confirms that finiteness does not select a constitutive law.

## Safe proposed corpus/priority edits

### 1. `reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md`

Heading: `## 3. Coincident-birth requirement`

Replace the implication beginning with “Hence” by:

> On the exact quadratic control, $z=(\alpha/c_f)T$ exactly. The sharp necessary-and-sufficient local condition is
> $$
> \int_0^\varepsilon \frac{M(z)}{z^3}\,dz<\infty.
> $$
> Nonnegativity and local regularity alone imply no pointwise bound
> $M(z)=O(z^{2+\delta})$. That bound is sufficient but not necessary. If
> $M(z)\sim Cz^p$ with $C>0$, finiteness is equivalent to $p>2$. If $M$ is
> analytic at $z=0$, its coefficients through quadratic order must vanish, so
> $M(z)=O(z^3)$; cubic is the first possible nonzero analytic order, not a
> selected maturity law.

Plainly: this preserves the obstruction while correcting its strongest mathematical statement.

Claim grade: **derived on the exact prescribed quadratic control; conditional derived corollary under analyticity**.

Operator-checkable falsifier: a valid proof that the stated nonnegative locally regular class excludes
$M(z)=z^2/\log^2(1/z)$, or an analytic nonnegative $M$ with a nonzero coefficient of degree at most two and finite weighted integral.

Also replace “more-than-quadratic suppression” in the finding and disposition with “weighted-integrable coincident-birth suppression; equivalently, vanishing through quadratic Taylor order for analytic candidates.”

Plainly: this prevents the corrected theorem from drifting back to an unjustified pointwise claim.

### 2. `reference/priorities/app-solver/analysis/analysis-transmitter-factor-fold-and-coincident-birth.md`

Heading: `### 3.6 Why an event-only repair cannot work`

After the exact $D_t=\alpha T$ equation, add

$$
z=\frac{|D_t|}{c_f}=\frac{\alpha}{c_f}T
$$

and replace the general $O(z^{2+\delta})$ assertion with the weighted-integrability criterion and the pure-power/analytic corollaries above.

Plainly: this file owns the exact control, so it should own the exact $z$ conversion and the sharp theorem.

Claim grade: **derived prescribed-control identity and derived integrability criterion**.

Operator-checkable falsifier: direct substitution into the displayed quadratic history produces a different local newborn root, separation, or $D_t$, or the logarithmic counterexample fails the declared activation assumptions.

### 3. `reference/priorities/master-equation-closure/characteristic-tail.md`

Heading: `### Step 12 — Confront coincident same-transmitter birth`

Replace the paired integral-plus-$O(T^{2+\delta})$ display with the weighted integral alone as the unconditional pass condition. Add the pure-power and analytic consequences as explicitly conditional sentences.

Plainly: Step 12 should test the true acceptance condition and must not reject a finite logarithmically corrected maturity merely because it lacks a fixed positive power margin.

Claim grade: **safe mathematical correction to a priority acceptance test**.

Operator-checkable falsifier: a tail candidate satisfying the weighted integral but failing the actual finite accumulated-acceleration calculation on the exact control.

### 4. `reference/priorities/master-equation-closure/brainstorming.md`

Heading: `### Reception-Active Cubic Maturity and Account-Transfer Candidate`

Change “The derived part is only the endpoint order condition” to say that the derived part is weighted integrability; cubic is the first analytic order. Keep the clipped-cubic function explicitly unproven and unselected.

Plainly: the clipped cubic remains a useful candidate without being mistaken for the general theorem.

Claim grade: **safe clarification; proposal grade unchanged**.

Operator-checkable falsifier: a current primitive derivation selecting the clipped cubic’s scale and global form would supersede its unselected status.

### 5. `reference/priorities/master-equation-closure/work-log.md`

Entries dated `2026-07-24` and `2026-07-20`

Replace “must vanish more than quadratically” with “must satisfy the weighted-integrability condition; within an analytic ansatz its Taylor coefficients through degree two vanish.”

Plainly: this corrects the durable summary without changing queue status.

Claim grade: **safe summary correction**.

Operator-checkable falsifier: the owning packet retains a valid proof of the stronger unconditional pointwise statement.

### 6. `reference/priorities/app-solver/analysis/master-equation-import-audit-walkthrough-2026-07-18.md`

Heading: `### 6.1 Mathematical checks`, item 7

Make the same summary correction.

Plainly: this prevents an older audit summary from reintroducing the overstatement after the owner is repaired.

Claim grade: **safe duplicate-summary correction**.

Operator-checkable falsifier: mismatch with the corrected owning theorem.

### Reader-facing corpus disposition

No immediate reader-facing edit is required. The searched dynamics files state only the nonintegrable birth warning and fail-closed status; they do not currently repeat the false pointwise implication.

Plainly: the correction belongs first in the priority research record. The corpus should receive it only if a future reader-facing section explicitly states the maturity theorem.

## Rejected promotions

- Reject promotion of $M=O(z^{2+\delta})$ as a necessary condition under mere local regularity.
- Reject promotion of a nonzero cubic coefficient as necessary.
- Reject selection of $z^3/(1+z^3)$, a clipped cubic, or any transition scale from finiteness alone.
- Reject interpreting the exact quadratic control as a realized persistent branch, a stability result, or EOM solver authority.
- Reject using $D_r/D_t=-1$ to cancel the acceleration singularity; playback and acceleration strength are separate records.
- Reject an event-only value at $T=0$ as a repair. The divergence lives on every open post-birth neighborhood.

Plainly: the calculation narrows the admissible maturity class but does not derive the constitutive law or physical continuation.

## Remaining blocker

The smallest unresolved theory decision is not the order of vanishing. It is whether an Architrino-native causal wake update can derive one complete nonnegative maturity function, including its transition scale and reception dependence, together with bounded energy and vector-momentum accounts on the same update.

Plainly: the mathematics now says exactly which functions make the prescribed singular control finite. It still does not say which function nature uses, how that function evolves, or how its wake accounts close.

Thread state: DONE
Mode: audit/report
Authority used: report-only
Files changed: none
Validation: analytic substitution and counterexample audit completed; no executable validation required
Closure goal: incorporate the exact linear birth chart and weighted-integrability correction into the evolving review-packet research note without promoting the prescribed control into realized dynamics.

### Follow-on B — Conservation, measure class, and angular compatibility

#### Prompt issued

```text
Closure goal:
Assess whether the internal Tao-style conservation results yield safe, exact corpus changes: the point-reception measure obstruction, the K-P compatibility relation, the conditional linear-P theorem, and angular-account conditions.

Use live repository access. This is a report-only mathematical follow-on; do not edit any file.

Read the exact owning material needed, prioritizing:
- reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md
- content/markdown/aaa/dynamics/energy.md
- content/markdown/aaa/dynamics/effective-lagrangian.md
- content/markdown/aaa/dynamics/master-equation.md
- nearby canon directly cited by those files.
Preserve all existing edits and live claim grades.

Tasks:
1. Verify the definitions and integration measures for e and pi. Determine precisely whether a point-local finite transfer is impossible for ordinary L1 densities and what alternatives (Radon atom, finite patch, nonlocal redistribution) are mathematically valid without adding ontology.
2. Derive the exact compatibility relation between rotational motion accounts K(s) and P(s) from dot K = V dot dot P for arbitrary acceleration. Compare against live energy prose and identify duplicates.
3. Prove or reject the conditional claim that strict local radial transfer, free radial propagation, and no intrinsic angular account force P(s)=a s. State every assumption and identify whether the theorem belongs in corpus or only priority material.
4. Audit angular momentum lever arms at the reception point and after free propagation.
5. Produce exact proposed changes mapped to file and heading, separating safe correction, mathematical advance, useful clarification, duplicate, unsafe import, unsupported inference, and operator discussion.
6. Supply claim grade and operator-checkable falsifier for each accepted item.

Every technical unit must be immediately followed by Plainly:. Do not call the coefficient a primitive mass, do not import standard mechanics as a premise, and do not promote a ledger identity to physical conservation.

Return: Findings; Derivations; Evidence classification table; Proposed corpus/priority changes; Rejected comments; Smallest unresolved decision.
```

#### Response received

## Findings

1. A point reception cannot transfer a finite nonzero amount through ordinary \(L^1\) wake-account densities integrated against \(dT_e\,d\Omega\). A singleton has zero product measure, so changing \(e\) or \(\boldsymbol\pi\) only at \((T_e,\boldsymbol\omega_{\mathsf h})\) leaves the integrated totals unchanged.

   Plainly: a density value at one exact direction and emission time contributes zero area under the integral.

2. Three mathematically valid alternatives exist:

   - finite signed/vector Radon measures with a Dirac atom at reception;
   - an \(L^1\) update spread over a predeclared positive-measure patch;
   - an explicitly nonlocal redistribution kernel.

   None selects the transfer amount, proves bounded energy, or closes conservation. The first two can remain mathematical representations without adding a material substance, but accepting an independently evolving account state would still change the present corpus dependency claim that wakes have no freely specifiable state beyond transmitter history.

   Plainly: these alternatives repair the bookkeeping space, not the missing physical rule.

3. The live Energy chapter already contains the correct work-power compatibility relation
   \[
   P'(s)=\frac{K'(s)}{s}.
   \]
   It is therefore a duplicate, not a new derivation. What is new is that the live statement lacks the necessary near-zero assumptions and describes the relation too loosely as direct conjugacy “of \(K\).”

   Plainly: the formula is already present, but its domain and interpretation need tightening.

4. Strict local radial transfer, free radial propagation, and no separate angular account force
   \[
   P(s)=a\,s
   \]
   on every connected admitted speed interval, provided oblique velocity/line-of-action configurations are allowed. Combining this with the live \(K\)-\(P\) compatibility relation forces
   \[
   K(s)=\frac{a}{2}s^2
   \]
   after \(K(0)=0\).

   Plainly: if both acceleration and its locally balancing momentum change must always point along the hit line, only the quadratic bookkeeping family survives.

5. Angular momentum closes instantaneously only when the receiver credit and wake debit use the same reception-point lever arm. Free radial propagation adds a second condition: pointwise conservation requires wake momentum to remain radial, while global conservation permits only the weaker cancellation of all sector torques after integration.

   Plainly: matching the momentum amounts is insufficient; their positions and later transport directions must also match.

## Derivations

### 1. Point-reception measure obstruction

Let
\[
Z_T=I_T\times S^2,
\qquad
\mu=dT_e\otimes d\Omega,
\]
where normalization of \(d\Omega\) is irrelevant to null sets. Suppose
\[
e\in L^1(Z_T,\mu),
\qquad
\boldsymbol\pi\in L^1(Z_T,\mu;\mathbb R^3).
\]
If a reception update is supported only at \(z_{\mathsf h}=(T_e,\boldsymbol\omega_{\mathsf h})\), then
\[
\int_{Z_T}(e^+-e^-)\,d\mu=0,
\qquad
\int_{Z_T}(\boldsymbol\pi^+-\boldsymbol\pi^-)\,d\mu=\mathbf0.
\]

Plainly: two ordinary integrable densities that differ only at one point define the same integrated account.

The exact local measure-valued alternative is
\[
\mathsf E^+
=
\mathsf E^-+\Delta E_{\mathsf h}\,\delta_{z_{\mathsf h}},
\qquad
\boldsymbol{\mathsf\Pi}^+
=
\boldsymbol{\mathsf\Pi}^-
+\Delta\mathbf p_{\mathsf h}\,\delta_{z_{\mathsf h}},
\]
with \(\mathsf E\) a finite signed Radon measure and \(\boldsymbol{\mathsf\Pi}\) a finite vector-valued Radon measure.

Plainly: a Dirac atom gives one exact reception a finite integral, but the theory must still derive the atom’s magnitude, sign, visibility, and lower bound.

A finite-patch alternative uses \(g_{\mathsf h}\in L^1(\mu)\) with
\[
\operatorname{supp}g_{\mathsf h}\subset A_{\mathsf h},
\qquad
\mu(A_{\mathsf h})>0,
\qquad
\int g_{\mathsf h}\,d\mu=1,
\]
and updates
\[
e^+=e^-+\Delta E_{\mathsf h}g_{\mathsf h},
\qquad
\boldsymbol\pi^+
=
\boldsymbol\pi^-+\Delta\mathbf p_{\mathsf h}g_{\mathsf h}.
\]

Plainly: this keeps ordinary densities, but exact point locality is replaced by a patch whose width and shape must be derived or predeclared.

A continuous receiver history traces a lower-dimensional set in \(I_T\times S^2\), so an exact source supported on that trace remains singular with respect to \(dT_e\,d\Omega\). Mollification can approximate it by patches, but the limit is measure-valued.

Plainly: accumulating many exact point hits does not automatically turn them into an ordinary smooth density.

### 2. Exact \(K\)-\(P\) compatibility

For \(s=\|\mathbf V\|>0\), write
\[
\mathbf u=\frac{\mathbf V}{s},
\qquad
\mathbf p(\mathbf V)=P(s)\mathbf u,
\qquad
\dot s=\mathbf u\cdot\mathbf A.
\]
Then
\[
\dot K
=
K'(s)\,\mathbf u\cdot\mathbf A,
\qquad
\mathbf V\cdot\dot{\mathbf p}
=
sP'(s)\,\mathbf u\cdot\mathbf A.
\]
Therefore
\[
\dot K=\mathbf V\cdot\dot{\mathbf p}
\quad\text{for arbitrary }\mathbf A
\quad\Longleftrightarrow\quad
K'(s)=sP'(s).
\]

Plainly: arbitrary acceleration lets the common velocity projection be varied, forcing the two speed derivatives to agree coefficient by coefficient.

The integral formula
\[
P(s)=\int_0^s\frac{K'(u)}{u}\,du
\]
requires \(K'(u)/u\) to be locally integrable at zero. A sufficient regularity package is \(K'(0)=0\) with \(K\in C^2\) near zero. Strict convexity, monotonicity, and \(K(0)=0\) alone are insufficient: \(K(s)=s+s^2\) satisfies those three properties but makes the integral diverge logarithmically.

Plainly: the current Energy assumptions do not by themselves guarantee a finite momentum account at rest.

The exact Legendre reconstruction is
\[
\ell_K(s)\equiv sP(s)-K(s).
\]
Using \(K'=sP'\),
\[
\ell_K'(s)=P(s),
\qquad
\frac{\partial\ell_K}{\partial\mathbf V}
=
P(s)\mathbf u=\mathbf p,
\qquad
\mathbf p\cdot\mathbf V-\ell_K=K.
\]

Plainly: \(K\) is the kinetic Hamiltonian energy, while \(\ell_K=sP-K\) is the kinetic Lagrangian that directly generates the canonical momentum.

### 3. Conditional linear-\(P\) theorem

For \(\mathbf v=s\mathbf u\neq0\), the derivative of the isotropic momentum map in a unit line-of-action direction \(\mathbf n\) is
\[
D\mathbf p(\mathbf v)[\mathbf n]
=
\frac{P(s)}{s}\mathbf n
+
\left(
P'(s)-\frac{P(s)}{s}
\right)
(\mathbf u\cdot\mathbf n)\mathbf u.
\]

Plainly: a radial velocity change generally produces a momentum change with an additional component along the existing velocity.

Assume:

- \(P\in C^1\) on a connected speed interval;
- every per-hit acceleration increment is parallel to \(\mathbf n\);
- its local balancing wake-momentum update must also be parallel to \(\mathbf n\);
- no simultaneous cross-sector redistribution or intrinsic angular account is allowed;
- admissible histories include oblique pairs with \(0<|\mathbf u\cdot\mathbf n|<1\).

For the displayed derivative to remain parallel to \(\mathbf n\) for every oblique pair, its \(\mathbf u\)-component must vanish:
\[
P'(s)-\frac{P(s)}{s}=0.
\]
Hence
\[
sP'(s)=P(s),
\qquad
P(s)=a\,s.
\]

Plainly: an oblique hit exposes the nonradial part of every nonlinear isotropic momentum map.

Combining this theorem with \(K'=sP'\) gives
\[
K'(s)=a\,s,
\qquad
K(s)=\frac{a}{2}s^2
\]
after \(K(0)=0\). The coefficient \(a\) is a universal bookkeeping coefficient, not primitive physical mass.

Plainly: the quadratic proxy is selected by the combined collinearity and work-power requirements, not imported from a mass law.

The ontology-neutral radial-collinearity lemma belongs safely in the Energy corpus. Its application to an independently evolving point-local wake account should remain priority-only until that state class is accepted.

### 4. Angular-account audit

For one wake sector,
\[
\mathbf Y=\mathbf C+R\boldsymbol\omega,
\qquad
\dot{\mathbf Y}=c_f\boldsymbol\omega.
\]
Under free transport of \(\boldsymbol\pi\),
\[
\frac{d}{dT}
\left[
(\mathbf Y-\mathbf X_0)\times\boldsymbol\pi
\right]
=
c_f\boldsymbol\omega\times\boldsymbol\pi.
\]

Plainly: a nonradial momentum vector carried outward along a radial trajectory changes its orbital angular account even when its momentum itself is constant.

Thus pointwise angular constancy without another account requires
\[
\boldsymbol\omega\times\boldsymbol\pi=\mathbf0
\quad\text{almost everywhere}.
\]
If only total angular conservation is required, the weaker condition is
\[
\int_{Z_T}
\boldsymbol\omega\times\boldsymbol\pi\,d\mu
=
\mathbf0,
\]
together with boundary terms.

Plainly: every sector must be radial for sectorwise closure, while a global ledger may permit carefully balanced nonradial sectors.

At reception, let the receiver be at \(\mathbf X_r=\mathbf Y(z_{\mathsf h})\). If
\[
\Delta\boldsymbol{\mathsf\Pi}
=
-\Delta\mathbf p_r\,\delta_{z_{\mathsf h}},
\]
then
\[
(\mathbf X_r-\mathbf X_0)\times\Delta\mathbf p_r
+
(\mathbf Y-\mathbf X_0)\times(-\Delta\mathbf p_r)
=
\mathbf0.
\]

Plainly: an equal and opposite momentum update cancels angular momentum instantly when both sides use the exact same spatial point.

If the wake debit is instead booked at the frozen emission center \(\mathbf C\), the residual is
\[
\Delta\mathbf J
=
(\mathbf Y-\mathbf C)\times\Delta\mathbf p_r
=
R\boldsymbol\omega\times\Delta\mathbf p_r.
\]

Plainly: moving the wake side’s lever arm back to the emission center creates a torque residual unless the transferred momentum is radial.

A concrete counterexample sets \(c_f=1\), \(\mathbf C=\mathbf X_0=\mathbf0\), \(R=1\), \(\boldsymbol\omega=\mathbf e_x\), and \(\Delta\mathbf p_r=\mathbf e_y\). The receiver gains \(\mathbf e_z\) angular momentum, while a center-booked wake debit carries zero angular momentum.

Plainly: the mismatch is one full unit of angular account even though linear momentum balances exactly.

## Evidence classification

| Item | Verdict | Grade and falsifier | Exact destination and disposition |
|---|---|---|---|
| Singleton \(L^1\) obstruction | Mathematical advance | Derived conditional theorem. Falsified only by a nonzero \(dT_e\,d\Omega\) integral supported on one singleton, or by showing the owner uses a different non-null base measure. | `analysis-independent-causal-wake-state.md`, new measure-class subsection under §2 or §4. Promote now to priority only. |
| Radon atom | Useful clarification | Valid mathematical construction, not physical closure. Fails if the measure has infinite total variation or if its coefficient is chosen from the residual afterward. | Same packet; list as one admissible representation requiring a constitutive rule. |
| Finite patch | Useful clarification | Valid \(L^1\) construction. Fails point-locality or becomes arbitrary if patch width/kernel is not predeclared. | Same packet; candidate only. |
| Nonlocal redistribution | Operator discussion | Mathematically valid but changes the locality premise and may trigger the regular-law visibility trilemma. | MEC-002/MEC-004 research route; do not promote as accepted mechanism. |
| \(P'=K'/s\) | Duplicate | Already in `energy.md` lines 37–43 and `master-equation.md` lines 4624–4627. Falsifier: a \(C^1\) pair satisfying work-power equality for arbitrary acceleration while violating the relation. | No duplicate equation needed. Tighten assumptions and interpretation in Energy. |
| Near-zero regularity | Safe correction | Derived domain correction. Counterexample \(K=s+s^2\) falsifies the current sufficiency of strict convexity, monotonicity, and \(K(0)=0\). | `energy.md`, “Kinetic Energy and Momentum of a Single Architrino.” |
| \(\ell_K=sP-K\) reconstruction | Mathematical advance | Derived identity. Falsified by direct failure of \(\ell_K'=P\) or \(\mathbf p\cdot\mathbf V-\ell_K=K\). | `energy.md`; cross-reference from `effective-lagrangian.md`. |
| Linear-\(P\) theorem | Mathematical advance | Derived conditional theorem. Falsified by one admissible oblique hit with nonlinear \(P\) and exact radial-only local closure without another account. | Ontology-neutral lemma in `energy.md`; wake-state application in the priority packet. |
| Quadratic-\(K\) corollary | Mathematical advance | Derived under both compatibility conditions. Falsified if \(P=as\), \(K'=sP'\), \(K(0)=0\), but \(K\neq as^2/2\). | Energy clarification and priority obstruction. |
| Instant same-lever-arm cancellation | Safe correction | Exact vector identity. Falsified if the two account updates occur at the same point with equal/opposite momentum but leave nonzero instantaneous angular residual. | Priority packet §1–§2 angular-account paragraph. |
| Free-propagation radial condition | Mathematical advance | Derived pointwise; globally only the integrated condition is necessary. Falsifier: constant \(\boldsymbol\pi\), radial \(\dot{\mathbf Y}\), no other account, and constant \(\mathbf Y\times\boldsymbol\pi\) with \(\boldsymbol\omega\times\boldsymbol\pi\neq0\). | Priority packet; concise conditional clarification may later enter Energy. |
| “No intrinsic angular account is needed” without qualifications | Unsupported inference | False unless reception lever arms match and free transport satisfies the pointwise or integrated torque condition. | Correct locally in the priority packet; do not propagate unqualified. |
| Radon atom as proof of conservation | Rejected comment | Representation does not derive coefficients, lower bounds, emission capacity, or visibility. | No corpus promotion. |
| Treat \(a\) as primitive mass | Unsafe import | Barred by substrate ontology and unnecessary mathematically. | Reject everywhere. |

Plainly: the durable advances are the measure obstruction, corrected kinetic-account theorem, and angular lever-arm conditions; none derives the missing reception coefficients.

## Proposed corpus and priority changes

### Safe corpus corrections

- In `content/markdown/aaa/dynamics/energy.md`, under “Kinetic Energy and Momentum of a Single Architrino”:

  - retain \(P'=K'/s\);
  - state the needed assumptions \(K'(0)=0\) and local integrability of \(K'(s)/s\);
  - describe it as work-power compatibility;
  - add \(\ell_K=sP-K\) as the kinetic Lagrangian that generates \(\mathbf p\);
  - add the ontology-neutral radial-collinearity lemma.

  Claim grade: derived mathematical identity. Falsifier: the explicit derivative tests above.

- In `content/markdown/aaa/dynamics/effective-lagrangian.md`, under “Regularized Nonlocal Action and Variation”:

  - state that the displayed action currently uses the quadratic bookkeeping proxy;
  - do not replace its kinetic term by the energy \(K(s)\);
  - a general kinetic chart would require \(\ell_K=sP-K\) and a complete revariation;
  - preserving arbitrary radial per-hit acceleration with a scalar radial momentum exchange forces the quadratic family.

  Claim grade: safe correction plus conditional no-go. Falsifier: a fully varied nonquadratic action reproducing the same arbitrary line-of-action acceleration while retaining radial-only local momentum exchange.

- In `content/markdown/aaa/dynamics/effective-lagrangian.md`, under “Symmetries and History-Aware Conservation Laws,” use \(K_\mu\), not an unrestricted \(K\), for conservation claims derived from the currently displayed quadratic action unless the generalized kinetic action is separately supplied.

  Claim grade: safe scope correction. Falsifier: a live derivation in that section already using \(\ell_K\) and reproducing the Master EOM for general \(K\); none is present.

- In `content/markdown/aaa/dynamics/master-equation.md`, “Generalized Momentum and Angular Momentum,” retain the existing cross-reference as the duplicate authority. Add only a scope sentence that the displayed action-derived charges use the quadratic proxy; general \(K\) requires the separate kinetic-action construction.

  Claim grade: useful clarification. Falsifier: same as above.

### Priority-only advances

Add to `reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md`:

1. the \(L^1\) singleton obstruction and explicit account-measure definition;
2. the three admissible representation classes, without selecting one;
3. the same-reception-point lever-arm lemma;
4. the free-propagation torque condition;
5. the conditional linear-\(P\)/quadratic-\(K\) no-go;
6. the consequence that a nonquadratic motion account requires at least one of:

   - nonradial or cross-sector wake momentum redistribution;
   - an additional angular account;
   - a nonlocal action-derived boundary charge;
   - a restriction excluding oblique hit geometries.

Plainly: this turns the conservation gap into a sharper fork rather than pretending to solve it.

The result narrows MEC-002 and MEC-004 but resolves neither. No work-queue status should change: reception coefficients, emission capacity, boundedness, and dynamically visible versus inert account behavior remain unselected.

Plainly: the queue blocker is smaller and more precise, but still active.

## Rejected comments

- Reject any claim that an ordinary pointwise density update carries finite total account.
- Reject any claim that choosing Radon measures derives conservation.
- Reject the inference \(P=as\) from \(P'=K'/s\) alone; radial collinearity is an additional assumption.
- Reject unqualified claims that line-of-action acceleration guarantees angular closure.
- Reject wake momentum debited at the emission center when the receiver credit occurs at the reception point, except for strictly radial transfer.
- Reject direct substitution of the energy \(K(s)\) as the kinetic Lagrangian.
- Reject interpreting the universal coefficient \(a\) as primitive architrino mass.
- Reject corpus promotion of an autonomous reception-updated wake state while `foundations/architrino.md` still says the wake has no freely specifiable state beyond transmitter history.

## Smallest unresolved decision

The smallest operator decision is whether the MEC-002 proof program may enlarge \((e,\boldsymbol\pi)\) from ordinary densities to finite Radon measures with reception atoms.

If yes, the next decisive calculation is one oblique reception with \(c_f=1\), finite \(\Delta\mathbf V\parallel\boldsymbol\omega\), and a predeclared atomic wake debit. It must report momentum, reception-point angular balance, post-reception free-propagation torque, energy lower-bound behavior, and later-receiver visibility on the same update.

If no, strict exact point-local reception is closed negatively for ordinary \(L^1\) accounts. The remaining routes are a derived finite patch or an explicitly nonlocal action/boundary construction.

Plainly: the next step is not another broad conservation discussion; it is a choice of account measure followed by one oblique-hit falsification test.

Thread state: DONE
Mode: audit/report
Authority used: report-only
Files changed: none
Validation: not run; mathematical derivations checked directly against live owner and nearby canon
Closure goal: Decide the admissible reception-account measure, then execute the oblique-hit momentum and angular closure test on that exact representation.

## Proposed corpus changes

### Evidence-conscious assessment

| Response item | Verdict | Evidence and claim grade | Operator-checkable falsifier | Exact destination | Recommended disposition |
| --- | --- | --- | --- | --- | --- |
| Direction-resolution quotient | Useful clarification | The live owner already says different wake directions must be distinguishable and that $(m,e,\boldsymbol\pi)$ need not be independent coordinates. The operational quotient sharpens that statement. Grade: inferred representation theorem under Markov, locality, and independent-reception assumptions. | A sufficient present state that identifies two independently updatable, reception-distinguishable directions while predicting both futures uniquely. | `reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md`, §1 | Add the quotient formulation at priority grade; do not promote a three-independent-coordinate ontology. |
| Weighted-integrability correction | Safe correction and mathematical advance | The exact quadratic control gives $D_t=\alpha T$, $z=(\alpha/c_f)T$, and acceleration proportional to $T^{-3}$. The continuous example $M(T)=T^2/\log^2(1/T)$ has finite weighted integral but violates every $O(T^{2+\delta})$ bound. Grade: derived on the prescribed control. | Direct substitution into the exact control yields a different $D_t$ or the logarithmic example fails the stated integral. | Owning priority §3; exact-control analysis §3.6; characteristic-tail Step 12; work-log summaries | Correct the unconditional implication. Retain cubic only as the first possible analytic order. |
| Explicit maturity nonuniqueness | Mathematical advance | Cubic and quartic controls with the same matching threshold give different finite impulses. Grade: derived counterexample family; the functions are inverse constructions, not candidates selected by the theory. | An allowed native functional equation uniquely fixes the complete maturity law and scale. | Owning priority §3 | Add one explicit two-law counterexample and preserve underdetermination. |
| Homogeneous transport versus age-changing maturity | Safe correction | The displayed free law transports stored $m$ unchanged along a characteristic. An age-changing maturity therefore needs a sourced evolution or a reception-time output map. Grade: derived compatibility observation. | A definition showing that the stored variable changes with age while still satisfying the homogeneous transport equation. | Owning priority §§2–3 | Define whether $m$ is stored state or an output computed from stored state and root geometry. |
| Singleton $L^1$ reception obstruction | Mathematical advance | A singleton has zero $dT_e\,d\boldsymbol\omega$ measure, so changing an ordinary density only there changes no integrated total. Grade: derived conditional theorem. | A nonzero ordinary-density integral whose update is supported on one singleton under the stated base measure. | Owning priority, new measure-class subsection under §2 or §4 | Promote to priority grade. List atom, finite patch, and nonlocal redistribution as unselected alternatives. |
| $P'(s)=K'(s)/s$ | Duplicate with useful domain correction | The equation already appears in `content/markdown/aaa/dynamics/energy.md` and is cross-referenced by `master-equation.md`. The integral for $P$ additionally needs local integrability of $K'(s)/s$ near zero. Grade: existing scaffold plus derived domain correction. | A differentiable pair satisfying work-power equality for arbitrary acceleration while violating the equation, or a proof that the existing assumptions alone make the integral finite at zero. | Energy, “Kinetic Energy and Momentum of a Single Architrino” | Do not duplicate the equation. Add its near-zero assumptions and identify it as work-power compatibility. |
| $\ell_K=sP-K$ reconstruction | Mathematical advance | Given $K'=sP'$, direct differentiation gives $\ell_K'=P$ and $\mathbf p\cdot\mathbf V-\ell_K=K$. Grade: derived identity. | Direct differentiation fails under the stated smoothness assumptions. | Energy kinetic section; Effective Lagrangian kinetic-term scope note | Add as the general kinetic-Lagrangian reconstruction; do not substitute $K$ itself as a Lagrangian. |
| Conditional linear-$P$/quadratic-$K$ theorem | Mathematical advance with narrow hypotheses | If every oblique radial hit must produce a radial local momentum-account transfer, with no cross-sector redistribution or intrinsic angular account, then $P'=P/s$, hence $P=as$; compatibility then gives $K=as^2/2$. Grade: derived conditional theorem. | One admitted oblique hit with nonlinear $P$ and exact radial-only local closure under all stated exclusions. | Ontology-neutral lemma in Energy; wake-state application in the owning priority | Propose the lemma for corpus inclusion, but keep its wake-state consequence priority-only until the representation is accepted. |
| Angular lever-arm and transport conditions | Safe correction and mathematical advance | Instantaneous angular cancellation requires equal and opposite momentum updates at the same reception point. Free pointwise orbital closure additionally requires $\boldsymbol\omega\times\boldsymbol\pi=0$, or the weaker integrated torque cancellation when cross-sector balance is allowed. Grade: derived conditional identities. | Same-point equal/opposite transfers leave a nonzero instant residual, or a freely transported nonradial sector keeps constant orbital angular account without compensation. | Owning priority §§1–4; later concise Energy clarification | Qualify the current statement that no intrinsic angular account is needed. |
| Visibility trilemma | Useful clarification | A reception-updated component that changes later acceleration makes the later law depend on more than transmitter history; a passive component avoids that conflict but does not derive bounded or physical accounts. Grade: derived under operational visibility and independent-reception assumptions. | Two histories differing only in an operationally visible earlier reception yield identical acceleration for every admissible future reception. | Owning priority §5 | State as a conditional visibility theorem, not an exhaustive ontology theorem. |
| Positive-emission obstruction | Useful clarification | Finite-window balance is exact, while infinite growth requires a nonintegrable positive emission rate. Grade: derived bookkeeping estimate, physical interpretation missing. | A lower-bounded global account sustains nonintegrable positive isolated emission without debit, return, or boundary term. | Owning priority §6 | Add the finite/infinite-window distinction; do not treat a boundary flux as removal of global account. |
| Well-posedness architecture | Proposed testable route | Finite impulse alone does not control state sensitivity, event accumulation, or branch selection. Grade: inferred theorem architecture, not a proof for an unspecified map. | A declared update satisfies the proposed state-space, Lipschitz, event, and integral assumptions but has two local continuations. | MEC-002/MEC-003 focused proof packet or owner §8 | Use as the acceptance design for a future concrete map, not as a new generic gate. |

Plainly: only the weighted-integrability statement is an unconditional
correction to the current owner. The measure and momentum results are rigorous
only after their stated representation and locality assumptions are chosen.

### Proposed change batch A — priority mathematical corrections

1. In
   `reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md`,
   replace the implication from weighted integrability to
   $O(T^{2+\delta})$ with the exact weighted-integrability condition. State
   separately that a pure power needs exponent greater than two and that an
   analytic law must vanish through quadratic order.
2. In that same owner, add the exact-control relation
   $z=(\alpha/c_f)T$, the logarithmic counterexample, and one explicit
   cubic-versus-quartic nonuniqueness calculation.
3. In that same owner, declare the mathematical class of $e$ and
   $\boldsymbol\pi$. If exact reception remains point-local, ordinary
   $L^1(dT_e\,d\boldsymbol\omega)$ densities are insufficient for a finite
   transfer.
4. Qualify the angular-account paragraph with the same-reception-point lever
   arm and free-propagation torque conditions.
5. Propagate only the corrected maturity summary into
   `reference/priorities/app-solver/analysis/analysis-transmitter-factor-fold-and-coincident-birth.md`,
   `reference/priorities/master-equation-closure/characteristic-tail.md`,
   `reference/priorities/master-equation-closure/brainstorming.md`,
   `reference/priorities/master-equation-closure/work-log.md`, and the directly
   dependent import-audit summary.

Plainly: this batch repairs the proof record and makes the remaining state-space
choice explicit. It does not choose a maturity law or account measure.

Promotion classification: `promote now` inside the priority research record;
`defer with blocker` for reader-facing wake-state prose until a representation
is selected.

### Proposed change batch B — reader-facing kinetic-account corrections

1. In `content/markdown/aaa/dynamics/energy.md`, retain the existing
   $P'=K'/s$ equation, add the needed near-zero integrability assumptions, and
   describe it as work-power compatibility.
2. Add the reconstruction $\ell_K=sP-K$ as the kinetic Lagrangian whose
   velocity derivative is the declared momentum account.
3. Add an ontology-neutral radial-collinearity lemma: arbitrary oblique radial
   increments can remain radial in momentum space only for $P(s)=as$; combined
   with work-power compatibility and $K(0)=0$, this yields
   $K(s)=as^2/2$.
4. In `content/markdown/aaa/dynamics/effective-lagrangian.md`, state that the
   displayed action uses the quadratic bookkeeping kinetic term. A general
   kinetic chart requires $\ell_K$, followed by a complete revariation.
5. In `content/markdown/aaa/dynamics/master-equation.md`, retain Energy as the
   authority and add only the corresponding scope sentence where generalized
   momentum is discussed.

Plainly: this batch corrects mathematical generation and scope. The coefficient
$a$ remains a universal bookkeeping coefficient and is not primitive physical
mass.

Promotion classification: `promote now` as safe mathematical clarification,
provided the live concurrent Energy and action edits are reconciled before any
implementation batch.

### Priority impact

- MEC-002 should remain `In progress`. Its blocker becomes more precise:
  select and derive a reception-account measure class and a complete
  constitutive transfer map.
- MEC-003 should remain `Deferred / blocked`. Weighted integrability is now
  stated correctly, but no maturity law, transition scale, or uniqueness
  estimate is selected.
- MEC-004 should remain `Deferred / blocked`. The account representation and
  conditional momentum/angular constraints are sharper, but emission capacity,
  transfer coefficients, boundedness, and independent conservation remain
  absent.

Plainly: the work moved from a broad conservation gap to two explicit forks,
but no completion criterion has been met.

### Decisive next research calculation

After the operator selects an admissible reception-account measure class,
execute one oblique point-reception control with $c_f=1$, a finite
$\Delta\mathbf V\parallel\boldsymbol\omega$, and a predeclared wake debit. The
record must report:

1. motion- and wake-momentum increments;
2. instantaneous angular balance at the reception point;
3. angular-account change during subsequent free propagation;
4. scalar-account lower-bound behavior;
5. later-receiver visibility; and
6. the state-sensitivity integral through any coincident-birth continuation.

Pass requires all balances to follow from the predeclared map, finite total
variation or finite patch norm, bounded scalar account, no future receiver
input, and recovery of the canonical regular acceleration. Failure of any item
closes that representation-and-map candidate negatively.

Plainly: this single oblique test forces momentum, angular momentum, bounded
energy, visibility, and local uniqueness to confront the same update rather
than being repaired in separate ledgers.

## Non-promotions and remaining blocker

- Do not promote the internal persona response as external expert authority.
- Do not promote weighted integrability into a selected maturity function.
- Do not promote cubic order into a nonzero cubic coefficient, a transition
  scale, or a complete law.
- Do not promote a Radon atom, finite patch, or nonlocal kernel into wake
  ontology merely because it fixes a representation problem.
- Do not call post hoc residual cancellation conservation.
- Do not infer that line-of-action acceleration automatically closes angular
  momentum.
- Do not interpret the coefficient $a$ in the conditional quadratic family as
  primitive architrino mass.
- Do not change the Master Equation, EOM solver, retained-branch status, or
  downstream recovery claims from this note.

The smallest unresolved theory decision is whether MEC-002 may represent exact
reception transfers by finite Radon measures with atoms. If that representation
is rejected, exact point-local transfer is closed negatively for ordinary
$L^1(dT_e\,d\boldsymbol\omega)$ accounts; the remaining mathematical routes are
a derived finite patch or an explicitly nonlocal redistribution/action
construction.

Plainly: choosing the measure class does not solve conservation, but the theory
cannot even state a finite exact reception transfer until that representation
question is answered.

Final movement: **the blocker is narrowed and a testable route is specified**.
The owning priority is not resolved.

## Stage 3 — External-service response and assessment

### Source record

The operator supplied an external-service response labeled `Tao`. The complete
response is preserved in Appendix A of this packet.
The source artifact is
`/Users/markmorris/.codex/attachments/4cb076a4-9701-47e8-88ac-783f4391f957/pasted-text.txt`.

The service label identifies a review lens. It does not establish that Terence
Tao personally participated in, authored, endorsed, or approved the response.

Plainly: this is genuinely external to the internal subagent analysis, but it
is still a source to audit rather than an authority to accept.

### Assessment of the external response

| External result | Verdict | Evidence and corrected claim grade | Exact destination | Disposition |
| --- | --- | --- | --- | --- |
| Weighted-integrability correction and logarithmic counterexample | Safe correction; independent duplicate | The calculation matches Follow-on A. The weighted integral is the exact finite-impulse condition for nonnegative $M$ on the prescribed control. Monotonicity is not needed merely to state that equivalence. Because the live exact control proves $z=(\alpha/c_f)T$, the analytic-in-$z$ cubic-order corollary is valid there. Grade: derived on the prescribed quadratic control. | Owning priority §3 and the dependent priority summaries already listed in change batch A | Accept; do not add a second copy of the same proof. Use `weighted-integrability condition`, not new project terminology. |
| Radial momentum reduction | Mathematical advance under an added premise | If the reception rule requires every wake momentum deposit and withdrawal to be parallel to $\boldsymbol\omega$, then $\boldsymbol\pi=\pi\boldsymbol\omega$ and $(\mathbf Y-\mathbf C)\times\boldsymbol\pi=\mathbf0$. The current packet's line-of-action acceleration alone does not force that premise: the unknown reception map reads retained history and can construct other vector directions. Grade: derived conditional lemma, not an unconditional state reduction. | Owning priority §1, adjacent to the angular-account paragraph | Add only as a conditional representation fork. Do not replace the vector account unless the radial-account premise is selected. |
| Photon Gate B dichotomy | Requires operator discussion; premature destination | The emission-site identity constrains Gate B only if photon angular information is proposed to reside in this still-unaccepted wake momentum account. Current Gate B remains a downstream assembly and event-ledger recovery target. Grade: inferred downstream implication. | No current photon edit | Defer. Do not create a cross-lane dependency from an unaccepted MEC-002 representation. |
| Ordinary-density singleton obstruction | Mathematical advance; independent duplicate | A singleton update changes no $L^1(dT_e\,d\boldsymbol\omega)$ total. This matches Follow-on B. Grade: derived conditional theorem. | Owning priority measure-class subsection proposed in batch A | Accept once, with atom, finite-patch, and nonlocal alternatives left unselected. |
| Universal $\eta^{-2}$ blow-up | Useful scaling idea, insufficiently specified | For a declared receiver patch of transverse size $\eta$ at fixed radius $r$, its solid angle scales as $(\eta/r)^2$, so a fixed transferred total requires density of order $(r/\eta)^2$. The source packet declares no receiver-size regulator or patch kernel, and its existing wake-width regulator is not automatically the same object. Grade: conditional scale estimate only. | Future measure-class or finite-patch candidate, not the current owner as theorem | Defer until the receiver geometry, normalization, base measure, and regulator family are declared. |
| “Atoms are dynamically invisible almost everywhere,” reducing the trilemma to a dilemma | Unsupported inference | Visibility depends on the later acceleration observation map. An atom may be invisible to ordinary-density integration, visible to a convolution or trace operator, or sampled on a deterministic recurrent branch. The packet declares no probability or branch measure that licenses “almost everywhere.” Grade: unsupported as a general theorem. | None | Reject the trilemma-to-dilemma promotion. Retain the conditional visibility theorem from Stage 2. |
| Finite impulse gives a Carathéodory existence candidate | Unsupported as stated | An $L^1$ time envelope alone does not prove existence. A Carathéodory argument also needs time measurability, state continuity, local growth control, a defined state space, and a closed causal update. Grade: useful proof direction with missing hypotheses. | Owning priority §8 or a focused MEC-003 proof packet | Rewrite as a proof obligation, not a derived existence result. |
| Uniqueness, continuous dependence, and root-ledger control remain separate | Useful clarification | The separation of burdens is correct. However, the time factor $T^{\delta-1}$ is integrable for every $\delta>0$ and does not by itself defeat uniqueness. Uniqueness depends on the state modulus; a sufficient route is integrability of both acceleration and its state-Lipschitz modulus. At birth $D_t=0$, so the proof needs a one-sided normal form and branch-selection rule, followed by a positive floor only after re-entry to a regular chart. Grade: inferred theorem architecture with corrected hypotheses. | Owning priority §8; MEC-003 acceptance design | Accept the burden separation with these corrections. |
| Fail-closed disposition | Safe conclusion | No response item supplies a complete maturity, emission, reception, boundary, or conserved-account update. | MEC-002/MEC-003/MEC-004 | Retain every current queue status. |

Plainly: the external service independently confirms the main asymptotic
correction and the ordinary-density obstruction. Its radial angular identity is
useful only conditionally. Its proposed dilemma and automatic existence claim
overreach the supplied mathematics.

### Delta to the proposed change batches

Add two items to proposed change batch A:

1. State the conditional radial-account lemma:
   if $\boldsymbol\pi=\pi\boldsymbol\omega$ is imposed by the reception rule,
   then the wake sector has zero orbital angular account about its frozen
   emission center. Keep $\boldsymbol\pi$ vector-valued until that premise is
   selected.
2. State the well-posedness burden as a one-sided birth normal form, a unique
   branch-selection rule, integrable acceleration and state sensitivity,
   continuous dependence on retained history, and regular-chart re-entry.

Do not add:

- a photon Gate B cross-reference;
- a universal $\eta^{-2}$ theorem without a declared receiver patch;
- an “atoms are inert” claim;
- a trilemma-to-dilemma promotion; or
- existence from finite impulse alone.

Plainly: the external response improves the research note but does not alter
the measure-class decision or justify a downstream theory promotion.

### Updated priority impact

MEC-002 remains `In progress`; MEC-003 and MEC-004 remain
`Deferred / blocked`. The external response narrows the same two forks already
identified in Stage 2:

1. select a mathematically defined reception-account representation; and
2. derive one complete update whose birth continuation and account balances
   are well posed on that representation.

Plainly: agreement across the internal and external-service responses
strengthens the case for the corrections, but it is not independent physical
evidence and does not fill the missing constitutive rule.

## Stage 4 — Truncated external-service follow-up

### Source and completeness

The partial follow-up is preserved exactly in Appendix B of this packet.
Its source artifact contains 6,403 bytes and 64 lines and ends during the first
proof, at `G_{\boldsymbol\om`.

Plainly: the response began the requested detailed round but the export stopped
before it completed Q1. Its summaries can be recorded, but missing derivations
cannot be treated as delivered.

### Material safely incorporated now

The partial response explicitly withdraws two earlier overclaims:

1. radial receiver acceleration does not by itself force radial wake momentum;
2. a Dirac atom is not intrinsically dynamically invisible.

It also supplies a useful hypothesis ledger:

- `I1`: proper-rotation equivariance;
- `I2`: a rich open set of admissible finite increments;
- `I3`: a declared reference measure for Lebesgue decomposition;
- `I4`: normalized observation kernels; and
- `I5`: a normed retained-history state space.

Plainly: these assumptions identify what the earlier response had used
implicitly. They are candidate mathematical hypotheses, not new substrate
postulates.

### Completed Q1 audit

The partial source states the correct one-vector equivariance theorem. Let
$F(\boldsymbol\omega;\mathbf s)$ be a vector output, where $\mathbf s$ denotes
rotation-invariant scalar data. If

$$
F(R\boldsymbol\omega;\mathbf s)
=
R F(\boldsymbol\omega;\mathbf s)
$$

for every $R\in SO(3)$, and $\boldsymbol\omega$ is the only vector input, then

$$
F(\boldsymbol\omega;\mathbf s)
=
a(\mathbf s)\boldsymbol\omega .
$$

Plainly: rotations that leave $\boldsymbol\omega$ fixed rotate every transverse
direction. A vector unchanged by all those rotations can only be parallel to
$\boldsymbol\omega$.

This theorem does not apply when retained history supplies another vector. For
example, let $\mathbf v$ be any covariant history vector and define

$$
F(\boldsymbol\omega,\mathbf v)
=
\mathbf v
-
(\mathbf v\cdot\boldsymbol\omega)\boldsymbol\omega .
$$

Plainly: this is the part of $\mathbf v$ tangent to the wake surface. It is
generically nonzero and nonradial.

For every $R\in SO(3)$,

$$
F(R\boldsymbol\omega,R\mathbf v)
=
R F(\boldsymbol\omega,\mathbf v).
$$

Plainly: the tangential construction respects rotations, so rotational
equivariance alone does not force a radial account when a second vector is
available.

Claim grade: **derived mathematical theorem and counterexample**. Scope:
representation only. Falsifier: an equivariant one-vector map with a nonzero
transverse component, or failure of the displayed two-vector map to transform
covariantly.

Exact destination: proposed priority change batch A, under the owning packet's
state-representation discussion. Disposition: `priority-only` until the
reception map's allowed inputs are selected.

### Claims retained only as incomplete source assertions

| Partial-response assertion | Present evidence | Current disposition |
| --- | --- | --- |
| Same-point booking constrains nothing; emission-center booking plus event-wise angular closure forces $\mathbf p(\mathbf V)=a\mathbf V$ | Summary only; detailed finite-increment proof is absent | Consistent with the conditional Stage 2 theorem, but not newly verified from this source |
| Trace and kernel observations make atoms visible | Summary only; observation operators and limits are absent | Retain as a candidate theorem target |
| Patch density is $\Delta Qr^2/(\pi\varepsilon^2)$ and converges weak-* to a shape-independent atom | Summary only; patch geometry, normalization, and weak-* proof are absent | Do not promote until the complete derivation is supplied |
| An integrable state modulus gives uniqueness and finite impulse permits a nonunique counterexample without one | Summary promises an explicit counterexample “below,” but the source truncates before it | Retain the corrected well-posedness burden; do not claim the missing theorem or counterexample |
| No photon Gate B cross-reference is justified | Explicit conclusion, consistent with Stage 3 assessment | Accept as a safe non-promotion |

Plainly: these statements may be correct, but the partial export gives only
their conclusions. The research note records them without upgrading their
claim grades.

### Updated disposition

The truncated follow-up strengthens the Q1 representation result and confirms
the Stage 3 retractions. It does not change the proposed corpus batches, the
measure-class decision, or any work-queue status.

The complete follow-up is still needed for:

1. the finite-increment Q2 rigidity proof;
2. explicit Q3 observation operators and the trilemma verdict;
3. the Q4 patch and weak-* calculation;
4. the Q5 existence/uniqueness theorem and promised counterexample;
5. the bounded Q6 Gate B argument; and
6. required response sections 4–8.

Plainly: incorporation is complete for the available source, while the external
consultation itself remains incomplete.

## Stage 5 — Expanded truncated external-service follow-up

### Superseding source record

The second export supersedes the shorter partial source record. It contains
8,541 bytes and 120 lines. It restarts at the beginning, completes Q1, and
continues through three booking calculations in Q2. It then ends mid-sentence
in the `Plainly:` explanation following the free-propagation angular-rate
identity.

Plainly: this export supplies more of the requested mathematics, but its claim
that it is the complete response is contradicted by the artifact itself.

### Q1: externally completed and independently matched

The external response now contains the full stabilizer proof for the
one-vector equivariance theorem and the more general two-term counterexample

$$
\Delta\boldsymbol\pi
=
\alpha\boldsymbol\omega_{\mathsf h}
+
\beta\left(
\mathbf V_r^-
-
(\mathbf V_r^-\cdot\boldsymbol\omega_{\mathsf h})
\boldsymbol\omega_{\mathsf h}
\right).
$$

Plainly: the first term is radial, while the second is the transverse part of
an available retained-history velocity. The second term shows exactly why
rotational equivariance does not force radial transfer once the reception map
may inspect another vector.

This matches the independently completed Stage 4 theorem and counterexample.
The external result is therefore a **corroborating duplicate**, not independent
physical evidence. Its claim grade remains **derived mathematical result,
conditional on sector blindness for the radial conclusion**.

Falsifier: exhibit a sector-blind, proper-rotation-equivariant one-vector map
with a nonzero transverse output, or show that the displayed two-vector
counterexample fails rotational covariance.

Exact destination: proposed priority change batch A, in the state-
representation discussion of the owning packet. Recommended disposition:
retain as a priority-only theorem/counterexample pair until the allowed inputs
to the reception map are selected.

### Q2: three exact booking identities now visible

For same-point booking, the response computes

$$
\int \mathbf Y\times d\boldsymbol{\mathsf\Pi}
=
-\mathbf X_r\times\Delta\mathbf p_r.
$$

Plainly: receiver credit and wake debit use the same lever arm, so they cancel
for every transfer direction. This booking convention cannot force radiality.

For emission-center booking, the residual is

$$
\mathbf X_r\times\Delta\mathbf p_r
-
\mathbf C\times\Delta\mathbf p_r
=
R\boldsymbol\omega_{\mathsf h}\times\Delta\mathbf p_r.
$$

Plainly: changing the debit point from the reception point to the emission
center leaves the radius crossed with the transfer. Event-wise closure then
requires a radial transfer when $R>0$.

For later free propagation with constant stored momentum,

$$
\frac{d}{dT}
\left(
\mathbf Y\times\boldsymbol\pi
\right)
=
c_f\boldsymbol\omega\times\boldsymbol\pi.
$$

Plainly: a nonradial stored momentum makes the angular account change as the
wake element moves outward; radial stored momentum makes this particular rate
zero.

These are **derived account identities under the stated booking and
propagation assumptions**. They were already derived in the internal
follow-on analysis, so the new source corroborates rather than extends the
research note. They remain accounting mathematics, not a derived physical
conservation law.

Falsifier: direct substitution into the declared debit, lever arm, and
free-propagation definitions produces a different residual or derivative.

Exact destination: proposed priority change batch A, beside the explicit
booking-choice obligation. Recommended disposition: safe priority-level
clarification; do not promote to wake ontology, the Master Equation, or a
conservation claim.

### Still not delivered

The response summary announces that event-wise emission-center closure over a
rich finite-increment set forces
$\mathbf p(\mathbf V)=a\mathbf V$, but the export stops before that proof.
Accordingly, the theorem is still a **provisional source assertion**, not a
newly verified result from this response.

The expanded export also still omits:

1. the remainder of Q2, including the finite-increment rigidity proof;
2. Q3 observation operators and the atom-visibility classification;
3. Q4 finite-patch normalization and weak-* limit;
4. Q5 existence, uniqueness, continuous dependence, and the promised
   nonuniqueness counterexample;
5. Q6's bounded Gate B cross-reference analysis; and
6. required response sections 4–8.

Plainly: only Q1 and the displayed part of Q2 can be assessed from this
artifact. The summary cannot substitute for the missing derivations.

### Priority and corpus effect

No work-queue row, proposed corpus incorporation batch, or claim grade changes
because of this expanded partial response. It increases confidence in the
already recorded representation theorem and booking identities but does not
resolve the constitutive closure blocker.

The next operator-checkable action is exact: obtain the response beginning
immediately after the truncated phrase in Q2 and continuing through required
section 8. On receipt, assess the finite-increment theorem first, because it is
the only announced new result that could sharpen the owning priority's
constitutive choice.

Plainly: the research note has absorbed everything this artifact actually
contains. The remaining useful target is the missing proof, not another broad
review.

## Stage 6 — Finite-increment proof and partial observation-operator analysis

### Source and completeness

The third export is preserved in Appendix C. It contains 12,631 bytes and 150
lines. It restates Q1, supplies the complete advertised finite-increment proof
for Q2, and begins Q3. It ends at `Two costs: (i`, before completing the
AC-factoring case.

Plainly: this artifact materially advances Q2, but it is still not the complete
response promised by its opening sentence.

### Q2 finite-increment rigidity: accepted with an explicit richness repair

Let $D\subseteq\mathbb R^3$ be a connected open velocity domain and let
$\mathbf p:D\to\mathbb R^3$. Assume that for every pair
$\mathbf u,\mathbf w\in D$,

$$
\mathbf p(\mathbf w)-\mathbf p(\mathbf u)
\parallel
\mathbf w-\mathbf u.
$$

Plainly: every finite change in the momentum account is required to point along
the corresponding finite change in velocity, for every pair in the domain.

For a non-collinear triple $\mathbf u,\mathbf v,\mathbf w$, write

$$
\begin{aligned}
\mathbf p(\mathbf w)-\mathbf p(\mathbf u)
&=
\lambda_{uw}(\mathbf w-\mathbf u),\\
\mathbf p(\mathbf w)-\mathbf p(\mathbf v)
&=
\lambda_{vw}(\mathbf w-\mathbf v),\\
\mathbf p(\mathbf v)-\mathbf p(\mathbf u)
&=
\lambda_{uv}(\mathbf v-\mathbf u).
\end{aligned}
$$

Plainly: each edge of the velocity triangle has an account increment parallel
to that edge, with a scalar multiplier that could initially depend on the
edge.

Adding the last two increments and comparing the two independent triangle
directions gives

$$
\lambda_{uw}
=
\lambda_{vw}
=
\lambda_{uv}.
$$

Plainly: a closed non-collinear triangle cannot support three different
parallel scaling factors while preserving additivity of the account map.

Every collinear pair can be joined to an off-axis point inside the open domain,
locally and then through overlapping open neighborhoods. The common factor is
therefore constant on the connected domain:

$$
\mathbf p(\mathbf V)
=
a\mathbf V+\mathbf b.
$$

Plainly: the only map satisfying the all-pairs collinearity rule is one common
scalar rescaling of velocity plus a fixed offset.

If $\mathbf0\in D$ and $\mathbf p(\mathbf0)=\mathbf0$, then
$\mathbf b=\mathbf0$. Proper-rotation equivariance on a rotation-invariant
domain also removes a fixed nonzero vector offset. Thus

$$
\mathbf p(\mathbf V)=a\mathbf V.
$$

Plainly: after the account is normalized at rest, its vector value is a single
constant times velocity. The theorem does not determine the sign or magnitude
of that constant and does not make it primitive architrino mass.

Verdict: **mathematical advance and corroborating proof**, conditional on
emission-center booking, event-wise angular closure, and the strong richness
condition above. No continuity assumption is needed under the all-pairs
hypothesis. The source's looser phrase that admissible pairs “fill an open
connected domain in both slots” should be replaced by the explicit all-pairs
condition, or by a separately proved triangle-connected increment graph.

Falsifier: a non-affine map on a connected open domain satisfying the displayed
collinearity condition for every pair, or failure of additivity on one
non-collinear triangle.

Exact destinations:

- proposed Batch A, beside the booking-choice obligation in
  `reference/priorities/master-equation-closure/analysis-independent-causal-wake-state.md`;
- proposed Batch B's ontology-neutral radial-collinearity lemma in
  `content/markdown/aaa/dynamics/energy.md`.

Recommended disposition: promote the theorem only with its booking and
richness hypotheses. Do not infer it from the Master Equation's radial
acceleration direction or from one realized trajectory's increment set.

### Q3 partial observation-operator analysis

The Lebesgue decomposition

$$
\mu
=
\mu_{\mathrm{ac}}
+
\mu_{\mathrm{sc}}
+
\mu_{\mathrm{at}}
$$

is a valid representation tool once a finite Radon account and reference
measure are declared.

Plainly: the account can then be separated into ordinary density, diffuse
singular, and atomic parts, but that separation alone says nothing about which
parts later dynamics can read.

Three proposed observation classes can be assessed only conditionally:

1. The exact-point functional
   $\mathcal O_h[\mu]=\mu(\{h\})$ sees atoms and discards non-atomic parts at
   that point by definition.
2. A nonnegative approximate-identity kernel sees an atom and can diverge as
   its width shrinks if its center value scales as
   $K_\rho(0)\asymp\rho^{-k}$.
3. A functional that explicitly factors through
   $\mu_{\mathrm{ac}}$ discards atoms by construction.

Plainly: atoms are neither intrinsically visible nor intrinsically inert. The
answer is selected by the observation rule coupled to the wake account.

The second result does **not** follow from kernel normalization alone.
Nonnegativity, concentration, local profile, and a lower bound at the sampled
point must be stated. Likewise, the source's claim that later self-hit geometry
automatically reproduces an earlier atomic reception coordinate is not proved
by the owning packet. A self-hit root references a past emission, but a later
root need not revisit the same emission-direction coordinate without an
additional recurrence argument.

Verdict:

- Lebesgue decomposition: **useful mathematical clarification**.
- Operator-dependent visibility: **mathematical advance, conditional on each
  declared operator**.
- Universal kernel divergence from normalization: **unsupported as stated;
  repairable with approximate-identity hypotheses**.
- Automatic root–atom recurrence: **unsupported inference**.
- AC-factoring costs and final trilemma verdict: **not assessable because the
  source truncates mid-sentence**.

Exact destination: proposed Batch A, in a new observation-operator paragraph
next to the reception-account measure-class choice. Recommended disposition:
priority-only until the reception representation and observation map are
selected together.

### Updated priority effect

The Q2 theorem sharpens the consequences of one booking convention but does
not select that convention. The partial Q3 analysis shows that choosing finite
Radon measures alone is insufficient: the owner must also declare how later
acceleration samples the account.

MEC-002 therefore remains `In progress`; MEC-003 and MEC-004 remain
`Deferred / blocked`. No work-queue row should change.

The still-missing source material is:

1. the remainder of Q3 after `Two costs: (i`;
2. all of Q4–Q6; and
3. required response sections 4–8.

Plainly: the new artifact converts the announced rigidity result into a
checkable conditional theorem and exposes observation-operator choice as a
second representation fork. It does not close the constitutive update.

## Stage 7 — Completed response assessment

### Source completion

Appendix D preserves the final continuation supplied by the operator. It
resumes Q3 by restating the interrupted Class 3 sentence and runs through Q6
and its final source section. Combined with Appendix C, the mathematical
response is substantively complete.

Plainly: the source-collection problem is closed. The remaining work is
mathematical adjudication and any separately authorized incorporation.

The source does not literally follow the requested response structure: it has
no separately numbered `Rejected paths` section, and it labels `Missing
context` as section 8 rather than section 9. Rejected arguments are nevertheless
recoverable from Q3, Q5, Q6, and the assessment above.

Plainly: no further source is needed to assess the mathematics, but the service
did not satisfy the requested numbering and section separation exactly.

### Q3: atom visibility survives; the claimed dilemma does not

For a bounded linear functional $\mathcal O$ on finite Radon measures,
linearity gives

$$
\mathcal O[\mu+m\delta_p]-\mathcal O[\mu]
=
m\,\mathcal O[\delta_p].
$$

Plainly: an atomic update is visible exactly when the declared reader assigns a
nonzero value to that point mass.

If $\mathcal O[\delta_p]=0$ for every $p$ in a declared realizable set, total-
variation continuity makes $\mathcal O$ vanish on the closed atomic subspace
generated by those Dirac measures. This means that $\mathcal O$ factors
through the quotient by that atomic subspace. It does **not** imply that
$\mathcal O$ factors through the absolutely continuous part alone:
singular-continuous content may remain visible.

Plainly: a reader can ignore all point masses while still reading other
singular structure. The source's AC-factoring class is one stronger choice,
not the only atom-blind choice.

Verdicts:

- visibility depends on the observation operator: **derived mathematical
  clarification**;
- atomic blindness as an explicit operator property: **safe correction**;
- equivalence of atom blindness with AC-only factoring: **false as stated;
  narrow to quotienting by the relevant atomic subspace**;
- structural recurrence of every reception atom under self-hit:
  **unsupported inference**;
- reduction to a fixed-patch-versus-AC-reader dilemma: **rejected as
  non-exhaustive**.

Other admissible mathematical possibilities remain open, including a bounded
atom-aware operator, a singular-continuous reader, nonlocal redistribution, a
derived finite patch, or a nonlinear saturating observation map. Listing
these does not select them as ontology.

Exact destination: proposed Batch A, in the owning packet's measure-class and
observation-map discussion. Claim grade: priority-only conditional
mathematics. Falsifier: an exhaustive theorem from the allowed primitives
proving that every bounded causal observation/update pair falls into exactly
the two proposed horns.

### Q4: finite-patch weak-* limit

If $\varepsilon$ is defined as the Euclidean transverse radius of a spherical
cap on a shell of radius $r$, then

$$
\theta_\varepsilon
=
\arcsin(\varepsilon/r),
\qquad
|\Omega_\varepsilon|
=
2\pi(1-\cos\theta_\varepsilon)
=
\pi\frac{\varepsilon^2}{r^2}
\left(
1+\frac14\frac{\varepsilon^2}{r^2}
+O(\varepsilon^4/r^4)
\right).
$$

Plainly: under this cap convention, the leading solid angle is patch area
divided by radius squared. A planar disc viewed from the center would use
$\arctan(\varepsilon/r)$ and would have a different higher-order correction,
so the geometry must be declared.

For a uniform finite account $\Delta Q$ on any shrinking patch family with
diameter tending to zero and controlled shape,

$$
\Delta\mu_\varepsilon
\overset{*}{\rightharpoonup}
\Delta Q\,\delta_{\boldsymbol\omega_{\mathsf h}}.
$$

Plainly: the density becomes tall while its support shrinks, but the total
account stays finite and converges to a point mass.

The leading density is

$$
\frac{\Delta Q\,r^2}{\pi\varepsilon^2}
$$

Plainly: this coefficient applies to the stated circular spherical-cap
convention; different shapes preserve the inverse-area scaling but change the
coefficient.

This is therefore a **derived conditional scaling**, and the weak-* atom is a
**derived measure limit**. The wake-thickness regulator $\eta$ is absent from
this angular marginal by construction. Regulator-path independence on the
full emission-label-by-direction space still requires a declared joint law;
it cannot be inferred from the marginal calculation.

Exact destination: proposed Batch A, beside the account measure-class choice.
Recommended disposition: promote at priority grade after fixing the geometric
definition; do not call a fixed nonzero $\varepsilon$ a physical constant
without a constitutive derivation.

### Q5: counterexample accepted; proposed theorem remains architecture

The scalar equation

$$
\dot S
=
T^{-1/2}\operatorname{sgn}(S)\sqrt{|S|},
\qquad
S(0)=0
$$

Plainly: the time multiplier is integrable near zero, while the square-root
state response fails the uniqueness modulus.

It has the zero solution and, for every $a\ge0$,

$$
S_a(T)
=
\left(
\max(0,\sqrt T-\sqrt a)
\right)^2.
$$

Plainly: each displayed $S_a$ can wait at zero for an arbitrary time and then
leave, even though the time envelope is integrable.

Direct differentiation verifies the family for $T>a$. The state modulus
$\omega(s)=\sqrt s$ fails the Osgood divergence condition because

$$
\int_{0^+}\frac{ds}{\sqrt s}
<
\infty.
$$

Plainly: finite accumulated size does not prevent branching when nearby states
are separated by a non-unique state response.

Verdict: **derived counterexample**. It independently establishes that an
$L^1$ time envelope alone does not imply uniqueness.

The proposed Q5 theorem is not yet a valid theorem for the wake-state system.
Its useful core is the Osgood/Bihari uniqueness route on a continuous
evolution segment. The full claim additionally needs:

1. one actual Banach history phase space—the proposed present state augmented
   by a $C^0$ history segment is not finite-dimensional;
2. existence hypotheses appropriate to that space;
3. single-valued event maps with their own continuity or Osgood/Lipschitz
   controls;
4. continuous and transverse event-time/root maps;
5. a nonaccumulation or controlled-Zeno theorem stronger than summable impulse
   magnitudes; and
6. a BV or càdlàg solution class if reception produces genuine jumps, rather
   than the asserted $W^{1,1}$ class.

Plainly: controlling the differential equation between receptions does not
control how solutions cross receptions. Both pieces must be part of one
well-posedness proof.

This agrees with the nearby conditional finite-$\eta$ well-posedness scaffold
in `content/markdown/aaa/dynamics/master-equation.md`, which uses an
infinite-dimensional history space and explicitly leaves verification of the
state-dependent-delay framework open.

Exact destination: proposed Batch A and the future MEC-003 proof design.
Recommended disposition: incorporate the counterexample and corrected
obligation list; do not promote Theorem Q5 as proved.

### Q6 and sections 4–8

The Q6 guard is safe: center-relative radial sector accounts do not constrain
assembly-level angular structure or photon Gate B. No photon cross-reference
should be added.

The strongest accepted result remains the conditional affine-rigidity theorem
from Stage 6, with the explicit all-pairs or triangle-connected richness
hypothesis. The source's correction list is otherwise accepted only to the
extent recorded above.

The proposed four-way observation-by-booking numerical calculation is
**diagnostic only** and not decisive. The four cases differ because their
operators and booking rules are defined differently; replaying those
definitions on a prescribed history would demonstrate model behavior, not
select a constitutive axiom or provide independent physical evidence. Its
assumed guaranteed root–atom recurrence is also unproved.

The highest-value next calculation remains construction-level: declare one
complete MEC-002 reception representation, observation map, booking
convention, event map, and boundary update; then perform the oblique reception
balance and birth state-sensitivity calculation on that same predeclared
system. Pass and fail conditions remain those stated in the proposed
incorporation plan and Stage 2.

Plainly: the consultation has finished and sharpened the proof burden. It has
not selected the missing constitutive update.

### Final priority effect

- MEC-002 remains `In progress`.
- MEC-003 remains `Deferred / blocked`.
- MEC-004 remains `Deferred / blocked`.

No work-queue row should change. The completed response supplies safe
priority-level corrections and a better proof architecture, but no queue
completion condition.

## Appendix A — Complete first external-service response

- Source artifact:
  `/Users/markmorris/.codex/attachments/4cb076a4-9701-47e8-88ac-783f4391f957/pasted-text.txt`
- Source size: 11,694 bytes; 90 lines
- Service label supplied by the operator: `Tao`
- Attribution: external-service response using the requested analysis lens;
  this does not establish participation, authorship, endorsement, or approval
  by Terence Tao
- Authority: source response only; the assessed dispositions in Stages 3–5
  control this research packet

```text
**Finding and claim grade.** I concur with the packet's fail-closed disposition and its central obstruction, and I add four analysis results: (1) a **correction** to the §3 necessity claim — the stated power bound $M(T)=O(T^{2+\delta})$ is *sufficient but not necessary*; the sharp condition is the Dini integral itself, and a log-borderline counterexample exists (derived). (2) A **reduction lemma**: under the declared line-of-action exchange, the wake momentum account collapses from a vector to a scalar radial density, and the wake then carries *zero angular momentum about its own emission site* — with a direct consequence for any future photon Gate B burden (derived). (3) A **measure-theoretic sharpening of the §5 trilemma**: for point receivers, horn 2 degenerates — reception updates are either atoms (dynamically invisible almost everywhere, collapsing into horn 3) or force an $\eta^{-2}$ density blow-up in the regulator limit (derived under the stated mollifier model, inferred in generality). (4) A **well-posedness ladder**: even if a maturity law $M$ were supplied, finite impulse alone does not license an accepted transition; existence, uniqueness, and continuous-dependence certificates remain separately open (derived burden statement).

**Assumptions and theory layer.** Substrate level only: architrinos, absolute time $T$, causal wakes with transmitter-side weight $W^{\mathrm{acc}}=c_f/|D_t|$, retained history, no primitive mass. $c_f$ kept symbolic throughout since its dependence matters; any numerical instantiation sets $c_f=1$. I take the packet's §1–§2 state class and update form as given hypotheses, not as accepted canon.

---

## 1. Correction to §3: the power bound is not necessary

The finite-impulse condition is exactly

$$
\int_0^L \frac{M(T)}{T^3}\,dT < \infty .
$$

The packet writes "Hence $M(T)=O(T^{2+\delta})$ for some $\delta>0$." That inference is false as a necessity claim. Take

$$
M(T) = \frac{T^2}{\log^2(1/T)} .
$$

Then $\int_0^L M(T)T^{-3}\,dT = \int_0^L \frac{dT}{T\log^2(1/T)} = \left[\log^{-1}(1/T)\right]_0^L < \infty$, yet $M(T)\ne O(T^{2+\delta})$ for any $\delta>0$, since $T^{-\delta} \le C\log^2(1/T)$ fails as $T\to0^+$.

Plainly: the document claims that finite kick at coincident birth forces the suppression to beat quadratic by a genuine power. It does not — beating quadratic by a squared logarithm already suffices. The true boundary of admissible suppressions is the integral condition itself, which is strictly weaker than any power condition. The corrected statement is:

- **Necessary and sufficient (given monotone regular $M$ near $0$):** $\int_0^L M(T)T^{-3}\,dT<\infty$.
- **Sufficient:** $M(T)=O(T^{2+\delta})$, some $\delta>0$.
- **Unchanged:** the analytic-case conclusion. If $M(z)=\sum_{k\ge0}c_k z^k$ is analytic at $z=0$, integrability forces $c_0=c_1=c_2=0$, so the leading admissible integer power is cubic. This part of §3 is correct.

The correction *widens* the underdetermination: the admissible class is even larger than the packet states, so the "current primitives select none of them" conclusion is strengthened, not weakened.

Claim grade: derived. Falsifier: a proof that the log-refined $M$ above violates some other declared primitive constraint, restoring the power bound as necessary.

## 2. Reduction lemma: line-of-action kills the vector momentum account and the emission-site angular account

Under the packet's own line-of-action assumption, the exchange direction at any reception of surface element $(T_t,\boldsymbol\omega)$ is $\hat{\mathbf r}_t = \boldsymbol\omega$. If wake momentum is only ever deposited or withdrawn along the line of action, then the momentum density is normal to the wake surface:

$$
\boldsymbol\pi_i(T;T_t,\boldsymbol\omega) = \pi_i(T;T_t,\boldsymbol\omega)\,\boldsymbol\omega .
$$

The minimal per-surface-point state is then three scalars $(m_i,e_i,\pi_i)$, not five. Substituting into the packet's angular-momentum formula:

$$
\mathbf Y_i \times \boldsymbol\pi_i
= \big(\mathbf X_i(T_t) + c_f(T-T_t)\boldsymbol\omega\big)\times \pi_i\,\boldsymbol\omega
= \mathbf X_i(T_t)\times \pi_i\,\boldsymbol\omega .
$$

The radial-expansion term vanishes identically. Consequently a line-of-action wake carries **zero angular momentum about its own emission site**, for every maturity law and every account assignment.

Plainly: if wakes only push and pull along the line joining emission point to receiver, then a wake can never store any "twist" about the place it was born. All wake angular momentum about any origin comes purely from the offset of the birthplace, never from circulation on the expanding sphere.

Consequence for the theorem-target photon: if photon spin/polarization (Gate B) is ever to be exported from wake content rather than from assembly orbital structure, the line-of-action assumption *forces* either (a) spin carried entirely by the twelve-worldline orbital configuration, with wakes contributing nothing intrinsic, or (b) enlargement of the exchange law beyond line-of-action, which the packet correctly flags as requiring an enlarged angular account. This converts the packet's conditional remark into a sharp dichotomy that Gate B work must confront explicitly.

Claim grade: derived (the lemma is two lines of vector algebra from the stated assumption). Falsifier: exhibition of a line-of-action reception map whose accumulated wake update produces nonzero angular momentum about the emission site — impossible by the identity above unless "line of action" is redefined.

## 3. Sharpening the §5 trilemma: atoms versus densities

The packet's §4 totals integrate $e_i,\boldsymbol\pi_i$ against $dT_t\,d\boldsymbol\omega$, so the accounts are **densities**. A point receiver intersects the wake of transmitter $t$ at a single $(T_t,\boldsymbol\omega_{\mathsf h})$ per root — a set of measure zero on the surface. Then:

- **Density-valued update:** modifying the density at one point changes no integral and no later reception (later intersections at the exact same $(T_t,\boldsymbol\omega)$ form a measure-zero coincidence set for generic later receivers). Horn 2's back-reaction is a.e. invisible: it collapses into horn 3.
- **Mollified receiver of size $\eta>0$ at distance $r$:** the intercepted solid-angle measure scales like $(\eta/r)^2$ up to geometry. Balancing a finite receiver impulse $\Delta\mathbf V_r$ against an account change supported on measure $\sim\eta^2$ forces the transferred density to scale like $\eta^{-2}$. In the regulator limit $\eta\to0$ this is exactly a Dirac atom on the surface — returning to the invisible-atom case.
- **Atom-valued accounts:** the totals in §4 pick up Dirac contributions; conservation can be made to close *algebraically*, but the atoms influence no later dynamics for a.e. configuration, so the account is dynamically inert — the packet's horn 3, and precisely the post-hoc ledger assignment the packet rejects.

Plainly: because a wake surface is two-dimensional and a receiver touches it at a point, any bookkeeping change made "at the touch point" is either spread over nothing (and so does nothing) or concentrated into a spike that no later receiver will ever hit. So the middle option in the trilemma — reception edits the wake and later dynamics feel it — is not merely in tension with the universal regular law; under point receivers and density accounts it is generically unavailable at all. The trilemma is effectively a **dilemma**, which is a stronger no-go than the packet claims.

Claim grade: derived under the mollifier model stated; inferred for general regulators. Falsifier: a declared regulator family under which the $\eta\to0$ limit of reception-updated accounts converges to a nontrivial absolutely continuous density that later receivers intersect with positive probability under the branch measure.

## 4. Well-posedness ladder beyond finite impulse

The packet correctly requires a maturity law but understates what acceptance would need even after one is supplied. Near coincident birth, $z=|D_t|/c_f$ is determined by a **state-dependent delay root**, so the effective field $M(z)/z^3$ composed with the root map is a functional of the unknown trajectory. Finite impulse gives an $L^1$-in-time envelope, hence Carathéodory-type *existence* of a continuation candidate. It does not give:

1. **Uniqueness.** One needs an Osgood-type modulus for the composed field near the singular time; for $M(z)\sim z^{2+\delta}$ with $\delta<1$ the field blows up like $T^{\delta-1}$ and uniqueness is not automatic.
2. **Continuous dependence** on retained-history data across the birth stratum, uniformly in the branch chart.
3. **Root-ledger stability**: the derived root count and transversality floor must be shown continuous through the transition, or the accepted-step machinery fails closed regardless of the impulse bound.

Plainly: even if someone hands us the missing suppression rule tomorrow, "the kick is finite" only guarantees that *some* continuation exists. It does not guarantee there is exactly one, that it varies stably with the input history, or that the root bookkeeping survives the transition. Those are three separate certificates, and none is addressed by any candidate $M$.

Claim grade: derived burden statement. Falsifier: a proof that the integral condition alone implies an Osgood modulus for the composed root field — I expect this is false for $\delta<1$, and a counterexample construction is a well-scoped task.

---

**Evidence and independence.** Results 1, 2, and 4 are closed-form arguments checkable by hand against the packet's own displayed equations; no instrument is invoked and no same-implementation replay is at issue. Result 3 depends on the stated mollifier scaling; an independent check would be a small quadrature computing intercepted solid-angle measure versus $\eta$ for a moving receiver at $c_f=1$ — a fixture-level diagnostic, not evidence of dynamics.

**Failure conditions.** Listed per item above. Globally: any predeclared, non-circular emission/reception/boundary construction closing §4's three balance equations on nonsymmetric histories falsifies the strengthened dilemma of §3 here and the packet's §5 simultaneously.

**Durable destination.** These belong as amendments inside the owning derivation, not as a new gate: (i) replace the §3 "Hence" with the corrected necessity statement and record the log counterexample; (ii) add the line-of-action reduction lemma and the Gate B dichotomy as a corollary in §1; (iii) upgrade §5 from trilemma to dilemma with the atom/density argument and the $\eta^{-2}$ scaling; (iv) append the three well-posedness certificates to §8's mandatory-conditions list, making six conditions total. The Gate B dichotomy should additionally be cross-referenced from the photon theorem-target burden in the braid-program priorities, since it constrains that lane's admissible constructions.

**Disposition.** I endorse the packet's promotion classification unchanged: closed negatively under the current primitive set, first-ranked derivation target, singular evolution fail closed. Nothing here licenses an accepted transition; every result above either widens the underdetermination or raises the acceptance bar.

Closure goal: amend the wake-state obstruction record with the corrected Dini-sharp suppression condition, the line-of-action angular-momentum reduction lemma and its Gate B dichotomy, the atom/density degeneration of trilemma horn 2, and the three additional well-posedness certificates, so the derivation target carries its full and correct burden list before any constitutive construction is attempted.
```

Plainly: Appendix A preserves the source verbatim. Its claims remain subordinate
to the evidence-by-evidence assessment in Stage 3, which rejects the
unconditional radial reduction, atom-invisibility conclusion, and automatic
existence inference.

## Appendix B — Expanded partial external-service follow-up

- Latest source artifact:
  `/Users/markmorris/.codex/attachments/9a2cbd0a-8956-408e-b7ad-45db97fd98d0/pasted-text.txt`
- Prior shorter source artifact:
  `/Users/markmorris/.codex/attachments/faebeac4-89a6-443e-8388-cead108b2050/pasted-text.txt`
- Latest source size: 8,541 bytes; 120 lines
- Completeness: expanded, but still truncated during Q2
- Attribution and authority: same source-only boundary as Appendix A

```text
No — the previous reply was truncated mid-proof (it ends inside the stabilizer argument for Q1). Here is the complete response.

### 1. Finding in plain language

Relative to my earlier findings:

- **Survives:** the weighted-integrability correction, the singleton-$L^1$ obstruction, and the fail-closed disposition (the last now on sharper grounds: two undeclared axioms, not the withdrawn claims).
- **Withdrawn as derived, reinstated as conditional:** "radial acceleration ⇒ radial wake momentum." It holds only under a sector-blindness hypothesis or an explicit booking axiom; a two-term equivariant counterexample exists.
- **Withdrawn:** "Dirac atoms are dynamically invisible." Visibility is a property of the observation operator, which is not supplied. Under trace-type and kernel-type observation atoms are visible (kernel-type divergently so); only an AC-factoring observation makes them inert, by construction.
- **Narrowed:** the finite-patch scaling — coefficient $1/\pi$, no role for $\eta$ in the angular density, weak-* limit a finite Radon atom, shape-independent.
- **Narrowed:** the near-birth concern — a $T^{\delta-1}$ envelope with $\delta>0$ threatens nothing and proves nothing; uniqueness lives entirely in the state modulus. Explicit counterexample: finite impulse with nonunique continuation.
- **New positive result:** a genuine rigidity theorem — emission-center booking plus event-wise angular closure over a rich increment set forces radial transfer and the linear momentum-account map $\mathbf p(\mathbf V)=a\mathbf V$.

All positive results are **derived** as pure mathematics from stated hypotheses. Nothing here establishes physical realization, retained-branch status, or solver authority.

### 2. Prompt-bounded assumptions

Supplied by this prompt (quoting the packet):

1. $\Delta\mathbf V_r\parallel\boldsymbol\omega_{\mathsf h}$ (receiver acceleration along the surface normal).
2. Reception map $(\Delta\mathbf V_r,\Delta m,\Delta e,\Delta\boldsymbol\pi)=\mathcal R(\mathcal H^T,\mathcal W)$.
3. Reception geometry $\mathbf X_r=\mathbf C+R\,\boldsymbol\omega_{\mathsf h}$, $\mathbf C$ the frozen emission center.
4. Candidate debit $\Delta\boldsymbol{\mathsf\Pi}=-\Delta\mathbf p_r\,\delta_{(T_e,\boldsymbol\omega_{\mathsf h})}$ and angular integral $\int\mathbf Y\times d\boldsymbol{\mathsf\Pi}$.
5. Free propagation $\mathbf Y(T)=\mathbf C+c_f(T-T_e)\boldsymbol\omega$ with $\boldsymbol\pi$ constant along the ray.
6. Wake account: finite Radon measure $\mu$ on emission label $\times$ direction.
7. Distinct regulators $\varepsilon$ (patch radius), $\eta$ (wake thickness), $r$ (shell radius).
8. Abstract system $\dot S=A(T,S)$; envelope candidate $T^{\delta-1}$, $\delta>0$.
9. Constraints: massless architrinos, acceleration-first law, $c_f$ symbolic except numerics at $c_f=1$, account identity ≠ physical conservation.

**Introduced assumptions (marked):**

- (I1) Equivariance group is $SO(3)$.
- (I2) "Rich increment set": Q2 closure conditions are quantified over an open connected set of $(\mathbf V,\boldsymbol\omega,s)$.
- (I3) A reference measure $\lambda$ on the label space $\Sigma$ for Lebesgue decomposition.
- (I4) Kernel normalization $\int K_\rho\,d\lambda=1$.
- (I5) A history norm ($C^0$ or $W^{1,1}$ on the retained window) for continuous dependence.
- (I6) Q5's existence statement uses a finite-dimensional present-state chart; the Banach version requires Lipschitz modulus.

### 3. Detailed answers

---

#### Q1 — Line-of-action acceleration does not force radial wake momentum

**Five distinct objects.**

1. *Direction of receiver acceleration:* given, along $\boldsymbol\omega_{\mathsf h}$; a fact about one output component of $\mathcal R$.
2. *Direction of the wake momentum transfer* $\Delta\boldsymbol\pi$: a different output component; nothing ties it to (1) without an accounting identity.
3. *Direction of stored wake momentum:* an accumulated ledger over many hits with different $\boldsymbol\omega$'s; even eventwise-radial transfers do not sum to anything radial with respect to a single direction.
4. *Isotropy at emission:* constrains the emitted spherical account, not the reception-time debit.
5. *Anisotropy created by reception:* reception selects one $\boldsymbol\omega_{\mathsf h}$, breaking isotropy, but does not make $\boldsymbol\omega_{\mathsf h}$ the only covariant vector if retained history supplies others.

**Theorem Q1 (weakest sufficient hypotheses).** Assume:

- (H1) *Sector blindness:* $\Delta\boldsymbol\pi$ depends on $(\mathcal H^T,\mathcal W)$ only through $SO(3)$-invariant scalars and the single unit vector $\boldsymbol\omega_{\mathsf h}$.
- (H2) *Equivariance:* $\Delta\boldsymbol\pi(R\boldsymbol\omega;\text{scalars})=R\,\Delta\boldsymbol\pi(\boldsymbol\omega;\text{scalars})$ for all $R\in SO(3)$.

Then

$$
\Delta\boldsymbol\pi=\pi(\text{scalars})\,\boldsymbol\omega_{\mathsf h}.
$$

Plainly: if the map is only allowed to see one direction in space and treats all rotated situations identically, its vector output has nowhere to point except along that direction.

*Proof.* Fix $\boldsymbol\omega$ and let $G_{\boldsymbol\omega}\cong SO(2)$ be its stabilizer in $SO(3)$. For $R\in G_{\boldsymbol\omega}$, (H1)–(H2) give $R\,\Delta\boldsymbol\pi=\Delta\boldsymbol\pi$ (the scalars and $\boldsymbol\omega$ are unchanged). The only vectors in $\mathbb R^3$ fixed by every rotation about the axis $\boldsymbol\omega$ are multiples of $\boldsymbol\omega$. Hence $\Delta\boldsymbol\pi=\pi\,\boldsymbol\omega$, and applying a general $R$ shows $\pi$ depends only on the invariant scalars. $\square$

**Counterexample when (H1) fails.** Let $\mathbf V^-_r$ be the receiver's incoming velocity — an allowed retained-history vector. Define

$$
\Delta\boldsymbol\pi
=\alpha\,\boldsymbol\omega_{\mathsf h}
+\beta\,\bigl(\mathbf V^-_r-(\mathbf V^-_r\!\cdot\!\boldsymbol\omega_{\mathsf h})\,\boldsymbol\omega_{\mathsf h}\bigr),
$$

with $\alpha,\beta$ invariant scalars.

Plainly: the second term is the part of the receiver's incoming velocity perpendicular to the surface normal; rotating the whole scene rotates it consistently, so the map is fully rotationally equivariant, local, and causal — yet the output is not radial whenever $\mathbf V^-_r\not\parallel\boldsymbol\omega_{\mathsf h}$.

This is the smallest such counterexample (one extra allowed vector, one extra term), and it is compatible with $\Delta\mathbf V_r\parallel\boldsymbol\omega_{\mathsf h}$, since $\Delta\mathbf V_r$ and $\Delta\boldsymbol\pi$ are independent output slots of $\mathcal R$.

**Verdict.** The reduction $\boldsymbol\pi=\pi\boldsymbol\omega$ is **conditional** (on (H1), or on a booking axiom via Q2). It is not derived; without those hypotheses it is unsupported.

---

#### Q2 — Where the transfer is booked decides everything

**Same-point booking.** Receiver credit $\mathbf X_r\times\Delta\mathbf p_r$; wake debit via the packet's integral,

$$
\int\mathbf Y\times d\boldsymbol{\mathsf\Pi}
=\mathbf Y(T;T_e,\boldsymbol\omega_{\mathsf h})\times(-\Delta\mathbf p_r)
=-\mathbf X_r\times\Delta\mathbf p_r,
$$

Plainly: because the reception point and the intercepted wake element are the same point, the two lever arms are identical and the angular books cancel exactly.

Residual $=\mathbf X_r\times\Delta\mathbf p_r-\mathbf X_r\times\Delta\mathbf p_r=\mathbf 0$ for **every** direction of $\Delta\mathbf p_r$. Same-point booking closes the angular account identically and **constrains nothing**.

**Emission-center booking.** Debit lever arm $\mathbf C$:

$$
\text{Residual}
=\mathbf X_r\times\Delta\mathbf p_r-\mathbf C\times\Delta\mathbf p_r
=(\mathbf X_r-\mathbf C)\times\Delta\mathbf p_r
=R\,\boldsymbol\omega_{\mathsf h}\times\Delta\mathbf p_r.
$$

Plainly: the mismatch between the two lever arms is exactly the radius vector, so the leftover angular account is the radius crossed with the transfer.

Since $R>0$, event-wise angular closure holds iff $\boldsymbol\omega_{\mathsf h}\times\Delta\mathbf p_r=\mathbf 0$, i.e. iff the transfer is radial. Sign convention: residual = receiver credit minus wake debit; closure means residual $=\mathbf 0$.

**Later free propagation.** With $\mathbf Y(T)=\mathbf C+c_f(T-T_e)\boldsymbol\omega$ and $\boldsymbol\pi$ constant,

$$
\frac{d}{dT}\bigl(\mathbf Y\times\boldsymbol\pi\bigr)
=\dot{\mathbf Y}\times\boldsymbol\pi
=c_f\,\boldsymbol\omega\times\boldsymbol\pi.
$$

Plainly: the wake element moves outward along $\boldsymbol\omega$; if its stored momentum is not along $
```

Plainly: Appendix B preserves every character supplied by the expanded export.
The export still stops in Q2; it does not contain the announced
finite-increment rigidity proof, Q3–Q6, or required response sections 4–8.

## Appendix C — Finite-increment proof and partial Q3 source

- Source artifact:
  `/Users/markmorris/.codex/attachments/1114d93e-8693-4f09-8f46-32674b190f6e/pasted-text.txt`
- Source size: 12,631 bytes; 150 lines
- Completeness: supplies Q2 and part of Q3, but still truncates mid-sentence
- Attribution and authority: same source-only boundary as Appendices A and B

```text
Below is the complete artifact, self-contained. Sections 1–2 are restated compactly (their content already survived two deliveries); the full budget goes to Q2's finite-increment proof, Q3–Q6, and sections 4–8.

### 1. Finding in plain language

- **Survive:** weighted-integrability correction; singleton-$L^1$ obstruction; fail-closed disposition (now resting on two undeclared axioms — the observation operator and the booking convention — rather than the withdrawn claims).
- **Withdrawn as derived, reinstated as conditional:** radial acceleration ⇒ radial wake momentum (needs sector blindness or a booking axiom; equivariant counterexample exists).
- **Withdrawn:** atoms are dynamically invisible (visibility is a property of the observation operator; not supplied).
- **Narrowed:** patch scaling (coefficient $1/\pi$; $\eta$ absent from the angular density; weak-* limit a shape-independent finite atom).
- **Narrowed:** near-birth uniqueness ($T^{\delta-1}$ envelope neither threatens nor establishes anything; uniqueness lives in the state modulus; explicit finite-impulse nonuniqueness counterexample).
- **New:** a genuine conditional rigidity theorem forcing $\mathbf p(\mathbf V)=a\mathbf V$ (Q2).

All positive results are **derived** as pure mathematics from stated hypotheses; nothing establishes physical realization, retained-branch status, or solver authority.

### 2. Prompt-bounded assumptions

**Supplied:** (P1) $\Delta\mathbf V_r\parallel\boldsymbol\omega_{\mathsf h}$; (P2) $\mathcal R$ signature with four outputs; (P3) $\mathbf X_r=\mathbf C+R\boldsymbol\omega_{\mathsf h}$; (P4) debit $\Delta\boldsymbol{\mathsf\Pi}=-\Delta\mathbf p_r\,\delta_{(T_e,\boldsymbol\omega_{\mathsf h})}$ and integral $\int\mathbf Y\times d\boldsymbol{\mathsf\Pi}$; (P5) free propagation $\mathbf Y(T)=\mathbf C+c_f(T-T_e)\boldsymbol\omega$, $\boldsymbol\pi$ constant on the ray; (P6) $\mu$ a finite Radon measure on emission label × direction; (P7) distinct symbols $\varepsilon,\eta,r$; (P8) $\dot S=A(T,S)$, envelope $T^{\delta-1}$, $\delta>0$; (P9) the constraint block (massless architrinos, acceleration-first, $c_f$ symbolic except numerics at $c_f=1$, account identity ≠ conservation).

**Introduced (marked):** (I1) equivariance group $SO(3)$; (I2) rich increment set — closure quantified over an open connected set of $(\mathbf V,\boldsymbol\omega,s)$; (I3) a reference measure $\lambda$ on the label space $\Sigma$; (I4) kernel normalization $\int K_\rho\,d\lambda=1$; (I5) a history norm ($C^0$ or $W^{1,1}$ on the retained window); (I6) finite-dimensional present-state chart for the existence half of Q5; (I7) normalization $\mathbf p(\mathbf 0)=\mathbf 0$ ("no intrinsic account at rest"), used only where flagged.

### 3. Detailed answers

---

#### Q1 — Line-of-action acceleration does not force radial wake momentum

**Five distinct objects.** (1) Direction of receiver acceleration: given along $\boldsymbol\omega_{\mathsf h}$ — one output slot of $\mathcal R$. (2) Direction of the transfer $\Delta\boldsymbol\pi$: a different output slot; unconstrained without an accounting identity. (3) Direction of stored momentum: a ledger summed over hits with different $\boldsymbol\omega$'s; eventwise radiality does not make the sum radial with respect to anything. (4) Isotropy at emission constrains the emitted spherical account only. (5) Reception breaks isotropy by selecting $\boldsymbol\omega_{\mathsf h}$, but retained history may supply additional covariant vectors.

**Theorem Q1.** Assume (H1) *sector blindness*: $\Delta\boldsymbol\pi$ depends on $(\mathcal H^T,\mathcal W)$ only through $SO(3)$-invariant scalars and the single unit vector $\boldsymbol\omega_{\mathsf h}$; (H2) $SO(3)$-equivariance. Then

$$
\Delta\boldsymbol\pi=\pi(\text{scalars})\,\boldsymbol\omega_{\mathsf h}.
$$

Plainly: if the map can only see one direction and treats rotated scenes identically, its vector output has nowhere else to point.

*Proof.* For $R$ in the stabilizer $G_{\boldsymbol\omega}\cong SO(2)$ of $\boldsymbol\omega$, (H1)–(H2) give $R\,\Delta\boldsymbol\pi=\Delta\boldsymbol\pi$; the fixed set of every axial rotation is $\mathbb R\boldsymbol\omega$; a general $R$ then shows $\pi$ depends only on invariants. $\square$

**Counterexample when (H1) fails.** With $\mathbf V_r^-$ the receiver's incoming velocity (an allowed retained-history vector):

$$
\Delta\boldsymbol\pi
=\alpha\,\boldsymbol\omega_{\mathsf h}
+\beta\bigl(\mathbf V_r^--(\mathbf V_r^-\!\cdot\!\boldsymbol\omega_{\mathsf h})\boldsymbol\omega_{\mathsf h}\bigr),
\qquad \alpha,\beta\ \text{invariant scalars}.
$$

Plainly: the second term is the transverse part of the incoming velocity; rotating the whole scene rotates it consistently, so the map is fully equivariant, local, and causal, yet non-radial whenever $\mathbf V_r^-\not\parallel\boldsymbol\omega_{\mathsf h}$ — and it is compatible with (P1) because $\Delta\mathbf V_r$ and $\Delta\boldsymbol\pi$ are independent output slots. This is minimal: one extra allowed vector, one extra term.

**Verdict:** $\boldsymbol\pi=\pi\boldsymbol\omega$ is **conditional** (on (H1), or on Q2's booking axiom); without either, **unsupported**.

---

#### Q2 — Booking location decides everything; the finite-increment rigidity theorem

**Same-point booking.** Receiver angular credit $\mathbf X_r\times\Delta\mathbf p_r$; wake debit through the packet's integral:

$$
\int\mathbf Y\times d\boldsymbol{\mathsf\Pi}
=\mathbf Y(T;T_e,\boldsymbol\omega_{\mathsf h})\times(-\Delta\mathbf p_r)
=-\,\mathbf X_r\times\Delta\mathbf p_r .
$$

Plainly: reception point and intercepted wake element coincide, so both lever arms are the same vector and the angular books cancel identically.

Residual (convention: credit minus debit) $=\mathbf 0$ for **every** direction of $\Delta\mathbf p_r$. Same-point booking closes the angular account trivially and **constrains nothing**.

**Emission-center booking.** Debit lever arm $\mathbf C$:

$$
\text{Residual}
=(\mathbf X_r-\mathbf C)\times\Delta\mathbf p_r
=R\,\boldsymbol\omega_{\mathsf h}\times\Delta\mathbf p_r .
$$

Plainly: the two lever arms differ by exactly the radius vector, so the leftover angular account is the radius crossed with the transfer.

Since $R>0$, event-wise closure holds iff $\boldsymbol\omega_{\mathsf h}\times\Delta\mathbf p_r=\mathbf 0$: **radial transfer**.

**Later free propagation.** From (P5),

$$
\frac{d}{dT}\bigl(\mathbf Y\times\boldsymbol\pi\bigr)
=\dot{\mathbf Y}\times\boldsymbol\pi
=c_f\,\boldsymbol\omega\times\boldsymbol\pi .
$$

Plainly: the element rides outward along $\boldsymbol\omega$; unless its stored momentum is parallel to $\boldsymbol\omega$, its orbital angular account about the emission center keeps changing even after reception is over.

So demanding **stationarity during free propagation** forces $\boldsymbol\pi\parallel\boldsymbol\omega$ per sector — but converting sector radiality into transfer radiality needs two extra axioms: **no cross-sector reshuffling** (the debit hits only the intercepted sector $(T_e,\boldsymbol\omega_{\mathsf h})$) and **no intrinsic angular account** (all wake angular content is orbital, $\mathbf Y\times\boldsymbol\pi$).

**Finite-increment rigidity.** Model the receiver's momentum account by a map $\mathbf p:\mathbb R^3\to\mathbb R^3$, so a reception with velocity jump $\mathbf V\mapsto\mathbf V+s\boldsymbol\omega$ (finite $s$, per (P1)) produces $\Delta\mathbf p_r=\mathbf p(\mathbf V+s\boldsymbol\omega)-\mathbf p(\mathbf V)$. Event-wise closure under emission-center booking reads:

$$
\boldsymbol\omega\times\bigl(\mathbf p(\mathbf V+s\boldsymbol\omega)-\mathbf p(\mathbf V)\bigr)=\mathbf 0
\quad\text{for all }(\mathbf V,\boldsymbol\omega,s)\in U,
$$

Plainly: every finite jump of the momentum account must be parallel to the direction of the velocity jump that caused it.

**Theorem Q2 (rigidity).** If the displayed condition holds on a rich set $U$ (I2) whose $(\mathbf V,\mathbf V+s\boldsymbol\omega)$ pairs fill an open connected $D\subseteq\mathbb R^3$ in both slots, then $\mathbf p(\mathbf V)=a\mathbf V+\mathbf b$ on $D$ with a scalar constant $a$ and constant $\mathbf b$. With (I7) $\mathbf p(\mathbf 0)=\mathbf 0$ (or with $SO(3)$-equivariance of $\mathbf p$, which kills $\mathbf b$ and any non-scalar linear part), $\mathbf p(\mathbf V)=a\mathbf V$. **No continuity assumption is needed.**

*Proof.* The condition says: for all $\mathbf u,\mathbf w\in D$, $\mathbf p(\mathbf w)-\mathbf p(\mathbf u)=\lambda(\mathbf u,\mathbf w)(\mathbf w-\mathbf u)$ for some scalar $\lambda$ (take $\boldsymbol\omega=(\mathbf w-\mathbf u)/|\mathbf w-\mathbf u|$, $s=|\mathbf w-\mathbf u|$). Take non-collinear $\mathbf u,\mathbf v,\mathbf w$ and add increments around the triangle:

$$
\lambda_{uw}(\mathbf w-\mathbf u)
=\lambda_{vw}(\mathbf w-\mathbf v)+\lambda_{uv}(\mathbf v-\mathbf u).
$$

Plainly: the jump along the long side must equal the sum of the jumps along the two short sides.

Since $\mathbf w-\mathbf v$ and $\mathbf v-\mathbf u$ are linearly independent and $\mathbf w-\mathbf u$ is their sum, coefficient matching gives $\lambda_{uw}=\lambda_{vw}=\lambda_{uv}$. Hence $\lambda$ takes one value $a$ on all non-collinear pairs; collinear pairs are limits through non-collinear chains in the open connected $D$ (choose an intermediate off-axis point). So $\mathbf p(\mathbf w)-\mathbf p(\mathbf u)=a(\mathbf w-\mathbf u)$, i.e. $\mathbf p(\mathbf x)=a\mathbf x+\mathbf b$. (I7) or equivariance gives $\mathbf b=\mathbf 0$. $\square$

**Scorecard for "does $P(s)=as$ follow from …":**

| Source | Forces linearity? |
|---|---|
| instantaneous same-point balance | **No** — constrains nothing |
| later radial free propagation alone | **No** — forces per-sector $\boldsymbol\pi\parallel\boldsymbol\omega$ only; needs no-cross-sector + no-intrinsic-account to reach transfer radiality, then richness for linearity |
| emission-center booking + event-wise closure + rich set + (I7) | **Yes** — Theorem Q2 |
| closure only on the measure-zero set of increments actually realized by one trajectory | **No** — richness fails; nonlinear $\mathbf p$ survives |

**Verdict:** linear $\mathbf p=a\mathbf V$ is a **derived consequence of a booking axiom the packet does not declare**. The failure mode named in the prompt — conflating emission center with reception point — is exactly the difference between the trivially closed and the rigidly constrained books.

---

#### Q3 — Atoms are not intrinsically invisible; visibility is a property of the observation operator

Let $\Sigma$ = emission label × direction, $\mu$ finite Radon on $\Sigma$, with Lebesgue decomposition w.r.t. $\lambda$ (I3):

$$
\mu=\mu_{\mathrm{ac}}+\mu_{\mathrm{sc}}+\mu_{\mathrm{at}},
\qquad
\mu_{\mathrm{at}}=\sum_j m_j\,\delta_{p_j}.
$$

Plainly: any stored account splits into a smooth part, a singular-but-diffuse part, and a countable list of point masses.

**Class 1: trace on the exact later root** $h\in\Sigma$: $\mathcal O_{\mathsf h}[\mu]=\mu(\{h\})$. Then $\mathcal O_{\mathsf h}[\mu]=m_j$ if $h=p_j$, else $0$ — the operator sees **only** atoms. Whether an atom affects later acceleration reduces to whether the later root coincides with an atom location. Any "almost never" claim requires a measure on the set of realized later roots; the dynamics is deterministic and no admissible-history or branch measure is supplied. Worse, coincidence is not exceptional here: in the self-hit geometry, the later root of a self-emitted wake **is** the earlier emission label, so root–atom coincidence is produced by the dynamics itself, not by fine-tuning. **Atoms visible; no a.e. dismissal available.**

**Class 2: finite-width kernel**, $\mathcal O[\mu]=\int_\Sigma K_\rho(d(h,\cdot))\,d\mu$ with (I4) $\int K_\rho\,d\lambda=1$, so $\sup K_\rho\sim\rho^{-k}$, $k=\dim\Sigma$. An atom of mass $m_j$ within distance $\lesssim\rho$ of $h$ contributes $\sim m_j\rho^{-k}$:

$$
\mathcal O[\mu]\;\ge\;m_j\,K_\rho(d(h,p_j))\;\xrightarrow[\rho\to0]{}\;\infty
\quad\text{if }h=p_j .
$$

Plainly: a finite-width reader not only sees a point mass — as its resolution sharpens, the point mass dominates and diverges.

**Atoms visible, divergently so.** This is the mechanism behind the fail-closed disposition: kernel observation plus atomic updates plus root coincidence destroys bounded acceleration.

**Class 3: AC-factoring functional**, $\mathcal O[\mu]=\int\phi\,\frac{d\mu_{\mathrm{ac}}}{d\lambda}\,d\lambda$. Atoms are inert **by construction** — the operator is defined to discard singular support. Two costs: (i
```

Plainly: Appendix C preserves the supplied artifact exactly. It ends before
the AC-factoring costs, Q4–Q6, and required response sections 4–8.

## Appendix D — Final continuation through section 8

- Source artifact:
  `/Users/markmorris/.codex/attachments/c59cfc03-2663-4d3e-810e-d098f89d4279/pasted-text.txt`
- Source size: 22,502 bytes; 208 lines
- Completeness: completes Q3–Q6 and the substantive response; omits a separately numbered rejected-paths section and labels missing context as section 8
- Attribution and authority: same source-only boundary as Appendices A–C

```text
Continuing exactly from the truncation point (Q3, Class 3), and running through the end of section 8 without restating the already-delivered material.

---

#### Q3 (continued) — Class 3 and the trilemma

**Class 3: AC-factoring functional**, $\mathcal O[\mu]=\int_\Sigma\phi\,\frac{d\mu_{\mathrm{ac}}}{d\lambda}\,d\lambda$. Atoms are inert **by construction** — the operator is defined to discard singular support. Two costs: (i) inertness here is a modeling axiom, not a dynamical theorem — the atoms still sit in the ledger, and any later change of observation operator resurrects them; (ii) if the reception map's *updates* are atomic (as the finite-patch limit of Q4 says they are), then under Class 3 **all** reception-generated account content is invisible forever, i.e. the wake account decouples from the dynamics entirely, and the account becomes pure bookkeeping with no feedback.

Plainly: you can define the reader so it skips point masses, but then either the point masses never mattered (the account is dead weight) or someone must explain why the reader has exactly that blind spot.

**Theorem Q3 (inertness is equivalent to a choice, not derivable).** Let updates be atomic ($\Delta\mu=m\,\delta_p$, per the Q4 limit) and let $\mathcal O$ be any bounded linear observation functional on finite Radon measures. Then:

1. If $\mathcal O$ is continuous w.r.t. total variation and does not annihilate $\delta_p$ for the realized $p$, the atom alters $\mathcal O[\mu]$ by exactly $m\,\mathcal O[\delta_p]\ne0$: **visible**.
2. $\mathcal O$ annihilates every atomic update iff $\mathcal O$ factors through $\mu\mapsto\mu_{\mathrm{ac}}+\mu_{\mathrm{sc}}$; annihilating all singular parts is precisely the Class 3 axiom.

*Proof.* (1) is linearity. (2): $\mathcal O[\delta_p]=0$ for all $p$ in the support of realizable updates kills the atomic part by countable additivity and TV-continuity; conversely factoring through the nonatomic part annihilates atoms trivially. $\square$

Plainly: there is no middle ground — either the reader responds to point masses (then they are dynamical), or the reader is axiomatically blind to them (then their invisibility is stipulated, not proved).

**"Almost everywhere" audit.** Every a.e./"generic" claim needs a declared measure on one of: (a) admissible retained histories, (b) realized later roots, (c) branch selections. The packet supplies none. Moreover the self-hit geometry makes root–atom coincidence *structural* (the later root of a self-emitted sector is that sector's own label), so even a reasonable-looking history measure would not make coincidence null. **No a.e. dismissal is available.**

**Trilemma restated correctly.** The original trilemma was: (T1) atomic updates + trace/kernel observation ⇒ unbounded or ill-defined later acceleration; (T2) regularize updates into AC densities ⇒ requires a finite-patch law, but Q4 shows the $\varepsilon\to0$ limit is an atom, so a *fixed* $\varepsilon>0$ becomes a new physical constant needing declaration; (T3) AC-factoring observation ⇒ atoms inert by axiom, and reception-generated content decouples.

With the correct measure-theoretic hypotheses, (T1) is not an option one can hold — it is a **contradiction** with bounded dynamics whenever coincidence occurs (and it does, structurally). So the trilemma **reduces to a genuine dilemma**:

- **Horn A:** declare a fixed finite receiver-patch scale $\varepsilon_0>0$ (a new constitutive constant) so updates are AC;
- **Horn B:** declare the AC-factoring observation axiom and accept that atomic account content is nondynamical bookkeeping.

Plainly: the mathematics does not pick between a smallest reception size and a blind reader; one of the two must be declared as a modeling axiom, and neither is currently in the packet.

**Verdict on the previous claim:** "atoms are dynamically invisible" is **withdrawn**. Visibility is decided by the observation operator; two of three natural classes make atoms visible, one makes them invisible by fiat.

---

#### Q4 — Finite-patch scaling with regulators kept distinct

Setup: shell radius $r$ at reception, physical transverse patch radius $\varepsilon$ with $\varepsilon/r\ll1$, wake-thickness regulator $\eta$ (radial support width), account $\Delta Q$ spread uniformly over the intercepted patch.

**Intercepted solid angle.** A disc of physical radius $\varepsilon$ transverse to $\boldsymbol\omega$ at distance $r$ subtends the polar angle $\theta_\varepsilon=\arcsin(\varepsilon/r)$, so

$$
|\Omega_\varepsilon|
=2\pi\bigl(1-\cos\theta_\varepsilon\bigr)
=\pi\frac{\varepsilon^2}{r^2}\Bigl(1+\tfrac{1}{4}\frac{\varepsilon^2}{r^2}+O(\varepsilon^4/r^4)\Bigr).
$$

Plainly: a small coin held at distance $r$ covers a patch of directions whose size is (coin area)/(sphere-radius squared), i.e. $\pi\varepsilon^2/r^2$, with a computable small correction; the coefficient is $\pi$, not $4\pi$ and not $1$.

**Density with respect to $d\Omega$.** Uniform distribution of $\Delta Q$ over $\Omega_\varepsilon$:

$$
\frac{d(\Delta\mu)}{d\Omega}
=\frac{\Delta Q}{|\Omega_\varepsilon|}\,\mathbf 1_{\Omega_\varepsilon}
=\frac{\Delta Q\,r^2}{\pi\,\varepsilon^2}\,\mathbf 1_{\Omega_\varepsilon}\,\bigl(1+O(\varepsilon^2/r^2)\bigr).
$$

Plainly: the same finite account crammed into an ever-smaller directional window has a height that blows up like $r^2/\varepsilon^2$, with the exact prefactor $1/\pi$.

**Answers to the six determinations.**

1. *Scaling:* yes, $\sim r^2/\varepsilon^2$, with geometric coefficient $1/\pi$ (a square patch of side $2\varepsilon$ gives coefficient $1/4$ instead — coefficient is shape-dependent, scaling is not).
2. *Does $\eta$ enter?* **No.** The angular density is a distribution over direction on a single shell; $\eta$ regulates radial/temporal support (which shell content overlaps the receiver in time), an independent direction in the product $\Sigma=\text{labels}\times S^2$. It can only enter the angular law through an explicitly declared convolution coupling radial width to angular spread; none is supplied. Giving $\varepsilon$ and $\eta$ one symbol was a category error, now corrected.
3. *Weak-\* limit.* For any $f\in C(S^2)$,

$$
\int_{S^2} f\,\frac{d(\Delta\mu_\varepsilon)}{d\Omega}\,d\Omega
=\frac{\Delta Q}{|\Omega_\varepsilon|}\int_{\Omega_\varepsilon}f\,d\Omega
\;\xrightarrow[\varepsilon\to0]{}\;
\Delta Q\,f(\boldsymbol\omega_{\mathsf h}),
$$

Plainly: averaging a continuous function over a shrinking cap just evaluates it at the cap's center, so the limit object is the point mass $\Delta Q\,\delta_{\boldsymbol\omega_{\mathsf h}}$.

4. *Finite Radon atom:* yes — total mass $\Delta Q$ is $\varepsilon$-independent, so the limit is the finite atom $\Delta Q\,\delta_{\boldsymbol\omega_{\mathsf h}}$, not an infinite or ill-defined object. (The *density* diverges; the *measure* does not.)
5. *Shape and coupled limits:* any uniformly-shrinking patch family (bounded eccentricity, diameter $\to0$, containing $\boldsymbol\omega_{\mathsf h}$) gives the same weak-\* atom, by the same mean-value argument. Coupled limits $\eta(\varepsilon)\to0$ change nothing in the angular marginal because $\eta$ never entered it; they can matter only for the radial/temporal marginal, which is a separate ledger entry.
6. *Regulator-path independence* would be established by: (a) proving the joint family $\Delta\mu_{\varepsilon,\eta}$ converges weak-\* on the product $\Sigma$ along every path $(\varepsilon,\eta)\to(0,0)$ to the same product atom, and (b) exhibiting the declared convolution structure (if any) under which the two regulators commute. Without a declared joint law, only the marginal statements above are derivable.

**Consequence for Q3:** the natural $\varepsilon\to0$ update is atomic; this is what makes Horn A (fixed $\varepsilon_0$) vs Horn B (AC-factoring reader) unavoidable.

---

#### Q5 — Local well-posedness through coincident birth: theorem and counterexample

**Theorem Q5 (sufficient local theorem, birth chart).** Let $\mathcal S$ be the present-state space: a finite-dimensional chart (I6) $S=(\mathbf X,\mathbf V,\text{account variables})$ augmented by the retained-history segment $\mathcal H^T\in\mathcal C:=C^0([T-\tau_w,T];\mathbb R^d)$ with norm (I5). Suppose on $(0,T_1]\times B(S_0,\rho_0)$:

- (W1) $T\mapsto A(T,S)$ is Lebesgue-measurable for each fixed $S$;
- (W2) $\|A(T,S)\|\le g(T)$ with $g\in L^1(0,T_1)$ (e.g. $g(T)=C\,T^{\delta-1}$, $\delta>0$);
- (W3) $\|A(T,S)-A(T,S')\|\le \ell(T)\,\omega(\|S-S'\|)$ with $\ell\in L^1(0,T_1)$ and $\omega$ an Osgood modulus: $\omega$ continuous, increasing, $\omega(0)=0$, $\int_{0^+}\frac{ds}{\omega(s)}=\infty$;
- (W4) causality: $A(T,\cdot)$ depends on $\mathcal H^T$ only through its restriction to $[T-\tau_w,T]$, and the history update is the shift/append map (automatically nonexpansive in $C^0$);
- (W5) event structure: reception events in $(0,T_1]$ are finitely many or accumulate only at $T=0$ with $\sum_k(\text{impulse}_k)<\infty$ absolutely, and between events $A$ satisfies (W1)–(W3);
- (W6) branch selection at $D_t=0$: at birth the root ledger declares the one-sided branch (outgoing root retained, $D_t>0^+$), so $A$ is single-valued on $(0,T_1]$;
- (W7) re-entry: there exists $T_*\in(0,T_1]$ such that on $[T_*,T_1]$ the solution lies in a regular chart where (W3) holds with $\omega(s)=s$ (Lipschitz).

Then: there exists a unique solution $S\in W^{1,1}((0,T_1];\mathcal S)$ with $S(0^+)=S_0$, and the solution map $(S_0,\mathcal H^0)\mapsto S$ is continuous from $(\mathcal S,\|\cdot\|)\times(\mathcal C, C^0)$ into $C^0([0,T_1];\mathcal S)$.

Plainly: you need four separate things — a time-integrable size bound to build a solution, a time-integrable *state-sensitivity* bound with an Osgood modulus to make it unique, a declared branch choice at the birth instant so the equation is even single-valued, and a guarantee that events do not pile up anywhere except possibly at birth with summable total kick.

*Proof sketch.* (W1)–(W2) give a Carathéodory/Tonelli solution via Schauder or Peano-type compactness in the finite-dimensional chart (this is where (I6) is used; in an infinite-dimensional state space (W2) alone gives nothing and (W3)-Lipschitz is needed for Picard). Uniqueness and continuous dependence: for two solutions, $\phi(T)=\|S_1-S_2\|(T)$ satisfies $\phi(T)\le\phi(0)+\int_0^T\ell\,\omega(\phi)$; Osgood's lemma (Bihari inequality with $\int_{0^+}ds/\omega=\infty$) forces $\phi\equiv0$ when $\phi(0)=0$ and gives the modulus of continuous dependence otherwise. (W4)–(W6) ensure the composite flow across events is a finite (or absolutely summable) concatenation of such intervals; (W7) hands off to standard Picard theory away from birth. $\square$

**Audit of the $T^{\delta-1}$ claim.** The envelope $g(T)=CT^{\delta-1}$, $\delta>0$, is in $L^1(0,T_1)$, so unboundedness at $T=0$ is *harmless for existence* — the earlier alarm was misplaced. But integrability of $g$ says nothing about uniqueness: uniqueness is governed by (W3). If the state-sensitivity coefficient also scales as $\ell(T)=CT^{\delta-1}\in L^1$ with a Lipschitz (or Osgood) modulus, then **uniqueness holds** by the theorem — the singular time weight is absorbed by the change of variable $\sigma=\int_0^T\ell$.

Plainly: a spike in *how big* the force is at birth costs nothing if its time-integral is finite; only a failure of *how differently the force treats nearby states* can split solutions.

**Counterexample (finite impulse, nonunique continuation).** Scalar, $c_f=1$ irrelevant here:

$$
\dot S=A(T,S)=T^{-1/2}\,\mathrm{sgn}(S)\sqrt{|S|},
\qquad S(0)=0.
$$

Plainly: the time factor is integrable ($\int_0^1 T^{-1/2}dT=2$, finite impulse), but the state dependence has a square-root cusp at zero — infinitely steep sensitivity — which is exactly a failed Osgood condition... no wait, $\omega(s)=\sqrt s$ has $\int_{0^+}ds/\sqrt s<\infty$, i.e. it **violates** the Osgood divergence requirement.

Solutions: $S\equiv0$, and for every anchor $a\ge0$ the family

$$
S_a(T)=\Bigl(\max\bigl(0,\sqrt T-\sqrt a\bigr)\Bigr)^{2},
$$

(check: $\dot S_a=2(\sqrt T-\sqrt a)\cdot\frac{1}{2\sqrt T}=T^{-1/2}(\sqrt T-\sqrt a)=T^{-1/2}\sqrt{S_a}$ for $T>a$). A continuum of distinct solutions leaves $S=0$, all with the same finite impulse budget.

Plainly: the total kick is finite on every branch, yet the system can sit at zero for any waiting time $a$ and then depart — finite impulse does not buy uniqueness; only a state modulus does.

This is precisely the corrected adjudication point 4: an $L^1$-in-time envelope establishes neither Carathéodory uniqueness nor continuous dependence; and conversely (the audit above) it does not *threaten* them either. The uniqueness question for the birth chart is **open pending (W3)-type control of the actual reception map**, which the packet does not supply.

---

#### Q6 — No photon Gate B consequence follows now

The strongest result surviving Q1–Q2 is: *conditional on emission-center booking with event-wise angular closure over a rich increment set (plus (I7)), each wake sector carries momentum parallel to its own $\boldsymbol\omega$, hence zero orbital angular account about its own emission center, and $\mathbf p(\mathbf V)=a\mathbf V$.*

Four distinct objects, in decreasing degree of support:

1. *Zero orbital angular account of a radial sector about its own emission center:* $\mathbf Y\times\boldsymbol\pi=(\mathbf C+R\boldsymbol\omega)\times\pi\boldsymbol\omega=\mathbf C\times\pi\boldsymbol\omega$ — zero only if referred to the emission center itself. Conditional-derived (on the booking axiom).
2. *Intrinsic wake angular information:* explicitly **excluded by assumption** (no-intrinsic-account) in the only argument that used it; nothing is derived about it.
3. *Orbital angular information in an assembly:* sums of sector accounts over many emission centers with different $\mathbf C$'s and $\boldsymbol\omega$'s are generically nonzero even when every sector is radial. So sector radiality does **not** deplete assembly-level angular structure. No constraint follows.
4. *Photon Gate B (helicity/polarization recovery):* Gate B concerns retained twelve-worldline assembly structure. Point 3 shows radial sectors leave assembly angular content unconstrained, and points 1–2 rest on an undeclared booking axiom and an undeclared no-intrinsic-account axiom.

**Narrowest valid statement (a guard, not a corollary):** *No result from this review restricts, enables, or prefers any Gate B mechanism. In particular, "wake sectors carry no angular account" must not be cited for or against helicity recovery: it is conditional, center-relative, and silent about assemblies.* Any Gate B cross-reference from this packet would be a promotion of an unaccepted account representation and is rejected.

---

### 4. Strongest theorem or no-go

**Statement (Rigidity under emission-center booking).** Let receptions produce velocity jumps $\mathbf V\mapsto\mathbf V+s\boldsymbol\omega$ and momentum-account jumps $\mathbf p(\mathbf V+s\boldsymbol\omega)-\mathbf p(\mathbf V)$. If the angular account closes event-wise with the debit booked at the emission center, over a rich set of increments whose endpoint pairs fill an open connected domain, then every transfer is radial and $\mathbf p(\mathbf V)=a\mathbf V+\mathbf b$; with $\mathbf p(\mathbf 0)=\mathbf 0$ or $SO(3)$-equivariance, $\mathbf p(\mathbf V)=a\mathbf V$. No continuity of $\mathbf p$ is assumed.

**Assumptions:** (P3)–(P5); emission-center booking axiom (undeclared in packet — introduced); richness (I2); normalization (I7) or equivariance.

**Claim grade:** derived (pure mathematics from the stated hypotheses).

**Scope:** account bookkeeping only; not physical conservation, not realization, not a retained-branch or solver statement; silent on same-point booking, which is shown to constrain nothing.

**Operator-checkable falsifier:** exhibit $(\mathbf u,\mathbf v,\mathbf w)$ non-collinear in the declared rich domain and a map $\mathbf p$ satisfying the event-wise closure condition on all pairs among them with two distinct proportionality scalars — the triangle identity in the proof makes this impossible, so any such exhibit refutes the theorem (or shows the domain was not rich).

### 5. Corrections to the previous response

1. **Withdraw as derived:** "$\Delta\mathbf V_r\parallel\boldsymbol\omega$ implies $\boldsymbol\pi=\pi\boldsymbol\omega$." Correct status: conditional on (H1) sector blindness or on the emission-center booking axiom; the transverse-velocity counterexample (Q1) is equivariant, local, causal, and non-radial.
2. **Withdraw:** "Dirac atoms in the wake account are dynamically invisible." Correct status: visibility is decided by the observation operator (Theorem Q3); trace and kernel classes see atoms, the kernel class divergently; only the AC-factoring axiom makes them inert, by construction.
3. **Narrow:** any bare "$\varepsilon^{-2}$" patch-scaling statement — the density is $\Delta Q\,r^2/(\pi\varepsilon^2)$, $\eta$ does not enter the angular law, and the measure-level limit is the finite atom $\Delta Q\,\delta_{\boldsymbol\omega_{\mathsf h}}$, shape-independently.
4. **Narrow:** any suggestion that the $T^{\delta-1}$ envelope endangers well-posedness, and equally any suggestion that its integrability secures it. Both directions are wrong; uniqueness is decided by (W3) alone (Theorem Q5 + counterexample).
5. **Retain, re-ground:** the fail-closed disposition — now justified by two missing axioms (observation operator; booking convention) plus the open (W3) modulus, not by items 1–2.

### 6. Safe proposed amendments

**Priority-only corrections** (program-state notes, no reader-facing text):

- Record that the wake-account lane is blocked on two undeclared modeling axioms: observation-operator class (Horn A vs Horn B of the Q3 dilemma) and booking convention (same-point vs emission-center).
- Record that the birth-chart uniqueness obligation is specifically a (W3) state-modulus obligation on the reception map, not an impulse-size obligation.

**Reader-facing mathematical corrections** (where the corpus states these claims):

- Replace any radiality-of-$\boldsymbol\pi$ assertion with the conditional form (H1)/booking-axiom form.
- Replace any $\varepsilon^{-2}$ slogan with $\Delta Q\,r^2/(\pi\varepsilon^2)$ and the weak-\* atom statement; keep $\varepsilon$, $\eta$, and any emission-time mollifier as distinct symbols.

**Deferred theory decisions** (not mine to make from this lens):

- Choice between dilemma horns A and B (fixed $\varepsilon_0$ constitutive constant vs AC-factoring observation axiom).
- Choice of booking convention, and whether no-cross-sector / no-intrinsic-account are adopted as axioms.

**Rejected promotions:**

- Any Gate B cross-reference from this packet (Q6 guard).
- Treating the rigidity theorem's $\mathbf p=a\mathbf V$ as a derived physical momentum or mass statement — it is account bookkeeping under an undeclared axiom, and architrinos have no primitive mass.

### 7. Decisive next calculation

**Selected representation:** emission-center booking with atomic sector accounts (Horn-A deferred; the calculation is representation-diagnostic, not representation-committing).

- **State variables:** one transmitter–receiver pair; receiver state $(\mathbf X_r,\mathbf V_r)$; per-sector ledger $(T_e,\boldsymbol\omega,\pi)$ for each received sector; $c_f=1$.
- **Observation map:** Class 1 trace (exact-root evaluation), chosen deliberately because it is the worst case for atoms.
- **Inputs:** a prescribed two-worldline history with one self-hit root coincidence event (guaranteed root–atom coincidence, per Q3), finite retained window, declared root ledger.
- **Method:** propagate the sector ledger under (P5); at the coincidence event evaluate the later acceleration under (a) Class 1 and (b) Class 3 observation; compute the angular residual under (i) same-point and (ii) emission-center booking for the same finite increment.
- **Pass condition:** the four outcomes (a-i, a-ii, b-i, b-ii) are numerically distinguishable at declared tolerance — confirming that observation class and booking convention are *dynamically inequivalent*, which forces the two axioms onto the declaration ledger.
- **Fail condition:** all four outcomes agree within tolerance across a refinement sweep — which would show the axioms are dynamically degenerate for this class of histories and the dilemma is weaker than claimed.
- **Exact claim changed:** upgrades "the packet is missing two axioms" from a formal observation (derived, this review) to a demonstrated dynamical bifurcation (measured, with instrument and tolerance), or else narrows my Q3 dilemma claim.

### 8. Missing context

The attached file does not determine, and I do not guess:

1. The observation operator $\mathcal O_{\mathsf h}$ through which later acceleration reads the wake account — the single largest gap.
2. The booking convention for angular accounts (same-point vs emission-center), and whether no-cross-sector and no-intrinsic-account are axioms.
3. Any measure on admissible histories, realized roots, or branch selections (required for any "generic"/a.e. claim).
4. The state-sensitivity modulus (W3) of the actual reception map near birth — the impulse envelope is supplied; the modulus is not.
5. Whether a fixed receiver-patch scale $\varepsilon_0>0$ exists as a constitutive constant (Horn A) or is a pure regulator.
6. Any declared convolution coupling $\eta$ to the angular law (absent one, $\eta$ is provably irrelevant to the angular density).
7. The normalization $\mathbf p(\mathbf 0)=\mathbf 0$ (I7) — used only to kill the affine constant in the rigidity theorem.
8. Whether the wake-account representation under review has any standing in the owning corpus — this review is a mathematics-of-the-packet exercise and confers none.

Closure goal: the four adjudication points are resolved — radiality is conditional (counterexample supplied), atom visibility is an observation-operator property (dilemma, not trilemma), the patch law is $\Delta Q\,r^2/(\pi\varepsilon^2)\to\Delta Q\,\delta_{\boldsymbol\omega_{\mathsf h}}$ with $\eta$ irrelevant, and birth uniqueness hinges on the undetermined state modulus (finite-impulse nonuniqueness counterexample supplied); next action is the four-way observation×booking bifurcation calculation in section 7.
```

Plainly: Appendix D preserves the final continuation exactly. Its mathematical
claims are accepted, narrowed, or rejected in Stage 7 rather than treated as
authority.
