
Best-in-class markdown drafting pipeline rather than a generic one.

• Best strategy is a hybrid, not full baton and not full-group free-for-all.

  1. With codex write draft v1 of doc.md to textbook quality baseline (definitions, regimes, equations, observables, $\mathbb{A}\mathbb{A}\mathbb{A}$ mapping goals, falsifiable checks).
  2. Focused review round (single pass per selected subset of council members ):
  3. Single integrator pass by one lead writer (codex) to merge comments into one coherent voice.

  Avoid:

  - Full council multi-rounds from the start (too much style drift, low signal).
  - Person-to-person baton rewrites (fragmented logic and inconsistent notation).

 