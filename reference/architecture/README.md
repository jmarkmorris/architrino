# Architecture

This directory is for durable architecture reference material.

Use it for subsystem boundaries, contracts, invariants, and architectural guidance intended for developers rather than the app runtime.
